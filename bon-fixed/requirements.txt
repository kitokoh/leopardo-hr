# BON — Facebook Groups Publisher
# Dépendances (v4.1 - Moderne & Optimisé)

# Moteur navigateur moderne (remplace Selenium)
playwright==1.49.0

# Requêtes HTTP (CDN sélecteurs, check_license réseau futur)
requests==2.32.3

# Utilitaires
python-dotenv==1.0.1
emoji==2.14.0

# Tests
pytest>=8.0.0

# Optionnel : infos réseau (MACs multiples dans check_license)
# psutil>=5.9.0

# Notes :
# - Selenium a été supprimé (obsolète, lent, détectable)
# - Playwright est plus rapide, moderne et moins détectable
# - Pour installer : pip install -r requirements.txt && playwright install chromium

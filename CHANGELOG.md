# Changelog

Toutes les évolutions notables de ce projet sont documentées dans ce fichier.

Le format s'appuie sur [Keep a Changelog](https://keepachangelog.com/fr/1.1.0/)
et le projet vise l'adhésion au [Semantic Versioning](https://semver.org/lang/fr/).

> **Note — journal historique.** Ce fichier était auparavant un journal d'exécution
> détaillé des agents (385 Ko). Le détail complet de chaque livraison reste disponible
> dans l'historique git de ce fichier, notamment jusqu'au commit
> `0bba16974c1b032a9b9bf19bd4b1db7e9bbc6da2`
> (`git log -p 0bba16974c1b032a9b9bf19bd4b1db7e9bbc6da2 -- CHANGELOG.md`).

## [Unreleased]

### Added
- **chore(qualite): #7655 tranche 2 — lot B exécuté : campagne d'annotations `@property` sur les 38 modèles Eloquent TravelAgency, baselines PHPStan élaguées de 17 019 → 11 090 lignes (−35 %, occurrences gelées 4 991 → 2 558, −49 % au compteur canonique du cliquet) (Refs #7655).** Première tranche de RÉSORPTION réelle (la tranche 1 avait livré carte + cliquet sans toucher au code) : le plus gros bloc homogène de la dette était `property.notFound` sur `app/Modules/TravelAgency` (669 occ dans la baseline modules, module le plus endetté hors tests). **(1) 284 annotations `@property`** dérivées des **migrations tenant** (création gagnante = la plus ancienne, guard `schemaTableExists` ; colonnes ajoutées/relâchées par la consolidation #7452 incluses) puis surchargées par les `$casts` (enums du domaine, `Carbon`, entiers) — les Resources API portaient déjà `@mixin`, elles guérissent par ricochet. Trois propriétés volontairement NON annotées car les colonnes n'existent pas en base (bugs latents documentés, lot A) : `TravelAdvert::$payment_id`, `TravelAdvert::$published_at`, `TravelLoyaltyAccount::$contact_id`. **(2) Code mort révélé par le typage** : garde `instanceof Carbon` toujours vrai supprimé dans `TravelCancellationPolicyService` (seul écart signalé par PHPStan après annotation). **(3) Baselines RÉGÉNÉRÉES par `--generate-baseline` sous run complet vert** (les entrées résorbées ET les entrées périmées tombent) : `phpstan-modules-baseline.neon` 3 182 → **38 lignes** (530 → 6 entrées — il ne reste que 6 erreurs réelles hors TravelAgency), `phpstan-strict-baseline.neon` 9 963 → **7 178 lignes** (1 660 → 1 196 entrées, 3 267 → 1 512 occurrences au compteur du cliquet), **0 entrée ajoutée, 0 `count:` augmenté** ; `phpstan-baseline.neon` (legacy level max) volontairement inchangée — aucun check CI n'exécute ce scope en entier, une régénération n'y serait pas prouvable. Le niveau PHPStan global est INCHANGÉ (aucune config touchée). Cartographie mise à jour (`docs/qualite/DETTE_PHPSTAN_7655.md` §8, trajectoire chiffrée). **Vérifié localement (PHP 8.4 + composer + PHPStan rootless via `bootstrap-local-php.sh` #7585)** : `php -l` OK sur les 39 fichiers touchés ; **run COMPLET `phpstan-strict.neon` (level 8) `[OK] No errors`** ; **run complet `phpstan-modules.neon` `[OK]`** ; gate diff-scopée de `tests.yml` reproduite (level 8 + 3 baselines) verte sur le module, les 110 fichiers de tests consommant TravelAgency et les 14 fichiers legacy hors module (identique avant/après) ; cliquets `check-phpstan-baseline-delta.sh` + `check-phpstan-baseline-debt.sh guard` rejoués verts. Restent dans #7655 : lots A/C/D/E/F et les points RBAC + Sanctum.
- **fix(front): fin du fallback silencieux vers l'API Render de DEV dans les 4 fronts (Closes #7842, audit externe 2026-09-20).** Un build de **production** sans variable d'environnement pointait silencieusement `front/web`, `front/travel-web` (`backend-url.ts`), `front/web/src/lib/csp.ts` (`DEFAULT_API_ORIGIN` du connect-src) et `front/admin-dashboard` (`api.js`) vers `gestionemployerbackend.onrender.com` — risque de fuite de données réelles vers l'environnement de dev et incident indétectable. Désormais : **(1)** en production (`NODE_ENV=production` / `import.meta.env.PROD`), variable manquante → **throw explicite** avec message actionnable (variable à poser + environnement Vercel/Render/Cloudflare visé) ; une `NEXT_PUBLIC_API_URL` non parsable lève aussi en prod au lieu de retomber sur l'origine de dev dans la CSP ; **(2)** en dev/test, le fallback est conservé mais accompagné d'un `console.warn` explicite ; **(3)** factorisation : `front/web/src/lib/backend-url.ts` et `front/travel-web/src/lib/backend-url.ts` sont désormais STRICTEMENT identiques (fichiers miroirs documentés — la résolution `API_PROXY_TARGET > BACKEND_API_URL > NEXT_PUBLIC_API_URL` et la politique prod/dev vivent dans une seule fonction partagée par les deux copies). Nouvelle suite `front/web/src/lib/__tests__/backend-url.test.ts` (5 cas : chaîne de précédence, fallback dev avec warning, throw prod serveur, throw prod client, prod configurée sans warning). **Vérifié localement** : `tsc --noEmit` 0, `eslint --max-warnings 0` OK, **Jest 142 suites / 1231 tests verts** (dont `proxy-csp.test.ts` inchangé — le comportement dev/test est préservé).
- **fix(bc21): réparation post-merge des deux gardes CI rouges laissées par le lot BC-21 (PR #7732, Refs #7726 #7727 #7728).** **(1) Parité `.env.example` (#1487)** : les 5 clés `env()` lues par `config/restaurantmanager.php` mais absentes de `api/.env.example` sont ajoutées (`RESTAURANT_CARD_ONLINE_SUCCESS_URL`, `RESTAURANT_CARD_ONLINE_CANCEL_URL`, `RESTAURANT_MOBILE_MONEY_PRODUCTION_ENABLED=false`, `RESTAURANT_MOBILE_MONEY_INITIATE_URL`, `RESTAURANT_MOBILE_MONEY_API_KEY`) — valeurs vides/désactivées par défaut, aucun secret. **(2) Couverture OpenAPI (#1473)** : les 9 routes du lot BC-21 sont documentées dans `api/openapi.yaml` — CRUD + activation `/billing/payment-profiles` (#7727, schéma `TenantPaymentProfile`, secrets masqués write-only), `GET/PUT /platform/billing/gateways` + `POST /platform/billing/gateways/{gateway}/test` (#7726, schémas `PlatformPaymentGateway`/`PaymentGatewayTestResult`, masque `sk_live_••••1234`, jamais de clé en clair), `GET /restaurant/payments/configuration` (#7728, indicateurs sans secret). **Vérifié localement** : `check-env-example-parity.sh` ✔ 381 clés / 0 manquante, `check-openapi-route-coverage.py --strict-staleness` exit 0, `redocly lint` « API description is valid », YAML parsé. Aucun changement de code PHP/TS — documentation et parité d'environnement uniquement.
- **feat(retail): handoff livraison BC-26 des commandes en ligne Leopardo Marché — création automatique de la livraison à la confirmation + suivi livreur sur la page publique (Closes #7811, spec `MARKETPLACE_RETAIL_PUBLIC.md` §6).** La confirmation vendeur (`pending → confirmed`, `RetailOnlineOrderService::confirm`) émet désormais l'événement `RetailOnlineOrderConfirmed` (APRÈS commit — jamais de livraison fantôme sur rollback ; classe sous `App\Shared\Events`, payload 100 % scalaires : AUCUN import croisé `Modules/Delivery → Modules/Retail`, règle d'isolation #5584, pattern documenté `Shared\Contracts\Crm`) ; le listener `Delivery\Application\Listeners\CreateRetailOnlineDelivery` crée la livraison BC-26 via `CreateDeliveryAction` — déjà **idempotente** sur (company_id, source, source_reference) : double confirm (422) ou rejeu brut de l'événement → UNE seule livraison. Nouvelle source `DeliverySource::RetailOnline = 'retail_online'` (`source_reference` = référence `WEB-…` de la commande, liste `in:` de `DeliveryStoreRequest` étendue — aucune contrainte CHECK en base à migrer) ; **COD v1 reporté** : `cod_amount_minor` = total serveur de la commande + nouvelle colonne `cod_currency` (ISO 4217, nullable) — migration tenant ALTER dédiée idempotente (`Schema::hasColumn`, SANS FK, miroir `CreatesMvpSchema`), règle « une table, une migration » respectée. Gardes fail-closed du listener : tenant courant = vendeur de l'événement ET feature flag `delivery` actif (vendeur sans module BC-26 → confirmation OK, aucune livraison), toute exception journalisée sans casser la confirmation déjà commitée. **Suivi public enrichi** : `GET /public/market/orders/{reference}` expose `delivery` (statut + horodatages publics UNIQUEMENT — DTO fail-closed `PublicDeliveryStatus`, jamais d'id/COD/données internes) via le nouveau port en LECTURE SEULE `App\Shared\Contracts\Delivery\PublicDeliveryStatusProvider` implémenté et bindé par le module Delivery (`EloquentPublicDeliveryStatusProvider`, requête toujours bornée au company_id) — Retail ne requête JAMAIS les tables Delivery ; la page `/suivi` de `front/marketplace` affiche l'encart « Suivi livreur » (FR, optionnel et défensif : parsing tolérant, statut inconnu → libellé générique, rien n'est affiché sans le champ). Nouvelle suite `RetailDeliveryHandoffTest` (6 cas : confirm → livraison `retail_online` avec COD/dropoff, idempotence double confirm + rejeu d'événement, annulation depuis pending → zéro livraison, vendeur sans feature `delivery` → zéro livraison, isolation tenant croisée, suivi public exposant le statut avec DTO fermé) + cas `retail_online` ajouté au provider de `DeliverySourceContractApiTest` et clé `delivery` actée dans le DTO fermé de `RetailOnlineOrderTest`. **Vérifié localement (PHP 8.4 + PostgreSQL 14)** : `php -l` OK ×17 fichiers, `pint --test` PASS, **PHPUnit vert — `RetailDeliveryHandoffTest` 6/6, `RetailOnlineOrderTest` + `DeliverySourceContractApiTest` 18 tests / 175 assertions**. **Non vérifié localement** : PHPStan strict (workers tués par la sandbox partagée — OOM/timeout même réduit aux fichiers touchés) et le build `front/marketplace` — à confirmer par la CI.
- **feat(marketplace): comptes acheteurs Leopardo Marché — inscription légère, favoris, avis vérifiés & notations publiques (Closes #7814, épic Leopardo Marché BC-17).** La marketplace publique (#7807/#7808) restait 100 % invitée : pas d'historique, pas de favoris, pas d'avis. Livré : **(1) comptes acheteurs PLATEFORME** — la marketplace étant cross-tenant, les acheteurs n'appartiennent à aucun vendeur : 4 nouvelles tables CENTRALES (schéma `public`, une table = une migration, sans FK, idempotentes) `marketplace_buyers` (email unique + mot de passe hashé + nom + téléphone optionnel), `marketplace_buyer_tokens` (**jeton opaque** `mkb_`+64 hex, hash SHA-256 en base, TTL 30 j — pas de Sanctum : ses personal access tokens vivent dans les schémas tenants), `marketplace_favorites`, `marketplace_reviews` ; routes `/api/v1/public/market/account/*` (register/login/logout/me) sous `throttle:shop-public` + bucket strict dédié `market-account-public` (10/min/IP anti-bruteforce), middleware `market.buyer` 401 fail-closed uniforme. **(2) Liaison commande↔compte** — `retail_orders.buyer_id` nullable (migration ALTER tenant idempotente, sans FK cross-schema) : un jeton buyer VALIDE au `POST /public/market/orders` lie la commande (jeton invalide → jamais bloquant, checkout invité inchangé) ; `GET /public/market/account/orders` = historique cross-tenant paginé (DTO public strict : `reference, seller{name,slug,city}, total_minor, currency, fulfillment_status, created_at, tracking_token, items[]` — jamais de company_id). **(3) Favoris** — POST/GET/DELETE `/public/market/account/favorites` : référence produit interne `(company_id, product_id)` résolue CÔTÉ SERVEUR (le client n'envoie que l'id public), liste rendue en DTO produits publics avec filtre fail-closed des produits dépubliés/vendeurs sortis de l'opt-in, ajout/retrait idempotents. **(4) Avis VÉRIFIÉS post-livraison** — `POST /public/market/account/reviews` (note 1..5 + commentaire ≤ 1000) accepté UNIQUEMENT si la commande du buyer contient le produit ET `fulfillment_status = delivered` (422 `ORDER_NOT_DELIVERED`/`PRODUCT_NOT_IN_ORDER`, 404 sur la commande d'un autre buyer) ; anti-abus : UNIQUE (buyer, commande, produit) + throttle strict `market-reviews-public` 5/min/IP (pattern RESTO-902) ; modération `pending|approved|rejected` (auto-approve v1, champ prêt pour la v2) ; `GET /public/market/products/{id}/reviews` (approuvés seuls, paginés, auteur en nom PUBLIC « Prénom I. » — jamais d'email) ; agrégats `rating_avg`/`rating_count` (calcul à la volée sur les avis approuvés) ajoutés aux DTO publics produit ET boutique. **(5) Front `front/marketplace`** — pages `/compte` (connexion/inscription à bascule, jeton en localStorage, hook `useBuyer` sur useSyncExternalStore), `/compte/commandes` (historique + statuts + lien suivi + **formulaire d'avis par article quand la commande est livrée**), `/compte/favoris` ; bouton cœur sur les cartes produit et la fiche ; note moyenne (étoiles accessibles) + section avis sur `/produits/[id]` ; le checkout envoie le jeton buyer s'il existe ; FR, mobile-first, états vides/erreur/chargement, a11y, zéro dépendance ajoutée. Spec `MARKETPLACE_RETAIL_PUBLIC.md` mise à jour (§3.2bis contrat comptes/favoris/avis, §6 : comptes acheteurs déplacés dans le livré). **Vérifié localement (PHP 8.4 + PostgreSQL 14)** : nouvelle suite `RetailMarketBuyerAccountTest` (6 tests / 132 assertions : register/login/me/logout + jeton révoqué/expiré, liaison commande-buyer + invité + jeton invalide, historique cross-tenant 2 vendeurs + isolation entre buyers, favoris CRUD + fail-closed + isolation, avis refusé avant `delivered` puis accepté après + doublon refusé + agrégats produit/boutique) et **`tests/Feature/Retail` 48/48 verts** (les 3 assertions de DTO fermé étendues à `rating_avg`/`rating_count`) ; `npx eslint src --max-warnings 0` + `tsc --noEmit` verts dans `front/marketplace` (après `npm ci`) ; gardes migrations (collisions basename + duplication `Schema::create --strict`), layer-purity, module-isolation et unrouted-controllers vertes ; **PHPStan strict `[OK] No errors`** (run complet mono-worker).
- **feat(retail): Leopardo Marché — marketplace grand public multi-vendeurs BC-17 phase 2 : API publique cross-tenant, checkout invité, web client dédié et espace vendeur (Closes #7807, #7808, #7809, #7810 — épic #7806, spec `docs/specifications/MARKETPLACE_RETAIL_PUBLIC.md`).** Les fondations vendeur (#7718) laissaient le canal `online` de `RetailOrderSource` réservé sans flux : le grand public ne pouvait pas acheter en ligne les produits des boutiques Retail. **(1) API publique** `/api/v1/public/market/*` (throttle `shop-public`, AUCUNE auth, pattern BC-27/BC-28) : recherche cross-tenant bornée aux vendeurs opt-in (nouvelle table `retail_online_settings`, `enabled` fail-closed, 1 ligne par company ; produits `published` ET `online_visible` — nouvelle colonne opt-in — uniquement ; agrégation `withoutGlobalScope` + `whereIn(company_id, éligibles)`, pattern marketplace travel #7737), fiches produit et annuaire boutiques en DTO strict (jamais de `company_id`/stock chiffré/marges — seul `available: bool`), filtres q/boutique/catégorie/prix/tri, pagination ≤ 50. **(2) Checkout invité** `POST /public/market/orders` : 1 commande = 1 vendeur, prix RELUS en base, totaux serveur en minor units, référence `WEB-…`, `tracking_token` 64 hex, idempotence par clé (rejeu → 200 même payload) ; suivi public `GET /public/market/orders/{reference}?token=` 404 fail-closed. **(3) Gestion vendeur** `/retail/online/*` (pile tenant + `module.retail`) : réglages boutique (create-or-update, `RetailOnlineSettingsPolicy`), `publish-online`/`unpublish-online` par produit, commandes web avec machine d'états `pending → confirmed → ready → shipped → delivered` (+ `cancelled`) — transitions invalides → 422 `INVALID_TRANSITION`, confirmation = décrément stock par `RetailStockService` (mouvements `sale`, seule voie d'écriture #7673), annulation post-confirmation = mouvements `return`. Migrations tenant idempotentes sans FK (miroirs `CreatesMvpSchema`, garde #5443), OpenAPI +14 opérations, 14 tests Feature (opt-in, isolation tenant, DTO fermés, idempotence, transitions avec stock, RBAC). **(4) Web client dédié `front/marketplace`** (nouvelle app Next.js App Router indépendante, TS strict, Tailwind 4, FR, direct-to-API `NEXT_PUBLIC_MARKET_API_BASE`) : accueil, catalogue (recherche/filtres/tri/pagination sans JS requis), fiches, boutiques, panier multi-vendeurs localStorage, checkout invité (1 POST par boutique, clés d'idempotence persistées, succès partiel géré), confirmation avec jetons, suivi à timeline — déployé en projet Vercel dédié `https://leopardo-marche.vercel.app` (#7815, `docs/ops/DEPLOYMENT_URLS.md`). **(5) Espace vendeur** `/commerce/boutique` dans `front/web` (patterns #7675) : réglages, bascule « En ligne » par produit, commandes web avec actions — machine d'états pure testée (+6 tests Jest), i18n ×4 locales synchronisée (`I18N_VALIDATION_OK`). **Vérifié localement** : `front/marketplace` build vert + ESLint 0 warning ; `front/web` ESLint 0, **Jest 135 suites / 1201 tests verts**, `check:mojibake` OK, build vert ; parité i18n ×4 verte ; OpenAPI route-coverage 0 drift, YAML valide. **Non vérifié localement (PHP/PostgreSQL absents de la sandbox)** : PHPStan strict, Pint et les 2 suites Feature Retail — à confirmer par la CI.
- **feat(restaurant): encaissement réel branché sur les profils de paiement du tenant — carte en ligne Stripe sur les clés DU restaurateur, mobile money production feature-flaggé, fail-closed propre côté commande publique (Closes #7728, BC-25×BC-21, dépend de #7727/PR #7732).** Le POS Restaurant enregistrait cash/carte de façon déclarative et le mobile money était sandbox uniquement ; aucune commande publique ne pouvait être encaissée RÉELLEMENT sur le compte du restaurateur. **(1) Contrat partagé étendu** (`TenantPaymentProfileResolverInterface`, garde d'isolation #5584 — jamais d'import direct de Modules/Billing) : `stripeCredentialsForCompany()` et `activeMobileMoneyProfileForCompany()` résolvent les profils ACTIFS d'une compagnie DONNÉE — nécessaire aux surfaces publiques (le tenant vient du lien signé, pas du contexte courant) ; requêtes hors scope global mais TOUJOURS filtrées par la compagnie désignée, table absente → null (fail-closed, jamais de 500). **(2) Nouveau gateway `card_online`** (`CardOnlinePaymentGateway`, registre RESTO-406) : Stripe Checkout Session REST (pattern Accounting #5272) sur les **clés PROPRES du tenant** (profil `stripe_keys` actif — AUCUN fallback plateforme sur la surface publique restaurant), métadonnées traçant company_id + profil, verify (payment_status) et refund (payment_intent) inclus ; enum `PaymentProvider` + `InitiatePaymentResult.checkoutUrl` étendus. **(3) Mobile money production** : hors sandbox (comportement historique inchangé par défaut), l'initiation exige le feature flag `restaurantmanager.mobile_money.production.enabled` + endpoint provider en env (PVIT/Orange Money) + **profil `mobile_money` ACTIF du tenant** (opérateur + numéro d'encaissement du restaurateur) — sinon fail-closed ; la confirmation reste portée par le callback HMAC RESTO-407, aucun secret en dur. **(4) Commande publique** (`RestaurantPublicOrderController::pay`) : provider par défaut dérivé de la configuration du restaurateur (carte en ligne prioritaire), réponse enrichie de `checkout_url`, providers sur place (cash/carte terminal) refusés sur la surface publique ; **sans profil actif → 422 `online_payment_not_configured`** (message utilisateur ×4 locales `errors.RESTAURANT_ONLINE_PAYMENT_NOT_CONFIGURED`) **+ `fallback: pay_on_site`** — jamais un 500 (renderer dédié `PaymentGatewayException` dans bootstrap/app.php, paiement avorté tracé `failed`, aucun pending fantôme) ; le menu public expose `payments.online_providers` + `pay_on_site` pour informer AVANT le paiement. Les paiements publics portent `pos_session_id` — les sessions de caisse agrègent les encaissements en ligne réels comme avant. **(5) Écran restaurateur** : `GET /restaurant/payments/configuration` (auth tenant, aucun secret — uniquement des indicateurs) + page front/web `/restaurant/payments` (tuile hub) : état carte en ligne / mobile money (sandbox vs production, profil actif) / paiement sur place, lien vers Réglages → Encaissements (#7727) ; i18n ×4 locales (`restaurant.payments.*`) propagée par les 3 syncs. **Vérifié localement (PHP 8.4 + PostgreSQL rootless dans la sandbox)** : nouvelle suite Feature `RestaurantPaymentRoutingTest` **8 tests verts** (checkout sur la clé Stripe DU TENANT + traçage du profil via Http::fake, défaut card_online avec profil actif, 422 propre + fallback sans profil — 3 chemins, aucun paiement fantôme, cash/card refusés en public, sandbox mobile money préservé, menu public, écran restaurateur configuré/non configuré) ; suites existantes vertes : `RestaurantPaymentGatewayTest` + `RestaurantPublicOrderTest` + `RestaurantKioskTest` (23 tests), `TenantPaymentProfileApiTest` (6), `AccountingOnlinePaymentTest` (15) ; échecs préexistants de l'environnement local (RestaurantPublicShopTest & co) reproduits à L'IDENTIQUE sans mes changements (stash) — hors périmètre ; `php -l` OK ×19, module-isolation 0 nouvel import, dépendances BC conformes (119 arêtes), unrouted-controllers OK ; front/web `tsc --noEmit` 0 + `eslint src --max-warnings=0` OK + Jest restaurant 26 tests verts. **PR empilée sur `bc/bc21-paiements-encaissement`** (le PM reciblera sur main après merge de #7732). Hors périmètre assumé : Stripe Connect (`stripe_account_id` réservé), webhook Stripe côté restaurant (la vérification passe par `verify` à la demande), surface `/restaurant/public/shop` (jeton boutique) qui garde son flux cash/mobile money historique.
- **feat(billing): lot BC-21 paiements — configuration des passerelles PSP depuis l'admin plateforme (stockage chiffré BDD + fallback env) et profils de paiement du tenant avec routage des encaissements (Closes #7726, Closes #7727).** **(1) #7726 — config PSP plateforme** : nouvelle table publique `payment_gateway_settings` (une ligne par passerelle stripe|chargily, `mode` test|live, `config` non secrète, `secrets` chiffrés au repos via cast `encrypted:array`, `is_active`, audit `updated_by`) ; `GatewaySettingsService` (contrat partagé `App\Shared\Contracts\Payments\PaymentGatewayConfigProviderInterface`, lié par `BillingServiceProvider`) applique la précédence **BDD → fallback env champ par champ** (zéro rupture sans ligne BDD ; cache applicatif à TTL court qui ne stocke que le CIPHERTEXT des secrets, invalidé à chaque écriture admin — changement de clé effectif sans redéploiement) ; `StripeService`, `ChargilyService` (Billing, dont la vérification de signature des webhooks) et `StripePaymentGateway`/`ChargilyPaymentGateway` (Accounting) consomment ce service au lieu de `config()` direct — via le contrat partagé, **aucun nouvel import cross-module** (garde #5584 verte). Endpoints admin `GET/PUT /platform/billing/gateways` + `POST /platform/billing/gateways/{gateway}/test` (ping Stripe `/v1/account`, Chargily `/api/v2/balance`), permission `platform.permission:billing.manage`, **secrets write-only jamais renvoyés en clair** (masque `sk_live_••••1234`, valeur vide au PUT = clé conservée), écriture auditée SANS valeur de secret (seuls les NOMS des champs modifiés). Écran Vue 3 « Passerelles de paiement » (`/settings/payment-gateways`, groupe Paramètres à côté des offres #7430) : formulaire par gateway, toggle test/live, bouton « Tester la connexion », badge configuré-BDD / configuré-env / non-configuré, i18n ×4 locales. **(2) #7727 — profils de paiement du tenant** : table tenant `tenant_payment_profiles` (`company_id` uuid indexé sans FK publique, type stripe_keys|bank_account|mobile_money, statut draft/verified/active, `is_default`, `details` non secrets, `secrets` chiffrés `encrypted:array`, colonne `stripe_account_id` **réservée** pour Stripe Connect — lot ultérieur), modèle `TenantPaymentProfile` sous `BelongsToCompany` (isolation fail-closed #3727) ; Actions `SaveTenantPaymentProfile` (type immuable, secrets write-only allowlistés par type, retour en draft quand une clé change) et `ActivateTenantPaymentProfile` (un seul profil actif par type, refus d'activer un profil Stripe sans clé) ; endpoints tenant `GET/POST/PUT/DELETE /billing/payment-profiles` + `/{id}/activate` réservés au rôle `principal`. **Routage des encaissements** : `PaymentGatewayFactory` (Accounting) résout d'abord le profil `stripe_keys` ACTIF du tenant courant via le contrat partagé `TenantPaymentProfileResolverInterface` — le checkout de la facture client part avec **les clés Stripe DU TENANT** et trace `metadata[payment_profile_id]` ; sans profil, comportement plateforme inchangé (routage pays ADR-0017) ; la vérification webhook Stripe essaie le secret plateforme puis le webhook secret du profil actif de la compagnie désignée par les métadonnées (le company_id ne sert qu'à SÉLECTIONNER le secret candidat — la signature HMAC reste l'unique preuve, fail-closed). UI client `front/web` : page « Encaissements » (`/settings/encaissements`, entrée de nav à côté de « Collaborateurs et rôles » #7555, réservée au principal), liste des profils avec badge statut/défaut, formulaires par type, activation, suppression avec confirmation, avertissement sécurité, replis FR centralisés (`payment-profiles.ts`, garde PA2-I18N-014 verte). **Tests** : `PlatformPaymentGatewayAdminApiTest` (7 cas — source env + masquage, chiffrement au repos + précédence BDD, write-only, fallback env intact, permission 403, webhook secret BDD avec contre-preuve env, ping sans fuite de clé) et `TenantPaymentProfileApiTest` (6 cas — CRUD/activation avec masques + chiffrement, write-only + suppression, 403 non-principal, isolation cross-tenant liste + 404 accès direct, routage checkout avec clé tenant + traçage du profil via `Http::fake`, fallback clés plateforme sans profil). **Vérifié localement** : gardes migrations vertes (`check-migration-basename-collisions.sh` OK, `check-duplicate-schema-create.py --strict --fail-on-duplicate` 0 duplication), `check-module-isolation.sh` 0 nouvel import croisé, `php -l` OK sur tous les fichiers PHP touchés, front/web `tsc --noEmit` 0 + `eslint --max-warnings 0` OK + `check:mojibake` OK, admin-dashboard `eslint` OK, garde i18n `check-i18n-diff.js` verte. **PHPUnit/PHPStan/Pint non exécutés localement (ni vendor Composer ni PostgreSQL dans la sandbox d'agent) — à confirmer par la CI.** **Hors périmètre, assumé** : bloc « Coordonnées de règlement » (IBAN/mobile money) sur le PDF des factures Accounting et bascule du checkout front `route.ts` vers l'API — pointés dans la PR comme suites de #7726/#7727.
- **chore(ops): drill de restauration — les binaires PostgreSQL 18 passent en tête de PATH sur le runner (Refs #7657, suite du correctif client 18).** Le drill re-déclenché après installation de `postgresql-client-18` (run 35487411111) échouait encore — proprement cette fois, sur la nouvelle garde de parité de version — car `/usr/bin/pg_dump` est le wrapper `pg_wrapper` de postgresql-common, qui sélectionne le cluster 16 préinstallé sur l'image du runner plutôt que la version la plus récente. Les deux jobs (backup quotidien, drill mensuel) ajoutent `/usr/lib/postgresql/18/bin` en tête de PATH via `$GITHUB_PATH` et vérifient la version du binaire 18 dans l'étape d'installation.
- **ci(travel): pipeline GitHub Actions prêt-à-secrets pour déployer `front/travel-web` sur Vercel — preview (dev) sur PR, production sur push main (Refs #7740, épic #7736 — PR empilée sur #7781).** Le déploiement initial (#7830) et le raccordement des projets (#7835) étaient MANUELS (Vercel CLI/API) : aucun chemin CI ne redéployait travel-web. Nouveau workflow `.github/workflows/travel-web-deploy.yml`, calqué sur les chemins Vercel/Cloudflare existants : PR touchant `front/travel-web/**` → déploiement **preview** sur le projet dev `leopardo-travel` (compte africanovatech, URL unique du run health-checkée — 401/403 Vercel Authentication accepté comme sain) ; push `main` → **production** sur `leopardo-travel-prod` (compte ibrahimkoubaye, `vercel pull → build → deploy --prebuilt --prod` depuis la racine — rootDirectory appliqué une seule fois, fix #6810 — + healthcheck 30×10 s sur le domaine promu) ; `workflow_dispatch` preview/production. **Prêt-à-secrets** : 6 secrets `VERCEL_TRAVEL_{DEV,PROD}_{TOKEN,ORG_ID,PROJECT_ID}` (2 jeux car 2 comptes — P07), non lisibles dans un `if:` → détour env de job (pattern exact `deploy-admin-dashboard.yml`) et skip EXPLICITE `::warning::` + step summary si absents (règle AGENTS.md, jamais de skip silencieux) — aucun secret dans le repo, la pose est une action propriétaire. Runbook `docs/ops/travel-web-vercel.md` (création tokens, récupération orgId/projectId sans rien committer, env vars runtime `BACKEND_API_URL`/`NEXT_PUBLIC_SITE_URL` par volet, domaines + procédure domaine custom, checklist propriétaire, dépannage) ; `docs/CI_CD_SECRETS.md` complété (garde de parité #7271 verte — au passage, ligne manquante `BACKUP_AGE_IDENTITY` du drill #7657 ajoutée : la garde était rouge sur toute PR workflows). `front/travel-web/vercel.json` vérifié cohérent (framework nextjs, région cdg1, ignoreCommand scopé) — inchangé. L'intégration à la promotion par tag (`deploy-prod.yml`) reste tracée en suivi dans #7740/#7736. **Vérifié localement** : `actionlint` 0 erreur sur le workflow, `bash dev-hub/tools/check-ci-secrets-doc.sh` vert (parité secrets↔doc), YAML parsé.
- **chore(ops): drill de restauration — client PostgreSQL 18 (PGDG) sur le runner + fail-fast sur échec pg_dump (Refs #7657).** Le drill du 2026-09-20 (run 35487018909) échouait sur « pg_dump: server version mismatch » : le serveur Neon est en 18.6 alors que le paquet `postgresql-client` d'Ubuntu 24.04 fournit pg_dump 16.15 — le dump faisait 0 octet et les 5 tables ressortaient MISSING. Correctifs : **(1)** les jobs backup et drill de `database-backup.yml` installent `postgresql-client-18` depuis le dépôt PGDG (`apt.postgresql.org.sh`) ; **(2)** `backup_drill.sh` cesse d'avaler l'échec de pg_dump — le code de sortie de la commande lancée via `\!` psql (non propagé par ON_ERROR_STOP) est capturé dans un fichier et vérifié, un dump de 0 octet fait échouer le drill avant chiffrement/restauration, et une garde de parité de version majeure client/serveur échoue tôt avec un message actionnable (`installer postgresql-client-N`).
- **chore(ops): drill de restauration — tolérance à la dérive de schéma de la liste des tables critiques (Refs #7657).** Le drill échouait au premier `COUNT(*)` car `shared_tenants.user_invitations` n'existe plus en production (liste codée en dur dans `backup_drill.sh`). Les counts sont désormais générés depuis les tables réellement présentes (`to_regclass`), warning sonore par table dérivée, échec fail-loud si moins de 3 tables critiques restent vérifiables.
- **ops(queue): worker Render dédié queue+scheduler — blueprints alignés sur la réalité, provisionnement API tenté et BLOQUÉ billing, drain web désormais débrayable (Refs #7649).** Provisionnement des background workers tenté le 2026-09-19 via l'API Render (POST /v1/services, type `background_worker`, plan starter, dev ET prod) : refus **402 « Payment information is required »** sur les deux workspaces (`tea-da9svvpsrm7s73d8o2j0` prod, `tea-d7dq20naqgkc73fqc94g` dev) — aucun moyen de paiement enregistré, et les workers sont inéligibles au tier gratuit ; l'ajout d'une carte est une décision propriétaire (dashboard → Billing). En attendant, tout est prêt pour une bascule en un geste : **(1) blueprints** — `render.yaml` (dev) et `render.prod.yaml` (prod, worker dé-commenté) déclarent UN worker starter par tier (`leopardo-queue-worker[-prod]`) portant queue:work ET le scheduler dans un seul service (`sh -c 'php artisan schedule:work & exec php artisan queue:work …'` — l'ancien second service scheduler-prod commenté est fusionné, ~7 $/mois au lieu de 14), `RUN_MIGRATIONS=false` (#6558), commentaires « n'existe pas dans le workspace » réalignés sur le constat 402 daté ; **(2) drain web débrayable** — les boucles intérimaires de `docker-entrypoint.sh` (respawn queue:work #7041 et scheduler #7649) sont gatées par `WEB_QUEUE_DRAIN` / `WEB_SCHEDULER_LOOP` (défaut `true` : comportement de prod inchangé tant que le worker n'existe pas ; à passer à `false` sur le web service dès le worker provisionné — évite double consommateur sur la table jobs et double schéduleur sans `onOneServer()`). Le volet workflow était déjà soldé par #7694 (`queue-worker-fallback.yml` supprimé, `queue-supervision.yml` = sonde HTTP pure sans credentials — garde `check-queue-strategy-coherence.sh` verte sur ces changements). **Vérifié** : `bash -n`/`sh -n` sur l'entrypoint, YAML des deux blueprints parsé, garde cohérence queue verte. Restent dans #7649 (suivi PR) : ajout carte + provisionnement (blueprint sync ou API), flip des deux flags, migration `QUEUE_CONNECTION` → Redis, généralisation `EnsureTenantContext` sur ~36 jobs.
- **fix(travel): la CI PHPStan de la pile #7736 repasse au vert — deux erreurs d'analyse statique introduites par le lot comptes clients #7739 (PR #7795) sont corrigées (Refs #7739).** Le check requis « PHPStan — Modules Architecture » bloquait la PR #7781 (auto-merge armé) : `instanceof.alwaysTrue` sur le `PersonalAccessToken` au logout de `TravelCustomerAccountController` (le token courant est toujours un `PersonalAccessToken` sous `auth:travel_customer` — la garde conditionnelle est supprimée, révocation directe) ; et en strict niveau 8, `property.notFound` sur `TravelBooking::$customer_account_id` dans `TravelMarketplaceController` (colonne non annotée `@property` — accès via `getAttribute()`). Aucun changement de comportement runtime, aucune baseline touchée. **Vérifié localement (PHP 8.4 rootless #7585)** : `php -l` OK, `pint --test` PASS ×2, PHPStan `phpstan-modules.neon` **[OK] No errors** (config complète) et `phpstan-strict.neon` **[OK] No errors** sur `app/Modules/TravelAgency`.
- **fix(travel): la clé `model` du provider `travel_customers` est reposée au RUNTIME — le driver custom sans `model` (#7739) fatalait à chaque requête authentifiée du guard `travel_customer` (#7739/#7781).** Le passage au driver custom `travel_customer_accounts` supprimait la clé `auth.providers.travel_customers.model`… que `Sanctum\Guard::hasValidProvider()` lit à CHAQUE validation de token : `$tokenable instanceof null` → « Class name must be a valid object or a string » (Guard.php:153) — me/logout/« mes réservations » répondaient 500 (reproduit en local : 3/6 tests `TravelCustomerAccountApiTest` en Error). La clé est posée par `config()->set()` dans `TravelAgencyServiceProvider::boot()`, jamais statiquement : sous analyse le guard est masqué (`LEOPARDO_STATIC_ANALYSIS`) et Larastan ne parcourt que les providers référencés par un guard — l'union de `request->user()` et la baseline strict restent intactes. **Vérifié localement (PHP 8.4 + PostgreSQL rootless)** : `TravelCustomerAccountApiTest` **6/6 verts** (49 assertions), `TravelMarketplaceApiTest` 6/6, PHPStan strict **[OK] No errors** sur le provider, les middlewares/AppServiceProvider de l'union et les contrôleurs travel, `pint --test` PASS, guard+provider complets au runtime (`config:show`) et masqués sous analyse (vérification empirique du boot).
- **fix(test): `TravelCustomerAccountApiTest` repasse au vert — reset du cache de guard entre les requêtes d'un même test Feature (#7739/#7781).** Les 6 tests du lot comptes clients #7739 n'avaient JAMAIS tourné en CI (runs Backend tous annulés sur la branche) et 3 échouaient : le `RequestGuard` sanctum CACHE l'utilisateur résolu entre deux requêtes du même test — après `logout` le client révoqué restait authentifié (200 au lieu de 401), la requête du voisin héritait du client précédent malgré un bearer différent (2 réservations au lieu de 1), et le checkout invité se voyait rattacher la réservation au client encore en cache. Correctif test-only : `app('auth')->forgetGuards()` aux 3 points de bascule d'identité (pattern repo établi, cf. `TravelAdvertDestroyTest`). **Vérifié localement (PHP 8.4 + PostgreSQL rootless, base fraîche entièrement re-migrée)** : `TravelCustomerAccountApiTest` **6 tests / 49 assertions verts**, `TravelMarketplaceApiTest` 6/6 verts, `php -l` et `pint --test` OK ; PHPStan strict et modules rejoués en local en complément du masquage `LEOPARDO_STATIC_ANALYSIS` déjà porté par la branche (PR #7781).
- **fix(travel): la CI PHPStan de la pile #7736 repasse au vert — les erreurs d'analyse statique introduites par le lot comptes clients #7739 (PR #7795) sont corrigées, zéro baseline touchée (Refs #7739).** Le check requis « PHPStan — Strict » (71 erreurs) et « PHPStan — Modules Architecture » bloquaient la PR #7781 (auto-merge armé). **(1) La cause principale était systémique** : la clé `model` du nouveau provider `travel_customers` dans `config/auth.php` ajoutait `TravelCustomerAccount` à l'union de type de `request->user()`/`Auth::user()` que Larastan calcule pour TOUTE l'app — ~68 entrées de `phpstan-strict-baseline.neon` (HR, Payroll, Billing, Recruitment, Restaurant… `isManager()`, `$company_id`, …) ne matchaient plus (dérive de message) et ressortaient comme erreurs neuves. Le provider devient un **driver custom** (`Auth::provider('travel_customer_accounts', …)` dans `TravelAgencyServiceProvider::boot()`, `EloquentUserProvider` standard sur le même modèle) : plus de clé `model` statique, l'union redevient celle d'avant #7795, runtime strictement identique (guard Sanctum dédié inchangé). **(2)** logout client : la garde conditionnelle sur `currentAccessToken()` était toujours-vraie pour PHPStan (`instanceof.alwaysTrue`/`if.alwaysTrue`) — révocation directe, le token est garanti sous `auth:travel_customer`. **(3)** `TravelBooking::$customer_account_id` n'est pas annotée `@property` (modèle à accès magique) — accès via `getAttribute()` dans `TravelMarketplaceController` et le test Feature. **Vérifié localement (PHP 8.4 rootless #7585)** : `php -l` OK ×5, `pint --test` PASS ×5, PHPStan `phpstan-modules.neon` **[OK] No errors** (config complète) et `phpstan-strict.neon` **[OK]** sur `app/Modules/TravelAgency`, `tests/Feature/Travel`, `AppServiceProvider` et les FormRequests HR impactés (le strict full-repo dépasse la sandbox — à confirmer par le check requis). PHPUnit non exécuté localement (pas de PostgreSQL) — comportement runtime inchangé, suite `TravelCustomerAccountApiTest` à confirmer par la CI.
- **feat(travel): comptes clients grand public sur le site marketplace — inscription, connexion, « mes réservations » cross-agences (Closes #7739, épic #7736 — PR empilée sur #7738/#7781).** Le grand public peut désormais créer un compte sur `front/travel-web` ; le checkout invité reste possible. **Backend** : nouvelle table PLATEFORME `travel_customer_accounts` (migration `public/`, pas de `company_id` — un client n'appartient à aucun tenant, même pattern pré-tenant que `marketing_leads`/`acquisition_funnel_events` #7496) + colonne nue `travel_bookings.customer_account_id` (tenant, nullable, indexée, SANS FK cross-schéma — constitution §II, guards `schemaTableExists`/`schemaHasColumn`) ; guard Sanctum **DÉDIÉ** `travel_customer` (provider `travel_customers` — jamais le guard employés ni super-admin) ; `TravelCustomerAccountController` (`POST /public/travel/marketplace/account/register|login|logout`, `GET …/me|/bookings`) — register/login sous `throttle:auth-sensitive` (e-mail + IP) **+ verrouillage 15 min après 5 échecs** (pattern #6563), mot de passe HASHÉ (`Password::min(12)->numbers()` + `NotCommonPassword`), 401 indifférencié. **Rattachement** : à l'inscription, les réservations marketplace existantes portant le même e-mail de contact (insensible à la casse, encore orphelines) sont revendiquées par le compte (`claimed_bookings`) ; connecté, `storeBooking` rattache la réservation à la création (la vérification par lien e-mail est un lot ultérieur de l'épic, documenté). **« Mes réservations »** : liste paginée cross-agences STRICTEMENT bornée par `customer_account_id` (isolation client), charge utile publique — référence, statut, trajet/villes, numéros de billets (JAMAIS le code de validation, secret de possession #7395) et nom public d'agence, aucun identifiant tenant. **Frontend `travel-web`** : pages `/account/login`, `/account/register`, `/account` (profil + tableau de bord réservations + déconnexion), lien d'en-tête Connexion/Mon compte, `AccountProvider` (token en localStorage, validé au montage via `/me`, purgé si révoqué), **pré-remplissage checkout** (e-mail/téléphone du compte, sans écraser la saisie) et token relayé à la création de réservation ; le proxy same-origin n'autorise le header `Authorization` QUE sur `account/*` + `bookings` (fail-closed conservé) ; i18n fr/en (`account.*`). openapi : +5 paths. Nouvelle suite `TravelCustomerAccountApiTest` (6 tests : hash + revendication par e-mail cross-agences ×2 agences, verrouillage login, me/logout guard dédié, isolation stricte entre deux clients, rattachement à la création + invité non rattaché, mots de passe faibles rejetés). **Vérifié localement** : `php -l` OK (9 fichiers), `tsc --noEmit` 0, `eslint src --max-warnings=0` 0, gardes migrations vertes (basename/préfixes, conventions BC, schéma tenant #1613, duplication #7452), `check-openapi-route-coverage.py` exit 0. **Non vérifié localement (ni composer ni PostgreSQL dans la sandbox)** : PHPStan strict et PHPUnit reposent sur la CI.
- **feat(travel): `front/travel-web` — site public Next.js de réservation de billets inter-agences : recherche, choix de sièges, checkout invité, confirmation et suivi par référence (Fixes #7738, épic #7736).** Le lot frontend de la marketplace (#7737) : nouvelle app autonome `front/travel-web` (Next.js 16 App Router, TypeScript strict, Tailwind v4 — propre `package-lock.json`, `turbopack.root`/`outputFileTracingRoot` épinglés comme front/web #7305, AUCUN impact sur les workspaces existants ni sur les paths filters CI). **Parcours livré** : accueil SSR avec recherche (villes desservies via autocomplete `marketplace/cities`, date), résultats SSR agrégés toutes agences (`marketplace/trips` — horaires, agence, prix « à partir de », sièges restants, état vide/panne backend gérés), page trajet avec **plan de sièges cliquable** (sièges libres du payload `marketplace/trips/{id}`, attribution auto possible), **checkout invité** (passagers adult/child/infant × classes tarifaires, contact e-mail/téléphone, consentement notifications, **clé d'idempotence unique par session de checkout** — un rejeu réseau ne double jamais la réservation), **confirmation** avec référence mise en avant + échéance de paiement, et page **« Retrouver ma réservation »** (référence + code de validation e-billet #7395 : statut, billets, PDF via URL signée, annulation en ligne motivée). **Checkout « à payer à l'agence » assumé** : `marketplace/payments/initiate` existe mais les providers en ligne dépendent de la config de chaque agence — le paiement en ligne côté front est le lot #7739/#7740 (décision documentée, la réservation expire côté backend si impayée). **Sécurité/config** : proxy same-origin `/api/v1/[...path]` restreint par allowlist fail-closed à `public/travel/*` (jamais de relais vers l'API authentifiée, headers Authorization/Cookie supprimés, panne backend → 502 JSON, pattern #3523), cible configurable `API_PROXY_TARGET` > `BACKEND_API_URL` > `NEXT_PUBLIC_API_URL` > défaut Render dev (chaîne #1701) — **aucun secret dans le repo** (`.env.local.example` sans valeur sensible). i18n **fr/en** maison (fr défaut, cookie `travel_lang` lu au SSR — `<html lang>` correct sans mismatch d'hydratation ; ar/tr = lot ultérieur de l'épic), SEO de base (metadata/OpenGraph par page, robots.txt — confirmation noindex —, sitemap), design sobre responsive mobile-first, `vercel.json` pattern front/web (ignoreCommand scopé `:/front/travel-web`, headers sécurité, région cdg1) pour les projets `leopardo-travel`/`leopardo-travel-prod` (DOMAINS.md mis à jour, garde canonique verte) ; scripts racine `travel:dev|build|lint` ajoutés sans toucher aux agrégats. **Vérifié localement** : `tsc --noEmit` 0, `eslint src --max-warnings 0` 0, `next build` OK, smoke `next start` (accueil SSR 200, proxy 404 fail-closed sur `/auth/me`, relais backend fonctionnel — les routes marketplace répondront dès le redéploiement du backend dev incluant #7737).
- **ci(travel): raccordement Vercel de `front/travel-web` finalisé côté projets — framework/rootDirectory posés, lien git prod, hygiène `.vercel/` (Fixes #7740).** Suite du déploiement initial #7830 : les deux projets Vercel (`leopardo-travel` dev / `leopardo-travel-prod` prod) sont désormais configurés `framework=nextjs` + `rootDirectory=front/travel-web` via l'API ; le projet prod est LIÉ au repo GitHub `kitokoh/leopardo-hr` (branche de production `main` — déploiement production automatique à chaque merge), le lien git du projet dev est bloqué par une action propriétaire (« Login Connection » GitHub absente du compte Vercel africanovatech — documenté, déploiement dev via Vercel CLI en attendant). Hygiène : `front/travel-web/.vercel/project.json` (IDs projet/orga, committé par erreur dans #7830) est retiré du dépôt et `.vercel/` rejoint le `.gitignore` racine. `docs/ops/DEPLOYMENT_URLS.md` et `docs/ops/DOMAINS.md` alignés sur l'état réel (conformité P07 dev/prod séparés, variables d'env listées SANS valeurs secrètes) ; l'intégration au pipeline `deploy-prod.yml` reste tracée en suivi (issue #7740 / épic #7736).
- **chore(ops): drill de restauration backup exécutable — clé `age` matérialisée depuis le secret `BACKUP_AGE_IDENTITY` + `CONFIRM_RESTORE=YES` (Refs #7657).** `backup_drill.sh` exige un *chemin* de fichier d'identité age et une confirmation destructive explicite ; le workflow passait un secret-contenu comme chemin et omettait la confirmation — le drill mensuel était donc inexécutable même secrets posés. La clé est écrite en fichier éphémère du runner (umask 077) et la cible reste la base scratch dédiée `leopardo_drill`.
- **ci(travel): `front/travel-web` déployé sur les projets Vercel provisionnés — `leopardo-travel` (dev) et `leopardo-travel-prod` (prod) servent désormais la vraie app (Closes #7740, décision #6918, épic #7736).** Les deux domaines remplaçaient des pages de validation hors repo : déploiement initial effectué via Vercel CLI depuis `front/travel-web` (contenu PR #7781), HTTP 200 vérifié sur les deux URLs le 2026-09-19 (titre SSR correct, proxy same-origin `public/travel/*` fonctionnel). Variables d'env configurées CÔTÉ VERCEL, aucun secret dans le repo : dev → `BACKEND_API_URL=https://gestionemployerbackend.onrender.com/api/v1`, prod → `BACKEND_API_URL=https://leopardo-prod.onrender.com/api/v1` (+ `NEXT_PUBLIC_SITE_URL` par volet), conformité P07 dev/prod séparés (comptes africanovatech / ibrahimkoubaye). `docs/ops/DOMAINS.md` et `docs/ops/DEPLOYMENT_URLS.md` mis à jour (nouvelle section Travel : projets, env, procédure) ; les endpoints `public/travel/marketplace/*` répondront 200 après merge du backend #7737/#7781 et redéploiement Render (proxy déjà vérifié) ; intégration `deploy-prod.yml` tracée en suivi dans l'issue.
### Added
- **feat(billing): crédits IA achetables — ledger, checkout one-shot, décompte réel, UI facturation (Closes #7764, spec MISSION_ESPACE_CLIENT §3.4, `[FREEZE-EXCEPTION]` fondateur).** Nouveau grand livre tenant `ai_credit_ledger` (mouvements signés en tokens : `purchase`/`consumption`/`adjustment`, `company_id` obligatoire, index unique **partiel** sur la référence des achats = idempotence au niveau base) + compteur mensuel **persistant** `ai_usage_counters` (une ligne par company × mois — choix « table dédiée » plutôt qu'agrégation du ledger : le quota de plan se compte en REQUÊTES, le ledger en TOKENS). Packs versionnés (`AiCreditService::PACKS_VERSION=2026-09-19.1` : S=100 k tokens/9 €, M=500 k/39 €, L=2 M/129 €) ; `credit()` idempotent par référence (pré-check + savepoint + index unique), `debit()` **fail-closed** (rollback si le solde deviendrait négatif → 422 `AI_CREDITS_EXHAUSTED`, catalogue d'erreurs ×4 langues). Achat : `POST /billing/ai-credits/checkout` (principal only) → Stripe Checkout **`mode=payment` one-shot** (`price_data` inline, metadata `{purpose, company_id, pack, tokens}`) ; le webhook `checkout.session.completed` en mode payment crédite le ledger — idempotent via le registre #5444 **et** la référence unique (un événement distinct portant la même session ne crédite pas deux fois). Mode **sandbox** opt-in explicite (`SANDBOX_CHECKOUT`, même sémantique que le front #2628 : crédit immédiat + redirection `success_url?sandbox=1`, jamais déduit d'une clé Stripe absente). `GET /billing/ai-credits` : solde + packs + historique paginé. Front : section « Crédits IA » **uniquement** dans `(dashboard)/billing` (solde, packs achetables, historique récent), **strictement facultative** (un échec de l'endpoint crédits n'empêche ni abonnement ni factures), i18n `billing.ai_credits.*` fr/en/ar/tr via `shared/i18n` (catalogues régénérés ×3 surfaces). Consommation : quota mensuel du plan d'abord, puis débit forfaitaire des crédits achetés (`TOKENS_PER_REQUEST=1000`, aligné `ai.max_tokens`), solde nul → 422 fail-closed (même contrat `{error, message, localized_message}` que `AI_QUOTA`/`AI_TOKEN_BUDGET`). OpenAPI +2 paths (miroir + SDK régénérés, redocly 0 erreur, couverture routes verte). **Vérifié** : gardes migrations (collisions basename + `--strict --fail-on-duplicate`) vertes ; front/web `eslint --max-warnings 0`, `tsc --noEmit`, **jest 128 suites / 1107 tests** (dont 3 nouveaux sur la section), `check:mojibake`, `I18N_VALIDATION_OK (4 locales)` ; `php -l` sans erreur sur tous les fichiers touchés ; **PHPUnit (AiCreditsTest 10 cas + TokenBudgetTest étendu) non exécuté localement — pas de runtime PHP 8.4 dans la sandbox, à confirmer par le check requis `Tests - Leopardo RH`.**

### Fixed
- **fix(queue): le contexte tenant des jobs queued est généralisé ET verrouillé par un test d'architecture — plus aucun job tenant ne peut arriver en prod sans `EnsureTenantContext` (Refs #7649, volet code — le provisioning Render reste une action propriétaire).** L'audit #7649 pointait « middleware non systématique sur les ~36 jobs » : inventaire complet des 49 classes `ShouldQueue` de `api/app` — 37 jobs sur 39 portaient déjà le pattern `TenantScopedJob` + `middleware() [new EnsureTenantContext]`. Les 2 restants sont soldés : **`DeliverTravelWebhookJob`** (livraison HMAC des webhooks transporteur, TRAVEL-806) reçoit le middleware (résolution `company_id` via la delivery `withoutGlobalScopes`, pattern `ExportTravelReportJob`) ; **`ProvisionDemoTenantJob`** est documenté comme job PLATEFORME (il CRÉE le tenant — aucun contexte préexistant possible). Nouveau garde `tests/Unit/Architecture/QueueTenantContextArchitectureTest` (pur PHPUnit, sans boot Laravel ni DB) : scanne `app/` par réflexion, échoue si une future classe `ShouldQueue` n'implémente pas `TenantScopedJob` + `EnsureTenantContext` — exemptions EXPLICITES et justifiées une par une (`ProvisionDemoTenantJob` plateforme ; listeners queued `AuditLogger`/`WebhookListener` — dette « mode schema » assumée ; Mailables/Notifications exclus par catégorie, le middleware de job ne s'y applique pas), avec un second test qui périme les exemptions obsolètes. Côté ops : `render.yaml`/`render.prod.yaml` (worker `queue:work` + `schedule:run`) sont portés par la PR #7819 (même issue), le drain CI `queue-worker-fallback.yml` a déjà été supprimé par #7694 (supervision = pure alarme HTTP) ; la procédure propriétaire complète (billing → blueprint sync → débrayage du drain web → migration `QUEUE_CONNECTION=redis`) est consolidée dans `docs/ops/RENDER_WORKER_PROVISIONING_7649.md`. **Vérifié localement (PHP 8.4 rootless #7585)** : `php -l` OK, PHPStan strict `[OK] No errors` sur les 3 fichiers PHP touchés, nouveau test d'architecture **2 tests / 8 assertions verts** + preuve A/B (middleware retiré du job Travel → le garde échoue en le nommant). **PHPUnit Feature non exécuté localement (pas de PostgreSQL dans la sandbox)** — la suite complète à confirmer par le check requis `Tests - Leopardo`.
- **fix(ai): AIRateLimiter cesse d'utiliser un plan codé en dur `'starter'` (un PlanCode qui n'existe plus) et un compteur Cache volatil (Refs #7764).** Le plan effectif est dérivé **fail-closed** via `EntitlementGuard` (pas de souscription active → `free`) ; `config('ai.quotas')` est réaligné sur les PlanCode canoniques `free|pilot|operations|enterprise` (10/50/200/illimité — valeurs équivalentes aux anciennes `trial/starter/business` qui ne matchaient jamais) ; le compteur mensuel vit en DB (`ai_usage_counters`, survit aux redéploiements/`Cache::flush`), avec repli Cache uniquement si la table est absente (fixtures de test partielles, pattern #5265).

- **fix(marketing): i18n L-10 du front marketing web — chaînes FR en dur externalisées ×4 locales + tests unitaires des composants #7755 (PR #7784, delta résiduel du lot BC-12 après le merge de #7757).** Les composants livrés par #7757 (`MediaUrlsInput`, `PostInteractions`, `WeeklyReportCard`, page `social-marketing`) affichaient ~20 libellés français en dur (« Commentaires », « Supprimer », « Ajouter », « Bilan des 7 derniers jours », états loading/empty/sent…) : externalisation complète en clés `marketing.web.*` dans le catalogue partagé (fr/en/ar/tr, `shared/i18n/locales` + syncs web/admin régénérés via `sync-web.js`), URLs de médias forcées `dir="ltr"` pour le rendu RTL, **contrat OpenAPI** : les 5 endpoints marketing livrés par #7757 (`GET/POST …/comments[/reply|/suggest-reply]`, `POST /marketing/ai/suggest-post`, `GET /marketing/reports/weekly`) n'étaient pas documentés dans `api/openapi.yaml` — la garde `check-openapi-route-coverage.py --strict-staleness` était rouge sur main, elle repasse verte (schémas alignés sur les FormRequests réels). Ajout de 3 suites Jest (`MediaUrlsInput` ×4 cas, `PostInteractions` ×6 dont `normalizeComments` exporté, `WeeklyReportCard` ×3) + `data-testid` de ciblage — aucun changement d'API ni de comportement.
- **feat(travel): clés API de lecture par distributeur — émission/rotation/révocation par le gérant, catalogue publié scopé et rate-limité, guide partenaire complété (Closes #7641).** La boutique publique reposait sur un jeton UNIQUE par tenant (`X-Travel-Shop-Token`) : impossible de donner un accès distinct, révocable et traçable à plusieurs plateformes de distribution (les `partner-keys` #6086 sont entrantes — transporteurs — pas des clés de lecture). Nouvelle table tenant `travel_distributor_keys` (hash SHA-256 au repos, jamais de token en clair — pattern TravelCarrierApiKey/ZKTeco), modèle `TravelDistributorKey` (BelongsToCompany, allowlist `SCOPES` fermée : `catalog.read`, `bookings.read`). **Gestion côté gérant** (policy principal/rh, 404 sûr cross-tenant) : `GET/POST /travel/distributor-keys` (token `dsk_…` affiché UNE fois, scope inconnu → 422 fail-closed), `POST …/{key}/rotate` (remplacement atomique du hash — l'ancien token est immédiatement invalide, clé révoquée → 422) et `POST …/{key}/revoke` (idempotent, la ligne reste pour les stats/traçabilité). **Surface de lecture distributeur** (hors auth:sanctum, header `X-Distributor-Key`, middleware `travel.distributor:<scope>` miroir de `TravelPartnerAuthMiddleware` — contexte tenant posé, search_path restauré en finally #4787, échecs audités) : `GET /travel/distributor/catalog` (voyages PUBLIÉS uniquement, même règle que la boutique ; filtres villes/dates, per_page ≤ 100) et `GET /travel/distributor/bookings/{reference}` (extrait minimal sans PII passager, posture portail #7395) — throttle dédié 60/min ; stats d'usage par clé (`last_used_at` + `usage_count` incrémenté). Contrats OpenAPI (+4 paths, +1 schéma, miroir dev-hub + SDK JS/Python régénérés, garde de couverture 0 nouveau drift) ; `docs/travel/PARTNER_INTEGRATION_GUIDE.md` §5.2 (gestion, scopes, auth, isolation, rotation recommandée). Nouvelle suite `TravelDistributorKeyTest` (7 cas : token affiché une fois et jamais relisté, scope pirate 422 + employé simple 403, catalogue publié seul exposé, enforcement scope/révocation/rotation, stats d'usage, **isolation cross-tenant** catalogue + booking + gestion 404, suivi booking sans PII). **Vérifié** : `php -l` OK sur les 12 fichiers PHP touchés, gardes migrations (collisions + `--strict --fail-on-duplicate`) vertes, isolation modules/couches/BC 0 nouvelle violation, YAML OpenAPI parsé, `generate-openapi-sdk.mjs --check` OK. **Non vérifié localement (PHP 8.4/PostgreSQL absents)** : PHPStan strict et la suite Feature — à confirmer par la CI.
- **feat(onboarding,travel): vertical « Agence de voyage » dans l'entretien de préparation — activation auto du module et checklist orientée agence (Closes #7640, epic #7486).** Le chemin d'activation existait déjà de bout en bout (secteur `travel` dans l'allowlist `SetupInterviewPlanner` → verticale `travelagency` via `SolutionActivator`, manifest au catalogue depuis #7220-bis, cartes-réponses i18n ×4 déjà présentes) mais restait **non prouvé** et sans suite : aucune checklist agence après l'entretien. (1) **Checklist orientée agence** (`SeedDefaultSteps`) : un tenant dont la verticale `travelagency` est active reçoit trois étapes dédiées — créer son réseau (`travel_setup_network`), programmer son premier voyage (`travel_first_trip`), enregistrer sa première vente (`travel_first_sale`) — titres servis par `api/lang/*/onboarding.php` ×4 locales (garde PA2-I18N-007) + libellés riches `nextSteps.steps.travel_*` ×4 côté web (carte « Prochaines étapes » #7494) ; étapes déclaratives (aucun prédicat `StepCompletionGuard` : vérifier gares/voyages exigerait un import Onboarding → TravelAgency, interdit par la garde d'isolation #5584). (2) **Preuve par test** (`SetupInterviewTravelVerticalTest`, 3 cas) : un inscrit qui se déclare agence obtient le flag `companies.features.travelagency` SANS intervention plateforme (l'entrée « Voyages » de la nav web est gatée sur ce flag), la checklist agence est ordonnée réseau → voyage → vente, et un tenant non-travel ne voit JAMAIS ces étapes. Catalogues propagés par les 3 syncs (web/backend/mobile — rattrape au passage la propagation mobile des clés `travel.staff.*` de #7639). **Vérifié** : `php -l` OK sur les 5 fichiers PHP touchés, `I18N_VALIDATION_OK (4 locales)`. **Non vérifié localement (PHP 8.4/PostgreSQL absents de la session)** : PHPStan strict et la nouvelle suite Feature — à confirmer par la CI.
- **feat(travel): page Équipe de l'espace gérant — affectations staff sur bureau ou voyage, révocation en 2 clics, historique (Closes #7639, épique #7642).** Nouvelle page `/travel/staff` (front/web, pattern des pages gérant #7633..#7637) consommant le pont RH #7638 : nomination d'un employé RH du tenant sur un rôle travel (chauffeur, guichetier, contrôleur, chef de bureau) scopé sur un bureau de vente OU un voyage (`POST /travel/staff-assignments`, exactement un scope — contrat `StoreTravelStaffAssignmentRequest`), révocation en deux clics (`POST /travel/staff-assignments/{id}/revoke`, la ligne reste pour l'historique) et section « Historique des affectations » repliable (statuts révoquée/expirée, date de révocation). Tuile « Équipe » ajoutée au hub `/travel`, route `/travel/staff` déclarée dans `ROUTE_TO_MODULE` (même gating `travelagency` que le reste de l'espace gérant), i18n ×4 locales (clés imbriquées `travel.staff.*`, `travel.staffRole.*`, `travel.staffStatus.*`, propagées par `sync-web`). **Vérifié** : Jest 4/4 verts sur la nouvelle suite `staff-page.test.tsx` (liste active, révocation, nomination bureau, historique), `eslint --max-warnings 0` 0, `tsc --noEmit` 0.
- **feat(marketing): tranche 1 Marketing & Communication — campagnes email effectives + IA + interactions sociales (Closes #7751).** **(1) Campagnes email CRM effectives** : les campagnes `email` portent enfin leur contenu (`subject`/`body`, migration tenant additive `2026_09_19_000004_7751`, `down()` complet), le lancement matérialise les envois puis les traite en file (`ProcessCampaignSendsJob` sur la queue dédiée, tenant-scoped `EnsureTenantContext`, réclamation atomique des sends — rejeu sans double envoi) via le nouveau fournisseur **Resend** (`ResendEmailProvider`, clé par env `RESEND_API_KEY` uniquement, aucun secret committé, fail-closed si absente) ; commande schedulée idempotente `crm:process-campaign-sends` (`withoutOverlapping` + `onOneServer`). **(2) IA Marketing** : suggestions de posts sociaux et bilan hebdomadaire générés via l'abstraction LLM du dépôt (jamais d'appel vendeur direct), endpoints `POST /marketing/ai/suggest-post` et `GET /marketing/ai/weekly-report` (gate module + RBAC marketing). **(3) Interactions sociales** : lecture des commentaires et réponse (unitaire + en masse) via `AyrshareClient`, endpoints `SocialCommentController` documentés. **(4) Front web** (`front/web`) : éditeur de post enrichi (médias par URL), carte bilan hebdo, panneau interactions — i18n ×4 locales (FR/EN/AR/TR, catalogues `crm`/`marketing`). Arête BC déclarée dans `bounded-context-dependencies.json`. **Tests Feature** : `CrmCampaignSendProcessingTest`, `ResendEmailProviderTest`, `MarketingAiTest`, `SocialCommentControllerTest` (`Http::fake`, aucun appel réseau réel). Réparation CI au passage : renumérotation de la migration `2026_09_19_000001` en collision avec #7687 (garde #1962) et retrait d'un tiret cadratin flagué par PA2-I18N-007 dans un log du worker.
- **feat(restaurant): RESTO-902 — commande en ligne depuis la page publique par slug + avis/notes clients avec modération gérant (Refs #7747, base RESTO-901 #7746).** **Commande publique par slug** (réutilise le pipeline RESTO-805 — AUCUN second pipeline) : `POST /public/restaurants/{slug}/orders` (panier `items[].product_code` — l'identifiant métier désormais exposé par le menu publié du profil 901, cohérent avec la boutique RESTO-805 —, quantité, note par article agrégée dans `note_redacted`, nom + téléphone client jamais ré-exposés, `order_type` pickup|takeaway|delivery, idempotence par `idempotency_key` — rejeu → même commande, 200), via `CreateOnlineOrderAction` (prix + TVA SERVEUR, source `online`, outbox `restaurant.order.created.v1` ; l'action, sans appelant jusqu'ici, passe désormais la commande en `open` comme le service RESTO-805 routé) ; produits acceptés UNIQUEMENT si `is_published_online` ET `is_available` ET actifs et servis par la branche (422 sinon) ; branche non publique/société suspendue → **404 fail-closed** (nouveau `RestaurantPublicBranchResolver`, mêmes garde-fous que l'annuaire 901). `GET /public/restaurants/{slug}/orders/{ref}` : suivi par référence `RST-…` non énumérable, borné à LA branche du slug, **sans PII**. `POST …/{ref}/pay` : `cash` (alias public `cash_on_delivery`) / `mobile_money` via `RestaurantPublicOrderService::pay()` — **PSP carte volontairement fermé (spec #5272 en attente)**. **Avis clients** : migration `restaurant_reviews` (company_id uuid, branch_id, `order_reference` — la référence RST- aléatoire EST déjà non énumérable, même stockage que RESTO-805 — unique par tenant = un avis par commande, rating 1–5 tinyint, comment nullable, author_name, status pending|published|rejected défaut pending, index (branch_id,status)), modèle `BelongsToCompany`. `POST /public/restaurants/{slug}/reviews` : preuve d'achat = `order_ref` d'une commande de CETTE branche en statut terminal servi/livré (served/paid/closed), un seul avis par commande (409), statut initial pending, **throttle strict dédié** `restaurant-reviews-public` (5/min/IP, config + .env.example) en plus du `shop-public` du groupe ; `GET …/reviews` : publiés uniquement, paginés, DTO strict (author_name, rating, comment, date — jamais l'order_reference). **Modération tenant** (chemins EXACTS du front lot 904) : `GET /restaurant/reviews?status=`, `POST /restaurant/reviews/{review}/publish|reject` — `RestaurantReviewPolicy::moderate` (pattern gérant `canManageBranchResource` #7599), listing scopé `ScopesRestaurantBranchListings`, 404 cross-tenant. **Note moyenne** : `rating_avg` + `reviews_count` (avis PUBLIÉS seuls) sur les cartes de l'annuaire (sous-requêtes SQL sur le schéma partagé — **pas de compteur dénormalisé, zéro dérive, la modération fait foi**) et sur le profil public (porté par le cache 901, **purgé à chaque publish/reject**). i18n ×4 locales (`restaurant.public_reviews.*` + réparation des clés `restaurant.public_shop.*` référencées par RESTO-805 mais jamais créées). OpenAPI +8 opérations (tag Restaurant) — miroir `dev-hub/openapi/v1.yaml` et SDK **non committés** (#7654 : artefacts régénérés en CI, retirés de l'index dans cette PR). CI : table `restaurant_reviews` ajoutée à `CreatesMvpSchema` (parité #5443) ; les 3 contrôleurs publics par slug allowlistés avec justification dans `check-vertical-controller-policies.sh` (#7599/#7600 — surfaces volontairement publiques, mécanisme prévu par la garde). **Vérifié localement (PHP 8.4 rootless #7585)** : `php -l` OK ×23 fichiers, **PHPStan strict level 8 `[OK]` sur `app/Modules/RestaurantManager`**, `pint --test` PASS, gardes vertes : module-isolation (0 nouvel import), unrouted-controllers, OpenAPI route-coverage (0 nouveau drift), migrations (basename, conventions BC #5431, tenant-schema #1613, préfixes inter-PR), duplicate-routes + public-routes (`route:list` booté : 7 routes publiques + 3 privées enregistrées), policies-single-point, bounded-context registry/dependencies, layer-purity, tests déterministes, parité .env.example. **Non vérifié localement (pas de PostgreSQL)** : les 2 suites Feature (`RestaurantPublicSlugOrderTest` ×7 cas, `RestaurantReviewTest` ×7 cas) — à confirmer par le check requis `Tests - Leopardo RH`.
- **fix(flutter): #7652 tranche 2 — la feature `attendance` (data + provider) redevient canonique dans `leopardo_core`, paramétrée par app via config ; la couche d'état `auth` de l'app employee cesse d'être un fork (Refs #7652 — les écrans divergents restants et `absences`/`attendance_geo` exigent la CI Flutter, indisponible en sandbox).** Suite directe de la tranche 1 (#7743, inventaire + garde `mobile-core-duplication-guard.yml`). **(1) Fusion attendance data/provider** — la « divergence croisée » désignée pire du lot par l'audit est résorbée : `leopardo_core/.../attendance/data/attendance_repository.dart` devient le superset des deux forks (apports employee : `work_type`/`punch_note` dans les payloads de pointage, règle F-21 « 1er pointage gagne » sur la file hors-ligne Hive, `sessions`/`summary`/`session_number` dans `decodeTodayResponse`, `getMyAnomalies`/`getTodayTasks`/`completeTask` ; acquis core conservés : garde payload non-Map #3406, détection hors-ligne #5407, clé d'idempotence rejouée, endpoints manager anomalies/corrections/day-detail), idem pour `attendance_provider.dart` (état superset `todaySessions`/`daySummary`, notices `overtime`/`break`) ; le modèle employee-only `attendance_anomaly.dart` remonte dans `leopardo_core/lib/features/attendance/models/`. **(2) Différence par app via config, pas par fork** (exigence expresse de l'issue) : nouveau `AttendanceFeatureConfig` + `attendanceFeatureConfigProvider` (défaut manager/RH = comportement core historique) ; `leopardo_employee/main.dart` surcharge le provider (`ProviderScope(overrides:)`) pour le ton self-service du message de pointage hors zone — seule différence fonctionnelle légitime relevée entre les deux forks. **(3) Suppression des copies employee** : `attendance/data` + `attendance/providers` deviennent des shims de ré-export pur (pattern #5279 — chemins exigés par `validate-mobile-location-readiness.ps1`/`validate-mobile-notification-production-proof.ps1`), `attendance/screens/history_screen.dart` (12 lignes de diff cosmétiques) et `auth/data/auth_repository.dart` sont SUPPRIMÉS, l'app importe les versions core ; les 2 grands écrans encore forkés (`attendance_screen.dart` 2 432 L, `monthly_summary_screen.dart`) basculent dès maintenant sur le provider/modèles CORE (ils ne consomment plus aucun état local dupliqué). **(4) Couche d'état auth unifiée** : le diff core↔employee de `auth_provider.dart`/`auth_repository.dart` était exclusivement commentaires + formatage (prouvé par diff normalisé) — shim + suppression ; `core_providers.dart` de l'app cesse de shadow-déclarer `authRepositoryProvider`/`attendanceRepositoryProvider` : une SEULE instance Riverpod par provider dans toute l'app (le logout-401 du notifier attendance agit désormais sur le MÊME `authProvider` que le routeur — le fork rendait ce chemin silencieusement inopérant), et 12 fichiers employee (router, home, settings, profile, écrans auth…) importent `package:leopardo_core` directement. **(5) Baseline de la garde : 48 → 43 entrées** (`--update-baseline`, cliquet respecté) ; §6 de `docs/audits/FLUTTER_DUPLICATION_CORE_APPS_7652.md` documente la tranche et le reliquat (écrans attendance/auth brandés, `absences`, `settings`, `attendance_geo`, forks auth des apps satellites — fusions d'écrans injustifiables sans `flutter analyze`). **Vérifié localement (pas de SDK Flutter en sandbox — analyse statique exhaustive)** : garde #7652 verte (43 en baseline, 15 shims, 0 nouvelle duplication), zéro référence résiduelle aux fichiers supprimés (`rg`), tous les imports/exports `package:` des fichiers touchés résolus sur disque (script), accolades/parenthèses équilibrées sur les 12 fichiers Dart modifiés, clés l10n consommées présentes ×4 ARB, marqueurs interdits (`isManager`…) absents de l'app employee (garde split), tokens des contrats workflow (`attendanceNoClock`, `.checkIn(`…) toujours présents — `flutter analyze`/tests à confirmer par `Mobile Apps CI - Flutter`.
- **feat(pharmacy): verticale PharmaManager complète — MVP officines de pharmacie (Closes #7798, #7799, #7800, #7801, #7802, #7803, #7804 — série PHARMA-001→007, BC-30, lot BC_BATCH_BRANCH).** Nouvelle solution sectorielle activable par tenant pour les fondateurs d'officines, sur le patron exact d'EduManager/FuelStation. **(1) Fondation (#7798)** — `PharmacyManifest` (code `pharmacy`, maturité pilot, requis `['rh']`, optionnels accounting/crm/attendance, PII santé déclarées) enregistré dans la `SolutionCatalogue` (allowlist fail-closed) par `PharmacyServiceProvider`, flag `pharmacy` ajouté à `Company::KNOWN_MODULES` ET au registre versionné `config/feature-flags.php` (MAT-010 — sans quoi `FeatureFlag::enabled()` reste fail-closed même avec le flag tenant posé), entrée ROADMAP (règle APV L.08), trait `ChecksPharmacySolution` (403 `PHARMACY_SOLUTION_INACTIVE` fail-closed, 404 anti-fuite cross-tenant), routes `api/routes/modules/pharmacy.php` dans le groupe v1, structure Clean Architecture 5 couches, gouvernance BC-30 PHARMACY (registre MAT-001, arêtes BC-30→{BC-01,BC-02,BC-03} MAT-002, CODEOWNERS, parité docs architecture ×4). **(2) Référentiel produits (#7799)** — table tenant `pharmacy_products` (`BelongsToCompany`) : DCI, forme galénique, dosage, code-barres et code interne uniques PAR TENANT, `prescription_required`/`is_controlled`, catégorie, prix achat/vente et taux de taxe en decimal (jamais de float), seuil `min_stock_level`, statut active/archived ; API `/api/v1/pharmacy/products` (recherche nom/DCI/code-barres, filtres, archivage — jamais de suppression), RBAC deny-by-default. **(3) Stock par lots (#7800)** — `pharmacy_batches` (n° de lot, péremption, quantité restante, coût, CHECK ≥ 0, unicité company/product/batch) + `pharmacy_stock_movements` **journal immuable append-only** (gardes modèle anti-UPDATE/DELETE, CHECK types) ; `PharmacyStockService` transactionnel + `lockForUpdate` : `receive()`, `adjust()` (raison obligatoire, jamais de lot négatif), `dispenseFefo()` **First-Expired-First-Out multi-lots** refusant lots périmés et stock insuffisant (422 `PHARMACY_INSUFFICIENT_STOCK`, zéro effet partiel), `returnToBatches()` ; API `/pharmacy/stock` (niveaux hors périmés, lots, mouvements, ajustements manager) + `/pharmacy/alerts` (sous seuil, périmant < N jours défaut 90, périmés à retirer). **(4) Achats (#7801)** — `pharmacy_suppliers` + `pharmacy_purchase_orders`/lines : cycle `draft→ordered→partially_received→received|cancelled` (422 `PHARMACY_INVALID_TRANSITION` sinon), numéro `PO-YYYY-XXXX` séquencé PAR TENANT (verrou pessimiste), réception partielle → `PharmacyStockService::receive()` (lots + mouvements receipt tracés au PO), SUR-réception refusée (422 `PHARMACY_OVER_RECEIPT` + CHECK SQL) ; API CRUD + place/cancel/receive. **(5) Ventes comptoir POS (#7802)** — `pharmacy_sales` (numéro `VT-YYYY-XXXXX` séquencé/tenant, paiement cash|card|mobile|insurance, statut completed|voided) + lignes à prix/taxe FIGÉS ; `PharmacySaleService::create()` transactionnel : ordonnance exigée si produit `prescription_required` OU `is_controlled` (422 `PHARMACY_PRESCRIPTION_REQUIRED`), délivrance FEFO par ligne, totaux calculés SERVEUR (bcmath — le total client est ignoré) ; `void` (raison obligatoire, manager) conserve la vente et ré-crédite les lots d'ORIGINE par mouvements `return` (contre-passation). **(6) Ordonnancier (#7803)** — `pharmacy_prescribers` + `pharmacy_prescriptions` (référence unique/tenant, patients = PII santé jamais exposées cross-tenant, prescription validée dans le tenant à la vente — 422 `PHARMACY_PRESCRIPTION_NOT_FOUND`) ; `GET /pharmacy/controlled-register` **lecture seule, dérivé du journal immuable** des mouvements sale/return des produits contrôlés (date, produit, lot, quantité, n° de vente, ordonnance, prescripteur, patient, délivreur — les annulations apparaissent en contre-passation), accès manager, export CSV borné `?from=&to=&format=csv`. **(7) Dashboard fondateur (#7804)** — `PharmacyDashboardService` + `GET /pharmacy/dashboard` (manager) : CA jour/7j/30j, nb ventes du jour, panier moyen 30j, répartition par paiement (ventes voided EXCLUES partout), valorisation du stock (lots non périmés, quantité × coût), produits sous seuil, lots périmant < 90 j, lots périmés, top 5 produits 30 j, PO en cours, délivrances contrôlées 30 j — montants en décimal string. OpenAPI : 34 opérations `pharmacy` documentées (tag Pharmacy, garde de couverture #1473 verte, Redocly valide). **Vérifié localement (PHP 8.4 + PostgreSQL)** : PHPStan strict L8 `[OK] 0 erreur` sur le module, **PHPUnit 35 tests / 289 assertions verts** (6 suites Feature : produits, stock/FEFO/alertes, achats/réceptions, ventes/void, ordonnances/ordonnancier/CSV, dashboard — isolation tenant et 403 solution inactive couverts partout), gardes registre BC (30 BCs) + matrice MAT-002 + isolation modules + parité docs (30 modules ×4) + routes dupliquées + collisions de migrations + contrôleurs orphelins toutes vertes.
- **feat(accounting): les 7 écrans web du rôle comptable arrivent dans le dashboard admin — documents/factures, plan comptable, journal/grand livre/balance, lettrage, exercices & clôture, banque/rapprochement, TVA & états financiers (Fixes #7776, spec `.specify/features/5534-accounting-web-ui/spec.md`, reprise de la ghost close #5534).** Le backend comptabilité (~50 endpoints, `api/routes/modules/accounting.php`) n'avait que 3 écrans web (Dashboard/Activation/Settings) : l'expert-comptable devait appeler l'API à la main. Sept nouvelles vues Vue (`front/admin-dashboard/src/views/accounting/`), branchées sur les shapes de réponse EXACTES des controllers (lues dans le code, pas devinées), design system existant (tokens `glass-*`, badges de statut, états loading/empty, toasts) : **(1) Documents** — liste filtrée (type/statut/dates), création de brouillon avec éditeur de lignes + aperçu du prochain numéro (`/documents/next-number`), détail dépliable (lignes, paiements), envoi, annulation motivée, avoir lié, encaissement (POST `/documents/{id}/payments`, méthodes du FormRequest) ; **(2) Plan comptable** — filtre type/actifs, recherche, création (code/libellé/type/classe 1-8), activation/désactivation (PUT), suppression des seuls comptes libres (422 métier mis en mots) ; **(3) Journal & grand livre** — 3 onglets : journal de période (totaux, badges équilibré/clôturé, export CSV expert, clôture de période avec confirmation), grand livre paginé à solde cumulé + solde d'ouverture par compte, balance de vérification avec indicateur d'équilibre + export FEC DGFiP ; **(4) Lettrage** — sélection multi-écritures avec Σ débits/Σ crédits en direct et indicateur d'équilibre, garde min. 2 écritures côté client, erreurs métier `LETTERING_*` traduites, lecture seule sur période clôturée, délettrage par lettre ; **(5) Exercices & clôture** — ouverture par année, clôture IRRÉVERSIBLE derrière une confirmation « taper l'année », restitution du résultat (report à nouveau, écritures, périodes figées) ; **(6) Banque & rapprochement** — import CSV de relevé (multipart, période+référence), KPIs de rapprochement (lignes matchées, montants, écart de clôture), rapprochement auto, matching manuel ligne→paiement (proposition du moteur signalée), export CSV ; **(7) TVA & états financiers** — déclaration TVA par période (collectée/déductible/nette, détail par taux, export CSV), bilan par sections PCG avec invariant actif = passif+capitaux affiché, compte de résultat (produits/charges/résultat net). Routeur + sidebar : 7 routes et 7 entrées de sous-menu « Comptabilité » (#7725 conservé). i18n ×4 locales admin (fr/en/tr/ar, ~280 clés `accounting.*` + `navigation.*`, parité vérifiée par script). **Vérifié** : `npm ci` OK, `eslint .` 0 erreur, `vite build` + garde CSP verts, parité de clés i18n ×4 verte, clés utilisées par les vues toutes présentes (audit scripté). **Non exécuté localement** : ce package n'a ni TypeScript (`tsc --noEmit` sans objet) ni suite Jest (la FR-3 de la spec visait le front Next.js) — seuls les e2e Playwright existent (non lancés en sandbox, aucun spec existant ne couvre le sous-menu comptable), à confirmer par la CI.
- **feat(restaurant): RESTO-901 — profil public par branche + annuaire public des restaurants avec recherche & proximité (Closes #7746).** Chaque succursale peut désormais publier un profil public **opt-in** (`is_public`, défaut false) : migration additive sur `restaurant_branches` (`public_slug` unique GLOBAL cross-tenant, `establishment_type` — nouvelle enum `RestaurantEstablishmentType` ×8 valeurs —, `cuisine_types` JSON, `public_description`, `cover_image_url`, `latitude`/`longitude` decimal(10,7), index `(is_public, latitude, longitude)`) et sur `restaurant_products` (`is_published_online`, défaut false, indexé). **Endpoints privés** (pile tenant) : `GET/PUT /restaurant/branches/{branch}/public-profile` (validation stricte, slug généré depuis le nom avec suffixe numérique en cas de collision cross-tenant, slug explicite en collision → 422 ; autorisation `RestaurantBranchPolicy` — pattern ressource-scopé #7599) et `PATCH /restaurant/products/{product}/publication` (`RestaurantProductPolicy`). **Endpoints publics** (pattern BC-27/BC-28 : `throttle:shop-public`, AUCUN middleware tenant/Sanctum, 404 fail-closed, DTO public strict sans `company_id` ni ID interne — le slug est l'identifiant) : `GET /public/restaurants` (filtres q/city/type/cuisine, proximité `near=lat,lng` + `radius_km` défaut 10 max 50 par **formule haversine SQL** bornée least/greatest, tri distance sinon nom, pagination max 50/page, requête cross-tenant qualifiée `shared_tenants.*` — pattern PlatformMetricsOverview — filtrée sociétés ni suspendues/expirées + flag `restaurantmanager`) et `GET /public/restaurants/{slug}` (profil + horaires `restaurant_hours` + menu PUBLIÉ uniquement — `is_published_online` ET `is_available` ET actif —, cache serveur `RestaurantPublicDirectoryCache` TTL 10 min invalidé à la mutation du profil, ancien et nouveau slug purgés). OpenAPI +5 paths (tag Restaurant) + miroir `dev-hub/openapi/v1.yaml` et SDK régénérés. **Vérifié localement (PHP 8.4 rootless #7585)** : `php -l` OK sur les 16 fichiers, **PHPStan strict `[OK]` sur `app/Modules/RestaurantManager`**, gardes migrations (collisions de basename + duplication `Schema::create`), module-isolation et unrouted-controllers vertes. **Non vérifié localement (pas de PostgreSQL dans la session)** : les 2 nouvelles suites Feature (`RestaurantBranchPublicProfileTest` ×7 cas, `RestaurantPublicDirectoryTest` ×5 cas) — à confirmer par le check requis `Tests - Leopardo RH`.
- **feat(accounting): les 7 écrans web du rôle comptable dans la console admin — documents/factures, plan comptable, grand livre/journal, lettrage, exercices & clôture, banque/rapprochement, TVA & états financiers (Closes #7776, reprise de la spec `.specify/features/5534-accounting-web-ui/spec.md`).** Le backend comptabilité (~50 endpoints `api/routes/modules/accounting.php`) n'avait que 3 écrans côté `front/admin-dashboard` (Dashboard, Activation, Settings) : l'expert-comptable devait appeler l'API à la main. Livré : **(1) `AccountingDocumentsView`** — liste filtrable (type/statut/période), création de brouillon avec lignes dynamiques + aperçu du prochain numéro (`/documents/next-number`), détail (lignes, totaux HT/TVA/TTC, paiements), envoi, annulation motivée (dialog in-app, jamais `confirm()` natif — #3494), avoir lié pré-rempli, comptabilisation au journal, enregistrement de paiement (trésorerie #5229). **(2) `AccountingChartView`** — comptes classes 1-8 (filtre type, badge Système), création, activation/désactivation, suppression des seuls comptes libres (les comptes système ne proposent pas la suppression, US-2). **(3) `AccountingLedgerView`** — onglets Grand livre (running balance + solde d'ouverture + filtre compte + pagination), Balance (totaux généraux + indicateur d'équilibre) et Journal (équilibré/clôturé, export CSV expert, clôture de période confirmée), bouton **export FEC** (US-3). **(4) `AccountingLetteringView`** — sélection multi-écritures + lettre, garde locale < 2 sélections (aucun appel API, US-6.3), délettrage, lecture seule sur période clôturée. **(5) `AccountingFiscalYearsView`** — ouverture d'exercice, statuts, clôture avec confirmation irréversible (US-5). **(6) `AccountingBankView`** — import de relevé CSV (période + référence), rapprochement auto, matching manuel ligne→paiement (proposition du matching auto en tête de liste), état (rapprochées/en attente/écart), export CSV. **(7) `AccountingStatementsView`** — onglets Bilan (sections PCG emboîtées + invariant actif = passif + capitaux), Compte de résultat (produits/charges/résultat net) et **déclaration TVA** (collectée/déductible/net + détail par taux + export CSV). Ergonomie : hub de navigation rapide sur le dashboard comptable (US-1), sous-menu Comptabilité complet dans la sidebar (pattern #7725), design system existant (glass-card, RowActionButton #7434, dialogs in-app), i18n **×4 locales** (≈105 nouvelles clés `accountingModule.*`/`bankRecon.*` + 7 clés `navigation.*` admin, catalogues propagés par les 3 synchroniseurs, `I18N_VALIDATION_OK`). **Vérifié localement** : `eslint .` 0 erreur/0 warning, `vite build` OK (garde CSP verte), garde action-labels #7434 verte, parité i18n ×4 verte, `validate.js` OK. **Non livré ici (assumé)** : tests unitaires front — `front/admin-dashboard` n'a pas de harnais Jest/Vitest sur main (il vit sur `bc/bc01-admin-web-plateforme`) ; la couverture e2e Playwright suivra l'atterrissage du harnais.
- **fix(db): consolidation des migrations divergentes CRM/Fuel/EdgeSync/Restaurant — la dérive schéma↔modèles/tests qui tenait la suite backend au rouge est soldée (Refs #7452).** La dédup « une table, une migration » étant terminée (garde `--strict` verte), le rouge restant venait de la **génération perdante encore attendue par le code, les tests et les fixtures** : `column "legal_name" of relation "crm_accounts" does not exist` (28×), `relation "fuel_stock_reconciliations" does not exist` (6× — les 3 tables `fuel_deliveries`/`fuel_stock_movements`/`fuel_stock_reconciliations` avaient perdu leur UNIQUE déclaration lors de la dédup #7572), colonnes de liaison `uuid` inécrivables face à des PK bigint (22P02 sur `crm_opportunities.pipeline_id/lead_id/owner_id`, `crm_automations.created_by`, `crm_leads.account_id/owner_id`, `crm_export_jobs.user_id`), NOT NULL jamais renseignés par les écrivains réels (`crm_pipelines.stages`, `fuel_meter_intervals.previous/current_reading_id`, `travel_passengers.class_id`), et des **tests destructeurs** qui droppaient les tables canoniques en tearDown (4 suites CRM, 8 suites Edge — depuis le cache de fixture #6928 le setUp suivant ne rebâtit plus rien → cascade « relation does not exist » sur tout le worker). **Correctifs** : 3 migrations correctives idempotentes forward-only (pattern `2026_09_15_001600_7452` : `Schema::table` gardé `schemaHasColumn`, `DROP NOT NULL` rejouable, re-typage uuid→bigint gardé par le type courant avec mapping NULL des valeurs non numériques — documenté sans perte car ces colonnes étaient inécrivables) + alignement des `Schema::create` sources pour les bases fraîches ; modèle `CrmPipeline` créé (la table existait, les tests de scoping le référencent) + défauts d'attributs `CrmPipelineStage` ; écrivains corrigés (`TravelLegacyImportService` omettait `arrival_date`/`arrival_time` NOT NULL sur les trips — le `code` des routes/trips ayant été posé en parallèle par #7783, `CrmDashboardReadModel` `whereRaw('AND …')` → 42601 et `having('cnt')` → 42703, `CrmReadModelService` lisait `crm_leads.name` qui n'existe pas) ; factories complétées (`TravelOutboxEvent`, `RestaurantBranch`/`Category`/`TaxRate`/`Product` : `company_id` NOT NULL hors contexte tenant) ; fixture MVP alignée (`sync_queue.dedup_key` + unique #6554 dans le SQL et le PHP, stubs des 3 tables Fuel restaurées, helper `recreateCanonicalEdgeNodesTable()` pour les tests Edge legacy — restauration ciblée au lieu d'un rebuild fixture ~30 s/test) ; tests fixture-era réalignés sur le schéma canonique (ids uuid forcés, `contact_id` non numériques, consents sans `source`/en double sur la contrainte unique, import `SyncEngineService` au mauvais namespace). **Mesuré avant → après** (PHP 8.4 + PG14, runs groupés par verticale) : CRM **104 → 42** rouges/236, Edge **7 → 3** (plus aucune erreur), EdgeSync 5 → 4, Restaurant 91 → 84, Travel 144/593 → 134/611 (mesure post-merge de #7783/#7778, +18 tests), Fuel 48 → 46 — total verticales **402 → 316**, et surtout **plus aucun `column/relation does not exist` ni 23502/22P02 de dérive schéma** dans CRM/Travel/Fuel/Restaurant : le reliquat est la dette **non-schéma** déjà cartographiée dans #7452 (T2 : `TypeError`/constantes de génération croisée, T3 : ~300 échecs d'assertion de contrats API, + cascades 25P02 propres aux runs groupés #7469). **Vérifié** : migrate fresh public+tenant vert + **rejeu idempotent** vert (recette Render), `check-duplicate-schema-create.py --strict` vert (385 tables, 0 dupliquée), `check-migration-basename-collisions.sh` vert, `check-mvp-schema-parity.sh` vert, PHPStan strict `[OK]` sur app/Modules/CRM + TravelLegacyImportService, Pint PASS sur les 33 fichiers touchés.
- **chore(repo): tranche finale du volet immédiat de l'audit #7654 — `.gitignore` racine durci sur toutes les catégories d'artefacts purgées et garde CI élargie (Closes #7654).** Les tranches 1–3 (PR #7679, #7692, #7735) avaient sorti du tree tous les artefacts dénoncés par l'audit (`api/composer.phar`, `ci_log.txt`, `build/test_cache/*.dill` 52,7 MB, `CHANGELOG_ARCHIVE.md` 4,3 MB — archive consultable dans l'historique [@6a3819a](https://github.com/kitokoh/leopardo-hr/blob/6a3819a9254d31d5f124c3e14bd3cdd5f31848c8/CHANGELOG_ARCHIVE.md) —, `docs/api-mock-data/`, miroir OpenAPI + SDK générés), mais le `.gitignore` racine ne bloquait pas encore toutes les catégories à la source : seul `composer.phar` (pas `*.phar`), rien pour `test_cache/`, `*.dill` ni les `build/` d'apps mobiles (seuls les `.gitignore` par app les couvraient — le blob de 52,7 MB prouvait que ça ne suffit pas). Désormais : `*.phar`, `test_cache/`, `*.dill` et `front/mobile_apps/*/build/` sont ignorés à la racine, et `repo-hygiene-guard.yml` refuse en plus `*.phar` (généralisé) et `front/mobile_apps/*/build/*` sur toute PR (actionlint OK, garde rejouée localement : zéro artefact interdit traké, zéro fichier traké nouvellement ignoré). Les fichiers générés VOLONTAIREMENT conservés (`api/openapi.yaml` servi au runtime, catalogues i18n cibles importés au build) ne bougent pas ; leur plan de sortie et la réécriture d'historique (pack 93 MiB → < 15 MiB) restent des décisions propriétaire tracées dans `docs/ops/REECRITURE_HISTORIQUE_7654.md` et `docs/ops/ACTIONS_PROPRIETAIRES.md` (décision 9).
- **feat(travel): pont RH employee_id + affectations d'équipage chauffeur/guichetier/contrôleur — le manifeste de voyage liste enfin l'équipage (Closes #7638).** La décision d'architecture de `SOLUTION_TRAVEL_AGENCY.md` §685 (« HR reste propriétaire des employés ; la verticale référence employee_id par valeur ») n'était pas implémentée : aucun modèle Travel ne portait d'`employee_id`, le manifeste ne listait que les passagers, et le RBAC restait limité aux managers globaux. Nouvelle table tenant `travel_staff_assignments` (`company_id` NOT NULL, `employee_id` **par valeur** — aucune FK vers `employees`, l'appartenance au tenant est validée au FormRequest —, rôle métier travel `driver|agent|controller|office_manager`, scope **exactement un** de `office_id`/`trip_id`, statut `active|revoked` avec `revoked_at`/`revoked_by_user_id` — une révocation ne supprime jamais la ligne, l'historique des manifestes passés est préservé) ; une table = une migration, garde `schemaTableExists` anti-collision. Endpoints CRUD `/travel/staff-assignments` (index filtrable `employee_id`/`office_id`/`trip_id`/`role`/`status`, doublon actif employé×rôle×scope → **409**, révocation par `POST …/{id}/revoke` idempotente — acte métier, convention #4930) + `TravelStaffAssignmentPolicy` composée avec le RBAC ressource-scopé #7600 (trait `ChecksResourceScopedAccess`, clé `travel_office` : un responsable porteur de `manage` sur SON bureau gère son équipage, historique principal/rh sinon ; lecture ouverte au tenant) — la PR #7632 étant mergée, aucune attente. **Manifeste enrichi** : `GET /travel/trips/{trip}/manifest` ajoute la clé top-level `crew` (affectations ACTIVES du trajet, nom RH résolu via Core\Auth\Employee — la verticale lit Core, jamais l'inverse) sans toucher au contrat `data` existant (rétro-compatible). Contrat OpenAPI DOCUMENTÉ (les 6 routes + schéma `TravelStaffAssignment` dans `api/openapi.yaml`, plutôt que l'allowlist du pattern EPIC #6528 — entrées purgées, exigence `--strict-staleness` ; miroir `dev-hub/openapi/v1.yaml` + SDKs JS/Python régénérés, Redocly lint valide). **Vérifié** : `pint --test` PASS et **PHPStan strict `[OK] No errors`** sur les fichiers touchés (PHP 8.4 rootless) ; nouvelle suite `TravelStaffAssignmentTest` (12 cas : affectation bureau et voyage, révocation idempotente, 409 doublon, scope exclusif, employé/bureau étrangers → 422, ressource étrangère → 404, index isolé par tenant + filtres, employé self-service → 403 en écriture, manifeste listant l'équipage actif seul avec nom RH, 401 sans auth) — **PHPUnit non exécuté localement (pas de PostgreSQL dans la sandbox), à confirmer par le check requis `Tests - Leopardo RH`**.
- **feat(support): notifications email des tickets client↔plateforme — nouveau ticket, réponse support, réponse client (Closes #7760, spec MISSION_ESPACE_CLIENT_DELEGATION_BILLING_SUPPORT §3.2).** Le centre de support pilote (PA2-COMM-012) ne déclenchait **zéro e-mail** : un ticket ouvert restait invisible tant qu'un super-admin n'ouvrait pas la console, et le client ne savait jamais qu'on lui avait répondu. `PlatformSupportTicketService` déclenche désormais, APRÈS commit de la transaction (jamais de notification fantôme sur rollback) : **(a)** nouveau ticket → tous les super-admins ACTIFS porteurs de la permission plateforme `support.manage` (matrice `PlatformRole::permissions()`, #7553 — `super_admin`, `admin`, `support` ; jamais `marketing`/`finance`/`ops` ni un compte suspendu) ; **(b)** réponse super-admin → employé auteur du ticket, dans SA langue (préférence employé sinon langue du tenant, même résolution que `UserInvitationMail`) ; **(c)** réponse employé → super-admin assigné (`assigned_super_admin_id`, s'il est actif) sinon l'équipe `support.manage`. Trois Mailables `ShouldQueue` (envoi en file) sur le mécanisme canonique `EmailTemplateResolver` (#7347 — surcharge admin possible, registre `EmailTemplateRegistry` +3 templates, défauts i18n ×4 locales dans `api/lang/{fr,en,ar,tr}/emails.php`, templates blade sur le layout canonique) ; **le sujet porte la référence du ticket** (`[Ticket #N] …`) et le corps ne recopie jamais le message (l'e-mail est une notification, la conversation se lit dans l'espace client / la console). Nouvelle suite `SupportTicketEmailNotificationTest` (5 tests, `Mail::fake()`) : les 3 déclencheurs, le repli équipe sans assigné, l'exclusion des rôles sans `support.manage` et des comptes suspendus, **aucun destinataire d'un autre tenant ne reçoit rien**, et la référence du ticket dans le sujet. **Vérifié localement** : `php -l` OK sur les 10 fichiers PHP touchés, gardes `check-module-isolation`/`check-layer-purity`/`check-i18n-catalog-parity` vertes. **Non vérifié localement (PHP 8.4/PostgreSQL absents de la sandbox)** : PHPStan strict et PHPUnit — à confirmer par la CI.
- **feat(support): espace tickets support côté client — liste, création, fil de messages, clôture (Closes #7759, spec MISSION_ESPACE_CLIENT_DELEGATION_BILLING_SUPPORT §3.2).** L'API tenant `/api/v1/support-tickets` (PA2-COMM-012) existait et était testée, mais **aucune UI** ne la consommait dans `front/web` (les clés i18n `support_tickets` étaient orphelines) : le client ne pouvait ni ouvrir un ticket ni lire les réponses de la plateforme. Nouvelle page `(dashboard)/support` sur les patterns de `settings/team` (#7555) : liste des tickets avec statut/catégorie/priorité/dernière activité et filtres par statut, formulaire de création (sujet, catégorie `general|billing|technical|onboarding|other`, priorité `low|normal|high|urgent`, message), fil de messages (plateforme vs entreprise) avec réponse, et **clôture avec confirmation inline** (jamais de `window.confirm`) ; un ticket clos masque le formulaire de réponse et le 422 API (`TICKET_ALREADY_CLOSED`) est mis en mots. Entrées de menu Support (tiroir mobile + menu utilisateur, tous les rôles pour l'instant — la restriction par grant `support` arrive avec la délégation par module §3.1) ; `/support` rejoint `PROTECTED_PREFIXES` (source unique #3377), le gate de session `DASHBOARD_PREFIXES` de `proxy.ts`, `sw.js` et robots.txt (garde anti-oubli #7663 verte). i18n : clés `support_tickets.*` orphelines réutilisées et complétées ×4 locales dans `shared/i18n` (syncs web/mobile/backend rejoués, `I18N_VALIDATION_OK`), repli FR centralisé dans `src/lib/i18n/support-tickets.ts` (même mécanique que `team-roles.ts`, garde PA2-I18N-014 respectée). **Vérifié** : `eslint src --max-warnings 0` OK, `tsc --noEmit` 0, **Jest 128 suites / 1116 tests verts** (dont 9 nouveaux : liste+badges, filtre statut, état vide, création → fil ouvert, validation espaces, fil+réponse, ticket clos, 422 mis en mots, clôture confirmée), `check:mojibake` OK.
- **feat(travel): API publique MARKETPLACE inter-agences — recherche agrégée cross-tenant des trajets publiés et tunnel d'achat résolu par trajet (Closes #7737, épic #7736).** La boutique publique restait MONO-tenant (TRAVEL-1001/#6114 : jeton `X-Travel-Shop-Token` détenu par UNE agence) : le grand public ne pouvait pas comparer les départs de plusieurs agences. Nouvelle surface `/api/v1/public/travel/marketplace/*` SANS jeton d'agence (throttle `shop-public`, aucune auth) : **(1) opt-in borné** — seules les agences au jeton boutique ACTIF **et** à la feature `travelagency` active (kill switch plateforme respecté) sont visibles, aucune migration d'opt-in nécessaire ; **(2) villes desservies** (`GET /marketplace/cities`) et **recherche agrégée** (`GET /marketplace/trips?origin_city_id&destination_city_id&date`) — `travel_cities` étant un référentiel PAR TENANT, l'agrégation matche par identité géographique (nom insensible à la casse + pays ISO2), chaque requête étant explicitement contrainte `whereIn(company_id, éligibles)` (jamais de lecture non bornée) et n'exposant que prix/sièges disponibles/villes/NOM public de l'agence ; **(3) tunnel d'achat résolu par trajet** — détail + plan de sièges (`GET /marketplace/trips/{trip}`, 404 fail-closed hors opt-in) et réservation (`POST /marketplace/bookings`) qui pose le tenant du trajet via `TenantManager::withinTenant()` + marqueur `tenant_scope_required` (pattern `EnsurePublicShopAccess`) puis délègue au flux TRAVEL-1001 existant (`CreateBookingAction` : verrouillage sièges, montant serveur, idempotence) avec la nouvelle source `booking_source=marketplace` (contrainte CHECK étendue par migration dédiée) ; **(4) paiement** (`POST /marketplace/payments/initiate`) résolu par RÉFÉRENCE de réservation (référence ambiguë cross-tenant → 404 fail-closed, pattern #7395), même contrat idempotent que TRAVEL-1002 ; suivi/annulation/e-billet réutilisés tels quels (référence + code de validation) via des alias `/marketplace/*`. La boutique mono-agence est INCHANGÉE. OpenAPI +8 paths (miroir dev-hub + SDK régénérés, garde de couverture 0 drift, Redocly valide). Nouveau `TravelMarketplaceApiTest` (6 cas Feature multi-tenant : agrégation ≥ 2 agences sans jeton, exclusion jeton inactif/feature absente, déduplication des villes, 404 fail-closed sur trajet non opt-in, réservation créée chez la BONNE agence et invisible ailleurs + idempotence, paiement résolu par référence). **PHPUnit/PHPStan non exécutés localement (PHP 8.1 sans vendor dans l'image d'agent) — à confirmer par la CI.** — `CHANGELOG_ARCHIVE.md` (4,2 Mo) et `docs/api-mock-data/` (56 JSON générés) sortis du dépôt, fichier tracké-ignoré `.specify/.../.composed/speckit.specify.md` dé-tracké.** L'archive reste intégralement consultable dans l'historique git ([version complète @6a3819a](https://github.com/kitokoh/leopardo-hr/blob/6a3819a9254d31d5f124c3e14bd3cdd5f31848c8/CHANGELOG_ARCHIVE.md)) ; le processus de release (CONVENTIONS.md §4.3, AGENTS.md) archive désormais dans les notes de release GitHub au lieu d'un fichier versionné qui regonflait le pack à chaque release. Les exemples API se régénèrent à la demande (`python dev-hub/tools/generate_api_examples.py`, désormais gitignoré) — READMEs mobiles et docs/README.md mis à jour, exclusion obsolète retirée de `check-canonical-domains.sh`. Réparation au passage : deux entrées de changelog flottaient AU-DESSUS du header `## [Unreleased]` (résolution de merge défaillante — le symptôme même que #7654 vise) : réintégrées sous le header. Le reste de #7654 (miroir OpenAPI/SDK et catalogues i18n générés au build, réécriture d'historique ~1,3 Go) demande des arbitrages propriétaire et reste dans l'issue.

### Added
- **feat(retail): paiement en ligne du checkout marketplace Leopardo Marché — mobile money / PSP, BC-21 pragmatique (Closes #7812, épic Leopardo Marché, spec `docs/specifications/MARKETPLACE_RETAIL_PUBLIC.md` §8).** Le checkout public `POST /public/market/orders` accepte `payment_method: cash|online` (COD reste le défaut). En ligne : intent de paiement `retail_online_payment_intents` (migration tenant idempotente sans FK — référence 64 hex unique par tenant, provider, `amount_minor`, statut `pending|processing|succeeded|failed|expired|refunded`, `checkout_url`, `provider_payload` auditable, idempotence par commande) et réponse enrichie `payment.{method,intent_reference,status,checkout_url}` (rejeu → même intent). **Abstraction provider locale au module Retail** (`RetailPaymentProviderInterface` + `Infrastructure/Payments/` : `ChargilyProvider` — Chargily Pay v2, même intégration que Accounting #5272 — et `MockProvider` tests/dev ; registre pattern RESTO-406), sélection par `config('retail.payments.provider')` + credentials env (`RETAIL_PAY_CHARGILY_SECRET`…, **aucun secret en dur**) — les profils de paiement tenant BC-21 (PR #7732, non mergé) se brancheront dans le registre sans changer les appelants. **Webhook signé fail-closed** `POST /public/market/payments/webhook/{provider}` : signature HMAC-SHA256 du corps brut OBLIGATOIRE (invalide/secret absent → 401 sans effet, provider inconnu → 404), idempotent (rejeu → 200 sans double effet), contrôle anti-fraude du montant ; `succeeded` → intent + commande payée (`payment_status`/`paid_at` via ALTER idempotent sur `retail_orders` + trace d'encaissement `RetailOrderPayment` online/captured, cohérence POS #7674) ; `failed`/`expired` → statut correspondant, commande non payée. **Réconciliation** `retail:payments:reconcile [--minutes=]` (même voie applicative que le webhook, transactionnelle + verrou). **Remboursement vendeur** `POST /retail/online/orders/{id}/refund` (RetailOrderPolicy@pay, uniquement intent `succeeded`, trace auditable). Suivi public : `payment.{method,status}` fail-closed. **Front `front/marketplace`** : choix « Paiement à la livraison / Paiement en ligne » au checkout, redirection vers la `checkout_url` du PSP (multi-boutiques → bouton « Payer en ligne » par commande sur /confirmation), nouvelle page `/paiement/retour` (jeton relu en localStorage — jamais transmis au PSP —, poll du statut, états payé/en attente/remboursé/erreur, FR mobile-first), statut de paiement sur `/suivi`. **Vérifié localement (PHP 8.4 + PG14)** : nouvelle suite `RetailOnlinePaymentTest` ×11 (checkout online → intent + rejeu idempotent, webhook signé valide → payé, signature invalide → 401 sans effet, rejeu webhook sans double effet, failed → non payée, montant incohérent ignoré, réconciliation console, refund RBAC + isolation tenant, méthode inconnue → 422) — dossier `tests/Feature/Retail` **53/53 verts** ; PHPStan strict `[OK]` sur `app/Modules/Retail` + tests + migrations ; gardes collisions de basename et duplication `Schema::create` vertes ; `front/marketplace` : `eslint --max-warnings 0` et `tsc --noEmit` verts.
- **feat(cameras): application mobile « Leopardo Caméras » — liste des caméras, direct, événements et alertes (Closes #7426, BC-19 DEVICE).** Le module Surveillance Caméras (API `/cameras/*`, #7424/#7425/#7427) n'avait AUCUNE surface mobile — seule une entrée `coming_soon` à `route: null` dans `MobileExperienceService` mentait à l'interface. Nouvelle app dédiée `front/mobile_apps/leopardo_cameras` (option A de l'issue, recommandée PM : cible le propriétaire/agent de sécurité/gardien qui ne veut QUE les caméras), calquée sur `leopardo_travel_agent` (Riverpod 3 + go_router + `leopardo_core` partagé, StartupGate premier widget, `requestWithRetry`/`extractDataList`, thème clair/sombre `TenantTheme`, i18n ×4 fr/ar/tr/en avec RTL — catalogue local `AppStrings`, pattern #5236) : **(1) mur des caméras** du tenant (GET /cameras, respect du flag `cameras` + `api.manager`) ; **(2) direct** par caméra (GET /cameras/{id}/stream-token — même contrat que la surface web #7425 : `stream_url` WebRTC MediaMTX + jeton JWT signé à durée limitée, régénérable ; le lecteur WebRTC embarqué suit dans une itération dédiée) ; **(3) événements** (GET /cameras/events — indicateur `has_snapshot` seul, l'API n'expose aucun chemin RGPD) avec route `/events?focus=<id>`, cible du deep link notification (#7427) ; **(4) alertes** dédupliquées (GET /cameras/alerts) avec acquittement/clôture idempotents et ouverture de l'événement lié. **Refus explicites, jamais d'écran vide (AC 2)** : 403 `FEATURE_NOT_ENABLED` → « Module Caméras non activé », autre 403 → « Accès refusé ». **Aucun identifiant RTSP stocké ni journalisé côté mobile (AC 4)** : seuls le token Sanctum (stockage sécurisé) et le stream-token éphémère (jamais persisté) transitent. **`MobileExperienceService` ne déclare plus `coming_soon` pour `cameras` (AC 5)** : module `active` servi sans route (précédent `dashboard_admin` #2212 — carte non cliquable, aucun crash GoRouter ; garde `check-mobile-manifest-routes.sh` rejouée verte). Intégration : `melos.yaml`, matrices analyze/test + build debug de `mobile-apps-ci.yml` (artefact `leopardo-cameras-debug-apk`), `front/mobile_apps/README.md` et AGENTS.md (8 apps) ; fichiers auth préfixés `cameras_*` pour le garde anti-duplication core<->apps #7652 (aucune nouvelle entrée de baseline, vérifié par rejeu local de `check-mobile-core-duplication.py`). Tests : parité i18n ×4 + libellés des enums du contrat, modèles (payloads complets/minimaux, garde « pas de champ RTSP », cycle open→acknowledged→resolved), widgets du mur (AC 1 + les deux refus explicites + mur vide). **Non vérifié localement (Flutter absent de l'image d'agent)** : `flutter analyze`/`flutter test`/build debug reposent sur la CI mobile ; distribution Firebase App Distribution volontairement hors lot (les apps accounting/travel_agent n'y sont pas non plus — provisionnement app Firebase = action propriétaire).
- **feat(billing): emails « facture émise » et « reçu de paiement » au principal du tenant, PDF joint, i18n ×4 (Closes #7763).** Aucun email de facture n'existait : `GenerateMonthlyInvoices` envoie désormais `InvoiceIssuedMail` (queue, `ShouldQueue`) au principal (`manager_role=principal`, même identification que les drips de trial) après commit de chaque facture, et un `InvoicePaymentReceiptMail` part quand une facture transite vers `paid` — branché au **point unique** `Invoice::transitionTo(Paid)` (nouvel event `InvoicePaid` + listener `SendInvoicePaymentReceipt`), donc commun aux webhooks Stripe ET Chargily, et jamais re-dispatché sur la re-transition idempotente `paid → paid` d'un webhook rejoué (zéro reçu en double, en plus du registre #5444). Contenus i18n fr/en/ar/tr via `EmailTemplateResolver` (templates `invoice_issued` / `invoice_payment_receipt` surchargeables depuis l'admin #7347), PDF généré par le MÊME code que `GET /billing/invoices/{id}/pdf`, factorisé dans `InvoicePdfRenderer` (Infrastructure). Un email manquant (pas de principal) est journalisé et ne fait JAMAIS échouer la génération ni un webhook.
- **fix(restaurant): savepoint sur le test de contrainte pos_sessions — répare le 25P02 introduit par #7729 (suite #7717).** Dans `RestaurantPosSessionPartialUniqueTest::test_single_open_session_per_branch_is_still_enforced_by_the_database` (mergé via #7729), l'INSERT violant l'index unique partiel `(company_id, branch_id) WHERE status='open'` laissait la transaction posée par RefreshDatabase en état « aborted » : le `SET search_path` du tearDown échouait alors en **25P02** (`current transaction is aborted`) et le test était rouge sur main. Correctif : l'insertion fautive est isolée dans une transaction IMBRIQUÉE (savepoint) via `DB::transaction` — la violation 23505 y est rollbackée et la transaction du test reste saine (pattern repo établi par le constat #6954, identique à `AbsenceTypesTenantUniqueTest`). Aucun changement de code applicatif, aucun changement de couverture : l'invariant « une seule session OUVERTE par branche » reste vérifié au niveau BASE (QueryException attendue) et au niveau API (409). **Vérifié localement (PHP 8.4 + PostgreSQL 14)** : reproduction du 25P02 sur main, puis suite `RestaurantPosSessionPartialUniqueTest` **3 tests / 10 assertions verts** avec le correctif.
- **fix(payroll): `PaySlipCabinetArchiveTest` repasse au vert — le job d'archivage Cabinet est exécuté SOUS contexte tenant dans les tests (Closes #7721).** Depuis #7712, `company_id` des modèles Cabinet n'est plus fillable : il est posé par le trait `BelongsToCompany` depuis le tenant actif (en prod, le middleware de queue `EnsureTenantContext` l'établit). Les 4 invocations directes de `ArchivePaySlipsToCabinetJob::handle()` du test créaient donc des documents `company_id = NULL` : `test_read_only_payslip_cannot_be_deleted` attendait 403 et recevait 404 (le scope tenant du DELETE ne voyait plus le document) — le workflow « Tests module Payroll » était rouge sur `main` (reproduit sur `749c551a5` pur). Correctif **test-only** : helper `runArchiveJob()` qui enveloppe le job dans `TenantManager::withinTenant($company, …)`, miroir exact du middleware de prod. **Vérifié** : suite 4 tests / 20 assertions verte, `pint --test` PASS, aucun code de production touché.
- **fix(tenant): les ~14 derniers modèles tenant-scopés raccordés à BelongsToCompany + exceptions plateforme documentées (Closes #7711, suite #7646/#7678/#7710).** Dernière tranche de la classification #7646, pattern Cabinet appliqué : **13 modèles raccordés en pattern strict** (trait + `company_id` HORS `$fillable`) — `Attendance/{ApprovalRequest, ApprovalWorkflow, AttendanceCorrectionRequest, AttendanceModeSettings, EmployeeAttendancePreference, EmployeeLocationEvent, KioskAnnouncement, ZktecoDevice}`, `HR/ExportHistory`, `Notification/DeviceToken`, `Payroll/LoanRepayment`, `Planning/ClientEvent`, `EdgeSync/EdgeLicense` — avec délégation du `company_id` au trait dans leurs 10 writers (contrôleurs/services sous tenant). **Flux pré-tenant préservés et prouvés par test** : le lookup device ZKTeco par `serial_number` (`AuthenticateZktecoDevice`, heartbeat/sync hors middleware tenant) passe par `withoutGlobalScope('company')` explicite ; le flux kiosque #7651 lit `kiosk_announcements` en Query Builder brut (non affecté) ; les routes machine Edge (`/edge-node/*`, validate-license) restent hors tenant → scope neutre, et `EdgeLicenseService::issueLicense` passe en `firstOrNew`+`forceFill` (pas de licence orpheline hors tenant). **`Core/Auth/AuditLog` raccordé avec DÉROGATION documentée** : le trait apporte l'isolation en LECTURE tenant + neutralisation du spoof, mais `company_id` RESTE fillable — 30+ writers plateforme (`PlatformUserController`, `PlatformTeamController`, consumers Platform*, actions Showcase…) créent via `create()` hors contexte tenant où le trait ne peut pas remplir, et NULL = événement plateforme par design. **`Payroll/Commission` TRANCHÉ plateforme (pas de trait, commenté dans le modèle)** : table du schéma PUBLIC (migrations `public/`, module Growth), lecteurs légitimement cross-tenant (dashboard partenaire multi-sociétés filleules — pourtant sous middleware tenant, un scope global l'aurait vidé — et admin Growth). **Exceptions documentées par commentaire** (pas de trait) : `Company` (la frontière elle-même), `PlatformAnnouncement`/`PlatformImpersonationSession`/`PlatformSupportTicket` (surfaces super-admin — et `company_id` RETIRÉ du `$fillable` des deux derniers, leurs writers passent en `forceFill` de valeurs serveur), `PublicHoliday` (férié national `company_id NULL`), `UserEmployeeLink` (résolu au login, pré-tenant), `Billing/Partner`+`PartnerReferral` (Growth plateforme), `EdgeSync/EdgeNode` (SQLite edge + routes machine). **Une migration révélée nécessaire** : `device_tokens` n'avait JAMAIS reçu la colonne `company_id` en migrations tenant (le schéma de test MVP l'avait, d'où le garde `Schema::hasColumn` du service — dérive réelle) → migration additive idempotente `2026_09_19_000001_7711` (uuid nullable + backfill depuis `employees.company_id` + index), garde anti-collision verte. Fixtures `create()` → `forceCreate()` (54 sites / 18 fichiers, pattern #7678). Nouvelle suite `RemainingModelsTenantIsolationTest` (9 tests : matrice scope/spoof/update sur 10 modèles + cas dédiés ApprovalRequest, LoanRepayment, EdgeLicense machine-flow, ZKTeco pré-tenant, dérogation AuditLog). **Non vérifié localement (ni PHP ni PostgreSQL dans la session d'agent) : PHPStan strict, Pint et PHPUnit reposent sur la CI.**
- **feat(branding): l'image de marque du tenant arrive dans l'espace client web, le thème du dashboard et les PDF (Closes #7713).** L'API `GET/PATCH /company/branding` existait (avec son écran mobile `company_branding`) mais aucune surface web ne l'exposait. **(1) Page « Image de marque »** (`/settings/branding`) : nom d'affichage, upload de logo (JPG/PNG/WebP, garde-fou 2 Mo côté client, aperçu immédiat), sélecteurs couleur primaire/accent (color picker + champ hex validé), mode de marque (default/light/dark/auto) et carte de prévisualisation en direct ; l'upload passe par un POST multipart `_method=PATCH` (PHP ne peuple pas `$_FILES` sur un PATCH multipart natif) avec idempotence désactivée, et `apiFetch` cesse de forcer `Content-Type: application/json` quand le corps est un `FormData` (la frontière multipart est posée par fetch) ; un 403 du PATCH (réservé managers `principal|rh`) est mis en mots ; entrée ajoutée au menu de compte et au tiroir mobile ; i18n `brandingPage.*` ×4 locales (catalogues partagés + syncs web/mobile/backend). **(2) Thème tenant sur le shell du dashboard** : couleurs primaire/accent exposées en CSS custom properties (`--tenant-primary`/`--tenant-accent`) à la racine, logo du tenant affiché à la place du badge LRH dans le rail métier et la barre du haut quand il existe — repli silencieux vers le thème par défaut si l'appel échoue ou sans branding. **(3) Logo et couleur dans les PDF** : nouveau helper `App\Support\PdfBranding` (chemin fichier LOCAL résolu via Storage — dompdf ne suit pas d'URL http —, couleur hex validée, toute défaillance → null/défaut sans exception) consommé par `invoice.blade.php`, `receipt.blade.php` et `payslip.blade.php` : logo en en-tête et en-têtes de tableau/bandeaux de section teintés par `metadata.branding.primary_color` ; sans branding exploitable le rendu reste STRICTEMENT identique (aucune migration). **Vérifié** : `tsc --noEmit` 0, `eslint` 0, `next build` OK, Jest des suites layout vertes, `I18N_VALIDATION_OK` ×4 + parité des catalogues. **Non vérifié localement (PHP absent de l'image d'agent)** : `php -l`, PHPStan, Pint et le nouveau `PdfBrandingTest` (5 cas, calqué sur `PaySlipPdfLocaleTest`/`BillingControllerTest`) reposent sur la CI.
- **fix(security): la supervision queue ne transporte plus AUCUN credential de prod dans les runners GitHub — drain CI supprimé, supervision rétrogradée en sonde HTTP (Closes #7694, volet sécurité de #7649).** `queue-worker-fallback.yml` et `queue-supervision.yml` injectaient `DB_HOST`/`DB_USERNAME`/`DB_PASSWORD` **et `APP_KEY` de production** dans des jobs cron `*/5` sur `ubuntu-latest` (`php artisan queue:work`/`queue:health-check` directement contre la base Neon de prod) : surface d'exfiltration permanente — toute modification de ces workflows sur `main`, ou de n'importe quelle action qu'ils consomment, exécutait du code arbitraire avec les secrets DB prod 288 fois/jour, et `APP_KEY` en CI = capacité de déchiffrer sessions et champs chiffrés hors de tout périmètre de prod. **(1) Drain supprimé** : `queue-worker-fallback.yml` est retiré (un CI n'est pas le worker de secours d'une prod — #7649) ; le drain de prod reste le mono-conteneur Render (`docker-entrypoint.sh`), le worker dédié restant à provisionner (#7649, bloqué billing). **(2) Supervision sans credentials** : `queue-supervision.yml` devient une sonde `curl`+`jq` sur `GET $APP_URL/api/v1/health` — zéro checkout, zéro PHP, `permissions: {}`, seuls secrets restants `APP_URL` (skip explicite si absent, pattern conservé) et le webhook Slack opt-in ; les seuils du DoD #5282 (`QUEUE_MAX_PENDING`/`QUEUE_MAX_FAILED`/`QUEUE_MAX_STALE_MINUTES`) sont appliqués côté sonde (détection au pire inchangée : 5 min + stale 10 min = 15 min). **(3) `/health` étendu** : `checks.queue.oldest_reserved_seconds` (âge de la plus vieille réservation, via `QueueObservabilityService::oldestReservedAgeSeconds()` — `null` = aucune réservation en cours ou non mesurable, on ne devine pas) pour que la détection « worker mort » survive au retrait de l'accès SQL ; 2 nouveaux cas dans `HealthQueuePendingVsScheduledTest` (la plus ancienne de deux réservations gagne, `null` sans réservation). **(4) Garde anti-régression** : `check-queue-strategy-coherence.sh` (workflow `architecture-check.yml`, bloquant) — son contrôle n°5, qui **exigeait** le drain CI, est inversé : échec si `queue-worker-fallback.yml` réapparaît ou si un workflow référence `secrets.DB_*`/`secrets.APP_KEY`. Docs alignées (CI_CD_SECRETS — avec consigne de **rotation de `DB_PASSWORD` + `APP_KEY` prod et purge des secrets GitHub s'ils ont été posés** —, INCIDENTS I2, topologie Render, ARCHITECTURE, P07, render.yaml, README workflows). **Restes hors-repo (documentés dans la PR et l'issue)** : suppression des secrets DB/APP_KEY du dépôt GitHub, rotation, allowlist IP Neon. **Vérifié** : `actionlint` + `shellcheck` verts sur le workflow réécrit et la garde ; sonde rejouée contre un serveur HTTP simulé (5 scénarios : sain → exit 0, stale → 1, backlog+failed+ok=false → 1 avec les 4 violations listées, `APP_URL` absent → skip notice, API injoignable → 1 après retries) ; garde cohérence queue verte sur le dépôt modifié ; PHPStan strict `[OK] No errors` sur les 2 fichiers PHP touchés ; `pint --test` PASS ×3. **PHPUnit non exécuté localement (pas de PostgreSQL dans la sandbox)** — les 2 nouveaux cas `/health` à confirmer par la CI.
- **fix(restaurant): index unique partiel sur les sessions POS ouvertes — deux sessions fermées coexistent sur la même branche (Closes #7717, BC-25).** L'unique `(company_id, branch_id, status)` posé par la migration 2026_08_30_001527_6173 sur `restaurant_pos_sessions` interdisait deux sessions FERMÉES sur la même branche : `ClosePosSessionAction` écrit `status='closed'` sans suffixe, la **2e clôture** violait la contrainte (23505) — le POS restaurant cassait au 2e jour d'exploitation d'une branche (défaut constaté pendant l'audit Retail #7674). Correctif **forward-only** (pattern éprouvé côté Retail, migration 2026_09_19_000003_7674 sur `retail_pos_sessions`) : la migration 2026_09_19_000100_7717 droppe la contrainte unique et pose un **index unique PARTIEL Postgres** `(company_id, branch_id) WHERE status = 'open'` — l'invariant « une seule session OUVERTE par branche » est conservé (409 API + violation d'unicité en base), les sessions `closed`/`cancelled` coexistent librement ; idempotente (`IF EXISTS`/`IF NOT EXISTS`, garde `schemaTableExists`), aucun nettoyage de données requis (sous l'ancienne contrainte, au plus une ligne fermée par branche existe), `down()` ne recrée PAS la contrainte défectueuse. Aucun changement de code applicatif. Nouvelle suite `RestaurantPosSessionPartialUniqueTest` (3 tests : deux cycles ouverture→clôture consécutifs sur la même branche — le cas qui explosait —, 2e session `open` refusée au niveau BASE sur INSERT direct, et 409 API conservé). **Vérifié localement** : `check-migration-basename-collisions.sh` vert, `check-duplicate-schema-create.py --strict` vert (0 duplication) ; **PHPUnit non exécuté localement (PHP absent de l'image d'agent)** — à confirmer par la CI.
- **fix(security): admin-dashboard — bearer super-admin hors de portée du DOM pendant la session + CSP durcie générée au build, avec test de garde (Closes #7695).** Le cockpit plateforme cumulait deux défauts qui se composaient : le token super-admin vivait en `sessionStorage` (lisible par toute XSS) et la CSP enforcée laissait `script-src 'unsafe-inline'` + `connect-src https: wss:` nu — une XSS pouvait donc lire le bearer ET l'exfiltrer vers n'importe quelle origine HTTPS. **(1) Token en mémoire volatile** (`src/services/token-storage.js`, API inchangée pour les 4 consommateurs) : variable de module pendant toute la session active, `sessionStorage` réduit à un hand-off éphémère de rechargement — consommé (lu **puis supprimé**) au boot du module et au retour bfcache (`pageshow`), réécrit uniquement à `pagehide`/`beforeunload` ; un `sessionStorage.getItem('admin_token')` injecté en cours de session ne rend plus rien. La migration complète cookie httpOnly + BFF (pattern front/web #1299) reste trackée côté #1299 — le SPA statique Cloudflare Pages n'a pas de serveur pour poser le cookie. Les e2e restent compatibles (seed via `addInitScript`, avant le boot). **(2) CSP générée au build** (`scripts/csp.mjs` + plugin `csp-headers` de vite.config.js, fail-closed si le placeholder de `public/_headers` manque) : `script-src 'self'` sans `'unsafe-inline'` (le bundle Vite n'émet que des scripts externes — vérifié sur `dist/index.html`), `connect-src` explicite dérivé de `VITE_API_URL`/`VITE_WEBSOCKET_URL` (origines connues au build, fournies par `deploy-admin-dashboard.yml`) — fin du wildcard « any tenant-configured backend origin » : un backend hors variables exige un rebuild, c'est voulu. **(3) Test de garde** `scripts/check-csp-guard.mjs` chaîné à `npm run build` (donc exécuté par Web CI et le workflow de déploiement) : rouge si la CSP de `dist/_headers` contient encore `'unsafe-inline'` dans script-src, un scheme nu (`https:`, `wss:`, …) ou `*` dans connect-src, ou le placeholder non remplacé. **Vérifié** : ESLint 0 ; build prod OK avec et sans `VITE_WEBSOCKET_URL` (l'origine wss n'est ajoutée que si le push est configuré, cohérent #7303) ; **preuve A/B du garde** — `unsafe-inline` réinjecté dans dist → exit 1, `https:` nu réinjecté → exit 1 ; Playwright local **72 passed / 18 skipped (specs live/staging), 0 failed**. Le garde déploiement `admin-pages-deploy-guard.yml` reste compatible (il ne compare pas la valeur exacte du header).
- **chore(ci): les 7 workflows « … - Leopardo RH » sont renommés « … - Leopardo » — le nom de la CI publique cesse de porter l'ancienne catégorie (Refs #7428).** Étape hors plan §5 mais même dette : chaque PR affichait « Tests - Leopardo RH » dans ses checks, chaque release GitHub se titrait « Leopardo RH x.y.z », et le rapport CI par e-mail était brandé de même. Renommage **atomique** (workflow_run matche par nom) : `tests.yml`, `deploy-main.yml`, `deploy-staging.yml`, `deploy-prod.yml`, `release.yml` (titre de release inclus), `codeql.yml`, `k6-load-smoke.yml` + TOUTES les références — consommateurs `workflow_run` (`deploy-main-catchup`, `e2e-staging`, `owasp-zap`, `pr-external-checks-report`, `mobile-distribute-main`, `ci-saturation-report`), gate `required_workflows` de `deploy-main`/`deploy-staging` et son test `check-deploy-gate-outcome-test.mjs` (10 occurrences), `verify-deploy-workflows/action.yml`, `.github/workflows/README.md`, docs opérateur (`BRANCH_PROTECTION_REQUIRED.md`, `STRATEGIE_CI_RAPIDE.md`, `AGENTS.md` l.421, `DEVELOPMENT.md` l.240). Les récits historiques (`RENDER_DEV_PROD_TOPOLOGY.md` « 12 runs Deploy - Leopardo RH consécutifs », CHANGELOG, archives) sont volontairement inchangés — on ne réécrit pas l'histoire. Aucun des 4 checks requis au merge n'est concerné (`BRANCH_PROTECTION_REQUIRED.md`) ; fenêtre de transition assumée : un run de l'ancien nom encore en vol au moment du merge ne déclenchera pas les consommateurs renommés (rattrapage : `deploy-main-catchup` ou dispatch manuel).
- **fix(ops): les tâches planifiées tournent enfin — boucle `schedule:run` intérimaire dans le conteneur web (Refs #7649).** Vérifié via l'API Render (2026-09-19) : aucun worker ni cron dans les deux workspaces — `php artisan schedule:run` ne s'exécutait NULLE PART (accruals congés, relances billing/contrats, purges, réconciliations : jamais déclenchées hors workflows GitHub ponctuels). Le provisionnement du service `leopardo-scheduler` dédié est **bloqué par le billing** (plan Render free — les background workers exigent un plan payant, création via API refusée « Payment information is required »). Intérim : `docker-entrypoint.sh` lance une boucle `schedule:run` 60 s en arrière-plan du conteneur web, à côté du drain de queue existant — limites documentées dans le script (spin-down du plan free, une seule instance donc pas de double exécution, à retirer dès que le scheduler dédié existe). L'issue #7649 reste ouverte pour le vrai worker + scheduler + migration queue Redis.
- **fix(tenant): modèles Cabinet raccordés à l'isolation tenant + garde `updating` sur BelongsToCompany — le déplacement cross-tenant d'un enregistrement existant est fermé (#7646, suite de #7678).** Les 3 modèles Cabinet (`CabinetDocument`, `CabinetFolder`, `CabinetShare`) vivaient dans le schéma partagé `shared_tenants` **sans** `BelongsToCompany` : aucune isolation en lecture (`ShareDocument::handle` faisait un `findOrFail` non filtré — n'importe quel identifiant deviné donnait accès au document d'un autre tenant) et `company_id` mass-assignable en écriture. Ils portent désormais le trait (scope global + auto-remplissage), `company_id` sort de leur `$fillable`, `CabinetService`/`ArchivePaySlipsToCabinetJob` s'appuient sur le trait (le job tourne sous `EnsureTenantContext`, valeur identique), et l'accès public par token (`accessByToken`, hors middleware tenant) reste fonctionnel par le chemin permissif documenté du trait. **Nouvelle garde `updating` dans le trait, effective sur TOUS les modèles tenant-scopés** : la PR #7678 forçait `company_id` à la création mais le chemin update restait ouvert (un payload validé pouvait DÉPLACER un enregistrement vers un autre tenant) — sous tenant actif, toute modification de `company_id` est restaurée et journalisée ; hors contexte tenant, comportement permissif conservé. Fixtures Cabinet converties `create()` → `forceCreate()` (16 sites, pattern #7678). Nouvelle suite `CabinetTenantIsolationTest` (4 tests : 404 cross-tenant sur document + download, spoof à la création ignoré, update cross-tenant restauré, comportement hors tenant conservé). Restent ~20 modèles orphelins recensés (Attendance ×8, EdgeSync, HR, Notification, Payroll, Planning…) — classification et plan posés dans #7646.
- **fix(tenant): l'écriture cross-tenant par mass assignment de `company_id` est fermée — le hook `creating` de `BelongsToCompany` FORCE le tenant actif, les 8 modèles CRM `$guarded = []` repassent en `$fillable` explicite (Closes #7646).** L'audit du 2026-09-19 mesurait que le scope global protégeait la lecture mais pas l'écriture : le hook `creating` ne remplissait `company_id` que s'il était vide, alors que `company_id` figure dans le `$fillable` de ~270/307 modèles — tout endpoint laissant passer `company_id` dans un payload validé permettait d'écrire dans un autre tenant. **(1) Trait durci** : quand un tenant est actif, `company_id` est TOUJOURS forcé depuis le tenant courant ; une valeur différente fournie est écrasée ET journalisée (`Log::warning`, tentative de spoof) — une écriture inter-tenant légitime passe par `TenantManager::withinTenant()` du tenant CIBLE, jamais par un `company_id` mass-assigné ; hors contexte tenant (console, jobs, seeders, fixtures), le comportement permissif est conservé et documenté, et la garde fail-closed `tenant_scope_required` (#3727) est inchangée. **(2) Mass assignment CRM** : les 8 modèles `$guarded = []` (`CrmChannel`, `CrmChannelConversation`, `CrmChannelMessage`, `CrmAutomation`, `CrmAutomationRun`, `CrmAutomationState`, `CrmExportJob`, `CrmWebhookChannelLookup`) reçoivent un `$fillable` explicite déduit des migrations, SANS `company_id` pour les 6 modèles tenant-scopés ; deux exceptions justifiées par commentaire — `CrmAutomationState` (`company_id` est sa clé primaire, écrite côté serveur via `currentCompany()`) et `CrmWebhookChannelLookup` (table `public` de mapping provider_key → tenant, écrite côté serveur). Les fixtures de test CRM qui posaient `company_id` hors contexte tenant passent à `forceCreate()` (pattern `MultiTenantSharedIsolationTest`). **(3) Tests réalignés sur le nouveau contrat** : les 5 suites qui créaient des fixtures d'un AUTRE tenant sous un tenant actif (`EduGradeTest`, `EduAdmissionTest`, `EduTimetableTest`, `AccountingAuditRetentionTest`, `PortalJourneyE2ETest`) créent désormais ces fixtures SOUS leur tenant propriétaire via le nouveau helper `Tests\Support\SwitchesTenantContext` (bascule + restauration du contexte). **(4) Non-régression** : nouvelle suite `TenantCompanyIdSpoofingTest` — spoof écrasé + journalisé (modèle à `company_id` fillable ET modèle CRM à allowlist), auto-remplissage nominal conservé, comportement hors tenant conservé. **Non vérifié localement (ni PHP ni PostgreSQL dans la session d'agent) : `php -l`, PHPStan strict, Pint et PHPUnit reposent sur la CI.** **Hors périmètre, assumé (point 3 de l'issue)** : l'ajout de `BelongsToCompany` aux ~25 modèles orphelins (`Cabinet*`, `Attendance/{ApprovalRequest,ZktecoDevice}`, `Billing/Partner`…) — chantier à part entière, trop risqué sans exécution locale de la suite ; reste ouvert côté audit #7646.
- **fix(ci): ordre des timeouts de verrous conservé mais valeurs conservatrices — `idle_in_transaction_session_timeout=120s` < `lock_timeout=150s` (Refs #7683, 2e itération après #7684).** Le réglage #7684 (idle 15s < lock 45s) corrigeait l'ordre reaper/waiters (lock timeouts : 60 → 2 sur le run de la PR #7656) mais le reaper à 15s tuait des transactions LÉGITIMES pendant les migrations paratest (workers CPU-starved > 15s idle mid-transaction) → bases à moitié migrées, 567 échecs d'une nouvelle classe (`relation "edge_nodes" does not exist` ×26, `cannot drop table crm_pipelines` ×14, `current transaction is aborted` ×15). Retour à la valeur de réap éprouvée de #7661 (120s) en inversant seulement la relation : les zombies meurent à 120s, les waiters patientent jusqu'à 150s et acquièrent le verrou après le réap — pire cas ~2 min d'attente au lieu d'un échec. À confirmer par le run `Tests - Leopardo RH` de cette PR puis celui de main.
- **docs(positionnement): le README et la fiche GitHub cessent de présenter Leopardo comme un produit RH — application de la décision #7428 aux surfaces GitHub (Refs #7428).** La décision P03 du 2026-09-16 (« Leopardo est une suite métier ») était actée dans le référentiel mais pas sur la première surface publique du projet : la fiche GitHub disait encore « AI-native HR & payroll OS », la homepage pointait vers un backend Vercel de dev, et le README — pourtant déjà « business operations platform » — gardait « Leopardo RH » dans des phrases de présentation (interdit §4) et **omettait entièrement les solutions verticales**, alors que TravelAgency (355 fichiers PHP), RestaurantManager (353), FuelStation (181) et EduManager (158) sont les plus gros modules du dépôt. (1) **README** : pitch canonique EN de `MESSAGE.md` en tête, catégorie « business suite », structure à trois couches (platform core → modules partagés → solutions verticales), nouvelle section « Vertical solutions » (les 4 solutions du `SolutionCatalogue`, maturité **pilot** affichée — garde anti-sur-promesse #7058 respectée), verticales ajoutées à la carte produit et au diagramme d'architecture ; « Leopardo RH » ne subsiste que comme identifiant technique. (2) **Fiche GitHub** (hors code, appliqué à la main) : description = pitch canonique, topics élargis (`crm`, `accounting` — les topics RH de recherche sont conservés, critère 4 « élargir, pas remplacer »), homepage → `kitokoh.github.io/leopardo-hr`. (3) Résidus corrigés : titre de `docs/REFERENTIEL_PRODUIT/README.md`, description du `package.json` racine ; §5 de `POSITIONNEMENT_SUITE_METIER.md` mis à jour (étape 1bis). **Vérifié** : `check-naming-drift.sh` vert (0 nouvelle occurrence interdite, baseline inchangée). Aucune ligne de code produit touchée.
- **fix(web): la CSP de front/web passe enfin en ENFORCE — nonce par requête via le proxy, plus d'`unsafe-inline` dans script-src, connect-src par environnement (Closes #7650).** L'audit du 2026-09-19 constatait une CSP **Report-Only depuis #1300** (décision #1607 du 2026-08-09 jamais révisée, `CSP_ENFORCE` jamais activé — une report-only ne bloque rien), avec `script-src 'unsafe-inline'` et un `connect-src` **figé sur l'API dev** dans `vercel.json`, alors même que le site injecte du HTML non sanitizé (SVG QR 2FA). Bascule complète : **(1) source unique** `src/lib/csp.ts` — les trois copies divergentes (`next.config.ts`, `vercel.json`, aucune en enforce) sont remplacées par un builder unique ; `next.config.ts` et `vercel.json` n'émettent plus de CSP (ce qui solde le hardcode dev de `vercel.json` : `connect-src` suit `NEXT_PUBLIC_API_URL`, posée par environnement). **(2) Nonce par requête émis par le proxy** : 128 bits Web Crypto, posé sur les en-têtes de la REQUÊTE (`content-security-policy` + `x-nonce`) — Next le lit (`parseRequestHeaders`) et l'appose lui-même sur ses scripts inline (bootstrap, flight data) et preloads, possible parce que **toutes les routes HTML sont déjà rendues dynamiquement** (le root layout lit `headers()`, #3807 — vérifié au build : seuls robots/sitemap restent statiques, sans script) ; le matcher du proxy gagne un motif attrape-tout (hors `/api`, `/_next`, fichiers) pour couvrir les routes HTML qu'il ne voyait pas (`/auth/*`, `/shop`, `/kiosk`…), les entrées littérales des zones gardées restant inchangées (gardes #3377/#7663 vertes). **(3) `script-src` sans `'unsafe-inline'`** : `'nonce-…' 'strict-dynamic'` (les scripts injectés côté client — GA4/Mixpanel de `ConsentScripts`, `afterInteractive` — sont couverts par strict-dynamic ; le Consent Mode `beforeInteractive` du layout porte le nonce lu dans `x-nonce` ; les hosts CDN restent en repli CSP2 ; les blocs JSON-LD ne sont pas des scripts exécutables, non concernés) ; `style-src` garde `'unsafe-inline'` (Tailwind v4/framer-motion, hors périmètre — le vecteur exécutable est script-src) ; `next dev` ajoute `'unsafe-eval'` + `ws:` (React Refresh/HMR), jamais en prod. **(4) Enforce par défaut, rollback opérationnel** : `CSP_REPORT_ONLY=true` (env Vercel) rebascule en Report-Only sans redéploiement — l'opt-in `CSP_ENFORCE` jamais actionné disparaît, l'état sûr devient le défaut. **Vérifié** : `tsc --noEmit` 0 ; `eslint src+e2e --max-warnings 0` OK ; `check:mojibake` OK ; **Jest 128 suites / 1117 tests verts** (dont 13 nouveaux `proxy-csp.test.ts` : header enforce, nonce unique par requête et propagé à Next, script-src sans unsafe-inline, connect-src par env, levier de rollback) ; `next build` OK ; **smoke réel contre le build de prod** (`next start` + Chromium) : `/`, `/pricing`, `/auth/login`, `/signup`, `/faq` — en-tête enforce, 22–37 balises script TOUTES nonce-ées (0 inline sans nonce), hydratation complète, `gtag` défini (le script inline de consentement s'exécute), **0 violation CSP en console** ; e2e `security-headers.spec.ts` réécrit pour le contrat enforce (3/3 verts) et **gate funnel `funnel-tracking.spec.ts` 2/2 verts** contre le build enforcé. Points de vigilance : vérifier sur la preview Vercel le checkout Stripe et l'analytics après consentement ; la prod DOIT poser `NEXT_PUBLIC_API_URL` (sinon repli sur l'API dev, comme avant) ; l'endpoint d'ingestion `report-to` (#1607) reste à câbler séparément.
- **fix(security): lot hygiène #7696 — 8 workflows SHA-pinnés, fin des fallbacks « password123 », shell_exec échappé (Closes #7696).** (1) **Épinglage** : 65 workflows SHA-pinnaient déjà, 8 restaient sur des tags mutables (dont `pages-deploy.yml` avec `id-token: write` et deux actions tierces `dorny/paths-filter@v4`, `shivammathur/setup-php@v2`) — tous alignés sur le standard SHA + commentaire de version. (2) **Mot de passe démo** : `config/demo.php` perdait « password123 » en fallback dès que `DEMO_PASSWORD` manquait — le fallback en dur est supprimé (env obligatoire), `GET /api/v1/demo-users` répond **503** si le mode démo est actif sans mot de passe configuré (nouveau test), `SuperAdminSeeder`/`DemoCompanyOnceSeeder` retombent sur aléatoire/no-op, et `DemoDzSeeder` — qui gardait un littéral **raté par l'audit #1697** — refuse désormais de seeder sans `DEMO_PASSWORD` (RuntimeException). Valeur de test explicitée dans `phpunit.xml`. (3) **`VoiceController:254`** : la sonde `command -v` sur `ai.voice.edge_tts_binary` passe par `escapeshellarg` (cohérence `CameraService`, défense en profondeur).
- **fix(edge): TLS interne par défaut sur le LAN client + manifeste d'installation signé RS256 — plus aucun trafic applicatif en clair, chaîne d'intégrité non circulaire (Closes #7653).** Audit sécurité 2026-09-19 : `edge/Caddyfile.edge` servait tout en `:80` HTTP pur et `docker-compose.yml` publiait `7878:80`/`7879:3000` en direct — pointage, PII et biométrie ZKTeco circulaient **en clair sur le réseau du client** ; le manifeste `sha256.txt` était téléchargé depuis le MÊME endpoint que les fichiers qu'il vérifie (protège de la troncature, pas d'une compromission du cloud) ; le jeton d'enrôlement passait en argv (`--token`, visible dans `ps`/history) ; montage `license.pub` déclaré deux fois dans le compose. **Quatre volets** : (1) **TLS par défaut** — Caddy `tls internal` + `local_certs` (CA locale persistée dans `caddy_data`), sites émis pour `EDGE_DOMAIN` et l'IP LAN détectée à l'installation (`EDGE_LAN_IP`, injectés via .env) ; les ports historiques 7878/7879 restent joignables mais **via le proxy en TLS** (edge-api/edge-ui ne publient plus rien sur l'hôte), `:80` ne sert plus que la redirection 308 (+ `/health`) ; procédure d'installation de la CA racine sur les kiosques (Linux/Windows/Android) documentée dans `edge/README.md` ; l'UI offline passe en **same-origin** (`/api/*` routé par le proxy sur chaque origine — le défaut `http://leopardo.local:7878` en dur aurait été bloqué en mixed content). (2) **Manifeste signé** — nouvel endpoint public `GET /edge/download/sha256.txt.sig` (signature SHA256-RSA base64 des octets EXACTS du manifeste, clé privée `EDGE_LICENSE_PRIVATE_KEY` déjà provisionnée ; source unique `manifestContent()` partagée par les deux endpoints) ; `install.sh` vérifie la signature avec la clé publique **avant** d'utiliser le manifeste, fail-closed (`--insecure-manifest` réservé au dev), clé épinglable hors bande (`--pubkey`/`EDGE_MANIFEST_PUBKEY_FILE`, sinon TOFU avec avertissement) ; OpenAPI + miroir dev-hub documentés. (3) **Jeton hors argv** — `--token` est refusé avec message explicite ; sources : env `EDGE_TOKEN`, `--token-file`, ou saisie interactive masquée ; la commande `install_command` émise par `POST /v1/edge` et la vitrine (`EDGE_INSTALL_CMD`) passent à `EDGE_TOKEN=… sudo --preserve-env=EDGE_TOKEN`. (4) **Compose assaini** — montage `license.pub` dédupliqué, URL cloud d'install.sh paramétrable aussi par env `CLOUD_API_URL`. **Vérifié** : nouveau garde CI `edge/tests/install-manifest-signature.sh` (branché dans `edge-ci.yml` — signature valide acceptée, manifeste altéré/clé étrangère refusés, postures --token et TLS verrouillées) **6/6 vert localement**, gardes `install-manifest-parse.sh` et `check-edge-migrations-path.sh` toujours vertes, vitest web-offline **23/23 verts**, `bash -n install.sh` OK, garde OpenAPI `check-openapi-route-coverage.py` **0 nouveau drift** ; 2 nouveaux cas PHPUnit (`EdgeDownloadControllerTest` : 503 sans clé, signature vérifiant les octets exacts et rejetant l'altération) **non exécutés localement (PHP absent de l'image d'agent)** — à confirmer par la CI. **Point de vigilance** : les kiosques existants doivent installer la CA racine et passer leurs URLs en `https://` (procédure README) ; la signature exige `EDGE_LICENSE_PRIVATE_KEY` côté Render (déjà requise en prod pour les licences depuis #6560), sinon `sha256.txt.sig` répond 503 et install.sh s'arrête fail-closed.
- **fix(ci): le reaper idle-in-transaction agit AVANT l'expiration des attentes de verrous — la suite backend cesse d'échouer en masse sur `SQLSTATE[55P03]` (Closes #7683, suite de #7660).** Le réglage initial de #7661 (`lock_timeout=30s`, `idle_in_transaction_session_timeout=120s`) échangeait les blocages infinis contre des échecs en masse : les connexions zombies « idle in transaction (aborted) » qui tiennent les verrous n'étaient réapées qu'à 120s alors que les waiters abandonnaient à 30s — 60 occurrences de `canceling statement due to lock timeout` par run observées sur les deux derniers `Tests - Leopardo RH` de main (2026-09-19, 02:39 et 04:08 UTC), plus les cascades (Not null violation quand la fixture précédente a échoué). Inversion de la relation dans `.github/actions/setup-backend-db/action.yml` : `idle_in_transaction_session_timeout=15s` (aucune transaction de test ne reste légitimement idle 15s) < `lock_timeout=45s` — le waiter survit au réap du zombie et acquiert le verrou ; `statement_timeout=300s` inchangé. Vérifiable uniquement en CI (le scénario exige la contention paratest réelle) : à confirmer par le run `Tests - Leopardo RH` de cette PR puis celui de main.
- **fix(platform): routes platform dédupliquées — les déclarations SANS `platform.permission` masquaient les versions protégées (Closes #7680).** `RouteCollisionGuardTest` était rouge sur main : `PATCH companies/{company}/country`, `GET|PATCH …/subscription` et `GET|PATCH …/features` étaient déclarées deux fois dans `routes/api.php` (l.472–491), d'abord sans middleware puis avec les permissions granulaires. Laravel matchant la **première** route enregistrée, les granularités `billing.view/manage` et `companies.view/manage` étaient **désactivées de fait** sur ces 5 endpoints. Les 5 doublons non protégés sont supprimés, les versions protégées conservées (commentaire #1952 rattaché). Aucune route effective ne change de contrôleur ; garde OpenAPI 0 nouveau drift. PHPUnit non exécuté localement (PHP absent de l'image d'agent) — `RouteCollisionGuardTest` à confirmer par la CI.
- **chore(web): suppression de 4 scripts d'audit a11y orphelins à la racine de `front/web` (`ap3.mjs`, `ap4.mjs`, `vf.mjs`, `verify.mjs`) — tranche immédiate de l'audit #7654.** Scripts Playwright+axe jetables de vérification de contraste, jamais référencés par aucun `package.json`, workflow ni doc (`rg` négatif sur tout le dépôt), avec deux doublons quasi identiques (`ap3`≈`ap4`, `vf`≈`verify`) et des **identifiants de seed committés en dur** (`ahmed.benali@techcorp-algerie.dz` / `password123`, `ap3.mjs:11-12`). L'outillage a11y pérenne vit déjà dans `front/web/e2e` et `lighthouserc.json`. Aucun code de production touché. Le reste de #7654 (CHANGELOG_ARCHIVE hors dépôt, génération SDK/i18n au build, réécriture d'historique) demande des arbitrages propriétaire et reste dans l'issue.
- **fix(api): alias de supervision non versionné `GET /api/health` + doc du seeder corrigée (Closes #7666).** Vérifié en prod le 2026-09-19 : `GET /api/health` répondait 404 (la route canonique est `/api/v1/health`, tout `routes/api.php` vit sous `Route::prefix('v1')`) — les sondes externes qui essaient d'abord le chemin non versionné (convention UptimeRobot/Better Uptime, intégrateurs) concluaient « API morte » alors qu'elle était up, et `DatabaseSeeder` affichait « Tester : GET /api/health », instruction qui échouait telle quelle. Alias déclaré HORS du groupe v1, même contrôleur (`HealthController`), même throttle 60/1 — compatible `ApiVersionMiddleware` (segment 2 non `v\d+` → version courante) ; `/api/v1/health` reste la route canonique (Render `healthCheckPath`, docs/ops/HEALTH_ENDPOINTS.md inchangés) ; message du seeder corrigé en `/api/v1/health`. Nouveau cas dans `HealthEndpointTest` (l'alias sert la même sonde). **Vérifié** : garde OpenAPI `check-openapi-route-coverage.py` verte (0 nouveau drift — l'alias résout vers le chemin `/health` déjà documenté) ; **PHPUnit non exécuté localement (PHP absent de l'image d'agent)** — à confirmer par la CI.
- **fix(vitrine): audit vendeur du funnel — URLs devinables réparées, zones applicatives protégées, 404 localisée, Navbar accentuée (Closes #7662, Closes #7663, Closes #7664, Closes #7665).** Audit complet vitrine → onboarding mené le 2026-09-19 sur la prod (leopardo-prod.vercel.app), quatre défauts corrigés en lot : **(1) #7662** — `/login`, `/connexion`, `/register`, `/inscription`, `/onboarding`, `/tarifs` et `/a-propos` répondaient **404 en prod** (vérifié curl) alors que ce sont les URLs les plus tapées par un prospect ; 7 redirects ajoutés dans `next.config.ts` (307 pour les alias d'auth/inscription — précédent #7259 —, 301 pour les slugs FR des pages canoniques — précédent #7101). **(2) #7663** — `/crm`, `/accounting`, `/edu-manager` et `/fuel` étaient servis **sans session** (200 + ~27 Ko de HTML applicatif à un visiteur anonyme, vérifié en prod) et **absents du disallow robots.txt** : ajoutés à `PROTECTED_PREFIXES` (source unique #3377) ET à la liste `DASHBOARD_PREFIXES` du gate de session de `proxy.ts` — distincte du matcher et jamais couverte par la garde existante — + `sw.js` ; **deux nouveaux tests de garde** : chaque préfixe protégé doit être couvert par le gate de session (pas seulement par le matcher), et tout répertoire de `src/app/(dashboard)` doit être protégé ou allowlisté avec justification écrite (`/travel` documenté : split de routes requis à cause de `/travel/contact` public, précédent `/restaurant` → `/restaurateur`). **(3) #7664** — la 404 globale avait un titre localisé mais un **corps 100 % FR en dur** (contraire à PA2-I18N-014) : nouvelles clés `vitrine.notFound.*` ×4 locales dans `shared/i18n`, propagées par les 3 synchroniseurs, locale résolue via `x-vitrine-lang` (#4004). **(4) #7665** — la Navbar affichait un FR sans accents (« Communaute », « Dernieres mises a jour »…) et un TR sans diacritiques (« Iletisim », « Degisiklikler »…) : pass complet des 25 libellés/descriptions. **Vérifié** : `eslint src --max-warnings 0` OK, `tsc --noEmit` 0, Jest complet vert, `check:mojibake` OK, `I18N_VALIDATION_OK (4 locales)`, garde protected-prefixes 75 tests verts.
- **feat(retail): le module vendeur BC-17 passe de « planned » à actif — produits, stock moderne, POS et espace web /commerce (Closes #7672, #7673, #7674, #7675).** Mandat fondateur : donner au gérant-vendeur ses outils opérationnels dans SON espace (distinct de l'admin plateforme), prêt pour la vente à la caisse aujourd'hui et en ligne demain. **(1) Fondations** (#7672) : nouveau module `api/app/Modules/Retail` (5 couches, structure validée), flag `retail` (fail-closed, `feature-flags.php` + `KNOWN_MODULES`), middleware `module.retail`, `RetailCategory`/`RetailProduct` (SKU et slug uniques par tenant, code-barres, prix ET coût en minor units + devise ISO-4217, statut draft|published|archived), CRUD `/v1/retail` + publish/unpublish, policies deny-by-default enregistrées centralement (PA2-ARCH-008), registre BC-17 → actif (`api/app/Modules/Retail`, arête BC-17→BC-03 déclarée). **(2) Stock moderne** (#7673) : `retail_locations` (boutique/entrepôt), `retail_stock_levels` (unique tenant+emplacement+produit, seuils de réassort et d'alerte), `retail_inventory_movements` (delta signé, 7 motifs, référence polymorphe) — **toute quantité ne bouge que par un mouvement tracé** (`RetailStockService::applyMovement`, transactionnel, `lockForUpdate`, résultat négatif → 422, `ConnectionInterface` injectée — la pureté de couche interdit les façades en Application/) ; endpoints niveaux (filtre sous-seuil), mouvements, alertes. **(3) POS v1** (#7674) : sessions de caisse (fonds d'ouverture, cash attendu = ouverture + paiements cash, variance signée + motif), ventes `POS-YYYYMMDD-XXXXXX` avec lignes figées (nom et prix snapshotés côté serveur), paiements idempotents cash|card|mobile (**`online` réservé à la future plateforme e-commerce**) ; paiement complet → commande `completed` + mouvements `sale` par ligne ; survente → ajustement auto TRACÉ puis vente (on ne bloque jamais un encaissement réel) ; annulation d'une vente complétée → contre-mouvements `return` ; reçu = `GET pos/orders/{id}` (lignes + paiements). **Le miroir restaurant est cassé sur ce point précis** : son unique `(company, branch, status)` interdit deux sessions fermées sur la même branche — Retail utilise un index unique PARTIEL Postgres (`WHERE status = 'open'`), régression verrouillée par test, défaut restaurant signalé en issue BC-25. **(4) Espace vendeur web** (#7675) : vertical `commerce` (nav gatée par le flag `retail`), pages `/commerce` (hub + compteurs), `/commerce/products` (CRUD + publication), `/commerce/stock` (niveaux/mouvements/emplacements/alertes), `/commerce/pos` (session → panier → encaissement idempotent (`crypto.randomUUID`) → ticket imprimable → clôture avec variance), `CommerceCrudTable` calqué sur le pattern travel, helpers monétaires testés, i18n complète ×4 locales (vraies traductions ar/tr, RTL par le layout racine) propagée par les 3 synchroniseurs ; la garde `check-i18n-diff.js` apprend la signature maison `t(locale, clé, repli)` (200 faux positifs sur le diff travel déjà mergé, doctrine #7482 : on étend le guard) + 2 fixtures d'auto-test. **Vérifié** : API **27 tests / 321 assertions** verts (`tests/Feature/Retail` — flag-off 403, RBAC écriture, isolation tenant 404, SKU unique 422, survente tracée, idempotence paiement, variance de clôture, deux sessions fermées coexistent) ; PHPStan strict L8 `[OK] No errors`, 0 entrée baseline ; web `tsc --noEmit` 0, `eslint --max-warnings 0` 0, **Jest 128 suites / 1085 tests**, `next build` OK, mojibake OK ; i18n ×4 validée + parité ; gardes migrations (collisions, conventions BC, duplication `Schema::create` 384 tables/0 dup, schéma tenant), routes (doublons, publiques, tenant/platform, contrôleurs routés), policies single-point, isolation modules, pureté de couche, parité MVP-schema, couverture OpenAPI `--strict-staleness` + Redocly 0 erreur + SDK régénéré — toutes vertes en local.
- **feat(travel): rapports du gérant — ventes, occupation, revenus et export CSV asynchrone (Closes #7637, épique #7642).** Dernière tranche de l'espace gérant : page `/travel/reports` (front/web) avec 4 onglets — ventes, occupation, recettes, annulations — servis par `GET /travel/reports/{type}` (`TravelReportRequest` : `from`/`to` REQUIS, bornés ≤ 365 j), sélecteur de période (défaut 30 derniers jours), KPIs en minor units recalculés serveur, ventilations par canal/statut et table d'occupation par trajet (taux trié décroissant côté serveur). **Export CSV asynchrone et idempotent** : `POST /travel/reports/export` (202 + asset, `idempotency_key` générée client via `crypto.randomUUID()`), polling `GET /travel/reports/export/{asset}` jusqu'à `generated` → lien de téléchargement sur l'URL signée éphémère (jamais stockée), `failed` affiché avec l'erreur expurgée ; le bouton n'est proposé que sur l'onglet ventes car `StoreTravelExportRequest` n'allowliste que `report_type: sales`. i18n ×4 locales (9 clés imbriquées `travel.reports.*`, RTL préservé), test e2e `/travel/reports` dé-skippé (mocks des rapports + du cycle export pending→generated). **Vérifié** : ESLint 0, `tsc --noEmit` 0, Jest 193/193, `validate.js` OK, Playwright chromium **7/7**.
- **feat(travel): réservations et billets — cycle de vie complet et check-in (Closes #7636, épique #7642).** Page `/travel/bookings` : liste filtrée (`status`, `trip_id`, `reference` exacte), détail en modal avec passagers (RGPD : `has_document` seul, jamais le n° de pièce) et billets, cycle de vie reflétant les gardes backend (confirm sur `pending` ; cancel sur `pending|confirmed` avec motif 3–500 ; refund intégral, refund par passager — `refund_key` idempotente — et émission des billets sur `confirmed` ; les 422 serveur sont affichés tels quels). Billetterie : PDF via URL signée (`GET /travel/tickets/{t}/pdf`), check-in sur `issued`, révocation refusée après embarquement. E2E `travel-admin-smoke.spec.ts` dé-skippé et réécrit (nav gated par flag, hub, réseau, trajets, réservations). i18n ×4 (13 clés, dont `bookingStatus.completed` et `paymentStatus.confirmed` — les enums backend divergeaient du catalogue legacy).
- **feat(travel): gestion des voyages — création, tarifs, publication (Closes #7635, épique #7642).** Page `/travel/trips` : liste avec bascule automatique sur `GET /travel/trips/search` dès qu'un filtre est actif (origine, destination, date, statut), création/édition alignée sur `StoreTravelTripRequest` (départ ≥ aujourd'hui, heures `H:i` normalisées, 1–200 places), tarifs par classe en minor units (unicité trip×classe = 409 backend), publication avec confirmation, annulation avec motif requis 3–500, manifeste passagers RGPD-safe. L'édition est masquée sur un trajet publié/annulé (verrou backend reflété, workflow cancel→update→republish).
- **feat(travel): référentiel réseau — gares, bureaux, lignes et arrêts (Closes #7634, épique #7642).** Page `/travel/network` (3 onglets stations/bureaux/lignes) + panneau d'arrêts ordonnés par ligne (`rank` optionnel = fin de ligne, escale, durée minimale nullable). Nouveau composant générique `TravelCrudTable` config-driven adapté aux FormRequests Travel (selects numériques, booléens natifs, champs `sometimes` omis à vide, `nullable` → `null` pour effacer) exportant `buildCrudPayload()` et `readApiError()` (premier message 422 Laravel), réutilisés par les pages trips/bookings/reports.
- **feat(travel): hub gérant et navigation espace voyages (Closes #7633, épique #7642).** `(dashboard)/travel/page.tsx` devient un hub façon restaurant (il redirigeait vers le portail voyageur) : KPIs du jour via `GET /travel/reports/dashboard` (recette nette, passagers, taux d'occupation, annulations), 4 tuiles d'accès (réseau, voyages, réservations, rapports) et carte vers le portail voyageur préservé (`/travel/portal`, nouvelle clé module `travel_portal` au même gating `travelagency`). `ROUTE_TO_MODULE` étendu aux sous-routes, libéllés nav ×4 locales, 12 clés `travel.home.*` imbriquées ×4.
- **fix(ops): garde DEPLOY_TIER fail-closed sur le DROP-database et le force-reset super-admin de l'entrypoint (Closes #7647).** L'audit architecture 2026-09-19 a mesuré deux rayons d'explosion embarqués dans l'image prod : la logique `RESET_TEST_DB_ONCE` (DROP de TOUTES les tables/séquences/vues + `DROP SCHEMA shared_tenants CASCADE`) vit dans `api/docker-entrypoint.sh`, et `FORCE_SUPER_ADMIN_PASSWORD_RESET=true` + `SUPER_ADMIN_PASSWORD` (posées en permanence sur le service Render dev, vérifié via l'API Render) re-forcent le hash super-admin à CHAQUE déploiement — un accès dashboard Render = takeover admin permanent. **Aggravant : le tier dev tourne en `APP_ENV=production` (dette assumée, cf. topologie), donc aucun garde APP_ENV ne peut distinguer dev et prod — c'est précisément le trou que #6537 ne pouvait pas boucher.** Nouvelle variable de tier explicite `DEPLOY_TIER` (`dev`|`prod`), découplée d'APP_ENV, posée par les blueprints (`render.yaml` → `dev`, `render.prod.yaml` → `prod`), et **fail-closed** dans l'entrypoint : (1) `RESET_TEST_DB_ONCE=true` exige désormais `DEPLOY_TIER=dev` explicite — variable absente, vide ou autre valeur ⇒ refus de démarrer avec message référençant #7647, **EN PLUS des gardes APP_ENV #6537, conservées à l'identique** (les gardes s'additionnent, aucune n'est retirée) ; (2) `FORCE_SUPER_ADMIN_PASSWORD_RESET` — traitée par `SuperAdminSeeder` (PHP), donc gardée à la source de propagation : hors `DEPLOY_TIER=dev`, l'entrypoint **vide la variable AVANT `db:seed`** avec un warning bruyant (rotation hors dev : `php artisan super-admin:reset-password` via canal sécurisé, jamais une env var permanente). Contrat documenté dans l'en-tête de l'entrypoint, `api/.env.example` et une section dédiée de `docs/ops/RENDER_DEV_PROD_TOPOLOGY.md`. **Vérifié** : `bash -n` + `sh -n` sur l'entrypoint ; `dev-hub/tools/tests/check-entrypoint-reset-guard.test.sh` étendu de 3 à **6 cas** (extraction élargie jusqu'à `reset_key=` pour couvrir les deux familles de gardes) — **6/6 verts** : production refusée même avec `DEPLOY_TIER=dev` (#6537 intact), staging+`DEPLOY_TIER=dev` passe, reset absent sans effet, et refus pour `DEPLOY_TIER` absente/`prod`/vide (#7647) ; YAML des deux blueprints parsé sans erreur. **Actions propriétaires restantes (dashboard Render, hors dépôt)** : poser `DEPLOY_TIER=dev` sur le service dev AVANT le prochain usage de `RESET_TEST_DB_ONCE` (sinon le refus fail-closed est le comportement voulu), et retirer `SUPER_ADMIN_PASSWORD`/`FORCE_SUPER_ADMIN_PASSWORD_RESET` des env vars permanentes du service dev (4ᵉ case de l'issue). Le verrou one-shot consommable du force-reset (3ᵉ case, côté seeder) reste à traiter — dit dans la PR, pas maquillé.
- **feat(onboarding,auth,web): première connexion sans mot de passe en clair — e-mail de bienvenue à lien magique, page publique de définition (TTL 72 h) et connexion par code pour les comptes sans mot de passe (Closes #7490, epic #7486).** L'e-mail de bienvenue self-service (`TrialWelcomeMail`) envoyait encore le **mot de passe temporaire en clair** (anti-pattern sécurité, contraire à SECURITY.md et au constat de l'issue) alors que le dépôt possédait déjà mieux : l'auto-connexion post-code (#2437) et `POST /trial/set-password` porté par le `provisioning_token` (#2903). **Trois volets, qui généralisent l'existant au lieu de revenir en arrière** : (1) **l'e-mail de bienvenue ne contient plus AUCUN secret** — le bloc « Email / Mot de passe » est remplacé par le rappel de l'e-mail de connexion et un **lien magique de définition de mot de passe** (`/auth/set-password?token=…`, `provisioning_token` à usage unique déjà garanti par `password_set_at`, désormais borné par un **TTL de 72 h** : `POST /trial/set-password` répond `410 TRIAL_PASSWORD_LINK_EXPIRED` au-delà, clé `billing.trial_password_link_expired` ×4) ; les clés `email_trial_welcome_password_label`/`change_pw` disparaissent des 4 catalogues et le rendu est produit dans la locale du tenant comme avant (#7346/#7347) ; en l'absence de ligne de provisioning, le mail retombe sur un texte sans lien (jamais d'e-mail cassé). (2) **Page publique `front/web` `/auth/set-password`** (critère 3 : « définir le mot de passe sans être connecté ») : token lu depuis `?token=` avec **repli sur le token déjà détenu par le navigateur** (`lp_trial_provisioning_token`), POST via le proxy same-origin `/api/forms/trial-password` (token en `X-Token`, #4931 — le proxy relaie désormais aussi le `410`), politique partagée `password-policy` (12 caractères + chiffre), copie `setPassword.*` ×4 locales, mise en mots de tous les codes d'erreur (invalide / expiré / déjà défini / non prêt). Le **CTA principal de l'écran de bienvenue** (#7604) devient « **Définir mon mot de passe maintenant** » et route vers cette page (action `set_password` ; « Plus tard » inchangé, l'écran reste non bloquant et acquitté par les deux issues). (3) **« Recevoir un code de connexion » sur `/auth/login`** (critère 4) : nouveaux endpoints publics `POST /auth/login-code/request|verify` (bucket `auth-sensitive`), réservés aux comptes dont `trial_provisionings.password_set_at` est NULL — le canal **se referme dès qu'un mot de passe existe**. La demande répond **toujours générique** (anti-énumération, posture de la réinitialisation) ; le code 6 chiffres part par `LoginCodeMail` (registre `login_code`, catalogue ×4, locale du tenant), vit **10 minutes en cache, HASHÉ**, est **consommé au premier usage** et verrouillé après **5 échecs** (429) ; la vérification réutilise `AuthService::loginViaEmail` (chemin SSO éprouvé) avec le même contrat de réponse que `/auth/login`, et un compte 2FA est **refusé** (`403 LOGIN_CODE_PASSWORD_REQUIRED` + token révoqué — le canal code ne contourne jamais un challenge, posture #5436). Côté web, le mode code est intégré à la page de connexion (bascule, renvoi de code, retour mot de passe, événements `login_code_*` distincts pour la mesure) et le token Sanctum est posé en **cookie httpOnly** par un route handler dédié qui ne renvoie jamais le token brut (posture #1299). OpenAPI (`api/openapi.yaml` + miroir dev-hub) : +2 paths, +410 sur set-password. **Vérifié** : Jest **125 suites / 1030 tests verts** (dont 7 nouveaux sur la page set-password, 4 sur le route handler cookie, et l'écran de bienvenue mis à jour), `tsc --noEmit` 0, `eslint src --max-warnings 0` 0, `check:mojibake` OK, `validate.js` i18n **OK ×4 locales** (catalogues régénérés par `sync-web`), parité des catalogues `api/lang` ×4 préservée (clés ajoutées/retirées dans les 4 langues), YAML OpenAPI parsé sans erreur. Nouveau `FirstLoginPasswordlessTest` (8 cas Feature : lien magique dans le mail + aucun secret rendu, 410 après 72 h, 200 avant, envoi du code éligible, réponse générique inconnue, session ouverte + usage unique, verrou 5 échecs, canal refermé après définition) — **non exécuté localement, PHP absent de l'image d'agent** : à confirmer par le check requis `Tests - Leopardo RH`. Périmètre assumé : le rappel-bannière `must_set_password` (proposition 4 de l'issue, hors critères d'acceptation) n'est pas traité ici.
- **feat(funnel): tracking par étape du tunnel d'acquisition, dashboard admin des conversions et e2e bloquants (Closes #7496, epic #7486).** Le plan de tracking #7542 (5 jalons côté client, GA/Mixpanel uniquement) est complété et rendu **exploitable** : (1) `FUNNEL_EVENTS` couvre désormais le plan complet de l'issue (`welcome_seen`, `interview_started/question_answered/skipped/completed/dismissed`, `first_module_opened`), câblé sur l'assistant d'accueil (`OnboardingWizard`, incarnation actuelle de l'entretien #7493 — les noms d'événements sont stables pour la suite) ; (2) **collecte first-party** : chaque étape part aussi vers `POST /api/forms/funnel-event` (route Next) qui relaie serveur-à-serveur vers `POST /api/v1/funnel/events` avec le secret partagé `MARKETING_LEAD_WEBHOOK_TOKEN` (jamais détenu par le navigateur) — nouvelle table globale `acquisition_funnel_events` (pré-tenant, même pattern que `marketing_leads`), liste FERMÉE d'événements, contexte filtré par liste blanche, **aucune PII** (critère 4 : ni e-mail, ni valeur de formulaire, ni réponse d'entretien) et **consentement mesure d'audience requis** côté client (double barrière #7593 — sans accord, ni GA ni beacon) ; (3) **dashboard admin** `GET /admin/funnel/stats` (`platform.permission:metrics.view`) + vue `/crm/acquisition-funnel` (admin-dashboard, nav + palette) : taux de passage par étape (comptés en **parcours distincts** — un renvoi de code ne gonfle pas l'étape), conversion visite → espace prêt **par jour et par source** (attribution captée au premier écran), et **alerte livraison OTP** (ratio vérifiés/envoyés du jour sous 50 % avec ≥ 5 envois → bannière — le mailer en échec déjà vu en prod) ; (4) **e2e bloquants** : nouvelle spec hermétique `e2e/funnel-tracking.spec.ts` (jalons émis dans l'ordre, corrélation stable, attribution conservée, zéro PII, zéro beacon sans consentement) exécutée par `.github/workflows/funnel-e2e-gate.yml` sur toute PR touchant les fichiers du tunnel (`SignupForm`, `proxy.ts`, `OnboardingWizard`, `funnel.ts`, route beacon) — `conversion-funnel.spec.ts` volontairement hors gate (2 cas dépendent du backend réel et échouent déjà sur main sans backend chaud ; le parcours réel reste couvert par `e2e-staging.yml`) ; à ajouter aux required checks (`BRANCH_PROTECTION_REQUIRED.md`). Contrat OpenAPI documenté (+2 paths), miroir dev-hub + SDK régénérés, couverture de routes stricte verte. **Vérifié** : web Jest **123 suites / 1024 tests** verts (dont 4 nouveaux cas funnel), `tsc --noEmit` 0, `eslint src+e2e --max-warnings 0` OK, Playwright funnel-tracking **2/2 verts contre le build de production** ; API **PHPStan strict `[OK] No errors`** sur les 2 contrôleurs + 2 suites de tests (PHP 8.4 rootless #7585), `pint` PASS, gardes migrations (collisions + duplication `Schema::create`) vertes ; **PHPUnit (7 cas écrits : secret fail-closed, accepté sans secret à la #7301, liste fermée, filtrage PII, agrégats, alerte OTP déclenchée/muette) non exécuté localement — pas de PostgreSQL dans la sandbox, à valider par la CI**. Vue admin : ESLint 0, `vite build` OK.
- **feat(vitrine): pass copy & design du tunnel d'acquisition — wording ×4 locales, renvoi OTP en 1 clic, a11y et micro-interactions (Closes #7495, epic #7486).** Avant les campagnes payantes, chaque écran du tunnel est relu : **zéro jargon** (« Nous provisionnons votre sandbox de démonstration » → « Nous préparons votre espace … vous pouvez rester sur cette page, elle se met à jour toute seule »), écrans d'attente/échec reformulés orientés bénéfice, note d'indisponibilité honnête sans détail technique (« redémarrage serveur » retiré), le tout dans `shared/i18n` **×4 locales** (fr/en/ar/tr) propagé par les 3 synchronisations (garde I18N verte, parité OK). **Code expiré → renvoi en 1 clic** (point 4 de l'issue) : bouton « Renvoyer le code » sur l'écran OTP qui rejoue la soumission d'origine (même capture de lead côté backend), délai anti-spam 30 s (le seau `throttle:5,15` de `/trial/signup` n'est pas 429é), confirmation et échec **honnêtes** annoncés en `aria-live`, et le renvoi est mesuré (`signup_otp_sent {resend:true}`, signal de friction pour #7496). **Accessibilité** : focus déplacé sur le titre à chaque transition d'écran (jamais au premier rendu — on ne vole pas le champ e-mail), groupe OTP `role=group` + `autocomplete=one-time-code` + `dir=ltr` (l'ordre des chiffres ne s'inverse pas en arabe), erreurs `role=alert`, suivi de préparation en `aria-live=polite` (préparation → prêt/échec annoncés), `prefers-reduced-motion` respecté (`MotionConfig reducedMotion="user"` sur les transitions du tunnel). **Jamais de spinner muet > 10 s** : l'écran de préparation ajoute un message de réassurance après 10 s (« votre espace arrive, inutile de recharger ») ; l'écran d'échec gagne un **lien support actionnable** (/contact). **Vérifié** : Jest vitrine **45 suites / 539 tests** verts (2 nouveaux cas renvoi OTP — succès avec désactivation anti-spam, échec honnête sans faux délai), `tsc --noEmit` 0, `eslint --max-warnings 0` OK, `check:mojibake` OK, `I18N_VALIDATION_OK (4 locales)`. **Hors périmètre, assumé** : la déclinaison des visuels 3D `SignupArtwork` aux écrans de l'entretien et de bienvenue attend l'entretien conversationnel #7493 (branche dédiée en cours — les écrans finaux n'existent pas encore sur main) ; la revue croisée propriétaire (critère 1) et le rapport Lighthouse (critère 4) se font sur la preview de cette PR.
- **feat(rbac): cycle de vie des accès ressource — invitation pré-assignée, vue « qui a accès à quoi », révocation en cascade au départ, audit exportable (Closes #7601, R4 de l'épique #7597).** Le parcours cible de l'épique se boucle : le restaurateur invite « Moussa — gérant Almadies (manage) + Plateau (view) » **en un geste**, Moussa active et son espace est borné, son départ révoque tout, et l'audit reste lisible. **(1) Invitation pré-assignée** : `POST /v1/employees` accepte `resource_assignments` (type/ressource/niveau, validés contre le registre et l'existence tenant — 422 `RESOURCE_NOT_FOUND` sinon), réservé au `principal` (champ `prohibited` pour tout autre acteur, fail-closed) ; les accès voyagent dans `user_invitations.metadata` et sont créés **à l'activation** (`UserInvitationService::accept`) — s'il n'active jamais, aucun accès n'existe ; une ressource disparue entre-temps est ignorée. **(2) Vue inverse** `GET /v1/resources/{type}/{id}/access` : une ressource → ses collaborateurs, niveaux, qui a donné l'accès et quand (principal uniquement). **(3) Révocation en cascade** : `DepartureService::registerDeparture` supprime les assignations une à une (jamais par requête bulk) pour que **chaque révocation laisse sa ligne d'audit** (trait `Auditable`). **(4) Rapport d'audit** `GET /v1/resource-access/audit` (+ `?format=csv` téléchargeable) : qui avait accès à quoi, quand, donné/retiré par qui — lu depuis `audit_logs` (poses, changements de niveau, révocations, cascade comprise). Contrats OpenAPI ajoutés (Redocly 0 erreur), miroir `dev-hub/openapi/v1.yaml` + SDK JS/Python régénérés ; le docblock d'`AuditLog` corrige au passage un mensonge de type (`old_values`/`new_values` sont **nullables** en base — le `+` naïf sur deux nulls a été trouvé par le test, pas par la relecture). **Vérifié** : `ResourceAccessLifecycleTest` — **4 tests / 17 assertions** couvrant les 4 volets (dont « aucune assignation avant activation » et « l'audit survit à la cascade ») ; aucune régression sur le parcours d'invitation existant (le flux `createAndSend`/`accept` garde sa signature par défaut) ; PHPStan strict `[OK]` ; `pint` PASS. **Hors périmètre, signalé** : l'écran web dédié « qui a accès à quoi » n'est pas livré (l'API l'est) — le panneau R2 côté collaborateur couvre le geste d'assignation ; l'UX d'invitation web ne pose pas encore `resource_assignments` (l'API le supporte).
- **feat(rbac): généralisation du patron ressource-scopé — Travel, Fleet, Fuel, Edu, Cameras, garde anti-conditions-mortes, RBAC_SYSTEM.md réaligné (Closes #7600, R3 de l'épique #7597).** Le motif mort mesuré par l'épique (`hasManagerRole` élargi à des valeurs qui n'ont **jamais** été assignables via l'API — `manager`, `server`, `kitchen`, `rider`, `agent`, `checkin` : la condition était toujours fausse, seuls principal/rh passaient) est **éradiqué du dépôt** : 41 fichiers corrigés (≈25 policies + 12 contrôleurs TravelAgency, CRM, Catalog, Attendance, EduCampus — `rg "hasManagerRole\([^)]*'(manager|server)'"` → **0 occurrence**), et une **garde CI** `check-manager-role-enum.sh` (workflow `resource-rbac-guards.yml`) interdit son retour : tout littéral passé à `hasManagerRole()` doit appartenir à l'enum assignable (`principal, rh, dept, comptable, superviseur, marketing`). Le patron du pilote R2 est généralisé par un trait **Core** réutilisable `ChecksResourceScopedAccess` (jamais de `Core → Modules`, garde #5584) appliqué aux policies d'ancrage des verticales : `VehiclePolicy` (Fleet — « tout manager voit tout » ne survit pas à la première assignation `vehicle`), `FuelStationPolicy`, `EduCampusPolicy` (la constante `MANAGER_ROLES` perd sa valeur morte), `TravelOfficePolicy`/`TravelStationPolicy` (les ancres du scoping Travel) ; `CameraPolicy` combine l'assignation `camera` avec son système `CameraPermission` existant de façon **additive** (un employé assigné lit/gère sans rôle manager, aucun accès existant n'est retiré). Le registre `config/resource_types.php` s'étend à `edu_campus`, `fuel_station`, `travel_office`, `travel_station` (fail-closed : un type hors registre reste refusé). `docs/security/RBAC_SYSTEM.md` documente la couche « accès aux ressources » au même titre que dept/superviseur (règle PA2-SEC-005 : la doc suit le code). **Vérifié** : `ResourceScopedVerticalPoliciesTest` (miroir de `ResourceScopedRbacTest` — 3 tests/23 assertions : comportement historique conservé avant assignation, fail-closed après, valeur morte sans effet) ; PHPStan strict `[OK]` sur l'ensemble des chemins du check requis ; les deux gardes du workflow vertes ; `actionlint` OK. **Portée assumée** : les policies d'ancrage de chaque verticale sont scopées ; le scoping des ressources **filles** Travel (billets, réservations par gare/bureau) suit le même patron et reste à généraliser tranche par tranche.
- **feat(restaurant): pilote R2 du RBAC ressource-scopé — le gérant gère SA succursale, le serveur opère SA branche, l'employé non assigné ne lit plus les données métier (Closes #7599, R2 de l'épique #7597).** Les 31 policies RestaurantManager reposaient sur « même `company_id` + rôle », avec les **conditions mortes** `hasManagerRole(...,'manager','server')` (valeurs hors enum assignable — seuls principal/rh passaient réellement) et une **lecture métier ouverte à tous** (`viewAny=true`, `view=company_id` : commandes, stocks, COGS, caisses lisibles par tout employé self-service). Cette tranche branche le socle R1 (#7598) sur le pilote Restaurant : **(1) policies ressource-scopées** — trait `ChecksRestaurantBranchAccess` (lecture = `view`, POS/commandes/service = `operate`, menus/prix/stocks/achats/rapports/COGS/référentiels = `manage`), appliqué aux 31 policies ; les conditions mortes sont supprimées et **remplacées par l'assignation** ; règle de progressivité conservée (aucune assignation `restaurant_branch` dans l'entreprise → comportement historique, dès la première → fail-closed) ; ressources company-wide (fournisseurs, unités, taux de taxe, programme fidélité) : écriture `manage` réservée au `principal` dès que le scoping est actif. **(2) Listings bornés** aux succursales accessibles (`accessibleResourceIds`) dans 19 contrôleurs (`branches` lui-même compris — le sélecteur de branche du dashboard web est donc limité côté serveur) + alertes stock + surfaces mobiles (serveur/manager/sync). **(3) Trous bouchés (trou n°4 de l'épique)** : `RestaurantCogsController` exige `manage` sur la succursale de la session (donnée de coûts sensible), la file cuisine exige `operate` sur LA branche (l'ancien `isKitchenRole` listait des valeurs mortes), les `show()` de 25 contrôleurs appellent désormais la policy `view` (le détail d'une commande d'une autre branche → 403), et les opérations offline (`RestaurantMobileSyncService`) re-vérifient la branche par opération. **(4) Garde CI** `check-vertical-controller-policies.sh` (workflow `resource-rbac-guards.yml`) : tout contrôleur RestaurantManager doit appeler une policy, allowlist justifiée (webhooks signés, callbacks PSP, santé, surfaces publiques, kiosque, mobiles dont l'autorisation vit dans le service). **(5) UI web** : panneau « Accès & ressources » par collaborateur dans `employees` (matrice ressources × aucun/lecture/opérer/gérer, types restaurant_branch/site/vehicle/camera, branché sur l'API R1, i18n ×4). **Tests réalignés sur le nouveau contrat** : les acteurs de flux qui utilisaient les valeurs mortes deviennent `principal`, les personas RBAC (manager de salle, serveur, cuisinier) deviennent des employés porteurs d'une **assignation** (`AssignsResourceAccess`) — `RestaurantRbacMatrixTest` prouve désormais qu'un gérant assigné gère SA succursale et pas l'autre, et la nouvelle suite HTTP `RestaurantResourceScopedRbacTest` couvre les 4 critères de l'issue (gérant refusé sur l'autre branche, serveur borné à sa branche, non-assigné fail-closed, comportement inchangé avant la première assignation). **Vérifié** : PHPStan strict, garde contrôleurs↔policies verte, `actionlint` OK, front `tsc`/`eslint --max-warnings 0`/`jest` verts, `pint` PASS, i18n validée ×4 locales.
- **chore(migrations): audit de clôture de la dérive de schéma Travel — 0 duplication restante, la garde « une table, une migration » passe en zéro tolérance (Refs #7452).** L'issue #7452 recensait 28 tables `travel_*` déclarées par 2 à 3 migrations divergentes et 8 colonnes fantômes (`contact_id`, `from_currency`, `label`, `hours_before_departure`, `passenger_count`, `travel_routes.code`, `starts_at`, `slug`). L'audit mené sur `main` à jour (après les tranches #7467 — consolidation Travel — et #7571 — EDU, dette soldée à l'échelle du dépôt) confirme qu'**aucune dérive de schéma ne subsiste** : la garde `check-duplicate-schema-create.py` mesure **381 tables, 0 dupliquée, 0 divergente** (y compris en `--strict --fail-on-duplicate`), plus aucune table travel n'a deux `Schema::create` (formes de garde négative ET inverse vérifiées, leçon de la recette #7452), et chacune des colonnes citées est couverte — `label`/`from_currency`/`hours_before_departure`/`starts_at`/`slug` par `2026_09_15_001600_7452_consolidate_travel_duplicate_schema`, `passenger_count` par le `DROP NOT NULL` de `2026_09_15_000001_7452_relax_legacy_travel_not_null`, `contact_id` par l'alignement du code sur `contact_identifier`/`customer_contact_id` (#7445), `travel_routes.code` renseignée par la factory et exigée par `StoreTravelRouteRequest`. Conséquence actée : le workflow `migration-duplication-guard.yml` passe du mode régression (dette affichée, non bloquante) au mode **`--strict --fail-on-duplicate` par défaut sur PR et push main** — toute redéclaration d'une table existante est désormais rouge, même sans divergence de colonnes. Deux migrations reçoivent au passage leur polissage Pint (lignes vides surnuméraires, fin de fichier). Les échecs restants de `tests/Feature/Travel` (~181) sont **fonctionnels, pas schéma** (cf. AGENTS.md, recette #7452) : ils relèvent de #7420/#7417/#7445 — c'est pourquoi cette PR référence #7452 sans la fermer.
- **chore(ci): Pint au vert sur deux migrations tenant — reprise du commit orphelin de `fix/2026-09-16-tranches-7452-schema-audit` (contexte #7452, sans le fermer).** Deux lignes vides surnuméraires (`2026_04_01_000104_create_payrolls_tasks_evaluations_notifications.php`, `2026_08_30_000500_5803_create_fuel_stock_tables.php`) signalées par Pint. Style uniquement : **aucun changement de schéma ni de logique**, diff net -3 lignes. La branche orpheline (53 commits derrière `main`) est supprimée après merge ; son unique commit vivait ici.
- **fix(ci): la garde governance et `deploy-main` suivent l'archivage de `PILOTAGE.md` (Closes #7627, suite de #7622).** La refonte #7622 a déplacé `PILOTAGE.md` vers `docs/archive/` sans mettre à jour ses deux consommateurs fonctionnels : `check-governance.ps1` exigeait encore le fichier à la racine — `Governance Gates` rouge sur **toute PR touchant `api/**`** (constaté sur #7623 ; #7622 n'a pas déclenché la garde, `tests.yml` étant filtré `api/**`) — et le step « Resolve program version » de `deploy-main.yml` lisait `PROGRAM_VERSION` au même endroit, promettant un `Unable to resolve PROGRAM_VERSION` au prochain déploiement (même famille de panne que la dérive Render #7304). Les deux références pointent vers `docs/archive/PILOTAGE.md` (le marqueur `# PROGRAM_VERSION = 4.24.0` y est conservé) et l'entrée morte `PILOTAGE\.md` sort du motif de périmètre critique. Reste ouvert (dit dans l'issue) : sortir `PROGRAM_VERSION` d'un fichier archivé.
- **fix(ci): la garde Governance Gates suit PILOTAGE.md dans `docs/archive/` — le check requis était rouge pour toute PR à périmètre critique.** La refonte documentaire #7428 a déplacé `PILOTAGE.md` (archivé #6698) de la racine vers `docs/archive/PILOTAGE.md` sans mettre à jour `dev-hub/tools/check-governance.ps1`, qui exigeait toujours le fichier à la racine : depuis ce merge, **toute PR touchant un périmètre critique échouait** sur `FAIL: Missing required governance file: PILOTAGE.md` (constaté sur #7623). La liste `requiredFiles` et le motif `criticalPattern` pointent désormais vers `docs/archive/PILOTAGE.md`. **Vérifié** : `pwsh dev-hub/tools/check-governance.ps1` rejoué localement sur le diff exact de la PR — rouge avant, vert après (toutes les gardes passent).
- **feat(ci): la garde du gate de déploiement ne peut plus être contournée par un préfixe, un élargissement de prédicat ou une dérive push ↔ pull_request (Closes #7577, reliquat de #7533 porté SANS sa régression).** La garde `check-deploy-gate-outcome.sh` vérifiait l'union/inclusion des prédicats du gate contre les `paths:` de `push:` des workflows requis, mais son sens ⊆ ne lisait que les littéraux `p === '…'` : un `p.startsWith('…/')` ajouté à un prédicat passait sous le radar, les `paths:` de `pull_request:` pouvaient diverger de `push:` sans détection (le feu vu en PR ne prédisait plus le run que le gate exige après merge), et rien n'interdisait à une PR d'ÉLARGIR un prédicat pour faire passer son merge. **Quatre durcissements** : (1) **parité stricte `push:` ↔ `pull_request:`** pour chaque workflow requis (`tests.yml`, `web-ci.yml`) ; (2) **couverture des préfixes `startsWith`** — chaque préfixe du gate doit correspondre à un pattern `<préfixe>/**` d'un workflow requis, et un préfixe sans `/` final est refusé (un startsWith non borné matcherait des chemins voisins) ; (3) **pas de relâchement** — aucun chemin ne peut être couvert par les DEUX prédicats, et `isApiPath` ne peut JAMAIS couvrir `front/admin-dashboard/**` : c'est précisément la régression qui a fait écarter la PR de #7533 (`api_changed` pilote le redéploiement Render — l'élargir redéploierait l'API sur chaque merge front-only, #7530 documente l'exclusion inverse) ; (4) **rôle de `.github/paths-filters.yml` rendu explicite et vérifié** — source de dorny/paths-filter dans `tests.yml` (detect-changes), PAS des prédicats du gate, volontairement plus large (leçon #7528) ; la garde vérifie seulement qu'il ne devient jamais plus ÉTROIT que les déclencheurs requis (sinon detect-changes rate des changements que les workflows voient). **Quatre nouvelles mutations d'auto-test** (6 à 9 : divergence PR, `isApiPath` élargi au front, préfixe `mobile/apps/` sans workflow, `paths-filters.yml` rétréci). **Vérifié** : `--self-test` vert (9 mutations) ; garde verte sur le dépôt ; chaque mutation échoue avec le message du contrôle VISÉ (vérifié message par message) ; **A/B inversé contrôle par contrôle** — parité neutralisée → le self-test échoue sur la mutation 6, contrôle des préfixes retiré → mutation 8, anti-relâchement retiré → mutation 7, rôle de paths-filters neutralisé → mutation 9 ; test comportemental `check-deploy-gate-outcome-test.mjs` inchangé au fond (10 verdicts verts), son en-tête documente le partage des rôles. Périmètre : la garde, son auto-test, l'en-tête du test comportemental, une ligne du `REGISTRE_GARDES.md` — **aucun workflow ni prédicat modifié** (le durcissement fige l'existant, il ne le change pas).
- **feat(onboarding): écran de bienvenue de première connexion, affiché UNE seule fois (Closes #7604, tranche du critère 2 de #7490).** L'utilisateur qui venait de valider son code d'inscription atterrissait dans son espace **sans qu'on lui explique ce qui venait de se passer ni comment il se reconnecterait** : l'unique drapeau serveur existant (`metadata.onboarding_completed`, #7262/#R8) décrit la **fin** de l'assistant de configuration, pas la bienvenue. Nouvelle route `POST /api/v1/onboarding/welcome-ack` : persiste `companies.metadata.welcome_seen_at` via le writer tenant-safe existant (requête qualifiée `public.companies`), **idempotente** (la date n'est jamais réécrite, second appel → `already_acknowledged: true`), RBAC **responsable** (`principal`/`rh`, miroir de `CompanyModuleController::activate`) ; aucune route de lecture nouvelle — `/auth/me` expose déjà `company.metadata`. Côté web, écran **non bloquant** (les deux issues acquittent) monté dans le layout de l'espace **avant** l'assistant d'accueil (jamais empilés), avec une garde `shouldShowFirstLoginWelcome` qui est le **miroir exact du RBAC serveur** (un comptable ne voit pas un écran dont l'acquittement répondrait 403 et reviendrait à chaque connexion) ; copie lue dans le **catalogue i18n** (4 locales, repli fr) et non dans l'arbre `getCopy` codé en dur. `api/openapi.yaml` documenté. **Vérifié** : API **6 tests / 23 assertions** ; web **9 tests** + zone dashboard **22 suites / 102 tests** vertes ; `tsc --noEmit` 0 erreur ; `eslint --max-warnings 0` OK ; **PHPStan strict 0 erreur** ; `pint` PASS ; garde de parité i18n verte. **Deux défauts trouvés par les tests et corrigés** : la garde sans société renvoyait `true` (écran inacquittable) et le garde anti-double-clic posé en `state` laissait partir **deux** POST (garde synchrone par `ref`).
- **feat(platform): les leads d'acquisition de la vitrine deviennent enfin LISIBLES par l'admin (tranche de #7595).** La table globale `marketing_leads` recevait les **5 formulaires publics** de la vitrine depuis des mois (`POST /marketing/leads`), mais **aucune route ne la relisait** : pas un seul `GET` n'existait. Un lead était donc écrit puis invisible — un commercial ne pouvait pas rappeler une demande de démo sans ouvrir la base, et la seule lecture existante (`PlatformSolutionSurveyStatsController`) n'est qu'un agrégat restreint au type `solution_survey`. Le comble : `MarketingLeadRepository::all()` existait déjà côté Marketing… **sans aucun appelant**. Nouvelles routes `GET /platform/marketing/leads` et son miroir `GET /admin/marketing/leads` (contrat SPA admin-dashboard), toutes deux gardées par `platform.permission:crm.view` — permission que le rôle plateforme `marketing` porte déjà. Filtres `type`/`status`/`source`/`search` (e-mail, `external_id`, page, campagne), période `from`/`to`, pagination bornée 1..100, enveloppe `{data, meta}` avec compteurs d'onglets **globaux** (volontairement non filtrés : filtrer dessus les rendrait inutilisables dès qu'un filtre de statut est actif). **Isolation des modules respectée** : lecture via `DB::table('marketing_leads')`, **aucun import du module Marketing** — `api/ARCHITECTURE.md` interdit tout `use App\Modules\<X>\…` depuis un module ≠ X (garde #5584, bloquante en CI) ; corollaire assumé et documenté, les listes de types/statuts sont dupliquées et non importées. **Deux bugs silencieux désamorcés au passage** : (1) lu par le query builder et non par Eloquent, un `jsonb` revient de PDO en **chaîne** — le cast `array` du modèle ne s'applique pas, donc sans décodage la SPA aurait reçu le message de contact sous forme de chaîne JSON échappée, une donnée « présente mais inexploitable » ; (2) une borne haute `to=2026-09-17` posée à minuit par `Carbon::parse()` **ferait disparaître la journée entière** du filtre, sans aucune erreur visible — la borne couvre désormais la fin de journée, sauf si elle porte déjà une heure explicite. **Vérifié** : **PHPUnit 12 tests / 57 assertions verts** sur la nouvelle suite (le sandbox exécute désormais la vraie suite API : préfixe PHP rootless + PostgreSQL 14 rootless), `pint --test` PASS sur les 2 fichiers touchés, PHPStan strict — et **preuve A/B** que les tests discriminent : borne à minuit rétablie → le lead de 12:00 **disparaît** du filtre ; décodage retiré → `payload` revient en chaîne échappée. Périmètre : `api` uniquement, aucune migration. **Reste dans #7595** : collecte first-party d'événements de visite et écran webadmin dédié.
- **feat(tenant): un responsable peut donner SES ressources à SES collaborateurs — socle R1 de l'accès ressource-scopé (Closes #7598).** Jusqu'ici les ressources d'un tenant (restaurants, véhicules, caméras, sites) n'étaient **pas** un objet d'autorisation : toute policy se contentait de « même `company_id` + rôle », donc « Moussa gère le restaurant Almadies » était inexprimable. Tranche socle de l'épique #7597 : table tenant `employee_resource_assignments` (`employee_id`, `resource_type`, `resource_id`, `access_level` ∈ `view|operate|manage` ordonnés, `created_by`, unique (collaborateur, ressource)) et helpers `Employee::resourceAssignments()`, `hasResourceAccess(type, id, min)`, `accessibleResourceIds(type, min)` (règle de **progressivité** : aucune assignation pour un type → comportement historique conservé ; dès la première, le type est **fail-closed** pour les non-assignés). API : `GET/PUT /v1/employees/{id}/resource-assignments` (le `PUT` remplace le jeu complet — c'est ce qui rend la **révocation** possible en un geste ; réservé au `principal` du tenant, jamais sur un employé d'une autre société) et `GET /v1/resources/{type}` (le sélecteur du responsable, filtré par ce que l'acteur voit du type ; type inconnu → 404 `RESOURCE_TYPE_UNKNOWN`, ressource d'un autre tenant → 422 `RESOURCE_NOT_FOUND`). Registre déclaratif `config/resource_types.php` (aucun import croisé de modules : R3 étendra la liste) ; chaque pose/modification/révocation est **auditée** via le trait `Auditable` (aucun code d'audit dans les contrôleurs) ; 5 codes d'erreur ajoutés **×4 langues** (`RESOURCE_ACCESS_DENIED`, `RESOURCE_TYPE_UNKNOWN`, `RESOURCE_NOT_FOUND`, `RESOURCE_ASSIGNMENTS_REQUIRED`, `RESOURCE_ACCESS_LEVEL_INVALID`) ; contrat OpenAPI + miroir `dev-hub/openapi/v1.yaml` + SDK JS/Python régénérés. **Vérifié** : `ResourceScopedRbacTest` (miroir de `DepartmentScopedRbacTest`) — **14 tests, 61 assertions**, dont « avant toute assignation le comportement est inchangé », « la 1re assignation scope le type en fail-closed », « le niveau décide (`operate` ne satisfait pas `manage`) », « le `PUT` révoque ce qui n'est plus envoyé », « un type inconnu et une ressource d'un autre tenant sont refusés », « chaque geste est audité » ; PHPStan strict (level 8) sans erreur sur les fichiers touchés ; gardes du dépôt vertes (migrations sans collision, pas de duplication `Schema::create`, couverture OpenAPI des nouvelles routes, module isolation) ; `actionlint`/`pint` OK.
- **feat(vitrine): politique de confidentialité complétée, mentions légales publiées, transparence sous les formulaires (Closes #7593).** Suite de l'audit vitrine : GA4/Mixpanel partaient sans consentement (corrigé juste avant), mais la page de confidentialité restait **muette sur l'essentiel** — ni responsable de traitement, ni bases légales, ni durées de conservation, ni destinataires/sous-traitants, ni transferts hors UE, ni section cookies, ni autorité de contrôle — et le formulaire de contact, la demande de démonstration et l'inscription à la newsletter ne portaient **aucune mention** de finalité.
  · **Politique de confidentialité complétée** (×4 langues) : responsable de traitement (votre entreprise pour les données de vos salariés, Leopardo RH **sous-traitant** ; Leopardo RH responsable pour la vitrine et la relation commerciale), bases légales traitement par traitement, durées de conservation (compte 12 mois, journaux 12 mois, paie selon les durées légales, mesure d'audience 14 mois, choix de consentement 6 mois), destinataires et sous-traitants, transferts hors UE et garanties, section cookies renvoyant au contrôle « Gérer mes cookies », exercice des droits **et** voie de réclamation (ANPDP en Algérie, autorité nationale dans l'UE), information en cas de violation de données.
  · **Page mentions légales** (`/mentions-legales`, ×4 langues) : éditeur, hébergement (infrastructure UE), propriété intellectuelle, renvoi vers la politique. Elle répare un lien trompeur du pied de page — « Mentions légales » pointait vers les CGU avant le lot vitrine — et rejoint le sitemap. **Les identifiants d'éditeur (dénomination, siège, immatriculation, identifiant fiscal, nom de l'hébergeur) ne sont pas dans le dépôt et n'ont pas été inventés** : la page indique comment les obtenir, et l'en-tête du fichier de contenu liste précisément ce qui reste à fournir avant mise en production.
  · **Transparence au moment de la collecte** : composant unique `FormDataNotice` sous les formulaires contact, démonstration et newsletter — finalité, base légale, lien vers la politique — avec les mêmes libellés localisés ×4.
  **Filet** : `e2e/legal-pages.spec.ts` (6 tests) vérifie que les trois pages répondent et exposent leur contenu, que les mentions attendues sont réellement des **titres de section rendus**, que le pied de page pointe vers `/mentions-legales` (et non vers les CGU, régression possible du lot précédent), que les mentions sont affichées sous les formulaires, et que le sitemap publie la page. Vérifié : `jest` 120 suites / 987 tests, `tsc --noEmit`, `eslint --max-warnings 0` (src + e2e), `validate.js` + parité i18n (2 × 20 clés propagées aux 4 surfaces).
- **feat(vitrine): bandeau de consentement, Consent Mode et traceurs coupés par défaut — GA4 et Mixpanel ne partent plus avant un accord explicite (participe à #7593).** Constat mesuré (audit du 2026-09-16) : GA4 et Mixpanel étaient injectés dans le `<head>` (`strategy="afterInteractive"`) pour **tout visiteur**, avant même qu'il ait pu s'exprimer, conditionnés au seul drapeau d'environnement `NEXT_PUBLIC_ENABLE_ANALYTICS` ; **aucun bandeau de consentement n'existait** dans le dépôt, aucun choix n'était mémorisé, et le Consent Mode de Google était absent.
  · **Refus par défaut, et ce n'est pas qu'une intention** : le Consent Mode v2 est posé en `denied` (`beforeInteractive`, donc avant tout script de mesure) ; les traceurs ne sont injectés qu'après un accord (`ConsentScripts`) ; et `getAnalytics()` renvoie un **client inerte** tant que la mesure d'audience n'est pas accordée — deuxième barrière, pour qu'un `gtag` présent malgré tout (extension, autre script) ne suffise pas à déclencher un envoi. Les usages existants (`useAnalytics`, `trackSignup`…) deviennent donc sans effet sans consentement, sans qu'aucun appelant change.
  · **Bandeau sans dark pattern** : « Tout accepter » et « Tout refuser » au même niveau et au même poids visuel, « Personnaliser » pour choisir par finalité (nécessaires verrouillés, mesure d'audience, personnalisation marketing), et un contrôle de retrait « Gérer mes cookies » dans le pied de page — retirer son consentement doit être aussi simple que de le donner. `role="dialog"`, titre et description reliés, cases à cocher natives (pas de `<div>` cliquable).
  · **Choix mémorisé** (cookie first-party 6 mois + `localStorage` en secours), horodaté et **versionné** : si le texte change, le visiteur est re-sollicité — un consentement n'est pas un blanc-seing permanent.
  · **i18n ×4** (17 clés par langue dans `shared/i18n`, propagées par les 3 synchronisations).
  **Preuve en bout-en-bout** (serveur de dev avec `NEXT_PUBLIC_ENABLE_ANALYTICS=true` et identifiants de mesure) : **0 requête** vers `googletagmanager`/`mxpnl` avant tout choix et après un refus ; après acceptation, 3 requêtes, `window.gtag` défini, Consent Mode poussé en `default` puis `update`, cookie écrit. 5 tests e2e dédiés (`consent.spec.ts`) + 11 tests unitaires (état/stockage/versionnement du consentement, inactivité totale du client analytics, couverture de sa surface).
  **Reste sur #7593** : complétion de la politique de confidentialité (responsable de traitement, bases légales, durées, sous-traitants, transferts, CNIL, DPO) et mentions d'information sous les formulaires.
- **test(travel): les fixtures d'annonces voyage utilisent les colonnes et le contrat réels (Closes #7617, tranche de #7420/#7467).** Les fixtures de `TravelAdvertExpirationTest` et `TravelAdvertLifecycleTest` écrivaient des colonnes qui n'existent dans **aucune** migration (`body_redacted`, `character_count`, `total_minor`, `price_image_minor`, `published_at`) et omettaient `price_minor` (`NOT NULL` sans défaut) : l'INSERT ne pouvait pas aboutir, quelle que soit la migration appliquée — c'est ce qui avait fait conclure à tort (#7467) à des colonnes « zombies » du chantier #7452. Sur `travel_advert_prices`, la fixture écrivait hors `$fillable` (`price_image_minor` → `price_per_image_minor`), affectation silencieusement ignorée. Les attentes sont alignées sur le contrat réel : payload `content` (ce que `StoreTravelAdvertRequest` réclame déjà), prix `data.price_minor` calculé côté serveur par `SubmitTravelAdvertAction`, `published_at` retirée (`validated_at`). **Aucun code de production touché** — deux fichiers de test.
- **fix(trial): le verrou applicatif de `/trial/verify` redevient observable, et le test du profil `solo` est aligné sur le plancher #7423 (Closes #7609).** `api/tests/Feature/SelfServiceTrialTest.php` était **rouge sur `main`** (2 échecs), reproduits sans changement local. (1) `/trial/signup`, `/trial/verify` et `/trial/set-password` partageaient `throttle:5,15` : mesuré sur `main`, la 5ᵉ vérification recevait **`429 TOO_MANY_REQUESTS`** (le throttle) au lieu du **verrou applicatif** (`otp_locked_until` → `429 OTP_TOO_MANY_ATTEMPTS`) — l'utilisateur n'apprenait donc jamais qu'il était verrouillé 15 minutes, et un inscrit légitime pouvait être bloqué sans comprendre pourquoi, y compris sur la définition de son mot de passe. Seau **dédié** `trial-verify` (10/15 min, clé e-mail + IP) ; **`throttle:5,15` conservé sur `/trial/signup`** (garde anti-spam intacte, verrouillée par un test dédié). Note : le verrou se pose **sur** le 5ᵉ échec (le test attendait `5 × 400` puis le verrou — expectation jamais atteignable — et `TOO_MANY_ATTEMPTS`, chaîne absente de l'API) ; le test est aligné sur le contrat réel, justification écrite dans le fichier. (2) Le test `solo` exigeait `false` pour **tous** les `TEAM_TOOLS`, `attendance` inclus, en contradiction avec `SOLO_FLOOR_TOOLS` (#7423, livré) : la boucle asserte désormais le plancher à `true`. **Vérifié** : **17 tests / 129 assertions, 0 échec** ; PHPStan strict 0 erreur ; `pint` PASS.
- **fix(api): la dette EDU « une table, plusieurs migrations » est soldée — un fichier canonique par table, schéma prouvé IDENTIQUE (Closes #7571, tranche de #7452).** **14 tables EDU étaient déclarées 2 à 4 fois** (`edu_admissions`, `edu_assessments`, `edu_report_cards`, `edu_classes`, `edu_subjects`, `edu_teacher_subjects`, `edu_academic_years`, `edu_grades`, `edu_grade_versions`, `edu_attendance_corrections`, `edu_attendances`, `edu_students`, `edu_guardians`, `edu_report_card_lines`) par **trois générations successives** de migrations. Toutes ces déclarations sont gardées par `if (! schemaTableExists(...))` : **la première exécutée crée la table, les suivantes ne s'exécutent jamais** — leur contenu est du code mort, y compris les `CHECK` et index qu'elles déclarent. **La mesure de l'issue était fausse — et la garde en était la cause** : les « 45 colonnes à rattraper » existent toutes, apportées par `2026_09_14_000002_5819_repair_edu_schema_drift` ; `check-duplicate-schema-create.py` ne lit que les `Schema::create`, donc il ne voit ni les migrations de réconciliation ni les `ALTER`. **Arbitrage retenu, table par table** : le fichier canonique est celui qui **crée réellement la table** (la 1ʳᵉ déclaration) — c'est le seul choix qui préserve le schéma ; adopter les déclarations de 2ᵉ/3ᵉ génération aurait **ajouté** des `UNIQUE`/index jamais appliqués (ex. `edu_assessments_company_class_subject_title_unique`), soit un changement de schéma que l'issue ne demande pas. **13 fichiers entiers** retirés (code mort, aucune référence externe hors le document d'audit régénéré), et **un élagage correctif** : `..._000707_5821_create_edu_attendance_tables` déclarait aussi `edu_attendance_corrections` — le bloc **et** son `Schema::dropIfExists` sont retirés ensemble, sinon un `rollback` aurait supprimé une table que ce fichier ne crée plus. **Le vrai verrou était le test de contrat** : `EduManagerMigrationsTest` et `EduAcademicYearTest` listaient comme créateurs les fichiers de 2ᵉ génération, donc ils **exigeaient que le code mort reste** ; leurs listes sont alignées sur l'arbitrage — le test asserte désormais la réalité, pas une intention. **Preuve d'équivalence de schéma, avant/après** : installation **fraîche** (`migrate:fresh` public puis tenant, schéma `shared_tenants` recréé) et `pg_dump --schema-only` comparés — **strictement identiques** (seuls les jetons de session de `pg_dump` diffèrent). **Vérifié** : garde **380 tables, 0 dupliquée, 0 divergente** — la dette est soldée **à l'échelle du dépôt**, `--strict` et `--fail-on-duplicate` passent (jusqu'ici « hors d'atteinte » par construction) ; suite **EduManager 280 tests / 1292 assertions OK** ; `php -l` OK ; `pint --test` **PASS** ; PHPStan strict **`[OK] No errors`**. **Artefact rendu reproductible** : `check-duplicate-schema-create.py --write` régénère `docs/audits/MIGRATIONS_DUPLIQUEES_TENANT.md`, qui annonçait encore **24** tables dupliquées alors qu'il n'en restait 0 — un document généré qu'on ne peut pas régénérer finit toujours par mentir ; `--audit` garde son contrat d'affichage. **Assumé et signalé, hors périmètre** : les déclarations mortes portaient des `CHECK`/index qui n'ont **jamais** été appliqués ; les rétablir est un changement de schéma délibéré (ex. `edu_academic_years_status_check`, index `edu_attendance_corrections_company_attendance_idx`), à trancher côté conception.
- **feat(platform): la piste d'audit d'une suppression de tenant reste lisible après la purge (Closes #7576).** `GET /platform/tenant-deletion-audits?company_id=&slug=&limit=` rend la preuve d'une suppression **sans exiger qu'une entreprise existe encore** : le seul lecteur existant, `GET /platform/companies/{company}/deletion-audits`, passe par `PlatformCompanyLookup::findOrFail` et répond donc 404 précisément après une purge réussie — l'opérateur ne pouvait plus répondre à « qui a supprimé cet espace, quand, pourquoi, et avec quel volume ». Filtres `company_id` (uuid), `slug`, `limit` (1-100, défaut 20), permission plateforme `companies.view`, opération documentée dans `api/openapi.yaml` (miroir SDK régénéré). La réponse ne contient que des compteurs, des identités plateforme et la justification — aucune donnée locataire.
- **fix(api): les deux dernières annotations PHPDoc désarmées de la dette #7582 (Closes #7602, tranche de #7582).** La garde `check-double-docblock.py` livrée par #7590 mesure encore **45** occurrences après la fusion ; cette tranche en solde **2**, à **43**. `Attendance/Application/Actions/SetEmployeeAttendanceMode` portait un `@throws GpsConsentMissingException` **désarmé** par le `@param array<string, mixed> $data` d'un second docblock (seul le dernier docblock compte : l'annotation du premier était ignorée) — fusion en une seule, dans l'ordre imposé par Pint (`@param` avant `@throws`). `tests/Feature/ProcessBulkPaymentJobTest` portait un `@return array{0: Company, 1: Employee}` désarmé par une note #6548 placée dans un second docblock. **Ces deux fichiers ne sont pas un oubli** : ils venaient du lot concurrent fermé pour doublon de sujet (#7588), dont le commentaire de fermeture s'engageait à ne rien perdre en les reprenant — engagement honoré ici, #7582 étant déjà clos (d'où la sous-issue de tranche, PA2-OPS-008). **Vérifié** : `php -l` OK sur les deux ; `pint --test` **PASS** sur les deux ; PHPStan strict sur `app/Modules/Attendance` → **`[OK] No errors`** ; garde verte, dette **45 → 43**. **Correctif complémentaire (même tranche, check requis rouge)** : la fusion des deux docblocks de `tests/Feature/ProcessBulkPaymentJobTest` a **armé une annotation fausse** — le `@return array{0: Company, 1: Employee}` était un reliquat du helper `companyAndManager()` (sa vraie place, l. 390) collé sur un test dont le type natif est **`void`**. Tant que les deux docblocks restaient séparés, PHPStan ne voyait que le second et l'annotation dormait ; en les fusionnant, l'erreur est devenue visible — c'est le check requis `PHPStan — Strict` qui l'a signalée (`return.phpDocType`, 1 erreur). L'annotation parasite est retirée. **A/B mesuré** : `phpstan analyse --configuration=phpstan-strict.neon tests/Feature/ProcessBulkPaymentJobTest.php` → **1 erreur** avant, **`[OK] No errors`** après. Leçon : « armer » une annotation ne consiste pas seulement à la faire voir par l'analyseur — il faut d'abord vérifier qu'elle est **vraie**.
- **feat(web): les 3 schémas zod morts sont branchés, les formulaires publics parlent la langue du site et le checkout valide enfin la carte (Closes #7594, dernière tranche).** La tranche anti-bot (honeypot + time-trap réel + contrôle d'origine sur les 5 routes `/api/forms/*`, et la récupération du champ `company` côté serveur) était déjà sur `main` ; restaient les trois points de l'audit côté client. **(1) Schémas branchés, localisés ×4** : `contactFormSchema`, `demoFormSchema` et `newsletterFormSchema` n'étaient référencés que par leurs tests — la validation réelle se limitait au HTML5, donc des messages dans la langue du **navigateur**, pas du site. Ils deviennent des fabriques par locale (même patron que `signupFormSchema`, #2648), alimentées par un nouveau bloc `forms.validation.*` dans `shared/i18n/locales` (source de vérité, 4 locales, resynchronisé vers web/admin/mobile — parité et `validate.js` verts), et sont appelés en `safeParse` avant tout appel réseau par `/contact`, `/demo` et les **deux** formulaires newsletter ; `contactFormSchema` déclare désormais `company` (le schéma client l'aurait perdu comme le serveur avant la tranche 1). **Piège trouvé en câblant** : le sujet du formulaire de contact vient d'un `<select>` dont les **valeurs sont les libellés localisés** — en arabe « أخرى » (Autre) fait 4 caractères, et le `min(5)` du schéma serveur rejetait donc un parcours réel ; aligné à `min(2)` côté serveur et client. **(2) A11y des formulaires publics** : chaque champ validé porte `aria-invalid` + `aria-describedby` vers son message d'erreur rendu dans une région `aria-live="polite"` (présente dans le DOM avant l'erreur, sinon rien n'est annoncé), les erreurs globales passent en `role="alert"`/`aria-live` (contact, demo, newsletter ×2, hero e-mail, checkout) ; le checkout, qui avait **0 `htmlFor`**, relie ses 10 labels (compte + paiement) à des `id` dédiés. **(3) Validation carte du checkout** : elle était « non vide » seulement — n'importe quelle suite de chiffres partait au serveur. Nouveaux helpers purs dans `validation.ts` : `luhnCheck` (somme de Luhn, espaces tolérés, 12–19 chiffres), `isCardExpiryValid` (format MM/AA strict, non passée, le mois courant reste valide) et `isCardCvcValid` (cohérence réseau : 4 chiffres pour Amex IIN 34/37, 3 sinon), câblés dans `StepPayment` avec messages localisés par champ (nouvelles clés `payment.errors.cardNumber|expiry|cvc|cardName` ×4 locales dans `data/checkout.ts`) — ces contrôles évitent un aller-retour inutile, ils ne remplacent pas la vérification du PSP. **Vérifié** : `tsc --noEmit` 0 ; `eslint src --max-warnings 0` 0 ; `check:mojibake` OK ; **Jest 123 suites / 1042 tests verts** (dont 12 nouveaux cas Luhn/expiration/CVC et 12 cas ×4 locales verrouillant que chaque schéma émet le message du catalogue partagé dans la locale demandée, plus un cas `company` conservé côté contact) ; parité i18n backend/shared/ARB verte et catalogues régénérés par les synchroniseurs (`versions.json` à jour). Périmètre : `front/web` + catalogues `shared/i18n` (régénérés : web, admin, ARB mobile).
- **feat(web): les formulaires publics cessent d'être spammables — honeypot, time-trap RÉEL et contrôle d'origine (tranche de #7594).** L'audit avait mesuré **zéro barrière anti-bot** à l'entrée des 5 routes `/api/forms/*` (contact, démo, newsletter, signup, solution-survey) : honeypot absent, contrôle d'origine absent, et le « time-trap » **factice** — les fronts envoyaient un `timestamp` qu'aucune route ne comparait jamais. Nouveau module `_lib/antispam.ts`, évalué **avant toute validation et toute persistance** : (1) **honeypot** — un champ masqué, hors tabulation, `autoComplete="off"` (les trois précautions comptent : un leurre qui capture un utilisateur au clavier ou que l'autocomplétion remplit est un bug, pas une protection) ; (2) **time-trap réel** ; (3) **contrôle d'origine** fondé sur l'origine de la requête et `NEXT_PUBLIC_SITE_URL`/`APP_URL`/`VERCEL_URL`, **aucun domaine codé en dur** (le dépôt se déploie sur dev, staging, prod et previews). **Le piège le plus coûteux a été trouvé en câblant, pas en relisant** : les fronts posaient déjà un `timestamp`, mais **au moment de la SOUMISSION** — comparer cette valeur revient à comparer zéro à zéro, donc un contrôle « trop rapide » sur ce champ aurait rejeté **tous les vrais utilisateurs**. Le time-trap lit désormais un champ dédié, `form_rendered_at`, posé au chargement de la page ; un test verrouille explicitement que le `timestamp` de soumission est **ignoré**. **Deux choix assumés, écrits dans le code** : un signal **absent** n'est pas un signal négatif (sans jeton signé, une requête sans champ est indistinguable d'un appel e2e/intégration — refuser casserait des parcours réels), et le rejet est **silencieux** (même 201 qu'un succès, mais rien n'est persisté : un rejet explicite apprendrait au robot qu'il a été détecté). Câblage : les **5 routes** lisent le corps **une seule fois** sur le brut (les schémas zod ne déclarent ni le honeypot ni l'horodatage de rendu, donc zod les supprimerait), puis les **10 sites d'appel** joignent les champs via `antispamFields()`, et le leurre est rendu dans le DOM par `HoneypotField` (contact, démo, newsletter). **Corrige aussi une perte de donnée silencieuse** : le formulaire de contact collecte `company` (`id="company"`) et l'envoyait, mais `contactSchema` ne le déclarait pas — zod supprime les clés inconnues, donc la valeur **n'atteignait jamais le lead**. **Vérifié** : `tsc --noEmit` 0 ; `eslint src --max-warnings 0` 0 ; **Jest 117 suites / 985 tests verts** (dont 18 cas unitaires sur la règle et 5 cas au niveau de la route) ; et **preuve A/B sur le branchement** — garde retirée de la route : les 3 tests anti-spam **échouent** et les 2 autres passent ; garde restaurée : 5/5. Périmètre : `front/web` uniquement. **Reste dans #7594** (non traité ici) : branchement des 3 schémas zod morts côté client avec messages localisés ×4, `aria-live`/`aria-invalid` sur les formulaires qui n'en ont pas, et validation carte du checkout.
- **fix(ci): la garde des docblocks doubles reçoit une base résolvable — le check requis `actionlint (+ shellcheck)` ne pouvait pas être vert, et ce n'était pas un défaut de code (Refs #7582).** `check-double-docblock.py` était appelé avec `--base origin/main` dans `actionlint.yml`, or un checkout par défaut (`fetch-depth: 1`, ref = SHA de fusion) **ne crée pas** `origin/main` : la garde sortait en **2** (« ref de comparaison introuvable ») et rendait **rouge** le check **requis** `actionlint (+ shellcheck)` — donc bloquait tout lot touchant `.github/**` (constaté sur le lot #7590, cinq issues bloquées sans cause de code). **Correctif** : la base est résolue depuis l'événement (`pull_request.base.sha` → `merge_group.base_sha` → `github.event.before`, convention `architecture-check.yml`), avec repli sur `HEAD~1` puis `git fetch` explicite du SHA (absent d'un clone superficiel) ; si rien ne résout, le message reste explicite et la garde échoue franchement. `--base` accepte un committish, un SHA convient. **Vérifié** : panne reproduite à l'identique dans un clone `--depth 1 --single-branch` (même message, exit 2), puis logique corrigée → « Aucune nouvelle annotation désarmée. (dette existante : 45, inchangée) », exit 0 ; `actionlint` + `shellcheck` verts sur le workflow modifié.
- **feat(devx): PHP 8.4 + composer reconstitués dans une session sans root, pour rejouer le check requis localement (Closes #7585).** L'image d'agent n'a **ni PHP ni composer** : les checks requis `PHPStan — Strict` et les gardes PHP ne pouvaient pas être rejoués localement, et chaque itération coûtait un cycle CI de 10 à 20 min sur une file saturée. `dev-hub/tools/bootstrap-local-php.sh` reconstitue la chaîne entière **sans root** et sans rien installer dans `/` : il lit le `VERSION_CODENAME` réel et la version réellement publiée sur le PPA `ondrej` (rien n'est figé), extrait les `.deb` par `dpkg-deb -x`, fabrique un `php.ini` minimal, installe composer par l'installateur officiel après **vérification du SHA-384**, et écrit un `env.sh` + des shims `php`/`composer`. Trois pièges ont été trouvés et corrigés en le construisant, tous documentés : **(1)** l'`extension_dir` ne se devine pas par tri — `8.4` se trie *après* `20240924` et l'on tombe sur le répertoire de scan-dir, vide ; il faut prendre le répertoire qui contient **réellement** les `.so` ; **(2)** un `.so` qui ne charge pas sort en **code 0**, seul le *texte* du warning le dit, et le test doit être **cumulatif** (`pdo_sqlite` échoue seul alors qu'il est valide, parce qu'il dépend de `pdo` déjà chargé) ; **(3)** surtout, composer relance PHP pour ses scripts (`@php artisan package:discover`) via `PHP_BINARY` **sans propager le `-c`** — sans `PHPRC`, l'enfant démarre sans `php.ini`, ni `tokenizer` ni `mbstring`, et l'échec est trompeur (`Call to undefined function NunoMaduro\Collision\token_get_all()`, enveloppé dans Whoops) ; `env.sh` pose donc `PHPRC`, et les shims le posent eux-mêmes. **Vérifié sur un préfixe vierge** : 21 extensions chargées (seule `zip` est écartée faute de `libzip.so.4` — non bloquant, `composer.json` n'exige aucune `ext-*` et `unzip` prend le relais), `php -v` = 8.4.25, `composer --version` = 2.10.3, `php artisan --version` = Laravel 12.69.1 (donc **l'enfant** fonctionne, ce qui était l'échec d'origine), `vendor/bin/phpstan --version` = 2.2.9, et la commande **exacte** du check requis tourne : `phpstan analyse --configuration=phpstan-strict.neon --memory-limit=1G --no-progress` → `[OK] No errors`. Idempotence mesurée : 2ᵉ exécution en **3,1 s**, sans re-téléchargement. Documenté dans `DEVELOPMENT.md` (section « Session d'agent sans root »).
- **fix(web): les panneaux d'en-tête se referment enfin au clic sur leur propre déclencheur (Closes #7584).** Le motif `closeHeaderPanels(); setX((value) => !value)` est un piège React : les deux mises à jour sont traitées **dans le même lot**, le helper remet le panneau à `false`, puis l'updater fonctionnel relit cet état et le repasse à `true` — le panneau se **rouvre** à chaque clic et ne peut jamais être fermé par son propre déclencheur. Le sous-menu RH et le menu de compte avaient été corrigés par #7556 (cible calculée **avant** la fermeture des autres) ; le motif subsistait sur **trois** panneaux : la cloche de notifications, le bouton « Modules & plan » et le menu de modules mobile. Corrigés de la même façon. **Vérifié** : plus aucune occurrence du motif dans `front/web` ni `front/admin-dashboard` (grep sur les deux frontends) ; trois cas unitaires ajoutés dans `layout-header-menu.test.tsx`, dont l'assertion porte sur le **démontage** du panneau (ils sont rendus conditionnellement) et **pas** sur `aria-expanded`, comme l'issue l'exige, plus un cas verrouillant la non-régression #7556. **Preuve A/B** : sur le code d'avant, ce sont exactement les deux cas « se referme au second clic » qui **échouent** (le troisième, déjà garanti par #7556, passe) ; après correctif, 9/9 verts.
- **feat(ci): une garde ferme le trou par lequel `git merge-file --union` passait (Closes #7583).** Résoudre un conflit de catalogue i18n avec `--union` produit du JSON **invalide** (`--union` concatène les deux côtés ligne à ligne, donc duplique virgules et clés) : 13 à 17 fichiers cassés au drain #7562, et les synchronisateurs plantaient au démarrage. Ce qui **était** couvert : `shared/i18n/locales/*.json`, les catalogues générés et `versions.json` (par `validators/validate.js`) ainsi que les 4 `.arb` mobiles (par la garde de parité). Ce qui ne l'était **pas** : **tout le reste** — `package.json`, `composer.json`, les configs, les contrats `dev-hub/tools/*.json`, budgets — soit ~190 fichiers, qu'aucune garde ne lisait ; et les gardes i18n étant filtrées par `paths:`, un JSON cassé hors de ces chemins ne déclenchait même pas le workflow. Nouvelle garde `dev-hub/tools/check-json-catalogs-parse.sh` : elle parse **tous** les JSON/ARB suivis, nomme les fautifs avec l'erreur exacte, et rappelle la méthode qui marche (merge profond + régénération). Branchée dans `actionlint.yml` — un check **requis**, déjà exécuté sans filtre à chaque PR — donc couverture universelle **sans un seul run supplémentaire**. **Vérifié** : 196 fichiers analysés sur le dépôt, **aucun cassé** (donc aucun faux positif, la garde peut bloquer sans risque) ; auto-test vert : rouge sur `{"a": 1,,` et sur un ARB cassé en nommant les deux, sans signaler le fichier sain, et vert sur un arbre sain (la garde n'est pas toujours rouge). Inscrite au `REGISTRE_GARDES.md`.
- **fix(api): une seule docblock par déclaration — et une garde contre la réapparition (Closes #7582).** En PHP, **seul le DERNIER docblock** avant une déclaration compte : une annotation placée dans un docblock séparé est **silencieusement ignorée** par PHPStan. Constat #7582 (`EmployeeFactory`) : `@extends Factory<Employee>` ignoré, `Employee::factory()->create()` typé `Model`, ~20 erreurs du check requis « PHPStan — Strict ». Le défaut est invisible à la lecture et sans effet à l'exécution — le pire profil. Nouvelle garde `dev-hub/tools/check-double-docblock.py` : mode par défaut = **bloque les nouvelles occurrences** et affiche la dette (doctrine du dépôt pour une dette large : une garde rouge sur tout l'existant est désarmée le premier jour) ; `--strict` pour tout bloquer ; `--audit` pour l'inventaire. Branchée dans `actionlint.yml` et inscrite au `REGISTRE_GARDES.md`. **Dette mesurée : 49 occurrences**, dont 23 dans le périmètre du check requis. Quatre sont corrigées, celles où le second docblock ne portait **aucune** annotation — l'annotation du premier était donc totalement perdue. La fusion a dû être faite **par chaîne** : il y avait jusqu'à **trois** docblocks consécutifs, pas deux, et un merge par paire laissait le motif en place. **La leçon de méthode compte plus que les quatre fichiers** : le merge mécanique ne suffit pas — **deux des quatre annotations étaient périmées** (`@param $allowed` et `@param $slabs` référencent des paramètres qui n'existent plus sur ces méthodes), et les activer faisait apparaître **2 nouvelles erreurs strict** (`parameter.notFound`, lignes 384 et 137). Elles ont donc été **retirées, pas activées** : « un docblock par déclaration » ne veut pas dire « ressusciter toute annotation trouvée ». **Vérifié** : `php -l` sur les 4 fichiers ; `pint --test` **PASS** sur les 4 ; PHPStan strict sur `app/Modules/Accounting` + `app/Modules/Payroll` → **`[OK] No errors`** (contre 2 erreurs avec les annotations périmées activées) ; garde verte, dette **49 → 45** ; A/B de la garde : injection d'une occurrence → rouge en nommant le fichier et le delta (1 → 2), restauration → vert.
- **feat(devx): prouver le sous-ensemble avant de fermer une PR « couvert par le lot » (Closes #7581).** Au drain #7562, deux PR corrigeant un vrai bug (`front/web` : `optimizeCss` exige `critters`, absent de `package.json` **et** du `package-lock.json` → install propre = web cassé) ont été **fermées** au motif « couvert par le lot #7524 ». Le lot ne l'a jamais couvert : le bug était **toujours vivant sur `main`** après le drain, et il a fallu #7565 pour le corriger. Même classe : les reliquats de #7533 et #7535 étaient encore ouverts après le drain. Fermer « couvert par le lot » est une **affirmation de contenu** ; quand elle est fausse, le correctif disparaît **et** le backlog verdit à tort — le « ghost close » que `AGENTS.md` interdit pour les issues, appliqué aux PR. Nouvel outil `dev-hub/tools/check-pr-subset-of-lot.sh` qui **mesure** au lieu d'affirmer : une PR est un sous-ensemble du lot si chaque fichier qu'elle modifie existe dans le lot **et** si chaque ligne qu'elle ajoute (vs la base commune) y est présente ; sortie `0` = prouvé, `1` = non prouvé avec les fichiers/lignes manquants nommés — c'est le texte à citer dans le commentaire de fermeture, ou la raison de ne pas fermer. Règle rendue opposable dans le protocole : nouveau **§4bis** (procédure + commande), ligne du tableau **§6.3** mise à jour (elle renvoyait à une règle sans outil), case ajoutée à la checklist **§5** ; inscrit au `REGISTRE_GARDES.md`. **Vérifié** par auto-test `--self-test` qui construit un dépôt temporaire et couvre les **deux** sens : lot portant le correctif → « SOUS-ENSEMBLE PROUVÉ » (0) ; lot sans le correctif → « NON PROUVÉ » (1) avec la ligne manquante nommée (`f.txt : 1 ligne(s), ex. 'correctif PR'`). La perte constatée en production est donc désormais détectée mécaniquement.
- **feat(cameras): le module Caméras devient un outil horizontal auto-activable par le client (Closes #7476).** Le module existait (API, surface web #7425, flag plateforme `cameras`) mais **n'était activable que par la console plateforme** : `cameras` était absent de `Company::HORIZONTAL_TOOLS` (allowlist fail-closed → `422`) et de son miroir web `SELF_ACTIVATABLE_MODULE_KEYS` — un client ne pouvait donc pas équiper son local, alors que le propriétaire demande que « chaque entrepreneur » puisse avoir des caméras. `cameras` entre dans `HORIZONTAL_TOOLS` **et** dans `HORIZONTAL_TOOL_FEATURES` (`cameras → cameras`) : l'auto-activation écrit les **deux sources de vérité** (`metadata.modules` + `companies.features`), le flag plateforme restant un **kill switch** pour la console, et le module restant verrouillé par rôle (responsable `principal`/`rh`). La matrice de plans (`FeaturePlanMatrixSeeder`) l'ouvre **dès le pilote** (exclu du gratuit) — sinon la promesse n'est pas vérifiable en recette. **Supersession explicite, à connaître** : `front/web/src/lib/__tests__/client-features-cameras.test.ts` verrouillait l'inverse (« n'est pas auto-activable — la vidéosurveillance touche à la vie privée, seule la plateforme l'active », #7425 point 4) ; l'assertion est **inversée avec justification écrite dans le fichier**, conformément à la convention du dépôt pour deux tests contradictoires — le garde-fou de vie privée qui subsiste est le flag plateforme (veto) et le verrouillage par rôle. **Vérifié** : `CompanyModuleActivationTest` **7/7** (nouveau cas cameras, miroir du flag vérifié en base) ; tests web `client-features*` **13/13** ; `eslint --max-warnings 0` sans erreur ; **PHPStan strict 0 erreur** sur `app/Core` ; `pint` PASS. **Hors périmètre, signalé** : le sélecteur d'outils de l'inscription (proposition 3 de l'issue) reste un sous-ensemble **curé** — `training` et `showcase`, eux aussi horizontaux, n'y figurent pas ; y ajouter `cameras` seul serait incohérent (à traiter avec le pass copy #7495). Deux échecs de test **pré-existants sur `main`** ont été confirmés par `git stash` (plancher solo #7423 contredit par `SelfServiceTrialTest`, et un `429` attendu `400`) : ils méritent leur propre correctif.
- **docs(protocole): la vérification d'un correctif de gate/CI entre dans le protocole — §6.2 Règle 8 de P07 (Closes #7586).** La leçon #7559/#7566 → #7579 (un run au statut `requested` n'était pas considéré « en vol », le gate lisait une conclusion `null` et fabriquait le motif `tests-null`, rendant `main` rouge malgré tous les tests locaux verts) n'existait que dans `AGENTS.md`. Elle devient une **règle du protocole** `docs/PROTOCOLES/P07_ARCHITECTURE_DEV_PROD.md` §6.2 : (1) relire un **run réel de production** après merge ; (2) **citer le `run id`** qui prouve le comportement attendu ; (3) modéliser l'état qui a échoué dans le test comportemental (le cas `requested` y est, vérifié) ; (4) aucun motif `*-null` constructible. Historique P07 porté à **v1.1**. Miroir `AGENTS.md` déjà à jour ; gardes `check-deploy-gate-outcome.sh` (+ `--self-test`) et `check-deploy-gate-outcome-test.mjs` **vertes**.
- **feat(platform,admin): le super admin peut enfin déléguer — rôles internes de plateforme, écran « Équipe » et menus filtrés par permissions (Closes #7553, Closes #7557, Closes #7554).** L'espace plateforme était **binaire** : `public.super_admins` ne portait aucune colonne de rôle (seule `status` avait été ajoutée) et les groupes `/api/v1/platform/*` et `/api/v1/admin/*` partageaient le seul middleware `auth:super_admin_api`. Confier le support, la facturation ou l'observabilité à un collaborateur interne imposait donc de lui donner **tous** les droits — provisioning d'entreprises, impersonation, kill switches, gestion des comptes plateforme. **Modèle (#7553)** : colonne additive `super_admins.platform_role` (varchar(32) `NOT NULL DEFAULT 'super_admin'`, CHECK PostgreSQL), enum `PlatformRole` + matrice `PlatformPermission` (**6 rôles / 18 permissions**, `admin` a tout **sauf** `team.manage` : la distribution des rôles reste la prérogative du propriétaire du SaaS), et middleware `platform.permission:<perm>` posé sur **49 routes sensibles** (entreprises/paramétrage/provisioning, abonnements et offres, comptes des utilisateurs d'entreprise, impersonation, kill switches, observabilité et métriques, support, annonces, CRM, nœuds edge, équipe). Nouveaux endpoints `/api/v1/platform/team` (liste, création, changement de rôle, activation/désactivation) tracés dans le journal d'audit. **Garde-fous anti auto-verrouillage, testés** : un compte ne modifie ni son propre rôle ni son propre statut (`422 CANNOT_CHANGE_OWN_PLATFORM_ROLE` / `CANNOT_DISABLE_OWN_ACCOUNT`), le **dernier `super_admin` actif** ne peut être rétrogradé ni désactivé (`422 LAST_SUPER_ADMIN_REQUIRED`), une désactivation **révoque les tokens Sanctum** (parité #2630), une permission manquante rend `403 PLATFORM_PERMISSION_REQUIRED` avec les permissions requises et le rôle courant, et le login d'un compte non `active` reste refusé (`403 ACCOUNT_SUSPENDED`). `platform_role` reste **hors `$fillable`** (#3597, garde `SensitiveFillableGuardTest` étendue) : un rôle ne s'attribue que par un chemin explicite. Rétrocompatibilité stricte : le défaut `super_admin` couvre les comptes existants, un rôle absent ou inconnu retombe sur `super_admin` côté modèle, et `GET /platform/auth/me` **ajoute** `platform_role` + `permissions` **sans** changer `role: "super_admin"` (le SPA déployé ne casse pas). **Console (#7557)** : `front/admin-dashboard` ne détruit plus une session dont `role !== 'super_admin'` (deux gardes : login et `checkAuth`) mais accepte tout compte plateforme actif exposé par l'API, stocke `platform_role`/`permissions`, expose `hasPermission()`, ajoute l'écran **« Équipe plateforme »** (`/team` : liste, création avec mot de passe ≥ 12, changement de rôle — désactivé pour soi-même, activation/désactivation avec confirmation in-app, erreurs de garde-fou explicites) et **filtre le menu latéral par permission**. **Cohérence du menu (#7554)** : une **source de vérité unique** de navigation (`src/navigation/navigation.js`) est désormais consommée par la sidebar, la palette de commandes (`Ctrl+K`), les raccourcis clavier et la recherche d'en-tête — l'entrée `showcase` (nommée `showcase` alors que la route est `showcase-editor`, donc jamais surlignée) est corrigée, la condition comptabilité morte (`user.role === 'manager'` sur une session qui ne peut plus l'être) est branchée sur les permissions plateforme, les icônes dupliquées sont dé-dupliquées, **6 pages routées mais inatteignables** gagnent une entrée (`/settings/payroll/*`, `/solutions/survey-stats`, `/fuel-station/operations`), le bloc « Système » passe à l'i18n, l'entrée Travel ne décale plus la mise en page (place réservée) et le pied de sidebar redondant disparaît (Mon compte + Déconnexion deviennent des entrées du groupe Paramètres). **Vérifié** : `PlatformTeamApiTest` (**11 cas** : matrice exposée, 403 par rôle, création, rôle inconnu refusé, changement audité, auto-rôle refusé, dernier super admin protégé, révocation des tokens, login refusé, `auth/me` rétrocompatible, application réelle des permissions sur les routes sensibles) ; **PHPStan strict 0 erreur** ; migration sans collision de préfixe ; OpenAPI (+5 paths, +4 schémas) et miroir/SDK régénérés, **couverture des routes stricte à 0 route non documentée** ; console : ESLint 0, `vite build` OK, **72 specs Playwright vertes** (dont la nouvelle `platform-team`), garde i18n 0 littéral. Docs : `docs/security/PLATFORM_INTERNAL_ROLES.md` (nouveau) et `docs/security/RBAC_ROUTE_MATRIX.md` (légende + ligne « Platform administration »). **Hors périmètre, assumé** : la généralisation de `platform.permission` à **tout** le cockpit `/admin/*` (seuls l'impersonation et le tableau de bord y sont couverts ici) — dette explicite, à trancher séparément.
- **feat(web): l'espace client gère enfin ses collaborateurs et leurs rôles, et le menu mobile redevient utilisable (Closes #7555, Closes #7556).** Un client qui créait son compte depuis le web pouvait ajouter des collaborateurs mais **jamais leur donner de rôle** : le formulaire d'ajout envoyait `role: 'employee'` **en dur** (`front/web/src/app/(dashboard)/employees/page.tsx`), aucun écran n'exposait les rôles `manager` (RH, département, comptable, superviseur, marketing) pourtant acceptés par l'API, et les invitations — `GET /invitations` et `POST /invitations/{id}/resend`, disponibles côté API — n'étaient appelées par **aucune** page : impossible de savoir qui avait accepté son invitation ni de la relancer. **Nouvelle page `/settings/team`** (« Collaborateurs et rôles », réservée aux managers, héritée du gate de session `/settings/*`) : liste paginée et recherchable des collaborateurs avec **rôle et statut d'invitation** (fusion `GET /employees` + `GET /invitations`), invitation avec choix du rôle (`send_invitation: true`, `manager_role` requis pour un manager), **changement de rôle** par ligne (`PATCH /employees/{id}`), renvoi d'invitation, archivage avec confirmation in-app (jamais `window.confirm`, #3494), états de chargement/erreur/succès explicites et messages d'erreur API traduits (`EMPLOYEE_ROLE_CHANGE_MANAGER_ONLY`, `INVITATION_ALREADY_ACCEPTED`, …). **Les garde-fous de l'API sont reflétés, pas contournés** : `principal` n'est jamais proposé à la promotion (l'API le refuse) et la ligne de l'utilisateur courant n'est pas modifiable (l'API neutralise l'auto-modification). Le sélecteur de rôle est aussi ajouté au formulaire historique de `/employees` (fin du `role: 'employee'` codé en dur) et le rôle s'affiche en clair dans la liste. Entrée de navigation dans le menu de compte (2 lignes localisées). **Menu mobile du dashboard (#7556)** : deux boutons hamburger **identiques** cohabitaient sous 768 px (rail métier et modules), les panneaux déroulants (notifications, modules, utilisateur, sous-menu RH) ne se fermaient qu'en recliquant leur bouton, sans voile, sans verrouillage du défilement ni hauteur bornée — et les réglages n'étaient atteignables que par le petit avatar. Désormais **un seul point d'entrée** sous `md` : un tiroir unique (rail métier + modules de l'entreprise + section Compte : Mon compte, Collaborateurs et rôles, mot de passe, 2FA, notifications, déconnexion), avec voile, verrouillage du scroll **compté**, fermeture par Échap, au clic extérieur et à chaque navigation ; les 5 panneaux sont bornés (`max-h-[70vh]` + défilement interne), un seul ouvert à la fois, et le dropdown de modules reste disponible entre `md` et `lg` (la nav horizontale est `lg:flex`). Accessibilité : `aria-expanded`/`aria-haspopup`/`aria-controls`, `role="dialog"` + `aria-modal` sur le tiroir mobile, focus visible. **Contrats e2e préservés** : les 4 cas de `e2e/dashboard-mobile-nav.spec.ts` (testids, `aria-expanded`, voile, verrou de scroll, Échap, pas de débordement horizontal) passent, complétés par `e2e/dashboard-mobile-drawer.spec.ts` (6 cas : fermeture des panneaux, hauteur bornée, hamburger unique, liens de compte dans le tiroir). **Vérifié** : `tsc --noEmit` 0 erreur, `eslint --max-warnings 0` 0, `check:mojibake` OK, **Jest 105 suites / 899 tests** verts (dont 17 nouveaux sur la gestion des rôles : payload d'invitation manager RH, PATCH de rôle, `principal` absent des options, accès réservé sans appel API…), Playwright `dashboard-mobile-nav` 4/4 × 5 navigateurs + `dashboard-mobile-drawer` 6/6 × 5 navigateurs, garde i18n 0 littéral, parité des catalogues (backend / shared / ARB) et `I18N_VALIDATION_OK` après régénération par le synchroniseur (namespace `teamRoles.*`, 4 locales). **Reste ouvert, mesuré et non maquillé** : `e2e/dashboard-quick-actions.spec.ts` échoue sur `'Collaborateurs recents'` (sans accent) alors que la page rend « Collaborateurs récents » — reproduit à l'identique **avant** ce lot (dette préexistante, hors périmètre).
> Archivage du 2026-09-18 (règle #1729) : les deux sections `## [Unreleased]` dupliquées ont été
> fusionnées, et les entrées [Unreleased] les plus anciennes ont été déplacées dans
> `CHANGELOG_ARCHIVE.md` (section « [Unreleased] — entrées archivées le 2026-09-18 » — fichier
> sorti du dépôt depuis, #7654 : [version complète](https://github.com/kitokoh/leopardo-hr/blob/6a3819a9254d31d5f124c3e14bd3cdd5f31848c8/CHANGELOG_ARCHIVE.md)).
> Rien n'est supprimé : l'historique intégral reste consultable dans git.

- **Vertical Voyage (Travel)** : référentiel réseau (gares, lignes, arrêts), gestion des voyages et tarifs, réservations/billets avec check-in, rapports gérant et export CSV, équipages liés à la RH, clés API distributeurs, marketplace publique inter-agences et site public Next.js `front/travel-web` (recherche, sièges, checkout, comptes clients cross-agences) avec pipeline de déploiement Vercel.
- **Vertical Restaurant** : profil public par branche + annuaire avec recherche/proximité (RESTO-901), commande en ligne et avis modérés (RESTO-902), encaissement réel via les profils de paiement du tenant (Stripe sur les clés du restaurateur), pilote RBAC ressource-scopé par succursale.
- **Vertical Commerce/Retail & Leopardo Marché (BC-17)** : module vendeur actif (produits, stock, POS, espace `/commerce`), marketplace grand public multi-vendeurs cross-tenant (API publique, checkout invité, web), comptes acheteurs plateforme (favoris, historique, avis vérifiés post-livraison).
- **Vertical Santé/Pharmacie** : MVP PharmaManager complet pour les officines (#7798–#7804).
- **Verticaux Fuel, Edu, Fleet, Cameras** : généralisation du patron RBAC ressource-scopé, consolidation des migrations, module Caméras horizontal auto-activable + application mobile « Leopardo Caméras » (BC-19).
- **Paie multi-pays** : moteur de paie durci (indemnités congés payés, solde de tout compte, certificat de travail, journal mensuel, workflow de clôture via API), mentions légales DZ, cumuls annuels sur bulletin PDF, benchmark 10 000 employés, gate de couverture Payroll ≥ 80 % bloquant.
- **Facturation & paiements (BC-21)** : configuration des passerelles PSP depuis l'admin plateforme (stockage chiffré + fallback env), crédits IA achetables (ledger, checkout, décompte réel), emails facture/reçu avec PDF joint i18n ×4, rapprochement `stripe_invoice_id`, prix des plans paramétrables en base.
- **Module Communication (R0–R6)** : connexion Gmail par utilisateur (OAuth, tokens chiffrés), sync incrémentale, classification IA + liaison CRM, relances automatiques, réponses assistées avec politiques par boîte, UI « Boîte connectée » côté client et plateforme.
- **RBAC & espace client** : cycle de vie des accès ressource (invitation pré-assignée, vue « qui a accès à quoi », révocation en cascade), grants de modules composables par collaborateur, délégation plateforme (rôles internes, écran Équipe), gestion des collaborateurs côté client.
- **Onboarding & acquisition** : entretien de préparation conversationnel (remplace la modale 10 étapes), première connexion sans mot de passe en clair (lien magique), tracking par étape du tunnel d'acquisition avec dashboard des conversions, écran de bienvenue unique.
- **Marketing & support** : campagnes email effectives + IA + interactions sociales, publication sociale via jobs queued (LinkedIn/Meta/Twitter), espace tickets support client avec notifications email bidirectionnelles.
- **Comptabilité** : les 7 écrans web du rôle comptable dans le dashboard admin (documents/factures, plan comptable, grand livre/journal, lettrage…).
- **Branding & i18n** : image de marque du tenant dans l'espace client, thème dashboard et PDF ; internationalisation ×4 locales des surfaces publiques et marketing.

### Changed

- **Positionnement produit** : Leopardo cesse d'être présenté comme un logiciel RH — README, vitrine, console admin, workflows CI et docs alignés « suite métier » (#7428 et suites).
- **Gouvernance du dépôt** : `.gitignore` durci, miroir OpenAPI et SDK générés sortis du versioning, gros artefacts purgés (audit #7654), cartographie et cliquet anti-croissance de la dette PHPStan (#7655), navigation client et sidebar admin refondues (BC-01).
- **Ops/infra** : topologie Render dev/prod honnête (`APP_ENV=staging` + `DEPLOY_TIER=dev`), worker Render dédié queue+scheduler, drills de restauration des backups (PostgreSQL 18, clé `age`, fail-fast), boucle `schedule:run` intérimaire.
- **Migrations & schéma** : consolidation des migrations divergentes CRM/Fuel/EdgeSync/Restaurant/Travel/Edu, règle « une table, une migration » gardée en CI, tests sur les vraies migrations.

### Fixed

- **CI/CD** : suites backend fiabilisées (timeouts de verrous Postgres, reaper idle-in-transaction, dérive de fixtures), gardes governance suivant les déplacements de docs, workflows SHA-pinnés, gate de déploiement non contournable, déploiements Vercel/staging réparés.
- **Isolation tenant** : raccordement systématique des modèles à `BelongsToCompany` (hook `creating` forçant le `company_id`, garde `updating` anti cross-tenant), contexte tenant des jobs queued verrouillé par un test d'architecture, routes plateforme dédupliquées sous `platform.permission`.
- **Front web** : CSP en ENFORCE avec nonce par requête, panneaux d'en-tête, formulaires publics anti-spam (honeypot, time-trap, contrôle d'origine), schémas zod branchés.
- **Audit externe 2026-09-20 (en cours)** : URL backend Render dev codée en dur comme fallback silencieux dans 4 fronts remplacée par un fail-fast en production (#7842) ; assainissement de la gouvernance racine — CHANGELOG réécrit, AGENTS.md recentré, docs de pilotage déplacées, doublon `BRANCH_PROTECTION_REQUIRED.md` résorbé (#7843).

### Security

- **Multi-tenant** : défense en profondeur SmartAttendance, écriture cross-tenant par mass assignment fermée, piste d'audit lisible après purge d'un tenant.
- **Secrets & tokens** : tokens d'auth en cookie httpOnly (front/web + admin-dashboard), bearer super-admin hors du DOM + CSP durcie au build, token kiosk hors DOM + PIN admin, supervision queue sans credentials de prod dans les runners GitHub, alerte hebdo secrets exposés, fin des fallbacks « password123 ».
- **Edge** : TLS interne par défaut sur le LAN client + manifeste d'installation signé RS256.
- **Conformité** : droit à l'effacement RGPD (anonymisation employé), rétention et purge biométrique automatique, journalisation des accès aux données sensibles, bandeau de consentement et traceurs coupés par défaut, chiffrement au repos du metadata des documents de paie.
- **Dépendances** : bumps de sécurité (guzzle CVE-2026-59883/69245/69246, commonmark, shell-quote, sharp…).
- **Audit externe 2026-09-20 (en cours)** : EdgeSync `applyGeneric` — allowlist du nom de table dynamique et forçage tenant du payload (#7840) ; travel-web — token d'auth migré de localStorage vers le pattern cookie httpOnly (#7841) ; durcissement conteneurs et défauts (Dockerfile.edge non-root, mots de passe par défaut, collision port 5432, header `X-XSS-Protection` déprécié retiré) (#7847).

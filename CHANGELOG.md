# Changelog

Toutes les évolutions notables de ce projet sont documentées dans ce fichier.

Le projet suit un journal continu inspiré de
[Keep a Changelog](https://keepachangelog.com/fr/1.1.0/) : les livraisons sont
consignées au fil de l'eau dans la section [Unreleased], et les versions
publiées sont taguées selon le [Semantic Versioning](https://semver.org/lang/fr/)
(voir la section « Journal continu » ci-dessous).

> **Note — journal historique.** Ce fichier était auparavant un journal d'exécution
> détaillé des agents (385 Ko). Le détail complet de chaque livraison reste disponible
> dans l'historique git de ce fichier, notamment jusqu'au commit
> `0bba16974c1b032a9b9bf19bd4b1db7e9bbc6da2`
> (`git log -p 0bba16974c1b032a9b9bf19bd4b1db7e9bbc6da2 -- CHANGELOG.md`).

## [Unreleased]
- **fix(scheduler): UNE SEULE source de planification — 8 commandes ne tournent plus en double, `withoutOverlapping` sur les commandes sensibles, expireur Travel legacy supprimé (Closes #8139, BOS-006A).** Le scheduler était défini **deux fois** (`bootstrap/app.php` via `withSchedule()` **et** `routes/console.php`) : Laravel charge les deux, donc 8 commandes tournaient en double avec des horaires/paramètres contradictoires — `leave:accrue` **quotidien ET mensuel** (double acquisition de congés), `billing:generate-invoices` à 02:00 **et** 03:00, `attendance:auto-close` avec **et** sans verrou, `travel:expire-pending-bookings` deux fois, plus deux expireurs Travel publiant deux événements différents (`travel.booking.expired.v1` legacy vs `travel.booking.cancelled.v1` canonique). Livré : **`routes/console.php` devient la source unique** (convention déjà actée dans le dépôt — le bloc `withSchedule()` est retiré et commenté dans `bootstrap/app.php`), horaires unifiés (la définition canonique gagne, la plus complète — args + verrous — est conservée pour `attendance:auto-close` et `travel:expire-pending-bookings`), migration à l'identique des 14 entrées qui n'existaient que dans `bootstrap/app.php` (dont `leopardo:fleet:sync` avec `TRACCAR_SYNC_INTERVAL` et `withoutOverlapping(30)`), `withoutOverlapping()` ajouté aux commandes sensibles (`leave:accrue`, `leave:carry-forward`, `contracts:alert-expiring`, `billing:check-trials|check-overdue|generate-invoices`), **suppression de `TravelExpireBookingsCommand`** (legacy) et de sa planification, et **tableau des entrées** (commande / fréquence / verrou) documenté en tête du bloc. Fix adjacent exigé par la gate : import manquant `Illuminate\Support\Facades\Log` dans `bootstrap/app.php` (erreur PHPStan préexistante sur un fichier désormais modifié).
  **Preuve (red → green)** : nouveau `tests/Feature/Console/SchedulerUniquenessTest` — **3 tests ROUGES sur `main`** (doublons présents dans `schedule:list`, legacy encore planifié, verrous absents) et **verts après** : plus aucun nom de commande dupliqué dans `schedule:list` (vérifié aussi manuellement : 8 paires de doublons → 0), `travel:expire-bookings` ni classe ni commande enregistrée, un seul expireur (`travel:expire-pending-bookings`), verrous présents sur les 8 commandes sensibles. **Vérifié localement (PHP 8.4.26 + PostgreSQL 16)** : `tests/Feature/Contracts` (64 tests) et `SanctumTokenHygieneTest` verts ; `phpstan` config CI (level 8 + baselines) 0 erreur sur `bootstrap/app.php`, `routes/console.php` et le nouveau test ; `pint --test` vert ; gardes `check-queue-strategy-coherence`, `check-module-isolation`, `check-migration-basename-collisions`, `check-no-claim-marker` vertes. Les 8 échecs de `HealthEndpointTest`/`HealthQueuePendingVsScheduledTest` rencontrés pendant la vérification sont **préexistants sur `main` propre** (hors périmètre).
  **Note de déploiement** : les runs de minuit doublonnés disparaissent ; déployer hors fenêtre de run critique (02:00–04:30 UTC : factures, paie, congés, travel) pour qu'aucune commande en cours ne perde son verrou. Rollback = revert (configuration uniquement, aucune donnée).
- **fix(security,ai): sanitization RGPD RÉCURSIVE des payloads sortants — plus aucune PII (emails/téléphones/ID) dans les `tool_result` en tableaux de la branche Claude, ni sur OpenAI/Groq (Closes #8141, BOS-001).** `PrivacySanitizer::sanitizeMessages()` ne nettoyait que les `content` de type **string** : dans la boucle tool-calling, la branche Claude envoie ses résultats d'outils sous forme de **TABLEAUX** de blocs (`[{type: tool_result, content: "{…email…}"}]`) — un outil comme `get_employees`/`search_employees` (qui sélectionne `email` et `phone`) faisait donc partir des PII **en clair vers l'API Anthropic**, sans que rien ne le signale (les drivers OpenAI/Groq, qui embarquent le résultat dans une string, étaient déjà couverts — seul le chemin Claude fuyait). Livré : sanitization **récursive** de la structure (`sanitizeStructure()` : strings nettoyées à toute profondeur, tableaux parcourus valeur par valeur, **clés de structure jamais réécrites** — `role`/`type`/`tool_use_id`/`input` intacts pour ne pas casser le protocole provider), appliquée sur les 3 branches provider via le point unique `Orchestrator::chat()`. **Preuve (red → green)** : nouveau `tests/Feature/AI/PrivacySanitizerOutboundPayloadTest` qui joue la vraie boucle (tool call scripté → vrai handler `IntentEngine` renvoyant un employé porteur de `jean.dupont@exemple.fr` → 2ᵉ appel LLM) et **inspecte le corps HTTP réellement envoyé** pour claude/openai/groq : rouge sur `main` pour Claude (email en clair dans le payload), vert après le correctif ; `PrivacySanitizerTest` unitaire étendu (récursion, blocs imbriqués, préservation des clés) — l'ancien test qui figeait le comportement v1 (« seuls les strings sont nettoyés ») est remplacé par le contrat corrigé. **Vérifié localement (PHP 8.4.26 + PostgreSQL 16, `max_locks_per_transaction=512`)** : 3 nouveaux tests Feature + 7 tests unitaires verts ; suite `tests/Feature/AI` + `tests/Unit/AI` : **5 échecs préexistants reproduits à l'identique sur `main` propre** (fixtures absences/leave-balances — #8004/#8146, hors périmètre), zéro régression introduite ; `pint --test` vert ; `phpstan` (config `phpstan.neon`, level max) **0 erreur sur les 3 fichiers** ; gardes `check-ai-vendor-boundary.sh` et `check-module-isolation.sh` vertes. Rollback = revert (aucune donnée modifiée).
- **feat(hospitality): vitrine publique API HOSP-006 — fiche par slug, disponibilités, réservation en ligne idempotente, suivi/annulation par code (Closes #7948, BC-32).** Routes sans auth `/public/hospitality/*` (throttle dédié `hospitality-public`, défaut 30/min/IP configurable) : `GET /properties/{slug}` (fiche publiée consommée en SSR par `/stay/{slug}` — HOSP-008 #7950 devient fonctionnelle), `GET /properties/{slug}/availability` (même calcul que l'interne HOSP-004), `POST /properties/{slug}/reservations` (idempotence par `idempotency_key`, **lock inventaire** identique au guichet, `pending` + `expires_at=+30 min`, **montant SERVEUR** `base_price_minor × nuits` — jamais accepté du client, `tracking_code` aléatoire présenté UNE seule fois, seul son hash sha256 est persisté), `GET /reservations/{reference}?code=` (suivi sans compte, DTO minimal sans PII au-delà du nom), `POST /reservations/{reference}/cancel` (machine à états : 409 sur transition invalide). `HospitalityPublicPropertyResolver` (miroir du pattern RESTO-902 #7747) : résolution fail-closed (`is_public` + actif + société saine + verticale `hospitality` activée → **404 uniforme** anti-énumération) puis exécution DANS le tenant via `TenantManager::withinTenant` — aucune fuite cross-tenant ; le suivi par référence vérifie le code par `hash_equals` sur chaque candidat (référence unique par tenant). Overbooking refusé en **409** `HOSPITALITY_NO_AVAILABILITY` (contrat openapi.yaml aligné sur ce code métier, déjà celui de l'interne). 5 entrées `openapi-reverse-allowlist.txt` purgées (le contrat écrit par #8102 est désormais routé). Tests `PublicStaySurfaceTest` (13 cas : 404 uniformes, DTO sans fuite, idempotence, montant serveur, one-time code, overbooking, room_type d'un autre établissement → 422, suivi sans PII, annulation + libération d'inventaire, isolation inter-tenant). **Vérifié localement** : `php -l` 8 fichiers, **PHPStan strict level 8 : 0 erreur** (module + providers), Pint appliqué (1 fix cosmétique pré-existant `HospitalityAvailabilityController`), garde `check-openapi-route-coverage.py --strict-staleness` verte (0 nouveau drift direct/inverse/paramètres). **À confirmer par la CI** : PHPUnit Feature (pas de PostgreSQL en sandbox).
- **fix(tenant): encadrer les usages de `withoutGlobalScope` (surface de bypass de l'isolation tenant) — wrappers explicites + garde ratchet CI (Closes #7960).** Trois scopes nommés sur `BelongsToCompany` remplacent les appels bruts : `forCompany($company)` (lecture tenant explicite), `crossTenantForPlatformAdmin()` (super-admin, derrière autorisation plateforme), `crossTenantForSystemTask($justification)` (console/jobs — justification obligatoire, vérifiée à l'exécution ; docblock volontairement non `non-empty-string` pour garder la sonde non tautologique, doctrine #8014). Migration des appels directs (purges audit/partages, auth device ZKTeco, rappels CRM) avec justifications 100 % ASCII (garde PA2-I18N-007). Test `BelongsToCompanyExplicitBypassTest` (146 lignes) + garde ratchet `check-without-global-scope.sh` (baseline 253, aucun nouveau bypass) branchée dans `architecture-check.yml`. Reprise PM de la branche dormante `bc/backend-cleanup-20260923` (travail Zentor-G livré sans PR) + correctifs de session. **Vérifié localement** : `check-without-global-scope.sh` 253/253, `check-hardcoded-accented-messages.sh` vert sur le diff.
- **chore(style): rattrapage post-merge #8107 — Backend Quality (Pint) réparé sur les 4 fichiers du lot sécurité 1 + test cap PartnerClick rendu déterministe (Refs #8107, Refs #8060).** La PR #8107 (lot sécurité 1) a été mergée avec deux défauts visibles sur la CI du head SHA : **(1)** 4 fautes de style Pint sur les fichiers du lot (constructeur vide `{}`, ordre des imports, ligne vide finale, indentation docblock) — corrigées au preset `laravel` du repo, `pint --test` vert sur les 5 fichiers ; **(2)** `GrowthModuleTest::test_global_per_ip_cap_bounds_writes_across_campaigns` comptait 13 clics au lieu de 12 : la logique du middleware est correcte (vérifiée en isolation sur `ArrayStore` : 12 inserts/15 hits), mais le test comptait SANS filtre alors que le schéma MVP partagé n'isole pas `partner_clicks` entre les tests du fichier (résidu d'1 clic du test dedup déclaré avant) — le comptage est désormais scopé (`whereIn partner_link_id`). Les 453 autres échecs du run sont identiques à la baseline `main` pré-merge (455 échecs pré-existants, chantier #8004) — hors périmètre.
- **chore(devx): lot d'hygiène monorepo #7986 — tranche 5 (marketplace robots/sitemap, `typecheck` web, pins web-offline, digests Dockerfile.prod) (Refs #7986).** **(1) Marketplace SEO** : `front/marketplace` était la seule app Next sans `robots.ts`/`sitemap.ts` (constat de l'issue) — ajout sur le modèle `travel-web` : robots avec `Disallow` des routes transactionnelles (`/api/`, `/commande/`, `/compte/`, `/confirmation/`, `/paiement/`, `/panier/`, `/suivi/` — panier, paiement, suivi tokenisé et compte acheteur ne sont jamais indexables), sitemap avec revalidation horaire et entrées dynamiques **best-effort** (doctrine RESTO-903 #7748 : boutiques via `fetchSellers()`, produits disponibles via `fetchProducts({per_page: 100})`, API indisponible = entrées dynamiques omises, jamais de casse), URL canonique `NEXT_PUBLIC_SITE_URL` avec repli sur le déploiement documenté `leopardo-marche.vercel.app` (`marketplace-deploy.yml`). **(2) Script `typecheck` pour `front/web`** (`tsc --noEmit`) : la plus grosse app TS du dépôt n'en avait pas — vérifié **vert** localement (0 erreur). **(3) Pins web-offline** : `next ^16.3.5 → 16.3.4`, `react`/`react-dom ^19.3.0 → 19.2.8` (alignement sur les 3 autres apps Next, builds reproductibles) ; le drift `typescript` est déjà soldé sur `main` (^5.9.3 partout) et le bump 7.x reste au dependabot #8078 — pas de recouvrement. Lockfile régénéré. **(4) Digests des images de base `api/Dockerfile.prod`** (`node:22-alpine`, `dunglas/frankenphp:php8.4-alpine` épinglés par sha256, règle #3972 — digests sourcés de l'API Docker Hub au 2026-09-23). **Constats périmés re-vérifiés avant travail** : bindings `0.0.0.0` déjà tous en `127.0.0.1` sur main (#7847/#7996), config telescope/pulse et deps admin-dashboard déjà soldées par les tranches 1-4 (#8109). **Vérifié localement** : `next build` marketplace vert (routes `/robots.txt` + `/sitemap.xml` générées), `tsc --noEmit` + `eslint` 0 erreur sur les deux fichiers ajoutés ; web-offline **23/23 tests vitest**, `eslint` et `next build` verts après pins ; `tsc --noEmit` vert sur front/web. **Non vérifié (docker absent de la sandbox)** : build réel de l'image prod avec digests — syntaxe `FROM tag@sha256` standard, digests vérifiés à la source. Tranches restantes hors lot (outillage indisponible ici) : composer (predis/tinker), Flutter (fonts/pubspec), chargement de routes en interne (Attendance/HR/EdgeSync).
- **feat(vitrine): lot P2 (audit 22/09) — ancres sous navbar + rôle clarifié de la 3D, pricing arrondi + charte + réassurance, FAQ réassurance self-host ×4 + JSON-LD FAQPage landing (Closes #8073, Closes #8074, Closes #8076).** **(1) #8073** : les 4 ancres de la navbar atterrissent désormais SOUS la navbar fixe (h-20) — `scroll-mt-24` sur FeaturesSection/PricingSection/TestimonialsSection/FAQSection, et les ids manquants posés (`temoignages`, `faq` : les liens navbar ne pointaient vers RIEN). Collision 3D vérifiée au code : libellés et canvas clippés par `overflow-hidden`, aucun portail HTML (aucun z-index > navbar possible) — la capture fr-5/6 datait du ticker pré-#8072, sorti de la section 3D par #8110. Titre de la pile 3D passé au bénéfice (« Un socle commun, des modules pour chaque métier » ×4) et distinct de VerticalsSection (#8072) : la 3D reste la section « architecture » qui EXPLIQUE (décision documentée dans `data/solution-stack.ts` ; coût maîtrisé : chunk three.js différé à l'idle + montage à l'approche du viewport + repli CSS sans WebGL). **(2) #8074** : équivalents mensuels du tarif annuel ARRONDIS à l'euro (24,17 → 24 ; 65,83 → 66) via la règle unique `approxMonthlyFromAnnualEur(PLAN_ANNUAL_TOTALS_EUR.*)` — les totaux exacts (290/790 €/an) restent visibles dans `annualPeriod` (« /mois env. · N €/an facturé… » ×4) et inchangés côté checkout/PlanSeeder (cohérence `data/currency.ts` vérifiée : la conversion indicative s'applique au prix affiché, l'écart d'arrondi reste dans la tolérance « approx. »). Header pricing réaligné sur la charte verte (badge + dégradé — dernière occurrence violette du site, constat fr-12) et titre repris (« Des tarifs simples, un plan gratuit pour commencer » ×4) ; réassurance « Sans carte bancaire · Annulable à tout moment » sous chaque CTA de plan payant + rappel self-host gratuit avant la comparaison (clés `pricing.section.reassurance`/`selfHostNote` ×4). Contenu moissonné de la branche dormante `bc/vitrine-audit-lot1` (sous-ensemble PROUVÉ non couvert par main via `check-pr-subset-of-lot.sh` — sinon le correctif était perdu, #7581). **(3) #8076** : 7 Q/R de réassurance ajoutées à `data/faq.ts` ×4 locales (équipe IT, cloud vs self-host, sécurité/hébergement reformulée, migration Excel/Sage, réversibilité/export, coût réel cloud vs self-host, paie 21 pays du registre `CountryDefaults` + mobile money Orange Money/Wave/MTN MoMo « pas encore disponible, à l'étude, sans date promise » — aucune capacité inexistante présentée comme disponible) ; JSON-LD `FAQPage` ajouté à la landing via `(landing)/layout.tsx` (locale SSR `x-vitrine-lang`/Accept-Language, pattern #3921/#4201 du layout /faq). Tests : `data/__tests__/faq.test.ts` (parité ×4, 6 questions exigées FR/EN, honnêteté mobile money, 21 pays), garde titre #8073 dans `solution-stack.test.ts`, `pricing-checkout-alignment.test.ts` aligné sur l'arrondi. **Vérifié localement (front/web)** : Jest **153 suites / 1315 tests verts**, `tsc --noEmit` 0 erreur, `eslint src` 0 warning, `check:french-copy` OK, `check:mojibake` OK, `sync-web.js` + `validate.js` i18n OK.
- **ci(front): garde de parité des copies partagées entre fronts Next.js — la dérive silencieuse de `backend-url.ts`/`proxy.ts` devient impossible (Refs #7964, précédent mobile #7652).** L'audit 2026-09-20 constatait `backend-url.ts` md5-identique entre `front/web` et `front/travel-web` mais **sans aucune garde** (et des proxies auth déjà divergents) : un fix sécurité appliqué à une copie ne se propage plus aux autres. Livré : **(1)** `dev-hub/tools/check-front-shared-parity.sh` — échoue dès qu'une paire canonique n'est plus byte-identique (`backend-url.ts` web↔travel aujourd'hui, `proxy.ts` CSP travel↔marketplace dès que #8042 est mergée ; paire dormante tolérée tant qu'aucun membre n'existe, copie manquante seule signalée non bloquante) ; **(2)** workflow `front-shared-parity-guard.yml` (PR + push main, paths scopés, même doctrine que `mobile-core-duplication-guard.yml` #7652) ; **(3)** en-têtes des deux `backend-url.ts` (reportés à l'identique, évidemment) pointant la garde. **Décision d'architecture tranchée et documentée dans l'issue** : le package npm/workspaces demandé par #7964 renverserait la contrainte d'autonomie des fronts (#7305 : lockfile propre, `turbopack.root` épinglé, build Vercel par sous-dossier — rappelée dans l'en-tête même de `backend-url.ts`) — c'est une décision propriétaire ; en attendant, la synchronisation manuelle devient opposable au lieu d'être silencieuse. **Vérifié** : garde verte sur les copies actuelles, rouge sur dérive simulée, `actionlint` 0 erreur.
- **feat(hospitality): vitrine publique `/stay/{slug}` + contrat OpenAPI public + docs données/RBAC — HOSP-008 (Closes #7950, BC-32).** Page SSR **indexable** `front/web/src/app/stay/[slug]/page.tsx` (modèle `/restaurants/{slug}` RESTO-903 #7748) : metadata dynamique + canonical + JSON-LD schema.org `LodgingBusiness`/`Hotel`/`BedAndBreakfast`/`ApartmentComplex` selon le type, fiche rendue serveur via `GET /public/hospitality/properties/{slug}` (nouveau helper `lib/hospitality-public-api.ts`, DTO public strict, 404/dé-publié → `notFound()` fail-closed). Parcours transactionnel porté par le composant client `StayBookingPanel` : dates + voyageurs → disponibilités par type de chambre → réservation **idempotente** (`idempotency_key` uuid stable par tentative, statut `pending` expirant à +30 min) → **confirmation avec référence + code de suivi** (affiché une seule fois — le serveur ne stocke que le hash) → suivi/annulation sans compte par référence + code — tous les appels via le proxy same-origin `/api/v1` (`apiFetch`), aucun jeton. `/stay` reste HORS `PROTECTED_PREFIXES` (déjà prévu par HOSP-001 #7943). **OpenAPI contract-first** : les 5 opérations `/public/hospitality/*` de la spec §6 (fiche, disponibilités, réservation, suivi, annulation) sont documentées dans `api/openapi.yaml` avec schémas de requête/réponse et sémantique d'erreurs (404 uniforme anti-énumération, 422 overbooking, 409 transition) ; les routes Laravel étant livrées par HOSP-006 (#7948, lot parallèle), 5 entrées **justifiées et datées** rejoignent `openapi-reverse-allowlist.txt` — à purger au merge de #7948. i18n **×4** (`stay.public.*`, 44 clés fr/en/tr/ar dans `shared/i18n/locales/` + sync web). Docs annexes : `docs/hospitality/DONNEES_RBAC.md` (tables livrées, invariants #8019, RBAC ressource-scopé progressif `hospitality_property`, surface publique). Sitemap : pas d'énumération des slugs — la spec §6 ne définit AUCUN annuaire public hospitality (contrairement à RESTO-901), l'indexabilité vient de la page elle-même. **Vérifié localement** : `tsc --noEmit` 0 erreur (front/web), parse YAML openapi, `check-openapi-route-coverage.py --strict-staleness` vert (0 nouveau drift direct/inverse/paramètres), `check-i18n-catalog-parity.sh` vert (4 langues). **Non vérifié (API HOSP-006 non mergée)** : rendu bout en bout contre l'API réelle — la page rend `notFound()` tant que les routes publiques n'existent pas, aucun risque d'écran cassé.
- **fix(payroll): robustesse des flux de paie — chunking, jobs uniques, re-check sous verrou (Closes #8057).** Trois défauts de robustesse corrigés sur les flux financiers : **(1) OOM prévisible** — `BankExportGenerator` chargeait tous les bulletins validés d'un run via `->get()` et `PayrollCalculator::executeCalculateRun()` toute la population active via `->get()` ; les deux passent à **`lazyById(500)`** (hydratation bornée par lots, signatures internes en `Enumerable` — les tests unitaires qui passent des `Collection` restent compatibles ; chaque énumération count/sum/foreach ré-exécute la requête par chunks, coût en requêtes accepté contre une mémoire bornée sur conteneur 512 Mo). **(2) Double traitement concurrent** — aucun job Payroll ne portait `ShouldBeUnique` : deux workers pouvaient calculer le même run ou générer le même export en parallèle ; `ProcessPayrollBatchJob` (clé `payroll-run:<company>:<run_id>`, TTL 600 s aligné sur `timeout`) et `GenerateBankExportJob` (clé `bank-export:<id>`, TTL 120 s) portent désormais un verrou d'unicité au niveau queue — complément du claim conditionnel #6529 qui ne protégeait pas contre deux jobs simultanés déjà en file. **(3) TOCTOU sur la validation** — le check `status === 'validated'` de `PayrollService::validate()` était HORS transaction : deux requêtes concurrentes pouvaient le passer toutes les deux et valider deux fois → **double déduction d'avance sur salaire** ; le bulletin est désormais re-lu avec `lockForUpdate()` DANS la transaction et son statut re-vérifié avant toute écriture (le fast-fail hors transaction est conservé pour l'UX, la garantie vient du verrou). Tests : simulation déterministe de la double validation concurrente (modèle périmé `draft` en mémoire pendant qu'une autre requête a validé en base → `PayrollAlreadyValidatedException`, avance intacte) + garanties d'unicité des deux jobs. **Vérifié localement** : `php -l` + `pint` sur les 8 fichiers touchés (pas d'env PHP 8.4/Postgres dans la sandbox — suites Feature exécutées par la CI).
- **fix(style): rattrapage Pint post-merge #8101 — `main` redevient vert sur Backend Quality (Refs #7984).** La PR #8101 a été mergée avec 2 fautes de style non bloquantes signalées par Pint : ligne vide surnuméraire en fin de `app/Modules/Attendance/Domain/Models/ZktecoSyncLog.php` (`single_blank_line_at_eof`) et `new` avec parenthèses dans `tests/Unit/DatetimeCastsTest.php` (`new_with_parentheses`, style du dépôt : `(new Foo)`). Correctif minimal en 2 lignes pour que le check « Backend Quality (Pint) » ne soit plus rouge sur toutes les PRs suivantes.
- **chore(devx): lot d'hygiène monorepo #7986 — tranches 1-4 (CI/build, deps npm, orchestration racine, convention Controllers/) (Refs #7986).** **(1) CI/workflows** : `concurrency` ajoutée sur `design-token-sync`/`web-design-tokens`/`public-promises-guard` (sérialisation par ref, annulation PR uniquement — règle #2131), `timeout-minutes` sur `public-promises-guard`/`application-layer-placement-guard`/`pages-deploy`, et réparation de la concurrence de `mobile-distribute-main` (groupe keyé sur `github.sha` = aucune exclusion mutuelle → distributions main sérialisées sur le ref, `cancel-in-progress: false` conservé). **(2) Build/config** : `edge-tts==7.2.8` pinné dans `api/Dockerfile.prod` (provider TTS par défaut), config morte `api/config/telescope.php` + env `TELESCOPE_ENABLED`/`PULSE_ENABLED` retirées (packages non installés), engines zkteco-kiosk alignés (`node >=20.9.0`). **(3) npm admin-dashboard** : 7 deps runtime jamais importées retirées (echarts, chart.js, vue-chartjs, globe.gl, three, vue3-leaflet, date-fns — vérifié 0 import, dynamiques et CSS inclus) → lock allégé de ~4 800 lignes ; `leaflet` CONSERVÉ (utilisé par `FleetView.vue` — constat de l'issue inexact) ; `vue-toastification` pinné exact `2.0.0-rc.5` (aucune stable 2.x publiée). **(4) Orchestration racine** : 11 scripts nommés (marketplace, web-offline, zkteco-kiosk, travel test/typecheck) + agrégats `lint`/`test`/`build` étendus à toutes les apps (trous CI) ; kiosk lint+test vérifiés localement (32 OK). **(5) Convention Controllers/** : 5 controllers déplacés sous `Interfaces/Api/V1/Controllers/` (0 controller hors convention restant) + doublon mort `Core/Solutions/PlatformSolutionSurveyStatsController` supprimé (0 référence) ; `docs/architecture/AI_SUBSYSTEM.md` documente l'exception `app/AI`. Constat périmé vérifié : bindings 0.0.0.0 déjà tous en 127.0.0.1 sur main (#7847/#7996). **Vérifié localement** : YAML des 6 workflows parsés, scans `rg` des références, tests kiosk 32/32 — le reste (PHPUnit/tsc des apps) à confirmer par la CI. Tranches restantes hors lot : composer (runtime PHP requis), Flutter (fonts/pubspec), web-offline (dependabot #8078), chargement de routes en interne (tranche dédiée).
- **fix(security): lot sécurité 1 (audit architecture API 2026-09-22) — 5 correctifs sur branche unique de lot (Closes #8052, Closes #8053, Closes #8054, Closes #8058, Closes #8060).** **(1) #8052 🔴 CRITIQUE** : `IdempotencyMiddleware` snapshottait le corps COMPLET des réponses 2xx dans le cache (TTL 24 h) — or `TokenAutoRefreshMiddleware` y injecte le token Sanctum roté EN CLAIR (clé `_auth`, #5581) : tokens valides lisibles dans Redis et rejouables périmés. Le corps est désormais épuré de `_auth` AVANT persistance (fail-safe : jamais de repli sur le corps original) ; la réponse directe livre toujours le token (seul canal sûr), la clé n'est ni persistée ni rejouée. **(2) #8053** : OIDC fail-closed — `exp` et `iat` sont OBLIGATOIRES (OIDC Core §2 : un id_token sans `exp` n'expirait jamais) ; une liste `audiences` fournie mais vide (ou un `client_id` vide en fallback) REJETTE au lieu de sauter le contrôle `aud` (substitution de token émis pour un autre client) ; `GoogleIdentityVerifier` refuse explicitement toute vérification sans audience configurée, et les clés `services.google.{web,android,ios}_client_id` sont enfin mappées sur l'env (lues mais jamais définies donc non configurables en prod). **(3) #8054** : l'anti-bot des boutiques publiques Travel/Restaurant était factice — seul le caractère non-vide du header `X-Captcha-Token` était exigé, le secret configuré jamais utilisé (`curl -H "X-Captcha-Token: x"` bypassait tout). `App\Core\Http\Security\CaptchaVerifier` partagé : POST `siteverify` (Turnstile par défaut, compatible reCAPTCHA), timeout 5 s, FAIL-CLOSED (provider injoignable = refus) ; doublon mort `EnsureRestaurantShopPublicAccess` supprimé. **(4) #8058** : helper central `App\Shared\Support\TenantCache` (`key()` fail-closed hors contexte tenant, `keyFor()` explicite — format canonique `tenant:{company_id}:{suffix}`, maillon cache de la politique `BelongsToCompany`) + migration des sites tenant-scopés (HR headcount, Recruitment feed, CRM EmailRateLimiter, CatalogPublicCache) + garde CI `check-tenant-cache-keys.sh` (job Hygiene Guards) interdisant les clés littérales non scopées dans `app/Modules` (exemptions justifiées). **(5) #8060** : `PartnerLinkMiddleware` écrivait `PartnerClick` à CHAQUE hit (DoS stockage sur la DB partagée, fraude à l'attribution) — écriture bornée : dédup (IP, lien) par fenêtre, plafond horaire par IP, filtre bots ; redirection et cookie d'attribution toujours posés ; mode dégradé fail-open journalisé (#6557). Tests ajoutés par correctif (dont `OidcIdTokenValidatorHardeningTest` 9 cas, `PublicShopCaptchaTest` 7 cas). **PHPUnit non exécuté localement (PHP absent de la sandbox) — à confirmer par la CI ; gardes sans PHP vertes** (`check-tenant-cache-keys.sh`, collisions migrations non applicable — aucune migration).
- **docs(architecture): tranche #7848 (nommage du dépôt) — titre de `.specify/constitution.md` corrigé (« Leopardo Constitution », plus « Leopardo HR ») et vision §X alignée sur la charte « suite métier » #7428 ; dossier de décision `docs/architecture/DECISION_RENOMMAGE_DEPOT.md` préparé pour l'arbitrage propriétaire (inventaire mesuré : 142 fichiers référencent `kitokoh/leopardo-hr` en dur, dont 5 workflows ; options A `leopardo` / B `leopardo-suite` / C statu quo justifié + plan par vagues s'appuyant sur les redirections GitHub) (Refs #7848).
- **docs(gouvernance): dépôt public assaini — CSV CRM nominatif anonymisé + consolidation des 4 dossiers commerciaux en 2 (Refs #7982, Refs #7844 — phase 1 du plan `docs/README.md` §4).** **(1) #7982** : `LEOPARDO_RH_CRM_EXEMPLE_10_LIGNES.csv` publiait 10 lignes ressemblant à de vrais prospects bêta (contacts, entreprises, probabilités de closing, objections) — réécrit avec des identités **explicitement fictives** (chaque ligne marquée `DONNEES FICTIVES (#7982)`), et le README du dossier ne prétend plus à tort que les données étaient déjà fictives ; bandeau « contenu interne publié — décision propriétaire en attente (#7982) » posé sur les 5 docs internes (scripts commerciaux, templates prospection, stratégie acquisition 2026, journal racine, prompts d'exécution). La décision dépôt privé vs transparence assumée reste au propriétaire (l'historique git conserve les versions antérieures — réécriture BFG à évaluer avec cette décision). **(2) #7844 phase 1** : `docs/GTM/` fusionné dans `docs/GOTO_MARKET/` (templates prospection, cas clients, good first issues — le README doublon disparaît) ; `docs/STRATEGIE_COMMERCIALE/` + `docs/commercial/` fusionnés dans **`docs/business/vente/`** (dossier vente unique : scripts, modèles CRM, benchmarks, appels d'offres) avec README consolidé. Références mises à jour (`docs/README.md` — table de correspondance, taxonomie, §3, phase 1 marquée faite —, `PROTOCOLES/P03`, `dossierdeConception` ×2, `GOTO_MARKET/README.md`). **Vérifié** : `rg` — plus aucune référence active à `docs/GTM/`, `docs/commercial/`, `docs/STRATEGIE_COMMERCIALE/` hors mentions historiques/archives ; aucun workflow ni script `dev-hub/tools/*` ne référençait ces chemins (le workflow rapport mensuel GTM sur `docs/GOTO_MARKET/rapports-mensuels/` est intact).
- **ops(quota): réduire la sollicitation de la base Neon — la prod brûlait elle-même le quota gratuit du compte (issue #8050).** Audit chiffré du 2026-09-22 : les plans gratuits Neon plafonnent à **5 Go de transfert/mois partagés par TOUS les projets du compte** (d'où l'arrêt simultané de la prod et du dev le 2026-09-20), et trois postes internes le consommaient : (1) le worker de file du conteneur web teste **les 8 queues à chaque cycle** (~138 000 requêtes/jour à `--sleep=5`, ≈ 2 Go/mois) ; (2) le job `sync` du workflow de secours retéléchargeait le dump de la base vivante **toutes les heures** (~1,5 Go/mois) ; (3) `travel:outbox-dispatch` était planifié **deux fois** (`bootstrap/app.php` + `routes/console.php`), soit ~1 440 passes/jour pour rien. Correctifs : **`WEB_QUEUE_SLEEP`** pilote désormais le `--sleep` du drain intérimaire (défaut 5 = comportement historique, **10 posé en prod et dev** → moitié des requêtes, latence ≤ 10 s sur un job en file) ; doublon de planification retiré ; cadence du workflow de secours revue (watchdog **horaire** au lieu de /20 min, synchro **/6 h ancrée sur 03:23 UTC** — le créneau de la sauvegarde quotidienne, qui ne devait donc pas être déplacé —, battement de cœur par push limité à 1 vrai contrôle/60 min au lieu de /15 min). RPO assumé et documenté : ≤ 6 h au lieu de ≤ 1 h, en échange de ~4 Go/mois → **~1,5 Go/mois**. Aucun changement applicatif visible ; `WEB_QUEUE_SLEEP=5` pour revenir au comportement strictement antérieur.
- **ops(backup): les sauvegardes chiffrées sont archivées hors GitHub sur Backblaze B2 (API native) — bucket `leopardos`, arborescence `postgres/daily/AAAA/MM/JJ/` (Closes #8008).** La chaîne S3 historique (`database-backup.yml`) est morte faute de secrets ; les identifiants Backblaze fournis par le propriétaire le 2026-09-21 **fonctionnent avec l'API native** (`b2_authorize_account`, upload vérifié : fichier déposé puis relisté) mais sont **refusés par l'API S3** de B2 (`InvalidAccessKeyId: Malformed Access Key Id`, prouvé avec `awscli` **et** `boto3` sur `s3.us-east-005.backblazeb2.com`, les deux permutations clé/secrète testées) — le workflow utilise donc l'API native, sans dépendance supplémentaire (3 appels `curl` : authorize → list_buckets → get_upload_url → upload, avec masquage des jetons). Nouveaux secrets `B2_KEY_ID` / `B2_APP_KEY` / `B2_BUCKET_NAME` (documentés dans `docs/CI_CD_SECRETS.md`, garde de parité verte), dégradation propre en `::notice::` s'ils sont absents (la sauvegarde reste alors en artefact GitHub). Combiné au fait que la sauvegarde est prise sur la base **LIVE** (lue via l'API Render), l'archive suit automatiquement la rotation primaire/secours. **Vérifié** : upload natif réel dans le bucket puis relecture par `b2_list_file_names` ; `bash -n` + parsing YAML sur le workflow.
- **fix(ci): la garde i18n de diff ne signale plus les tracés SVG (faux positif mesuré sur les glyphes de la vitrine) — motif documenté + cas de test (Refs #8008, famille #7482/#8000).** En livrant les glyphes symboliques du bloc « secteurs » (voir l'entrée ci-dessus), la garde `check-i18n-diff.js` a signalé les **commandes de tracé** (`d="M2 12h4l2.5-6 3 12 2.5-6h8"`) comme « nouvelle chaîne en dur » — un faux positif qui pousse à réécrire du SVG pourtant correct, exactement ce que l'issue #7482 interdit. Les attributs de **géométrie et de peinture SVG** (`d`, `viewBox`, `points`, `transform`, `cx/cy/r/rx/ry`, `x1/y1/x2/y2`, `fill`, `stroke*`, `stop-color`, `offset`, `preserveAspectRatio`) rejoignent la liste des attributs structurels (leur valeur n'est jamais du texte affiché : le libellé reste hors du SVG). Nouveau **cas 1bis** dans `check-i18n-diff-test.sh` (fichier TSX avec glyphes) qui verrouille le motif. **Vérifié localement** : `bash dev-hub/tools/check-i18n-diff-test.sh` → `PASS` (les autres cas de la garde restent inchangés : `title`/`aria-label`/`placeholder`/`:label` toujours détectés).
- **feat(web): la preuve sociale de la vitrine devient symbolique et vivante — plus aucune icône littérale (avion, usine, camion…) dans le bloc « secteurs » (Closes #8008).** Demande propriétaire (2026-09-21) : « la preuve n'est pas représentative de la solution et n'est pas assez vivante ; en essayant de mettre avion et autre sur la preuve, risque de confondre le lecteur — mets plutôt des trucs symboliques. » Les 8 icônes métier de `TrustedBrands` (Factory, HardHat, Landmark, **Plane**, Radio, ShoppingBag, Truck, Cpu — `lucide-react`) sont remplacées par des **glyphes SVG symboliques** dessinés dans le composant (treillis, hexagone, paliers, orbe, impulsion, réseau, flux, ondes) : aucune image d'objet ne peut plus être lue comme un client ou un secteur « déjà servi », ce qui referme le risque de confusion documenté dans le fichier (PA2-MKT-011 : la section avait déjà dû être requalifiée « secteurs adressés » faute de liste de clients autorisée). Côté « vivant » : le défilement continu est conservé, une **respiration douce** des glyphes est ajoutée (translation/scale/rotation lente) et l'ensemble est **désactivé si `prefers-reduced-motion`**. Le fichier perd sa dépendance aux icônes littérales (imports `lucide-react` supprimés) ; aucun texte n'est ajouté en dur (garde `check-i18n-diff.js` verte en local sur les surfaces PA2-I18N-014), les libellés et la copie restent ceux des catalogues du composant. **Vérifié** : garde i18n de diff en local (0 nouvelle chaîne en dur), `Frontend — ESLint + TypeScript` verte sur la PR.
- **fix(web): le badge « Trial » revient dans la sidebar pour les modules cœur — un module en essai était devenu invisible dans la navigation (Closes #8028, régression du refactor #7908).** `e2e/client-feature-gates.spec.ts` (« trial module is visible as trial and remains usable ») échouait de façon déterministe sur `main` depuis le 2026-09-20 ~06:47 UTC : `page.getByText('Trial').first()` ne trouvait rien sur `/reports`. Diagnostic reproduit localement (Playwright + serveur local, échec identique à la CI) : le refactor de la sidebar unifiée #7908 a déplacé le badge « Trial » dans `BusinessRailCard` — donc **uniquement** pour les modules `scope: 'business'` — alors que l'ancien `NavPill` du layout le rendait pour TOUT module `enabled && state === 'trial'` (vérifié sur `layout.tsx` @ `14b4a260`). « Rapports » étant un module **cœur** (`group: 'general'`), un tenant dont le module est en essai ne voyait plus aucun signal d'essai dans la navigation (seule la page `/modules` l'affichait encore). Correctif : `SidebarModuleLink` (rendu UNIQUE des entrées cœur — liens directs et enfants d'accordéon) porte à nouveau la pastille « Trial » (`data-testid="sidebar-module-trial-badge"`) à droite du libellé, à côté de la pastille d'item actif ; l'essai n'est PAS un verrou, le lien reste navigable. **Vérifié localement (front/web)** : `e2e/client-feature-gates.spec.ts` **7/7 vertes** (l'échec était déterministe avant le correctif), nouveaux cas Jest dans `layout-sidebar-unified.test.tsx` (badge présent en essai, absent en module inclus), **Jest 149 suites / 1298 tests verts**, `tsc --noEmit` 0, `eslint src` + `eslint e2e` 0 warning, `check:mojibake` OK, gardes `check-i18n-catalog-parity.sh` / `check-web-design-tokens.sh` / `check-public-promises.sh` vertes. Périmètre assumé : les autres specs du job vitrine n'ont pas été rejouées ici.
- **fix(ci): le tuning `max_locks_per_transaction` du service PostgreSQL CI ne pouvait pas s'exécuter — le rôle `postgres` n'existe pas (Refs #8004).** L'étape « Raise PostgreSQL max_locks_per_transaction (#8004) » de `tests.yml` faisait `docker exec -u postgres leopardo-postgres psql …` alors que le service est initialisé avec `POSTGRES_USER: leopardo_user` : `initdb` ne crée alors QUE ce rôle comme superutilisateur, jamais `postgres` → `FATAL: role "postgres" does not exist`, exit 2, et le job **Backend (PHP 8.4 + PostgreSQL 16 + Redis 7)** mourait **avant la première migration, donc avant le premier test**. C'est la cause racine du rouge `main` introduit par #8036 — et elle était INVISIBLE dans les annotations de check run (seul « Process completed with exit code 2 » y figure) : la lecture du log de job a désigné la panne en une passe. **(1) Correction** : `psql -U leopardo_user` (`POSTGRES_USER` est superutilisateur, `ALTER SYSTEM` reste permis ; l'auth locale du conteneur est `trust`). **(2) Sonde rendue non tautologique** (doctrine #8014/#8018) : le `SHOW max_locks_per_transaction` final se contentait d'imprimer la valeur — il la COMPARE désormais à 512 et échoue en `::error::` sinon, et l'attente de reprise après `docker restart` échoue explicitement au lieu de poursuivre silencieusement. **Vérifié localement** : `actionlint` 1.7.7 (0 erreur, shellcheck 0.8.0 appliqué au bloc `run:`) et parse YAML. **Non vérifié (Docker absent de la sandbox)** : exécution réelle du job — c'est la CI qui tranche, et l'état des ~4xx tests Feature annoncés par #8004 ne sera lisible qu'APRÈS ce correctif (le job s'arrêtait avant de lancer la moindre suite).
- **fix(ci,security): garde Trivy `publish.sh` en OPT-OUT (scan par défaut) + acquittements `.trivyignore.yaml` réalignés sur l'état réel, et correction du bug CRITICAL de `edge/Dockerfile` (Closes #8024, Refs #8013).** **(1) Garde par défaut** : `edge/publish.sh` avait `TRIVY_ENFORCE="${TRIVY_ENFORCE:-0}"` — un opt-in jamais posé en pratique, donc une garde qui ne protégeait rien (réserves de la revue de #8013). Défaut porté à **1** : un scan CRITICAL/HIGH corrigeable non acquitté refuse désormais le push ; l'opt-out reste possible mais devient un acte explicite et tracé (`TRIVY_ENFORCE=0 bash edge/publish.sh <version>`). Le **fail-closed** est conservé : sans binaire `trivy` dans le PATH, le script s'arrête AVANT tout push (jamais de skip silencieux). En-tête du fichier et CHANGELOG réécrits en conséquence. **(2) Acquittements contradictoires** — un acquittement qui contredit le code est pire qu'aucun (il masque un défaut réapparu sans que personne ne le voie) : trois entrées de `.trivyignore.yaml` décrivaient l'état **antérieur** à #7966/#7996 et ont été retirées — `api/docker/php84/Dockerfile` porte bien `USER leopardo` (l'entrée prétendait un périmètre « explicitement exclu du passage non-root »), `edge/Dockerfile` porte bien `USER www-data` sur `:8080` (l'entrée prétendait un entrypoint chownant `:80` en root). L'entrée `front/web-offline/Dockerfile` est **réécrite** : elle invoquait une « refonte du port d'écoute (>1024) » alors que `EXPOSE 3000` et `nginx.conf` (`listen 3000`) sont DÉJÀ non privilégiés — la vraie remédiation (pid/cache/log inscriptibles + `USER nginx`, ou base `nginx-unprivileged`) est maintenant décrite honnêtement, dette datée conservée. **(3) Bug CRITICAL révélé puis corrigé** : `edge/Dockerfile` n'avait **aucun `FROM base`** avant la section de copie de la PWA — tout le bloc final (static export, Caddyfile, entrypoint, `EXPOSE`, `USER`) s'exécutait donc dans le stage `pwa-build` (`node:20-alpine`), référence d'alias courant incluse. Vérifié avec `trivy config` : le fichier d'origine remonte **`AVD-DS-0006` (CRITICAL) « COPY --from should not mention current alias 'pwa-build' »** ; ajout du `FROM base` manquant → l'image de référence documentée (API Laravel FrankenPHP + PWA embarquée) est réellement produite, et le scan config repasse à **0 CRITICAL/HIGH** sur les 8 Dockerfiles avec un `.trivyignore.yaml` réduit de 5 à 2 entrées. **(4) Mise en garde CI** : `image-scan.yml` documente désormais l'interdiction d'inscrire ses jobs en required status checks tant que le filtre `paths:` est présent (piège PA2-OPS-003 / #2032 : GitHub attendrait indéfiniment un statut qui n'arrive jamais sur une PR sans Dockerfile) — le rendu requis exige un job-agrégateur non filtré rendant un verdict explicite, comme `detect-changes` de `architecture-check.yml` (#7269). **Vérifié localement** : `trivy config --severity CRITICAL,HIGH --ignorefile .trivyignore.yaml .` → exit 0, 0 finding sur les 8 Dockerfiles ; `bash -n edge/publish.sh` OK.
- **fix(security): unicité RÉELLE de la politique mots de passe — comptes plateforme (`super_admins`) migrés, 6 sites inline dédupliqués, garde CI durcie (Closes #8021, suivi #8002 — norme #5620).** La revue PM de #8002 avait laissé trois réserves. **(1)** Les comptes les PLUS PRIVILÉGIÉS du SaaS gardaient la politique la PLUS FAIBLE : `PlatformTeamController::store` (création d'un collaborateur interne) et `PlatformUserController::store`/`::update` validaient `['required','string','min:12']` / `['sometimes','string','min:12']` — **sans chiffre ni blocklist** (`a-very-long-password` passait la validation) ; **(2)** six sites inline dupliquaient encore la norme au lieu du helper : `SelfServiceTrialController::setPassword`, `UserAuthController::register` **et** `::changePassword` (`new_password`), `PlatformAuthController::changePassword` (`new_password`), `ActivateInvitationRequest` (activation d'invitation onboarding), `TravelCustomerAccountController::register` (compte client grand public) — l'inscription `UserAuthController` n'avait même aucune borne `max` ; **(3)** `dev-hub/tools/check-password-policy.sh` ne voyait ni `new_password`, ni un `min:12` sans robustesse. Livré : **(1)** les 7 surfaces passent par `App\Shared\Rules\PasswordPolicy` — `required(confirmed: false)` là où le formulaire n'envoie pas de confirmation (comptes plateforme, inscription user, inscription client travel), `required()` là où `confirmed` était déjà exigé, `array_merge(['sometimes'], PasswordPolicy::optional())` pour la mise à jour plateforme (sémantique `sometimes`/`nullable` préservée : champ absent = mot de passe inchangé) — et les imports `Illuminate\Validation\Rules\Password` / `NotCommonPassword` devenus inutiles sont retirés ; **(2)** garde RÉÉCRITE en scanner Python borné par l'équilibre des crochets (fin de la fenêtre glissante de 6 lignes qui débordait sur la règle suivante) : champs `password|new_password|password_confirmation`, tout site « politique » (`min:`, `Password::…`, `NotCommonPassword`, `confirmed`) hors `PasswordPolicy` échoue avec le motif exact (minimum absent/&lt;12, `numbers()` manquant, blocklist manquante, règle dupliquée) ; sont hors périmètre les champs de VÉRIFICATION d'un mot de passe existant (login, re-auth 2FA — `['required','string']`) et les affectations techniques (`Hash::make(...)`, `config(...)`), commentaires/docblocks ignorés ; **(3)** liste canonique `dev-hub/governance/password-policy-exceptions.json` (`exceptions` justifiées + `legacy_pending_review` = dette documentée non bloquante, **vide** après migration — la garde sort en 0), conformément à la règle « une garde nouvelle n'échoue jamais sur l'existant » (leçon #7999) ; **(4)** `PasswordPolicyTest` étendu à 6 tests : création d'un collaborateur plateforme avec un mot de passe de **12 caractères SANS chiffre** → 422 avec le message de la règle exacte (`validation.password.numbers`), mise à jour `/platform/users` idem **et** absence de mot de passe toujours acceptée (`sometimes`), fixtures `PlatformTeamApiTest` réalignées (`COMPLIANT_PASSWORD`) ; **(5)** l'exemple de docblock obsolète de `NotCommonPassword` (`Password::min(10)->letters()`) renvoie désormais au helper. **Vérifié localement (PHP 8.4 + PostgreSQL 14, base dédiée `leopardo_test_w4`) : `PasswordPolicyTest` 6/6 (dont preuve par échec 201→422 en revenant à `min:12` seul), `PlatformTeamApiTest`+`PlatformUserApiTest` 20/20, `SelfServiceTrialTest`/`PlatformAuthTest`/`UserAuthTest`/`TravelCustomerAccountApiTest`/`EmployeeInvitationOnboardingTest`/`RegisterLoginFlowTest`/`OnboardingE2ETest`/`Billing/TrialSignupLocalizationTest` — 7 échecs PRÉEXISTANTS reproduits à l'identique après `git stash` (3 flux 2FA web plateforme, 1 unicité e-mail profil, 1 ordre de clés metadata d'assignation, 2 fixtures à mot de passe faible de main rouge #8004) ; garde `check-password-policy.sh` verte (exit 0) ; pint conforme.**
- **docs(dev,infra): suivi #8014 — notes de migration dev, sondes queue/scheduler non tautologiques, mention `edge-ci` corrigée (Closes #8018).** **(1) `DEVELOPMENT.md`** — nouvelle section « Migration d'un poste dev existant » : `POSTGRES_PASSWORD` ne s'applique qu'au PREMIER `initdb` (volume `pgdata` existant → garder l'ancien `secret` ou recréer le volume), `sudo chown -R $(id -u):$(id -g) api/` après une stack lancée en root (images devenues non-root en #7966/#7996), et perte de l'accès LAN pour les devices physiques (tous les ports dev publiés sur `127.0.0.1` depuis #7847/#7996) avec le workaround `docker-compose.override.yml`. **(2) `docker-compose.yml`** — les deux sondes `/proc` (`queue:work`, `schedule:run`) se matchaient ELLES-MÊMES : `grep -qs <motif> /proc/[0-9]*/cmdline` porte le motif dans sa propre cmdline → sonde tautologique toujours verte ; motifs passés en classe de caractères `[q]ueue:work` / `[s]chedule:run` (commentaire anti-« simplification » ajouté), sémantique des healthchecks inchangée (le `sh -c` du worker porte toujours le littéral). **(3) `CHANGELOG.md`** — les deux mentions « à confirmer par la CI (`edge-ci`) » d'un build d'image Edge sont corrigées en **non vérifié — build manuel** : `edge-ci` n'exécute que des gardes de scripts et ne construit AUCUNE image. **(4) `AGENTS.md`** — leçon opérationnelle datée sur le piège de la sonde auto-matchée. **Vérifié localement** : YAML du compose parsé (python3 — Docker indisponible dans la sandbox), simulation des sondes OLD/NEW en bash (OLD verte sans worker = tautologie, NEW rouge puis verte avec worker = sémantique préservée), `actionlint` sur `edge-ci.yml` (non modifié), `./scripts/pre-push-checks.sh` vert. **Non vérifié (Docker absent)** : build/boot réels des images — build manuel requis (même constat que ci-dessus).
- **fix(ai,communication): suivi de revue #8012 — le docblock du read-tool `email_reply_draft` redevient VRAI : le mode `auto` est EXCLU du chemin read-tool (`IntentEngine` → `CommunicationReplyService::prepare(..., allowAutoSend: false)` rétrograde `auto` en `confirm`) + caveat de rollback documenté sur le `down()` de la migration CHECK `manager_role` (Closes #8023).** **(1) Docblock fautif** : `IntentEngine::draftCommunicationReply()` affirmait « cet outil n'envoie JAMAIS rien » alors qu'en politique `auto` opt-in le handler appelait `prepare()`, qui route par `match($mode)` vers `handleAuto()` → `GoogleGmailReplySender::send()` — un envoi Gmail SYNCHRONE et irréversible décidé par un tool call du LLM, sans validation humaine, dans la boucle de l'Orchestrator. **(2) Décision tranchée — `auto` exclu du chemin read-tool** : `CommunicationReplyService::prepare()` gagne un paramètre optionnel `bool $allowAutoSend = true` (rétro-compatible, défaut = comportement canonique inchangé) ; le read-tool passe `allowAutoSend: false`, ce qui rétrograde `auto` en `confirm` (même mécanisme de défense en profondeur que les catégories finance/RH/juridique bloquées en dur) : la proposition générée reste dans la file Pending (validation humaine, endpoint approve) et le brouillon est toujours renvoyé à l'agent ; le mode `draft` (dépôt d'un brouillon Gmail réversible `gmail.compose`, jamais d'envoi) reste autorisé, c'est la fonction même de l'outil. Le chemin CANONIQUE (job `PrepareCommunicationReplyJob`, endpoints, approve) conserve le mode `auto` opt-in intact — non-régression verrouillée par test. Les descriptions LLM (`CommunicationAiToolCatalog`, `AIToolRegistrySeeder`) et le commentaire de matrice (`config/ai.php`) sont réalignés sur ce comportement. **(3) Caveat de la migration #8004** : le `down()` de `2026_09_21_000003_8004_unify_manager_role_check_constraint.php` ré-impose la liste restaurant-only — le commentaire documente désormais qu'un rollback est PERTE DE DONNÉES si des rôles `dispatcher`/`delivery_manager` ont été écrits entre `up()` et `down()` (SQLSTATE 23514 au ADD CONSTRAINT, rollback avorté laissant la contrainte absente) et donne la remédiation (purge des valeurs delivery avant rollback, ou pas de rollback du tout : l'union EST le correctif du CHECK écrasé). **Vérifié localement (PHP 8.4 + PostgreSQL 14)** : nouvelle régression `CommunicationReplyTest::test_email_reply_draft_tool_never_sends_even_with_auto_policy` (spy `GoogleGmailReplySender` : `send()` jamais appelé + `Http::assertNothingSent()` + proposition `pending`/`confirm` + aucun log `AUTO_SENT`) — rouge sans le correctif, verte avec ; contre-épreuve `test_canonical_auto_policy_still_sends_outside_the_tool_path` (le chemin canonique envoie toujours) ; suite `CommunicationReplyTest` 24/24 ; `php -l` + `pint --test` verts sur les fichiers touchés.
- **fix(security): matrice `platform.permission` complétée sur les routes `/admin` et `/platform` résiduelles — plus aucun rôle délégué sur les contenus cross-tenant (Closes #8020, suivi #7973/#8005).** La matrice #7553 laissait 13 routes plateforme sans garde : `GET /platform/country-defaults`, `GET /admin/ai/conversations`, `GET /admin/ai/conversations/{conversation}/messages`, `POST /admin/ai/chat`, `GET /admin/solutions/survey-stats`, `GET /admin/fleet/alerts`, `GET /admin/hr-reports`, `GET /admin/training/{courses,sessions,enrollments}`, `GET /admin/platform/ai/{monitoring,health}`, `POST /admin/payroll/simulate` — tout rôle délégué (support, finance, ops, marketing) y accédait. Arbitrage (AUCUNE nouvelle permission — réutilisation du catalogue #7553) : **(1) contenus tenant CROSS-TENANT → `companies.manage`** (admin seul) : conversations IA (titres + messages), assistant `/admin/ai/chat`, rapports RH dont résumé de paie, formations cross-tenant (inscriptions nominatives) et alertes flotte — permission déjà déclarée par les écrans SPA `/chat` et `/exports` ; **(2) agrégats marketing → `crm.view`** : `/admin/solutions/survey-stats` (leads `solution_survey` de `marketing_leads`, même famille que `/admin/marketing/leads`) ; **(3) écrans Paramètres → `settings.manage`** (admin seul) : suivi/health de l'assistant IA (voisins de `/admin/platform/ai/settings`) et `POST /admin/payroll/simulate` (appelé par les écrans barèmes fiscaux et cotisations sociales) ; **(4) référence pays non sensible → `companies.view`** : `/platform/country-defaults` (même famille que `GET /platform/companies`, sélecteur de pays des écrans Entreprises). `PlatformPermissionMatrixDeploymentTest` étendu : table route → permission vérifiée sur la définition réelle (`Route::getRoutes()->gatherMiddleware()` — seule preuve possible pour `country-defaults`, porté par TOUS les rôles), 403 des rôles délégués sur `/admin/hr-reports`, `/admin/ai/conversations*`, `/admin/ai/chat`, `/admin/training/*`, `/admin/fleet/alerts`, et passage effectif pour admin/super_admin. **Vérifié localement (PHP 8.4.25 + PostgreSQL 14)** : `php -l` OK, `pint --test` PASS, suite ciblée verte (25 cas, seule la garde préexistante `test_webhooks_require_webhooks_manage` reste rouge — voir #8004), `check-golden-journeys.sh` (1983 routes résolues statiquement) et `dev-hub/tools/check-routes-tenant-platform.sh` verts, `pre-push-checks.sh` sans régression. **Non couvert (hors périmètre)** : les sondes de menu `front/admin-dashboard/src/navigation/navigation.js` de `/fleet`, `/training` et `/solutions/survey-stats` restent `permission: null` — les rôles délégués voient l'écran mais reçoivent désormais 403 (alignement de menu à suivre).
- **fix(hospitality): durcissements post-revue du lot BC-32 (HOSP-003/004) — course sur `updateReservation`, garde de suppression d'unité, affectation staff transactionnelle, `per_page` borné, exceptions de domaine et code mort retiré (Closes #8019).** Les huit réserves de la revue PM de #8001 sont traitées : **(1) `updateReservation`** — la garde d'état terminal quitte le hors-transaction pour l'intérieur d'une `DB::transaction` avec `lockForUpdate()` + re-vérification sur la ligne fraîche (une annulation/check-out concurrent entre lecture et écriture ne peut plus être écrasée) et `unit_id` est contraint au MÊME `room_type` que la réservation. **(2) `UnitController::destroy`** refuse désormais la suppression d'une unité référencée par une réservation ACTIVE (422 `HOSPITALITY_UNIT_IN_USE`), même invariant que `HOSPITALITY_PROPERTY_IN_USE` / `HOSPITALITY_ROOM_TYPE_IN_USE`. **(3) `HospitalityPropertyStaffService::assign`** devient transactionnel (verrou de la ligne existante + restauration d'une ligne soft-deleted) et rattrape la violation d'unicité `23505` en **409 `EMPLOYEE_ALREADY_ASSIGNED`** au lieu du 500 de la course « deux affectations simultanées ». **(4) Pagination bornée** (`BoundsPagination`) sur les index property/unit/reservation, alignée sur l'endpoint staff (max 1000). **(5) Exceptions de domaine** : les `abort(422/404)` du service (`CHECKOUT_BEFORE_CHECKIN`, réservation verrouillée introuvable) deviennent `HospitalityCheckoutBeforeCheckinException` / exception dédiée — l'API rend les MÊMES codes et le même corps d'erreur (contrat inchangé, tests existants verts). **(6) Code mort supprimé** : `HospitalityAccess` (jamais appelé) ; `holdsInventory()`/`INVENTORY_HOLDING_STATUSES` remplacés par un comptage explicite `heldCount()`. **(7) KPIs dashboard** bornés aux établissements accessibles à l'acteur. **(8) i18n ×4** (`fr/en/ar/tr`) et tests ajoutés pour chaque comportement visible (422 unité en usage, 409 affectation concurrente, bornage `per_page`, incompatibilité `unit_id`/`room_type`). **Vérifié localement (PHP 8.4 + PostgreSQL 14)** : suite `tests/Feature/Hospitality` **entièrement verte** (243 assertions), `pint --test` vert sur les fichiers du lot, `check-i18n-catalog-parity.sh` OK (4 langues), `./scripts/pre-push-checks.sh` vert. Au passage : `assertExitCode()` est paresseux — les tests d'expiration asservissaient l'état d'AVANT exécution ; corrigés par `->run()` (leçon #8004).
- **perf(edu,restaurant): fin des agrégats en mémoire (dashboard EduManager) et du N+1 `items→product` sur les suivis publics Restaurant (Closes #7985).** **(1) `EduDashboardController::index`** chargeait TOUTES les lignes avant de calculer : les charges en attente (`EduFeeCharge … ->get()` puis `count()`/`sum('amount')` en PHP) et les admissions (`->get()->countBy('status')`) — remplacés par des agrégats SQL : `selectRaw('count(*) as pending_count, coalesce(sum(amount), 0) as pending_amount')->first()` (UNE requête, zéro ligne hydratée) et `selectRaw('status, count(*) as aggregate')->groupBy('status')->pluck('aggregate', 'status')` ; le payload reste STRICTEMENT identique (mêmes clés, `int`/`float` inchangés). **(2) `RestaurantPublicShopController::track`** chargeait `items` puis `items.product` ligne par ligne (1 requête produit par article) → `->with('items.product')`, même patron que `RestaurantPublicSlugOrderController::branchOrder` (déjà eager-loadé : le N+1 y était déjà couvert, désormais verrouillé par un test). **(3) Preuve par comptage de requêtes** (et non par relecture) : `EduDashboardTest::test_dashboard_aggregates_are_sql_level_and_bounded` (N=4 puis 2N=8 lignes → nombre de requêtes du dashboard IDENTIQUE, + assertions SQL `count(*)`, `sum(amount)`, `group by "status"` qui rougissent sur un `select *` remettant le calcul en mémoire) ; nouvelle suite `RestaurantPublicPerformanceTest` (suivi boutique : 5 → 9 requêtes sans le fix ; suivi par slug : 6 → 10 sans `with('items.product')` ; constantes = 5 et 6 requêtes métier après). Hors périmètre assumé (documenté) : les endpoints de CRÉATION de commande restent O(nb d'articles) — lookups produit par ligne (`CreateOnlineOrderAction`, `publishedProduct()`) + un INSERT par ligne, par construction.
- **fix(security,ops): durcissement TLS/session — plus de downgrade TLS silencieux vers la base et cookies de session HTTPS-only sur les DEUX blueprints Render (Closes #7980).** Inventaire des trois constats de l'issue : **(1) CSP** sur `travel-web` et `marketplace` — déjà livrée par #8006 (les deux `next.config.ts` posent la `Content-Security-Policy` ; le durcissement par nonce reste suivi par #8022). **(2) `SESSION_SECURE_COOKIE`** — présent dans `render.prod.yaml` (#7980) ; **il manquait le service DEV** : ajouté à `render.yaml` (`true`), Render servant toujours l'app en TLS, y compris le tier staging. **(3) `sslmode => 'prefer'` en dur** — la config est pilotée par `DB_SSLMODE` et son défaut n'est plus permissif : **`require` dès qu'on n'est plus en `local`/`testing`** (avant, un `DB_URL` sans `?sslmode=require` pouvait retomber en clair sans que rien ne le signale — la doc recommandait `require` mais rien ne le garantissait). `DB_SSLMODE: require` est désormais déclaré dans les deux blueprints (`render.yaml` + `render.prod.yaml`). Les environnements dont le PostgreSQL n'a pas de certificat posent la valeur **explicitement** : `DB_SSLMODE=prefer` dans `api/phpunit.xml` (suite de tests), `docker-compose.yml` (3 services applicatifs) et `api/.env.example` (déjà présent) — plus aucun downgrade implicite, et un self-host sans TLS garde l'échappatoire documentée `DB_SSLMODE=prefer|disable`. **Vérifié** : `php -l` sur `config/database.php` ; table de vérité rejouée (`local`/`testing` → `prefer`, `production`/`staging` sans surcharge → `require`) ; suite Feature exécutée avec la nouvelle config (connexion base OK) ; `../../scripts/pre-push-checks.sh` après commit.
- **fix(ci,tests): tranche 2 du déblocage de `main` — enregistrement des commandes artisan de module (13 non enregistrées, 4 noms en collision), bug de test systémique `assertExitCode()` paresseux, et `max_locks_per_transaction` du service PostgreSQL CI (Refs #8004).** **(1) Commandes de module invisibles d'`artisan`** : Laravel n'auto-découvre QUE `app/Console/Commands` — jamais `app/Modules/*/Console/Commands`. 13 commandes du module n'étaient listées dans AUCUN provider : `crm:tasks:send-overdue-reminders`, `delivery:close-route`, `delivery:export-report`, `delivery:replay-dlq`, `restaurant:no-show-expire`, `restaurant:send-reminders`, `leopardo:restaurant:reservation-jobs`, `leopardo:restaurant:stock-alerts`, `travel:recalculate-read-models`, `travel:expire-pending-bookings`, `leopardo:travel:import-legacy`, `travel:sales-settle`, `travel:settle-sales`. Elles sont désormais enregistrées dans leur ServiceProvider (pattern #7420/#5729) — d'où une vingtaine de tests Feature qui échouaient en `Command not found`. **(2) 4 noms en collision** : `travel:expire-pending-bookings`, `leopardo:travel:import-legacy`, `leopardo:restaurant:stock-alerts` et `pilot:seed` avaient DEUX implémentations chacune, la copie racine masquant celle du module (comportement dépendant de l'ordre d'enregistrement) — l'implémentation du module, plus riche (tenant-aware `--company`, verrou de ligne, libération des sièges, événement outbox), devient l'unique ; les doublons racine sont supprimés et `travel:expire-pending-bookings` conserve `--sync` (compatibilité #6070) en plus de `--company`. **(3) Bug de test SYSTÉMIQUE révélé** : `PendingCommand::assertExitCode()` est **paresseux** — il n'exécute PAS la commande, l'exécution est différée au `__destruct`. Tout test qui assertait l'état (file d'attente, base) juste après `$cmd->assertExitCode(...)` observait donc l'état **d'avant l'exécution**. Corrigé par `->assertExitCode(0)->run()` dans `DeliveryAsyncTest` (3 tests), et `Bus::assertDispatchedCount()` (méthode inexistante) corrigé en `assertDispatchedTimes()` dans `TravelBookingExpirationTest` — bug masqué jusqu'ici par l'`InvalidOptionException` qui échouait avant. **(4) CI PostgreSQL** : le service `postgres:16` gardait `max_locks_per_transaction=64` alors que la fixture MVP et les purges `DROP SCHEMA … CASCADE` tiennent des centaines de verrous DDL dans une transaction → vrais `SQLSTATE[53200] Out of shared memory`. Le paramètre est alloué au démarrage du postmaster (inexprimable via les `options:` d'un service, cf. docker-library/postgres#1091) : il est posé par `ALTER SYSTEM` + redémarrage conteneur avant toute migration. **Vérifié localement (PHP 8.4 + PostgreSQL)** : grappe des tests de commandes **42 passés / 3 échoués** (les 3 restants sont **préexistants**, reproduits aux mêmes lignes en CI : `TravelLegacyImportTest:113/187`, `RestaurantStockAlertsCogsTest:135`) ; `DeliveryAsyncTest` 14/14 et `TravelBookingExpirationTest` 6/6 verts ; `pint --test` vert sur les fichiers touchés ; `actionlint` vert sur `tests.yml`. **Tranche partielle assumée** — `main` reste rouge : la dérive de la fixture `CreatesMvpSchema`/dump pgsql (`google_id`, `social_accounts`, `leave_balances`…) n'est PAS traitée dans ce lot (Refs #8004).
- **fix(security): politique de mots de passe UNIQUE sur toutes les surfaces de création de compte — norme #5620 (min 12 + chiffre + blocklist) via `App\Shared\Rules\PasswordPolicy` (Closes #7995, sévérité MAJEUR audit 2026-09-20).** Quatre surfaces acceptaient `min:8` sans robustesse alors que le changement de mot de passe exigeait la norme : création/modification d'employé (`Store/UpdateEmployeeRequest`), inscription acheteur marketplace PUBLIQUE (`RegisterMarketBuyerRequest`), acceptation d'invitation web (`Web/InvitationController` — le premier mot de passe du compte !). Livré : **(1)** helper unique `PasswordPolicy::required()/optional()` ; **(2)** les 4 surfaces alignées + les 3 sites historiques de la norme (#5620 : changement, reset, registration) refactorisés vers le helper — une seule règle, zéro duplication ; **(3)** garde CI `check-password-policy.sh` (job Hygiene Guards) : tout `min:` faible sur un champ password dans `api/app` échoue ; **(4)** `PasswordPolicyTest` (4 tests : rejet 422/session sur chaque surface) + tests existants alignés (`RbacMatrixTest`, `RetailMarketBuyerAccountTest`, `EmployeeInvitationOnboardingTest`, `EmployeeIbanValidationTest`, `ScheduleControllerTest` — mots de passe de fixture conformes). **PHPUnit non exécuté localement (PHP absent de la sandbox) — à confirmer par la CI.**
- **fix(security): défense en profondeur tenant sur la famille Marketplace — `BelongsToCompany` sur `MarketplaceReview`/`MarketplaceFavorite`, `company_id` retiré des `$fillable` de Review, liste canonique d'exceptions + garde CI (Closes #7999, reliquat #7730, sévérité MAJEUR audit 2026-09-20).** **(1)** Trait fail-closed (#3727) ajouté aux deux modèles : no-op sur les routes publiques `/market/*` (aucune compagnie courante liée — lectures publiques inchangées avec leurs `where` explicites), scope automatique sur toute future requête en surface tenant ; **(2)** `company_id` n'est plus mass-assignable sur `MarketplaceReview` (leçon #7646) — posé en affectation directe par `RetailBuyerReviewService` depuis la commande résolue serveur ; **(3)** exceptions documentées : `MarketplaceBuyer` (table centrale cross-tenant SANS colonne `company_id` — le signalement de l'issue était inexact sur ce modèle), `MarketingLead` (`converted_company_id` = pointeur de conversion, pas clé d'isolation), `TaxRateChangeLog` (lignes `NULL` plateforme légales visibles de tous, #1923), `UserEmployeeLink` (résolue au LOGIN avant contexte tenant, #7711) ; **(4)** liste canonique `dev-hub/governance/tenant-scope-exceptions.json` + garde `check-tenant-scope-models.sh` (job Hygiene Guards) : tout NOUVEAU modèle mentionnant `company_id` sans trait ni exception échoue ; les 11 modèles pré-#7999 non qualifiés sont consignés en `legacy_pending_review` (dette documentée, avertissement non bloquant — lot dédié à venir). **PHPUnit non exécuté localement (PHP absent de la sandbox) — à confirmer par la CI.**
- **feat(hospitality): HOSP-004 — réservations guichet : anti-overbooking transactionnel, machine à états, disponibilités, KPIs et expiration automatique (Refs #7946, EPIC #7951, BC-32).** **(1) Schéma** : table tenant `hospitality_reservations` (migration `2026_09_20_000016_7946`) — UNIQUE `(company_id, reference)` et `(company_id, idempotency_key)` (NULL en masse = unique partiel), CHECK `status`/`source`/`check_out > check_in`, `tracking_code_hash` réservé au suivi public HOSP-006, `version` de verrou optimiste. **(2) Anti-overbooking** (`HospitalityReservationService`) : création et édition verrouillent le TYPE de chambre (`lockForUpdate` — sérialisation des écritures concurrentes, spec §3) puis comptent les immobilisations chevauchant `[check_in, check_out)` (confirmed + checked_in + pending NON expirées) contre la capacité opérationnelle (unités du type hors maintenance/hors service) — refus **409 `HOSPITALITY_NO_AVAILABILITY`** ; une annulation libère immédiatement, les intervalles adjacents ne se chevauchent jamais, une pending expirée n'immobilise plus (filet avant la commande). **(3) Machine à états** : pending → confirmed/cancelled ; confirmed → checked_in/cancelled/no_show ; checked_in → checked_out — **409 `INVALID_RESERVATION_TRANSITION`** (re-vérifiée sous verrou en transaction, course de guichets couverte) ; check-in occupe l'unité affectée, check-out la libère ; la confirmation re-vérifie la disponibilité. États terminaux gelés (édition = 409), toute édition d'intervalle/type re-vérifie la disponibilité en s'excluant. **(4) Idempotence guichet** : `idempotency_key` → rejeu = 200 avec l'existant (course 23505 → relecture). **(5) API v1** : `GET|POST /hospitality/reservations`, `GET|PATCH /{r}`, transitions `confirm|check-in|check-out|cancel|no-show` (policy : lecture `view`, création/transitions `operate`, édition `manage` — scoping progressif HOSP-003), `GET /properties/{p}/availability` (capacité/immobilisé/disponible par type) et `GET /dashboard/kpis` (occupation du jour, arrivées/départs, pending en ligne). **(6) Expiration** : commande `hospitality:expire-pending-reservations` (schedulée toutes les 5 min, `--company`/`--limit`, re-vérification sous verrou par réservation, idempotente — pattern travel #6070) annulant les pending dépassées (online +30 min — HOSP-006). **OpenAPI** : +9 paths / 14 opérations + 3 schémas (0 drift). Tests Feature `HospitalityReservationsTest` (15 cas : gate/RBAC, validation dates, rattachement même établissement, overbooking 409 + libération à l'annulation + adjacent accepté + capacité réduite par maintenance + pending expirée ignorée, disponibilité chiffrée, chaîne complète de transitions avec effets sur l'unité, 409 systématiques, édition re-vérifiée + terminaux gelés, idempotence, commande d'expiration idempotente, 404 cross-tenant, KPIs exacts). **PHPUnit/PHPStan non exécutés localement (PHP absent de la sandbox) — à confirmer par la CI ; gardes sans PHP vertes** (OpenAPI 0 drift, collisions migrations, duplicate schema, parité MVP 328, module-isolation, dépendances BC).
- **feat(hospitality): HOSP-003 — équipe par établissement : pivot staff + RBAC ressource-scopé `hospitality_property` progressif (Refs #7945, EPIC #7951, BC-32).** **(1) Pivot staff** : table tenant `hospitality_property_staff` (migration `2026_09_20_000015_7945`, pattern `restaurant_branch_staff` #7909 — `company_id` uuid non nullable, sans FK, unique `(company_id, property_id, employee_id)` soft-deleted inclus, softDeletes) ; `HospitalityPropertyStaffService::assign()` avec contrôle cross-tenant STRICT de l'employé (**422 `EMPLOYEE_OUTSIDE_TENANT`**), doublon actif → **409 `EMPLOYEE_ALREADY_ASSIGNED`**, ré-affectation = restauration de la ligne (jamais de doublon physique) ; le `role` du pivot est un libellé MÉTIER descriptif (réceptionniste, gouvernante, gérant…) — JAMAIS une source d'autorisation (spec §4). **(2) RBAC ressource-scopé progressif** (copie du pattern restaurant #7598/#7599) : type `hospitality_property` déclaré dans `config/resource_types.php` + trait `ChecksHospitalityPropertyAccess` (view < operate < manage) — tant qu'AUCUNE assignation n'existe dans le tenant, fallback direction `hasManagerRole('principal','rh')` (comportement HOSP-002 inchangé) ; dès la première assignation, fail-closed via `Employee::hasResourceAccess()` : un non-assigné ne lit plus, `rh` retombe à la lecture seule, la création d'établissement (company-wide) est réservée au `principal`. Nommer un responsable de site = poser une assignation niveau `manage` (endpoints RH existants `/employees/{id}/resource-assignments`, rien à reconstruire). **(3) Câblage** : les 3 policies référentiel rebasées sur le trait (lecture = `view`, gestion = `manage`) + nouvelle `HospitalityPropertyStaffPolicy` au point unique `AuthServiceProvider` ; listes imbriquées (équipe, inventaire) scopées à L'ÉTABLISSEMENT de la route (`authorize('view', $property)`) et index des établissements borné aux sites accessibles en lecture (`accessibleResourceIds`, `null` = sans restriction). **API v1** : `GET|POST /hospitality/properties/{p}/staff`, `PATCH|DELETE .../staff/{a}` (affectation d'un autre établissement → 404). **OpenAPI** : +2 paths / 4 opérations + 2 schémas (0 drift). Tests Feature `HospitalityPropertyStaffTest` (8 cas : 401, cycle complet affecter/409/modifier/retirer/restaurer-la-même-ligne, 422 cross-tenant, 404 établissement et affectation étrangers, progressivité RBAC complète — lambda 403→manage sur SON site uniquement, rh replié lecture seule, principal intouchable). **PHPUnit/PHPStan non exécutés localement (PHP absent de la sandbox) — à confirmer par la CI ; gardes sans PHP vertes** (OpenAPI 0 drift, collisions migrations, duplicate schema, parité fixture MVP 327 tables, module-isolation, duplicate use imports).
- **feat(hospitality): HOSP-002 — référentiel multi-établissements : établissements, types de chambres et unités physiques, publication vitrine (Refs #7944, EPIC #7951, BC-32).** **(1) Schéma tenant** : migration `2026_09_20_000014_7944` — 3 tables `hospitality_properties` (code unique par tenant, `public_slug` unique GLOBAL nullable, CHECK type `hotel|residence|apartment_building|guesthouse` et statut), `hospitality_room_types` (unique `(company_id, property_id, code)`, prix en minor units) et `hospitality_units` (statut opérationnel borné `available|occupied|maintenance|out_of_service`, `room_type_id` nullable = appartement locatif hors typologie) — pattern « Travel moderne » (pas de FK physique, index nommés, idempotente), miroir fixture `CreatesMvpSchema`. **(2) API v1** sous `/hospitality` : CRUD établissements + `publish`/`unpublish` (le slug public — base lisible + suffixe aléatoire re-tiré jusqu'à unicité globale — est généré au premier passage `is_public=true` puis CONSERVÉ : URL `/stay/{slug}` stable à la re-publication), CRUD types de chambres et unités (routes imbriquées + shallow, `whereNumber`) — un établissement avec inventaire n'est pas supprimable (422 `HOSPITALITY_PROPERTY_IN_USE`), un type rattaché à des unités non plus (422 `HOSPITALITY_ROOM_TYPE_IN_USE`), le type d'une unité doit appartenir au MÊME établissement (422 en validation). **(3) RBAC deny-by-default** : `HospitalityAccess` (direction = manager principal/rh/sans sous-rôle — le RBAC ressource-scopé par établissement arrive en HOSP-003) + 3 policies enregistrées au point unique `AuthServiceProvider` ; cross-tenant → 404 fail-closed partout (show/update/delete, routes imbriquées incluses). **OpenAPI** : +8 paths / 15 opérations + 6 schémas (garde couverture 0 drift). Tests Feature `HospitalityReferentialTest` (13 cas : 401/403 flag/403 lambda, CRUD complet ×3 ressources, unicités par tenant réutilisables cross-tenant, slug unique + stabilité re-publication, règles d'attachement des unités, 404 cross-tenant systématique). Sonde `/hospitality/status` : jalon `HOSP-002`, capacités `properties`/`inventory` actives. **PHPUnit/PHPStan non exécutés localement (PHP absent de la sandbox) — à confirmer par la CI ; gardes sans PHP vertes** (OpenAPI 0 drift, collisions migrations, duplicate schema, parité fixture MVP).**
- **feat(hospitality): verticale HospitalityManager — fondations HOSP-001 (Refs #7943, EPIC #7951, BC-32 HOSPITALITY, spec `docs/specifications/SOLUTION_HOSPITALITY.md`, [FREEZE-EXCEPTION] fondateur 2026-09-20).** Nouveau module DDD `api/app/Modules/HospitalityManager` (32e module) sur le pattern HealthManager/EduManager : chaînes d'hôtels, résidences et gestion locative multi-sites (l'établissement EST le tenant — aucun couplage avec `TravelHotel` BC-24, hôtels partenaires d'agences). **Fondations fail-closed** : `HospitalityManagerManifest` (code `hospitality`, maturité pilot, modules requis rh/documents/notifications, PII clients/locataires et données financières déclarées sensibles, permissions `hospitality.admin|manager|reception`) enregistré aux TROIS points obligatoires (leçon #7220/#7235) — `SolutionCatalogue` via provider (inversion de dépendance #5584), `config/feature-flags.php` (scope solution, défaut false, killable, since 4.34.0) et `Company::KNOWN_MODULES` ; provider déclaré dans `bootstrap/providers.php` ; routes `/api/v1/hospitality/*` (stack auth+tenant+throttle) avec sonde `GET /hospitality/status` (état du module + jalon du programme HOSP-001→008) ; trait `ChecksHospitalitySolution` (flag → 403 `HOSPITALITY_SOLUTION_INACTIVE`, cross-tenant → 404) ; BC-32 au registre des bounded contexts + CODEOWNERS + arêtes MAT-002 (BC-32→BC-01/02/03) ; parité docs architecture ×4 (32 modules) ; préfixe front `/hospitality` protégé ×3 (`protected-prefixes.ts` + `proxy.ts` + `sw.js` — la vitrine publique `/stay/[slug]` restera hors préfixe) ; OpenAPI +1 path (tag `Hospitality`). Tests Feature `HospitalityModuleStatusTest` (6 cas : KNOWN_MODULES, flag fail-closed enregistré, manifest au catalogue, 401 sans auth, 403 flag inactif, 200 + contrat de la sonde). **PHPUnit/PHPStan non exécutés localement (PHP absent de la sandbox) — à confirmer par la CI ; gardes sans PHP vertes** (registre BC, dépendances BC, parité docs ×4, couverture OpenAPI, collisions migrations, duplication Schema::create). Référentiel/équipe/réservations/locatif/vitrine : lots HOSP-002→008 à venir sur la même branche de lot.
- **fix(security): matrice platform.permission (#7553) déployée sur les blocs oubliés — purge tenant, CRUD offres, webhooks sortants, réglages /admin, audit paie, alias edge-nodes (Closes #7973, lot BC-01 PLATFORM).** La matrice des rôles plateforme n'était appliquée que sur une partie des routes : tout compte d'équipe délégué (support, finance, ops, marketing) pouvait purger un tenant, modifier les offres tarifaires, créer des webhooks sortants vers une URL arbitraire (exfiltration), éditer les templates d'e-mails officiels (phishing via mails légitimes), les réglages IA à clés chiffrées, les barèmes fiscaux/cotisations nationaux (intégrité de la paie), valider les taux légaux et modifier les utilisateurs plateforme. **Nouvelles permissions** : `plans.manage` (finance), `webhooks.manage` (admin+ops), `settings.manage` (admin seul — templates e-mail, IA, tax-slabs, social-contributions, fériés FR/islamiques, rate-validation, oauth-config marketing), `payroll.view` (finance+ops — audit des calculs cross-tenant, **support volontairement exclu** : traces salariales ; cas `PayrollAuditPolicy` tranché ici, le Gate reste en défense en profondeur). **Routes armées** : suppression tenant → `companies.manage` (inventory/destroy), `companies.view` (history) ; CRUD `/platform/plans` → `plans.manage` ; TOUT le bloc `/admin/webhooks*` (lecture comprise — URLs cibles et secrets) → `webhooks.manage` ; bloc réglages `/admin/*` → `settings.manage` ; `/admin/payroll/audit*` → `payroll.view` ; `/admin/users` GET → `users.view`, PATCH → `users.manage` ; **alias `/admin/edge-nodes`** qui perdaient le `edge.manage` des routes `/platform/edge/nodes` au passage (bypass caractérisé) → réalignés. **Cockpit admin** (`navigation.js`) : sondes de menu alignées (référentiels paie ×4, e-mails, IA, marketing-oauth → `settings.manage` ; webhooks → `webhooks.manage`). **Tests** : nouvelle suite `PlatformPermissionMatrixDeploymentTest` (matrice rôle→permission + 403 pour rôle délégué sans permission et passage middleware pour rôle habilité, sur chaque bloc touché) ; aucun test existant ne fixe `platform_role` (défaut `super_admin` passe-partout) → non-régression préservée.
- **fix(backend,infra): lot BC-01 PLATFORM — critiques d'audit 2026-09-20 (#7974, #7975, #7977, #7978).** **(1) #7974 — `leopardo:migrate --fresh` destructible en production** : la commande exécutait `DROP SCHEMA shared_tenants CASCADE` + `DROP SCHEMA public CASCADE` sans aucune garde d'environnement. Désormais : refus sec (exit 1) en environnement `production` quelles que soient les options ; hors production, confirmation interactive rappelant hôte/base/environnement cible, sauf `--force` explicite (nouvelle option) ; `Makefile migrate-fresh` et `scripts/bootstrap.sh` (contextes dev docker non-interactifs) passent `--force` explicitement ; test Feature `LeopardoMigrateFreshGuardTest` (refus production + abandon sans confirmation) et `LeopardoMigrateRunnerTest` réaligné. **(2) #7975 — schéma non rejouable from scratch** : la migration `create_onboarding_progresses_table` vivait à la RACINE de `database/migrations/`, hors du runner canonique → table jamais créée par le pipeline ; déplacée dans `tenant/` (basename conservé + garde `schemaTableExists`). Précision d'audit : la table `users` EST créée par `public/2026_05_02_100001` (helper `createTableIfMissing($variable)` — invisible au grep littéral) ; le vrai défaut était la DIVERGENCE des fixtures maintenues à la main : `CreatesMvpSchema` + `mvp_schema.pgsql.sql` alignés sur le schéma migré (`provider` défaut `'email'`, `google_id` UNIQUE, `avatar_url`, `personal_statuses` jsonb, retrait des `DEFAULT ''` sur les noms, `failed_login_attempts` integer). Nouvelle garde CI `check-migration-root-orphans.sh` + test de rejouabilité dans `LeopardoMigrateRunnerTest`. **(3) #7977 — worker prod Render tronqué** : bloc commenté `leopardo-queue-worker-prod` de `render.prod.yaml` porté de 14 à 34 clés env à parité avec le worker dev (`MAIL_*`, `RESEND_KEY`, `FIREBASE_*`, `SENTRY_*` référencé depuis le web, `STRIPE_SECRET_KEY`, `CHARGILY_API_KEY`, `ADMIN_DASHBOARD_URL`, `SESSION_DRIVER`, `REDIS_*` via `fromDatabase`) ; `CACHE_STORE` file → **redis** ; rappel de parité en en-tête + garde CI `check-render-worker-env-parity.sh`. **(4) #7978 — environnement dev à froid dégradé** : `CACHE_DRIVER` (mort sous Laravel 12) → `CACHE_STORE: redis` ×3 dans le compose racine ; `scripts/bootstrap.sh` installe Composer avant `key:generate`/migrations ; `api/docker-compose.yml` legacy reçoit la boucle d'attente vendor + `restart: unless-stopped` ; devcontainer : copie `api/.env.example` avant `key:generate` + Node 20 → 22. **Vérifications locales (sandbox sans PHP)** : lint syntaxique PHP (php-parser) des fichiers touchés, gardes bash basename-collisions / tenant-schema / root-orphans / worker-env-parity vertes, YAML/JSON validés — PHPStan/Pest à confirmer par la CI.
- **fix(security): lot fronts — CSP stricte sur travel-web & marketplace, cookies de session prod HTTPS-only, sslmode DB piloté par env, session acheteur marketplace durcie, avatars admin 100 % locaux, session auth extraite de i18n.ts (Closes #7979, #7980, #8000 — audit 2026-09-20).** **(1) #7980** : `Content-Security-Policy` posée sur les deux fronts publics (default-src 'self', connect-src = 'self' + origine API résolue depuis la variable de build obligatoire #7963, object-src/frame-ancestors 'none', assouplissements dev bornés à NODE_ENV=development — nonce CSP = tranche suivante documentée) ; `SESSION_SECURE_COOKIE=true` et `DB_SSLMODE=require` dans `render.prod.yaml` ; `sslmode` pgsql piloté par `DB_SSLMODE` (fin du 'prefer' en dur). **(2) #7979** (tranche « à minima » de l'issue, le cookie HttpOnly reste la cible cross-stack) : TTL du jeton acheteur 30 j → **7 j** (`RetailBuyerAccountService`), compromis localStorage documenté dans `buyer.ts` (pile de mitigation : CSP #7980 + TTL court + jeton hashé révocable) ; le fallback d'URL prod pointé par l'issue était déjà supprimé par #7963. **(3) #8000** : avatars de l'admin-dashboard calculés LOCALEMENT (SVG data-URI à initiales, couleur déterministe) — fin de la fuite des noms vers ui-avatars.com (service tiers absent du registre RGPD) ; gestion de session auth extraite de `front/web/src/lib/i18n.ts` (3 662 lignes) vers `lib/auth-session.ts` dédié (type `StoredAuthUser`, clés, `getStoredUser`/`storeAuthSession`/`clearAuthSession`/`getDisplayName`) avec ré-exports de compatibilité depuis i18n.ts — aucun import existant cassé, cycle ESM documenté et inerte (appels runtime uniquement). **tsc/Jest non exécutés localement (node_modules absents) — à confirmer par la CI.**
- **fix(docker): durcissement conteneurs & HTTP — images dev/Edge non-root, ports dev bindés sur loopback, healthchecks applicatifs, fallback `POSTGRES_PASSWORD` dev-only explicite, cache preflight CORS (Refs #7996, audit 2026-09-20 — tranche dédiée annoncée par #7966).** Sept points de l'audit où l'infra contredisait des décisions déjà actées (#7847 bindings loopback, #7966 images non-root) : **(1) `api/docker/php84/Dockerfile`** — nouvel utilisateur `leopardo` (`USER`, uid/gid **1000** par défaut via `ARG APP_UID/APP_GID`, `HOME` explicite pour composer/DevContainer) : l'image des services dev api/queue/scheduler ne tourne plus en root ; l'uid 1000 = premier utilisateur Linux = propriétaire typique du clone bind-mounté (`./api:/var/www/html`), donc `vendor/` et `storage/` écrits depuis le conteneur restent modifiables par l'hôte (Docker Desktop macOS/Windows gère la correspondance) ; les args sont exposés dans les trois sections `build:` du compose (`${APP_UID:-1000}`) et le workflow `onboarding-smoke.yml` les aligne sur l'uid 1001 du runner (`APP_UID=$(id -u)` au `up --build`) — sans cela le bind-mount du checkout CI aurait été en lecture seule pour le conteneur. **(2) `edge/Dockerfile` standalone FrankenPHP** — rattrapage du lot #7966 (qui ne couvrait que `Dockerfile.edge`/`Dockerfile.publish`) : `USER www-data`, chown au build de `/data` + `/config` (XDG Caddy), entrypoint rendu compatible non-root (chowns conditionnels à root, erreur explicite si le volume `/data` provient d'une version root — procédure de rechownage documentée), `EXPOSE 8080` et `SERVER_NAME` cohérents avec `Caddyfile.standalone` rebasé de `:80` sur `:8080` (l'ancien `EXPOSE 80 443` datait du fonctionnement root). **(3) Bindings loopback** — `docker-compose.yml` publie désormais api (8000), mailpit (8025/1025), dashboard (3000) et web (3001) sur `127.0.0.1` uniquement, comme postgres/redis depuis #7847 ; l'émulateur Android (loopback hôte via 10.0.2.2) est inchangé. **(4) Commentaire MediaMTX honnête** (`edge/docker-compose.yml`) — l'ancien prétendait « exposés sur l'hôte local » alors que 8889/8888 sont publiés sur 0.0.0.0 ; l'exposition LAN est **volontaire et conservée** (les navigateurs du site lisent WebRTC/HLS directement sur le nœud Edge — ils ne tournent pas sur l'hôte, binder 127.0.0.1 casserait la lecture, topologie ADR-0021) et le commentaire rappelle que chaque lecture exige un `stream_token` JWT vérifié par l'API Leopardo (`authHTTPAddress`), l'API de contrôle 9997 restant non publiée. **(5) Healthchecks applicatifs** — api (GET HTTP `/api/v1/health` via PHP, ni curl ni wget dans `php:8.4-cli-bookworm`), queue/scheduler (sonde `/proc` + `vendor/autoload.php` pour distinguer l'attente `composer install` du worker réel), dashboard/web (`node -e fetch` — natif Node 20), edge-ui (`wget` busybox sur :3000) et edge-proxy (`wget` sur `:80/health` — le SEUL point d'entrée #7653 n'en avait pas). **(6) `POSTGRES_PASSWORD:-secret` ×4** — remplacé par le fallback explicite `leopardo-dev-local-only` (et `DB_PASSWORD` de `api/.env.example` aligné, les deux fichiers devant rester égaux) : la syntaxe `:?` (variable exigée) était écartée — aucun `.env.example` racine n'existe et le démarrage à froid documenté dans DEVELOPMENT.md (`cp api/.env.example api/.env && make install`) n'aurait plus fonctionné ; la barrière réelle reste le binding 127.0.0.1 (#7847) + les secrets injectés en prod (Render). **(7) `api/config/cors.php`** — `max_age` 0 → **600** : fin d'un preflight OPTIONS avant CHAQUE requête cross-origin en prod. **Vérifié localement** : `php -l` OK sur `api/config/cors.php`, YAML des deux compose parsé (python3 — docker absent de la sandbox), `sh -n` sur `docker-entrypoint.edge.sh`, gardes `check-env-example-safety.sh` / `check-env-example-parity.sh` / `check-queue-strategy-coherence.sh` vertes. **Non vérifié — build manuel** : build + boot des images et statuts healthy réels — `edge-ci` ne construit AUCUNE image (il n'exécute que des gardes de scripts, constat #8018) : la CI ne peut donc pas le confirmer, une passe manuelle `docker compose up` + inspection des statuts healthy est requise.
- **ci(securite): scan de vulnérabilités des images Docker et audit des Dockerfiles par Trivy — nouveau workflow `image-scan.yml`, acquittements documentés `.trivyignore.yaml`, garde pré-push optionnelle `TRIVY_ENFORCE=1` dans `edge/publish.sh` (Refs #7997).** L'audit du 2026-09-20 l'avait constaté : aucun des 73 workflows ne scannait les CVE des images Docker alors que `edge/publish.sh` pousse `leopardo/edge-*` sur Docker Hub — images installées chez les clients (appliances Edge, #7966) — et que les Dockerfiles n'étaient jamais audités (même logique de chaîne d'approvisionnement que le post-mortem secrets #7968). **(1) Workflow `.github/workflows/image-scan.yml`** (action `aquasecurity/trivy-action` SHA-pinnée `ed142fd` = v0.36.0, conventions du dépôt : `permissions: contents: read` strict, concurrency avec annulation PR-only, `timeout-minutes`, commentaires FR) — **volet PR** : `trivy config` sur le dépôt à chaque PR touchant un `**/Dockerfile*` (paths filter), CRITICAL/HIGH bloquantes (`exit-code: 1`) ; **volet images** (hebdo lundi 05:00 UTC, créneau libre entre CodeQL 04:00 et secret-history 06:00, + `workflow_dispatch`) : build des deux images de publication edge (`edge/Dockerfile.publish` → edge-api, `front/web-offline/Dockerfile` → edge-ui — miroir exact de `publish.sh`) puis `trivy image` CRITICAL/HIGH bloquant avec `ignore-unfixed` (seules les CVE corrigeables — actionnables — bloquent). Pas d'upload SARIF volontairement : sortie table en log, permissions minimales conservées. **(2) `.trivyignore.yaml`** : mécanisme d'acquittement SCOPÉ par chemin avec `statement` justificatif et `expired_at` obligatoires (fail-loud — un acquittement expiré redevient bloquant) ; inventaire initial mesuré localement : DS-0002 (conteneur root) justifié pour les images dev (`front/*/Dockerfile.dev`), l'image CI `api/docker/php84` (exclue de #7966) et l'image de référence non câblée `edge/Dockerfile`, acquitté en **dette datée prioritaire** pour `front/web-offline/Dockerfile` (edge-ui de production : refonte nginx non-root, même chantier que #7966, lot dédié) ; DS-0006 (`COPY --from` auto-référencé, image probablement non constructible) acquitté sur `edge/Dockerfile` non câblée — défaut réel signalé pour un lot dédié. **(3) `edge/publish.sh`** : avec `TRIVY_ENFORCE=1` (défaut 0, en-tête documenté), le push est REFUSÉ si une CRITICAL/HIGH corrigeable non acquittée subsiste — fail-closed si le binaire `trivy` est absent. **Vérifié localement** : `trivy config --exit-code 1` vert après acquittements (reproduit le constat CI à l'identique), `publish.sh` exercé par stubs (4 cas : refus sans binaire, refus sur CRITICAL, push si scan vert, défaut inchangé), YAML parsé, `actionlint` 0 erreur sur le workflow, garde `check-workflow-paths.sh` verte. **Non vérifié (Docker absent de la sandbox)** : build + scan réels des images edge — premier run effectif = le schedule CI.
- **fix(ci,ai,db): tranche 1 du déblocage de main (Refs #8004 — main rouge depuis le 19/09) — handler des tools IA `email_classify`/`email_reply_draft`, CHECK `manager_role` ré-unifié, fixture `CreatesMvpSchema` resynchronisée.** **(1) Bloquant unitaire** : les tools R3/R5 Communication (#7688/#7690) étaient seedés actifs dans `ai_tool_registry` SANS handler PHP — la garde #5625 (`ToolRegistryCoverageTest`, 2 échecs) rougissait chaque run. `IntentEngine` expose désormais les deux read-tools (liste statique Orchestrator + map de handlers) : `email_classify` = parité `ClassifyCommunicationMessageJob` (service canonique `EmailClassificationService`, chaînage idempotent `PrepareCommunicationReplyJob` si classé) ; `email_reply_draft` = chemin canonique `CommunicationReplyService::prepare()` — la politique off/draft/confirm/auto de la boîte décide, le tool n'envoie JAMAIS rien et retourne la proposition Pending ; matrice `ai.tool_permissions` complétée (fail-closed sinon) — `employee` + `communication.classify`/`communication.reply_draft` (déjà accordées dans `role_permissions`). **(2) CHECK `employees_manager_role_check` ré-unifié (migration `2026_09_21_000003_8004`)** : les migrations delivery #6285 et restaurant #6187 s'écrasaient mutuellement (chacune ré-écrivait sa seule liste) — la restaurant, exécutée en dernier, rejetait `dispatcher`/`delivery_manager` en SQLSTATE 23514 (cascades 25P02). La contrainte porte désormais l'UNION des 12 rôles actés ; le commentaire de migration impose de repartir de la liste complète pour tout futur rôle. **(3) Fixture `CreatesMvpSchema` (#7975, double source de schéma)** : la table `employees` de la fixture ignorait 13 colonnes ajoutées par migrations post-création (`google_id` ×16 échecs 42703, `candidate_id`, `two_fa_*`, matricules paie CNPS/CI/SN/BF/ML, `family_parts`) — resynchronisée avec index/unique de parité. **PHPUnit/PHPStan non exécutés localement (PHP absent de la sandbox) — à confirmer par la CI.** Hors périmètre de cette tranche (tri dans l'issue) : les autres familles d'échecs feature (~470) — 42P01 `employees`/`biometric_enrollments`, 23502 `absence_types.company_id`, 0A000 cached plan, 22P02 uuid, doublons `webhook_events`, 404 routes kiosk revoke/rotate-token, CommandNotFoundException Travel/Delivery/Restaurant — chacune fera l'objet d'un correctif dédié ; #8004 reste ouvert jusqu'au vert complet.
- **ci(ops): fronts Vercel — nouveau `marketplace-deploy.yml`, skip « secrets absents » bloquant sur push main, et garde anti-dérive étendue aux fronts (Refs #7994 — prod travel-web/marketplace figées au 19/09, audit 2026-09-20).** L'audit a prouvé que la prod des deux fronts n'était plus déployée (déploiements git CANCELED, secrets CI absents) alors que `main` portait le correctif de sécurité #7963 — avec des runs VERTS trompeurs. Livré : **(1) `marketplace-deploy.yml`** — `front/marketplace` n'avait AUCUN workflow : calqué sur `travel-web-deploy.yml` (#7740) — CLI Vercel épinglée 59.11.2, `vercel pull/build/deploy --prebuilt` depuis la racine (rootDirectory `front/marketplace`, fix #6810), preview PR (le projet `leopardo-marche` n'a pas de pendant dev : preview du même projet) et production sur push main + `workflow_dispatch`, healthchecks (instance unique en preview, domaine stable `https://leopardo-marche.vercel.app` en prod), secrets `VERCEL_MARCHE_PROD_*` documentés (`docs/CI_CD_SECRETS.md` + section Marketplace du runbook `docs/ops/travel-web-vercel.md` — **jamais de token voué à révocation**). **(2) Skip durci (#7994 §4)** — sur push main, l'absence des secrets fait désormais ÉCHOUER le job production (`::error` + exit 1, travel-web ET marketplace) : un push qui touche le front sans déploiement possible est VISIBLE ; le skip preview PR reste un `::warning` non bloquant. **(3) Garde drift fronts (#7994 §5)** — nouveau job `fronts-vercel` dans `deploy-drift-guard.yml` (matrice travel-web/marketplace, script `dev-hub/tools/check-vercel-front-drift.sh` utilisable en local) : comparateur SANS secret Vercel — les workflows de déploiement enregistrent désormais un **GitHub Deployment** après healthcheck réussi (`dev-hub/tools/register-github-deployment.sh`, environnements `Production – leopardo-travel-prod` / `Production – leopardo-marche`) et la garde compare le sha servi au dernier commit `main` du chemin du front (dérive, dernier déploiement non sain, ou **environnement non traçable** = échec visible ; script vérifié en conditions réelles : marketplace sort exactement dans ce dernier cas aujourd'hui). Reste dans #7994 (action propriétaire, hors code) : poser les secrets `VERCEL_TRAVEL_PROD_*` / `VERCEL_MARCHE_PROD_*` (tokens dédiés) puis redéployer — l'issue reste ouverte jusqu'à la première prod déployée par workflow enregistreur. **Vérifié localement** : actionlint 0 erreur sur les 3 workflows, shellcheck 0 finding sur les 2 scripts, YAML valide, garde `check-workflow-paths.sh` verte après commit. **Non vérifié (secrets absents)** : déploiement réel — par construction (prêt-à-secrets).
- **docs(presentation): artefacts développeurs réalignés sur les politiques du dépôt — collection Postman P03 + backend local par défaut, exemple SDK honnêt (fetch natif), READMEs fronts sans backend DEV, CONTRIBUTING en PHPUnit (Closes #7998, audit 2026-09-20).** **(1)** Postman : nom « Leopardo API » (lexique P03 — fin de « Leopardo RH Enterprise »), `baseUrl` = `http://localhost:8000/api/v1` (+ consigne prod dans la description, rappel de l'interdiction du backend DEV #7842/#7963), compte démo = persona seedé `ahmed.benali@techcorp-algerie.dz` avec renvoi à `docs/DEMO_ACCOUNTS.md` (fin du `admin@leopardo-rh.com` sur domaine non opérationnel, reliquat #3520) ; **(2)** `examples/sdk-usage-example.ts` réécrit : le paquet `@leopardo-rh/sdk` n'existe pas — exemple 100 % fetch natif contre l'API v1 réelle (`/auth/login`, `/employees`, `/user/logout`), URL + credentials par variables d'environnement (plus aucun mot de passe en dur) ; **(3)** READMEs `marketplace`/`web-offline` : `NEXT_PUBLIC_MARKET_API_BASE` OBLIGATOIRE sans défaut (#7963), cible dev = backend local (backend DEV Render retiré) ; **(4)** CONTRIBUTING : « PHPUnit for testing » (Pest annoncé mais jamais utilisé — 1 120 fichiers de tests en classes PHPUnit).
- **fix(restaurant): la verticale RestaurantManager devient activable par le chemin standard — flag `restaurantmanager` enregistré dans `config/feature-flags.php` et `Company::KNOWN_MODULES`, activation solution câblée (Closes #7976, sévérité MAJEUR audit 2026-09-20, classe de défaut #7220/#7235 rejouée sur BC-25).** Les 210 routes du module (`module.restaurantmanager` → `hasFeature('restaurantmanager')`) restaient en 403 après l'activation standard de la solution Restaurant : seul le flag `restaurant` était posé (`SolutionActivator`), `restaurantmanager` n'existait ni dans le registre feature-flags ni dans `KNOWN_MODULES` — la verticale (401 fichiers, 71 fichiers de test) n'était joignable que via la commande console. Livré : **(1)** entrée `restaurantmanager` au registre (scope `solution`, default false fail-closed, since 4.34.0, killable) ; **(2)** `restaurantmanager` dans `Company::KNOWN_MODULES` (le PATCH plateforme du flag devient possible) ; **(3)** listener `SolutionActivated` dans `RestaurantManagerServiceProvider` (pattern TravelAgency, zéro couplage core → module) : l'activation standard de `restaurant` pose aussi `restaurantmanager` et seed le référentiel via `ActivateRestaurantManagerAction` (idempotent). Tests : `RestaurantManagerModuleRegistryTest` (pattern #7220 : registre, KNOWN_MODULES, fail-closed `FeatureFlag::for()`, true après activation) + test d'activation standard dans `RestaurantActivationTest`. Note OPS : les tenants déjà activés avec le seul flag `restaurant` restent en 403 tant que l'activation n'est pas rejouée (ou PATCH plateforme du flag). **PHPUnit non exécuté localement (PHP absent de la sandbox) — à confirmer par la CI.**
- **fix(attendance): fin du pointage kiosque `fingerprint` déclaratif — biométrie sans preuve device dégradée en `manual` + audit `unverified`, PIN hors-ligne marqué, client kiosk et bridge honnêtes (Closes #7958, sévérité CRITIQUE audit 2026-09-20).** L'écran « fingerprint » du kiosque était un champ texte libre : n'importe qui pouvait pointer pour n'importe quel employé et l'événement était enregistré `method=fingerprint` — fausse garantie d'authenticité pour la paie et l'audit. **(1) Serveur** (`KioskAttendanceService`) : APRÈS la matrice BIO-006 (méthode désactivée → 422 et enrôlement → 403, inchangés), toute méthode biométrique réclamée (`fingerprint`/`face`) SANS preuve device vérifiable est dégradée en `manual` — fail-closed via le nouveau point d'extension `hasVerifiableBiometricProof()` (aucun vérificateur tant que l'intégration SDK lecteur n'existe ; le champ contrat `biometric_proof` est réservé, punch ET sync) ; l'audit `kiosk.punch.recorded` porte `result_code=UNVERIFIED_IDENTITY` + contexte `{claimed_method, unverified, reason}` ; la réponse punch expose `unverified: true` ; `manual` est accepté hors matrice (mode déclaratif honnête, distingué en audit). **(2) Sync offline** : même dégradation par événement + les événements marqués `unverified` par le bridge (PIN hors-ligne) conservent leur méthode mais produisent une ligne d'audit `kiosk.punch.unverified` — distinguables et validables après coup, jamais invisibles. **(3) Client** : `app.js` envoie `method=manual` (fonction renommée `submitDeclarativePunch`), i18n ×4 locales honnêtes (« Pointage déclaratif — identité non vérifiée », fin du « Posez le doigt » mensonger) ; le PIN hors-ligne n'est plus maquillé en `fingerprint` (coercion silencieuse corrigée : `VALID_PUNCH_METHODS` du bridge aligné sur le contrat serveur + marquage `unverified` à l'upload). Tests : `KioskFingerprintRegressionTest` réécrit (dégradation + fail-closed sur preuve quelconque + `manual` accepté), `KioskOfflineSyncReliabilityTest` (face→manual en sync + audit PIN `unverified`), assertions d'audit enrichies. **PHPUnit/PHPStan non exécutés localement (PHP absent de la sandbox) — à confirmer par la CI.** Hors périmètre assumé : le flux ADMS des terminaux ZKTeco physiques (`ZktecoController`) est inchangé — le terminal authentifié EST la preuve device.
- **fix(edge): images Docker Edge 100 % non-root — `USER www-data`, nginx :8080, supervisord déprivilégié (Closes #7966, sévérité MAJEUR audit 2026-09-20, appliances on-prem clients).** `Dockerfile.edge` et `Dockerfile.publish` (minima de l'issue) : nginx écoute sur **:8080** (`nginx.edge.conf`), `USER www-data` après les chowns (`/var/log/supervisor`, `/var/lib/nginx`, `/var/log/nginx`, `/run/nginx`, `/var/leopardo` pour publish), `CMD supervisord -u www-data` (no-op si déjà www-data — filet contre un `--user root`), `EXPOSE 8080`, healthcheck publish sur :8080 ; consommateurs propagés : `docker-compose.yml` (healthcheck), `Caddyfile.edge` (`reverse_proxy edge-api:8080`), `install.sh` (garde de contenu du Caddyfile téléchargé), `edge/README.md`. Le modèle #7847 (« supervisord root pour binder :80 ») est remplacé — plus aucun process root, PID 1 inclus. **Non vérifié — build manuel** : build + boot des images — `edge-ci` ne construit aucune image (gardes de scripts uniquement, constat #8018) : vérification manuelle requise. Hors périmètre justifié : `edge/Dockerfile` standalone (entrypoint FrankenPHP chownant `/data` en root + Caddy :80 — refonte port non triviale, image de référence non câblée) et `api/docker/php84/Dockerfile` (CLI dev/CI bind-mounté, jamais livré chez les clients).
- **fix(front): retrait total du fallback en dur vers le backend DEV Render — `NEXT_PUBLIC_API_URL`/`NEXT_PUBLIC_MARKET_API_BASE` exigées avec échec explicite au build (Closes #7963, sévérité MAJEUR audit 2026-09-20).** #7842 avait fail-fast au RUNTIME prod ; le domaine dev restait codé en dur et les builds passaient sans variable. Désormais : **(1)** `backend-url.ts` (web + travel-web, copies TOUJOURS byte-identiques) et `csp.ts` : constante onrender supprimée, repli dev/test = `http://localhost:8000(/api/v1)` + `console.warn`, **throw explicite pendant `next build`** (renverse l'exception lighthouse de #7842 — `lighthouse.yml` pose désormais `NEXT_PUBLIC_API_URL: http://localhost:8000/api/v1`) ET au runtime prod ; **(2)** `marketplace/api.ts` : `NEXT_PUBLIC_MARKET_API_BASE` exigée partout (seul `NODE_ENV=test` tolère un repli localhost) ; **(3)** mobile : message du fail-fast release aligné (#4524 existant, mention legacy retirée) ; **(4)** zéro référence `gestionemployerbackend` dans `front/**` hors `.md` (admin-dashboard, web-offline, vitrine restaurant-wizard neutralisés ; e2e specs durcies : `E2E_API_URL` obligatoire, assertion anti-fuite `onrender.com`). **(5) Vercel (API, comptes dev+prod)** : `NEXT_PUBLIC_API_URL` posée sur `leopardo-travel` (dev, production+preview) et `leopardo-travel-prod` (production) — leurs prochains builds auraient échoué sinon ; `leopardo-prod`/`leopardo`/`leopardo-marche` déjà couverts ; previews des projets prod SANS la variable = échec de build explicite voulu par l'issue. Tests Jest `backend-url.test.ts`/`proxy-csp.test.ts` mis à jour (build-throw + localhost). **Jest/tsc non exécutés localement (node_modules absents) — à confirmer par la CI.**
- **fix(ops): `APP_KEY` Render en `sync: false` après épinglage de la valeur courante — fin du risque de régénération à la re-sync blueprint (Refs #7967, tranche code de l'issue).** `render.yaml` (dev) et `render.prod.yaml` : `generateValue: true` → `sync: false` — une re-sync de blueprint ne régénérera plus la clé (sessions + valeurs chiffrées invalidées). Avant le basculement, la valeur GÉNÉRÉE courante a été lue via l'API Render puis **ré-écrite à l'identique** (`PUT /services/{id}/env-vars/APP_KEY`, services `gestionemployerBackend` dev et `leopardo-prod`) : aucune rotation, aucun impact runtime, la valeur survit désormais à toute re-sync. Reste dans #7967 (action propriétaire billing, hors code) : moyen de paiement + worker starter ~7 $/mois pour sortir queue/scheduler du web free (plan de sortie déjà documenté en tête des blueprints).
- **chore(ops): réalignement du déploiement dev Render sur `main` (Refs #7304, garde deploy-drift-guard).** La garde anti-dérive échouait depuis le run de 14:13 (dev servant `aee2a60` alors que le dernier commit `api/` était `2e59161`) : redéploiement déclenché via l'API Render (`POST /v1/services/{id}/deploys`) puis `workflow_dispatch` de la garde pour constater le vert — procédure du runbook `docs/ops/RENDER_DEV_ALIGNMENT.md` exécutée sans attendre un push `api/`.
- **feat(payroll): Canada — impôt provincial 2026 QC et ON + régimes québécois RRQ/RRQ2, RQAP et AE réduit (Closes #7933, lot BC-07 conformité légale multi-marchés — épic #7924).** `CanadaPayrollRules` ne modélisait que le fédéral. Ajouté : **(1) impôt provincial 2026** — Québec (14 % ≤ $54 345, 19 % ≤ $108 680, 24 % ≤ $132 245, 25,75 % au-delà ; crédit personnel de base $18 952 × 14 % ; **abattement du Québec −16,5 % de l'impôt fédéral de base**, T4032-QC 2026) et Ontario (5,05/9,15/11,16/12,16/13,16 % ; crédit $12 989 × 5,05 % ; **surtaxe** 20 % > $5 818 + 36 % > $7 446 — la contribution-santé ON n'est PAS modélisée, documenté) ; **(2) employé QC** : RRQ/QPP **6,30 %** (base 5,3 % + supplémentaire 1 %, exemption $3 500, YMPE $74 600) + RRQ2 4 % [YMPE, YAMPE $85 000] À LA PLACE du CPP, AE au taux **réduit Québec 1,30 %/1,82 %** (réduction RQAP 0,33 pt, CEIC 2026) et **RQAP 0,430 %/0,602 %** sur $103 000/an (codes `QPP_*`, `QPP2_*`, `EI_QC_*`, `QPIP_QC_*`) ; **(3) résolution de province EXPLICITE** : `forProvince()` (ISO 3166-2:CA) ou override entreprise `company.metadata.payroll_ca_province` lu par `forCompany()` — **code inconnu = `InvalidArgumentException`** (fin du fallback silencieux ; sans province = fédéral-seul documenté ; provinces reconnues hors QC/ON = fédéral-seul acté dans `complianceWarning()` et `CA_COMPLIANCE.md` §6) ; **(4)** `rulesVersion()` suffixé par la province (`-qc`/`-on`/`-federal`). Sources 2026 vérifiées le 2026-09-21 (Revenu Québec, ministère des Finances QC nov. 2025, Retraite Québec, CEIC/EDSC, T4032-QC) et consignées dans `docs/payroll/CA_COMPLIANCE.md` (§1bis, §2bis, §6, §9 — assiette provinciale simplifiée = assiette fédérale, limite documentée). Confidence reste `pilot`. **Golden tests calculés à la main** : nouvelle suite `GoldenCaProvincialPayrollTest` (10 tests : QC 2 000/6 000/9 000/15 000 — dont plafonds RRQ2/RQAP —, ON 2 000/6 000/9 000/15 000 — dont surtaxe palier 1 seul puis 2 paliers —, exception province inconnue, override entreprise) ; `tests/Unit/PayrollCountryRulesTest` mis à jour (l'ancien fallback silencieux `forProvince('XX')` attend désormais l'exception). **Vérifié localement (PHP 8.4 + PostgreSQL, base `leopardo_test_w3`)** : suites Golden CA (48 tests / 548 assertions) vertes, **suite complète `tests/Feature/Payroll` : 902 passed / 1 failed** — l'échec `PayrollRunStateMachineTest::bulk_pay_accepts_validated_run` (503) est **préexistant sur main** (reproduit à l'identique après stash, environnemental) ; `pint --dirty` PASS ; PHPStan strict `[OK] No errors` sur les fichiers touchés. Coverage : pas de driver pcov/xdebug local — gate CI Payroll ≥ 80 % à confirmer par la CI.
- **docs(payroll): référentiels de mentions obligatoires du bulletin de paie FR, TR, CA, CM, CI, SN (Closes #7932, lot BC-07 conformité légale multi-marchés — épic #7924).** Seul `BULLETIN_DZ_MENTIONS.md` existait ; six nouveaux référentiels `docs/payroll/BULLETIN_{FR,TR,CA,CM,CI,SN}_MENTIONS.md` sur le gabarit DZ, chacun avec : mentions obligatoires sourcées (FR : art. R3243-1 C. trav. + modèle « bulletin clarifié » arrêté 31/01/2023 avec montant net social ; TR : İş Kanunu 4857 m.37 *ücret hesap pusulası* + VUK m.238 ; CA : Code canadien du travail art. 254 + LNT Québec art. 46 ; CM : art. 68 loi 92/007 ; CI : art. 32.5/32.6 loi 2015-532 ; SN : art. L.115/L.116 loi 97-17), mentions **interdites ou sans effet** (FR : grève et activité de représentation — art. R3243-4, le motif d'absence maladie étant non obligatoire mais pas formellement interdit ; CM/CI/SN : « pour solde de tout compte » non opposable), et **checklist de couverture du générateur PDF** (`PaySlipPdfGenerator` + vue `pdf.payslip`) : champs déjà couverts (lignes earnings/deductions avec base-taux-montant, brut, net, cotisations patronales, cumuls annuels) vs manquants (FR : SIRET/APE/URSSAF, convention collective, regroupement par risque, net social, bloc PAS, coût employeur — écart IMPORTANT acté sans modifier le générateur ; TR/CA/CM/CI : identifiants légaux employeur/salarié, date de paiement, heures/taux horaire CA). Aucune promesse de conformité — tous en `pilot`, validation experte requise (`VALIDATION_EXPERTE.md`).
- **fix(ci,e2e): les deux suites Playwright web repassent au vert sur main après la refonte UX du 2026-09-20 (Closes #7955 — suites de #7882..#7886, #7896, #7898, #7748).** Web CI admin (« Web E2E Playwright ») : `sidebar-unique-entries.spec.js` réaligné sur la nav #7855/#7898 (« Formations » vit sous « RH & Paie » — le repli de « Modules des entreprises clientes » est testé sur « Flotte véhicules », le rattachement/repli de Formations sous « RH & Paie » est verrouillé) ; et **régression i18n réparée À LA SOURCE** : le catalogue partagé portait encore « Baremes fiscaux »/« Taux legaux » sans accents — la resynchronisation avait écrasé le correctif #7725 dans l'admin — `shared/i18n/locales/fr.json` restauré (« Barèmes fiscaux », « Taux légaux ») + 3 syncs rejoués (web/mobile/backend, `I18N_VALIDATION_OK (4 locales)`). Web Marketing Funnel E2E (front/web) : **(1) régression produit réparée** — le `loading` de la page équipe unique `/employees` (#7862/#7884) attendait `GET /invitations` avant d'afficher la liste (même leçon que #7321 pour les départements) : les invitations sont désormais chargées SANS bloquer l'affichage ; **(2)** la fixture `e2e/fixtures/authenticated.ts` mocke les nouveaux appels transverses du shell #7882 (`/company/branding`) et de la page équipe (`/invitations`) — non mockés, ils partaient vers le vrai backend CI avec le jeton e2e factice → 401 → purge de session et redirection /auth/login en plein test (options d'opt-out `mockBranding`/`mockInvitations`) ; **(3) specs obsolètes mises à jour** : `auth-client-smoke` (copie renommée #7882 : « Sign in to Leopardo », fr « Connexion à Leopardo »), `marketing-funnel` (#7853 : champ « entreprise » retiré de /signup, absence de `payload.company` verrouillée — `funnel.spec.ts` avait été migré, pas celui-ci), `payroll-compliance` (mock `GET /supported-countries` manquant), `shop-order` réécrit contre `/shop` (#7748 : `/order` = suivi seul, deep-link `?ref=` couvert). **Vérifié localement** : suite admin Playwright complète 71 verts (1 flaky préexistant platform-team), les 12 specs du job vitrine 35/35 verts (chromium, `npm run build && npm run start`), `npx jest --silent` 146 suites / 1278 tests verts, `tsc --noEmit` 0, eslint front/web + e2e + admin 0 warning, `check:mojibake` OK. Note : `check-public-promises.sh` porte 2 erreurs PRÉEXISTANTES sur main (blog.ts/alternatives.ts, pattern « conformité légale ») — hors périmètre, reproduites à l'identique sans ces changements.
- **fix(ci): Web Offline CI — PWA Edge repasse au vert — alignement de `front/web-offline` sur le standard du dépôt (Closes #7972).** `front/web-offline/package.json` épinglait `typescript: ^7.0.2` alors que `typescript-eslint@8.70.0` exige `typescript >=4.8.4 <6.1.0` (peer) : `npm ci` échouait en ERESOLVE et le workflow était rouge sur main depuis le 2026-09-19, ainsi que sur toute branche touchant `front/web-offline/**`. Correction : `typescript ^5.9.3` (comme `web`, `travel-web`, `marketplace`) + `package-lock.json` régénéré proprement. **Vérifié localement (Node 24)** : `npm ci --dry-run` vert, `npm ci` + `npm run lint` 0 erreur, `npm test` 23/23 verts (3 fichiers), `npm run build` Next.js OK.
- **feat(accounting): lot BC-08 « conformité légale multi-marchés » — plan comptable seedé par référentiel pays, fiscalité indirecte TR/CA complète, FEC France conforme, mentions légales étendues et rétention par juridiction (Closes #7925, #7926, #7927, #7928, #7929 — épic #7924, conception `docs/specifications/CONFORMITE_LEGALE_MULTI_MARCHES_2026.md`).** **(1) #7925 — seed du plan comptable par référentiel** : `ChartOfAccountsDefaults::forCountry()` résout le jeu de comptes provisionné depuis le pays du tenant via la NOUVELLE résolution de famille partagée `AccountingChartOfAccounts::familyFor()` (zone OHADA → **SYSCOHADA** AUDCIF classes 1→8 avec 521 Banques/571 Caisse/44571-44566 TVA, TR → **Tekdüzen Hesap Planı** ~35 comptes aux libellés localisés tr **et** fr (100 Kasa, 120 Alıcılar, 191 İndirilecek KDV, 320 Satıcılar, 391 Hesaplanan KDV…), CA → plan nord-américain usuel 1000-6700 aligné sur le registre `CA_ACCOUNTS`, sinon PCG historique — GB/US restent sur le socle PCG, hors périmètre documenté) ; `ChartOfAccountsService::ensureProvisioned()` accepte pays+langue (résolus depuis `public.companies` si absents), le listener `ProvisionChartOfAccounts` les transmet ; DZ/FR strictement inchangés (non-régression testée). **(2) #7926 — taxes TR & CA** : `TVA_RATES_BY_COUNTRY` passe TR en multi-taux **KDV 20/10/1** (KDV Kanunu m.28 + Cumhurbaşkanı Kararı 7346 — source GİB, liste des taux) ; nouvelle table `CA_TVA_RATES_BY_PROVINCE` (TPS 5 partout ; TVH ON 13, **NS 14 depuis le 2025-04-01** (ARC Notice 342), NB/NL/PE 15 ; TVQ QC 9,975 ; TVP BC 7 / SK 6 / RST MB 7 ; AB/territoires TPS seule — source ARC « GST/HST calculator (and rates) ») exposée via `AccountingSettingsDefaults::tvaRates($country, $province)` — la province (metadata `province`, ISO 3166-2:CA) est transmise par le provisioning, SANS défaut provincial silencieux (fédéral 5 % sinon) ; nouveaux `label_key` `super_reduced`/`hst`/`pst`/`qst` traduits ×4 locales ; `VatDeclarationService` ventile déjà par taux — golden test KDV 20/10/1 ajouté. **(3) #7927 — FEC France** : la devise `Devise`/`MontantDevise` n'est plus `DZD` codée en dur mais résolue depuis les réglages comptables du tenant puis son pays (`CountryDefaults` — EUR pour FR) ; pour un tenant FR, le fichier prend le nom officiel DGFiP **`SIRENFECAAAAMMJJ`** (art. A. 47 A-1 LPF, clôture d'exercice civile au 31/12) à partir du réglage SIREN (metadata `siren`, 9 chiffres validés, ou dérivé d'un SIRET 14 chiffres) — SIREN absent/invalide → 422 explicite `FEC_SIREN_MISSING`/`FEC_SIREN_INVALID` (messages ×4 locales), hors FR : nom `fec-YYYY-MM.csv` et comportement inchangés (`FecExportTest` vert). **(4) #7928 — mentions légales de facture** : `LEGAL_MENTIONS_BY_COUNTRY` passe de 7 à 21 pays — CEMAC CM (RCCM+**NIU** DGI), GA (RCCM+NIF), CG (RCCM+**NIU** niu.cg), TD/CF/GQ (RCCM+NIF) ; CEDEAO BF (RCCM+**IFU** DGI Burkina), BJ (RCCM+**IFU** ifu.impots.bj), ML/TG/NE (RCCM+NIF — DGI Mali/OTR Togo/DGI Niger) ; CA (**BN** + n° TPS/TVH + mention TVQ Québec), GB (Company No + VAT Registration No), US (EIN) — chaque identifiant sourcé en commentaire, les 7 pays historiques sont goldens inchangés. **(5) #7929 — rétention par juridiction** : `AccountingRetentionService::retentionMonthsFor($country)` — OHADA 120 mois (AUDCIF art. 24), FR 120 (C. com. L123-22), TR 120 (TTK 6102 m. 82 — VUK 5 ans, durée la plus longue retenue), **CA 72 (LIR 230(4) + règl. 5800)**, défaut conservateur 120 ; `accounting:purge-expired` sans option purge désormais PAR PAYS du tenant (`purgeByCountry`), `--older-than` reste l'override opérateur ; l'env `ACCOUNTING_RETENTION_MONTHS` devient le défaut des pays hors table (il ne raccourcit jamais une durée légale) ; `docs/security/ACCOUNTING_RETENTION.md` réécrite avec les sources. Tout nouveau référentiel est en confiance `pilot` (constitution §III) ; aucun fichier `front/`/`site/` touché (garde anti-over-promise #7922). **Vérifié localement (PHP 8.4 + PostgreSQL)** : 4 nouvelles suites Feature — `AccountingChartSeedByCountryTest` (7), `AccountingTaxRatesByCountryTest` (8), `FecExportFranceTest` (5), `AccountingLegalMentionsCoverageTest` (5), `AccountingRetentionByCountryTest` (5) — et non-régression `tests/Feature/Accounting` complète verte ; `pint --dirty` PASS ; PHPStan strict sur les fichiers touchés `[OK]`.
- **feat(leave): congés légaux France, Turquie, Canada — barèmes d'ancienneté dans le moteur de règles légales (Closes #7931, lot BC-06, épic #7924).** Suite de #7930 : **FR** 2,5 j ouvrables/mois, plafond 30 j ouvrables/an (C. trav. art. L3141-3) ; **TR** barème d'ancienneté İş Kanunu n° 4857 m. 53 en jours OUVRABLES (m. 56) — aucun droit avant 1 an, 14 j de 1 à 5 ans (5 ans inclus), 20 j de plus de 5 à moins de 15 ans, 26 j à 15 ans et plus (minimum 20 j des ≤ 18/≥ 50 ans non modélisé, pilot) ; **CA fédéral** Code canadien du travail art. 184/184.01 — 2 semaines (10 j ouvrables) < 5 ans, 3 semaines (15 j) de 5 à 10 ans, 4 semaines (20 j) à 10 ans et plus, indemnité de congé annuel 4/6/8 % du brut (art. 183) — normes provinciales hors modèle (documenté). **Extension d'abstraction SANS casser DZ/MA/TN/SN** : `LegalLeaveCountryRuleInterface` gagne `legalAnnualDaysForSeniority(float $seniorityYears)` (défaut `AbstractLegalLeaveCountryRule` = droit annuel de base — comportement historique inchangé pour les 16 pays sans barème) et `vacationPayRatePercent(float)` (défaut null ; CA expose 4/6/8 %) ; `LegalLeaveEntitlementService::projectedEntitlement` résout désormais le droit annuel via le barème avec l'ancienneté au 1er janvier de l'année cible (nouvelle méthode pure `seniorityYearsAtYearStart`) — pour les pays sans barème le calcul est STRICTEMENT identique (annuel/12 arrondi 4 décimales, plafond annuel). Doc `docs/payroll/LEGAL_LEAVES.md` : tableau + détail FR/TR/CA avec sources. Nouvelle suite `LegalLeaveFranceTurkeyCanadaRulesTest` (15 tests golden calculés à la main : bornes exactes du barème TR — 5 ans inclus → 14 j, 15 ans → 26 j, 1re année → 0 j —, seuils CA 5/10 ans et taux 4/6/8 %, FR 10 mois → 25 j / plafond 30 j, non-régression DZ avec 30 ans d'ancienneté). Confidence `pilot` partout, aucun fallback pays silencieux, aucune migration.
- **feat(leave): congés légaux CEMAC & CEDEAO — 12 nouveaux pays dans le registre des règles légales de congés (Closes #7930, lot BC-06, épic #7924).** `LegalLeaveRulesRegistry` ne couvrait que DZ/MA/TN/SN (#5289) alors que le moteur de paie supporte déjà ces marchés. Livré sur le pattern `AbstractLegalLeaveCountryRule` (résolution stricte, `UnsupportedLeaveCountryException` 422 pour tout pays inconnu — AUCUN fallback silencieux, confidence `pilot`) : **CEMAC** — CM 18 j ouvrables/an (1,5 j/mois, art. 89 loi 92/007), GA 24 j (2 j/mois, art. 185 loi 022/2021), CG 26 j (≈ 2,17 j/mois, loi 45-75 — article à confirmer par expert), et en **pilot prudent** TD 24 j (2 j/mois, art. 212 loi 038/PR/96 — à confirmer), CF 24 j (2 j/mois, art. 280 s. loi 09.004, plafond 30 j), GQ 30 j calendaires (« 1 mois », art. 40 ley 4/2021 — à confirmer) ; **CEDEAO** — CI 26,4 j (2,2 j ouvrables/mois, art. 25.1 loi 2015-532), BF 30 j calendaires (2,5 j/mois, art. 156 loi 028-2008/AN), ML 30 j (2,5 j/mois jours non ouvrables compris, art. L.148 s. loi 92-020), TG 30 j (2,5 j ouvrables/mois, loi 2021-012 — article à confirmer), BJ 24 j (2 j/mois, art. 158 loi 98-004), NE 30 j calendaires (2,5 j/mois, art. 116 s. loi 2012-45 — à confirmer). Chaque valeur est sourcée (code du travail national + article) dans `docs/payroll/LEGAL_LEAVES.md` (tableau récapitulatif + détail par pays, incertitudes marquées « à confirmer par expert ») ; les majorations d'ancienneté additives (CG/CF/NE…) restent hors modèle (documenté). Nouvelle suite `LegalLeaveCemacCedeaoRulesTest` : valeurs annuelles/mensuelles des 12 pays + 13 golden tests d'acquisition projetée calculés à la main (prorata mois entiers, plafond annuel — ex. CM 10 mois → 15 j, CI année pleine → 26,4 j, NE 9 mois → 22,5 j). Non-régression DZ/MA/TN/SN : suites #5289 inchangées et vertes.
- **docs(specifications): conception « Conformité légale multi-marchés Compta & RH » — CEMAC, CEDEAO, France, Turquie, Canada (Refs #7924).** Directive fondateur du 2026-09-20 (exception au freeze 60j #5147, tracée dans l'épic [FREEZE-EXCEPTION] #7924) : nouveau document `docs/specifications/CONFORMITE_LEGALE_MULTI_MARCHES_2026.md` — audit de l'existant (plans comptables, TVA/KDV/TPS, FEC, mentions légales, rétention ; moteur paie 20+ pays, déclarations sociales, congés légaux, bulletins), matrice d'exigences par marché, plan en 3 vagues (vague 1 : lots BC-08 #7925–#7929, BC-06 #7930–#7931, BC-07 #7932–#7933 ; vague 2 : #7934–#7941), et règles d'implémentation opposables (taux sourcés, golden tests manuels, pas de fallback pays, confidence `pilot` par défaut, posture anti-over-promise #7922). Documentation uniquement — aucun changement de code.
- **feat(web): espace vendeur /commerce/boutique — compléments réglages boutique en ligne et commandes web (Refs #7810, épic BC-17 vente-suite).** Le cœur de #7810 (page `/commerce/boutique` : opt-in + réglages boutique via GET/PUT `/retail/online/settings`, bascule « En ligne » par produit, liste des commandes web avec actions confirmer/prête/expédiée/livrée/annuler, i18n ×4, machine d'états pure testée) est livré par la branche dépendance `bc/bc17-marketplace-public` (commits #7807/#7808/#7810, mergée dans ce lot — AUCUNE duplication de périmètre). Ce complément branche l'espace vendeur sur les nouveautés du lot : **(1)** le détail d'une commande web affiche la **référence de livraison BC-26** (`delivery_reference`, handoff #7811) ; **(2)** un panneau **Paiement** (statut `paid|partial|cod` calculé par le nouveau helper pur `orderPaymentStatus` — même définition que le solde serveur #7812 — + liste des paiements capturés) ; **(3)** bouton **Facture PDF** (téléchargement blob `GET /retail/orders/{id}/invoice`, numérotation légale #7813, pattern billing/page.tsx). i18n ×4 locales via les registres partagés (`commerce.shop.orders.*`, syncs web/backend/mobile régénérés + versions.json, parité catalogues verte), 4 nouveaux tests unitaires `commerce-format.test.ts`. **Vérifié** : `tsc --noEmit` 0, `eslint --max-warnings 0` 0, Jest 22/22 sur la suite commerce-format, `check-i18n-catalog-parity.sh` vert.
- **feat(retail): reçus et factures — ticket POS imprimable et facture PDF des commandes, numérotation légale par tenant (Closes #7813, épic BC-17 vente-suite).** **(1) Ticket de caisse** `GET /retail/pos/orders/{order}/receipt` : PDF rouleau 80 mm (DomPDF + Blade `pdf.retail-receipt`) d'une vente POS `completed` — lignes snapshotées, remise, paiements capturés, i18n ×4 (nouvelles clés `pdf.retail_*` fr/en/ar/tr, parité verte), RTL via `I18nCatalog::isRtl` ; hors canal POS ou non encaissée → 422, autre tenant → 404. **(2) Facture PDF** `GET /retail/orders/{order}/invoice` (POS **et** commandes web #7808) : A4 avec branding tenant (#7713), **numérotation LÉGALE par tenant** `FAC-YYYY-NNNNNN` (`RetailInvoiceNumberService`) — assignée transactionnellement (verrou de ligne) à la PREMIÈRE génération puis IMMUABLE (les rejeux re-rendent le même numéro), séquence annuelle isolée par tenant, **index unique partiel (company_id, invoice_number)** en filet de course (23505 → retry, mécanique DeliveryReference), migration additive idempotente 2026_09_29_000503 + parité `CreatesMvpSchema` ; commande non `completed` → 422 ORDER_NOT_INVOICEABLE. **(3) Renderer** `RetailDocumentPdfRenderer` (Infrastructure) : MÊME mécanique canonique que le renderer PDF Billing (`InvoicePdfRenderer` #7763) et Payroll (`PaySlipPdfGenerator`) — DomPDF + templates Blade du module, lecture qualifiée `public.companies`, **zéro import cross-module** (garde #5584). OpenAPI +2 paths (0 drift). Nouvelle suite `RetailOrderDocumentsTest` (4 tests : ticket PDF rendu, fail-closed tenant/état, numéro légal immuable + séquences par tenant isolées + incrément, 422 non facturable). **PHPUnit/PHPStan non exécutés localement — à confirmer par la CI.**
- **chore(retail): paiement en ligne marketplace (Refs #7812, épic BC-17 vente-suite) — implémentation retirée de ce lot au profit de celle mergée dans main (PR #7858).** Au merge de main dans la branche, main livrait déjà #7812 (intents `retail_online_payment_intents`, providers Chargily/Mock, webhook signé `POST /public/market/payments/webhook/{provider}`, réconciliation, refund vendeur, front marketplace). Le doublon de la branche (`RetailOnlinePaymentService`, `RetailPaymentProviderContract`, `LoggingRetailPaymentProviderAdapter`, `RetailMarketPaymentPublicController`, table `retail_payment_intents`, `POST /public/market/orders/{reference}/pay`) est supprimé — main est la vérité pour ce périmètre ; le suivi public conserve `payment.{method,status}` (version main) et le COD du handoff BC-26 (#7811) reste le solde non encaissé (`RetailOrderPayment` capturés).
- **feat(retail): handoff livraison BC-26 — création automatique de la livraison à la confirmation d'une commande en ligne Leopardo Marché (Closes #7811, épic BC-17 vente-suite).** À la transition `pending → confirmed` (`RetailOnlineOrderService::confirm`), un événement `RetailOnlineOrderConfirmed` (payload 100 % scalaire) est dispatché APRÈS commit ; le listener BC-26 `CreateDeliveryForRetailOnlineOrder` (module Delivery) crée la livraison via `CreateDeliveryAction` — **intégration par événements, zéro import cross-module (registre BC, garde #5584)** : nouvelle source `retail_online` (enum `DeliverySource`, `DeliveryStoreRequest`, OpenAPI ×2), `source_reference = WEB-…`, **anti-doublon porté par l'unicité existante (company_id, source, source_reference)** (le rejeu de l'événement retourne la livraison existante), COD = solde non encaissé de la commande (`cod_amount_minor`, settlement COD BC-26 existant DELIVERY-205), gated par le feature flag tenant `delivery` et FAIL-SOFT (une erreur du handoff est journalisée, la confirmation vendeur déjà committée n'échoue jamais). En retour, `RetailOnlineOrderDeliveryCreated` (scalaires) permet à Retail de stocker la référence `DLV-…` sur SA table (`retail_orders.delivery_reference`, migration additive idempotente 2026_09_29_000501 + parité `CreatesMvpSchema`) — **le tracking livreur est partagé sur la page de suivi publique** (`GET /public/market/orders/{reference}?token=` expose `delivery.reference`) et dans l'espace vendeur (`delivery_reference` des payloads `/retail/online/orders`). Catalogue d'événements MAT-006 synchronisé (2 entrées, garde verte). Nouvelle suite `RetailOnlineDeliveryHandoffTest` (3 tests : livraison créée avec COD/adresse à la confirmation + référence partagée publiquement, rejeu d'événement sans doublon, aucun handoff sans feature `delivery`). **PHPUnit/PHPStan non exécutés localement (PHP absent de l'image d'agent) — à confirmer par la CI.**
- **feat(restaurant): affectation d'employés aux succursales restaurant — pivot staff ↔ RestaurantBranch, API v1, page Équipe front/web (Closes #7909, BC-25 RESTAURANT).** Nouvelle table tenant `restaurant_branch_staff` (migration `2026_09_20_000013_7909`, `company_id` uuid non nullable, sans FK — pattern Travel moderne du module, unicité `(company_id, branch_id, employee_id)`, softDeletes, miroir fixture `CreatesMvpSchema` #5443) ; modèle `RestaurantBranchStaff` (`BelongsToCompany` + `SoftDeletes`, relations `branch()`/`employee()`, `staff()` hasMany ajouté sur `RestaurantBranch`) ; `RestaurantBranchStaffService::assign()` (Infrastructure/Services, convention #6571) avec validation cross-tenant STRICTE de l'employé (`employee.company_id === branch.company_id`, sinon **422 EMPLOYEE_OUTSIDE_TENANT** — pattern `FuelShiftService::assign()`), doublon actif → **409 EMPLOYEE_ALREADY_ASSIGNED**, ré-affectation après retrait = restauration de la ligne soft-deleted (jamais de doublon physique sous l'unique). API v1 sous `/restaurant/branches/{branch}/staff` : GET liste paginée (employé embarqué id/prénom/nom + rôle + date), POST 201, PATCH `{role}`, DELETE soft delete — 404 sûr cross-tenant sur la branche ET l'affectation (jamais 403), `RestaurantBranchStaffPolicy` composée avec le RBAC ressource-scopé #7599 (`ChecksRestaurantBranchAccess`, geste de gestion = `manage`), enregistrée au point unique `AuthServiceProvider`. OpenAPI : 4 opérations documentées (garde couverture 0 drift). Tests Feature `RestaurantBranchStaffTest` (CRUD complet, 409 + restauration, 422 employé étranger, 404 branche étrangère, RBAC 403 employé simple). **Front/web** : page `/restaurant/team` (sélecteur `BranchSelect` partagé RESTO-904, tableau nom/rôle/date, ajout via référentiel `/employees`, édition du rôle en ligne, retrait avec confirmation — pattern travel/staff), tuile Équipe sur le hub `/restaurant`, i18n `restaurant.team.*` ×4 locales (`shared/i18n` + syncs), 4 tests Jest + scénario e2e ajouté à `restaurant-navigation.spec.ts`. **Vérifié localement** : front/web `eslint --max-warnings 0` OK, **Jest 147 suites / 1275 tests verts**, `check:mojibake` OK ; gardes sans PHP toutes vertes (couverture OpenAPI 0 drift, collisions migrations, duplication `Schema::create --strict`, parité `CreatesMvpSchema`, parité i18n ×4, module-isolation, dépendances BC). **Non vérifié localement (PHP absent de la sandbox)** : PHPUnit/PHPStan/Pint — à confirmer par la CI. Au passage : stub `retail_online_payment_intents` ajouté à `CreatesMvpSchema` (gap #7812 préexistant qui rendait la garde #5443 rouge).
- **feat(web): refonte de la navigation de l'espace web client — sidebar gauche unifiée TOUJOURS rendue + topbar réduite à une ligne fine + page /modules (Closes #7908).** Le shell `(dashboard)/layout.tsx` (~1665 lignes) est découpé : nouveaux composants `front/web/src/components/layout/Sidebar.tsx`, `AccountMenu.tsx` et `panel-dismiss.ts`. **(1) Sidebar unifiée** (colonne fixe w-64 desktop, tiroir sous `md`, rendue même sans verticale métier) : logo/nom compagnie ; section « Mon métier » (`buildBusinessRail`, inchangée, testid `business-rail` conservé) ; section « Entreprise » — les 4 groupes de l'ex-nav horizontale (hr/finance/growth/operations) en ACCORDÉONS repliables, état persisté en localStorage (`dashboard_sidebar_groups`), groupe actif auto-ouvert selon le pathname, testids `dashboard-<groupe>-menu(-panel)` conservés ; section « Plateforme » (Modules → /modules, Abonnement & factures → /billing, Intégrations → /settings/developer) ; pied « Mon compte » (avatar+nom+email, menu vers le haut : Mon compte, Encaissements, Image de marque, Support, **Langue en sous-menu fr/en/tr/ar** réutilisant `handleLanguageChange` — PATCH /auth/language + storeAuthSession + applyDocumentLocale/RTL — et Déconnexion ; testids `user-menu-toggle`/`user-menu`/`user-menu-logout` conservés). **(2) Topbar h-14 sticky** : burger mobile + titre contextuel (#7483 conservé) à gauche ; reprise onboarding, TrialBanner compact, cloche notifications (testids inchangés) et pastille présence (sr-only) à droite — nav horizontale, panneau « Modules & plan », select langue et menu avatar SUPPRIMÉS de la barre. **(3) Nouvelle page `/modules`** reprenant le contenu du panneau « Modules & plan » (statuts Trial/Présent/Lock des modules plateforme, auto-activation #7322 via `activate-module-<key>`, verticales sur demande → /contact?topic=upgrade) ; préfixe `/modules` ajouté à `protected-prefixes.ts` + matcher/`DASHBOARD_PREFIXES` de `proxy.ts` + `public/sw.js` (garde Jest #3377 verte). i18n : 3 nouvelles clés `dashboard.*` ×4 locales. Tests : `layout-header-menu.test.tsx` réécrit, nouvelles suites `layout-sidebar-unified.test.tsx` (6 cas) et `modules-page.test.tsx` (4 cas) ; specs Playwright `dashboard-mobile-drawer/nav`, `manager-workday-smoke`, `client-company-profile` adaptées aux nouveaux sélecteurs (non exécutées localement). **Vérifié localement** : `eslint src` + `eslint e2e` 0 warning, **Jest 148 suites / 1281 tests verts**, `tsc --noEmit` 0, `check:mojibake` OK, `check-i18n-catalog-parity.sh` et `check-web-design-tokens.sh` vertes.
- **feat(health): verticale HealthManager — solution sectorielle hôpitaux & cliniques privées, de la structure à la facturation des soins (Closes #7785, Closes #7786, Closes #7787, Closes #7788, Closes #7789, Closes #7790, Closes #7791, Closes #7792 — spec `docs/specifications/HEALTHMANAGER_SOLUTION.md`, BC-31 HEALTH).** Nouveau module DDD `api/app/Modules/HealthManager` (29e module) sur le pattern EduManager : un fondateur/directeur d'établissement de santé gère sa clinique de A à Z. **(1) Fondations fail-closed (HC-001)** : `HealthManagerManifest` (code `healthmanager`, maturité pilot, modules requis rh/documents/notifications, données de santé déclarées sensibles) enregistré aux TROIS points obligatoires (leçon #7220/#7235) — `SolutionCatalogue` via provider (inversion de dépendance #5584), `config/feature-flags.php` (scope solution, défaut false, killable) et `Company::KNOWN_MODULES` ; routes `/api/v1/health-manager/*` (stack complète auth+tenant+throttle), trait `ChecksHealthSolution` (flag → 403 `HEALTH_SOLUTION_INACTIVE`, cross-tenant → 404), socle RBAC `HealthAccess` (direction = manager principal/rh ; praticien = fiche `health_practitioners` active ; réception/facturation = `health_staff_roles`), BC-31 au registre des bounded contexts + CODEOWNERS + arêtes MAT-002 (BC-01/02/03). **(2) Référentiel structure (HC-002)** : services médicaux, salles typées, lits à états (free/occupied/maintenance), spécialités, praticiens liés aux employés RH (pivot spécialités tenant-scopé), rôles réception/facturation — CRUD complet, suppression bloquée si ressource utilisée (422 `HEALTH_RESOURCE_IN_USE`). **(3) Registre patients (HC-003)** : MRN serveur `PAT-YYYY-NNNN` séquentiel par tenant/année (verrou pessimiste + UNIQUE en garde ultime), PII et données médicales CHIFFRÉES au repos (casts encrypted — naissance, contacts, assurance, allergies, antécédents ; prouvé par tests sur la ligne SQL brute), archivage logique JAMAIS de suppression physique, recherche nom/MRN paginée. **(4) Rendez-vous & agenda (HC-004)** : machine à états scheduled→confirmed→checked_in→completed (+cancelled/no_show, transitions invalides 422 `HEALTH_INVALID_TRANSITION`), chevauchement praticien refusé 409 `HEALTH_APPOINTMENT_CONFLICT` (créneaux adjacents et annulés OK, re-vérifié au déplacement), praticien borné à SON agenda même avec filtre forgé, réception/direction voient tout. **(5) Consultations & prescriptions (HC-005)** : dossier médical (motif, examen clinique, diagnostic, constantes vitales structurées — tous chiffrés au repos), prescriptions ≥1 ligne en transaction ; CONFIDENTIALITÉ MÉDICALE : la réception est refusée MÊME EN LECTURE (403, testé), seul l'auteur ou la direction modifie, un praticien usurpant un practitioner_id étranger est ramené au sien. **(6) Hospitalisations & lits (HC-006)** : admission/transfert/sortie sous `DB::transaction` + `lockForUpdate` (verrouillage croissant anti-deadlock), lit occupé → 409 `HEALTH_BED_OCCUPIED`, transfert libère l'ancien lit atomiquement, sortie libère le lit, double sortie 422, endpoint d'occupation par service (taux exacts testés). **(7) Facturation des soins (HC-007)** : catalogue d'actes tarifés par tenant, factures `HINV-YYYY-NNNN` (numérotation race-safe), PRIX FIGÉS à la ligne (changer le tarif ne change pas la facture émise — testé), totaux recalculés SERVEUR en centimes (remise bornée), brouillon→émise→payée/partiellement payée (sur-paiement 422 `HEALTH_OVERPAYMENT`), annulation d'une facture payée refusée, stats CA du mois + impayés. **(8) Espace web clinique (HC-008, front/web)** : entrées `health*` du catalogue `client-features.ts` (scope business, vertical `health`, NON self-activatable, invisible sans flag — nav dérivée automatiquement), pages `(dashboard)/health/{hub,patients,appointments,admissions,billing,referential}` sur `module-page-shell` (tokens P05, zéro couleur en dur), client typé `health-api.ts`, `/health` ajouté aux préfixes protégés (session gate + proxy + sw.js — donnée de santé jamais servie sans session), dashboard temps réel (RDV du jour, occupation, CA du mois, patients récents) au contrat exact du contrôleur. **Migrations tenant** : 17 tables `health_*` (9 fichiers préfixés #7786..#7791, gardes F-17 #1613, `company_id` NON nullable partout, UNIQUE(id, company_id), CHECK statuts, parité fixture `CreatesMvpSchema` #5443). **Policies** : 17 policies deny-by-default enregistrées au point unique `AuthServiceProvider`. **OpenAPI** : +37 paths / 63 opérations / 20 schémas au style EduManager (codes d'erreur métier documentés), couverture routes↔spec 0 erreur, SDK JS/Python régénérés (1127 opérations), Redocly valide. **i18n ×4** (fr/en/ar/tr, ~180 clés `health.*` via `shared/i18n` + sync). **Docs** : spec solution, parité architecture ×4 (29 modules), `HEALTHMANAGER_DONNEES.md` + `HEALTHMANAGER_RBAC_MATRIX.md`. **Vérifié localement (PHP 8.4 + PostgreSQL 14)** : **PHPUnit `tests/Feature/HealthManager` 71 tests / 517 assertions VERTS** (matrice canonique par domaine : 401 / 403 flag off / 403 lambda / happy path / 404 cross-tenant), PHPStan strict `[OK] 0 erreur` sur tout le périmètre touché (module + AuthServiceProvider + Company + fixture), phpstan-modules `[OK]`, Pint PASS, migrations `EduManagerMigrationsTest` 12 tests verts après `migrate:fresh` complet, gardes locales TOUTES vertes (registre BC 30 BCs, dépendances BC, parité docs 29 modules ×4, parité MVP schema, collisions/conventions migrations, module-isolation, policies single-point, routes tenant/platform, duplicate routes, runbooks, golden journeys, tests déterministes, env parity) ; front/web `tsc --noEmit` 0, `eslint --max-warnings 0` OK, **Jest 134 suites / 1183 tests verts**, sync i18n OK ×4.
- **feat(web): refonte UX de l'espace web client — retours propriétaire du 2026-09-20 (Refs #7860, #7861, #7862, #7863, #7864 — PRs #7882..#7886).** Cinq lots mergés le même jour : **(1) Shell (#7860/PR #7882)** — en-tête de page compact (le hero ~200 px de `module-page-shell` devient une rangée p-4/text-xl, fin de l'eyebrow « Système Leopardo »), sous-menus du bandeau ouverts au survol avec hover-intent (150/300 ms) + navigation clavier, avatar du compte = icône `User` (fin des initiales), menu avatar simplifié (mot de passe/2FA/collaborateurs retirés, entrée « Abonnement & factures » → /billing ajoutée), et **branding tenant réactif** : nouveau `src/lib/tenant-branding.ts` (cache localStorage + CustomEvent `tenant-branding-updated`), le shell s'hydrate depuis le cache à chaque connexion, se re-thème sans rechargement après sauvegarde dans /settings/branding, CSS vars `--tenant-primary`/`--tenant-accent` réellement appliquées. **(2) Mon compte (#7861/PR #7883)** — profil éditable (prénom/nom/téléphone, `PATCH /auth/profile` accepte désormais `phone`), resynchronisation de session, changement de mot de passe conservé, **2FA intégrée** via le composant partagé `two-factor-panel` (la route /settings/security/2fa rend le même composant), carte « Abonnement & facturation » (GET /billing/subscription, silencieuse sur 403) ; ~30 clés i18n `settingsPage.*` ×4 locales propagées par les syncs (web/admin/mobile ARB). **(3) Équipe (#7862/PR #7884)** — /employees devient LA page unique de gestion d'équipe (liste riche + panneau de détails par employé : rôle, module-grants — nouveau `module-grants-panel` extrait de settings/team —, accès ressources, resend/révocation d'invitation, archivage) ; /settings/team = redirection ; onboarding première équipe conservé. **(4) Encaissements (#7863/PR #7885)** — type de profil `cash` (encaissement au local, aucun secret) ajouté à `TenantPaymentProfile::TYPES`, page /settings/encaissements réorganisée en 4 familles (en ligne / virement / mobile money / au local), **nouveaux endpoints** `POST/GET /billing/collections` (enregistrement manuel des encaissements comptoir : montant, devise, mode cash|card_terminal, note — table tenant `billing_collections`, scope fail-closed, principal only) ; OpenAPI : routes collections ET payment-profiles (#7727, drift rattrapé) documentées. **(5) Invitations (#7864/PR #7886)** — audit du parcours d'invitation collaborateur puis correctifs : le resend EFFAÇAIT les resource-assignments pré-portés (#7601) — corrigé ; `FRONTEND_URL` déclarée dans `render.yaml` et `render.prod.yaml` (les liens d'activation ne dépendent plus du fallback codé en dur) ; un employé archivé/parti ne peut plus activer son compte (410 `INVITATION_REVOKED`, clés d'erreur ×4 langues) ; `expires_at` null traité comme expiré (fail-closed) ; warning loggé si l'invitation n'est pas envoyée faute de contexte. **Vérifié** : intégration locale des 5 branches sans conflit — jest 56 suites / 424 tests verts, `tsc --noEmit` 0, eslint 0 warning sur les fichiers touchés, garde PA2-I18N-014 verte, `I18N_VALIDATION_OK (4 locales)`, `php -l` OK ; les 4 checks requis (PHPStan Strict, Module Structure, ESLint+TS, actionlint) verts sur les 5 PRs (2 collisions corrigées en route : préfixe de migration #1962 vs #7811, comptes d'occurrences de la baseline PHPStan sur le nouveau test Feature). PHPUnit Feature des lots API confirmé par la CI.
- **chore(qualite): #7655 tranche 2 — lot B exécuté : campagne d'annotations `@property` sur les 38 modèles Eloquent TravelAgency, baselines PHPStan élaguées de 17 019 → 11 090 lignes (−35 %, occurrences gelées 4 991 → 2 558, −49 % au compteur canonique du cliquet) (Refs #7655).** Première tranche de RÉSORPTION réelle (la tranche 1 avait livré carte + cliquet sans toucher au code) : le plus gros bloc homogène de la dette était `property.notFound` sur `app/Modules/TravelAgency` (669 occ dans la baseline modules, module le plus endetté hors tests). **(1) 284 annotations `@property`** dérivées des **migrations tenant** (création gagnante = la plus ancienne, guard `schemaTableExists` ; colonnes ajoutées/relâchées par la consolidation #7452 incluses) puis surchargées par les `$casts` (enums du domaine, `Carbon`, entiers) — les Resources API portaient déjà `@mixin`, elles guérissent par ricochet. Trois propriétés volontairement NON annotées car les colonnes n'existent pas en base (bugs latents documentés, lot A) : `TravelAdvert::$payment_id`, `TravelAdvert::$published_at`, `TravelLoyaltyAccount::$contact_id`. **(2) Code mort révélé par le typage** : garde `instanceof Carbon` toujours vrai supprimé dans `TravelCancellationPolicyService` (seul écart signalé par PHPStan après annotation). **(3) Baselines RÉGÉNÉRÉES par `--generate-baseline` sous run complet vert** (les entrées résorbées ET les entrées périmées tombent) : `phpstan-modules-baseline.neon` 3 182 → **38 lignes** (530 → 6 entrées — il ne reste que 6 erreurs réelles hors TravelAgency), `phpstan-strict-baseline.neon` 9 963 → **7 178 lignes** (1 660 → 1 196 entrées, 3 267 → 1 512 occurrences au compteur du cliquet), **0 entrée ajoutée, 0 `count:` augmenté** ; `phpstan-baseline.neon` (legacy level max) volontairement inchangée — aucun check CI n'exécute ce scope en entier, une régénération n'y serait pas prouvable. Le niveau PHPStan global est INCHANGÉ (aucune config touchée). Cartographie mise à jour (`docs/qualite/DETTE_PHPSTAN_7655.md` §8, trajectoire chiffrée). **Vérifié localement (PHP 8.4 + composer + PHPStan rootless via `bootstrap-local-php.sh` #7585)** : `php -l` OK sur les 39 fichiers touchés ; **run COMPLET `phpstan-strict.neon` (level 8) `[OK] No errors`** ; **run complet `phpstan-modules.neon` `[OK]`** ; gate diff-scopée de `tests.yml` reproduite (level 8 + 3 baselines) verte sur le module, les 110 fichiers de tests consommant TravelAgency et les 14 fichiers legacy hors module (identique avant/après) ; cliquets `check-phpstan-baseline-delta.sh` + `check-phpstan-baseline-debt.sh guard` rejoués verts. Restent dans #7655 : lots A/C/D/E/F et les points RBAC + Sanctum.
- **fix(front): fin du fallback silencieux vers l'API Render de DEV dans les 4 fronts (Closes #7842, audit externe 2026-09-20).** Un build de **production** sans variable d'environnement pointait silencieusement `front/web`, `front/travel-web` (`backend-url.ts`), `front/web/src/lib/csp.ts` (`DEFAULT_API_ORIGIN` du connect-src) et `front/admin-dashboard` (`api.js`) vers `gestionemployerbackend.onrender.com` — risque de fuite de données réelles vers l'environnement de dev et incident indétectable. Désormais : **(1)** en production (`NODE_ENV=production` / `import.meta.env.PROD`), variable manquante → **throw explicite** avec message actionnable (variable à poser + environnement Vercel/Render/Cloudflare visé) ; une `NEXT_PUBLIC_API_URL` non parsable lève aussi en prod au lieu de retomber sur l'origine de dev dans la CSP ; **(2)** en dev/test, le fallback est conservé mais accompagné d'un `console.warn` explicite ; **(3)** factorisation : `front/web/src/lib/backend-url.ts` et `front/travel-web/src/lib/backend-url.ts` sont désormais STRICTEMENT identiques (fichiers miroirs documentés — la résolution `API_PROXY_TARGET > BACKEND_API_URL > NEXT_PUBLIC_API_URL` et la politique prod/dev vivent dans une seule fonction partagée par les deux copies). Nouvelle suite `front/web/src/lib/__tests__/backend-url.test.ts` (5 cas : chaîne de précédence, fallback dev avec warning, throw prod serveur, throw prod client, prod configurée sans warning). **Vérifié localement** : `tsc --noEmit` 0, `eslint --max-warnings 0` OK, **Jest 142 suites / 1231 tests verts** (dont `proxy-csp.test.ts` inchangé — le comportement dev/test est préservé).
- **fix(bc21): réparation post-merge des deux gardes CI rouges laissées par le lot BC-21 (PR #7732, Refs #7726 #7727 #7728).** **(1) Parité `.env.example` (#1487)** : les 5 clés `env()` lues par `config/restaurantmanager.php` mais absentes de `api/.env.example` sont ajoutées (`RESTAURANT_CARD_ONLINE_SUCCESS_URL`, `RESTAURANT_CARD_ONLINE_CANCEL_URL`, `RESTAURANT_MOBILE_MONEY_PRODUCTION_ENABLED=false`, `RESTAURANT_MOBILE_MONEY_INITIATE_URL`, `RESTAURANT_MOBILE_MONEY_API_KEY`) — valeurs vides/désactivées par défaut, aucun secret. **(2) Couverture OpenAPI (#1473)** : les 9 routes du lot BC-21 sont documentées dans `api/openapi.yaml` — CRUD + activation `/billing/payment-profiles` (#7727, schéma `TenantPaymentProfile`, secrets masqués write-only), `GET/PUT /platform/billing/gateways` + `POST /platform/billing/gateways/{gateway}/test` (#7726, schémas `PlatformPaymentGateway`/`PaymentGatewayTestResult`, masque `sk_live_••••1234`, jamais de clé en clair), `GET /restaurant/payments/configuration` (#7728, indicateurs sans secret). **Vérifié localement** : `check-env-example-parity.sh` ✔ 381 clés / 0 manquante, `check-openapi-route-coverage.py --strict-staleness` exit 0, `redocly lint` « API description is valid », YAML parsé. Aucun changement de code PHP/TS — documentation et parité d'environnement uniquement.
- **feat(retail): handoff livraison BC-26 des commandes en ligne Leopardo Marché — création automatique de la livraison à la confirmation + suivi livreur sur la page publique (Closes #7811, spec `MARKETPLACE_RETAIL_PUBLIC.md` §6).** La confirmation vendeur (`pending → confirmed`, `RetailOnlineOrderService::confirm`) émet désormais l'événement `RetailOnlineOrderConfirmed` (APRÈS commit — jamais de livraison fantôme sur rollback ; classe sous `App\Shared\Events`, payload 100 % scalaires : AUCUN import croisé `Modules/Delivery → Modules/Retail`, règle d'isolation #5584, pattern documenté `Shared\Contracts\Crm`) ; le listener `Delivery\Application\Listeners\CreateRetailOnlineDelivery` crée la livraison BC-26 via `CreateDeliveryAction` — déjà **idempotente** sur (company_id, source, source_reference) : double confirm (422) ou rejeu brut de l'événement → UNE seule livraison. Nouvelle source `DeliverySource::RetailOnline = 'retail_online'` (`source_reference` = référence `WEB-…` de la commande, liste `in:` de `DeliveryStoreRequest` étendue — aucune contrainte CHECK en base à migrer) ; **COD v1 reporté** : `cod_amount_minor` = total serveur de la commande + nouvelle colonne `cod_currency` (ISO 4217, nullable) — migration tenant ALTER dédiée idempotente (`Schema::hasColumn`, SANS FK, miroir `CreatesMvpSchema`), règle « une table, une migration » respectée. Gardes fail-closed du listener : tenant courant = vendeur de l'événement ET feature flag `delivery` actif (vendeur sans module BC-26 → confirmation OK, aucune livraison), toute exception journalisée sans casser la confirmation déjà commitée. **Suivi public enrichi** : `GET /public/market/orders/{reference}` expose `delivery` (statut + horodatages publics UNIQUEMENT — DTO fail-closed `PublicDeliveryStatus`, jamais d'id/COD/données internes) via le nouveau port en LECTURE SEULE `App\Shared\Contracts\Delivery\PublicDeliveryStatusProvider` implémenté et bindé par le module Delivery (`EloquentPublicDeliveryStatusProvider`, requête toujours bornée au company_id) — Retail ne requête JAMAIS les tables Delivery ; la page `/suivi` de `front/marketplace` affiche l'encart « Suivi livreur » (FR, optionnel et défensif : parsing tolérant, statut inconnu → libellé générique, rien n'est affiché sans le champ). Nouvelle suite `RetailDeliveryHandoffTest` (6 cas : confirm → livraison `retail_online` avec COD/dropoff, idempotence double confirm + rejeu d'événement, annulation depuis pending → zéro livraison, vendeur sans feature `delivery` → zéro livraison, isolation tenant croisée, suivi public exposant le statut avec DTO fermé) + cas `retail_online` ajouté au provider de `DeliverySourceContractApiTest` et clé `delivery` actée dans le DTO fermé de `RetailOnlineOrderTest`. **Vérifié localement (PHP 8.4 + PostgreSQL 14)** : `php -l` OK ×17 fichiers, `pint --test` PASS, **PHPUnit vert — `RetailDeliveryHandoffTest` 6/6, `RetailOnlineOrderTest` + `DeliverySourceContractApiTest` 18 tests / 175 assertions**. **Non vérifié localement** : PHPStan strict (workers tués par la sandbox partagée — OOM/timeout même réduit aux fichiers touchés) et le build `front/marketplace` — à confirmer par la CI.
- **feat(marketplace): comptes acheteurs Leopardo Marché — inscription légère, favoris, avis vérifiés & notations publiques (Closes #7814, épic Leopardo Marché BC-17).** La marketplace publique (#7807/#7808) restait 100 % invitée : pas d'historique, pas de favoris, pas d'avis. Livré : **(1) comptes acheteurs PLATEFORME** — la marketplace étant cross-tenant, les acheteurs n'appartiennent à aucun vendeur : 4 nouvelles tables CENTRALES (schéma `public`, une table = une migration, sans FK, idempotentes) `marketplace_buyers` (email unique + mot de passe hashé + nom + téléphone optionnel), `marketplace_buyer_tokens` (**jeton opaque** `mkb_`+64 hex, hash SHA-256 en base, TTL 30 j — pas de Sanctum : ses personal access tokens vivent dans les schémas tenants), `marketplace_favorites`, `marketplace_reviews` ; routes `/api/v1/public/market/account/*` (register/login/logout/me) sous `throttle:shop-public` + bucket strict dédié `market-account-public` (10/min/IP anti-bruteforce), middleware `market.buyer` 401 fail-closed uniforme. **(2) Liaison commande↔compte** — `retail_orders.buyer_id` nullable (migration ALTER tenant idempotente, sans FK cross-schema) : un jeton buyer VALIDE au `POST /public/market/orders` lie la commande (jeton invalide → jamais bloquant, checkout invité inchangé) ; `GET /public/market/account/orders` = historique cross-tenant paginé (DTO public strict : `reference, seller{name,slug,city}, total_minor, currency, fulfillment_status, created_at, tracking_token, items[]` — jamais de company_id). **(3) Favoris** — POST/GET/DELETE `/public/market/account/favorites` : référence produit interne `(company_id, product_id)` résolue CÔTÉ SERVEUR (le client n'envoie que l'id public), liste rendue en DTO produits publics avec filtre fail-closed des produits dépubliés/vendeurs sortis de l'opt-in, ajout/retrait idempotents. **(4) Avis VÉRIFIÉS post-livraison** — `POST /public/market/account/reviews` (note 1..5 + commentaire ≤ 1000) accepté UNIQUEMENT si la commande du buyer contient le produit ET `fulfillment_status = delivered` (422 `ORDER_NOT_DELIVERED`/`PRODUCT_NOT_IN_ORDER`, 404 sur la commande d'un autre buyer) ; anti-abus : UNIQUE (buyer, commande, produit) + throttle strict `market-reviews-public` 5/min/IP (pattern RESTO-902) ; modération `pending|approved|rejected` (auto-approve v1, champ prêt pour la v2) ; `GET /public/market/products/{id}/reviews` (approuvés seuls, paginés, auteur en nom PUBLIC « Prénom I. » — jamais d'email) ; agrégats `rating_avg`/`rating_count` (calcul à la volée sur les avis approuvés) ajoutés aux DTO publics produit ET boutique. **(5) Front `front/marketplace`** — pages `/compte` (connexion/inscription à bascule, jeton en localStorage, hook `useBuyer` sur useSyncExternalStore), `/compte/commandes` (historique + statuts + lien suivi + **formulaire d'avis par article quand la commande est livrée**), `/compte/favoris` ; bouton cœur sur les cartes produit et la fiche ; note moyenne (étoiles accessibles) + section avis sur `/produits/[id]` ; le checkout envoie le jeton buyer s'il existe ; FR, mobile-first, états vides/erreur/chargement, a11y, zéro dépendance ajoutée. Spec `MARKETPLACE_RETAIL_PUBLIC.md` mise à jour (§3.2bis contrat comptes/favoris/avis, §6 : comptes acheteurs déplacés dans le livré). **Vérifié localement (PHP 8.4 + PostgreSQL 14)** : nouvelle suite `RetailMarketBuyerAccountTest` (6 tests / 132 assertions : register/login/me/logout + jeton révoqué/expiré, liaison commande-buyer + invité + jeton invalide, historique cross-tenant 2 vendeurs + isolation entre buyers, favoris CRUD + fail-closed + isolation, avis refusé avant `delivered` puis accepté après + doublon refusé + agrégats produit/boutique) et **`tests/Feature/Retail` 48/48 verts** (les 3 assertions de DTO fermé étendues à `rating_avg`/`rating_count`) ; `npx eslint src --max-warnings 0` + `tsc --noEmit` verts dans `front/marketplace` (après `npm ci`) ; gardes migrations (collisions basename + duplication `Schema::create --strict`), layer-purity, module-isolation et unrouted-controllers vertes ; **PHPStan strict `[OK] No errors`** (run complet mono-worker).
- **feat(onboarding): pop-up d'import du jeu de données de démonstration à la première entrée dans l'espace (Closes #7866, API #7865).** Un responsable (`principal`/`rh`) dont une verticale est active découvre désormais, après l'écran de bienvenue (#7604) et l'entretien de préparation (#7493), une invite « Voulez-vous découvrir votre espace avec des données de démonstration ? » : **Importer** (POST `demo-data/{code}/import` séquentiel pour chaque kit `available`+`not_imported`, état de succès puis rechargement de la surface d'activation — mécanisme #7245/#7322), **Plus tard** (fermeture pour LA session uniquement — aucun appel serveur, aucun localStorage) ou **Non merci** (POST `dismiss`, persisté dans `company.metadata.demo_data`). Nouveau composant `DemoDataPrompt` (front/web, pattern WelcomeScreen/SetupInterview : Leo, overlay non bloquant, Échap = Plus tard, erreur d'import inline non bloquante) avec pré-garde `shouldShowDemoDataPrompt` (miroir RBAC serveur + flags verticaux `/auth/me` + metadata) — le serveur reste la SOURCE DE VÉRITÉ : rien n'est rendu sans kit proposable au `GET /demo-data`. i18n `demoDataPrompt.*` ×4 locales (fr/en/ar/tr, `shared/i18n` + catalogues régénérés par les 3 syncs). **Vérifié localement** : `eslint --max-warnings 0` 0, `tsc --noEmit` 0, Jest onboarding 5 suites / 41 tests verts (dont 10 nouveaux `DemoDataPrompt`), `check:mojibake` OK, garde PA2-I18N-014 verte, `I18N_VALIDATION_OK (4 locales)` + parité ×4 verte.
- **feat(retail): Leopardo Marché — marketplace grand public multi-vendeurs BC-17 phase 2 : API publique cross-tenant, checkout invité, web client dédié et espace vendeur (Closes #7807, #7808, #7809, #7810 — épic #7806, spec `docs/specifications/MARKETPLACE_RETAIL_PUBLIC.md`).** Les fondations vendeur (#7718) laissaient le canal `online` de `RetailOrderSource` réservé sans flux : le grand public ne pouvait pas acheter en ligne les produits des boutiques Retail. **(1) API publique** `/api/v1/public/market/*` (throttle `shop-public`, AUCUNE auth, pattern BC-27/BC-28) : recherche cross-tenant bornée aux vendeurs opt-in (nouvelle table `retail_online_settings`, `enabled` fail-closed, 1 ligne par company ; produits `published` ET `online_visible` — nouvelle colonne opt-in — uniquement ; agrégation `withoutGlobalScope` + `whereIn(company_id, éligibles)`, pattern marketplace travel #7737), fiches produit et annuaire boutiques en DTO strict (jamais de `company_id`/stock chiffré/marges — seul `available: bool`), filtres q/boutique/catégorie/prix/tri, pagination ≤ 50. **(2) Checkout invité** `POST /public/market/orders` : 1 commande = 1 vendeur, prix RELUS en base, totaux serveur en minor units, référence `WEB-…`, `tracking_token` 64 hex, idempotence par clé (rejeu → 200 même payload) ; suivi public `GET /public/market/orders/{reference}?token=` 404 fail-closed. **(3) Gestion vendeur** `/retail/online/*` (pile tenant + `module.retail`) : réglages boutique (create-or-update, `RetailOnlineSettingsPolicy`), `publish-online`/`unpublish-online` par produit, commandes web avec machine d'états `pending → confirmed → ready → shipped → delivered` (+ `cancelled`) — transitions invalides → 422 `INVALID_TRANSITION`, confirmation = décrément stock par `RetailStockService` (mouvements `sale`, seule voie d'écriture #7673), annulation post-confirmation = mouvements `return`. Migrations tenant idempotentes sans FK (miroirs `CreatesMvpSchema`, garde #5443), OpenAPI +14 opérations, 14 tests Feature (opt-in, isolation tenant, DTO fermés, idempotence, transitions avec stock, RBAC). **(4) Web client dédié `front/marketplace`** (nouvelle app Next.js App Router indépendante, TS strict, Tailwind 4, FR, direct-to-API `NEXT_PUBLIC_MARKET_API_BASE`) : accueil, catalogue (recherche/filtres/tri/pagination sans JS requis), fiches, boutiques, panier multi-vendeurs localStorage, checkout invité (1 POST par boutique, clés d'idempotence persistées, succès partiel géré), confirmation avec jetons, suivi à timeline — déployé en projet Vercel dédié `https://leopardo-marche.vercel.app` (#7815, `docs/ops/DEPLOYMENT_URLS.md`). **(5) Espace vendeur** `/commerce/boutique` dans `front/web` (patterns #7675) : réglages, bascule « En ligne » par produit, commandes web avec actions — machine d'états pure testée (+6 tests Jest), i18n ×4 locales synchronisée (`I18N_VALIDATION_OK`). **Vérifié localement** : `front/marketplace` build vert + ESLint 0 warning ; `front/web` ESLint 0, **Jest 135 suites / 1201 tests verts**, `check:mojibake` OK, build vert ; parité i18n ×4 verte ; OpenAPI route-coverage 0 drift, YAML valide. **Non vérifié localement (PHP/PostgreSQL absents de la sandbox)** : PHPStan strict, Pint et les 2 suites Feature Retail — à confirmer par la CI.
- **feat(restaurant): encaissement réel branché sur les profils de paiement du tenant — carte en ligne Stripe sur les clés DU restaurateur, mobile money production feature-flaggé, fail-closed propre côté commande publique (Closes #7728, BC-25×BC-21, dépend de #7727/PR #7732).** Le POS Restaurant enregistrait cash/carte de façon déclarative et le mobile money était sandbox uniquement ; aucune commande publique ne pouvait être encaissée RÉELLEMENT sur le compte du restaurateur. **(1) Contrat partagé étendu** (`TenantPaymentProfileResolverInterface`, garde d'isolation #5584 — jamais d'import direct de Modules/Billing) : `stripeCredentialsForCompany()` et `activeMobileMoneyProfileForCompany()` résolvent les profils ACTIFS d'une compagnie DONNÉE — nécessaire aux surfaces publiques (le tenant vient du lien signé, pas du contexte courant) ; requêtes hors scope global mais TOUJOURS filtrées par la compagnie désignée, table absente → null (fail-closed, jamais de 500). **(2) Nouveau gateway `card_online`** (`CardOnlinePaymentGateway`, registre RESTO-406) : Stripe Checkout Session REST (pattern Accounting #5272) sur les **clés PROPRES du tenant** (profil `stripe_keys` actif — AUCUN fallback plateforme sur la surface publique restaurant), métadonnées traçant company_id + profil, verify (payment_status) et refund (payment_intent) inclus ; enum `PaymentProvider` + `InitiatePaymentResult.checkoutUrl` étendus. **(3) Mobile money production** : hors sandbox (comportement historique inchangé par défaut), l'initiation exige le feature flag `restaurantmanager.mobile_money.production.enabled` + endpoint provider en env (PVIT/Orange Money) + **profil `mobile_money` ACTIF du tenant** (opérateur + numéro d'encaissement du restaurateur) — sinon fail-closed ; la confirmation reste portée par le callback HMAC RESTO-407, aucun secret en dur. **(4) Commande publique** (`RestaurantPublicOrderController::pay`) : provider par défaut dérivé de la configuration du restaurateur (carte en ligne prioritaire), réponse enrichie de `checkout_url`, providers sur place (cash/carte terminal) refusés sur la surface publique ; **sans profil actif → 422 `online_payment_not_configured`** (message utilisateur ×4 locales `errors.RESTAURANT_ONLINE_PAYMENT_NOT_CONFIGURED`) **+ `fallback: pay_on_site`** — jamais un 500 (renderer dédié `PaymentGatewayException` dans bootstrap/app.php, paiement avorté tracé `failed`, aucun pending fantôme) ; le menu public expose `payments.online_providers` + `pay_on_site` pour informer AVANT le paiement. Les paiements publics portent `pos_session_id` — les sessions de caisse agrègent les encaissements en ligne réels comme avant. **(5) Écran restaurateur** : `GET /restaurant/payments/configuration` (auth tenant, aucun secret — uniquement des indicateurs) + page front/web `/restaurant/payments` (tuile hub) : état carte en ligne / mobile money (sandbox vs production, profil actif) / paiement sur place, lien vers Réglages → Encaissements (#7727) ; i18n ×4 locales (`restaurant.payments.*`) propagée par les 3 syncs. **Vérifié localement (PHP 8.4 + PostgreSQL rootless dans la sandbox)** : nouvelle suite Feature `RestaurantPaymentRoutingTest` **8 tests verts** (checkout sur la clé Stripe DU TENANT + traçage du profil via Http::fake, défaut card_online avec profil actif, 422 propre + fallback sans profil — 3 chemins, aucun paiement fantôme, cash/card refusés en public, sandbox mobile money préservé, menu public, écran restaurateur configuré/non configuré) ; suites existantes vertes : `RestaurantPaymentGatewayTest` + `RestaurantPublicOrderTest` + `RestaurantKioskTest` (23 tests), `TenantPaymentProfileApiTest` (6), `AccountingOnlinePaymentTest` (15) ; échecs préexistants de l'environnement local (RestaurantPublicShopTest & co) reproduits à L'IDENTIQUE sans mes changements (stash) — hors périmètre ; `php -l` OK ×19, module-isolation 0 nouvel import, dépendances BC conformes (119 arêtes), unrouted-controllers OK ; front/web `tsc --noEmit` 0 + `eslint src --max-warnings=0` OK + Jest restaurant 26 tests verts. **PR empilée sur `bc/bc21-paiements-encaissement`** (le PM reciblera sur main après merge de #7732). Hors périmètre assumé : Stripe Connect (`stripe_account_id` réservé), webhook Stripe côté restaurant (la vérification passe par `verify` à la demande), surface `/restaurant/public/shop` (jeton boutique) qui garde son flux cash/mobile money historique.
- **feat(billing): lot BC-21 paiements — configuration des passerelles PSP depuis l'admin plateforme (stockage chiffré BDD + fallback env) et profils de paiement du tenant avec routage des encaissements (Closes #7726, Closes #7727).** **(1) #7726 — config PSP plateforme** : nouvelle table publique `payment_gateway_settings` (une ligne par passerelle stripe|chargily, `mode` test|live, `config` non secrète, `secrets` chiffrés au repos via cast `encrypted:array`, `is_active`, audit `updated_by`) ; `GatewaySettingsService` (contrat partagé `App\Shared\Contracts\Payments\PaymentGatewayConfigProviderInterface`, lié par `BillingServiceProvider`) applique la précédence **BDD → fallback env champ par champ** (zéro rupture sans ligne BDD ; cache applicatif à TTL court qui ne stocke que le CIPHERTEXT des secrets, invalidé à chaque écriture admin — changement de clé effectif sans redéploiement) ; `StripeService`, `ChargilyService` (Billing, dont la vérification de signature des webhooks) et `StripePaymentGateway`/`ChargilyPaymentGateway` (Accounting) consomment ce service au lieu de `config()` direct — via le contrat partagé, **aucun nouvel import cross-module** (garde #5584 verte). Endpoints admin `GET/PUT /platform/billing/gateways` + `POST /platform/billing/gateways/{gateway}/test` (ping Stripe `/v1/account`, Chargily `/api/v2/balance`), permission `platform.permission:billing.manage`, **secrets write-only jamais renvoyés en clair** (masque `sk_live_••••1234`, valeur vide au PUT = clé conservée), écriture auditée SANS valeur de secret (seuls les NOMS des champs modifiés). Écran Vue 3 « Passerelles de paiement » (`/settings/payment-gateways`, groupe Paramètres à côté des offres #7430) : formulaire par gateway, toggle test/live, bouton « Tester la connexion », badge configuré-BDD / configuré-env / non-configuré, i18n ×4 locales. **(2) #7727 — profils de paiement du tenant** : table tenant `tenant_payment_profiles` (`company_id` uuid indexé sans FK publique, type stripe_keys|bank_account|mobile_money, statut draft/verified/active, `is_default`, `details` non secrets, `secrets` chiffrés `encrypted:array`, colonne `stripe_account_id` **réservée** pour Stripe Connect — lot ultérieur), modèle `TenantPaymentProfile` sous `BelongsToCompany` (isolation fail-closed #3727) ; Actions `SaveTenantPaymentProfile` (type immuable, secrets write-only allowlistés par type, retour en draft quand une clé change) et `ActivateTenantPaymentProfile` (un seul profil actif par type, refus d'activer un profil Stripe sans clé) ; endpoints tenant `GET/POST/PUT/DELETE /billing/payment-profiles` + `/{id}/activate` réservés au rôle `principal`. **Routage des encaissements** : `PaymentGatewayFactory` (Accounting) résout d'abord le profil `stripe_keys` ACTIF du tenant courant via le contrat partagé `TenantPaymentProfileResolverInterface` — le checkout de la facture client part avec **les clés Stripe DU TENANT** et trace `metadata[payment_profile_id]` ; sans profil, comportement plateforme inchangé (routage pays ADR-0017) ; la vérification webhook Stripe essaie le secret plateforme puis le webhook secret du profil actif de la compagnie désignée par les métadonnées (le company_id ne sert qu'à SÉLECTIONNER le secret candidat — la signature HMAC reste l'unique preuve, fail-closed). UI client `front/web` : page « Encaissements » (`/settings/encaissements`, entrée de nav à côté de « Collaborateurs et rôles » #7555, réservée au principal), liste des profils avec badge statut/défaut, formulaires par type, activation, suppression avec confirmation, avertissement sécurité, replis FR centralisés (`payment-profiles.ts`, garde PA2-I18N-014 verte). **Tests** : `PlatformPaymentGatewayAdminApiTest` (7 cas — source env + masquage, chiffrement au repos + précédence BDD, write-only, fallback env intact, permission 403, webhook secret BDD avec contre-preuve env, ping sans fuite de clé) et `TenantPaymentProfileApiTest` (6 cas — CRUD/activation avec masques + chiffrement, write-only + suppression, 403 non-principal, isolation cross-tenant liste + 404 accès direct, routage checkout avec clé tenant + traçage du profil via `Http::fake`, fallback clés plateforme sans profil). **Vérifié localement** : gardes migrations vertes (`check-migration-basename-collisions.sh` OK, `check-duplicate-schema-create.py --strict --fail-on-duplicate` 0 duplication), `check-module-isolation.sh` 0 nouvel import croisé, `php -l` OK sur tous les fichiers PHP touchés, front/web `tsc --noEmit` 0 + `eslint --max-warnings 0` OK + `check:mojibake` OK, admin-dashboard `eslint` OK, garde i18n `check-i18n-diff.js` verte. **PHPUnit/PHPStan/Pint non exécutés localement (ni vendor Composer ni PostgreSQL dans la sandbox d'agent) — à confirmer par la CI.** **Hors périmètre, assumé** : bloc « Coordonnées de règlement » (IBAN/mobile money) sur le PDF des factures Accounting et bascule du checkout front `route.ts` vers l'API — pointés dans la PR comme suites de #7726/#7727.
- **chore(ops): drill de restauration — les binaires PostgreSQL 18 passent en tête de PATH sur le runner (Refs #7657, suite du correctif client 18).** Le drill re-déclenché après installation de `postgresql-client-18` (run 35487411111) échouait encore — proprement cette fois, sur la nouvelle garde de parité de version — car `/usr/bin/pg_dump` est le wrapper `pg_wrapper` de postgresql-common, qui sélectionne le cluster 16 préinstallé sur l'image du runner plutôt que la version la plus récente. Les deux jobs (backup quotidien, drill mensuel) ajoutent `/usr/lib/postgresql/18/bin` en tête de PATH via `$GITHUB_PATH` et vérifient la version du binaire 18 dans l'étape d'installation.
- **ci(travel): pipeline GitHub Actions prêt-à-secrets pour déployer `front/travel-web` sur Vercel — preview (dev) sur PR, production sur push main (Refs #7740, épic #7736 — PR empilée sur #7781).** Le déploiement initial (#7830) et le raccordement des projets (#7835) étaient MANUELS (Vercel CLI/API) : aucun chemin CI ne redéployait travel-web. Nouveau workflow `.github/workflows/travel-web-deploy.yml`, calqué sur les chemins Vercel/Cloudflare existants : PR touchant `front/travel-web/**` → déploiement **preview** sur le projet dev `leopardo-travel` (compte africanovatech, URL unique du run health-checkée — 401/403 Vercel Authentication accepté comme sain) ; push `main` → **production** sur `leopardo-travel-prod` (compte ibrahimkoubaye, `vercel pull → build → deploy --prebuilt --prod` depuis la racine — rootDirectory appliqué une seule fois, fix #6810 — + healthcheck 30×10 s sur le domaine promu) ; `workflow_dispatch` preview/production. **Prêt-à-secrets** : 6 secrets `VERCEL_TRAVEL_{DEV,PROD}_{TOKEN,ORG_ID,PROJECT_ID}` (2 jeux car 2 comptes — P07), non lisibles dans un `if:` → détour env de job (pattern exact `deploy-admin-dashboard.yml`) et skip EXPLICITE `::warning::` + step summary si absents (règle AGENTS.md, jamais de skip silencieux) — aucun secret dans le repo, la pose est une action propriétaire. Runbook `docs/ops/travel-web-vercel.md` (création tokens, récupération orgId/projectId sans rien committer, env vars runtime `BACKEND_API_URL`/`NEXT_PUBLIC_SITE_URL` par volet, domaines + procédure domaine custom, checklist propriétaire, dépannage) ; `docs/CI_CD_SECRETS.md` complété (garde de parité #7271 verte — au passage, ligne manquante `BACKUP_AGE_IDENTITY` du drill #7657 ajoutée : la garde était rouge sur toute PR workflows). `front/travel-web/vercel.json` vérifié cohérent (framework nextjs, région cdg1, ignoreCommand scopé) — inchangé. L'intégration à la promotion par tag (`deploy-prod.yml`) reste tracée en suivi dans #7740/#7736. **Vérifié localement** : `actionlint` 0 erreur sur le workflow, `bash dev-hub/tools/check-ci-secrets-doc.sh` vert (parité secrets↔doc), YAML parsé.
- **chore(ops): drill de restauration — client PostgreSQL 18 (PGDG) sur le runner + fail-fast sur échec pg_dump (Refs #7657).** Le drill du 2026-09-20 (run 35487018909) échouait sur « pg_dump: server version mismatch » : le serveur Neon est en 18.6 alors que le paquet `postgresql-client` d'Ubuntu 24.04 fournit pg_dump 16.15 — le dump faisait 0 octet et les 5 tables ressortaient MISSING. Correctifs : **(1)** les jobs backup et drill de `database-backup.yml` installent `postgresql-client-18` depuis le dépôt PGDG (`apt.postgresql.org.sh`) ; **(2)** `backup_drill.sh` cesse d'avaler l'échec de pg_dump — le code de sortie de la commande lancée via `\!` psql (non propagé par ON_ERROR_STOP) est capturé dans un fichier et vérifié, un dump de 0 octet fait échouer le drill avant chiffrement/restauration, et une garde de parité de version majeure client/serveur échoue tôt avec un message actionnable (`installer postgresql-client-N`).
- **chore(ops): drill de restauration — tolérance à la dérive de schéma de la liste des tables critiques (Refs #7657).** Le drill échouait au premier `COUNT(*)` car `shared_tenants.user_invitations` n'existe plus en production (liste codée en dur dans `backup_drill.sh`). Les counts sont désormais générés depuis les tables réellement présentes (`to_regclass`), warning sonore par table dérivée, échec fail-loud si moins de 3 tables critiques restent vérifiables.
- **ops(queue): worker Render dédié queue+scheduler — blueprints alignés sur la réalité, provisionnement API tenté et BLOQUÉ billing, drain web désormais débrayable (Refs #7649).** Provisionnement des background workers tenté le 2026-09-19 via l'API Render (POST /v1/services, type `background_worker`, plan starter, dev ET prod) : refus **402 « Payment information is required »** sur les deux workspaces (`tea-da9svvpsrm7s73d8o2j0` prod, `tea-d7dq20naqgkc73fqc94g` dev) — aucun moyen de paiement enregistré, et les workers sont inéligibles au tier gratuit ; l'ajout d'une carte est une décision propriétaire (dashboard → Billing). En attendant, tout est prêt pour une bascule en un geste : **(1) blueprints** — `render.yaml` (dev) et `render.prod.yaml` (prod, worker dé-commenté) déclarent UN worker starter par tier (`leopardo-queue-worker[-prod]`) portant queue:work ET le scheduler dans un seul service (`sh -c 'php artisan schedule:work & exec php artisan queue:work …'` — l'ancien second service scheduler-prod commenté est fusionné, ~7 $/mois au lieu de 14), `RUN_MIGRATIONS=false` (#6558), commentaires « n'existe pas dans le workspace » réalignés sur le constat 402 daté ; **(2) drain web débrayable** — les boucles intérimaires de `docker-entrypoint.sh` (respawn queue:work #7041 et scheduler #7649) sont gatées par `WEB_QUEUE_DRAIN` / `WEB_SCHEDULER_LOOP` (défaut `true` : comportement de prod inchangé tant que le worker n'existe pas ; à passer à `false` sur le web service dès le worker provisionné — évite double consommateur sur la table jobs et double schéduleur sans `onOneServer()`). Le volet workflow était déjà soldé par #7694 (`queue-worker-fallback.yml` supprimé, `queue-supervision.yml` = sonde HTTP pure sans credentials — garde `check-queue-strategy-coherence.sh` verte sur ces changements). **Vérifié** : `bash -n`/`sh -n` sur l'entrypoint, YAML des deux blueprints parsé, garde cohérence queue verte. Restent dans #7649 (suivi PR) : ajout carte + provisionnement (blueprint sync ou API), flip des deux flags, migration `QUEUE_CONNECTION` → Redis, généralisation `EnsureTenantContext` sur ~36 jobs.
- **fix(travel): la CI PHPStan de la pile #7736 repasse au vert — deux erreurs d'analyse statique introduites par le lot comptes clients #7739 (PR #7795) sont corrigées (Refs #7739).** Le check requis « PHPStan — Modules Architecture » bloquait la PR #7781 (auto-merge armé) : `instanceof.alwaysTrue` sur le `PersonalAccessToken` au logout de `TravelCustomerAccountController` (le token courant est toujours un `PersonalAccessToken` sous `auth:travel_customer` — la garde conditionnelle est supprimée, révocation directe) ; et en strict niveau 8, `property.notFound` sur `TravelBooking::$customer_account_id` dans `TravelMarketplaceController` (colonne non annotée `@property` — accès via `getAttribute()`). Aucun changement de comportement runtime, aucune baseline touchée. **Vérifié localement (PHP 8.4 rootless #7585)** : `php -l` OK, `pint --test` PASS ×2, PHPStan `phpstan-modules.neon` **[OK] No errors** (config complète) et `phpstan-strict.neon` **[OK] No errors** sur `app/Modules/TravelAgency`.
- **fix(travel): la clé `model` du provider `travel_customers` est reposée au RUNTIME — le driver custom sans `model` (#7739) fatalait à chaque requête authentifiée du guard `travel_customer` (#7739/#7781).** Le passage au driver custom `travel_customer_accounts` supprimait la clé `auth.providers.travel_customers.model`… que `Sanctum\Guard::hasValidProvider()` lit à CHAQUE validation de token : `$tokenable instanceof null` → « Class name must be a valid object or a string » (Guard.php:153) — me/logout/« mes réservations » répondaient 500 (reproduit en local : 3/6 tests `TravelCustomerAccountApiTest` en Error). La clé est posée par `config()->set()` dans `TravelAgencyServiceProvider::boot()`, jamais statiquement : sous analyse le guard est masqué (`LEOPARDO_STATIC_ANALYSIS`) et Larastan ne parcourt que les providers référencés par un guard — l'union de `request->user()` et la baseline strict restent intactes. **Vérifié localement (PHP 8.4 + PostgreSQL rootless)** : `TravelCustomerAccountApiTest` **6/6 verts** (49 assertions), `TravelMarketplaceApiTest` 6/6, PHPStan strict **[OK] No errors** sur le provider, les middlewares/AppServiceProvider de l'union et les contrôleurs travel, `pint --test` PASS, guard+provider complets au runtime (`config:show`) et masqués sous analyse (vérification empirique du boot).
- **fix(test): `TravelCustomerAccountApiTest` repasse au vert — reset du cache de guard entre les requêtes d'un même test Feature (#7739/#7781).** Les 6 tests du lot comptes clients #7739 n'avaient JAMAIS tourné en CI (runs Backend tous annulés sur la branche) et 3 échouaient : le `RequestGuard` sanctum CACHE l'utilisateur résolu entre deux requêtes du même test — après `logout` le client révoqué restait authentifié (200 au lieu de 401), la requête du voisin héritait du client précédent malgré un bearer différent (2 réservations au lieu de 1), et le checkout invité se voyait rattacher la réservation au client encore en cache. Correctif test-only : `app('auth')->forgetGuards()` aux 3 points de bascule d'identité (pattern repo établi, cf. `TravelAdvertDestroyTest`). **Vérifié localement (PHP 8.4 + PostgreSQL rootless, base fraîche entièrement re-migrée)** : `TravelCustomerAccountApiTest` **6 tests / 49 assertions verts**, `TravelMarketplaceApiTest` 6/6 verts, `php -l` et `pint --test` OK ; PHPStan strict et modules rejoués en local en complément du masquage `LEOPARDO_STATIC_ANALYSIS` déjà porté par la branche (PR #7781).
- **fix(travel): la CI PHPStan de la pile #7736 repasse au vert — les erreurs d'analyse statique introduites par le lot comptes clients #7739 (PR #7795) sont corrigées, zéro baseline touchée (Refs #7739).** Le check requis « PHPStan — Strict » (71 erreurs) et « PHPStan — Modules Architecture » bloquaient la PR #7781 (auto-merge armé). **(1) La cause principale était systémique** : la clé `model` du nouveau provider `travel_customers` dans `config/auth.php` ajoutait `TravelCustomerAccount` à l'union de type de `request->user()`/`Auth::user()` que Larastan calcule pour TOUTE l'app — ~68 entrées de `phpstan-strict-baseline.neon` (HR, Payroll, Billing, Recruitment, Restaurant… `isManager()`, `$company_id`, …) ne matchaient plus (dérive de message) et ressortaient comme erreurs neuves. Le provider devient un **driver custom** (`Auth::provider('travel_customer_accounts', …)` dans `TravelAgencyServiceProvider::boot()`, `EloquentUserProvider` standard sur le même modèle) : plus de clé `model` statique, l'union redevient celle d'avant #7795, runtime strictement identique (guard Sanctum dédié inchangé). **(2)** logout client : la garde conditionnelle sur `currentAccessToken()` était toujours-vraie pour PHPStan (`instanceof.alwaysTrue`/`if.alwaysTrue`) — révocation directe, le token est garanti sous `auth:travel_customer`. **(3)** `TravelBooking::$customer_account_id` n'est pas annotée `@property` (modèle à accès magique) — accès via `getAttribute()` dans `TravelMarketplaceController` et le test Feature. **Vérifié localement (PHP 8.4 rootless #7585)** : `php -l` OK ×5, `pint --test` PASS ×5, PHPStan `phpstan-modules.neon` **[OK] No errors** (config complète) et `phpstan-strict.neon` **[OK]** sur `app/Modules/TravelAgency`, `tests/Feature/Travel`, `AppServiceProvider` et les FormRequests HR impactés (le strict full-repo dépasse la sandbox — à confirmer par le check requis). PHPUnit non exécuté localement (pas de PostgreSQL) — comportement runtime inchangé, suite `TravelCustomerAccountApiTest` à confirmer par la CI.
- **feat(travel): comptes clients grand public sur le site marketplace — inscription, connexion, « mes réservations » cross-agences (Closes #7739, épic #7736 — PR empilée sur #7738/#7781).** Le grand public peut désormais créer un compte sur `front/travel-web` ; le checkout invité reste possible. **Backend** : nouvelle table PLATEFORME `travel_customer_accounts` (migration `public/`, pas de `company_id` — un client n'appartient à aucun tenant, même pattern pré-tenant que `marketing_leads`/`acquisition_funnel_events` #7496) + colonne nue `travel_bookings.customer_account_id` (tenant, nullable, indexée, SANS FK cross-schéma — constitution §II, guards `schemaTableExists`/`schemaHasColumn`) ; guard Sanctum **DÉDIÉ** `travel_customer` (provider `travel_customers` — jamais le guard employés ni super-admin) ; `TravelCustomerAccountController` (`POST /public/travel/marketplace/account/register|login|logout`, `GET …/me|/bookings`) — register/login sous `throttle:auth-sensitive` (e-mail + IP) **+ verrouillage 15 min après 5 échecs** (pattern #6563), mot de passe HASHÉ (`Password::min(12)->numbers()` + `NotCommonPassword`), 401 indifférencié. **Rattachement** : à l'inscription, les réservations marketplace existantes portant le même e-mail de contact (insensible à la casse, encore orphelines) sont revendiquées par le compte (`claimed_bookings`) ; connecté, `storeBooking` rattache la réservation à la création (la vérification par lien e-mail est un lot ultérieur de l'épic, documenté). **« Mes réservations »** : liste paginée cross-agences STRICTEMENT bornée par `customer_account_id` (isolation client), charge utile publique — référence, statut, trajet/villes, numéros de billets (JAMAIS le code de validation, secret de possession #7395) et nom public d'agence, aucun identifiant tenant. **Frontend `travel-web`** : pages `/account/login`, `/account/register`, `/account` (profil + tableau de bord réservations + déconnexion), lien d'en-tête Connexion/Mon compte, `AccountProvider` (token en localStorage, validé au montage via `/me`, purgé si révoqué), **pré-remplissage checkout** (e-mail/téléphone du compte, sans écraser la saisie) et token relayé à la création de réservation ; le proxy same-origin n'autorise le header `Authorization` QUE sur `account/*` + `bookings` (fail-closed conservé) ; i18n fr/en (`account.*`). openapi : +5 paths. Nouvelle suite `TravelCustomerAccountApiTest` (6 tests : hash + revendication par e-mail cross-agences ×2 agences, verrouillage login, me/logout guard dédié, isolation stricte entre deux clients, rattachement à la création + invité non rattaché, mots de passe faibles rejetés). **Vérifié localement** : `php -l` OK (9 fichiers), `tsc --noEmit` 0, `eslint src --max-warnings=0` 0, gardes migrations vertes (basename/préfixes, conventions BC, schéma tenant #1613, duplication #7452), `check-openapi-route-coverage.py` exit 0. **Non vérifié localement (ni composer ni PostgreSQL dans la sandbox)** : PHPStan strict et PHPUnit reposent sur la CI.
- **feat(travel): `front/travel-web` — site public Next.js de réservation de billets inter-agences : recherche, choix de sièges, checkout invité, confirmation et suivi par référence (Fixes #7738, épic #7736).** Le lot frontend de la marketplace (#7737) : nouvelle app autonome `front/travel-web` (Next.js 16 App Router, TypeScript strict, Tailwind v4 — propre `package-lock.json`, `turbopack.root`/`outputFileTracingRoot` épinglés comme front/web #7305, AUCUN impact sur les workspaces existants ni sur les paths filters CI). **Parcours livré** : accueil SSR avec recherche (villes desservies via autocomplete `marketplace/cities`, date), résultats SSR agrégés toutes agences (`marketplace/trips` — horaires, agence, prix « à partir de », sièges restants, état vide/panne backend gérés), page trajet avec **plan de sièges cliquable** (sièges libres du payload `marketplace/trips/{id}`, attribution auto possible), **checkout invité** (passagers adult/child/infant × classes tarifaires, contact e-mail/téléphone, consentement notifications, **clé d'idempotence unique par session de checkout** — un rejeu réseau ne double jamais la réservation), **confirmation** avec référence mise en avant + échéance de paiement, et page **« Retrouver ma réservation »** (référence + code de validation e-billet #7395 : statut, billets, PDF via URL signée, annulation en ligne motivée). **Checkout « à payer à l'agence » assumé** : `marketplace/payments/initiate` existe mais les providers en ligne dépendent de la config de chaque agence — le paiement en ligne côté front est le lot #7739/#7740 (décision documentée, la réservation expire côté backend si impayée). **Sécurité/config** : proxy same-origin `/api/v1/[...path]` restreint par allowlist fail-closed à `public/travel/*` (jamais de relais vers l'API authentifiée, headers Authorization/Cookie supprimés, panne backend → 502 JSON, pattern #3523), cible configurable `API_PROXY_TARGET` > `BACKEND_API_URL` > `NEXT_PUBLIC_API_URL` > défaut Render dev (chaîne #1701) — **aucun secret dans le repo** (`.env.local.example` sans valeur sensible). i18n **fr/en** maison (fr défaut, cookie `travel_lang` lu au SSR — `<html lang>` correct sans mismatch d'hydratation ; ar/tr = lot ultérieur de l'épic), SEO de base (metadata/OpenGraph par page, robots.txt — confirmation noindex —, sitemap), design sobre responsive mobile-first, `vercel.json` pattern front/web (ignoreCommand scopé `:/front/travel-web`, headers sécurité, région cdg1) pour les projets `leopardo-travel`/`leopardo-travel-prod` (DOMAINS.md mis à jour, garde canonique verte) ; scripts racine `travel:dev|build|lint` ajoutés sans toucher aux agrégats. **Vérifié localement** : `tsc --noEmit` 0, `eslint src --max-warnings 0` 0, `next build` OK, smoke `next start` (accueil SSR 200, proxy 404 fail-closed sur `/auth/me`, relais backend fonctionnel — les routes marketplace répondront dès le redéploiement du backend dev incluant #7737).
- **ci(travel): raccordement Vercel de `front/travel-web` finalisé côté projets — framework/rootDirectory posés, lien git prod, hygiène `.vercel/` (Fixes #7740).** Suite du déploiement initial #7830 : les deux projets Vercel (`leopardo-travel` dev / `leopardo-travel-prod` prod) sont désormais configurés `framework=nextjs` + `rootDirectory=front/travel-web` via l'API ; le projet prod est LIÉ au repo GitHub `kitokoh/leopardo-hr` (branche de production `main` — déploiement production automatique à chaque merge), le lien git du projet dev est bloqué par une action propriétaire (« Login Connection » GitHub absente du compte Vercel africanovatech — documenté, déploiement dev via Vercel CLI en attendant). Hygiène : `front/travel-web/.vercel/project.json` (IDs projet/orga, committé par erreur dans #7830) est retiré du dépôt et `.vercel/` rejoint le `.gitignore` racine. `docs/ops/DEPLOYMENT_URLS.md` et `docs/ops/DOMAINS.md` alignés sur l'état réel (conformité P07 dev/prod séparés, variables d'env listées SANS valeurs secrètes) ; l'intégration au pipeline `deploy-prod.yml` reste tracée en suivi (issue #7740 / épic #7736).
- **chore(ops): drill de restauration backup exécutable — clé `age` matérialisée depuis le secret `BACKUP_AGE_IDENTITY` + `CONFIRM_RESTORE=YES` (Refs #7657).** `backup_drill.sh` exige un *chemin* de fichier d'identité age et une confirmation destructive explicite ; le workflow passait un secret-contenu comme chemin et omettait la confirmation — le drill mensuel était donc inexécutable même secrets posés. La clé est écrite en fichier éphémère du runner (umask 077) et la cible reste la base scratch dédiée `leopardo_drill`.
- **ci(travel): `front/travel-web` déployé sur les projets Vercel provisionnés — `leopardo-travel` (dev) et `leopardo-travel-prod` (prod) servent désormais la vraie app (Closes #7740, décision #6918, épic #7736).** Les deux domaines remplaçaient des pages de validation hors repo : déploiement initial effectué via Vercel CLI depuis `front/travel-web` (contenu PR #7781), HTTP 200 vérifié sur les deux URLs le 2026-09-19 (titre SSR correct, proxy same-origin `public/travel/*` fonctionnel). Variables d'env configurées CÔTÉ VERCEL, aucun secret dans le repo : dev → `BACKEND_API_URL=https://gestionemployerbackend.onrender.com/api/v1`, prod → `BACKEND_API_URL=https://leopardo-prod.onrender.com/api/v1` (+ `NEXT_PUBLIC_SITE_URL` par volet), conformité P07 dev/prod séparés (comptes africanovatech / ibrahimkoubaye). `docs/ops/DOMAINS.md` et `docs/ops/DEPLOYMENT_URLS.md` mis à jour (nouvelle section Travel : projets, env, procédure) ; les endpoints `public/travel/marketplace/*` répondront 200 après merge du backend #7737/#7781 et redéploiement Render (proxy déjà vérifié) ; intégration `deploy-prod.yml` tracée en suivi dans l'issue.
### Changed
- **ci: désaturation CI étape 1 — suppression du workflow mort `deploy-staging.yml` (aucun run depuis 2026-09-13, aucun staging provisionné #7256), désactivation justifiée du déclencheur `workflow_run` de `e2e-staging.yml` (Playwright ne martelle plus la prod free-tier à chaque push main ; dispatch manuel conservé), cron `ci-observability.yml` `*/10` → horaire (144 → 24 runs/jour, rapport non bloquant), `queue-supervision.yml` `*/5` volontairement conservé (DoD #5282 détection < 15 min) ; plan complet par étapes (crons restants, fusion des guards à deux têtes, revue des ~30 guards, matrice risque/gain) documenté dans `docs/infra/02_alignement/CI_SATURATION.md` (Closes #7846).**

### Added
- **feat(billing): crédits IA achetables — ledger, checkout one-shot, décompte réel, UI facturation (Closes #7764, spec MISSION_ESPACE_CLIENT §3.4, `[FREEZE-EXCEPTION]` fondateur).** Nouveau grand livre tenant `ai_credit_ledger` (mouvements signés en tokens : `purchase`/`consumption`/`adjustment`, `company_id` obligatoire, index unique **partiel** sur la référence des achats = idempotence au niveau base) + compteur mensuel **persistant** `ai_usage_counters` (une ligne par company × mois — choix « table dédiée » plutôt qu'agrégation du ledger : le quota de plan se compte en REQUÊTES, le ledger en TOKENS). Packs versionnés (`AiCreditService::PACKS_VERSION=2026-09-19.1` : S=100 k tokens/9 €, M=500 k/39 €, L=2 M/129 €) ; `credit()` idempotent par référence (pré-check + savepoint + index unique), `debit()` **fail-closed** (rollback si le solde deviendrait négatif → 422 `AI_CREDITS_EXHAUSTED`, catalogue d'erreurs ×4 langues). Achat : `POST /billing/ai-credits/checkout` (principal only) → Stripe Checkout **`mode=payment` one-shot** (`price_data` inline, metadata `{purpose, company_id, pack, tokens}`) ; le webhook `checkout.session.completed` en mode payment crédite le ledger — idempotent via le registre #5444 **et** la référence unique (un événement distinct portant la même session ne crédite pas deux fois). Mode **sandbox** opt-in explicite (`SANDBOX_CHECKOUT`, même sémantique que le front #2628 : crédit immédiat + redirection `success_url?sandbox=1`, jamais déduit d'une clé Stripe absente). `GET /billing/ai-credits` : solde + packs + historique paginé. Front : section « Crédits IA » **uniquement** dans `(dashboard)/billing` (solde, packs achetables, historique récent), **strictement facultative** (un échec de l'endpoint crédits n'empêche ni abonnement ni factures), i18n `billing.ai_credits.*` fr/en/ar/tr via `shared/i18n` (catalogues régénérés ×3 surfaces). Consommation : quota mensuel du plan d'abord, puis débit forfaitaire des crédits achetés (`TOKENS_PER_REQUEST=1000`, aligné `ai.max_tokens`), solde nul → 422 fail-closed (même contrat `{error, message, localized_message}` que `AI_QUOTA`/`AI_TOKEN_BUDGET`). OpenAPI +2 paths (miroir + SDK régénérés, redocly 0 erreur, couverture routes verte). **Vérifié** : gardes migrations (collisions basename + `--strict --fail-on-duplicate`) vertes ; front/web `eslint --max-warnings 0`, `tsc --noEmit`, **jest 128 suites / 1107 tests** (dont 3 nouveaux sur la section), `check:mojibake`, `I18N_VALIDATION_OK (4 locales)` ; `php -l` sans erreur sur tous les fichiers touchés ; **PHPUnit (AiCreditsTest 10 cas + TokenBudgetTest étendu) non exécuté localement — pas de runtime PHP 8.4 dans la sandbox, à confirmer par le check requis `Tests - Leopardo RH`.**

### Fixed

- **Contrat OpenAPI (#7657)** : doublons post-merges parallèles réparés — chemin `/public/market/payments/webhook/{provider}` (course #7901/#7821, version enrichie conservée) et schémas `HealthCheck`/`HealthResponse` dupliqués par la verticale Health (#7818).
- i18n : ARB mobiles régénérés après l'ajout des clés `setupinterview*` au catalogue partagé — `I18N Enterprise` de nouveau vert sur main (#7920).
- **fix(queue): le contexte tenant des jobs queued est généralisé ET verrouillé par un test d'architecture — plus aucun job tenant ne peut arriver en prod sans `EnsureTenantContext` (Refs #7649, volet code — le provisioning Render reste une action propriétaire).** L'audit #7649 pointait « middleware non systématique sur les ~36 jobs » : inventaire complet des 49 classes `ShouldQueue` de `api/app` — 37 jobs sur 39 portaient déjà le pattern `TenantScopedJob` + `middleware() [new EnsureTenantContext]`. Les 2 restants sont soldés : **`DeliverTravelWebhookJob`** (livraison HMAC des webhooks transporteur, TRAVEL-806) reçoit le middleware (résolution `company_id` via la delivery `withoutGlobalScopes`, pattern `ExportTravelReportJob`) ; **`ProvisionDemoTenantJob`** est documenté comme job PLATEFORME (il CRÉE le tenant — aucun contexte préexistant possible). Nouveau garde `tests/Unit/Architecture/QueueTenantContextArchitectureTest` (pur PHPUnit, sans boot Laravel ni DB) : scanne `app/` par réflexion, échoue si une future classe `ShouldQueue` n'implémente pas `TenantScopedJob` + `EnsureTenantContext` — exemptions EXPLICITES et justifiées une par une (`ProvisionDemoTenantJob` plateforme ; listeners queued `AuditLogger`/`WebhookListener` — dette « mode schema » assumée ; Mailables/Notifications exclus par catégorie, le middleware de job ne s'y applique pas), avec un second test qui périme les exemptions obsolètes. Côté ops : `render.yaml`/`render.prod.yaml` (worker `queue:work` + `schedule:run`) sont portés par la PR #7819 (même issue), le drain CI `queue-worker-fallback.yml` a déjà été supprimé par #7694 (supervision = pure alarme HTTP) ; la procédure propriétaire complète (billing → blueprint sync → débrayage du drain web → migration `QUEUE_CONNECTION=redis`) est consolidée dans `docs/ops/RENDER_WORKER_PROVISIONING_7649.md`. **Vérifié localement (PHP 8.4 rootless #7585)** : `php -l` OK, PHPStan strict `[OK] No errors` sur les 3 fichiers PHP touchés, nouveau test d'architecture **2 tests / 8 assertions verts** + preuve A/B (middleware retiré du job Travel → le garde échoue en le nommant). **PHPUnit Feature non exécuté localement (pas de PostgreSQL dans la sandbox)** — la suite complète à confirmer par le check requis `Tests - Leopardo`.
- **fix(ai): AIRateLimiter cesse d'utiliser un plan codé en dur `'starter'` (un PlanCode qui n'existe plus) et un compteur Cache volatil (Refs #7764).** Le plan effectif est dérivé **fail-closed** via `EntitlementGuard` (pas de souscription active → `free`) ; `config('ai.quotas')` est réaligné sur les PlanCode canoniques `free|pilot|operations|enterprise` (10/50/200/illimité — valeurs équivalentes aux anciennes `trial/starter/business` qui ne matchaient jamais) ; le compteur mensuel vit en DB (`ai_usage_counters`, survit aux redéploiements/`Cache::flush`), avec repli Cache uniquement si la table est absente (fixtures de test partielles, pattern #5265).

- **fix(marketing): i18n L-10 du front marketing web — chaînes FR en dur externalisées ×4 locales + tests unitaires des composants #7755 (PR #7784, delta résiduel du lot BC-12 après le merge de #7757).** Les composants livrés par #7757 (`MediaUrlsInput`, `PostInteractions`, `WeeklyReportCard`, page `social-marketing`) affichaient ~20 libellés français en dur (« Commentaires », « Supprimer », « Ajouter », « Bilan des 7 derniers jours », états loading/empty/sent…) : externalisation complète en clés `marketing.web.*` dans le catalogue partagé (fr/en/ar/tr, `shared/i18n/locales` + syncs web/admin régénérés via `sync-web.js`), URLs de médias forcées `dir="ltr"` pour le rendu RTL, **contrat OpenAPI** : les 5 endpoints marketing livrés par #7757 (`GET/POST …/comments[/reply|/suggest-reply]`, `POST /marketing/ai/suggest-post`, `GET /marketing/reports/weekly`) n'étaient pas documentés dans `api/openapi.yaml` — la garde `check-openapi-route-coverage.py --strict-staleness` était rouge sur main, elle repasse verte (schémas alignés sur les FormRequests réels). Ajout de 3 suites Jest (`MediaUrlsInput` ×4 cas, `PostInteractions` ×6 dont `normalizeComments` exporté, `WeeklyReportCard` ×3) + `data-testid` de ciblage — aucun changement d'API ni de comportement.
- **feat(travel): clés API de lecture par distributeur — émission/rotation/révocation par le gérant, catalogue publié scopé et rate-limité, guide partenaire complété (Closes #7641).** La boutique publique reposait sur un jeton UNIQUE par tenant (`X-Travel-Shop-Token`) : impossible de donner un accès distinct, révocable et traçable à plusieurs plateformes de distribution (les `partner-keys` #6086 sont entrantes — transporteurs — pas des clés de lecture). Nouvelle table tenant `travel_distributor_keys` (hash SHA-256 au repos, jamais de token en clair — pattern TravelCarrierApiKey/ZKTeco), modèle `TravelDistributorKey` (BelongsToCompany, allowlist `SCOPES` fermée : `catalog.read`, `bookings.read`). **Gestion côté gérant** (policy principal/rh, 404 sûr cross-tenant) : `GET/POST /travel/distributor-keys` (token `dsk_…` affiché UNE fois, scope inconnu → 422 fail-closed), `POST …/{key}/rotate` (remplacement atomique du hash — l'ancien token est immédiatement invalide, clé révoquée → 422) et `POST …/{key}/revoke` (idempotent, la ligne reste pour les stats/traçabilité). **Surface de lecture distributeur** (hors auth:sanctum, header `X-Distributor-Key`, middleware `travel.distributor:<scope>` miroir de `TravelPartnerAuthMiddleware` — contexte tenant posé, search_path restauré en finally #4787, échecs audités) : `GET /travel/distributor/catalog` (voyages PUBLIÉS uniquement, même règle que la boutique ; filtres villes/dates, per_page ≤ 100) et `GET /travel/distributor/bookings/{reference}` (extrait minimal sans PII passager, posture portail #7395) — throttle dédié 60/min ; stats d'usage par clé (`last_used_at` + `usage_count` incrémenté). Contrats OpenAPI (+4 paths, +1 schéma, miroir dev-hub + SDK JS/Python régénérés, garde de couverture 0 nouveau drift) ; `docs/travel/PARTNER_INTEGRATION_GUIDE.md` §5.2 (gestion, scopes, auth, isolation, rotation recommandée). Nouvelle suite `TravelDistributorKeyTest` (7 cas : token affiché une fois et jamais relisté, scope pirate 422 + employé simple 403, catalogue publié seul exposé, enforcement scope/révocation/rotation, stats d'usage, **isolation cross-tenant** catalogue + booking + gestion 404, suivi booking sans PII). **Vérifié** : `php -l` OK sur les 12 fichiers PHP touchés, gardes migrations (collisions + `--strict --fail-on-duplicate`) vertes, isolation modules/couches/BC 0 nouvelle violation, YAML OpenAPI parsé, `generate-openapi-sdk.mjs --check` OK. **Non vérifié localement (PHP 8.4/PostgreSQL absents)** : PHPStan strict et la suite Feature — à confirmer par la CI.
- **feat(onboarding,travel): vertical « Agence de voyage » dans l'entretien de préparation — activation auto du module et checklist orientée agence (Closes #7640, epic #7486).** Le chemin d'activation existait déjà de bout en bout (secteur `travel` dans l'allowlist `SetupInterviewPlanner` → verticale `travelagency` via `SolutionActivator`, manifest au catalogue depuis #7220-bis, cartes-réponses i18n ×4 déjà présentes) mais restait **non prouvé** et sans suite : aucune checklist agence après l'entretien. (1) **Checklist orientée agence** (`SeedDefaultSteps`) : un tenant dont la verticale `travelagency` est active reçoit trois étapes dédiées — créer son réseau (`travel_setup_network`), programmer son premier voyage (`travel_first_trip`), enregistrer sa première vente (`travel_first_sale`) — titres servis par `api/lang/*/onboarding.php` ×4 locales (garde PA2-I18N-007) + libellés riches `nextSteps.steps.travel_*` ×4 côté web (carte « Prochaines étapes » #7494) ; étapes déclaratives (aucun prédicat `StepCompletionGuard` : vérifier gares/voyages exigerait un import Onboarding → TravelAgency, interdit par la garde d'isolation #5584). (2) **Preuve par test** (`SetupInterviewTravelVerticalTest`, 3 cas) : un inscrit qui se déclare agence obtient le flag `companies.features.travelagency` SANS intervention plateforme (l'entrée « Voyages » de la nav web est gatée sur ce flag), la checklist agence est ordonnée réseau → voyage → vente, et un tenant non-travel ne voit JAMAIS ces étapes. Catalogues propagés par les 3 syncs (web/backend/mobile — rattrape au passage la propagation mobile des clés `travel.staff.*` de #7639). **Vérifié** : `php -l` OK sur les 5 fichiers PHP touchés, `I18N_VALIDATION_OK (4 locales)`. **Non vérifié localement (PHP 8.4/PostgreSQL absents de la session)** : PHPStan strict et la nouvelle suite Feature — à confirmer par la CI.
- **feat(travel): page Équipe de l'espace gérant — affectations staff sur bureau ou voyage, révocation en 2 clics, historique (Closes #7639, épique #7642).** Nouvelle page `/travel/staff` (front/web, pattern des pages gérant #7633..#7637) consommant le pont RH #7638 : nomination d'un employé RH du tenant sur un rôle travel (chauffeur, guichetier, contrôleur, chef de bureau) scopé sur un bureau de vente OU un voyage (`POST /travel/staff-assignments`, exactement un scope — contrat `StoreTravelStaffAssignmentRequest`), révocation en deux clics (`POST /travel/staff-assignments/{id}/revoke`, la ligne reste pour l'historique) et section « Historique des affectations » repliable (statuts révoquée/expirée, date de révocation). Tuile « Équipe » ajoutée au hub `/travel`, route `/travel/staff` déclarée dans `ROUTE_TO_MODULE` (même gating `travelagency` que le reste de l'espace gérant), i18n ×4 locales (clés imbriquées `travel.staff.*`, `travel.staffRole.*`, `travel.staffStatus.*`, propagées par `sync-web`). **Vérifié** : Jest 4/4 verts sur la nouvelle suite `staff-page.test.tsx` (liste active, révocation, nomination bureau, historique), `eslint --max-warnings 0` 0, `tsc --noEmit` 0.
- **feat(marketing): tranche 1 Marketing & Communication — campagnes email effectives + IA + interactions sociales (Closes #7751).** **(1) Campagnes email CRM effectives** : les campagnes `email` portent enfin leur contenu (`subject`/`body`, migration tenant additive `2026_09_19_000004_7751`, `down()` complet), le lancement matérialise les envois puis les traite en file (`ProcessCampaignSendsJob` sur la queue dédiée, tenant-scoped `EnsureTenantContext`, réclamation atomique des sends — rejeu sans double envoi) via le nouveau fournisseur **Resend** (`ResendEmailProvider`, clé par env `RESEND_API_KEY` uniquement, aucun secret committé, fail-closed si absente) ; commande schedulée idempotente `crm:process-campaign-sends` (`withoutOverlapping` + `onOneServer`). **(2) IA Marketing** : suggestions de posts sociaux et bilan hebdomadaire générés via l'abstraction LLM du dépôt (jamais d'appel vendeur direct), endpoints `POST /marketing/ai/suggest-post` et `GET /marketing/ai/weekly-report` (gate module + RBAC marketing). **(3) Interactions sociales** : lecture des commentaires et réponse (unitaire + en masse) via `AyrshareClient`, endpoints `SocialCommentController` documentés. **(4) Front web** (`front/web`) : éditeur de post enrichi (médias par URL), carte bilan hebdo, panneau interactions — i18n ×4 locales (FR/EN/AR/TR, catalogues `crm`/`marketing`). Arête BC déclarée dans `bounded-context-dependencies.json`. **Tests Feature** : `CrmCampaignSendProcessingTest`, `ResendEmailProviderTest`, `MarketingAiTest`, `SocialCommentControllerTest` (`Http::fake`, aucun appel réseau réel). Réparation CI au passage : renumérotation de la migration `2026_09_19_000001` en collision avec #7687 (garde #1962) et retrait d'un tiret cadratin flagué par PA2-I18N-007 dans un log du worker.
- **feat(restaurant): RESTO-902 — commande en ligne depuis la page publique par slug + avis/notes clients avec modération gérant (Refs #7747, base RESTO-901 #7746).** **Commande publique par slug** (réutilise le pipeline RESTO-805 — AUCUN second pipeline) : `POST /public/restaurants/{slug}/orders` (panier `items[].product_code` — l'identifiant métier désormais exposé par le menu publié du profil 901, cohérent avec la boutique RESTO-805 —, quantité, note par article agrégée dans `note_redacted`, nom + téléphone client jamais ré-exposés, `order_type` pickup|takeaway|delivery, idempotence par `idempotency_key` — rejeu → même commande, 200), via `CreateOnlineOrderAction` (prix + TVA SERVEUR, source `online`, outbox `restaurant.order.created.v1` ; l'action, sans appelant jusqu'ici, passe désormais la commande en `open` comme le service RESTO-805 routé) ; produits acceptés UNIQUEMENT si `is_published_online` ET `is_available` ET actifs et servis par la branche (422 sinon) ; branche non publique/société suspendue → **404 fail-closed** (nouveau `RestaurantPublicBranchResolver`, mêmes garde-fous que l'annuaire 901). `GET /public/restaurants/{slug}/orders/{ref}` : suivi par référence `RST-…` non énumérable, borné à LA branche du slug, **sans PII**. `POST …/{ref}/pay` : `cash` (alias public `cash_on_delivery`) / `mobile_money` via `RestaurantPublicOrderService::pay()` — **PSP carte volontairement fermé (spec #5272 en attente)**. **Avis clients** : migration `restaurant_reviews` (company_id uuid, branch_id, `order_reference` — la référence RST- aléatoire EST déjà non énumérable, même stockage que RESTO-805 — unique par tenant = un avis par commande, rating 1–5 tinyint, comment nullable, author_name, status pending|published|rejected défaut pending, index (branch_id,status)), modèle `BelongsToCompany`. `POST /public/restaurants/{slug}/reviews` : preuve d'achat = `order_ref` d'une commande de CETTE branche en statut terminal servi/livré (served/paid/closed), un seul avis par commande (409), statut initial pending, **throttle strict dédié** `restaurant-reviews-public` (5/min/IP, config + .env.example) en plus du `shop-public` du groupe ; `GET …/reviews` : publiés uniquement, paginés, DTO strict (author_name, rating, comment, date — jamais l'order_reference). **Modération tenant** (chemins EXACTS du front lot 904) : `GET /restaurant/reviews?status=`, `POST /restaurant/reviews/{review}/publish|reject` — `RestaurantReviewPolicy::moderate` (pattern gérant `canManageBranchResource` #7599), listing scopé `ScopesRestaurantBranchListings`, 404 cross-tenant. **Note moyenne** : `rating_avg` + `reviews_count` (avis PUBLIÉS seuls) sur les cartes de l'annuaire (sous-requêtes SQL sur le schéma partagé — **pas de compteur dénormalisé, zéro dérive, la modération fait foi**) et sur le profil public (porté par le cache 901, **purgé à chaque publish/reject**). i18n ×4 locales (`restaurant.public_reviews.*` + réparation des clés `restaurant.public_shop.*` référencées par RESTO-805 mais jamais créées). OpenAPI +8 opérations (tag Restaurant) — miroir `dev-hub/openapi/v1.yaml` et SDK **non committés** (#7654 : artefacts régénérés en CI, retirés de l'index dans cette PR). CI : table `restaurant_reviews` ajoutée à `CreatesMvpSchema` (parité #5443) ; les 3 contrôleurs publics par slug allowlistés avec justification dans `check-vertical-controller-policies.sh` (#7599/#7600 — surfaces volontairement publiques, mécanisme prévu par la garde). **Vérifié localement (PHP 8.4 rootless #7585)** : `php -l` OK ×23 fichiers, **PHPStan strict level 8 `[OK]` sur `app/Modules/RestaurantManager`**, `pint --test` PASS, gardes vertes : module-isolation (0 nouvel import), unrouted-controllers, OpenAPI route-coverage (0 nouveau drift), migrations (basename, conventions BC #5431, tenant-schema #1613, préfixes inter-PR), duplicate-routes + public-routes (`route:list` booté : 7 routes publiques + 3 privées enregistrées), policies-single-point, bounded-context registry/dependencies, layer-purity, tests déterministes, parité .env.example. **Non vérifié localement (pas de PostgreSQL)** : les 2 suites Feature (`RestaurantPublicSlugOrderTest` ×7 cas, `RestaurantReviewTest` ×7 cas) — à confirmer par le check requis `Tests - Leopardo RH`.
- **fix(flutter): #7652 tranche 2 — la feature `attendance` (data + provider) redevient canonique dans `leopardo_core`, paramétrée par app via config ; la couche d'état `auth` de l'app employee cesse d'être un fork (Refs #7652 — les écrans divergents restants et `absences`/`attendance_geo` exigent la CI Flutter, indisponible en sandbox).** Suite directe de la tranche 1 (#7743, inventaire + garde `mobile-core-duplication-guard.yml`). **(1) Fusion attendance data/provider** — la « divergence croisée » désignée pire du lot par l'audit est résorbée : `leopardo_core/.../attendance/data/attendance_repository.dart` devient le superset des deux forks (apports employee : `work_type`/`punch_note` dans les payloads de pointage, règle F-21 « 1er pointage gagne » sur la file hors-ligne Hive, `sessions`/`summary`/`session_number` dans `decodeTodayResponse`, `getMyAnomalies`/`getTodayTasks`/`completeTask` ; acquis core conservés : garde payload non-Map #3406, détection hors-ligne #5407, clé d'idempotence rejouée, endpoints manager anomalies/corrections/day-detail), idem pour `attendance_provider.dart` (état superset `todaySessions`/`daySummary`, notices `overtime`/`break`) ; le modèle employee-only `attendance_anomaly.dart` remonte dans `leopardo_core/lib/features/attendance/models/`. **(2) Différence par app via config, pas par fork** (exigence expresse de l'issue) : nouveau `AttendanceFeatureConfig` + `attendanceFeatureConfigProvider` (défaut manager/RH = comportement core historique) ; `leopardo_employee/main.dart` surcharge le provider (`ProviderScope(overrides:)`) pour le ton self-service du message de pointage hors zone — seule différence fonctionnelle légitime relevée entre les deux forks. **(3) Suppression des copies employee** : `attendance/data` + `attendance/providers` deviennent des shims de ré-export pur (pattern #5279 — chemins exigés par `validate-mobile-location-readiness.ps1`/`validate-mobile-notification-production-proof.ps1`), `attendance/screens/history_screen.dart` (12 lignes de diff cosmétiques) et `auth/data/auth_repository.dart` sont SUPPRIMÉS, l'app importe les versions core ; les 2 grands écrans encore forkés (`attendance_screen.dart` 2 432 L, `monthly_summary_screen.dart`) basculent dès maintenant sur le provider/modèles CORE (ils ne consomment plus aucun état local dupliqué). **(4) Couche d'état auth unifiée** : le diff core↔employee de `auth_provider.dart`/`auth_repository.dart` était exclusivement commentaires + formatage (prouvé par diff normalisé) — shim + suppression ; `core_providers.dart` de l'app cesse de shadow-déclarer `authRepositoryProvider`/`attendanceRepositoryProvider` : une SEULE instance Riverpod par provider dans toute l'app (le logout-401 du notifier attendance agit désormais sur le MÊME `authProvider` que le routeur — le fork rendait ce chemin silencieusement inopérant), et 12 fichiers employee (router, home, settings, profile, écrans auth…) importent `package:leopardo_core` directement. **(5) Baseline de la garde : 48 → 43 entrées** (`--update-baseline`, cliquet respecté) ; §6 de `docs/audits/FLUTTER_DUPLICATION_CORE_APPS_7652.md` documente la tranche et le reliquat (écrans attendance/auth brandés, `absences`, `settings`, `attendance_geo`, forks auth des apps satellites — fusions d'écrans injustifiables sans `flutter analyze`). **Vérifié localement (pas de SDK Flutter en sandbox — analyse statique exhaustive)** : garde #7652 verte (43 en baseline, 15 shims, 0 nouvelle duplication), zéro référence résiduelle aux fichiers supprimés (`rg`), tous les imports/exports `package:` des fichiers touchés résolus sur disque (script), accolades/parenthèses équilibrées sur les 12 fichiers Dart modifiés, clés l10n consommées présentes ×4 ARB, marqueurs interdits (`isManager`…) absents de l'app employee (garde split), tokens des contrats workflow (`attendanceNoClock`, `.checkIn(`…) toujours présents — `flutter analyze`/tests à confirmer par `Mobile Apps CI - Flutter`.
- **feat(pharmacy): verticale PharmaManager complète — MVP officines de pharmacie (Closes #7798, #7799, #7800, #7801, #7802, #7803, #7804 — série PHARMA-001→007, BC-30, lot BC_BATCH_BRANCH).** Nouvelle solution sectorielle activable par tenant pour les fondateurs d'officines, sur le patron exact d'EduManager/FuelStation. **(1) Fondation (#7798)** — `PharmacyManifest` (code `pharmacy`, maturité pilot, requis `['rh']`, optionnels accounting/crm/attendance, PII santé déclarées) enregistré dans la `SolutionCatalogue` (allowlist fail-closed) par `PharmacyServiceProvider`, flag `pharmacy` ajouté à `Company::KNOWN_MODULES` ET au registre versionné `config/feature-flags.php` (MAT-010 — sans quoi `FeatureFlag::enabled()` reste fail-closed même avec le flag tenant posé), entrée ROADMAP (règle APV L.08), trait `ChecksPharmacySolution` (403 `PHARMACY_SOLUTION_INACTIVE` fail-closed, 404 anti-fuite cross-tenant), routes `api/routes/modules/pharmacy.php` dans le groupe v1, structure Clean Architecture 5 couches, gouvernance BC-30 PHARMACY (registre MAT-001, arêtes BC-30→{BC-01,BC-02,BC-03} MAT-002, CODEOWNERS, parité docs architecture ×4). **(2) Référentiel produits (#7799)** — table tenant `pharmacy_products` (`BelongsToCompany`) : DCI, forme galénique, dosage, code-barres et code interne uniques PAR TENANT, `prescription_required`/`is_controlled`, catégorie, prix achat/vente et taux de taxe en decimal (jamais de float), seuil `min_stock_level`, statut active/archived ; API `/api/v1/pharmacy/products` (recherche nom/DCI/code-barres, filtres, archivage — jamais de suppression), RBAC deny-by-default. **(3) Stock par lots (#7800)** — `pharmacy_batches` (n° de lot, péremption, quantité restante, coût, CHECK ≥ 0, unicité company/product/batch) + `pharmacy_stock_movements` **journal immuable append-only** (gardes modèle anti-UPDATE/DELETE, CHECK types) ; `PharmacyStockService` transactionnel + `lockForUpdate` : `receive()`, `adjust()` (raison obligatoire, jamais de lot négatif), `dispenseFefo()` **First-Expired-First-Out multi-lots** refusant lots périmés et stock insuffisant (422 `PHARMACY_INSUFFICIENT_STOCK`, zéro effet partiel), `returnToBatches()` ; API `/pharmacy/stock` (niveaux hors périmés, lots, mouvements, ajustements manager) + `/pharmacy/alerts` (sous seuil, périmant < N jours défaut 90, périmés à retirer). **(4) Achats (#7801)** — `pharmacy_suppliers` + `pharmacy_purchase_orders`/lines : cycle `draft→ordered→partially_received→received|cancelled` (422 `PHARMACY_INVALID_TRANSITION` sinon), numéro `PO-YYYY-XXXX` séquencé PAR TENANT (verrou pessimiste), réception partielle → `PharmacyStockService::receive()` (lots + mouvements receipt tracés au PO), SUR-réception refusée (422 `PHARMACY_OVER_RECEIPT` + CHECK SQL) ; API CRUD + place/cancel/receive. **(5) Ventes comptoir POS (#7802)** — `pharmacy_sales` (numéro `VT-YYYY-XXXXX` séquencé/tenant, paiement cash|card|mobile|insurance, statut completed|voided) + lignes à prix/taxe FIGÉS ; `PharmacySaleService::create()` transactionnel : ordonnance exigée si produit `prescription_required` OU `is_controlled` (422 `PHARMACY_PRESCRIPTION_REQUIRED`), délivrance FEFO par ligne, totaux calculés SERVEUR (bcmath — le total client est ignoré) ; `void` (raison obligatoire, manager) conserve la vente et ré-crédite les lots d'ORIGINE par mouvements `return` (contre-passation). **(6) Ordonnancier (#7803)** — `pharmacy_prescribers` + `pharmacy_prescriptions` (référence unique/tenant, patients = PII santé jamais exposées cross-tenant, prescription validée dans le tenant à la vente — 422 `PHARMACY_PRESCRIPTION_NOT_FOUND`) ; `GET /pharmacy/controlled-register` **lecture seule, dérivé du journal immuable** des mouvements sale/return des produits contrôlés (date, produit, lot, quantité, n° de vente, ordonnance, prescripteur, patient, délivreur — les annulations apparaissent en contre-passation), accès manager, export CSV borné `?from=&to=&format=csv`. **(7) Dashboard fondateur (#7804)** — `PharmacyDashboardService` + `GET /pharmacy/dashboard` (manager) : CA jour/7j/30j, nb ventes du jour, panier moyen 30j, répartition par paiement (ventes voided EXCLUES partout), valorisation du stock (lots non périmés, quantité × coût), produits sous seuil, lots périmant < 90 j, lots périmés, top 5 produits 30 j, PO en cours, délivrances contrôlées 30 j — montants en décimal string. OpenAPI : 34 opérations `pharmacy` documentées (tag Pharmacy, garde de couverture #1473 verte, Redocly valide). **Vérifié localement (PHP 8.4 + PostgreSQL)** : PHPStan strict L8 `[OK] 0 erreur` sur le module, **PHPUnit 35 tests / 289 assertions verts** (6 suites Feature : produits, stock/FEFO/alertes, achats/réceptions, ventes/void, ordonnances/ordonnancier/CSV, dashboard — isolation tenant et 403 solution inactive couverts partout), gardes registre BC (30 BCs) + matrice MAT-002 + isolation modules + parité docs (30 modules ×4) + routes dupliquées + collisions de migrations + contrôleurs orphelins toutes vertes.
- **feat(accounting): les 7 écrans web du rôle comptable arrivent dans le dashboard admin — documents/factures, plan comptable, journal/grand livre/balance, lettrage, exercices & clôture, banque/rapprochement, TVA & états financiers (Fixes #7776, spec `.specify/features/5534-accounting-web-ui/spec.md`, reprise de la ghost close #5534).** Le backend comptabilité (~50 endpoints, `api/routes/modules/accounting.php`) n'avait que 3 écrans web (Dashboard/Activation/Settings) : l'expert-comptable devait appeler l'API à la main. Sept nouvelles vues Vue (`front/admin-dashboard/src/views/accounting/`), branchées sur les shapes de réponse EXACTES des controllers (lues dans le code, pas devinées), design system existant (tokens `glass-*`, badges de statut, états loading/empty, toasts) : **(1) Documents** — liste filtrée (type/statut/dates), création de brouillon avec éditeur de lignes + aperçu du prochain numéro (`/documents/next-number`), détail dépliable (lignes, paiements), envoi, annulation motivée, avoir lié, encaissement (POST `/documents/{id}/payments`, méthodes du FormRequest) ; **(2) Plan comptable** — filtre type/actifs, recherche, création (code/libellé/type/classe 1-8), activation/désactivation (PUT), suppression des seuls comptes libres (422 métier mis en mots) ; **(3) Journal & grand livre** — 3 onglets : journal de période (totaux, badges équilibré/clôturé, export CSV expert, clôture de période avec confirmation), grand livre paginé à solde cumulé + solde d'ouverture par compte, balance de vérification avec indicateur d'équilibre + export FEC DGFiP ; **(4) Lettrage** — sélection multi-écritures avec Σ débits/Σ crédits en direct et indicateur d'équilibre, garde min. 2 écritures côté client, erreurs métier `LETTERING_*` traduites, lecture seule sur période clôturée, délettrage par lettre ; **(5) Exercices & clôture** — ouverture par année, clôture IRRÉVERSIBLE derrière une confirmation « taper l'année », restitution du résultat (report à nouveau, écritures, périodes figées) ; **(6) Banque & rapprochement** — import CSV de relevé (multipart, période+référence), KPIs de rapprochement (lignes matchées, montants, écart de clôture), rapprochement auto, matching manuel ligne→paiement (proposition du moteur signalée), export CSV ; **(7) TVA & états financiers** — déclaration TVA par période (collectée/déductible/nette, détail par taux, export CSV), bilan par sections PCG avec invariant actif = passif+capitaux affiché, compte de résultat (produits/charges/résultat net). Routeur + sidebar : 7 routes et 7 entrées de sous-menu « Comptabilité » (#7725 conservé). i18n ×4 locales admin (fr/en/tr/ar, ~280 clés `accounting.*` + `navigation.*`, parité vérifiée par script). **Vérifié** : `npm ci` OK, `eslint .` 0 erreur, `vite build` + garde CSP verts, parité de clés i18n ×4 verte, clés utilisées par les vues toutes présentes (audit scripté). **Non exécuté localement** : ce package n'a ni TypeScript (`tsc --noEmit` sans objet) ni suite Jest (la FR-3 de la spec visait le front Next.js) — seuls les e2e Playwright existent (non lancés en sandbox, aucun spec existant ne couvre le sous-menu comptable), à confirmer par la CI.
- **feat(restaurant): RESTO-901 — profil public par branche + annuaire public des restaurants avec recherche & proximité (Closes #7746).** Chaque succursale peut désormais publier un profil public **opt-in** (`is_public`, défaut false) : migration additive sur `restaurant_branches` (`public_slug` unique GLOBAL cross-tenant, `establishment_type` — nouvelle enum `RestaurantEstablishmentType` ×8 valeurs —, `cuisine_types` JSON, `public_description`, `cover_image_url`, `latitude`/`longitude` decimal(10,7), index `(is_public, latitude, longitude)`) et sur `restaurant_products` (`is_published_online`, défaut false, indexé). **Endpoints privés** (pile tenant) : `GET/PUT /restaurant/branches/{branch}/public-profile` (validation stricte, slug généré depuis le nom avec suffixe numérique en cas de collision cross-tenant, slug explicite en collision → 422 ; autorisation `RestaurantBranchPolicy` — pattern ressource-scopé #7599) et `PATCH /restaurant/products/{product}/publication` (`RestaurantProductPolicy`). **Endpoints publics** (pattern BC-27/BC-28 : `throttle:shop-public`, AUCUN middleware tenant/Sanctum, 404 fail-closed, DTO public strict sans `company_id` ni ID interne — le slug est l'identifiant) : `GET /public/restaurants` (filtres q/city/type/cuisine, proximité `near=lat,lng` + `radius_km` défaut 10 max 50 par **formule haversine SQL** bornée least/greatest, tri distance sinon nom, pagination max 50/page, requête cross-tenant qualifiée `shared_tenants.*` — pattern PlatformMetricsOverview — filtrée sociétés ni suspendues/expirées + flag `restaurantmanager`) et `GET /public/restaurants/{slug}` (profil + horaires `restaurant_hours` + menu PUBLIÉ uniquement — `is_published_online` ET `is_available` ET actif —, cache serveur `RestaurantPublicDirectoryCache` TTL 10 min invalidé à la mutation du profil, ancien et nouveau slug purgés). OpenAPI +5 paths (tag Restaurant) + miroir `dev-hub/openapi/v1.yaml` et SDK régénérés. **Vérifié localement (PHP 8.4 rootless #7585)** : `php -l` OK sur les 16 fichiers, **PHPStan strict `[OK]` sur `app/Modules/RestaurantManager`**, gardes migrations (collisions de basename + duplication `Schema::create`), module-isolation et unrouted-controllers vertes. **Non vérifié localement (pas de PostgreSQL dans la session)** : les 2 nouvelles suites Feature (`RestaurantBranchPublicProfileTest` ×7 cas, `RestaurantPublicDirectoryTest` ×5 cas) — à confirmer par le check requis `Tests - Leopardo RH`.
- **feat(accounting): les 7 écrans web du rôle comptable dans la console admin — documents/factures, plan comptable, grand livre/journal, lettrage, exercices & clôture, banque/rapprochement, TVA & états financiers (Closes #7776, reprise de la spec `.specify/features/5534-accounting-web-ui/spec.md`).** Le backend comptabilité (~50 endpoints `api/routes/modules/accounting.php`) n'avait que 3 écrans côté `front/admin-dashboard` (Dashboard, Activation, Settings) : l'expert-comptable devait appeler l'API à la main. Livré : **(1) `AccountingDocumentsView`** — liste filtrable (type/statut/période), création de brouillon avec lignes dynamiques + aperçu du prochain numéro (`/documents/next-number`), détail (lignes, totaux HT/TVA/TTC, paiements), envoi, annulation motivée (dialog in-app, jamais `confirm()` natif — #3494), avoir lié pré-rempli, comptabilisation au journal, enregistrement de paiement (trésorerie #5229). **(2) `AccountingChartView`** — comptes classes 1-8 (filtre type, badge Système), création, activation/désactivation, suppression des seuls comptes libres (les comptes système ne proposent pas la suppression, US-2). **(3) `AccountingLedgerView`** — onglets Grand livre (running balance + solde d'ouverture + filtre compte + pagination), Balance (totaux généraux + indicateur d'équilibre) et Journal (équilibré/clôturé, export CSV expert, clôture de période confirmée), bouton **export FEC** (US-3). **(4) `AccountingLetteringView`** — sélection multi-écritures + lettre, garde locale < 2 sélections (aucun appel API, US-6.3), délettrage, lecture seule sur période clôturée. **(5) `AccountingFiscalYearsView`** — ouverture d'exercice, statuts, clôture avec confirmation irréversible (US-5). **(6) `AccountingBankView`** — import de relevé CSV (période + référence), rapprochement auto, matching manuel ligne→paiement (proposition du matching auto en tête de liste), état (rapprochées/en attente/écart), export CSV. **(7) `AccountingStatementsView`** — onglets Bilan (sections PCG emboîtées + invariant actif = passif + capitaux), Compte de résultat (produits/charges/résultat net) et **déclaration TVA** (collectée/déductible/net + détail par taux + export CSV). Ergonomie : hub de navigation rapide sur le dashboard comptable (US-1), sous-menu Comptabilité complet dans la sidebar (pattern #7725), design system existant (glass-card, RowActionButton #7434, dialogs in-app), i18n **×4 locales** (≈105 nouvelles clés `accountingModule.*`/`bankRecon.*` + 7 clés `navigation.*` admin, catalogues propagés par les 3 synchroniseurs, `I18N_VALIDATION_OK`). **Vérifié localement** : `eslint .` 0 erreur/0 warning, `vite build` OK (garde CSP verte), garde action-labels #7434 verte, parité i18n ×4 verte, `validate.js` OK. **Non livré ici (assumé)** : tests unitaires front — `front/admin-dashboard` n'a pas de harnais Jest/Vitest sur main (il vit sur `bc/bc01-admin-web-plateforme`) ; la couverture e2e Playwright suivra l'atterrissage du harnais.
- **fix(db): consolidation des migrations divergentes CRM/Fuel/EdgeSync/Restaurant — la dérive schéma↔modèles/tests qui tenait la suite backend au rouge est soldée (Refs #7452).** La dédup « une table, une migration » étant terminée (garde `--strict` verte), le rouge restant venait de la **génération perdante encore attendue par le code, les tests et les fixtures** : `column "legal_name" of relation "crm_accounts" does not exist` (28×), `relation "fuel_stock_reconciliations" does not exist` (6× — les 3 tables `fuel_deliveries`/`fuel_stock_movements`/`fuel_stock_reconciliations` avaient perdu leur UNIQUE déclaration lors de la dédup #7572), colonnes de liaison `uuid` inécrivables face à des PK bigint (22P02 sur `crm_opportunities.pipeline_id/lead_id/owner_id`, `crm_automations.created_by`, `crm_leads.account_id/owner_id`, `crm_export_jobs.user_id`), NOT NULL jamais renseignés par les écrivains réels (`crm_pipelines.stages`, `fuel_meter_intervals.previous/current_reading_id`, `travel_passengers.class_id`), et des **tests destructeurs** qui droppaient les tables canoniques en tearDown (4 suites CRM, 8 suites Edge — depuis le cache de fixture #6928 le setUp suivant ne rebâtit plus rien → cascade « relation does not exist » sur tout le worker). **Correctifs** : 3 migrations correctives idempotentes forward-only (pattern `2026_09_15_001600_7452` : `Schema::table` gardé `schemaHasColumn`, `DROP NOT NULL` rejouable, re-typage uuid→bigint gardé par le type courant avec mapping NULL des valeurs non numériques — documenté sans perte car ces colonnes étaient inécrivables) + alignement des `Schema::create` sources pour les bases fraîches ; modèle `CrmPipeline` créé (la table existait, les tests de scoping le référencent) + défauts d'attributs `CrmPipelineStage` ; écrivains corrigés (`TravelLegacyImportService` omettait `arrival_date`/`arrival_time` NOT NULL sur les trips — le `code` des routes/trips ayant été posé en parallèle par #7783, `CrmDashboardReadModel` `whereRaw('AND …')` → 42601 et `having('cnt')` → 42703, `CrmReadModelService` lisait `crm_leads.name` qui n'existe pas) ; factories complétées (`TravelOutboxEvent`, `RestaurantBranch`/`Category`/`TaxRate`/`Product` : `company_id` NOT NULL hors contexte tenant) ; fixture MVP alignée (`sync_queue.dedup_key` + unique #6554 dans le SQL et le PHP, stubs des 3 tables Fuel restaurées, helper `recreateCanonicalEdgeNodesTable()` pour les tests Edge legacy — restauration ciblée au lieu d'un rebuild fixture ~30 s/test) ; tests fixture-era réalignés sur le schéma canonique (ids uuid forcés, `contact_id` non numériques, consents sans `source`/en double sur la contrainte unique, import `SyncEngineService` au mauvais namespace). **Mesuré avant → après** (PHP 8.4 + PG14, runs groupés par verticale) : CRM **104 → 42** rouges/236, Edge **7 → 3** (plus aucune erreur), EdgeSync 5 → 4, Restaurant 91 → 84, Travel 144/593 → 134/611 (mesure post-merge de #7783/#7778, +18 tests), Fuel 48 → 46 — total verticales **402 → 316**, et surtout **plus aucun `column/relation does not exist` ni 23502/22P02 de dérive schéma** dans CRM/Travel/Fuel/Restaurant : le reliquat est la dette **non-schéma** déjà cartographiée dans #7452 (T2 : `TypeError`/constantes de génération croisée, T3 : ~300 échecs d'assertion de contrats API, + cascades 25P02 propres aux runs groupés #7469). **Vérifié** : migrate fresh public+tenant vert + **rejeu idempotent** vert (recette Render), `check-duplicate-schema-create.py --strict` vert (385 tables, 0 dupliquée), `check-migration-basename-collisions.sh` vert, `check-mvp-schema-parity.sh` vert, PHPStan strict `[OK]` sur app/Modules/CRM + TravelLegacyImportService, Pint PASS sur les 33 fichiers touchés.
- **chore(repo): tranche finale du volet immédiat de l'audit #7654 — `.gitignore` racine durci sur toutes les catégories d'artefacts purgées et garde CI élargie (Closes #7654).** Les tranches 1–3 (PR #7679, #7692, #7735) avaient sorti du tree tous les artefacts dénoncés par l'audit (`api/composer.phar`, `ci_log.txt`, `build/test_cache/*.dill` 52,7 MB, `CHANGELOG_ARCHIVE.md` 4,3 MB — archive consultable dans l'historique [@6a3819a](https://github.com/kitokoh/leopardo-hr/blob/6a3819a9254d31d5f124c3e14bd3cdd5f31848c8/CHANGELOG_ARCHIVE.md) —, `docs/api-mock-data/`, miroir OpenAPI + SDK générés), mais le `.gitignore` racine ne bloquait pas encore toutes les catégories à la source : seul `composer.phar` (pas `*.phar`), rien pour `test_cache/`, `*.dill` ni les `build/` d'apps mobiles (seuls les `.gitignore` par app les couvraient — le blob de 52,7 MB prouvait que ça ne suffit pas). Désormais : `*.phar`, `test_cache/`, `*.dill` et `front/mobile_apps/*/build/` sont ignorés à la racine, et `repo-hygiene-guard.yml` refuse en plus `*.phar` (généralisé) et `front/mobile_apps/*/build/*` sur toute PR (actionlint OK, garde rejouée localement : zéro artefact interdit traké, zéro fichier traké nouvellement ignoré). Les fichiers générés VOLONTAIREMENT conservés (`api/openapi.yaml` servi au runtime, catalogues i18n cibles importés au build) ne bougent pas ; leur plan de sortie et la réécriture d'historique (pack 93 MiB → < 15 MiB) restent des décisions propriétaire tracées dans `docs/ops/REECRITURE_HISTORIQUE_7654.md` et `docs/ops/ACTIONS_PROPRIETAIRES.md` (décision 9).
- **feat(travel): pont RH employee_id + affectations d'équipage chauffeur/guichetier/contrôleur — le manifeste de voyage liste enfin l'équipage (Closes #7638).** La décision d'architecture de `SOLUTION_TRAVEL_AGENCY.md` §685 (« HR reste propriétaire des employés ; la verticale référence employee_id par valeur ») n'était pas implémentée : aucun modèle Travel ne portait d'`employee_id`, le manifeste ne listait que les passagers, et le RBAC restait limité aux managers globaux. Nouvelle table tenant `travel_staff_assignments` (`company_id` NOT NULL, `employee_id` **par valeur** — aucune FK vers `employees`, l'appartenance au tenant est validée au FormRequest —, rôle métier travel `driver|agent|controller|office_manager`, scope **exactement un** de `office_id`/`trip_id`, statut `active|revoked` avec `revoked_at`/`revoked_by_user_id` — une révocation ne supprime jamais la ligne, l'historique des manifestes passés est préservé) ; une table = une migration, garde `schemaTableExists` anti-collision. Endpoints CRUD `/travel/staff-assignments` (index filtrable `employee_id`/`office_id`/`trip_id`/`role`/`status`, doublon actif employé×rôle×scope → **409**, révocation par `POST …/{id}/revoke` idempotente — acte métier, convention #4930) + `TravelStaffAssignmentPolicy` composée avec le RBAC ressource-scopé #7600 (trait `ChecksResourceScopedAccess`, clé `travel_office` : un responsable porteur de `manage` sur SON bureau gère son équipage, historique principal/rh sinon ; lecture ouverte au tenant) — la PR #7632 étant mergée, aucune attente. **Manifeste enrichi** : `GET /travel/trips/{trip}/manifest` ajoute la clé top-level `crew` (affectations ACTIVES du trajet, nom RH résolu via Core\Auth\Employee — la verticale lit Core, jamais l'inverse) sans toucher au contrat `data` existant (rétro-compatible). Contrat OpenAPI DOCUMENTÉ (les 6 routes + schéma `TravelStaffAssignment` dans `api/openapi.yaml`, plutôt que l'allowlist du pattern EPIC #6528 — entrées purgées, exigence `--strict-staleness` ; miroir `dev-hub/openapi/v1.yaml` + SDKs JS/Python régénérés, Redocly lint valide). **Vérifié** : `pint --test` PASS et **PHPStan strict `[OK] No errors`** sur les fichiers touchés (PHP 8.4 rootless) ; nouvelle suite `TravelStaffAssignmentTest` (12 cas : affectation bureau et voyage, révocation idempotente, 409 doublon, scope exclusif, employé/bureau étrangers → 422, ressource étrangère → 404, index isolé par tenant + filtres, employé self-service → 403 en écriture, manifeste listant l'équipage actif seul avec nom RH, 401 sans auth) — **PHPUnit non exécuté localement (pas de PostgreSQL dans la sandbox), à confirmer par le check requis `Tests - Leopardo RH`**.
- **feat(support): notifications email des tickets client↔plateforme — nouveau ticket, réponse support, réponse client (Closes #7760, spec MISSION_ESPACE_CLIENT_DELEGATION_BILLING_SUPPORT §3.2).** Le centre de support pilote (PA2-COMM-012) ne déclenchait **zéro e-mail** : un ticket ouvert restait invisible tant qu'un super-admin n'ouvrait pas la console, et le client ne savait jamais qu'on lui avait répondu. `PlatformSupportTicketService` déclenche désormais, APRÈS commit de la transaction (jamais de notification fantôme sur rollback) : **(a)** nouveau ticket → tous les super-admins ACTIFS porteurs de la permission plateforme `support.manage` (matrice `PlatformRole::permissions()`, #7553 — `super_admin`, `admin`, `support` ; jamais `marketing`/`finance`/`ops` ni un compte suspendu) ; **(b)** réponse super-admin → employé auteur du ticket, dans SA langue (préférence employé sinon langue du tenant, même résolution que `UserInvitationMail`) ; **(c)** réponse employé → super-admin assigné (`assigned_super_admin_id`, s'il est actif) sinon l'équipe `support.manage`. Trois Mailables `ShouldQueue` (envoi en file) sur le mécanisme canonique `EmailTemplateResolver` (#7347 — surcharge admin possible, registre `EmailTemplateRegistry` +3 templates, défauts i18n ×4 locales dans `api/lang/{fr,en,ar,tr}/emails.php`, templates blade sur le layout canonique) ; **le sujet porte la référence du ticket** (`[Ticket #N] …`) et le corps ne recopie jamais le message (l'e-mail est une notification, la conversation se lit dans l'espace client / la console). Nouvelle suite `SupportTicketEmailNotificationTest` (5 tests, `Mail::fake()`) : les 3 déclencheurs, le repli équipe sans assigné, l'exclusion des rôles sans `support.manage` et des comptes suspendus, **aucun destinataire d'un autre tenant ne reçoit rien**, et la référence du ticket dans le sujet. **Vérifié localement** : `php -l` OK sur les 10 fichiers PHP touchés, gardes `check-module-isolation`/`check-layer-purity`/`check-i18n-catalog-parity` vertes. **Non vérifié localement (PHP 8.4/PostgreSQL absents de la sandbox)** : PHPStan strict et PHPUnit — à confirmer par la CI.
- **feat(support): espace tickets support côté client — liste, création, fil de messages, clôture (Closes #7759, spec MISSION_ESPACE_CLIENT_DELEGATION_BILLING_SUPPORT §3.2).** L'API tenant `/api/v1/support-tickets` (PA2-COMM-012) existait et était testée, mais **aucune UI** ne la consommait dans `front/web` (les clés i18n `support_tickets` étaient orphelines) : le client ne pouvait ni ouvrir un ticket ni lire les réponses de la plateforme. Nouvelle page `(dashboard)/support` sur les patterns de `settings/team` (#7555) : liste des tickets avec statut/catégorie/priorité/dernière activité et filtres par statut, formulaire de création (sujet, catégorie `general|billing|technical|onboarding|other`, priorité `low|normal|high|urgent`, message), fil de messages (plateforme vs entreprise) avec réponse, et **clôture avec confirmation inline** (jamais de `window.confirm`) ; un ticket clos masque le formulaire de réponse et le 422 API (`TICKET_ALREADY_CLOSED`) est mis en mots. Entrées de menu Support (tiroir mobile + menu utilisateur, tous les rôles pour l'instant — la restriction par grant `support` arrive avec la délégation par module §3.1) ; `/support` rejoint `PROTECTED_PREFIXES` (source unique #3377), le gate de session `DASHBOARD_PREFIXES` de `proxy.ts`, `sw.js` et robots.txt (garde anti-oubli #7663 verte). i18n : clés `support_tickets.*` orphelines réutilisées et complétées ×4 locales dans `shared/i18n` (syncs web/mobile/backend rejoués, `I18N_VALIDATION_OK`), repli FR centralisé dans `src/lib/i18n/support-tickets.ts` (même mécanique que `team-roles.ts`, garde PA2-I18N-014 respectée). **Vérifié** : `eslint src --max-warnings 0` OK, `tsc --noEmit` 0, **Jest 128 suites / 1116 tests verts** (dont 9 nouveaux : liste+badges, filtre statut, état vide, création → fil ouvert, validation espaces, fil+réponse, ticket clos, 422 mis en mots, clôture confirmée), `check:mojibake` OK.
- **feat(travel): API publique MARKETPLACE inter-agences — recherche agrégée cross-tenant des trajets publiés et tunnel d'achat résolu par trajet (Closes #7737, épic #7736).** La boutique publique restait MONO-tenant (TRAVEL-1001/#6114 : jeton `X-Travel-Shop-Token` détenu par UNE agence) : le grand public ne pouvait pas comparer les départs de plusieurs agences. Nouvelle surface `/api/v1/public/travel/marketplace/*` SANS jeton d'agence (throttle `shop-public`, aucune auth) : **(1) opt-in borné** — seules les agences au jeton boutique ACTIF **et** à la feature `travelagency` active (kill switch plateforme respecté) sont visibles, aucune migration d'opt-in nécessaire ; **(2) villes desservies** (`GET /marketplace/cities`) et **recherche agrégée** (`GET /marketplace/trips?origin_city_id&destination_city_id&date`) — `travel_cities` étant un référentiel PAR TENANT, l'agrégation matche par identité géographique (nom insensible à la casse + pays ISO2), chaque requête étant explicitement contrainte `whereIn(company_id, éligibles)` (jamais de lecture non bornée) et n'exposant que prix/sièges disponibles/villes/NOM public de l'agence ; **(3) tunnel d'achat résolu par trajet** — détail + plan de sièges (`GET /marketplace/trips/{trip}`, 404 fail-closed hors opt-in) et réservation (`POST /marketplace/bookings`) qui pose le tenant du trajet via `TenantManager::withinTenant()` + marqueur `tenant_scope_required` (pattern `EnsurePublicShopAccess`) puis délègue au flux TRAVEL-1001 existant (`CreateBookingAction` : verrouillage sièges, montant serveur, idempotence) avec la nouvelle source `booking_source=marketplace` (contrainte CHECK étendue par migration dédiée) ; **(4) paiement** (`POST /marketplace/payments/initiate`) résolu par RÉFÉRENCE de réservation (référence ambiguë cross-tenant → 404 fail-closed, pattern #7395), même contrat idempotent que TRAVEL-1002 ; suivi/annulation/e-billet réutilisés tels quels (référence + code de validation) via des alias `/marketplace/*`. La boutique mono-agence est INCHANGÉE. OpenAPI +8 paths (miroir dev-hub + SDK régénérés, garde de couverture 0 drift, Redocly valide). Nouveau `TravelMarketplaceApiTest` (6 cas Feature multi-tenant : agrégation ≥ 2 agences sans jeton, exclusion jeton inactif/feature absente, déduplication des villes, 404 fail-closed sur trajet non opt-in, réservation créée chez la BONNE agence et invisible ailleurs + idempotence, paiement résolu par référence). **PHPUnit/PHPStan non exécutés localement (PHP 8.1 sans vendor dans l'image d'agent) — à confirmer par la CI.** — `CHANGELOG_ARCHIVE.md` (4,2 Mo) et `docs/api-mock-data/` (56 JSON générés) sortis du dépôt, fichier tracké-ignoré `.specify/.../.composed/speckit.specify.md` dé-tracké.** L'archive reste intégralement consultable dans l'historique git ([version complète @6a3819a](https://github.com/kitokoh/leopardo-hr/blob/6a3819a9254d31d5f124c3e14bd3cdd5f31848c8/CHANGELOG_ARCHIVE.md)) ; le processus de release (CONVENTIONS.md §4.3, AGENTS.md) archive désormais dans les notes de release GitHub au lieu d'un fichier versionné qui regonflait le pack à chaque release. Les exemples API se régénèrent à la demande (`python dev-hub/tools/generate_api_examples.py`, désormais gitignoré) — READMEs mobiles et docs/README.md mis à jour, exclusion obsolète retirée de `check-canonical-domains.sh`. Réparation au passage : deux entrées de changelog flottaient AU-DESSUS du header `## [Unreleased]` (résolution de merge défaillante — le symptôme même que #7654 vise) : réintégrées sous le header. Le reste de #7654 (miroir OpenAPI/SDK et catalogues i18n générés au build, réécriture d'historique ~1,3 Go) demande des arbitrages propriétaire et reste dans l'issue.

### Added

- **Vertical Voyage (Travel)** : référentiel réseau (gares, lignes, arrêts), gestion des voyages et tarifs, réservations/billets avec check-in, rapports gérant et export CSV, équipages liés à la RH, clés API distributeurs, marketplace publique inter-agences et site public Next.js `front/travel-web` (recherche, sièges, checkout, comptes clients cross-agences) avec pipeline de déploiement Vercel.
- **Vertical Restaurant** : profil public par branche + annuaire avec recherche/proximité (RESTO-901), commande en ligne et avis modérés (RESTO-902), encaissement réel via les profils de paiement du tenant (Stripe sur les clés du restaurateur), pilote RBAC ressource-scopé par succursale.
- **Vertical Commerce/Retail & Leopardo Marché (BC-17)** : module vendeur actif (produits, stock, POS, espace `/commerce`), marketplace grand public multi-vendeurs cross-tenant (API publique, checkout invité, web), comptes acheteurs plateforme (favoris, historique, avis vérifiés post-livraison).
- **Vertical Santé/Pharmacie** : MVP PharmaManager complet pour les officines (#7798–#7804).
- **Verticaux Fuel, Edu, Fleet, Cameras** : généralisation du patron RBAC ressource-scopé, consolidation des migrations, module Caméras horizontal auto-activable + application mobile « Leopardo Caméras » (BC-19).
- **Paie multi-pays** : moteur de paie durci (indemnités congés payés, solde de tout compte, certificat de travail, journal mensuel, workflow de clôture via API), mentions légales DZ, cumuls annuels sur bulletin PDF, benchmark 10 000 employés, gate de couverture Payroll ≥ 80 % bloquant.
- **Facturation & paiements (BC-21)** : configuration des passerelles PSP depuis l'admin plateforme (stockage chiffré + fallback env), crédits IA achetables (ledger, checkout, décompte réel), emails facture/reçu avec PDF joint i18n ×4, rapprochement `stripe_invoice_id`, prix des plans paramétrables en base.
- **Module Communication (R0–R6)** : connexion Gmail par utilisateur (OAuth, tokens chiffrés), sync incrémentale, classification IA + liaison CRM, relances automatiques, réponses assistées avec politiques par boîte, UI « Boîte connectée » côté client et plateforme.
- **RBAC & espace client** : cycle de vie des accès ressource (invitation pré-assignée, vue « qui a accès à quoi », révocation en cascade), grants de modules composables par collaborateur, délégation plateforme (rôles internes, écran Équipe), gestion des collaborateurs côté client.
- **Onboarding & acquisition** : entretien de préparation conversationnel (remplace la modale 10 étapes), première connexion sans mot de passe en clair (lien magique), tracking par étape du tunnel d'acquisition avec dashboard des conversions, écran de bienvenue unique.
- **Marketing & support** : campagnes email effectives + IA + interactions sociales, publication sociale via jobs queued (LinkedIn/Meta/Twitter), espace tickets support client avec notifications email bidirectionnelles.
- **Comptabilité** : les 7 écrans web du rôle comptable dans le dashboard admin (documents/factures, plan comptable, grand livre/journal, lettrage…).
- **Branding & i18n** : image de marque du tenant dans l'espace client, thème dashboard et PDF ; internationalisation ×4 locales des surfaces publiques et marketing.
- **Leopardo Marché — livraison & paiement en ligne** : handoff livraison BC-26 des commandes en ligne (création automatique de la livraison à la confirmation + suivi livreur sur la page publique) (#7811/#7857) ; paiement en ligne du checkout — intents PSP (Chargily/mobile money), webhook signé fail-closed, réconciliation et remboursements vendeur (#7812/#7858).
- **Espace client web** : page « Employés » unifiée (rôles, grants de modules, invitations, ressources) avec redirection depuis `settings/team` (#7862/#7884) ; page « Mon compte » refondue (profil éditable, mot de passe & 2FA intégrés, carte abonnement) (#7861/#7883) ; shell client modernisé (header compact, menus au survol, avatar standard, branding réactif) (#7860/#7882).
- **Growth** : stratégie d'acquisition 2026 T4 documentée + pages SEO « Alternative à X » lot 1 sur la vitrine (#7869/#7887).
- **Encaissements** : type « cash » (encaissement au local) + enregistrement manuel des encaissements avec page front dédiée et routes `/billing/collections` (#7863/#7885).
- **Onboarding** : pop-up d'import du jeu de données de démonstration à la première entrée dans l'espace (Importer / Plus tard / Non merci) (#7866/#7891), adossée à l'API demo-data par verticale (registre de kits, import idempotent, statuts persistés) (#7865/#7890).
- **Admin plateforme** : groupe « RH & Paie » unifié dans la navigation (#7898).
- **Vitrine** : Leo la pieuvre en visuel d'entrée du héro + pile 3D des solutions en section dédiée (#7851/#7894).

### Changed

- **Positionnement produit** : Leopardo cesse d'être présenté comme un logiciel RH — README, vitrine, console admin, workflows CI et docs alignés « suite métier » (#7428 et suites) ; repositionnement vitrine « suite métier open-source » (copie, SEO, CTAs) (#7895/#7899).
- **Gouvernance du dépôt** : `.gitignore` durci, miroir OpenAPI et SDK générés sortis du versioning, gros artefacts purgés (audit #7654), cartographie et cliquet anti-croissance de la dette PHPStan (#7655) puis tranche 2 de résorption — annotations `@property` TravelAgency et baselines élaguées de 35 % (#7820), navigation client et sidebar admin refondues (BC-01).
- **Ops/infra** : topologie Render dev/prod honnête (`APP_ENV=staging` + `DEPLOY_TIER=dev`), worker Render dédié queue+scheduler, drills de restauration des backups (PostgreSQL 18, clé `age`, fail-fast), boucle `schedule:run` intérimaire.
- **Migrations & schéma** : consolidation des migrations divergentes CRM/Fuel/EdgeSync/Restaurant/Travel/Edu, règle « une table, une migration » gardée en CI, tests sur les vraies migrations.

### Fixed

- **Main vert (#7657)** : contrat OpenAPI réparé (chemins `/billing/payment-profiles*` dédoublonnés, `exclusiveMinimum` conforme 3.0.3), 12 routes documentées (11 marketplace publique #7814 + remboursement en ligne Retail #7812), garde RBAC satisfaite sur `RestaurantPaymentConfigurationController` (policy `viewAny` des paiements, #7599).
- **CI/CD** : suites backend fiabilisées (timeouts de verrous Postgres, reaper idle-in-transaction, dérive de fixtures), gardes governance suivant les déplacements de docs, workflows SHA-pinnés, gate de déploiement non contournable, déploiements Vercel/staging réparés, mémoire PHPStan strict/modules relevée de 1 G à 3 G (#7879).
- **BC-21 post-merge** : parité `.env.example` (5 clés restaurant) et couverture OpenAPI des 9 routes paiements réparées (#7726/#7727/#7856).
- **Invitations employés** : préservation des accès au renvoi d'invitation, `FRONTEND_URL` honoré dans les liens, garde-fous sur `accept()` (#7864/#7886).
- **Réparation de main** : garde build-phase Lighthouse, Pint edge-sync, i18n admin et registre des scénarios de tests remis au vert (#7892/#7893).
- **Isolation tenant** : raccordement systématique des modèles à `BelongsToCompany` (hook `creating` forçant le `company_id`, garde `updating` anti cross-tenant), contexte tenant des jobs queued verrouillé par un test d'architecture, routes plateforme dédupliquées sous `platform.permission`.
- **Front web** : CSP en ENFORCE avec nonce par requête, panneaux d'en-tête, formulaires publics anti-spam (honeypot, time-trap, contrôle d'origine), schémas zod branchés.
- **Audit externe 2026-09-20 (en cours)** : URL backend Render dev codée en dur comme fallback silencieux dans 4 fronts remplacée par un fail-fast en production (#7842/#7880) ; assainissement de la gouvernance racine — CHANGELOG réécrit, AGENTS.md recentré, docs de pilotage déplacées, doublon `BRANCH_PROTECTION_REQUIRED.md` résorbé (#7843).

### Security

- **Multi-tenant** : défense en profondeur SmartAttendance, écriture cross-tenant par mass assignment fermée, piste d'audit lisible après purge d'un tenant.
- **Secrets & tokens** : tokens d'auth en cookie httpOnly (front/web + admin-dashboard), bearer super-admin hors du DOM + CSP durcie au build, token kiosk hors DOM + PIN admin, supervision queue sans credentials de prod dans les runners GitHub, alerte hebdo secrets exposés, fin des fallbacks « password123 ».
- **Edge** : TLS interne par défaut sur le LAN client + manifeste d'installation signé RS256.
- **Conformité** : droit à l'effacement RGPD (anonymisation employé), rétention et purge biométrique automatique, journalisation des accès aux données sensibles, bandeau de consentement et traceurs coupés par défaut, chiffrement au repos du metadata des documents de paie.
- **Dépendances** : bumps de sécurité (guzzle CVE-2026-59883/69245/69246, commonmark, shell-quote, sharp…).
- **Audit externe 2026-09-20 (en cours)** : EdgeSync `applyGeneric` — allowlist des `entity_type` et forçage tenant du payload (#7840/#7867) ; travel-web — session migrée de localStorage vers un cookie httpOnly (#7841/#7888) ; durcissement conteneurs et défauts (Dockerfile.edge non-root, ports locaux, mots de passe par défaut surchargables, header `X-XSS-Protection` déprécié retiré) (#7847/#7889).

## Journal continu — versions publiées

Ce fichier ne détaille pas chaque version : les entrées sont consignées en
continu ci-dessus, et le périmètre exact de chaque version publiée se lit via
les liens de comparaison entre tags.

| Version | Date | Périmètre |
| --- | --- | --- |
| [v4.33.0](https://github.com/kitokoh/leopardo-hr/releases/tag/v4.33.0) | 2026-09-20 | [v4.32.0...v4.33.0](https://github.com/kitokoh/leopardo-hr/compare/v4.32.0...v4.33.0) |
| [v4.32.0](https://github.com/kitokoh/leopardo-hr/releases/tag/v4.32.0) | 2026-09-13 | [v4.31.0...v4.32.0](https://github.com/kitokoh/leopardo-hr/compare/v4.31.0...v4.32.0) |
| [v4.31.0](https://github.com/kitokoh/leopardo-hr/releases/tag/v4.31.0) | 2026-09-13 | [v4.30.0...v4.31.0](https://github.com/kitokoh/leopardo-hr/compare/v4.30.0...v4.31.0) |
| [v4.30.0](https://github.com/kitokoh/leopardo-hr/releases/tag/v4.30.0) | 2026-09-12 | [v4.29.0...v4.30.0](https://github.com/kitokoh/leopardo-hr/compare/v4.29.0...v4.30.0) |
| [v4.29.0](https://github.com/kitokoh/leopardo-hr/releases/tag/v4.29.0) | 2026-09-11 | [v4.28.0...v4.29.0](https://github.com/kitokoh/leopardo-hr/compare/v4.28.0...v4.29.0) |
| [v4.28.0](https://github.com/kitokoh/leopardo-hr/releases/tag/v4.28.0) | 2026-09-10 | [v4.27.2...v4.28.0](https://github.com/kitokoh/leopardo-hr/compare/v4.27.2...v4.28.0) |
| [v4.27.2](https://github.com/kitokoh/leopardo-hr/releases/tag/v4.27.2) | 2026-09-03 | [v4.27.1...v4.27.2](https://github.com/kitokoh/leopardo-hr/compare/v4.27.1...v4.27.2) |
| [v4.27.1](https://github.com/kitokoh/leopardo-hr/releases/tag/v4.27.1) | 2026-09-03 | [v4.27.0...v4.27.1](https://github.com/kitokoh/leopardo-hr/compare/v4.27.0...v4.27.1) |
| [v4.27.0](https://github.com/kitokoh/leopardo-hr/releases/tag/v4.27.0) | 2026-09-03 | [v4.26.0...v4.27.0](https://github.com/kitokoh/leopardo-hr/compare/v4.26.0...v4.27.0) |
| [v4.26.0](https://github.com/kitokoh/leopardo-hr/releases/tag/v4.26.0) | 2026-09-02 | [v4.25.0...v4.26.0](https://github.com/kitokoh/leopardo-hr/compare/v4.25.0...v4.26.0) |
| [v4.25.0](https://github.com/kitokoh/leopardo-hr/releases/tag/v4.25.0) | 2026-08-25 | [v4.24.0...v4.25.0](https://github.com/kitokoh/leopardo-hr/compare/v4.24.0...v4.25.0) |
| [v4.24.0](https://github.com/kitokoh/leopardo-hr/releases/tag/v4.24.0) | 2026-08-11 | première release GitHub publiée |

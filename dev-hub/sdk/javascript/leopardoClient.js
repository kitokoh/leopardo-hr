// Generated by dev-hub/tools/generate-openapi-sdk.mjs from api/openapi.yaml.
// Do not edit manually.

export class LeopardoApiError extends Error {
  constructor(message, { status, response, body }) {
    super(message);
    this.name = "LeopardoApiError";
    this.status = status;
    this.response = response;
    this.body = body;
  }
}

export function createLeopardoClient({ baseUrl, token, fetchImpl = globalThis.fetch } = {}) {
  if (!baseUrl) {
    throw new TypeError("baseUrl is required");
  }

  if (typeof fetchImpl !== "function") {
    throw new TypeError("fetchImpl is required when global fetch is unavailable");
  }

  const normalizedBaseUrl = baseUrl.replace(/\/+$/, "");

  function buildPath(pathTemplate, pathParams = {}) {
    return pathTemplate.replace(/\{([^}]+)\}/g, (_, name) => {
      if (pathParams[name] === undefined || pathParams[name] === null) {
        throw new TypeError(`Missing path parameter: ${name}`);
      }

      return encodeURIComponent(String(pathParams[name]));
    });
  }

  function buildUrl(pathTemplate, { pathParams, query } = {}) {
    const url = new URL(`${normalizedBaseUrl}${buildPath(pathTemplate, pathParams)}`);

    for (const [key, value] of Object.entries(query ?? {})) {
      if (value === undefined || value === null) {
        continue;
      }

      if (Array.isArray(value)) {
        for (const item of value) {
          url.searchParams.append(key, String(item));
        }
      } else {
        url.searchParams.set(key, String(value));
      }
    }

    return url;
  }

  async function request(method, pathTemplate, { pathParams, query, body, headers = {} } = {}) {
    const requestHeaders = {
      Accept: "application/json",
      ...headers,
    };

    if (token) {
      requestHeaders.Authorization = `Bearer ${token}`;
    }

    const init = {
      method,
      headers: requestHeaders,
    };

    if (body !== undefined) {
      requestHeaders["Content-Type"] = requestHeaders["Content-Type"] ?? "application/json";
      init.body = requestHeaders["Content-Type"].includes("application/json") ? JSON.stringify(body) : body;
    }

    const response = await fetchImpl(buildUrl(pathTemplate, { pathParams, query }), init);
    const contentType = response.headers.get("content-type") ?? "";
    const payload = contentType.includes("application/json") ? await response.json() : await response.text();

    if (!response.ok) {
      throw new LeopardoApiError(`Leopardo API request failed with status ${response.status}`, {
        status: response.status,
        response,
        body: payload,
      });
    }

    return payload;
  }

  return {
    request,

    /** Lister les absences */
    getAbsences(options = {}) {
      return request("GET", "/absences", options);
    },

    /** Lister les absences */
    getAbsences2(options = {}) {
      return request("GET", "/absences", options);
    },

    /** Lister les absences */
    getAbsences3(options = {}) {
      return request("GET", "/absences", options);
    },

    /** Lister les absences */
    getAbsences4(options = {}) {
      return request("GET", "/absences", options);
    },

    /** Lister les absences */
    getAbsences5(options = {}) {
      return request("GET", "/absences", options);
    },

    /** Lister les absences */
    getAbsences6(options = {}) {
      return request("GET", "/absences", options);
    },

    /** Lister les absences */
    getAbsences7(options = {}) {
      return request("GET", "/absences", options);
    },

    /** Lister les absences */
    getAbsences8(options = {}) {
      return request("GET", "/absences", options);
    },

    /** Demander une absence */
    postAbsences(options = {}) {
      return request("POST", "/absences", options);
    },

    /** Demander une absence */
    postAbsences2(options = {}) {
      return request("POST", "/absences", options);
    },

    /** Demander une absence */
    postAbsences3(options = {}) {
      return request("POST", "/absences", options);
    },

    /** Demander une absence */
    postAbsences4(options = {}) {
      return request("POST", "/absences", options);
    },

    /** Demander une absence */
    postAbsences5(options = {}) {
      return request("POST", "/absences", options);
    },

    /** Demander une absence */
    postAbsences6(options = {}) {
      return request("POST", "/absences", options);
    },

    /** Demander une absence */
    postAbsences7(options = {}) {
      return request("POST", "/absences", options);
    },

    /** Demander une absence */
    postAbsences8(options = {}) {
      return request("POST", "/absences", options);
    },

    /** Annuler une absence (employe proprietaire) */
    deleteAbsencesByAbsence(options = {}) {
      return request("DELETE", "/absences/{absence}", options);
    },

    /** Annuler une absence (employe proprietaire) */
    deleteAbsencesByAbsence2(options = {}) {
      return request("DELETE", "/absences/{absence}", options);
    },

    /** Annuler une absence (employe proprietaire) */
    deleteAbsencesByAbsence3(options = {}) {
      return request("DELETE", "/absences/{absence}", options);
    },

    /** Annuler une absence (employe proprietaire) */
    deleteAbsencesByAbsence4(options = {}) {
      return request("DELETE", "/absences/{absence}", options);
    },

    /** Annuler une absence (employe proprietaire) */
    deleteAbsencesByAbsence5(options = {}) {
      return request("DELETE", "/absences/{absence}", options);
    },

    /** Annuler une absence (employe proprietaire) */
    deleteAbsencesByAbsence6(options = {}) {
      return request("DELETE", "/absences/{absence}", options);
    },

    /** Annuler une absence (employe proprietaire) */
    deleteAbsencesByAbsence7(options = {}) {
      return request("DELETE", "/absences/{absence}", options);
    },

    /** Annuler une absence (employe proprietaire) */
    deleteAbsencesByAbsence8(options = {}) {
      return request("DELETE", "/absences/{absence}", options);
    },

    /** Voir une absence */
    getAbsencesByAbsence(options = {}) {
      return request("GET", "/absences/{absence}", options);
    },

    /** Voir une absence */
    getAbsencesByAbsence2(options = {}) {
      return request("GET", "/absences/{absence}", options);
    },

    /** Voir une absence */
    getAbsencesByAbsence3(options = {}) {
      return request("GET", "/absences/{absence}", options);
    },

    /** Voir une absence */
    getAbsencesByAbsence4(options = {}) {
      return request("GET", "/absences/{absence}", options);
    },

    /** Voir une absence */
    getAbsencesByAbsence5(options = {}) {
      return request("GET", "/absences/{absence}", options);
    },

    /** Voir une absence */
    getAbsencesByAbsence6(options = {}) {
      return request("GET", "/absences/{absence}", options);
    },

    /** Voir une absence */
    getAbsencesByAbsence7(options = {}) {
      return request("GET", "/absences/{absence}", options);
    },

    /** Voir une absence */
    getAbsencesByAbsence8(options = {}) {
      return request("GET", "/absences/{absence}", options);
    },

    /** Modifier une demande d'absence en attente (dates/raison) */
    putAbsencesByAbsence(options = {}) {
      return request("PUT", "/absences/{absence}", options);
    },

    /** Modifier une demande d'absence en attente (dates/raison) */
    putAbsencesByAbsence2(options = {}) {
      return request("PUT", "/absences/{absence}", options);
    },

    /** Modifier une demande d'absence en attente (dates/raison) */
    putAbsencesByAbsence3(options = {}) {
      return request("PUT", "/absences/{absence}", options);
    },

    /** Modifier une demande d'absence en attente (dates/raison) */
    putAbsencesByAbsence4(options = {}) {
      return request("PUT", "/absences/{absence}", options);
    },

    /** Modifier une demande d'absence en attente (dates/raison) */
    putAbsencesByAbsence5(options = {}) {
      return request("PUT", "/absences/{absence}", options);
    },

    /** Modifier une demande d'absence en attente (dates/raison) */
    putAbsencesByAbsence6(options = {}) {
      return request("PUT", "/absences/{absence}", options);
    },

    /** Modifier une demande d'absence en attente (dates/raison) */
    putAbsencesByAbsence7(options = {}) {
      return request("PUT", "/absences/{absence}", options);
    },

    /** Modifier une demande d'absence en attente (dates/raison) */
    putAbsencesByAbsence8(options = {}) {
      return request("PUT", "/absences/{absence}", options);
    },

    /** Approuver une absence (manager) */
    postAbsencesByAbsenceApprove(options = {}) {
      return request("POST", "/absences/{absence}/approve", options);
    },

    /** Approuver une absence (manager) */
    postAbsencesByAbsenceApprove2(options = {}) {
      return request("POST", "/absences/{absence}/approve", options);
    },

    /** Approuver une absence (manager) */
    postAbsencesByAbsenceApprove3(options = {}) {
      return request("POST", "/absences/{absence}/approve", options);
    },

    /** Approuver une absence (manager) */
    postAbsencesByAbsenceApprove4(options = {}) {
      return request("POST", "/absences/{absence}/approve", options);
    },

    /** Approuver une absence (manager) */
    postAbsencesByAbsenceApprove5(options = {}) {
      return request("POST", "/absences/{absence}/approve", options);
    },

    /** Approuver une absence (manager) */
    postAbsencesByAbsenceApprove6(options = {}) {
      return request("POST", "/absences/{absence}/approve", options);
    },

    /** Approuver une absence (manager) */
    postAbsencesByAbsenceApprove7(options = {}) {
      return request("POST", "/absences/{absence}/approve", options);
    },

    /** Approuver une absence (manager) */
    postAbsencesByAbsenceApprove8(options = {}) {
      return request("POST", "/absences/{absence}/approve", options);
    },

    /** Approuver une absence (déprécié — utiliser POST) */
    putAbsencesByAbsenceApprove(options = {}) {
      return request("PUT", "/absences/{absence}/approve", options);
    },

    /** Approuver une absence (déprécié — utiliser POST) */
    putAbsencesByAbsenceApprove2(options = {}) {
      return request("PUT", "/absences/{absence}/approve", options);
    },

    /** Approuver une absence (déprécié — utiliser POST) */
    putAbsencesByAbsenceApprove3(options = {}) {
      return request("PUT", "/absences/{absence}/approve", options);
    },

    /** Approuver une absence (déprécié — utiliser POST) */
    putAbsencesByAbsenceApprove4(options = {}) {
      return request("PUT", "/absences/{absence}/approve", options);
    },

    /** Approuver une absence (déprécié — utiliser POST) */
    putAbsencesByAbsenceApprove5(options = {}) {
      return request("PUT", "/absences/{absence}/approve", options);
    },

    /** Approuver une absence (déprécié — utiliser POST) */
    putAbsencesByAbsenceApprove6(options = {}) {
      return request("PUT", "/absences/{absence}/approve", options);
    },

    /** Approuver une absence (déprécié — utiliser POST) */
    putAbsencesByAbsenceApprove7(options = {}) {
      return request("PUT", "/absences/{absence}/approve", options);
    },

    /** Approuver une absence (déprécié — utiliser POST) */
    putAbsencesByAbsenceApprove8(options = {}) {
      return request("PUT", "/absences/{absence}/approve", options);
    },

    /** Telecharger la piece jointe d'une demande d'absence */
    getAbsencesByAbsenceProof(options = {}) {
      return request("GET", "/absences/{absence}/proof", options);
    },

    /** Telecharger la piece jointe d'une demande d'absence */
    getAbsencesByAbsenceProof2(options = {}) {
      return request("GET", "/absences/{absence}/proof", options);
    },

    /** Telecharger la piece jointe d'une demande d'absence */
    getAbsencesByAbsenceProof3(options = {}) {
      return request("GET", "/absences/{absence}/proof", options);
    },

    /** Telecharger la piece jointe d'une demande d'absence */
    getAbsencesByAbsenceProof4(options = {}) {
      return request("GET", "/absences/{absence}/proof", options);
    },

    /** Telecharger la piece jointe d'une demande d'absence */
    getAbsencesByAbsenceProof5(options = {}) {
      return request("GET", "/absences/{absence}/proof", options);
    },

    /** Telecharger la piece jointe d'une demande d'absence */
    getAbsencesByAbsenceProof6(options = {}) {
      return request("GET", "/absences/{absence}/proof", options);
    },

    /** Telecharger la piece jointe d'une demande d'absence */
    getAbsencesByAbsenceProof7(options = {}) {
      return request("GET", "/absences/{absence}/proof", options);
    },

    /** Telecharger la piece jointe d'une demande d'absence */
    getAbsencesByAbsenceProof8(options = {}) {
      return request("GET", "/absences/{absence}/proof", options);
    },

    /** Telecharger la piece jointe d'une demande d'absence */
    getAbsencesByAbsenceProof9(options = {}) {
      return request("GET", "/absences/{absence}/proof", options);
    },

    /** Telecharger la piece jointe d'une demande d'absence */
    getAbsencesByAbsenceProof10(options = {}) {
      return request("GET", "/absences/{absence}/proof", options);
    },

    /** Telecharger la piece jointe d'une demande d'absence */
    getAbsencesByAbsenceProof11(options = {}) {
      return request("GET", "/absences/{absence}/proof", options);
    },

    /** Rejeter une absence (manager) */
    postAbsencesByAbsenceReject(options = {}) {
      return request("POST", "/absences/{absence}/reject", options);
    },

    /** Rejeter une absence (manager) */
    postAbsencesByAbsenceReject2(options = {}) {
      return request("POST", "/absences/{absence}/reject", options);
    },

    /** Rejeter une absence (manager) */
    postAbsencesByAbsenceReject3(options = {}) {
      return request("POST", "/absences/{absence}/reject", options);
    },

    /** Rejeter une absence (manager) */
    postAbsencesByAbsenceReject4(options = {}) {
      return request("POST", "/absences/{absence}/reject", options);
    },

    /** Rejeter une absence (manager) */
    postAbsencesByAbsenceReject5(options = {}) {
      return request("POST", "/absences/{absence}/reject", options);
    },

    /** Rejeter une absence (manager) */
    postAbsencesByAbsenceReject6(options = {}) {
      return request("POST", "/absences/{absence}/reject", options);
    },

    /** Rejeter une absence (manager) */
    postAbsencesByAbsenceReject7(options = {}) {
      return request("POST", "/absences/{absence}/reject", options);
    },

    /** Rejeter une absence (manager) */
    postAbsencesByAbsenceReject8(options = {}) {
      return request("POST", "/absences/{absence}/reject", options);
    },

    /** Refuser une absence (déprécié — utiliser POST) */
    putAbsencesByAbsenceReject(options = {}) {
      return request("PUT", "/absences/{absence}/reject", options);
    },

    /** Refuser une absence (déprécié — utiliser POST) */
    putAbsencesByAbsenceReject2(options = {}) {
      return request("PUT", "/absences/{absence}/reject", options);
    },

    /** Refuser une absence (déprécié — utiliser POST) */
    putAbsencesByAbsenceReject3(options = {}) {
      return request("PUT", "/absences/{absence}/reject", options);
    },

    /** Refuser une absence (déprécié — utiliser POST) */
    putAbsencesByAbsenceReject4(options = {}) {
      return request("PUT", "/absences/{absence}/reject", options);
    },

    /** Refuser une absence (déprécié — utiliser POST) */
    putAbsencesByAbsenceReject5(options = {}) {
      return request("PUT", "/absences/{absence}/reject", options);
    },

    /** Refuser une absence (déprécié — utiliser POST) */
    putAbsencesByAbsenceReject6(options = {}) {
      return request("PUT", "/absences/{absence}/reject", options);
    },

    /** Refuser une absence (déprécié — utiliser POST) */
    putAbsencesByAbsenceReject7(options = {}) {
      return request("PUT", "/absences/{absence}/reject", options);
    },

    /** Refuser une absence (déprécié — utiliser POST) */
    putAbsencesByAbsenceReject8(options = {}) {
      return request("PUT", "/absences/{absence}/reject", options);
    },

    /** Etat d'activation du module Comptabilite (check-list du wizard) */
    getAccountingActivation(options = {}) {
      return request("GET", "/accounting/activation", options);
    },

    /** Executer l'activation guidee du module Comptabilite (wizard) */
    postAccountingActivation(options = {}) {
      return request("POST", "/accounting/activation", options);
    },

    /** Audit trail du module Comptabilité (qui/quoi/quand, #5273) */
    getAccountingAuditLogs(options = {}) {
      return request("GET", "/accounting/audit-logs", options);
    },

    /** Balance de vérification */
    getAccountingBalance(options = {}) {
      return request("GET", "/accounting/balance", options);
    },

    /** Lister le plan comptable */
    getAccountingChart(options = {}) {
      return request("GET", "/accounting/chart", options);
    },

    /** Créer un compte du plan comptable */
    postAccountingChart(options = {}) {
      return request("POST", "/accounting/chart", options);
    },

    /** Supprimer un compte du plan comptable */
    deleteAccountingChartByCode(options = {}) {
      return request("DELETE", "/accounting/chart/{code}", options);
    },

    /** Détail d'un compte du plan comptable */
    getAccountingChartByCode(options = {}) {
      return request("GET", "/accounting/chart/{code}", options);
    },

    /** Mettre à jour un compte du plan comptable */
    putAccountingChartByCode(options = {}) {
      return request("PUT", "/accounting/chart/{code}", options);
    },

    /** Lister les contacts client/fournisseur */
    getAccountingContacts(options = {}) {
      return request("GET", "/accounting/contacts", options);
    },

    /** Creer un contact client/fournisseur */
    postAccountingContacts(options = {}) {
      return request("POST", "/accounting/contacts", options);
    },

    /** Supprimer un contact client/fournisseur */
    deleteAccountingContactsByContact(options = {}) {
      return request("DELETE", "/accounting/contacts/{contact}", options);
    },

    /** Detail d'un contact client/fournisseur */
    getAccountingContactsByContact(options = {}) {
      return request("GET", "/accounting/contacts/{contact}", options);
    },

    /** Mettre a jour un contact client/fournisseur */
    putAccountingContactsByContact(options = {}) {
      return request("PUT", "/accounting/contacts/{contact}", options);
    },

    /** Convertir un montant entre deux devises (multi-devises #5270) */
    postAccountingCurrencyConvert(options = {}) {
      return request("POST", "/accounting/currency/convert", options);
    },

    /** Tableau de bord comptable — factures emises, encaissements, impayes, depenses */
    getAccountingDashboard(options = {}) {
      return request("GET", "/accounting/dashboard", options);
    },

    /** Export CSV de la liste des impayes */
    getAccountingDashboardExport(options = {}) {
      return request("GET", "/accounting/dashboard/export", options);
    },

    /** Export CSV de la liste des impayes */
    getAccountingDashboardExport2(options = {}) {
      return request("GET", "/accounting/dashboard/export", options);
    },

    /** Lister les documents comptables du tenant (pagine, filtres, #5223) */
    getAccountingDocuments(options = {}) {
      return request("GET", "/accounting/documents", options);
    },

    /** Lister les documents comptables du tenant (pagine, filtres, #5223) */
    getAccountingDocuments2(options = {}) {
      return request("GET", "/accounting/documents", options);
    },

    /** Lister les documents comptables du tenant (pagine, filtres, #5223) */
    getAccountingDocuments3(options = {}) {
      return request("GET", "/accounting/documents", options);
    },

    /** Lister les documents comptables du tenant (pagine, filtres, #5223) */
    getAccountingDocuments4(options = {}) {
      return request("GET", "/accounting/documents", options);
    },

    /** Creer un brouillon de document numerote (facture, proforma, devis, avoir, irsaliye, recu, #5223) */
    postAccountingDocuments(options = {}) {
      return request("POST", "/accounting/documents", options);
    },

    /** Creer un brouillon de document numerote (facture, proforma, devis, avoir, irsaliye, recu, #5223) */
    postAccountingDocuments2(options = {}) {
      return request("POST", "/accounting/documents", options);
    },

    /** Creer un brouillon de document numerote (facture, proforma, devis, avoir, irsaliye, recu, #5223) */
    postAccountingDocuments3(options = {}) {
      return request("POST", "/accounting/documents", options);
    },

    /** Creer un brouillon de document numerote (facture, proforma, devis, avoir, irsaliye, recu, #5223) */
    postAccountingDocuments4(options = {}) {
      return request("POST", "/accounting/documents", options);
    },

    /** Detail d'un document comptable (lignes + paiements, #5223) */
    getAccountingDocumentsByDocument(options = {}) {
      return request("GET", "/accounting/documents/{document}", options);
    },

    /** Detail d'un document comptable (lignes + paiements, #5223) */
    getAccountingDocumentsByDocument2(options = {}) {
      return request("GET", "/accounting/documents/{document}", options);
    },

    /** Detail d'un document comptable (lignes + paiements, #5223) */
    getAccountingDocumentsByDocument3(options = {}) {
      return request("GET", "/accounting/documents/{document}", options);
    },

    /** Detail d'un document comptable (lignes + paiements, #5223) */
    getAccountingDocumentsByDocument4(options = {}) {
      return request("GET", "/accounting/documents/{document}", options);
    },

    /** Annuler un document non paye (#5223) */
    postAccountingDocumentsByDocumentCancel(options = {}) {
      return request("POST", "/accounting/documents/{document}/cancel", options);
    },

    /** Annuler un document non paye (#5223) */
    postAccountingDocumentsByDocumentCancel2(options = {}) {
      return request("POST", "/accounting/documents/{document}/cancel", options);
    },

    /** Annuler un document non paye (#5223) */
    postAccountingDocumentsByDocumentCancel3(options = {}) {
      return request("POST", "/accounting/documents/{document}/cancel", options);
    },

    /** Annuler un document non paye (#5223) */
    postAccountingDocumentsByDocumentCancel4(options = {}) {
      return request("POST", "/accounting/documents/{document}/cancel", options);
    },

    /** Creer un avoir lie a une facture source (#5223) */
    postAccountingDocumentsByDocumentCreditNote(options = {}) {
      return request("POST", "/accounting/documents/{document}/credit-note", options);
    },

    /** Creer un avoir lie a une facture source (#5223) */
    postAccountingDocumentsByDocumentCreditNote2(options = {}) {
      return request("POST", "/accounting/documents/{document}/credit-note", options);
    },

    /** Creer un avoir lie a une facture source (#5223) */
    postAccountingDocumentsByDocumentCreditNote3(options = {}) {
      return request("POST", "/accounting/documents/{document}/credit-note", options);
    },

    /** Creer un avoir lie a une facture source (#5223) */
    postAccountingDocumentsByDocumentCreditNote4(options = {}) {
      return request("POST", "/accounting/documents/{document}/credit-note", options);
    },

    /** Poster (ou re-poster) un document au journal */
    postAccountingDocumentsByDocumentJournal(options = {}) {
      return request("POST", "/accounting/documents/{document}/journal", options);
    },

    /** Poster (ou re-poster) un document au journal */
    postAccountingDocumentsByDocumentJournal2(options = {}) {
      return request("POST", "/accounting/documents/{document}/journal", options);
    },

    /** Poster (ou re-poster) un document au journal */
    postAccountingDocumentsByDocumentJournal3(options = {}) {
      return request("POST", "/accounting/documents/{document}/journal", options);
    },

    /** Poster (ou re-poster) un document au journal */
    postAccountingDocumentsByDocumentJournal4(options = {}) {
      return request("POST", "/accounting/documents/{document}/journal", options);
    },

    /** Enregistrer un paiement */
    postAccountingDocumentsByDocumentPayments(options = {}) {
      return request("POST", "/accounting/documents/{document}/payments", options);
    },

    /** Envoyer un brouillon (draft → sent, #5223) */
    postAccountingDocumentsByDocumentSend(options = {}) {
      return request("POST", "/accounting/documents/{document}/send", options);
    },

    /** Envoyer un brouillon (draft → sent, #5223) */
    postAccountingDocumentsByDocumentSend2(options = {}) {
      return request("POST", "/accounting/documents/{document}/send", options);
    },

    /** Envoyer un brouillon (draft → sent, #5223) */
    postAccountingDocumentsByDocumentSend3(options = {}) {
      return request("POST", "/accounting/documents/{document}/send", options);
    },

    /** Envoyer un brouillon (draft → sent, #5223) */
    postAccountingDocumentsByDocumentSend4(options = {}) {
      return request("POST", "/accounting/documents/{document}/send", options);
    },

    /** Apercu du prochain numero de la serie configuree (#5223) */
    getAccountingDocumentsNextNumber(options = {}) {
      return request("GET", "/accounting/documents/next-number", options);
    },

    /** Apercu du prochain numero de la serie configuree (#5223) */
    getAccountingDocumentsNextNumber2(options = {}) {
      return request("GET", "/accounting/documents/next-number", options);
    },

    /** Apercu du prochain numero de la serie configuree (#5223) */
    getAccountingDocumentsNextNumber3(options = {}) {
      return request("GET", "/accounting/documents/next-number", options);
    },

    /** Apercu du prochain numero de la serie configuree (#5223) */
    getAccountingDocumentsNextNumber4(options = {}) {
      return request("GET", "/accounting/documents/next-number", options);
    },

    /** Portail client — informations du document partagé (token) */
    getAccountingDocumentsSharedByToken(options = {}) {
      return request("GET", "/accounting/documents/shared/{token}", options);
    },

    /** Portail client — téléchargement du PDF partagé (token) */
    getAccountingDocumentsSharedByTokenDownload(options = {}) {
      return request("GET", "/accounting/documents/shared/{token}/download", options);
    },

    /** Portail client — téléchargement du PDF partagé (token) */
    getAccountingDocumentsSharedByTokenDownload2(options = {}) {
      return request("GET", "/accounting/documents/shared/{token}/download", options);
    },

    /** Lister les exercices comptables */
    getAccountingFiscalYears(options = {}) {
      return request("GET", "/accounting/fiscal-years", options);
    },

    /** Ouvrir un exercice comptable */
    postAccountingFiscalYears(options = {}) {
      return request("POST", "/accounting/fiscal-years", options);
    },

    /** Clôturer un exercice comptable */
    postAccountingFiscalYearsByYearClose(options = {}) {
      return request("POST", "/accounting/fiscal-years/{year}/close", options);
    },

    /** Journal comptable par periode */
    getAccountingJournal(options = {}) {
      return request("GET", "/accounting/journal", options);
    },

    /** Export FEC (Fichier des Écritures Comptables) */
    getAccountingJournalExportFec(options = {}) {
      return request("GET", "/accounting/journal/export-fec", options);
    },

    /** Export CSV du journal (expert-comptable) */
    getAccountingJournalExportCsv(options = {}) {
      return request("GET", "/accounting/journal/export.csv", options);
    },

    /** Export CSV du journal (expert-comptable) */
    getAccountingJournalExportCsv2(options = {}) {
      return request("GET", "/accounting/journal/export.csv", options);
    },

    /** Lettrage d'écritures */
    postAccountingJournalLettering(options = {}) {
      return request("POST", "/accounting/journal/lettering", options);
    },

    /** Délettrer des écritures */
    deleteAccountingJournalLetteringByLetter(options = {}) {
      return request("DELETE", "/accounting/journal/lettering/{letter}", options);
    },

    /** Cloturer une periode comptable */
    postAccountingJournalPeriodsByPeriodClose(options = {}) {
      return request("POST", "/accounting/journal/periods/{period}/close", options);
    },

    /** Cloturer une periode comptable */
    postAccountingJournalPeriodsByPeriodClose2(options = {}) {
      return request("POST", "/accounting/journal/periods/{period}/close", options);
    },

    /** Cloturer une periode comptable */
    postAccountingJournalPeriodsByPeriodClose3(options = {}) {
      return request("POST", "/accounting/journal/periods/{period}/close", options);
    },

    /** Grand livre comptable */
    getAccountingLedger(options = {}) {
      return request("GET", "/accounting/ledger", options);
    },

    /** Lister les paiements */
    getAccountingPayments(options = {}) {
      return request("GET", "/accounting/payments", options);
    },

    /** Rapprocher un paiement */
    postAccountingPaymentsByPaymentReconcile(options = {}) {
      return request("POST", "/accounting/payments/{payment}/reconcile", options);
    },

    /** Declencher les relances de paiement */
    postAccountingRemindersRun(options = {}) {
      return request("POST", "/accounting/reminders/run", options);
    },

    /** Declaration TVA simplifiee par periode */
    getAccountingReportsVatDeclaration(options = {}) {
      return request("GET", "/accounting/reports/vat-declaration", options);
    },

    /** Parametrage comptable de l'entreprise */
    getAccountingSettings(options = {}) {
      return request("GET", "/accounting/settings", options);
    },

    /** Mettre a jour le parametrage comptable de l'entreprise */
    putAccountingSettings(options = {}) {
      return request("PUT", "/accounting/settings", options);
    },

    /** Bilan comptable */
    getAccountingStatementsBalanceSheet(options = {}) {
      return request("GET", "/accounting/statements/balance-sheet", options);
    },

    /** Compte de résultat */
    getAccountingStatementsIncomeStatement(options = {}) {
      return request("GET", "/accounting/statements/income-statement", options);
    },

    /** Envoyer un message a l'assistant IA (super-admin) */
    postAdminAiChat(options = {}) {
      return request("POST", "/admin/ai/chat", options);
    },

    /** Envoyer un message a l'assistant IA (super-admin) */
    postAdminAiChat2(options = {}) {
      return request("POST", "/admin/ai/chat", options);
    },

    /** Envoyer un message a l'assistant IA (super-admin) */
    postAdminAiChat3(options = {}) {
      return request("POST", "/admin/ai/chat", options);
    },

    /** Envoyer un message a l'assistant IA (super-admin) */
    postAdminAiChat4(options = {}) {
      return request("POST", "/admin/ai/chat", options);
    },

    /** Envoyer un message a l'assistant IA (super-admin) */
    postAdminAiChat5(options = {}) {
      return request("POST", "/admin/ai/chat", options);
    },

    /** Conversations IA cross-tenant (super-admin) */
    getAdminAiConversations(options = {}) {
      return request("GET", "/admin/ai/conversations", options);
    },

    /** Conversations IA cross-tenant (super-admin) */
    getAdminAiConversations2(options = {}) {
      return request("GET", "/admin/ai/conversations", options);
    },

    /** Conversations IA cross-tenant (super-admin) */
    getAdminAiConversations3(options = {}) {
      return request("GET", "/admin/ai/conversations", options);
    },

    /** Conversations IA cross-tenant (super-admin) */
    getAdminAiConversations4(options = {}) {
      return request("GET", "/admin/ai/conversations", options);
    },

    /** Conversations IA cross-tenant (super-admin) */
    getAdminAiConversations5(options = {}) {
      return request("GET", "/admin/ai/conversations", options);
    },

    /** Messages d'une conversation IA (super-admin) */
    getAdminAiConversationsByConversationMessages(options = {}) {
      return request("GET", "/admin/ai/conversations/{conversation}/messages", options);
    },

    /** Messages d'une conversation IA (super-admin) */
    getAdminAiConversationsByConversationMessages2(options = {}) {
      return request("GET", "/admin/ai/conversations/{conversation}/messages", options);
    },

    /** Messages d'une conversation IA (super-admin) */
    getAdminAiConversationsByConversationMessages3(options = {}) {
      return request("GET", "/admin/ai/conversations/{conversation}/messages", options);
    },

    /** Messages d'une conversation IA (super-admin) */
    getAdminAiConversationsByConversationMessages4(options = {}) {
      return request("GET", "/admin/ai/conversations/{conversation}/messages", options);
    },

    /** Messages d'une conversation IA (super-admin) */
    getAdminAiConversationsByConversationMessages5(options = {}) {
      return request("GET", "/admin/ai/conversations/{conversation}/messages", options);
    },

    /** Activites recentes plateforme (cockpit super-admin) */
    getAdminDashboardActivities(options = {}) {
      return request("GET", "/admin/dashboard/activities", options);
    },

    /** Activites recentes plateforme (cockpit super-admin) */
    getAdminDashboardActivities2(options = {}) {
      return request("GET", "/admin/dashboard/activities", options);
    },

    /** Activites recentes plateforme (cockpit super-admin) */
    getAdminDashboardActivities3(options = {}) {
      return request("GET", "/admin/dashboard/activities", options);
    },

    /** Activites recentes plateforme (cockpit super-admin) */
    getAdminDashboardActivities4(options = {}) {
      return request("GET", "/admin/dashboard/activities", options);
    },

    /** Activites recentes plateforme (cockpit super-admin) */
    getAdminDashboardActivities5(options = {}) {
      return request("GET", "/admin/dashboard/activities", options);
    },

    /** Alertes systeme du cockpit super-admin */
    getAdminDashboardAlerts(options = {}) {
      return request("GET", "/admin/dashboard/alerts", options);
    },

    /** Alertes systeme du cockpit super-admin */
    getAdminDashboardAlerts2(options = {}) {
      return request("GET", "/admin/dashboard/alerts", options);
    },

    /** Alertes systeme du cockpit super-admin */
    getAdminDashboardAlerts3(options = {}) {
      return request("GET", "/admin/dashboard/alerts", options);
    },

    /** Alertes systeme du cockpit super-admin */
    getAdminDashboardAlerts4(options = {}) {
      return request("GET", "/admin/dashboard/alerts", options);
    },

    /** Alertes systeme du cockpit super-admin */
    getAdminDashboardAlerts5(options = {}) {
      return request("GET", "/admin/dashboard/alerts", options);
    },

    /** Masquer une alerte du cockpit (persiste le dismiss) */
    postAdminDashboardAlertsByAlertKeyDismiss(options = {}) {
      return request("POST", "/admin/dashboard/alerts/{alertKey}/dismiss", options);
    },

    /** Masquer une alerte du cockpit (persiste le dismiss) */
    postAdminDashboardAlertsByAlertKeyDismiss2(options = {}) {
      return request("POST", "/admin/dashboard/alerts/{alertKey}/dismiss", options);
    },

    /** Masquer une alerte du cockpit (persiste le dismiss) */
    postAdminDashboardAlertsByAlertKeyDismiss3(options = {}) {
      return request("POST", "/admin/dashboard/alerts/{alertKey}/dismiss", options);
    },

    /** Masquer une alerte du cockpit (persiste le dismiss) */
    postAdminDashboardAlertsByAlertKeyDismiss4(options = {}) {
      return request("POST", "/admin/dashboard/alerts/{alertKey}/dismiss", options);
    },

    /** Masquer une alerte du cockpit (persiste le dismiss) */
    postAdminDashboardAlertsByAlertKeyDismiss5(options = {}) {
      return request("POST", "/admin/dashboard/alerts/{alertKey}/dismiss", options);
    },

    /** Statistiques globales du cockpit super-admin */
    getAdminDashboardStats(options = {}) {
      return request("GET", "/admin/dashboard/stats", options);
    },

    /** Statistiques globales du cockpit super-admin */
    getAdminDashboardStats2(options = {}) {
      return request("GET", "/admin/dashboard/stats", options);
    },

    /** Statistiques globales du cockpit super-admin */
    getAdminDashboardStats3(options = {}) {
      return request("GET", "/admin/dashboard/stats", options);
    },

    /** Statistiques globales du cockpit super-admin */
    getAdminDashboardStats4(options = {}) {
      return request("GET", "/admin/dashboard/stats", options);
    },

    /** Statistiques globales du cockpit super-admin */
    getAdminDashboardStats5(options = {}) {
      return request("GET", "/admin/dashboard/stats", options);
    },

    /** Lister les nœuds Edge de tous les tenants (super-admin) */
    getAdminEdgeNodes(options = {}) {
      return request("GET", "/admin/edge-nodes", options);
    },

    /** Lister les nœuds Edge de tous les tenants (super-admin) */
    getAdminEdgeNodes2(options = {}) {
      return request("GET", "/admin/edge-nodes", options);
    },

    /** Lister les nœuds Edge de tous les tenants (super-admin) */
    getAdminEdgeNodes3(options = {}) {
      return request("GET", "/admin/edge-nodes", options);
    },

    /** Lister les nœuds Edge de tous les tenants (super-admin) */
    getAdminEdgeNodes4(options = {}) {
      return request("GET", "/admin/edge-nodes", options);
    },

    /** Lister les nœuds Edge de tous les tenants (super-admin) */
    getAdminEdgeNodes5(options = {}) {
      return request("GET", "/admin/edge-nodes", options);
    },

    /** Révoquer un nœud Edge et sa licence (super-admin) */
    postAdminEdgeNodesByNodeIdRevoke(options = {}) {
      return request("POST", "/admin/edge-nodes/{nodeId}/revoke", options);
    },

    /** Révoquer un nœud Edge et sa licence (super-admin) */
    postAdminEdgeNodesByNodeIdRevoke2(options = {}) {
      return request("POST", "/admin/edge-nodes/{nodeId}/revoke", options);
    },

    /** Révoquer un nœud Edge et sa licence (super-admin) */
    postAdminEdgeNodesByNodeIdRevoke3(options = {}) {
      return request("POST", "/admin/edge-nodes/{nodeId}/revoke", options);
    },

    /** Révoquer un nœud Edge et sa licence (super-admin) */
    postAdminEdgeNodesByNodeIdRevoke4(options = {}) {
      return request("POST", "/admin/edge-nodes/{nodeId}/revoke", options);
    },

    /** Révoquer un nœud Edge et sa licence (super-admin) */
    postAdminEdgeNodesByNodeIdRevoke5(options = {}) {
      return request("POST", "/admin/edge-nodes/{nodeId}/revoke", options);
    },

    /** Forcer la synchronisation d'un nœud Edge (super-admin) */
    postAdminEdgeNodesByNodeIdSync(options = {}) {
      return request("POST", "/admin/edge-nodes/{nodeId}/sync", options);
    },

    /** Forcer la synchronisation d'un nœud Edge (super-admin) */
    postAdminEdgeNodesByNodeIdSync2(options = {}) {
      return request("POST", "/admin/edge-nodes/{nodeId}/sync", options);
    },

    /** Forcer la synchronisation d'un nœud Edge (super-admin) */
    postAdminEdgeNodesByNodeIdSync3(options = {}) {
      return request("POST", "/admin/edge-nodes/{nodeId}/sync", options);
    },

    /** Forcer la synchronisation d'un nœud Edge (super-admin) */
    postAdminEdgeNodesByNodeIdSync4(options = {}) {
      return request("POST", "/admin/edge-nodes/{nodeId}/sync", options);
    },

    /** Forcer la synchronisation d'un nœud Edge (super-admin) */
    postAdminEdgeNodesByNodeIdSync5(options = {}) {
      return request("POST", "/admin/edge-nodes/{nodeId}/sync", options);
    },

    /** Alertes flotte cross-tenant (super-admin) */
    getAdminFleetAlerts(options = {}) {
      return request("GET", "/admin/fleet/alerts", options);
    },

    /** Alertes flotte cross-tenant (super-admin) */
    getAdminFleetAlerts2(options = {}) {
      return request("GET", "/admin/fleet/alerts", options);
    },

    /** Alertes flotte cross-tenant (super-admin) */
    getAdminFleetAlerts3(options = {}) {
      return request("GET", "/admin/fleet/alerts", options);
    },

    /** Alertes flotte cross-tenant (super-admin) */
    getAdminFleetAlerts4(options = {}) {
      return request("GET", "/admin/fleet/alerts", options);
    },

    /** Alertes flotte cross-tenant (super-admin) */
    getAdminFleetAlerts5(options = {}) {
      return request("GET", "/admin/fleet/alerts", options);
    },

    /** Rapports RH cross-tenant (super-admin) */
    getAdminHrReports(options = {}) {
      return request("GET", "/admin/hr-reports", options);
    },

    /** Rapports RH cross-tenant (super-admin) */
    getAdminHrReports2(options = {}) {
      return request("GET", "/admin/hr-reports", options);
    },

    /** Rapports RH cross-tenant (super-admin) */
    getAdminHrReports3(options = {}) {
      return request("GET", "/admin/hr-reports", options);
    },

    /** Rapports RH cross-tenant (super-admin) */
    getAdminHrReports4(options = {}) {
      return request("GET", "/admin/hr-reports", options);
    },

    /** Rapports RH cross-tenant (super-admin) */
    getAdminHrReports5(options = {}) {
      return request("GET", "/admin/hr-reports", options);
    },

    /** Lister les sessions d'impersonation super-admin */
    getAdminImpersonations(options = {}) {
      return request("GET", "/admin/impersonations", options);
    },

    /** Lister les sessions d'impersonation super-admin */
    getAdminImpersonations2(options = {}) {
      return request("GET", "/admin/impersonations", options);
    },

    /** Lister les sessions d'impersonation super-admin */
    getAdminImpersonations3(options = {}) {
      return request("GET", "/admin/impersonations", options);
    },

    /** Lister les sessions d'impersonation super-admin */
    getAdminImpersonations4(options = {}) {
      return request("GET", "/admin/impersonations", options);
    },

    /** Lister les sessions d'impersonation super-admin */
    getAdminImpersonations5(options = {}) {
      return request("GET", "/admin/impersonations", options);
    },

    /** Creer une session d'impersonation (login en tant que) */
    postAdminImpersonations(options = {}) {
      return request("POST", "/admin/impersonations", options);
    },

    /** Creer une session d'impersonation (login en tant que) */
    postAdminImpersonations2(options = {}) {
      return request("POST", "/admin/impersonations", options);
    },

    /** Creer une session d'impersonation (login en tant que) */
    postAdminImpersonations3(options = {}) {
      return request("POST", "/admin/impersonations", options);
    },

    /** Creer une session d'impersonation (login en tant que) */
    postAdminImpersonations4(options = {}) {
      return request("POST", "/admin/impersonations", options);
    },

    /** Creer une session d'impersonation (login en tant que) */
    postAdminImpersonations5(options = {}) {
      return request("POST", "/admin/impersonations", options);
    },

    /** Terminer une session d'impersonation */
    deleteAdminImpersonationsBySession(options = {}) {
      return request("DELETE", "/admin/impersonations/{session}", options);
    },

    /** Terminer une session d'impersonation */
    deleteAdminImpersonationsBySession2(options = {}) {
      return request("DELETE", "/admin/impersonations/{session}", options);
    },

    /** Terminer une session d'impersonation */
    deleteAdminImpersonationsBySession3(options = {}) {
      return request("DELETE", "/admin/impersonations/{session}", options);
    },

    /** Terminer une session d'impersonation */
    deleteAdminImpersonationsBySession4(options = {}) {
      return request("DELETE", "/admin/impersonations/{session}", options);
    },

    /** Terminer une session d'impersonation */
    deleteAdminImpersonationsBySession5(options = {}) {
      return request("DELETE", "/admin/impersonations/{session}", options);
    },

    /** Lister les fêtes islamiques d'une année (super-admin) */
    listIslamicCalendar(options = {}) {
      return request("GET", "/admin/islamic-calendar", options);
    },

    /** Lister les fêtes islamiques d'une année (super-admin) */
    listIslamicCalendar2(options = {}) {
      return request("GET", "/admin/islamic-calendar", options);
    },

    /** Lister les fêtes islamiques d'une année (super-admin) */
    listIslamicCalendar3(options = {}) {
      return request("GET", "/admin/islamic-calendar", options);
    },

    /** Lister les fêtes islamiques d'une année (super-admin) */
    listIslamicCalendar4(options = {}) {
      return request("GET", "/admin/islamic-calendar", options);
    },

    /** Lister les fêtes islamiques d'une année (super-admin) */
    listIslamicCalendar5(options = {}) {
      return request("GET", "/admin/islamic-calendar", options);
    },

    /** Lister les fêtes islamiques d'une année (super-admin) */
    listIslamicCalendar6(options = {}) {
      return request("GET", "/admin/islamic-calendar", options);
    },

    /** Lister les fêtes islamiques d'une année (super-admin) */
    listIslamicCalendar7(options = {}) {
      return request("GET", "/admin/islamic-calendar", options);
    },

    /** Lister les fêtes islamiques d'une année (super-admin) */
    listIslamicCalendar8(options = {}) {
      return request("GET", "/admin/islamic-calendar", options);
    },

    /** Lister les fêtes islamiques d'une année (super-admin) */
    listIslamicCalendar9(options = {}) {
      return request("GET", "/admin/islamic-calendar", options);
    },

    /** Lister les fêtes islamiques d'une année (super-admin) */
    listIslamicCalendar10(options = {}) {
      return request("GET", "/admin/islamic-calendar", options);
    },

    /** Modifier/confirmer une fête islamique (super-admin) */
    updateIslamicCalendarHoliday(options = {}) {
      return request("PUT", "/admin/islamic-calendar/{holidayKey}/{year}", options);
    },

    /** Modifier/confirmer une fête islamique (super-admin) */
    updateIslamicCalendarHoliday2(options = {}) {
      return request("PUT", "/admin/islamic-calendar/{holidayKey}/{year}", options);
    },

    /** Modifier/confirmer une fête islamique (super-admin) */
    updateIslamicCalendarHoliday3(options = {}) {
      return request("PUT", "/admin/islamic-calendar/{holidayKey}/{year}", options);
    },

    /** Modifier/confirmer une fête islamique (super-admin) */
    updateIslamicCalendarHoliday4(options = {}) {
      return request("PUT", "/admin/islamic-calendar/{holidayKey}/{year}", options);
    },

    /** Modifier/confirmer une fête islamique (super-admin) */
    updateIslamicCalendarHoliday5(options = {}) {
      return request("PUT", "/admin/islamic-calendar/{holidayKey}/{year}", options);
    },

    /** Modifier/confirmer une fête islamique (super-admin) */
    updateIslamicCalendarHoliday6(options = {}) {
      return request("PUT", "/admin/islamic-calendar/{holidayKey}/{year}", options);
    },

    /** Modifier/confirmer une fête islamique (super-admin) */
    updateIslamicCalendarHoliday7(options = {}) {
      return request("PUT", "/admin/islamic-calendar/{holidayKey}/{year}", options);
    },

    /** Modifier/confirmer une fête islamique (super-admin) */
    updateIslamicCalendarHoliday8(options = {}) {
      return request("PUT", "/admin/islamic-calendar/{holidayKey}/{year}", options);
    },

    /** Modifier/confirmer une fête islamique (super-admin) */
    updateIslamicCalendarHoliday9(options = {}) {
      return request("PUT", "/admin/islamic-calendar/{holidayKey}/{year}", options);
    },

    /** Modifier/confirmer une fête islamique (super-admin) */
    updateIslamicCalendarHoliday10(options = {}) {
      return request("PUT", "/admin/islamic-calendar/{holidayKey}/{year}", options);
    },

    /** Confirmer toutes les dates islamiques d'une année (super-admin) */
    confirmIslamicCalendarYear(options = {}) {
      return request("POST", "/admin/islamic-calendar/confirm-year/{year}", options);
    },

    /** Confirmer toutes les dates islamiques d'une année (super-admin) */
    confirmIslamicCalendarYear2(options = {}) {
      return request("POST", "/admin/islamic-calendar/confirm-year/{year}", options);
    },

    /** Confirmer toutes les dates islamiques d'une année (super-admin) */
    confirmIslamicCalendarYear3(options = {}) {
      return request("POST", "/admin/islamic-calendar/confirm-year/{year}", options);
    },

    /** Confirmer toutes les dates islamiques d'une année (super-admin) */
    confirmIslamicCalendarYear4(options = {}) {
      return request("POST", "/admin/islamic-calendar/confirm-year/{year}", options);
    },

    /** Confirmer toutes les dates islamiques d'une année (super-admin) */
    confirmIslamicCalendarYear5(options = {}) {
      return request("POST", "/admin/islamic-calendar/confirm-year/{year}", options);
    },

    /** Confirmer toutes les dates islamiques d'une année (super-admin) */
    confirmIslamicCalendarYear6(options = {}) {
      return request("POST", "/admin/islamic-calendar/confirm-year/{year}", options);
    },

    /** Confirmer toutes les dates islamiques d'une année (super-admin) */
    confirmIslamicCalendarYear7(options = {}) {
      return request("POST", "/admin/islamic-calendar/confirm-year/{year}", options);
    },

    /** Confirmer toutes les dates islamiques d'une année (super-admin) */
    confirmIslamicCalendarYear8(options = {}) {
      return request("POST", "/admin/islamic-calendar/confirm-year/{year}", options);
    },

    /** Confirmer toutes les dates islamiques d'une année (super-admin) */
    confirmIslamicCalendarYear9(options = {}) {
      return request("POST", "/admin/islamic-calendar/confirm-year/{year}", options);
    },

    /** Confirmer toutes les dates islamiques d'une année (super-admin) */
    confirmIslamicCalendarYear10(options = {}) {
      return request("POST", "/admin/islamic-calendar/confirm-year/{year}", options);
    },

    /** Lister les enregistrements d'audit des calculs de paie (platform_admin, cross-tenant) — issue #1874 */
    listPayrollAuditsAdmin(options = {}) {
      return request("GET", "/admin/payroll/audit", options);
    },

    /** Lister les enregistrements d'audit des calculs de paie (platform_admin, cross-tenant) — issue #1874 */
    listPayrollAuditsAdmin2(options = {}) {
      return request("GET", "/admin/payroll/audit", options);
    },

    /** Lister les enregistrements d'audit des calculs de paie (platform_admin, cross-tenant) — issue #1874 */
    listPayrollAuditsAdmin3(options = {}) {
      return request("GET", "/admin/payroll/audit", options);
    },

    /** Lister les enregistrements d'audit des calculs de paie (platform_admin, cross-tenant) — issue #1874 */
    listPayrollAuditsAdmin4(options = {}) {
      return request("GET", "/admin/payroll/audit", options);
    },

    /** Lister les enregistrements d'audit des calculs de paie (platform_admin, cross-tenant) — issue #1874 */
    listPayrollAuditsAdmin5(options = {}) {
      return request("GET", "/admin/payroll/audit", options);
    },

    /** Détail d'un enregistrement d'audit de calcul de paie (platform_admin) — issue #1874 */
    getPayrollAuditAdmin(options = {}) {
      return request("GET", "/admin/payroll/audit/{correlationId}", options);
    },

    /** Détail d'un enregistrement d'audit de calcul de paie (platform_admin) — issue #1874 */
    getPayrollAuditAdmin2(options = {}) {
      return request("GET", "/admin/payroll/audit/{correlationId}", options);
    },

    /** Détail d'un enregistrement d'audit de calcul de paie (platform_admin) — issue #1874 */
    getPayrollAuditAdmin3(options = {}) {
      return request("GET", "/admin/payroll/audit/{correlationId}", options);
    },

    /** Détail d'un enregistrement d'audit de calcul de paie (platform_admin) — issue #1874 */
    getPayrollAuditAdmin4(options = {}) {
      return request("GET", "/admin/payroll/audit/{correlationId}", options);
    },

    /** Détail d'un enregistrement d'audit de calcul de paie (platform_admin) — issue #1874 */
    getPayrollAuditAdmin5(options = {}) {
      return request("GET", "/admin/payroll/audit/{correlationId}", options);
    },

    /** Simuler l'impact d'un barème (platform_admin, dry-run) — issue #1814 */
    simulatePayrollAdmin(options = {}) {
      return request("POST", "/admin/payroll/simulate", options);
    },

    /** Simuler l'impact d'un barème (platform_admin, dry-run) — issue #1814 */
    simulatePayrollAdmin2(options = {}) {
      return request("POST", "/admin/payroll/simulate", options);
    },

    /** Simuler l'impact d'un barème (platform_admin, dry-run) — issue #1814 */
    simulatePayrollAdmin3(options = {}) {
      return request("POST", "/admin/payroll/simulate", options);
    },

    /** Simuler l'impact d'un barème (platform_admin, dry-run) — issue #1814 */
    simulatePayrollAdmin4(options = {}) {
      return request("POST", "/admin/payroll/simulate", options);
    },

    /** Simuler l'impact d'un barème (platform_admin, dry-run) — issue #1814 */
    simulatePayrollAdmin5(options = {}) {
      return request("POST", "/admin/payroll/simulate", options);
    },

    /** Config OAuth marketing (super-admin) */
    getAdminPlatformMarketingOauthConfig(options = {}) {
      return request("GET", "/admin/platform/marketing/oauth-config", options);
    },

    /** Config OAuth marketing (super-admin) */
    getAdminPlatformMarketingOauthConfig2(options = {}) {
      return request("GET", "/admin/platform/marketing/oauth-config", options);
    },

    /** Config OAuth marketing (super-admin) */
    getAdminPlatformMarketingOauthConfig3(options = {}) {
      return request("GET", "/admin/platform/marketing/oauth-config", options);
    },

    /** Config OAuth marketing (super-admin) */
    getAdminPlatformMarketingOauthConfig4(options = {}) {
      return request("GET", "/admin/platform/marketing/oauth-config", options);
    },

    /** Config OAuth marketing (super-admin) */
    getAdminPlatformMarketingOauthConfig5(options = {}) {
      return request("GET", "/admin/platform/marketing/oauth-config", options);
    },

    /** Enregistrer la config OAuth d'un provider marketing */
    putAdminPlatformMarketingOauthConfig(options = {}) {
      return request("PUT", "/admin/platform/marketing/oauth-config", options);
    },

    /** Enregistrer la config OAuth d'un provider marketing */
    putAdminPlatformMarketingOauthConfig2(options = {}) {
      return request("PUT", "/admin/platform/marketing/oauth-config", options);
    },

    /** Enregistrer la config OAuth d'un provider marketing */
    putAdminPlatformMarketingOauthConfig3(options = {}) {
      return request("PUT", "/admin/platform/marketing/oauth-config", options);
    },

    /** Enregistrer la config OAuth d'un provider marketing */
    putAdminPlatformMarketingOauthConfig4(options = {}) {
      return request("PUT", "/admin/platform/marketing/oauth-config", options);
    },

    /** Enregistrer la config OAuth d'un provider marketing */
    putAdminPlatformMarketingOauthConfig5(options = {}) {
      return request("PUT", "/admin/platform/marketing/oauth-config", options);
    },

    /** Lister les jours fériés d'un pays/année (super-admin : national + entreprise) */
    listPublicHolidaysAdmin(options = {}) {
      return request("GET", "/admin/public-holidays", options);
    },

    /** Lister les jours fériés d'un pays/année (super-admin : national + entreprise) */
    listPublicHolidaysAdmin2(options = {}) {
      return request("GET", "/admin/public-holidays", options);
    },

    /** Lister les jours fériés d'un pays/année (super-admin : national + entreprise) */
    listPublicHolidaysAdmin3(options = {}) {
      return request("GET", "/admin/public-holidays", options);
    },

    /** Lister les jours fériés d'un pays/année (super-admin : national + entreprise) */
    listPublicHolidaysAdmin4(options = {}) {
      return request("GET", "/admin/public-holidays", options);
    },

    /** Lister les jours fériés d'un pays/année (super-admin : national + entreprise) */
    listPublicHolidaysAdmin5(options = {}) {
      return request("GET", "/admin/public-holidays", options);
    },

    /** Lister les jours fériés d'un pays/année (super-admin : national + entreprise) */
    listPublicHolidaysAdmin6(options = {}) {
      return request("GET", "/admin/public-holidays", options);
    },

    /** Lister les jours fériés d'un pays/année (super-admin : national + entreprise) */
    listPublicHolidaysAdmin7(options = {}) {
      return request("GET", "/admin/public-holidays", options);
    },

    /** Lister les jours fériés d'un pays/année (super-admin : national + entreprise) */
    listPublicHolidaysAdmin8(options = {}) {
      return request("GET", "/admin/public-holidays", options);
    },

    /** Lister les jours fériés d'un pays/année (super-admin : national + entreprise) */
    listPublicHolidaysAdmin9(options = {}) {
      return request("GET", "/admin/public-holidays", options);
    },

    /** Lister les jours fériés d'un pays/année (super-admin : national + entreprise) */
    listPublicHolidaysAdmin10(options = {}) {
      return request("GET", "/admin/public-holidays", options);
    },

    /** Créer un jour férié national (super-admin) */
    storePublicHolidayAdmin(options = {}) {
      return request("POST", "/admin/public-holidays", options);
    },

    /** Créer un jour férié national (super-admin) */
    storePublicHolidayAdmin2(options = {}) {
      return request("POST", "/admin/public-holidays", options);
    },

    /** Créer un jour férié national (super-admin) */
    storePublicHolidayAdmin3(options = {}) {
      return request("POST", "/admin/public-holidays", options);
    },

    /** Créer un jour férié national (super-admin) */
    storePublicHolidayAdmin4(options = {}) {
      return request("POST", "/admin/public-holidays", options);
    },

    /** Créer un jour férié national (super-admin) */
    storePublicHolidayAdmin5(options = {}) {
      return request("POST", "/admin/public-holidays", options);
    },

    /** Créer un jour férié national (super-admin) */
    storePublicHolidayAdmin6(options = {}) {
      return request("POST", "/admin/public-holidays", options);
    },

    /** Créer un jour férié national (super-admin) */
    storePublicHolidayAdmin7(options = {}) {
      return request("POST", "/admin/public-holidays", options);
    },

    /** Créer un jour férié national (super-admin) */
    storePublicHolidayAdmin8(options = {}) {
      return request("POST", "/admin/public-holidays", options);
    },

    /** Créer un jour férié national (super-admin) */
    storePublicHolidayAdmin9(options = {}) {
      return request("POST", "/admin/public-holidays", options);
    },

    /** Créer un jour férié national (super-admin) */
    storePublicHolidayAdmin10(options = {}) {
      return request("POST", "/admin/public-holidays", options);
    },

    /** Supprimer un jour férié (super-admin) */
    destroyPublicHolidayAdmin(options = {}) {
      return request("DELETE", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Supprimer un jour férié (super-admin) */
    destroyPublicHolidayAdmin2(options = {}) {
      return request("DELETE", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Supprimer un jour férié (super-admin) */
    destroyPublicHolidayAdmin3(options = {}) {
      return request("DELETE", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Supprimer un jour férié (super-admin) */
    destroyPublicHolidayAdmin4(options = {}) {
      return request("DELETE", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Supprimer un jour férié (super-admin) */
    destroyPublicHolidayAdmin5(options = {}) {
      return request("DELETE", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Supprimer un jour férié (super-admin) */
    destroyPublicHolidayAdmin6(options = {}) {
      return request("DELETE", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Supprimer un jour férié (super-admin) */
    destroyPublicHolidayAdmin7(options = {}) {
      return request("DELETE", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Supprimer un jour férié (super-admin) */
    destroyPublicHolidayAdmin8(options = {}) {
      return request("DELETE", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Supprimer un jour férié (super-admin) */
    destroyPublicHolidayAdmin9(options = {}) {
      return request("DELETE", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Supprimer un jour férié (super-admin) */
    destroyPublicHolidayAdmin10(options = {}) {
      return request("DELETE", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié (super-admin) */
    updatePublicHolidayAdmin(options = {}) {
      return request("PUT", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié (super-admin) */
    updatePublicHolidayAdmin2(options = {}) {
      return request("PUT", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié (super-admin) */
    updatePublicHolidayAdmin3(options = {}) {
      return request("PUT", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié (super-admin) */
    updatePublicHolidayAdmin4(options = {}) {
      return request("PUT", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié (super-admin) */
    updatePublicHolidayAdmin5(options = {}) {
      return request("PUT", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié (super-admin) */
    updatePublicHolidayAdmin6(options = {}) {
      return request("PUT", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié (super-admin) */
    updatePublicHolidayAdmin7(options = {}) {
      return request("PUT", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié (super-admin) */
    updatePublicHolidayAdmin8(options = {}) {
      return request("PUT", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié (super-admin) */
    updatePublicHolidayAdmin9(options = {}) {
      return request("PUT", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié (super-admin) */
    updatePublicHolidayAdmin10(options = {}) {
      return request("PUT", "/admin/public-holidays/{publicHoliday}", options);
    },

    /** Approuver une modification de taux (platform_admin) — issue #1813 */
    approveRateValidation(options = {}) {
      return request("PUT", "/admin/rate-validation/{table}/{id}/approve", options);
    },

    /** Approuver une modification de taux (platform_admin) — issue #1813 */
    approveRateValidation2(options = {}) {
      return request("PUT", "/admin/rate-validation/{table}/{id}/approve", options);
    },

    /** Approuver une modification de taux (platform_admin) — issue #1813 */
    approveRateValidation3(options = {}) {
      return request("PUT", "/admin/rate-validation/{table}/{id}/approve", options);
    },

    /** Approuver une modification de taux (platform_admin) — issue #1813 */
    approveRateValidation4(options = {}) {
      return request("PUT", "/admin/rate-validation/{table}/{id}/approve", options);
    },

    /** Approuver une modification de taux (platform_admin) — issue #1813 */
    approveRateValidation5(options = {}) {
      return request("PUT", "/admin/rate-validation/{table}/{id}/approve", options);
    },

    /** Rejeter une modification de taux avec motif (platform_admin) — issue #1813 */
    rejectRateValidation(options = {}) {
      return request("PUT", "/admin/rate-validation/{table}/{id}/reject", options);
    },

    /** Rejeter une modification de taux avec motif (platform_admin) — issue #1813 */
    rejectRateValidation2(options = {}) {
      return request("PUT", "/admin/rate-validation/{table}/{id}/reject", options);
    },

    /** Rejeter une modification de taux avec motif (platform_admin) — issue #1813 */
    rejectRateValidation3(options = {}) {
      return request("PUT", "/admin/rate-validation/{table}/{id}/reject", options);
    },

    /** Rejeter une modification de taux avec motif (platform_admin) — issue #1813 */
    rejectRateValidation4(options = {}) {
      return request("PUT", "/admin/rate-validation/{table}/{id}/reject", options);
    },

    /** Rejeter une modification de taux avec motif (platform_admin) — issue #1813 */
    rejectRateValidation5(options = {}) {
      return request("PUT", "/admin/rate-validation/{table}/{id}/reject", options);
    },

    /** Lister les modifications de taux en attente de validation — issue #1813 */
    listPendingRateValidation(options = {}) {
      return request("GET", "/admin/rate-validation/pending", options);
    },

    /** Lister les modifications de taux en attente de validation — issue #1813 */
    listPendingRateValidation2(options = {}) {
      return request("GET", "/admin/rate-validation/pending", options);
    },

    /** Lister les modifications de taux en attente de validation — issue #1813 */
    listPendingRateValidation3(options = {}) {
      return request("GET", "/admin/rate-validation/pending", options);
    },

    /** Lister les modifications de taux en attente de validation — issue #1813 */
    listPendingRateValidation4(options = {}) {
      return request("GET", "/admin/rate-validation/pending", options);
    },

    /** Lister les modifications de taux en attente de validation — issue #1813 */
    listPendingRateValidation5(options = {}) {
      return request("GET", "/admin/rate-validation/pending", options);
    },

    /** Lister les règles de cotisations sociales nationales (platform_admin) — issue #1815 */
    listAdminSocialContributions(options = {}) {
      return request("GET", "/admin/social-contributions", options);
    },

    /** Lister les règles de cotisations sociales nationales (platform_admin) — issue #1815 */
    listAdminSocialContributions2(options = {}) {
      return request("GET", "/admin/social-contributions", options);
    },

    /** Lister les règles de cotisations sociales nationales (platform_admin) — issue #1815 */
    listAdminSocialContributions3(options = {}) {
      return request("GET", "/admin/social-contributions", options);
    },

    /** Lister les règles de cotisations sociales nationales (platform_admin) — issue #1815 */
    listAdminSocialContributions4(options = {}) {
      return request("GET", "/admin/social-contributions", options);
    },

    /** Lister les règles de cotisations sociales nationales (platform_admin) — issue #1815 */
    listAdminSocialContributions5(options = {}) {
      return request("GET", "/admin/social-contributions", options);
    },

    /** Lister les règles de cotisations sociales nationales (platform_admin) — issue #1815 */
    listAdminSocialContributions6(options = {}) {
      return request("GET", "/admin/social-contributions", options);
    },

    /** Lister les règles de cotisations sociales nationales (platform_admin) — issue #1815 */
    listAdminSocialContributions7(options = {}) {
      return request("GET", "/admin/social-contributions", options);
    },

    /** Lister les règles de cotisations sociales nationales (platform_admin) — issue #1815 */
    listAdminSocialContributions8(options = {}) {
      return request("GET", "/admin/social-contributions", options);
    },

    /** Lister les règles de cotisations sociales nationales (platform_admin) — issue #1815 */
    listAdminSocialContributions9(options = {}) {
      return request("GET", "/admin/social-contributions", options);
    },

    /** Lister les règles de cotisations sociales nationales (platform_admin) — issue #1815 */
    listAdminSocialContributions10(options = {}) {
      return request("GET", "/admin/social-contributions", options);
    },

    /** Créer une règle de cotisation nationale (platform_admin) — issue #1815 */
    storeAdminSocialContribution(options = {}) {
      return request("POST", "/admin/social-contributions", options);
    },

    /** Créer une règle de cotisation nationale (platform_admin) — issue #1815 */
    storeAdminSocialContribution2(options = {}) {
      return request("POST", "/admin/social-contributions", options);
    },

    /** Créer une règle de cotisation nationale (platform_admin) — issue #1815 */
    storeAdminSocialContribution3(options = {}) {
      return request("POST", "/admin/social-contributions", options);
    },

    /** Créer une règle de cotisation nationale (platform_admin) — issue #1815 */
    storeAdminSocialContribution4(options = {}) {
      return request("POST", "/admin/social-contributions", options);
    },

    /** Créer une règle de cotisation nationale (platform_admin) — issue #1815 */
    storeAdminSocialContribution5(options = {}) {
      return request("POST", "/admin/social-contributions", options);
    },

    /** Créer une règle de cotisation nationale (platform_admin) — issue #1815 */
    storeAdminSocialContribution6(options = {}) {
      return request("POST", "/admin/social-contributions", options);
    },

    /** Créer une règle de cotisation nationale (platform_admin) — issue #1815 */
    storeAdminSocialContribution7(options = {}) {
      return request("POST", "/admin/social-contributions", options);
    },

    /** Créer une règle de cotisation nationale (platform_admin) — issue #1815 */
    storeAdminSocialContribution8(options = {}) {
      return request("POST", "/admin/social-contributions", options);
    },

    /** Créer une règle de cotisation nationale (platform_admin) — issue #1815 */
    storeAdminSocialContribution9(options = {}) {
      return request("POST", "/admin/social-contributions", options);
    },

    /** Créer une règle de cotisation nationale (platform_admin) — issue #1815 */
    storeAdminSocialContribution10(options = {}) {
      return request("POST", "/admin/social-contributions", options);
    },

    /** Supprimer une règle de cotisation nationale (platform_admin) — issue #1815 */
    destroyAdminSocialContribution(options = {}) {
      return request("DELETE", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Supprimer une règle de cotisation nationale (platform_admin) — issue #1815 */
    destroyAdminSocialContribution2(options = {}) {
      return request("DELETE", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Supprimer une règle de cotisation nationale (platform_admin) — issue #1815 */
    destroyAdminSocialContribution3(options = {}) {
      return request("DELETE", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Supprimer une règle de cotisation nationale (platform_admin) — issue #1815 */
    destroyAdminSocialContribution4(options = {}) {
      return request("DELETE", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Supprimer une règle de cotisation nationale (platform_admin) — issue #1815 */
    destroyAdminSocialContribution5(options = {}) {
      return request("DELETE", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Supprimer une règle de cotisation nationale (platform_admin) — issue #1815 */
    destroyAdminSocialContribution6(options = {}) {
      return request("DELETE", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Supprimer une règle de cotisation nationale (platform_admin) — issue #1815 */
    destroyAdminSocialContribution7(options = {}) {
      return request("DELETE", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Supprimer une règle de cotisation nationale (platform_admin) — issue #1815 */
    destroyAdminSocialContribution8(options = {}) {
      return request("DELETE", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Supprimer une règle de cotisation nationale (platform_admin) — issue #1815 */
    destroyAdminSocialContribution9(options = {}) {
      return request("DELETE", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Supprimer une règle de cotisation nationale (platform_admin) — issue #1815 */
    destroyAdminSocialContribution10(options = {}) {
      return request("DELETE", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Modifier une règle de cotisation nationale (platform_admin) — issue #1815 */
    updateAdminSocialContribution(options = {}) {
      return request("PUT", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Modifier une règle de cotisation nationale (platform_admin) — issue #1815 */
    updateAdminSocialContribution2(options = {}) {
      return request("PUT", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Modifier une règle de cotisation nationale (platform_admin) — issue #1815 */
    updateAdminSocialContribution3(options = {}) {
      return request("PUT", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Modifier une règle de cotisation nationale (platform_admin) — issue #1815 */
    updateAdminSocialContribution4(options = {}) {
      return request("PUT", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Modifier une règle de cotisation nationale (platform_admin) — issue #1815 */
    updateAdminSocialContribution5(options = {}) {
      return request("PUT", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Modifier une règle de cotisation nationale (platform_admin) — issue #1815 */
    updateAdminSocialContribution6(options = {}) {
      return request("PUT", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Modifier une règle de cotisation nationale (platform_admin) — issue #1815 */
    updateAdminSocialContribution7(options = {}) {
      return request("PUT", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Modifier une règle de cotisation nationale (platform_admin) — issue #1815 */
    updateAdminSocialContribution8(options = {}) {
      return request("PUT", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Modifier une règle de cotisation nationale (platform_admin) — issue #1815 */
    updateAdminSocialContribution9(options = {}) {
      return request("PUT", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Modifier une règle de cotisation nationale (platform_admin) — issue #1815 */
    updateAdminSocialContribution10(options = {}) {
      return request("PUT", "/admin/social-contributions/{socialContribution}", options);
    },

    /** Lister les barèmes fiscaux nationaux (platform_admin) — issue #1814 */
    listAdminTaxSlabs(options = {}) {
      return request("GET", "/admin/tax-slabs", options);
    },

    /** Lister les barèmes fiscaux nationaux (platform_admin) — issue #1814 */
    listAdminTaxSlabs2(options = {}) {
      return request("GET", "/admin/tax-slabs", options);
    },

    /** Lister les barèmes fiscaux nationaux (platform_admin) — issue #1814 */
    listAdminTaxSlabs3(options = {}) {
      return request("GET", "/admin/tax-slabs", options);
    },

    /** Lister les barèmes fiscaux nationaux (platform_admin) — issue #1814 */
    listAdminTaxSlabs4(options = {}) {
      return request("GET", "/admin/tax-slabs", options);
    },

    /** Lister les barèmes fiscaux nationaux (platform_admin) — issue #1814 */
    listAdminTaxSlabs5(options = {}) {
      return request("GET", "/admin/tax-slabs", options);
    },

    /** Créer une tranche nationale (platform_admin) — issue #1814 */
    storeAdminTaxSlab(options = {}) {
      return request("POST", "/admin/tax-slabs", options);
    },

    /** Créer une tranche nationale (platform_admin) — issue #1814 */
    storeAdminTaxSlab2(options = {}) {
      return request("POST", "/admin/tax-slabs", options);
    },

    /** Créer une tranche nationale (platform_admin) — issue #1814 */
    storeAdminTaxSlab3(options = {}) {
      return request("POST", "/admin/tax-slabs", options);
    },

    /** Créer une tranche nationale (platform_admin) — issue #1814 */
    storeAdminTaxSlab4(options = {}) {
      return request("POST", "/admin/tax-slabs", options);
    },

    /** Créer une tranche nationale (platform_admin) — issue #1814 */
    storeAdminTaxSlab5(options = {}) {
      return request("POST", "/admin/tax-slabs", options);
    },

    /** Supprimer une tranche nationale (platform_admin) — issue #1814 */
    destroyAdminTaxSlab(options = {}) {
      return request("DELETE", "/admin/tax-slabs/{taxSlab}", options);
    },

    /** Supprimer une tranche nationale (platform_admin) — issue #1814 */
    destroyAdminTaxSlab2(options = {}) {
      return request("DELETE", "/admin/tax-slabs/{taxSlab}", options);
    },

    /** Supprimer une tranche nationale (platform_admin) — issue #1814 */
    destroyAdminTaxSlab3(options = {}) {
      return request("DELETE", "/admin/tax-slabs/{taxSlab}", options);
    },

    /** Supprimer une tranche nationale (platform_admin) — issue #1814 */
    destroyAdminTaxSlab4(options = {}) {
      return request("DELETE", "/admin/tax-slabs/{taxSlab}", options);
    },

    /** Supprimer une tranche nationale (platform_admin) — issue #1814 */
    destroyAdminTaxSlab5(options = {}) {
      return request("DELETE", "/admin/tax-slabs/{taxSlab}", options);
    },

    /** Modifier une tranche nationale (platform_admin) — issue #1814 */
    updateAdminTaxSlab(options = {}) {
      return request("PUT", "/admin/tax-slabs/{taxSlab}", options);
    },

    /** Modifier une tranche nationale (platform_admin) — issue #1814 */
    updateAdminTaxSlab2(options = {}) {
      return request("PUT", "/admin/tax-slabs/{taxSlab}", options);
    },

    /** Modifier une tranche nationale (platform_admin) — issue #1814 */
    updateAdminTaxSlab3(options = {}) {
      return request("PUT", "/admin/tax-slabs/{taxSlab}", options);
    },

    /** Modifier une tranche nationale (platform_admin) — issue #1814 */
    updateAdminTaxSlab4(options = {}) {
      return request("PUT", "/admin/tax-slabs/{taxSlab}", options);
    },

    /** Modifier une tranche nationale (platform_admin) — issue #1814 */
    updateAdminTaxSlab5(options = {}) {
      return request("PUT", "/admin/tax-slabs/{taxSlab}", options);
    },

    /** Réinitialiser les tranches nationales d'un pays aux valeurs légales (platform_admin) — issue #1814 */
    resetAdminTaxSlabs(options = {}) {
      return request("POST", "/admin/tax-slabs/reset-defaults", options);
    },

    /** Réinitialiser les tranches nationales d'un pays aux valeurs légales (platform_admin) — issue #1814 */
    resetAdminTaxSlabs2(options = {}) {
      return request("POST", "/admin/tax-slabs/reset-defaults", options);
    },

    /** Réinitialiser les tranches nationales d'un pays aux valeurs légales (platform_admin) — issue #1814 */
    resetAdminTaxSlabs3(options = {}) {
      return request("POST", "/admin/tax-slabs/reset-defaults", options);
    },

    /** Réinitialiser les tranches nationales d'un pays aux valeurs légales (platform_admin) — issue #1814 */
    resetAdminTaxSlabs4(options = {}) {
      return request("POST", "/admin/tax-slabs/reset-defaults", options);
    },

    /** Réinitialiser les tranches nationales d'un pays aux valeurs légales (platform_admin) — issue #1814 */
    resetAdminTaxSlabs5(options = {}) {
      return request("POST", "/admin/tax-slabs/reset-defaults", options);
    },

    /** Cours de formation cross-tenant (super-admin, avec société et nombre de sessions) */
    getAdminTrainingCourses(options = {}) {
      return request("GET", "/admin/training/courses", options);
    },

    /** Cours de formation cross-tenant (super-admin, avec société et nombre de sessions) */
    getAdminTrainingCourses2(options = {}) {
      return request("GET", "/admin/training/courses", options);
    },

    /** Cours de formation cross-tenant (super-admin, avec société et nombre de sessions) */
    getAdminTrainingCourses3(options = {}) {
      return request("GET", "/admin/training/courses", options);
    },

    /** Cours de formation cross-tenant (super-admin, avec société et nombre de sessions) */
    getAdminTrainingCourses4(options = {}) {
      return request("GET", "/admin/training/courses", options);
    },

    /** Cours de formation cross-tenant (super-admin, avec société et nombre de sessions) */
    getAdminTrainingCourses5(options = {}) {
      return request("GET", "/admin/training/courses", options);
    },

    /** Inscriptions aux formations cross-tenant (super-admin) */
    getAdminTrainingEnrollments(options = {}) {
      return request("GET", "/admin/training/enrollments", options);
    },

    /** Inscriptions aux formations cross-tenant (super-admin) */
    getAdminTrainingEnrollments2(options = {}) {
      return request("GET", "/admin/training/enrollments", options);
    },

    /** Inscriptions aux formations cross-tenant (super-admin) */
    getAdminTrainingEnrollments3(options = {}) {
      return request("GET", "/admin/training/enrollments", options);
    },

    /** Inscriptions aux formations cross-tenant (super-admin) */
    getAdminTrainingEnrollments4(options = {}) {
      return request("GET", "/admin/training/enrollments", options);
    },

    /** Inscriptions aux formations cross-tenant (super-admin) */
    getAdminTrainingEnrollments5(options = {}) {
      return request("GET", "/admin/training/enrollments", options);
    },

    /** Sessions de formation cross-tenant (super-admin) */
    getAdminTrainingSessions(options = {}) {
      return request("GET", "/admin/training/sessions", options);
    },

    /** Sessions de formation cross-tenant (super-admin) */
    getAdminTrainingSessions2(options = {}) {
      return request("GET", "/admin/training/sessions", options);
    },

    /** Sessions de formation cross-tenant (super-admin) */
    getAdminTrainingSessions3(options = {}) {
      return request("GET", "/admin/training/sessions", options);
    },

    /** Sessions de formation cross-tenant (super-admin) */
    getAdminTrainingSessions4(options = {}) {
      return request("GET", "/admin/training/sessions", options);
    },

    /** Sessions de formation cross-tenant (super-admin) */
    getAdminTrainingSessions5(options = {}) {
      return request("GET", "/admin/training/sessions", options);
    },

    /** Lister les utilisateurs plateforme (super-admin) — issue #2269 */
    getAdminUsers(options = {}) {
      return request("GET", "/admin/users", options);
    },

    /** Lister les utilisateurs plateforme (super-admin) — issue #2269 */
    getAdminUsers2(options = {}) {
      return request("GET", "/admin/users", options);
    },

    /** Lister les utilisateurs plateforme (super-admin) — issue #2269 */
    getAdminUsers3(options = {}) {
      return request("GET", "/admin/users", options);
    },

    /** Lister les utilisateurs plateforme (super-admin) — issue #2269 */
    getAdminUsers4(options = {}) {
      return request("GET", "/admin/users", options);
    },

    /** Lister les utilisateurs plateforme (super-admin) — issue #2269 */
    getAdminUsers5(options = {}) {
      return request("GET", "/admin/users", options);
    },

    /** Détail d'un utilisateur plateforme (super-admin) — issue #2269 */
    getAdminUsersByUser(options = {}) {
      return request("GET", "/admin/users/{user}", options);
    },

    /** Détail d'un utilisateur plateforme (super-admin) — issue #2269 */
    getAdminUsersByUser2(options = {}) {
      return request("GET", "/admin/users/{user}", options);
    },

    /** Détail d'un utilisateur plateforme (super-admin) — issue #2269 */
    getAdminUsersByUser3(options = {}) {
      return request("GET", "/admin/users/{user}", options);
    },

    /** Détail d'un utilisateur plateforme (super-admin) — issue #2269 */
    getAdminUsersByUser4(options = {}) {
      return request("GET", "/admin/users/{user}", options);
    },

    /** Détail d'un utilisateur plateforme (super-admin) — issue #2269 */
    getAdminUsersByUser5(options = {}) {
      return request("GET", "/admin/users/{user}", options);
    },

    /** Activer/désactiver un utilisateur plateforme (super-admin) — issue #2269 */
    patchAdminUsersByUser(options = {}) {
      return request("PATCH", "/admin/users/{user}", options);
    },

    /** Activer/désactiver un utilisateur plateforme (super-admin) — issue #2269 */
    patchAdminUsersByUser2(options = {}) {
      return request("PATCH", "/admin/users/{user}", options);
    },

    /** Activer/désactiver un utilisateur plateforme (super-admin) — issue #2269 */
    patchAdminUsersByUser3(options = {}) {
      return request("PATCH", "/admin/users/{user}", options);
    },

    /** Activer/désactiver un utilisateur plateforme (super-admin) — issue #2269 */
    patchAdminUsersByUser4(options = {}) {
      return request("PATCH", "/admin/users/{user}", options);
    },

    /** Activer/désactiver un utilisateur plateforme (super-admin) — issue #2269 */
    patchAdminUsersByUser5(options = {}) {
      return request("PATCH", "/admin/users/{user}", options);
    },

    /** Endpoints webhook cross-tenant (super-admin) */
    getAdminWebhooks(options = {}) {
      return request("GET", "/admin/webhooks", options);
    },

    /** Endpoints webhook cross-tenant (super-admin) */
    getAdminWebhooks2(options = {}) {
      return request("GET", "/admin/webhooks", options);
    },

    /** Endpoints webhook cross-tenant (super-admin) */
    getAdminWebhooks3(options = {}) {
      return request("GET", "/admin/webhooks", options);
    },

    /** Endpoints webhook cross-tenant (super-admin) */
    getAdminWebhooks4(options = {}) {
      return request("GET", "/admin/webhooks", options);
    },

    /** Endpoints webhook cross-tenant (super-admin) */
    getAdminWebhooks5(options = {}) {
      return request("GET", "/admin/webhooks", options);
    },

    /** Creer un endpoint webhook */
    postAdminWebhooks(options = {}) {
      return request("POST", "/admin/webhooks", options);
    },

    /** Creer un endpoint webhook */
    postAdminWebhooks2(options = {}) {
      return request("POST", "/admin/webhooks", options);
    },

    /** Creer un endpoint webhook */
    postAdminWebhooks3(options = {}) {
      return request("POST", "/admin/webhooks", options);
    },

    /** Creer un endpoint webhook */
    postAdminWebhooks4(options = {}) {
      return request("POST", "/admin/webhooks", options);
    },

    /** Creer un endpoint webhook */
    postAdminWebhooks5(options = {}) {
      return request("POST", "/admin/webhooks", options);
    },

    /** Supprimer un endpoint webhook */
    deleteAdminWebhooksByWebhookEndpoint(options = {}) {
      return request("DELETE", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Supprimer un endpoint webhook */
    deleteAdminWebhooksByWebhookEndpoint2(options = {}) {
      return request("DELETE", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Supprimer un endpoint webhook */
    deleteAdminWebhooksByWebhookEndpoint3(options = {}) {
      return request("DELETE", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Supprimer un endpoint webhook */
    deleteAdminWebhooksByWebhookEndpoint4(options = {}) {
      return request("DELETE", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Supprimer un endpoint webhook */
    deleteAdminWebhooksByWebhookEndpoint5(options = {}) {
      return request("DELETE", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Detail d'un endpoint webhook */
    getAdminWebhooksByWebhookEndpoint(options = {}) {
      return request("GET", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Detail d'un endpoint webhook */
    getAdminWebhooksByWebhookEndpoint2(options = {}) {
      return request("GET", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Detail d'un endpoint webhook */
    getAdminWebhooksByWebhookEndpoint3(options = {}) {
      return request("GET", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Detail d'un endpoint webhook */
    getAdminWebhooksByWebhookEndpoint4(options = {}) {
      return request("GET", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Detail d'un endpoint webhook */
    getAdminWebhooksByWebhookEndpoint5(options = {}) {
      return request("GET", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Mettre a jour partiellement un endpoint webhook */
    patchAdminWebhooksByWebhookEndpoint(options = {}) {
      return request("PATCH", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Mettre a jour partiellement un endpoint webhook */
    patchAdminWebhooksByWebhookEndpoint2(options = {}) {
      return request("PATCH", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Mettre a jour partiellement un endpoint webhook */
    patchAdminWebhooksByWebhookEndpoint3(options = {}) {
      return request("PATCH", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Mettre a jour partiellement un endpoint webhook */
    patchAdminWebhooksByWebhookEndpoint4(options = {}) {
      return request("PATCH", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Mettre a jour partiellement un endpoint webhook */
    patchAdminWebhooksByWebhookEndpoint5(options = {}) {
      return request("PATCH", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Mettre a jour un endpoint webhook */
    putAdminWebhooksByWebhookEndpoint(options = {}) {
      return request("PUT", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Mettre a jour un endpoint webhook */
    putAdminWebhooksByWebhookEndpoint2(options = {}) {
      return request("PUT", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Mettre a jour un endpoint webhook */
    putAdminWebhooksByWebhookEndpoint3(options = {}) {
      return request("PUT", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Mettre a jour un endpoint webhook */
    putAdminWebhooksByWebhookEndpoint4(options = {}) {
      return request("PUT", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Mettre a jour un endpoint webhook */
    putAdminWebhooksByWebhookEndpoint5(options = {}) {
      return request("PUT", "/admin/webhooks/{webhookEndpoint}", options);
    },

    /** Livraisons webhook en echec (dead letters) */
    getAdminWebhooksByWebhookEndpointDeadLetters(options = {}) {
      return request("GET", "/admin/webhooks/{webhookEndpoint}/dead-letters", options);
    },

    /** Livraisons webhook en echec (dead letters) */
    getAdminWebhooksByWebhookEndpointDeadLetters2(options = {}) {
      return request("GET", "/admin/webhooks/{webhookEndpoint}/dead-letters", options);
    },

    /** Livraisons webhook en echec (dead letters) */
    getAdminWebhooksByWebhookEndpointDeadLetters3(options = {}) {
      return request("GET", "/admin/webhooks/{webhookEndpoint}/dead-letters", options);
    },

    /** Livraisons webhook en echec (dead letters) */
    getAdminWebhooksByWebhookEndpointDeadLetters4(options = {}) {
      return request("GET", "/admin/webhooks/{webhookEndpoint}/dead-letters", options);
    },

    /** Livraisons webhook en echec (dead letters) */
    getAdminWebhooksByWebhookEndpointDeadLetters5(options = {}) {
      return request("GET", "/admin/webhooks/{webhookEndpoint}/dead-letters", options);
    },

    /** Rejouer une livraison webhook en echec */
    postAdminWebhooksByWebhookEndpointDeadLettersByDeliveryReplay(options = {}) {
      return request("POST", "/admin/webhooks/{webhookEndpoint}/dead-letters/{delivery}/replay", options);
    },

    /** Rejouer une livraison webhook en echec */
    postAdminWebhooksByWebhookEndpointDeadLettersByDeliveryReplay2(options = {}) {
      return request("POST", "/admin/webhooks/{webhookEndpoint}/dead-letters/{delivery}/replay", options);
    },

    /** Rejouer une livraison webhook en echec */
    postAdminWebhooksByWebhookEndpointDeadLettersByDeliveryReplay3(options = {}) {
      return request("POST", "/admin/webhooks/{webhookEndpoint}/dead-letters/{delivery}/replay", options);
    },

    /** Rejouer une livraison webhook en echec */
    postAdminWebhooksByWebhookEndpointDeadLettersByDeliveryReplay4(options = {}) {
      return request("POST", "/admin/webhooks/{webhookEndpoint}/dead-letters/{delivery}/replay", options);
    },

    /** Rejouer une livraison webhook en echec */
    postAdminWebhooksByWebhookEndpointDeadLettersByDeliveryReplay5(options = {}) {
      return request("POST", "/admin/webhooks/{webhookEndpoint}/dead-letters/{delivery}/replay", options);
    },

    /** Dispatcher un evenement de test sur l'endpoint */
    postAdminWebhooksByWebhookEndpointTest(options = {}) {
      return request("POST", "/admin/webhooks/{webhookEndpoint}/test", options);
    },

    /** Dispatcher un evenement de test sur l'endpoint */
    postAdminWebhooksByWebhookEndpointTest2(options = {}) {
      return request("POST", "/admin/webhooks/{webhookEndpoint}/test", options);
    },

    /** Dispatcher un evenement de test sur l'endpoint */
    postAdminWebhooksByWebhookEndpointTest3(options = {}) {
      return request("POST", "/admin/webhooks/{webhookEndpoint}/test", options);
    },

    /** Dispatcher un evenement de test sur l'endpoint */
    postAdminWebhooksByWebhookEndpointTest4(options = {}) {
      return request("POST", "/admin/webhooks/{webhookEndpoint}/test", options);
    },

    /** Dispatcher un evenement de test sur l'endpoint */
    postAdminWebhooksByWebhookEndpointTest5(options = {}) {
      return request("POST", "/admin/webhooks/{webhookEndpoint}/test", options);
    },

    /** Evenements webhook disponibles */
    getAdminWebhooksEvents(options = {}) {
      return request("GET", "/admin/webhooks/events", options);
    },

    /** Evenements webhook disponibles */
    getAdminWebhooksEvents2(options = {}) {
      return request("GET", "/admin/webhooks/events", options);
    },

    /** Evenements webhook disponibles */
    getAdminWebhooksEvents3(options = {}) {
      return request("GET", "/admin/webhooks/events", options);
    },

    /** Evenements webhook disponibles */
    getAdminWebhooksEvents4(options = {}) {
      return request("GET", "/admin/webhooks/events", options);
    },

    /** Evenements webhook disponibles */
    getAdminWebhooksEvents5(options = {}) {
      return request("GET", "/admin/webhooks/events", options);
    },

    /** Confirmer une action IA en attente */
    postAiActionsByPendingActionIdConfirm(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/confirm", options);
    },

    /** Confirmer une action IA en attente */
    postAiActionsByPendingActionIdConfirm2(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/confirm", options);
    },

    /** Confirmer une action IA en attente */
    postAiActionsByPendingActionIdConfirm3(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/confirm", options);
    },

    /** Confirmer une action IA en attente */
    postAiActionsByPendingActionIdConfirm4(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/confirm", options);
    },

    /** Confirmer une action IA en attente */
    postAiActionsByPendingActionIdConfirm5(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/confirm", options);
    },

    /** Confirmer une action IA en attente */
    postAiActionsByPendingActionIdConfirm6(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/confirm", options);
    },

    /** Confirmer une action IA en attente */
    postAiActionsByPendingActionIdConfirm7(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/confirm", options);
    },

    /** Confirmer une action IA en attente */
    postAiActionsByPendingActionIdConfirm8(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/confirm", options);
    },

    /** Confirmer une action IA en attente */
    postAiActionsByPendingActionIdConfirm9(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/confirm", options);
    },

    /** Confirmer une action IA en attente */
    postAiActionsByPendingActionIdConfirm10(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/confirm", options);
    },

    /** Confirmer une action IA en attente */
    postAiActionsByPendingActionIdConfirm11(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/confirm", options);
    },

    /** Rejeter une action IA en attente */
    postAiActionsByPendingActionIdReject(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/reject", options);
    },

    /** Rejeter une action IA en attente */
    postAiActionsByPendingActionIdReject2(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/reject", options);
    },

    /** Rejeter une action IA en attente */
    postAiActionsByPendingActionIdReject3(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/reject", options);
    },

    /** Rejeter une action IA en attente */
    postAiActionsByPendingActionIdReject4(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/reject", options);
    },

    /** Rejeter une action IA en attente */
    postAiActionsByPendingActionIdReject5(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/reject", options);
    },

    /** Rejeter une action IA en attente */
    postAiActionsByPendingActionIdReject6(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/reject", options);
    },

    /** Rejeter une action IA en attente */
    postAiActionsByPendingActionIdReject7(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/reject", options);
    },

    /** Rejeter une action IA en attente */
    postAiActionsByPendingActionIdReject8(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/reject", options);
    },

    /** Rejeter une action IA en attente */
    postAiActionsByPendingActionIdReject9(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/reject", options);
    },

    /** Rejeter une action IA en attente */
    postAiActionsByPendingActionIdReject10(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/reject", options);
    },

    /** Rejeter une action IA en attente */
    postAiActionsByPendingActionIdReject11(options = {}) {
      return request("POST", "/ai/actions/{pendingActionId}/reject", options);
    },

    /** Lancer l'agent IA sur une requête */
    postAiAgentRun(options = {}) {
      return request("POST", "/ai/agent/run", options);
    },

    /** Lancer l'agent IA sur une requête */
    postAiAgentRun2(options = {}) {
      return request("POST", "/ai/agent/run", options);
    },

    /** Lancer l'agent IA sur une requête */
    postAiAgentRun3(options = {}) {
      return request("POST", "/ai/agent/run", options);
    },

    /** Lancer l'agent IA sur une requête */
    postAiAgentRun4(options = {}) {
      return request("POST", "/ai/agent/run", options);
    },

    /** Lancer l'agent IA sur une requête */
    postAiAgentRun5(options = {}) {
      return request("POST", "/ai/agent/run", options);
    },

    /** Lancer l'agent IA sur une requête */
    postAiAgentRun6(options = {}) {
      return request("POST", "/ai/agent/run", options);
    },

    /** Lancer l'agent IA sur une requête */
    postAiAgentRun7(options = {}) {
      return request("POST", "/ai/agent/run", options);
    },

    /** Lancer l'agent IA sur une requête */
    postAiAgentRun8(options = {}) {
      return request("POST", "/ai/agent/run", options);
    },

    /** Lancer l'agent IA sur une requête */
    postAiAgentRun9(options = {}) {
      return request("POST", "/ai/agent/run", options);
    },

    /** Lancer l'agent IA sur une requête */
    postAiAgentRun10(options = {}) {
      return request("POST", "/ai/agent/run", options);
    },

    /** Lancer l'agent IA sur une requête */
    postAiAgentRun11(options = {}) {
      return request("POST", "/ai/agent/run", options);
    },

    /** Lister les workflows d'agent disponibles */
    getAiAgentWorkflows(options = {}) {
      return request("GET", "/ai/agent/workflows", options);
    },

    /** Lister les workflows d'agent disponibles */
    getAiAgentWorkflows2(options = {}) {
      return request("GET", "/ai/agent/workflows", options);
    },

    /** Lister les workflows d'agent disponibles */
    getAiAgentWorkflows3(options = {}) {
      return request("GET", "/ai/agent/workflows", options);
    },

    /** Lister les workflows d'agent disponibles */
    getAiAgentWorkflows4(options = {}) {
      return request("GET", "/ai/agent/workflows", options);
    },

    /** Lister les workflows d'agent disponibles */
    getAiAgentWorkflows5(options = {}) {
      return request("GET", "/ai/agent/workflows", options);
    },

    /** Lister les workflows d'agent disponibles */
    getAiAgentWorkflows6(options = {}) {
      return request("GET", "/ai/agent/workflows", options);
    },

    /** Lister les workflows d'agent disponibles */
    getAiAgentWorkflows7(options = {}) {
      return request("GET", "/ai/agent/workflows", options);
    },

    /** Lister les workflows d'agent disponibles */
    getAiAgentWorkflows8(options = {}) {
      return request("GET", "/ai/agent/workflows", options);
    },

    /** Lister les workflows d'agent disponibles */
    getAiAgentWorkflows9(options = {}) {
      return request("GET", "/ai/agent/workflows", options);
    },

    /** Lister les workflows d'agent disponibles */
    getAiAgentWorkflows10(options = {}) {
      return request("GET", "/ai/agent/workflows", options);
    },

    /** Lister les workflows d'agent disponibles */
    getAiAgentWorkflows11(options = {}) {
      return request("GET", "/ai/agent/workflows", options);
    },

    /** Couts IA par periode */
    getAiAnalyticsCosts(options = {}) {
      return request("GET", "/ai/analytics/costs", options);
    },

    /** Couts IA par periode */
    getAiAnalyticsCosts2(options = {}) {
      return request("GET", "/ai/analytics/costs", options);
    },

    /** Couts IA par periode */
    getAiAnalyticsCosts3(options = {}) {
      return request("GET", "/ai/analytics/costs", options);
    },

    /** Couts IA par periode */
    getAiAnalyticsCosts4(options = {}) {
      return request("GET", "/ai/analytics/costs", options);
    },

    /** Couts IA par periode */
    getAiAnalyticsCosts5(options = {}) {
      return request("GET", "/ai/analytics/costs", options);
    },

    /** Couts IA par periode */
    getAiAnalyticsCosts6(options = {}) {
      return request("GET", "/ai/analytics/costs", options);
    },

    /** Couts IA par periode */
    getAiAnalyticsCosts7(options = {}) {
      return request("GET", "/ai/analytics/costs", options);
    },

    /** Couts IA par periode */
    getAiAnalyticsCosts8(options = {}) {
      return request("GET", "/ai/analytics/costs", options);
    },

    /** Erreurs IA recentes */
    getAiAnalyticsErrors(options = {}) {
      return request("GET", "/ai/analytics/errors", options);
    },

    /** Erreurs IA recentes */
    getAiAnalyticsErrors2(options = {}) {
      return request("GET", "/ai/analytics/errors", options);
    },

    /** Erreurs IA recentes */
    getAiAnalyticsErrors3(options = {}) {
      return request("GET", "/ai/analytics/errors", options);
    },

    /** Erreurs IA recentes */
    getAiAnalyticsErrors4(options = {}) {
      return request("GET", "/ai/analytics/errors", options);
    },

    /** Erreurs IA recentes */
    getAiAnalyticsErrors5(options = {}) {
      return request("GET", "/ai/analytics/errors", options);
    },

    /** Erreurs IA recentes */
    getAiAnalyticsErrors6(options = {}) {
      return request("GET", "/ai/analytics/errors", options);
    },

    /** Erreurs IA recentes */
    getAiAnalyticsErrors7(options = {}) {
      return request("GET", "/ai/analytics/errors", options);
    },

    /** Erreurs IA recentes */
    getAiAnalyticsErrors8(options = {}) {
      return request("GET", "/ai/analytics/errors", options);
    },

    /** Usage des outils IA */
    getAiAnalyticsTools(options = {}) {
      return request("GET", "/ai/analytics/tools", options);
    },

    /** Usage des outils IA */
    getAiAnalyticsTools2(options = {}) {
      return request("GET", "/ai/analytics/tools", options);
    },

    /** Usage des outils IA */
    getAiAnalyticsTools3(options = {}) {
      return request("GET", "/ai/analytics/tools", options);
    },

    /** Usage des outils IA */
    getAiAnalyticsTools4(options = {}) {
      return request("GET", "/ai/analytics/tools", options);
    },

    /** Usage des outils IA */
    getAiAnalyticsTools5(options = {}) {
      return request("GET", "/ai/analytics/tools", options);
    },

    /** Usage des outils IA */
    getAiAnalyticsTools6(options = {}) {
      return request("GET", "/ai/analytics/tools", options);
    },

    /** Usage des outils IA */
    getAiAnalyticsTools7(options = {}) {
      return request("GET", "/ai/analytics/tools", options);
    },

    /** Usage des outils IA */
    getAiAnalyticsTools8(options = {}) {
      return request("GET", "/ai/analytics/tools", options);
    },

    /** Metriques d'usage IA */
    getAiAnalyticsUsage(options = {}) {
      return request("GET", "/ai/analytics/usage", options);
    },

    /** Metriques d'usage IA */
    getAiAnalyticsUsage2(options = {}) {
      return request("GET", "/ai/analytics/usage", options);
    },

    /** Metriques d'usage IA */
    getAiAnalyticsUsage3(options = {}) {
      return request("GET", "/ai/analytics/usage", options);
    },

    /** Metriques d'usage IA */
    getAiAnalyticsUsage4(options = {}) {
      return request("GET", "/ai/analytics/usage", options);
    },

    /** Metriques d'usage IA */
    getAiAnalyticsUsage5(options = {}) {
      return request("GET", "/ai/analytics/usage", options);
    },

    /** Metriques d'usage IA */
    getAiAnalyticsUsage6(options = {}) {
      return request("GET", "/ai/analytics/usage", options);
    },

    /** Metriques d'usage IA */
    getAiAnalyticsUsage7(options = {}) {
      return request("GET", "/ai/analytics/usage", options);
    },

    /** Metriques d'usage IA */
    getAiAnalyticsUsage8(options = {}) {
      return request("GET", "/ai/analytics/usage", options);
    },

    /** Envoyer un message au chatbot IA */
    postAiChat(options = {}) {
      return request("POST", "/ai/chat", options);
    },

    /** Envoyer un message au chatbot IA */
    postAiChat2(options = {}) {
      return request("POST", "/ai/chat", options);
    },

    /** Envoyer un message au chatbot IA */
    postAiChat3(options = {}) {
      return request("POST", "/ai/chat", options);
    },

    /** Envoyer un message au chatbot IA */
    postAiChat4(options = {}) {
      return request("POST", "/ai/chat", options);
    },

    /** Envoyer un message au chatbot IA */
    postAiChat5(options = {}) {
      return request("POST", "/ai/chat", options);
    },

    /** Envoyer un message au chatbot IA */
    postAiChat6(options = {}) {
      return request("POST", "/ai/chat", options);
    },

    /** Envoyer un message au chatbot IA */
    postAiChat7(options = {}) {
      return request("POST", "/ai/chat", options);
    },

    /** Envoyer un message au chatbot IA */
    postAiChat8(options = {}) {
      return request("POST", "/ai/chat", options);
    },

    /** Supprimer une conversation IA */
    deleteAiChatByConversationId(options = {}) {
      return request("DELETE", "/ai/chat/{conversationId}", options);
    },

    /** Supprimer une conversation IA */
    deleteAiChatByConversationId2(options = {}) {
      return request("DELETE", "/ai/chat/{conversationId}", options);
    },

    /** Supprimer une conversation IA */
    deleteAiChatByConversationId3(options = {}) {
      return request("DELETE", "/ai/chat/{conversationId}", options);
    },

    /** Supprimer une conversation IA */
    deleteAiChatByConversationId4(options = {}) {
      return request("DELETE", "/ai/chat/{conversationId}", options);
    },

    /** Supprimer une conversation IA */
    deleteAiChatByConversationId5(options = {}) {
      return request("DELETE", "/ai/chat/{conversationId}", options);
    },

    /** Supprimer une conversation IA */
    deleteAiChatByConversationId6(options = {}) {
      return request("DELETE", "/ai/chat/{conversationId}", options);
    },

    /** Supprimer une conversation IA */
    deleteAiChatByConversationId7(options = {}) {
      return request("DELETE", "/ai/chat/{conversationId}", options);
    },

    /** Supprimer une conversation IA */
    deleteAiChatByConversationId8(options = {}) {
      return request("DELETE", "/ai/chat/{conversationId}", options);
    },

    /** Historique des conversations IA */
    getAiChatHistory(options = {}) {
      return request("GET", "/ai/chat/history", options);
    },

    /** Historique des conversations IA */
    getAiChatHistory2(options = {}) {
      return request("GET", "/ai/chat/history", options);
    },

    /** Historique des conversations IA */
    getAiChatHistory3(options = {}) {
      return request("GET", "/ai/chat/history", options);
    },

    /** Historique des conversations IA */
    getAiChatHistory4(options = {}) {
      return request("GET", "/ai/chat/history", options);
    },

    /** Historique des conversations IA */
    getAiChatHistory5(options = {}) {
      return request("GET", "/ai/chat/history", options);
    },

    /** Historique des conversations IA */
    getAiChatHistory6(options = {}) {
      return request("GET", "/ai/chat/history", options);
    },

    /** Historique des conversations IA */
    getAiChatHistory7(options = {}) {
      return request("GET", "/ai/chat/history", options);
    },

    /** Historique des conversations IA */
    getAiChatHistory8(options = {}) {
      return request("GET", "/ai/chat/history", options);
    },

    /** Liste des outils IA disponibles */
    getAiTools(options = {}) {
      return request("GET", "/ai/tools", options);
    },

    /** Liste des outils IA disponibles */
    getAiTools2(options = {}) {
      return request("GET", "/ai/tools", options);
    },

    /** Liste des outils IA disponibles */
    getAiTools3(options = {}) {
      return request("GET", "/ai/tools", options);
    },

    /** Liste des outils IA disponibles */
    getAiTools4(options = {}) {
      return request("GET", "/ai/tools", options);
    },

    /** Liste des outils IA disponibles */
    getAiTools5(options = {}) {
      return request("GET", "/ai/tools", options);
    },

    /** Liste des outils IA disponibles */
    getAiTools6(options = {}) {
      return request("GET", "/ai/tools", options);
    },

    /** Liste des outils IA disponibles */
    getAiTools7(options = {}) {
      return request("GET", "/ai/tools", options);
    },

    /** Liste des outils IA disponibles */
    getAiTools8(options = {}) {
      return request("GET", "/ai/tools", options);
    },

    /** Exécuter une commande vocale */
    postAiVoiceCommand(options = {}) {
      return request("POST", "/ai/voice/command", options);
    },

    /** Exécuter une commande vocale */
    postAiVoiceCommand2(options = {}) {
      return request("POST", "/ai/voice/command", options);
    },

    /** Exécuter une commande vocale */
    postAiVoiceCommand3(options = {}) {
      return request("POST", "/ai/voice/command", options);
    },

    /** Exécuter une commande vocale */
    postAiVoiceCommand4(options = {}) {
      return request("POST", "/ai/voice/command", options);
    },

    /** Exécuter une commande vocale */
    postAiVoiceCommand5(options = {}) {
      return request("POST", "/ai/voice/command", options);
    },

    /** Exécuter une commande vocale */
    postAiVoiceCommand6(options = {}) {
      return request("POST", "/ai/voice/command", options);
    },

    /** Exécuter une commande vocale */
    postAiVoiceCommand7(options = {}) {
      return request("POST", "/ai/voice/command", options);
    },

    /** Exécuter une commande vocale */
    postAiVoiceCommand8(options = {}) {
      return request("POST", "/ai/voice/command", options);
    },

    /** Exécuter une commande vocale */
    postAiVoiceCommand9(options = {}) {
      return request("POST", "/ai/voice/command", options);
    },

    /** Exécuter une commande vocale */
    postAiVoiceCommand10(options = {}) {
      return request("POST", "/ai/voice/command", options);
    },

    /** Exécuter une commande vocale */
    postAiVoiceCommand11(options = {}) {
      return request("POST", "/ai/voice/command", options);
    },

    /** Synthétiser un texte en audio */
    postAiVoiceSynthesize(options = {}) {
      return request("POST", "/ai/voice/synthesize", options);
    },

    /** Synthétiser un texte en audio */
    postAiVoiceSynthesize2(options = {}) {
      return request("POST", "/ai/voice/synthesize", options);
    },

    /** Synthétiser un texte en audio */
    postAiVoiceSynthesize3(options = {}) {
      return request("POST", "/ai/voice/synthesize", options);
    },

    /** Synthétiser un texte en audio */
    postAiVoiceSynthesize4(options = {}) {
      return request("POST", "/ai/voice/synthesize", options);
    },

    /** Synthétiser un texte en audio */
    postAiVoiceSynthesize5(options = {}) {
      return request("POST", "/ai/voice/synthesize", options);
    },

    /** Synthétiser un texte en audio */
    postAiVoiceSynthesize6(options = {}) {
      return request("POST", "/ai/voice/synthesize", options);
    },

    /** Synthétiser un texte en audio */
    postAiVoiceSynthesize7(options = {}) {
      return request("POST", "/ai/voice/synthesize", options);
    },

    /** Synthétiser un texte en audio */
    postAiVoiceSynthesize8(options = {}) {
      return request("POST", "/ai/voice/synthesize", options);
    },

    /** Synthétiser un texte en audio */
    postAiVoiceSynthesize9(options = {}) {
      return request("POST", "/ai/voice/synthesize", options);
    },

    /** Synthétiser un texte en audio */
    postAiVoiceSynthesize10(options = {}) {
      return request("POST", "/ai/voice/synthesize", options);
    },

    /** Synthétiser un texte en audio */
    postAiVoiceSynthesize11(options = {}) {
      return request("POST", "/ai/voice/synthesize", options);
    },

    /** Transcrire un audio (commande vocale) */
    postAiVoiceTranscribe(options = {}) {
      return request("POST", "/ai/voice/transcribe", options);
    },

    /** Transcrire un audio (commande vocale) */
    postAiVoiceTranscribe2(options = {}) {
      return request("POST", "/ai/voice/transcribe", options);
    },

    /** Transcrire un audio (commande vocale) */
    postAiVoiceTranscribe3(options = {}) {
      return request("POST", "/ai/voice/transcribe", options);
    },

    /** Transcrire un audio (commande vocale) */
    postAiVoiceTranscribe4(options = {}) {
      return request("POST", "/ai/voice/transcribe", options);
    },

    /** Transcrire un audio (commande vocale) */
    postAiVoiceTranscribe5(options = {}) {
      return request("POST", "/ai/voice/transcribe", options);
    },

    /** Transcrire un audio (commande vocale) */
    postAiVoiceTranscribe6(options = {}) {
      return request("POST", "/ai/voice/transcribe", options);
    },

    /** Transcrire un audio (commande vocale) */
    postAiVoiceTranscribe7(options = {}) {
      return request("POST", "/ai/voice/transcribe", options);
    },

    /** Transcrire un audio (commande vocale) */
    postAiVoiceTranscribe8(options = {}) {
      return request("POST", "/ai/voice/transcribe", options);
    },

    /** Transcrire un audio (commande vocale) */
    postAiVoiceTranscribe9(options = {}) {
      return request("POST", "/ai/voice/transcribe", options);
    },

    /** Transcrire un audio (commande vocale) */
    postAiVoiceTranscribe10(options = {}) {
      return request("POST", "/ai/voice/transcribe", options);
    },

    /** Transcrire un audio (commande vocale) */
    postAiVoiceTranscribe11(options = {}) {
      return request("POST", "/ai/voice/transcribe", options);
    },

    /** Workflow IA de préparation de paie */
    postAiWorkflowsPreparePayroll(options = {}) {
      return request("POST", "/ai/workflows/prepare-payroll", options);
    },

    /** Workflow IA de préparation de paie */
    postAiWorkflowsPreparePayroll2(options = {}) {
      return request("POST", "/ai/workflows/prepare-payroll", options);
    },

    /** Workflow IA de préparation de paie */
    postAiWorkflowsPreparePayroll3(options = {}) {
      return request("POST", "/ai/workflows/prepare-payroll", options);
    },

    /** Workflow IA de préparation de paie */
    postAiWorkflowsPreparePayroll4(options = {}) {
      return request("POST", "/ai/workflows/prepare-payroll", options);
    },

    /** Workflow IA de préparation de paie */
    postAiWorkflowsPreparePayroll5(options = {}) {
      return request("POST", "/ai/workflows/prepare-payroll", options);
    },

    /** Workflow IA de préparation de paie */
    postAiWorkflowsPreparePayroll6(options = {}) {
      return request("POST", "/ai/workflows/prepare-payroll", options);
    },

    /** Workflow IA de préparation de paie */
    postAiWorkflowsPreparePayroll7(options = {}) {
      return request("POST", "/ai/workflows/prepare-payroll", options);
    },

    /** Workflow IA de préparation de paie */
    postAiWorkflowsPreparePayroll8(options = {}) {
      return request("POST", "/ai/workflows/prepare-payroll", options);
    },

    /** Workflow IA de préparation de paie */
    postAiWorkflowsPreparePayroll9(options = {}) {
      return request("POST", "/ai/workflows/prepare-payroll", options);
    },

    /** Workflow IA de préparation de paie */
    postAiWorkflowsPreparePayroll10(options = {}) {
      return request("POST", "/ai/workflows/prepare-payroll", options);
    },

    /** Workflow IA de préparation de paie */
    postAiWorkflowsPreparePayroll11(options = {}) {
      return request("POST", "/ai/workflows/prepare-payroll", options);
    },

    /** Rapport hebdomadaire généré par l'IA */
    getAiWorkflowsWeeklyReport(options = {}) {
      return request("GET", "/ai/workflows/weekly-report", options);
    },

    /** Rapport hebdomadaire généré par l'IA */
    getAiWorkflowsWeeklyReport2(options = {}) {
      return request("GET", "/ai/workflows/weekly-report", options);
    },

    /** Rapport hebdomadaire généré par l'IA */
    getAiWorkflowsWeeklyReport3(options = {}) {
      return request("GET", "/ai/workflows/weekly-report", options);
    },

    /** Rapport hebdomadaire généré par l'IA */
    getAiWorkflowsWeeklyReport4(options = {}) {
      return request("GET", "/ai/workflows/weekly-report", options);
    },

    /** Rapport hebdomadaire généré par l'IA */
    getAiWorkflowsWeeklyReport5(options = {}) {
      return request("GET", "/ai/workflows/weekly-report", options);
    },

    /** Rapport hebdomadaire généré par l'IA */
    getAiWorkflowsWeeklyReport6(options = {}) {
      return request("GET", "/ai/workflows/weekly-report", options);
    },

    /** Rapport hebdomadaire généré par l'IA */
    getAiWorkflowsWeeklyReport7(options = {}) {
      return request("GET", "/ai/workflows/weekly-report", options);
    },

    /** Rapport hebdomadaire généré par l'IA */
    getAiWorkflowsWeeklyReport8(options = {}) {
      return request("GET", "/ai/workflows/weekly-report", options);
    },

    /** Rapport hebdomadaire généré par l'IA */
    getAiWorkflowsWeeklyReport9(options = {}) {
      return request("GET", "/ai/workflows/weekly-report", options);
    },

    /** Rapport hebdomadaire généré par l'IA */
    getAiWorkflowsWeeklyReport10(options = {}) {
      return request("GET", "/ai/workflows/weekly-report", options);
    },

    /** Rapport hebdomadaire généré par l'IA */
    getAiWorkflowsWeeklyReport11(options = {}) {
      return request("GET", "/ai/workflows/weekly-report", options);
    },

    /** Lister les annonces de l'entreprise */
    getAnnouncements(options = {}) {
      return request("GET", "/announcements", options);
    },

    /** Lister les annonces de l'entreprise */
    getAnnouncements2(options = {}) {
      return request("GET", "/announcements", options);
    },

    /** Lister les annonces de l'entreprise */
    getAnnouncements3(options = {}) {
      return request("GET", "/announcements", options);
    },

    /** Lister les annonces de l'entreprise */
    getAnnouncements4(options = {}) {
      return request("GET", "/announcements", options);
    },

    /** Lister les annonces de l'entreprise */
    getAnnouncements5(options = {}) {
      return request("GET", "/announcements", options);
    },

    /** Lister les annonces de l'entreprise */
    getAnnouncements6(options = {}) {
      return request("GET", "/announcements", options);
    },

    /** Lister les annonces de l'entreprise */
    getAnnouncements7(options = {}) {
      return request("GET", "/announcements", options);
    },

    /** Lister les annonces de l'entreprise */
    getAnnouncements8(options = {}) {
      return request("GET", "/announcements", options);
    },

    /** Creer une annonce d'entreprise (manager) */
    postAnnouncements(options = {}) {
      return request("POST", "/announcements", options);
    },

    /** Creer une annonce d'entreprise (manager) */
    postAnnouncements2(options = {}) {
      return request("POST", "/announcements", options);
    },

    /** Creer une annonce d'entreprise (manager) */
    postAnnouncements3(options = {}) {
      return request("POST", "/announcements", options);
    },

    /** Creer une annonce d'entreprise (manager) */
    postAnnouncements4(options = {}) {
      return request("POST", "/announcements", options);
    },

    /** Creer une annonce d'entreprise (manager) */
    postAnnouncements5(options = {}) {
      return request("POST", "/announcements", options);
    },

    /** Creer une annonce d'entreprise (manager) */
    postAnnouncements6(options = {}) {
      return request("POST", "/announcements", options);
    },

    /** Creer une annonce d'entreprise (manager) */
    postAnnouncements7(options = {}) {
      return request("POST", "/announcements", options);
    },

    /** Creer une annonce d'entreprise (manager) */
    postAnnouncements8(options = {}) {
      return request("POST", "/announcements", options);
    },

    /** Supprimer une annonce d'entreprise */
    deleteAnnouncementsByAnnouncement(options = {}) {
      return request("DELETE", "/announcements/{announcement}", options);
    },

    /** Supprimer une annonce d'entreprise */
    deleteAnnouncementsByAnnouncement2(options = {}) {
      return request("DELETE", "/announcements/{announcement}", options);
    },

    /** Supprimer une annonce d'entreprise */
    deleteAnnouncementsByAnnouncement3(options = {}) {
      return request("DELETE", "/announcements/{announcement}", options);
    },

    /** Supprimer une annonce d'entreprise */
    deleteAnnouncementsByAnnouncement4(options = {}) {
      return request("DELETE", "/announcements/{announcement}", options);
    },

    /** Supprimer une annonce d'entreprise */
    deleteAnnouncementsByAnnouncement5(options = {}) {
      return request("DELETE", "/announcements/{announcement}", options);
    },

    /** Supprimer une annonce d'entreprise */
    deleteAnnouncementsByAnnouncement6(options = {}) {
      return request("DELETE", "/announcements/{announcement}", options);
    },

    /** Supprimer une annonce d'entreprise */
    deleteAnnouncementsByAnnouncement7(options = {}) {
      return request("DELETE", "/announcements/{announcement}", options);
    },

    /** Supprimer une annonce d'entreprise */
    deleteAnnouncementsByAnnouncement8(options = {}) {
      return request("DELETE", "/announcements/{announcement}", options);
    },

    /** Annuler une annonce programmee ou publiee (moderation PA2-COMM-011) */
    postAnnouncementsByAnnouncementCancel(options = {}) {
      return request("POST", "/announcements/{announcement}/cancel", options);
    },

    /** Annuler une annonce programmee ou publiee (moderation PA2-COMM-011) */
    postAnnouncementsByAnnouncementCancel2(options = {}) {
      return request("POST", "/announcements/{announcement}/cancel", options);
    },

    /** Annuler une annonce programmee ou publiee (moderation PA2-COMM-011) */
    postAnnouncementsByAnnouncementCancel3(options = {}) {
      return request("POST", "/announcements/{announcement}/cancel", options);
    },

    /** Annuler une annonce programmee ou publiee (moderation PA2-COMM-011) */
    postAnnouncementsByAnnouncementCancel4(options = {}) {
      return request("POST", "/announcements/{announcement}/cancel", options);
    },

    /** Annuler une annonce programmee ou publiee (moderation PA2-COMM-011) */
    postAnnouncementsByAnnouncementCancel5(options = {}) {
      return request("POST", "/announcements/{announcement}/cancel", options);
    },

    /** Annuler une annonce programmee ou publiee (moderation PA2-COMM-011) */
    postAnnouncementsByAnnouncementCancel6(options = {}) {
      return request("POST", "/announcements/{announcement}/cancel", options);
    },

    /** Annuler une annonce programmee ou publiee (moderation PA2-COMM-011) */
    postAnnouncementsByAnnouncementCancel7(options = {}) {
      return request("POST", "/announcements/{announcement}/cancel", options);
    },

    /** Annuler une annonce programmee ou publiee (moderation PA2-COMM-011) */
    postAnnouncementsByAnnouncementCancel8(options = {}) {
      return request("POST", "/announcements/{announcement}/cancel", options);
    },

    /** Publier immediatement une annonce (moderation PA2-COMM-011) */
    postAnnouncementsByAnnouncementPublish(options = {}) {
      return request("POST", "/announcements/{announcement}/publish", options);
    },

    /** Publier immediatement une annonce (moderation PA2-COMM-011) */
    postAnnouncementsByAnnouncementPublish2(options = {}) {
      return request("POST", "/announcements/{announcement}/publish", options);
    },

    /** Publier immediatement une annonce (moderation PA2-COMM-011) */
    postAnnouncementsByAnnouncementPublish3(options = {}) {
      return request("POST", "/announcements/{announcement}/publish", options);
    },

    /** Publier immediatement une annonce (moderation PA2-COMM-011) */
    postAnnouncementsByAnnouncementPublish4(options = {}) {
      return request("POST", "/announcements/{announcement}/publish", options);
    },

    /** Publier immediatement une annonce (moderation PA2-COMM-011) */
    postAnnouncementsByAnnouncementPublish5(options = {}) {
      return request("POST", "/announcements/{announcement}/publish", options);
    },

    /** Publier immediatement une annonce (moderation PA2-COMM-011) */
    postAnnouncementsByAnnouncementPublish6(options = {}) {
      return request("POST", "/announcements/{announcement}/publish", options);
    },

    /** Publier immediatement une annonce (moderation PA2-COMM-011) */
    postAnnouncementsByAnnouncementPublish7(options = {}) {
      return request("POST", "/announcements/{announcement}/publish", options);
    },

    /** Publier immediatement une annonce (moderation PA2-COMM-011) */
    postAnnouncementsByAnnouncementPublish8(options = {}) {
      return request("POST", "/announcements/{announcement}/publish", options);
    },

    /** Lister les tokens API du manager (self-service) */
    getApiTokens(options = {}) {
      return request("GET", "/api-tokens", options);
    },

    /** Lister les tokens API du manager (self-service) */
    getApiTokens2(options = {}) {
      return request("GET", "/api-tokens", options);
    },

    /** Lister les tokens API du manager (self-service) */
    getApiTokens3(options = {}) {
      return request("GET", "/api-tokens", options);
    },

    /** Lister les tokens API du manager (self-service) */
    getApiTokens4(options = {}) {
      return request("GET", "/api-tokens", options);
    },

    /** Lister les tokens API du manager (self-service) */
    getApiTokens5(options = {}) {
      return request("GET", "/api-tokens", options);
    },

    /** Lister les tokens API du manager (self-service) */
    getApiTokens6(options = {}) {
      return request("GET", "/api-tokens", options);
    },

    /** Lister les tokens API du manager (self-service) */
    getApiTokens7(options = {}) {
      return request("GET", "/api-tokens", options);
    },

    /** Lister les tokens API du manager (self-service) */
    getApiTokens8(options = {}) {
      return request("GET", "/api-tokens", options);
    },

    /** Lister les tokens API du manager (self-service) */
    getApiTokens9(options = {}) {
      return request("GET", "/api-tokens", options);
    },

    /** Lister les tokens API du manager (self-service) */
    getApiTokens10(options = {}) {
      return request("GET", "/api-tokens", options);
    },

    /** Lister les tokens API du manager (self-service) */
    getApiTokens11(options = {}) {
      return request("GET", "/api-tokens", options);
    },

    /** Creer un token API (renvoye une seule fois en clair) */
    postApiTokens(options = {}) {
      return request("POST", "/api-tokens", options);
    },

    /** Creer un token API (renvoye une seule fois en clair) */
    postApiTokens2(options = {}) {
      return request("POST", "/api-tokens", options);
    },

    /** Creer un token API (renvoye une seule fois en clair) */
    postApiTokens3(options = {}) {
      return request("POST", "/api-tokens", options);
    },

    /** Creer un token API (renvoye une seule fois en clair) */
    postApiTokens4(options = {}) {
      return request("POST", "/api-tokens", options);
    },

    /** Creer un token API (renvoye une seule fois en clair) */
    postApiTokens5(options = {}) {
      return request("POST", "/api-tokens", options);
    },

    /** Creer un token API (renvoye une seule fois en clair) */
    postApiTokens6(options = {}) {
      return request("POST", "/api-tokens", options);
    },

    /** Creer un token API (renvoye une seule fois en clair) */
    postApiTokens7(options = {}) {
      return request("POST", "/api-tokens", options);
    },

    /** Creer un token API (renvoye une seule fois en clair) */
    postApiTokens8(options = {}) {
      return request("POST", "/api-tokens", options);
    },

    /** Creer un token API (renvoye une seule fois en clair) */
    postApiTokens9(options = {}) {
      return request("POST", "/api-tokens", options);
    },

    /** Creer un token API (renvoye une seule fois en clair) */
    postApiTokens10(options = {}) {
      return request("POST", "/api-tokens", options);
    },

    /** Creer un token API (renvoye une seule fois en clair) */
    postApiTokens11(options = {}) {
      return request("POST", "/api-tokens", options);
    },

    /** Revoquer un token API */
    deleteApiTokensByTokenId(options = {}) {
      return request("DELETE", "/api-tokens/{tokenId}", options);
    },

    /** Revoquer un token API */
    deleteApiTokensByTokenId2(options = {}) {
      return request("DELETE", "/api-tokens/{tokenId}", options);
    },

    /** Revoquer un token API */
    deleteApiTokensByTokenId3(options = {}) {
      return request("DELETE", "/api-tokens/{tokenId}", options);
    },

    /** Revoquer un token API */
    deleteApiTokensByTokenId4(options = {}) {
      return request("DELETE", "/api-tokens/{tokenId}", options);
    },

    /** Revoquer un token API */
    deleteApiTokensByTokenId5(options = {}) {
      return request("DELETE", "/api-tokens/{tokenId}", options);
    },

    /** Revoquer un token API */
    deleteApiTokensByTokenId6(options = {}) {
      return request("DELETE", "/api-tokens/{tokenId}", options);
    },

    /** Revoquer un token API */
    deleteApiTokensByTokenId7(options = {}) {
      return request("DELETE", "/api-tokens/{tokenId}", options);
    },

    /** Revoquer un token API */
    deleteApiTokensByTokenId8(options = {}) {
      return request("DELETE", "/api-tokens/{tokenId}", options);
    },

    /** Revoquer un token API */
    deleteApiTokensByTokenId9(options = {}) {
      return request("DELETE", "/api-tokens/{tokenId}", options);
    },

    /** Revoquer un token API */
    deleteApiTokensByTokenId10(options = {}) {
      return request("DELETE", "/api-tokens/{tokenId}", options);
    },

    /** Revoquer un token API */
    deleteApiTokensByTokenId11(options = {}) {
      return request("DELETE", "/api-tokens/{tokenId}", options);
    },

    /** Lister les workflows d'approbation */
    getApprovalWorkflows(options = {}) {
      return request("GET", "/approval-workflows", options);
    },

    /** Lister les workflows d'approbation */
    getApprovalWorkflows2(options = {}) {
      return request("GET", "/approval-workflows", options);
    },

    /** Lister les workflows d'approbation */
    getApprovalWorkflows3(options = {}) {
      return request("GET", "/approval-workflows", options);
    },

    /** Lister les workflows d'approbation */
    getApprovalWorkflows4(options = {}) {
      return request("GET", "/approval-workflows", options);
    },

    /** Lister les workflows d'approbation */
    getApprovalWorkflows5(options = {}) {
      return request("GET", "/approval-workflows", options);
    },

    /** Lister les workflows d'approbation */
    getApprovalWorkflows6(options = {}) {
      return request("GET", "/approval-workflows", options);
    },

    /** Lister les workflows d'approbation */
    getApprovalWorkflows7(options = {}) {
      return request("GET", "/approval-workflows", options);
    },

    /** Lister les workflows d'approbation */
    getApprovalWorkflows8(options = {}) {
      return request("GET", "/approval-workflows", options);
    },

    /** Lister les workflows d'approbation */
    getApprovalWorkflows9(options = {}) {
      return request("GET", "/approval-workflows", options);
    },

    /** Lister les workflows d'approbation */
    getApprovalWorkflows10(options = {}) {
      return request("GET", "/approval-workflows", options);
    },

    /** Lister les workflows d'approbation */
    getApprovalWorkflows11(options = {}) {
      return request("GET", "/approval-workflows", options);
    },

    /** Creer un workflow */
    postApprovalWorkflows(options = {}) {
      return request("POST", "/approval-workflows", options);
    },

    /** Creer un workflow */
    postApprovalWorkflows2(options = {}) {
      return request("POST", "/approval-workflows", options);
    },

    /** Creer un workflow */
    postApprovalWorkflows3(options = {}) {
      return request("POST", "/approval-workflows", options);
    },

    /** Creer un workflow */
    postApprovalWorkflows4(options = {}) {
      return request("POST", "/approval-workflows", options);
    },

    /** Creer un workflow */
    postApprovalWorkflows5(options = {}) {
      return request("POST", "/approval-workflows", options);
    },

    /** Creer un workflow */
    postApprovalWorkflows6(options = {}) {
      return request("POST", "/approval-workflows", options);
    },

    /** Creer un workflow */
    postApprovalWorkflows7(options = {}) {
      return request("POST", "/approval-workflows", options);
    },

    /** Creer un workflow */
    postApprovalWorkflows8(options = {}) {
      return request("POST", "/approval-workflows", options);
    },

    /** Creer un workflow */
    postApprovalWorkflows9(options = {}) {
      return request("POST", "/approval-workflows", options);
    },

    /** Creer un workflow */
    postApprovalWorkflows10(options = {}) {
      return request("POST", "/approval-workflows", options);
    },

    /** Creer un workflow */
    postApprovalWorkflows11(options = {}) {
      return request("POST", "/approval-workflows", options);
    },

    /** Supprimer un workflow */
    deleteApprovalWorkflowsByApprovalWorkflow(options = {}) {
      return request("DELETE", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Supprimer un workflow */
    deleteApprovalWorkflowsByApprovalWorkflow2(options = {}) {
      return request("DELETE", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Supprimer un workflow */
    deleteApprovalWorkflowsByApprovalWorkflow3(options = {}) {
      return request("DELETE", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Supprimer un workflow */
    deleteApprovalWorkflowsByApprovalWorkflow4(options = {}) {
      return request("DELETE", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Supprimer un workflow */
    deleteApprovalWorkflowsByApprovalWorkflow5(options = {}) {
      return request("DELETE", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Supprimer un workflow */
    deleteApprovalWorkflowsByApprovalWorkflow6(options = {}) {
      return request("DELETE", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Supprimer un workflow */
    deleteApprovalWorkflowsByApprovalWorkflow7(options = {}) {
      return request("DELETE", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Supprimer un workflow */
    deleteApprovalWorkflowsByApprovalWorkflow8(options = {}) {
      return request("DELETE", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Supprimer un workflow */
    deleteApprovalWorkflowsByApprovalWorkflow9(options = {}) {
      return request("DELETE", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Supprimer un workflow */
    deleteApprovalWorkflowsByApprovalWorkflow10(options = {}) {
      return request("DELETE", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Supprimer un workflow */
    deleteApprovalWorkflowsByApprovalWorkflow11(options = {}) {
      return request("DELETE", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Modifier un workflow */
    putApprovalWorkflowsByApprovalWorkflow(options = {}) {
      return request("PUT", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Modifier un workflow */
    putApprovalWorkflowsByApprovalWorkflow2(options = {}) {
      return request("PUT", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Modifier un workflow */
    putApprovalWorkflowsByApprovalWorkflow3(options = {}) {
      return request("PUT", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Modifier un workflow */
    putApprovalWorkflowsByApprovalWorkflow4(options = {}) {
      return request("PUT", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Modifier un workflow */
    putApprovalWorkflowsByApprovalWorkflow5(options = {}) {
      return request("PUT", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Modifier un workflow */
    putApprovalWorkflowsByApprovalWorkflow6(options = {}) {
      return request("PUT", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Modifier un workflow */
    putApprovalWorkflowsByApprovalWorkflow7(options = {}) {
      return request("PUT", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Modifier un workflow */
    putApprovalWorkflowsByApprovalWorkflow8(options = {}) {
      return request("PUT", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Modifier un workflow */
    putApprovalWorkflowsByApprovalWorkflow9(options = {}) {
      return request("PUT", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Modifier un workflow */
    putApprovalWorkflowsByApprovalWorkflow10(options = {}) {
      return request("PUT", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Modifier un workflow */
    putApprovalWorkflowsByApprovalWorkflow11(options = {}) {
      return request("PUT", "/approval-workflows/{approvalWorkflow}", options);
    },

    /** Approuver une demande */
    postApprovalsByApprovalRequestApprove(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/approve", options);
    },

    /** Approuver une demande */
    postApprovalsByApprovalRequestApprove2(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/approve", options);
    },

    /** Approuver une demande */
    postApprovalsByApprovalRequestApprove3(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/approve", options);
    },

    /** Approuver une demande */
    postApprovalsByApprovalRequestApprove4(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/approve", options);
    },

    /** Approuver une demande */
    postApprovalsByApprovalRequestApprove5(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/approve", options);
    },

    /** Approuver une demande */
    postApprovalsByApprovalRequestApprove6(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/approve", options);
    },

    /** Approuver une demande */
    postApprovalsByApprovalRequestApprove7(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/approve", options);
    },

    /** Approuver une demande */
    postApprovalsByApprovalRequestApprove8(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/approve", options);
    },

    /** Approuver une demande */
    postApprovalsByApprovalRequestApprove9(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/approve", options);
    },

    /** Approuver une demande */
    postApprovalsByApprovalRequestApprove10(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/approve", options);
    },

    /** Approuver une demande */
    postApprovalsByApprovalRequestApprove11(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/approve", options);
    },

    /** Rejeter une demande */
    postApprovalsByApprovalRequestReject(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/reject", options);
    },

    /** Rejeter une demande */
    postApprovalsByApprovalRequestReject2(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/reject", options);
    },

    /** Rejeter une demande */
    postApprovalsByApprovalRequestReject3(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/reject", options);
    },

    /** Rejeter une demande */
    postApprovalsByApprovalRequestReject4(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/reject", options);
    },

    /** Rejeter une demande */
    postApprovalsByApprovalRequestReject5(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/reject", options);
    },

    /** Rejeter une demande */
    postApprovalsByApprovalRequestReject6(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/reject", options);
    },

    /** Rejeter une demande */
    postApprovalsByApprovalRequestReject7(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/reject", options);
    },

    /** Rejeter une demande */
    postApprovalsByApprovalRequestReject8(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/reject", options);
    },

    /** Rejeter une demande */
    postApprovalsByApprovalRequestReject9(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/reject", options);
    },

    /** Rejeter une demande */
    postApprovalsByApprovalRequestReject10(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/reject", options);
    },

    /** Rejeter une demande */
    postApprovalsByApprovalRequestReject11(options = {}) {
      return request("POST", "/approvals/{approvalRequest}/reject", options);
    },

    /** Historique des approbations */
    getApprovalsHistory(options = {}) {
      return request("GET", "/approvals/history", options);
    },

    /** Historique des approbations */
    getApprovalsHistory2(options = {}) {
      return request("GET", "/approvals/history", options);
    },

    /** Historique des approbations */
    getApprovalsHistory3(options = {}) {
      return request("GET", "/approvals/history", options);
    },

    /** Historique des approbations */
    getApprovalsHistory4(options = {}) {
      return request("GET", "/approvals/history", options);
    },

    /** Historique des approbations */
    getApprovalsHistory5(options = {}) {
      return request("GET", "/approvals/history", options);
    },

    /** Historique des approbations */
    getApprovalsHistory6(options = {}) {
      return request("GET", "/approvals/history", options);
    },

    /** Historique des approbations */
    getApprovalsHistory7(options = {}) {
      return request("GET", "/approvals/history", options);
    },

    /** Historique des approbations */
    getApprovalsHistory8(options = {}) {
      return request("GET", "/approvals/history", options);
    },

    /** Historique des approbations */
    getApprovalsHistory9(options = {}) {
      return request("GET", "/approvals/history", options);
    },

    /** Historique des approbations */
    getApprovalsHistory10(options = {}) {
      return request("GET", "/approvals/history", options);
    },

    /** Historique des approbations */
    getApprovalsHistory11(options = {}) {
      return request("GET", "/approvals/history", options);
    },

    /** Approbations en attente */
    getApprovalsPending(options = {}) {
      return request("GET", "/approvals/pending", options);
    },

    /** Approbations en attente */
    getApprovalsPending2(options = {}) {
      return request("GET", "/approvals/pending", options);
    },

    /** Approbations en attente */
    getApprovalsPending3(options = {}) {
      return request("GET", "/approvals/pending", options);
    },

    /** Approbations en attente */
    getApprovalsPending4(options = {}) {
      return request("GET", "/approvals/pending", options);
    },

    /** Approbations en attente */
    getApprovalsPending5(options = {}) {
      return request("GET", "/approvals/pending", options);
    },

    /** Approbations en attente */
    getApprovalsPending6(options = {}) {
      return request("GET", "/approvals/pending", options);
    },

    /** Approbations en attente */
    getApprovalsPending7(options = {}) {
      return request("GET", "/approvals/pending", options);
    },

    /** Approbations en attente */
    getApprovalsPending8(options = {}) {
      return request("GET", "/approvals/pending", options);
    },

    /** Approbations en attente */
    getApprovalsPending9(options = {}) {
      return request("GET", "/approvals/pending", options);
    },

    /** Approbations en attente */
    getApprovalsPending10(options = {}) {
      return request("GET", "/approvals/pending", options);
    },

    /** Approbations en attente */
    getApprovalsPending11(options = {}) {
      return request("GET", "/approvals/pending", options);
    },

    /** Historique de pointage */
    getAttendance(options = {}) {
      return request("GET", "/attendance", options);
    },

    /** Historique de pointage */
    getAttendance2(options = {}) {
      return request("GET", "/attendance", options);
    },

    /** Historique de pointage */
    getAttendance3(options = {}) {
      return request("GET", "/attendance", options);
    },

    /** Historique de pointage */
    getAttendance4(options = {}) {
      return request("GET", "/attendance", options);
    },

    /** Historique de pointage */
    getAttendance5(options = {}) {
      return request("GET", "/attendance", options);
    },

    /** Historique de pointage */
    getAttendance6(options = {}) {
      return request("GET", "/attendance", options);
    },

    /** Historique de pointage */
    getAttendance7(options = {}) {
      return request("GET", "/attendance", options);
    },

    /** Historique de pointage */
    getAttendance8(options = {}) {
      return request("GET", "/attendance", options);
    },

    /** Modifier directement un pointage */
    putAttendanceByAttendanceLog(options = {}) {
      return request("PUT", "/attendance/{attendanceLog}", options);
    },

    /** Modifier directement un pointage */
    putAttendanceByAttendanceLog2(options = {}) {
      return request("PUT", "/attendance/{attendanceLog}", options);
    },

    /** Modifier directement un pointage */
    putAttendanceByAttendanceLog3(options = {}) {
      return request("PUT", "/attendance/{attendanceLog}", options);
    },

    /** Modifier directement un pointage */
    putAttendanceByAttendanceLog4(options = {}) {
      return request("PUT", "/attendance/{attendanceLog}", options);
    },

    /** Modifier directement un pointage */
    putAttendanceByAttendanceLog5(options = {}) {
      return request("PUT", "/attendance/{attendanceLog}", options);
    },

    /** Modifier directement un pointage */
    putAttendanceByAttendanceLog6(options = {}) {
      return request("PUT", "/attendance/{attendanceLog}", options);
    },

    /** Modifier directement un pointage */
    putAttendanceByAttendanceLog7(options = {}) {
      return request("PUT", "/attendance/{attendanceLog}", options);
    },

    /** Modifier directement un pointage */
    putAttendanceByAttendanceLog8(options = {}) {
      return request("PUT", "/attendance/{attendanceLog}", options);
    },

    /** Telecharger la photo de pointage d'un log */
    getAttendanceByAttendanceLogPunchPhoto(options = {}) {
      return request("GET", "/attendance/{attendanceLog}/punch-photo", options);
    },

    /** Telecharger la photo de pointage d'un log */
    getAttendanceByAttendanceLogPunchPhoto2(options = {}) {
      return request("GET", "/attendance/{attendanceLog}/punch-photo", options);
    },

    /** Telecharger la photo de pointage d'un log */
    getAttendanceByAttendanceLogPunchPhoto3(options = {}) {
      return request("GET", "/attendance/{attendanceLog}/punch-photo", options);
    },

    /** Telecharger la photo de pointage d'un log */
    getAttendanceByAttendanceLogPunchPhoto4(options = {}) {
      return request("GET", "/attendance/{attendanceLog}/punch-photo", options);
    },

    /** Telecharger la photo de pointage d'un log */
    getAttendanceByAttendanceLogPunchPhoto5(options = {}) {
      return request("GET", "/attendance/{attendanceLog}/punch-photo", options);
    },

    /** Telecharger la photo de pointage d'un log */
    getAttendanceByAttendanceLogPunchPhoto6(options = {}) {
      return request("GET", "/attendance/{attendanceLog}/punch-photo", options);
    },

    /** Telecharger la photo de pointage d'un log */
    getAttendanceByAttendanceLogPunchPhoto7(options = {}) {
      return request("GET", "/attendance/{attendanceLog}/punch-photo", options);
    },

    /** Telecharger la photo de pointage d'un log */
    getAttendanceByAttendanceLogPunchPhoto8(options = {}) {
      return request("GET", "/attendance/{attendanceLog}/punch-photo", options);
    },

    /** Anomalies de pointage */
    getAttendanceAnomalies(options = {}) {
      return request("GET", "/attendance/anomalies", options);
    },

    /** Anomalies de pointage */
    getAttendanceAnomalies2(options = {}) {
      return request("GET", "/attendance/anomalies", options);
    },

    /** Anomalies de pointage */
    getAttendanceAnomalies3(options = {}) {
      return request("GET", "/attendance/anomalies", options);
    },

    /** Anomalies de pointage */
    getAttendanceAnomalies4(options = {}) {
      return request("GET", "/attendance/anomalies", options);
    },

    /** Anomalies de pointage */
    getAttendanceAnomalies5(options = {}) {
      return request("GET", "/attendance/anomalies", options);
    },

    /** Anomalies de pointage */
    getAttendanceAnomalies6(options = {}) {
      return request("GET", "/attendance/anomalies", options);
    },

    /** Anomalies de pointage */
    getAttendanceAnomalies7(options = {}) {
      return request("GET", "/attendance/anomalies", options);
    },

    /** Anomalies de pointage */
    getAttendanceAnomalies8(options = {}) {
      return request("GET", "/attendance/anomalies", options);
    },

    /** Check-in */
    postAttendanceCheckIn(options = {}) {
      return request("POST", "/attendance/check-in", options);
    },

    /** Check-in */
    postAttendanceCheckIn2(options = {}) {
      return request("POST", "/attendance/check-in", options);
    },

    /** Check-in */
    postAttendanceCheckIn3(options = {}) {
      return request("POST", "/attendance/check-in", options);
    },

    /** Check-in */
    postAttendanceCheckIn4(options = {}) {
      return request("POST", "/attendance/check-in", options);
    },

    /** Check-in */
    postAttendanceCheckIn5(options = {}) {
      return request("POST", "/attendance/check-in", options);
    },

    /** Check-in */
    postAttendanceCheckIn6(options = {}) {
      return request("POST", "/attendance/check-in", options);
    },

    /** Check-in */
    postAttendanceCheckIn7(options = {}) {
      return request("POST", "/attendance/check-in", options);
    },

    /** Check-in */
    postAttendanceCheckIn8(options = {}) {
      return request("POST", "/attendance/check-in", options);
    },

    /** Check-out */
    postAttendanceCheckOut(options = {}) {
      return request("POST", "/attendance/check-out", options);
    },

    /** Check-out */
    postAttendanceCheckOut2(options = {}) {
      return request("POST", "/attendance/check-out", options);
    },

    /** Check-out */
    postAttendanceCheckOut3(options = {}) {
      return request("POST", "/attendance/check-out", options);
    },

    /** Check-out */
    postAttendanceCheckOut4(options = {}) {
      return request("POST", "/attendance/check-out", options);
    },

    /** Check-out */
    postAttendanceCheckOut5(options = {}) {
      return request("POST", "/attendance/check-out", options);
    },

    /** Check-out */
    postAttendanceCheckOut6(options = {}) {
      return request("POST", "/attendance/check-out", options);
    },

    /** Check-out */
    postAttendanceCheckOut7(options = {}) {
      return request("POST", "/attendance/check-out", options);
    },

    /** Check-out */
    postAttendanceCheckOut8(options = {}) {
      return request("POST", "/attendance/check-out", options);
    },

    /** Lire la configuration de mode active pour l'employe connecte */
    getAttendanceConfig(options = {}) {
      return request("GET", "/attendance/config", options);
    },

    /** Lister les demandes de correction de pointage */
    getAttendanceCorrections(options = {}) {
      return request("GET", "/attendance/corrections", options);
    },

    /** Lister les demandes de correction de pointage */
    getAttendanceCorrections2(options = {}) {
      return request("GET", "/attendance/corrections", options);
    },

    /** Lister les demandes de correction de pointage */
    getAttendanceCorrections3(options = {}) {
      return request("GET", "/attendance/corrections", options);
    },

    /** Lister les demandes de correction de pointage */
    getAttendanceCorrections4(options = {}) {
      return request("GET", "/attendance/corrections", options);
    },

    /** Lister les demandes de correction de pointage */
    getAttendanceCorrections5(options = {}) {
      return request("GET", "/attendance/corrections", options);
    },

    /** Lister les demandes de correction de pointage */
    getAttendanceCorrections6(options = {}) {
      return request("GET", "/attendance/corrections", options);
    },

    /** Lister les demandes de correction de pointage */
    getAttendanceCorrections7(options = {}) {
      return request("GET", "/attendance/corrections", options);
    },

    /** Lister les demandes de correction de pointage */
    getAttendanceCorrections8(options = {}) {
      return request("GET", "/attendance/corrections", options);
    },

    /** Demander une correction de pointage */
    postAttendanceCorrections(options = {}) {
      return request("POST", "/attendance/corrections", options);
    },

    /** Demander une correction de pointage */
    postAttendanceCorrections2(options = {}) {
      return request("POST", "/attendance/corrections", options);
    },

    /** Demander une correction de pointage */
    postAttendanceCorrections3(options = {}) {
      return request("POST", "/attendance/corrections", options);
    },

    /** Demander une correction de pointage */
    postAttendanceCorrections4(options = {}) {
      return request("POST", "/attendance/corrections", options);
    },

    /** Demander une correction de pointage */
    postAttendanceCorrections5(options = {}) {
      return request("POST", "/attendance/corrections", options);
    },

    /** Demander une correction de pointage */
    postAttendanceCorrections6(options = {}) {
      return request("POST", "/attendance/corrections", options);
    },

    /** Demander une correction de pointage */
    postAttendanceCorrections7(options = {}) {
      return request("POST", "/attendance/corrections", options);
    },

    /** Demander une correction de pointage */
    postAttendanceCorrections8(options = {}) {
      return request("POST", "/attendance/corrections", options);
    },

    /** Appliquer une demande de correction de pointage */
    postAttendanceCorrectionsByCorrectionApprove(options = {}) {
      return request("POST", "/attendance/corrections/{correction}/approve", options);
    },

    /** Appliquer une demande de correction de pointage */
    postAttendanceCorrectionsByCorrectionApprove2(options = {}) {
      return request("POST", "/attendance/corrections/{correction}/approve", options);
    },

    /** Appliquer une demande de correction de pointage */
    postAttendanceCorrectionsByCorrectionApprove3(options = {}) {
      return request("POST", "/attendance/corrections/{correction}/approve", options);
    },

    /** Appliquer une demande de correction de pointage */
    postAttendanceCorrectionsByCorrectionApprove4(options = {}) {
      return request("POST", "/attendance/corrections/{correction}/approve", options);
    },

    /** Appliquer une demande de correction de pointage */
    postAttendanceCorrectionsByCorrectionApprove5(options = {}) {
      return request("POST", "/attendance/corrections/{correction}/approve", options);
    },

    /** Appliquer une demande de correction de pointage */
    postAttendanceCorrectionsByCorrectionApprove6(options = {}) {
      return request("POST", "/attendance/corrections/{correction}/approve", options);
    },

    /** Appliquer une demande de correction de pointage */
    postAttendanceCorrectionsByCorrectionApprove7(options = {}) {
      return request("POST", "/attendance/corrections/{correction}/approve", options);
    },

    /** Appliquer une demande de correction de pointage */
    postAttendanceCorrectionsByCorrectionApprove8(options = {}) {
      return request("POST", "/attendance/corrections/{correction}/approve", options);
    },

    /** Approuver une correction de pointage (déprécié — utiliser POST) */
    putAttendanceCorrectionsByCorrectionApprove(options = {}) {
      return request("PUT", "/attendance/corrections/{correction}/approve", options);
    },

    /** Approuver une correction de pointage (déprécié — utiliser POST) */
    putAttendanceCorrectionsByCorrectionApprove2(options = {}) {
      return request("PUT", "/attendance/corrections/{correction}/approve", options);
    },

    /** Approuver une correction de pointage (déprécié — utiliser POST) */
    putAttendanceCorrectionsByCorrectionApprove3(options = {}) {
      return request("PUT", "/attendance/corrections/{correction}/approve", options);
    },

    /** Approuver une correction de pointage (déprécié — utiliser POST) */
    putAttendanceCorrectionsByCorrectionApprove4(options = {}) {
      return request("PUT", "/attendance/corrections/{correction}/approve", options);
    },

    /** Approuver une correction de pointage (déprécié — utiliser POST) */
    putAttendanceCorrectionsByCorrectionApprove5(options = {}) {
      return request("PUT", "/attendance/corrections/{correction}/approve", options);
    },

    /** Approuver une correction de pointage (déprécié — utiliser POST) */
    putAttendanceCorrectionsByCorrectionApprove6(options = {}) {
      return request("PUT", "/attendance/corrections/{correction}/approve", options);
    },

    /** Approuver une correction de pointage (déprécié — utiliser POST) */
    putAttendanceCorrectionsByCorrectionApprove7(options = {}) {
      return request("PUT", "/attendance/corrections/{correction}/approve", options);
    },

    /** Approuver une correction de pointage (déprécié — utiliser POST) */
    putAttendanceCorrectionsByCorrectionApprove8(options = {}) {
      return request("PUT", "/attendance/corrections/{correction}/approve", options);
    },

    /** Refuser une demande de correction de pointage */
    postAttendanceCorrectionsByCorrectionReject(options = {}) {
      return request("POST", "/attendance/corrections/{correction}/reject", options);
    },

    /** Refuser une demande de correction de pointage */
    postAttendanceCorrectionsByCorrectionReject2(options = {}) {
      return request("POST", "/attendance/corrections/{correction}/reject", options);
    },

    /** Refuser une demande de correction de pointage */
    postAttendanceCorrectionsByCorrectionReject3(options = {}) {
      return request("POST", "/attendance/corrections/{correction}/reject", options);
    },

    /** Refuser une demande de correction de pointage */
    postAttendanceCorrectionsByCorrectionReject4(options = {}) {
      return request("POST", "/attendance/corrections/{correction}/reject", options);
    },

    /** Refuser une demande de correction de pointage */
    postAttendanceCorrectionsByCorrectionReject5(options = {}) {
      return request("POST", "/attendance/corrections/{correction}/reject", options);
    },

    /** Refuser une demande de correction de pointage */
    postAttendanceCorrectionsByCorrectionReject6(options = {}) {
      return request("POST", "/attendance/corrections/{correction}/reject", options);
    },

    /** Refuser une demande de correction de pointage */
    postAttendanceCorrectionsByCorrectionReject7(options = {}) {
      return request("POST", "/attendance/corrections/{correction}/reject", options);
    },

    /** Refuser une demande de correction de pointage */
    postAttendanceCorrectionsByCorrectionReject8(options = {}) {
      return request("POST", "/attendance/corrections/{correction}/reject", options);
    },

    /** Refuser une correction de pointage (déprécié — utiliser POST) */
    putAttendanceCorrectionsByCorrectionReject(options = {}) {
      return request("PUT", "/attendance/corrections/{correction}/reject", options);
    },

    /** Refuser une correction de pointage (déprécié — utiliser POST) */
    putAttendanceCorrectionsByCorrectionReject2(options = {}) {
      return request("PUT", "/attendance/corrections/{correction}/reject", options);
    },

    /** Refuser une correction de pointage (déprécié — utiliser POST) */
    putAttendanceCorrectionsByCorrectionReject3(options = {}) {
      return request("PUT", "/attendance/corrections/{correction}/reject", options);
    },

    /** Refuser une correction de pointage (déprécié — utiliser POST) */
    putAttendanceCorrectionsByCorrectionReject4(options = {}) {
      return request("PUT", "/attendance/corrections/{correction}/reject", options);
    },

    /** Refuser une correction de pointage (déprécié — utiliser POST) */
    putAttendanceCorrectionsByCorrectionReject5(options = {}) {
      return request("PUT", "/attendance/corrections/{correction}/reject", options);
    },

    /** Refuser une correction de pointage (déprécié — utiliser POST) */
    putAttendanceCorrectionsByCorrectionReject6(options = {}) {
      return request("PUT", "/attendance/corrections/{correction}/reject", options);
    },

    /** Refuser une correction de pointage (déprécié — utiliser POST) */
    putAttendanceCorrectionsByCorrectionReject7(options = {}) {
      return request("PUT", "/attendance/corrections/{correction}/reject", options);
    },

    /** Refuser une correction de pointage (déprécié — utiliser POST) */
    putAttendanceCorrectionsByCorrectionReject8(options = {}) {
      return request("PUT", "/attendance/corrections/{correction}/reject", options);
    },

    /** Statistiques du jour — Smart Attendance (manager/RH) */
    getAttendanceDashboard(options = {}) {
      return request("GET", "/attendance/dashboard", options);
    },

    /** Préférence mode géolocalisation d'un employé (manager/RH) */
    getAttendanceEmployeesByEmployeeIdPreference(options = {}) {
      return request("GET", "/attendance/employees/{employeeId}/preference", options);
    },

    /** Envoyer un événement géographique (entrée/sortie de zone) */
    postAttendanceGeoEvents(options = {}) {
      return request("POST", "/attendance/geo-events", options);
    },

    /** Lister les sessions GPS */
    getAttendanceGeoSessions(options = {}) {
      return request("GET", "/attendance/geo-sessions", options);
    },

    /** Détail d'une session GPS */
    getAttendanceGeoSessionsById(options = {}) {
      return request("GET", "/attendance/geo-sessions/{id}", options);
    },

    /** Approuver une session GPS (manager/RH) */
    postAttendanceGeoSessionsByIdApprove(options = {}) {
      return request("POST", "/attendance/geo-sessions/{id}/approve", options);
    },

    /** Rejeter une session GPS (manager/RH) */
    postAttendanceGeoSessionsByIdReject(options = {}) {
      return request("POST", "/attendance/geo-sessions/{id}/reject", options);
    },

    /** Parametres du mode de pointage de l'entreprise */
    getAttendanceModeSettings(options = {}) {
      return request("GET", "/attendance/mode-settings", options);
    },

    /** Configurer le mode de pointage (principal) */
    putAttendanceModeSettings(options = {}) {
      return request("PUT", "/attendance/mode-settings", options);
    },

    /** Rapport de pointage (journalier, hebdomadaire ou mensuel) */
    getAttendanceMonthlyReport(options = {}) {
      return request("GET", "/attendance/monthly-report", options);
    },

    /** Rapport de pointage (journalier, hebdomadaire ou mensuel) */
    getAttendanceMonthlyReport2(options = {}) {
      return request("GET", "/attendance/monthly-report", options);
    },

    /** Rapport de pointage (journalier, hebdomadaire ou mensuel) */
    getAttendanceMonthlyReport3(options = {}) {
      return request("GET", "/attendance/monthly-report", options);
    },

    /** Rapport de pointage (journalier, hebdomadaire ou mensuel) */
    getAttendanceMonthlyReport4(options = {}) {
      return request("GET", "/attendance/monthly-report", options);
    },

    /** Rapport de pointage (journalier, hebdomadaire ou mensuel) */
    getAttendanceMonthlyReport5(options = {}) {
      return request("GET", "/attendance/monthly-report", options);
    },

    /** Rapport de pointage (journalier, hebdomadaire ou mensuel) */
    getAttendanceMonthlyReport6(options = {}) {
      return request("GET", "/attendance/monthly-report", options);
    },

    /** Rapport de pointage (journalier, hebdomadaire ou mensuel) */
    getAttendanceMonthlyReport7(options = {}) {
      return request("GET", "/attendance/monthly-report", options);
    },

    /** Rapport de pointage (journalier, hebdomadaire ou mensuel) */
    getAttendanceMonthlyReport8(options = {}) {
      return request("GET", "/attendance/monthly-report", options);
    },

    /** Sessions GPS de l'employé courant */
    getAttendanceMySessions(options = {}) {
      return request("GET", "/attendance/my-sessions", options);
    },

    /** Mettre à jour les préférences de pointage */
    putAttendancePreferences(options = {}) {
      return request("PUT", "/attendance/preferences", options);
    },

    /** Régularité de présence (manager/RH) */
    getAttendanceRegularity(options = {}) {
      return request("GET", "/attendance/regularity", options);
    },

    /** Régularité de présence (manager/RH) */
    getAttendanceRegularity2(options = {}) {
      return request("GET", "/attendance/regularity", options);
    },

    /** Régularité de présence (manager/RH) */
    getAttendanceRegularity3(options = {}) {
      return request("GET", "/attendance/regularity", options);
    },

    /** Régularité de présence (manager/RH) */
    getAttendanceRegularity4(options = {}) {
      return request("GET", "/attendance/regularity", options);
    },

    /** Régularité de présence (manager/RH) */
    getAttendanceRegularity5(options = {}) {
      return request("GET", "/attendance/regularity", options);
    },

    /** Régularité de présence (manager/RH) */
    getAttendanceRegularity6(options = {}) {
      return request("GET", "/attendance/regularity", options);
    },

    /** Régularité de présence (manager/RH) */
    getAttendanceRegularity7(options = {}) {
      return request("GET", "/attendance/regularity", options);
    },

    /** Régularité de présence (manager/RH) */
    getAttendanceRegularity8(options = {}) {
      return request("GET", "/attendance/regularity", options);
    },

    /** Régularité de présence (manager/RH) */
    getAttendanceRegularity9(options = {}) {
      return request("GET", "/attendance/regularity", options);
    },

    /** Régularité de présence (manager/RH) */
    getAttendanceRegularity10(options = {}) {
      return request("GET", "/attendance/regularity", options);
    },

    /** Régularité de présence (manager/RH) */
    getAttendanceRegularity11(options = {}) {
      return request("GET", "/attendance/regularity", options);
    },

    /** Etat du jour */
    getAttendanceToday(options = {}) {
      return request("GET", "/attendance/today", options);
    },

    /** Etat du jour */
    getAttendanceToday2(options = {}) {
      return request("GET", "/attendance/today", options);
    },

    /** Etat du jour */
    getAttendanceToday3(options = {}) {
      return request("GET", "/attendance/today", options);
    },

    /** Etat du jour */
    getAttendanceToday4(options = {}) {
      return request("GET", "/attendance/today", options);
    },

    /** Etat du jour */
    getAttendanceToday5(options = {}) {
      return request("GET", "/attendance/today", options);
    },

    /** Etat du jour */
    getAttendanceToday6(options = {}) {
      return request("GET", "/attendance/today", options);
    },

    /** Etat du jour */
    getAttendanceToday7(options = {}) {
      return request("GET", "/attendance/today", options);
    },

    /** Etat du jour */
    getAttendanceToday8(options = {}) {
      return request("GET", "/attendance/today", options);
    },

    /** Journal d'audit */
    getAuditLogs(options = {}) {
      return request("GET", "/audit-logs", options);
    },

    /** Journal d'audit */
    getAuditLogs2(options = {}) {
      return request("GET", "/audit-logs", options);
    },

    /** Journal d'audit */
    getAuditLogs3(options = {}) {
      return request("GET", "/audit-logs", options);
    },

    /** Journal d'audit */
    getAuditLogs4(options = {}) {
      return request("GET", "/audit-logs", options);
    },

    /** Journal d'audit */
    getAuditLogs5(options = {}) {
      return request("GET", "/audit-logs", options);
    },

    /** Journal d'audit */
    getAuditLogs6(options = {}) {
      return request("GET", "/audit-logs", options);
    },

    /** Journal d'audit */
    getAuditLogs7(options = {}) {
      return request("GET", "/audit-logs", options);
    },

    /** Journal d'audit */
    getAuditLogs8(options = {}) {
      return request("GET", "/audit-logs", options);
    },

    /** Journal d'audit */
    getAuditLogs9(options = {}) {
      return request("GET", "/audit-logs", options);
    },

    /** Journal d'audit */
    getAuditLogs10(options = {}) {
      return request("GET", "/audit-logs", options);
    },

    /** Journal d'audit */
    getAuditLogs11(options = {}) {
      return request("GET", "/audit-logs", options);
    },

    /** Voir un evenement d'audit */
    getAuditLogsByAuditLog(options = {}) {
      return request("GET", "/audit-logs/{auditLog}", options);
    },

    /** Voir un evenement d'audit */
    getAuditLogsByAuditLog2(options = {}) {
      return request("GET", "/audit-logs/{auditLog}", options);
    },

    /** Voir un evenement d'audit */
    getAuditLogsByAuditLog3(options = {}) {
      return request("GET", "/audit-logs/{auditLog}", options);
    },

    /** Voir un evenement d'audit */
    getAuditLogsByAuditLog4(options = {}) {
      return request("GET", "/audit-logs/{auditLog}", options);
    },

    /** Voir un evenement d'audit */
    getAuditLogsByAuditLog5(options = {}) {
      return request("GET", "/audit-logs/{auditLog}", options);
    },

    /** Voir un evenement d'audit */
    getAuditLogsByAuditLog6(options = {}) {
      return request("GET", "/audit-logs/{auditLog}", options);
    },

    /** Voir un evenement d'audit */
    getAuditLogsByAuditLog7(options = {}) {
      return request("GET", "/audit-logs/{auditLog}", options);
    },

    /** Voir un evenement d'audit */
    getAuditLogsByAuditLog8(options = {}) {
      return request("GET", "/audit-logs/{auditLog}", options);
    },

    /** Voir un evenement d'audit */
    getAuditLogsByAuditLog9(options = {}) {
      return request("GET", "/audit-logs/{auditLog}", options);
    },

    /** Voir un evenement d'audit */
    getAuditLogsByAuditLog10(options = {}) {
      return request("GET", "/audit-logs/{auditLog}", options);
    },

    /** Voir un evenement d'audit */
    getAuditLogsByAuditLog11(options = {}) {
      return request("GET", "/audit-logs/{auditLog}", options);
    },

    /** Exporter le journal d'audit en CSV */
    getAuditLogsExportCsv(options = {}) {
      return request("GET", "/audit-logs/export-csv", options);
    },

    /** Exporter le journal d'audit en CSV */
    getAuditLogsExportCsv2(options = {}) {
      return request("GET", "/audit-logs/export-csv", options);
    },

    /** Exporter le journal d'audit en CSV */
    getAuditLogsExportCsv3(options = {}) {
      return request("GET", "/audit-logs/export-csv", options);
    },

    /** Exporter le journal d'audit en CSV */
    getAuditLogsExportCsv4(options = {}) {
      return request("GET", "/audit-logs/export-csv", options);
    },

    /** Exporter le journal d'audit en CSV */
    getAuditLogsExportCsv5(options = {}) {
      return request("GET", "/audit-logs/export-csv", options);
    },

    /** Exporter le journal d'audit en CSV */
    getAuditLogsExportCsv6(options = {}) {
      return request("GET", "/audit-logs/export-csv", options);
    },

    /** Exporter le journal d'audit en CSV */
    getAuditLogsExportCsv7(options = {}) {
      return request("GET", "/audit-logs/export-csv", options);
    },

    /** Exporter le journal d'audit en CSV */
    getAuditLogsExportCsv8(options = {}) {
      return request("GET", "/audit-logs/export-csv", options);
    },

    /** Exporter le journal d'audit en CSV */
    getAuditLogsExportCsv9(options = {}) {
      return request("GET", "/audit-logs/export-csv", options);
    },

    /** Exporter le journal d'audit en CSV */
    getAuditLogsExportCsv10(options = {}) {
      return request("GET", "/audit-logs/export-csv", options);
    },

    /** Exporter le journal d'audit en CSV */
    getAuditLogsExportCsv11(options = {}) {
      return request("GET", "/audit-logs/export-csv", options);
    },

    /** Derniere demande biometrie de l'utilisateur courant */
    getAuthBiometricEnrollment(options = {}) {
      return request("GET", "/auth/biometric-enrollment", options);
    },

    /** Derniere demande biometrie de l'utilisateur courant */
    getAuthBiometricEnrollment2(options = {}) {
      return request("GET", "/auth/biometric-enrollment", options);
    },

    /** Derniere demande biometrie de l'utilisateur courant */
    getAuthBiometricEnrollment3(options = {}) {
      return request("GET", "/auth/biometric-enrollment", options);
    },

    /** Derniere demande biometrie de l'utilisateur courant */
    getAuthBiometricEnrollment4(options = {}) {
      return request("GET", "/auth/biometric-enrollment", options);
    },

    /** Derniere demande biometrie de l'utilisateur courant */
    getAuthBiometricEnrollment5(options = {}) {
      return request("GET", "/auth/biometric-enrollment", options);
    },

    /** Soumettre une demande biometrie */
    postAuthBiometricEnrollment(options = {}) {
      return request("POST", "/auth/biometric-enrollment", options);
    },

    /** Soumettre une demande biometrie */
    postAuthBiometricEnrollment2(options = {}) {
      return request("POST", "/auth/biometric-enrollment", options);
    },

    /** Soumettre une demande biometrie */
    postAuthBiometricEnrollment3(options = {}) {
      return request("POST", "/auth/biometric-enrollment", options);
    },

    /** Soumettre une demande biometrie */
    postAuthBiometricEnrollment4(options = {}) {
      return request("POST", "/auth/biometric-enrollment", options);
    },

    /** Soumettre une demande biometrie */
    postAuthBiometricEnrollment5(options = {}) {
      return request("POST", "/auth/biometric-enrollment", options);
    },

    /** Changer le mot de passe */
    postAuthChangePassword(options = {}) {
      return request("POST", "/auth/change-password", options);
    },

    /** Changer le mot de passe */
    postAuthChangePassword2(options = {}) {
      return request("POST", "/auth/change-password", options);
    },

    /** Changer le mot de passe */
    postAuthChangePassword3(options = {}) {
      return request("POST", "/auth/change-password", options);
    },

    /** Changer le mot de passe */
    postAuthChangePassword4(options = {}) {
      return request("POST", "/auth/change-password", options);
    },

    /** Changer le mot de passe */
    postAuthChangePassword5(options = {}) {
      return request("POST", "/auth/change-password", options);
    },

    /** Demander un lien de reinitialisation de mot de passe */
    postAuthForgotPassword(options = {}) {
      return request("POST", "/auth/forgot-password", options);
    },

    /** Demander un lien de reinitialisation de mot de passe */
    postAuthForgotPassword2(options = {}) {
      return request("POST", "/auth/forgot-password", options);
    },

    /** Demander un lien de reinitialisation de mot de passe */
    postAuthForgotPassword3(options = {}) {
      return request("POST", "/auth/forgot-password", options);
    },

    /** Demander un lien de reinitialisation de mot de passe */
    postAuthForgotPassword4(options = {}) {
      return request("POST", "/auth/forgot-password", options);
    },

    /** Demander un lien de reinitialisation de mot de passe */
    postAuthForgotPassword5(options = {}) {
      return request("POST", "/auth/forgot-password", options);
    },

    /** Redirection vers le flux OAuth Google (state anti-CSRF en session, #2619) */
    getAuthGoogle(options = {}) {
      return request("GET", "/auth/google", options);
    },

    /** Redirection vers le flux OAuth Google (state anti-CSRF en session, #2619) */
    getAuthGoogle2(options = {}) {
      return request("GET", "/auth/google", options);
    },

    /** Redirection vers le flux OAuth Google (state anti-CSRF en session, #2619) */
    getAuthGoogle3(options = {}) {
      return request("GET", "/auth/google", options);
    },

    /** Redirection vers le flux OAuth Google (state anti-CSRF en session, #2619) */
    getAuthGoogle4(options = {}) {
      return request("GET", "/auth/google", options);
    },

    /** Redirection vers le flux OAuth Google (state anti-CSRF en session, #2619) */
    getAuthGoogle5(options = {}) {
      return request("GET", "/auth/google", options);
    },

    /** Callback OAuth Google — échange le code contre une session */
    getAuthGoogleCallback(options = {}) {
      return request("GET", "/auth/google/callback", options);
    },

    /** Callback OAuth Google — échange le code contre une session */
    getAuthGoogleCallback2(options = {}) {
      return request("GET", "/auth/google/callback", options);
    },

    /** Callback OAuth Google — échange le code contre une session */
    getAuthGoogleCallback3(options = {}) {
      return request("GET", "/auth/google/callback", options);
    },

    /** Callback OAuth Google — échange le code contre une session */
    getAuthGoogleCallback4(options = {}) {
      return request("GET", "/auth/google/callback", options);
    },

    /** Callback OAuth Google — échange le code contre une session */
    getAuthGoogleCallback5(options = {}) {
      return request("GET", "/auth/google/callback", options);
    },

    /** Connexion Google OAuth */
    postAuthGoogleToken(options = {}) {
      return request("POST", "/auth/google/token", options);
    },

    /** Connexion Google OAuth */
    postAuthGoogleToken2(options = {}) {
      return request("POST", "/auth/google/token", options);
    },

    /** Connexion Google OAuth */
    postAuthGoogleToken3(options = {}) {
      return request("POST", "/auth/google/token", options);
    },

    /** Connexion Google OAuth */
    postAuthGoogleToken4(options = {}) {
      return request("POST", "/auth/google/token", options);
    },

    /** Connexion Google OAuth */
    postAuthGoogleToken5(options = {}) {
      return request("POST", "/auth/google/token", options);
    },

    /** Connexion Google OAuth */
    postAuthGoogleToken6(options = {}) {
      return request("POST", "/auth/google/token", options);
    },

    /** Connexion Google OAuth */
    postAuthGoogleToken7(options = {}) {
      return request("POST", "/auth/google/token", options);
    },

    /** Connexion Google OAuth */
    postAuthGoogleToken8(options = {}) {
      return request("POST", "/auth/google/token", options);
    },

    /** Connexion Google OAuth */
    postAuthGoogleToken9(options = {}) {
      return request("POST", "/auth/google/token", options);
    },

    /** Connexion Google OAuth */
    postAuthGoogleToken10(options = {}) {
      return request("POST", "/auth/google/token", options);
    },

    /** Changer la langue */
    patchAuthLanguage(options = {}) {
      return request("PATCH", "/auth/language", options);
    },

    /** Changer la langue */
    patchAuthLanguage2(options = {}) {
      return request("PATCH", "/auth/language", options);
    },

    /** Changer la langue */
    patchAuthLanguage3(options = {}) {
      return request("PATCH", "/auth/language", options);
    },

    /** Changer la langue */
    patchAuthLanguage4(options = {}) {
      return request("PATCH", "/auth/language", options);
    },

    /** Changer la langue */
    patchAuthLanguage5(options = {}) {
      return request("PATCH", "/auth/language", options);
    },

    /** Changer la langue */
    patchAuthLanguage6(options = {}) {
      return request("PATCH", "/auth/language", options);
    },

    /** Changer la langue */
    patchAuthLanguage7(options = {}) {
      return request("PATCH", "/auth/language", options);
    },

    /** Changer la langue */
    patchAuthLanguage8(options = {}) {
      return request("PATCH", "/auth/language", options);
    },

    /** Changer la langue */
    patchAuthLanguage9(options = {}) {
      return request("PATCH", "/auth/language", options);
    },

    /** Changer la langue */
    patchAuthLanguage10(options = {}) {
      return request("PATCH", "/auth/language", options);
    },

    /** Connexion employee */
    postAuthLogin(options = {}) {
      return request("POST", "/auth/login", options);
    },

    /** Connexion employee */
    postAuthLogin2(options = {}) {
      return request("POST", "/auth/login", options);
    },

    /** Connexion employee */
    postAuthLogin3(options = {}) {
      return request("POST", "/auth/login", options);
    },

    /** Connexion employee */
    postAuthLogin4(options = {}) {
      return request("POST", "/auth/login", options);
    },

    /** Connexion employee */
    postAuthLogin5(options = {}) {
      return request("POST", "/auth/login", options);
    },

    /** Deconnexion employee */
    postAuthLogout(options = {}) {
      return request("POST", "/auth/logout", options);
    },

    /** Deconnexion employee */
    postAuthLogout2(options = {}) {
      return request("POST", "/auth/logout", options);
    },

    /** Deconnexion employee */
    postAuthLogout3(options = {}) {
      return request("POST", "/auth/logout", options);
    },

    /** Deconnexion employee */
    postAuthLogout4(options = {}) {
      return request("POST", "/auth/logout", options);
    },

    /** Deconnexion employee */
    postAuthLogout5(options = {}) {
      return request("POST", "/auth/logout", options);
    },

    /** Profil authentifie */
    getAuthMe(options = {}) {
      return request("GET", "/auth/me", options);
    },

    /** Profil authentifie */
    getAuthMe2(options = {}) {
      return request("GET", "/auth/me", options);
    },

    /** Profil authentifie */
    getAuthMe3(options = {}) {
      return request("GET", "/auth/me", options);
    },

    /** Profil authentifie */
    getAuthMe4(options = {}) {
      return request("GET", "/auth/me", options);
    },

    /** Profil authentifie */
    getAuthMe5(options = {}) {
      return request("GET", "/auth/me", options);
    },

    /** Mettre a jour le profil courant */
    patchAuthProfile(options = {}) {
      return request("PATCH", "/auth/profile", options);
    },

    /** Mettre a jour le profil courant */
    patchAuthProfile2(options = {}) {
      return request("PATCH", "/auth/profile", options);
    },

    /** Mettre a jour le profil courant */
    patchAuthProfile3(options = {}) {
      return request("PATCH", "/auth/profile", options);
    },

    /** Mettre a jour le profil courant */
    patchAuthProfile4(options = {}) {
      return request("PATCH", "/auth/profile", options);
    },

    /** Mettre a jour le profil courant */
    patchAuthProfile5(options = {}) {
      return request("PATCH", "/auth/profile", options);
    },

    /** Rafraichir le token */
    postAuthRefreshToken(options = {}) {
      return request("POST", "/auth/refresh-token", options);
    },

    /** Rafraichir le token */
    postAuthRefreshToken2(options = {}) {
      return request("POST", "/auth/refresh-token", options);
    },

    /** Rafraichir le token */
    postAuthRefreshToken3(options = {}) {
      return request("POST", "/auth/refresh-token", options);
    },

    /** Rafraichir le token */
    postAuthRefreshToken4(options = {}) {
      return request("POST", "/auth/refresh-token", options);
    },

    /** Rafraichir le token */
    postAuthRefreshToken5(options = {}) {
      return request("POST", "/auth/refresh-token", options);
    },

    /** Rafraichir le token */
    postAuthRefreshToken6(options = {}) {
      return request("POST", "/auth/refresh-token", options);
    },

    /** Rafraichir le token */
    postAuthRefreshToken7(options = {}) {
      return request("POST", "/auth/refresh-token", options);
    },

    /** Rafraichir le token */
    postAuthRefreshToken8(options = {}) {
      return request("POST", "/auth/refresh-token", options);
    },

    /** Rafraichir le token */
    postAuthRefreshToken9(options = {}) {
      return request("POST", "/auth/refresh-token", options);
    },

    /** Rafraichir le token */
    postAuthRefreshToken10(options = {}) {
      return request("POST", "/auth/refresh-token", options);
    },

    /** Inscription employe */
    postAuthRegister(options = {}) {
      return request("POST", "/auth/register", options);
    },

    /** Inscription employe */
    postAuthRegister2(options = {}) {
      return request("POST", "/auth/register", options);
    },

    /** Inscription employe */
    postAuthRegister3(options = {}) {
      return request("POST", "/auth/register", options);
    },

    /** Inscription employe */
    postAuthRegister4(options = {}) {
      return request("POST", "/auth/register", options);
    },

    /** Inscription employe */
    postAuthRegister5(options = {}) {
      return request("POST", "/auth/register", options);
    },

    /** Inscription employe */
    postAuthRegister6(options = {}) {
      return request("POST", "/auth/register", options);
    },

    /** Inscription employe */
    postAuthRegister7(options = {}) {
      return request("POST", "/auth/register", options);
    },

    /** Inscription employe */
    postAuthRegister8(options = {}) {
      return request("POST", "/auth/register", options);
    },

    /** Inscription employe */
    postAuthRegister9(options = {}) {
      return request("POST", "/auth/register", options);
    },

    /** Inscription employe */
    postAuthRegister10(options = {}) {
      return request("POST", "/auth/register", options);
    },

    /** Reinitialiser le mot de passe avec le jeton recu par email */
    postAuthResetPassword(options = {}) {
      return request("POST", "/auth/reset-password", options);
    },

    /** Reinitialiser le mot de passe avec le jeton recu par email */
    postAuthResetPassword2(options = {}) {
      return request("POST", "/auth/reset-password", options);
    },

    /** Reinitialiser le mot de passe avec le jeton recu par email */
    postAuthResetPassword3(options = {}) {
      return request("POST", "/auth/reset-password", options);
    },

    /** Reinitialiser le mot de passe avec le jeton recu par email */
    postAuthResetPassword4(options = {}) {
      return request("POST", "/auth/reset-password", options);
    },

    /** Reinitialiser le mot de passe avec le jeton recu par email */
    postAuthResetPassword5(options = {}) {
      return request("POST", "/auth/reset-password", options);
    },

    /** Lister les exports bancaires du tenant (pagine) */
    getBankExports(options = {}) {
      return request("GET", "/bank-exports", options);
    },

    /** Lister les exports bancaires du tenant (pagine) */
    getBankExports2(options = {}) {
      return request("GET", "/bank-exports", options);
    },

    /** Lister les exports bancaires du tenant (pagine) */
    getBankExports3(options = {}) {
      return request("GET", "/bank-exports", options);
    },

    /** Lister les exports bancaires du tenant (pagine) */
    getBankExports4(options = {}) {
      return request("GET", "/bank-exports", options);
    },

    /** Lister les exports bancaires du tenant (pagine) */
    getBankExports5(options = {}) {
      return request("GET", "/bank-exports", options);
    },

    /** Lister les exports bancaires du tenant (pagine) */
    getBankExports6(options = {}) {
      return request("GET", "/bank-exports", options);
    },

    /** Lister les exports bancaires du tenant (pagine) */
    getBankExports7(options = {}) {
      return request("GET", "/bank-exports", options);
    },

    /** Lister les exports bancaires du tenant (pagine) */
    getBankExports8(options = {}) {
      return request("GET", "/bank-exports", options);
    },

    /** Lister les exports bancaires du tenant (pagine) */
    getBankExports9(options = {}) {
      return request("GET", "/bank-exports", options);
    },

    /** Lister les exports bancaires du tenant (pagine) */
    getBankExports10(options = {}) {
      return request("GET", "/bank-exports", options);
    },

    /** Lister les exports bancaires du tenant (pagine) */
    getBankExports11(options = {}) {
      return request("GET", "/bank-exports", options);
    },

    /** Creer un export bancaire pour un run valide/paye (asynchrone) */
    postBankExports(options = {}) {
      return request("POST", "/bank-exports", options);
    },

    /** Creer un export bancaire pour un run valide/paye (asynchrone) */
    postBankExports2(options = {}) {
      return request("POST", "/bank-exports", options);
    },

    /** Creer un export bancaire pour un run valide/paye (asynchrone) */
    postBankExports3(options = {}) {
      return request("POST", "/bank-exports", options);
    },

    /** Creer un export bancaire pour un run valide/paye (asynchrone) */
    postBankExports4(options = {}) {
      return request("POST", "/bank-exports", options);
    },

    /** Creer un export bancaire pour un run valide/paye (asynchrone) */
    postBankExports5(options = {}) {
      return request("POST", "/bank-exports", options);
    },

    /** Creer un export bancaire pour un run valide/paye (asynchrone) */
    postBankExports6(options = {}) {
      return request("POST", "/bank-exports", options);
    },

    /** Creer un export bancaire pour un run valide/paye (asynchrone) */
    postBankExports7(options = {}) {
      return request("POST", "/bank-exports", options);
    },

    /** Creer un export bancaire pour un run valide/paye (asynchrone) */
    postBankExports8(options = {}) {
      return request("POST", "/bank-exports", options);
    },

    /** Creer un export bancaire pour un run valide/paye (asynchrone) */
    postBankExports9(options = {}) {
      return request("POST", "/bank-exports", options);
    },

    /** Creer un export bancaire pour un run valide/paye (asynchrone) */
    postBankExports10(options = {}) {
      return request("POST", "/bank-exports", options);
    },

    /** Creer un export bancaire pour un run valide/paye (asynchrone) */
    postBankExports11(options = {}) {
      return request("POST", "/bank-exports", options);
    },

    /** Détail d'un export bancaire */
    getBankExportsByBankExport(options = {}) {
      return request("GET", "/bank-exports/{bankExport}", options);
    },

    /** Détail d'un export bancaire */
    getBankExportsByBankExport2(options = {}) {
      return request("GET", "/bank-exports/{bankExport}", options);
    },

    /** Détail d'un export bancaire */
    getBankExportsByBankExport3(options = {}) {
      return request("GET", "/bank-exports/{bankExport}", options);
    },

    /** Détail d'un export bancaire */
    getBankExportsByBankExport4(options = {}) {
      return request("GET", "/bank-exports/{bankExport}", options);
    },

    /** Détail d'un export bancaire */
    getBankExportsByBankExport5(options = {}) {
      return request("GET", "/bank-exports/{bankExport}", options);
    },

    /** Détail d'un export bancaire */
    getBankExportsByBankExport6(options = {}) {
      return request("GET", "/bank-exports/{bankExport}", options);
    },

    /** Détail d'un export bancaire */
    getBankExportsByBankExport7(options = {}) {
      return request("GET", "/bank-exports/{bankExport}", options);
    },

    /** Détail d'un export bancaire */
    getBankExportsByBankExport8(options = {}) {
      return request("GET", "/bank-exports/{bankExport}", options);
    },

    /** Détail d'un export bancaire */
    getBankExportsByBankExport9(options = {}) {
      return request("GET", "/bank-exports/{bankExport}", options);
    },

    /** Détail d'un export bancaire */
    getBankExportsByBankExport10(options = {}) {
      return request("GET", "/bank-exports/{bankExport}", options);
    },

    /** Détail d'un export bancaire */
    getBankExportsByBankExport11(options = {}) {
      return request("GET", "/bank-exports/{bankExport}", options);
    },

    /** Telecharger un export bancaire */
    getBankExportsByBankExportDownload(options = {}) {
      return request("GET", "/bank-exports/{bankExport}/download", options);
    },

    /** Telecharger un export bancaire */
    getBankExportsByBankExportDownload2(options = {}) {
      return request("GET", "/bank-exports/{bankExport}/download", options);
    },

    /** Telecharger un export bancaire */
    getBankExportsByBankExportDownload3(options = {}) {
      return request("GET", "/bank-exports/{bankExport}/download", options);
    },

    /** Telecharger un export bancaire */
    getBankExportsByBankExportDownload4(options = {}) {
      return request("GET", "/bank-exports/{bankExport}/download", options);
    },

    /** Telecharger un export bancaire */
    getBankExportsByBankExportDownload5(options = {}) {
      return request("GET", "/bank-exports/{bankExport}/download", options);
    },

    /** Telecharger un export bancaire */
    getBankExportsByBankExportDownload6(options = {}) {
      return request("GET", "/bank-exports/{bankExport}/download", options);
    },

    /** Telecharger un export bancaire */
    getBankExportsByBankExportDownload7(options = {}) {
      return request("GET", "/bank-exports/{bankExport}/download", options);
    },

    /** Telecharger un export bancaire */
    getBankExportsByBankExportDownload8(options = {}) {
      return request("GET", "/bank-exports/{bankExport}/download", options);
    },

    /** Telecharger un export bancaire */
    getBankExportsByBankExportDownload9(options = {}) {
      return request("GET", "/bank-exports/{bankExport}/download", options);
    },

    /** Telecharger un export bancaire */
    getBankExportsByBankExportDownload10(options = {}) {
      return request("GET", "/bank-exports/{bankExport}/download", options);
    },

    /** Telecharger un export bancaire */
    getBankExportsByBankExportDownload11(options = {}) {
      return request("GET", "/bank-exports/{bankExport}/download", options);
    },

    /** Creer une session Stripe Checkout pour souscrire/upgrader (manager) */
    createBillingCheckoutSession(options = {}) {
      return request("POST", "/billing/checkout", options);
    },

    /** Creer une session Stripe Checkout pour souscrire/upgrader (manager) */
    createBillingCheckoutSession2(options = {}) {
      return request("POST", "/billing/checkout", options);
    },

    /** Creer une session Stripe Checkout pour souscrire/upgrader (manager) */
    createBillingCheckoutSession3(options = {}) {
      return request("POST", "/billing/checkout", options);
    },

    /** Creer une session Stripe Checkout pour souscrire/upgrader (manager) */
    createBillingCheckoutSession4(options = {}) {
      return request("POST", "/billing/checkout", options);
    },

    /** Creer une session Stripe Checkout pour souscrire/upgrader (manager) */
    createBillingCheckoutSession5(options = {}) {
      return request("POST", "/billing/checkout", options);
    },

    /** Creer une session Stripe Checkout pour souscrire/upgrader (manager) */
    createBillingCheckoutSession6(options = {}) {
      return request("POST", "/billing/checkout", options);
    },

    /** Creer une session Stripe Checkout pour souscrire/upgrader (manager) */
    createBillingCheckoutSession7(options = {}) {
      return request("POST", "/billing/checkout", options);
    },

    /** Creer une session Stripe Checkout pour souscrire/upgrader (manager) */
    createBillingCheckoutSession8(options = {}) {
      return request("POST", "/billing/checkout", options);
    },

    /** Creer une session Stripe Checkout pour souscrire/upgrader (manager) */
    createBillingCheckoutSession9(options = {}) {
      return request("POST", "/billing/checkout", options);
    },

    /** Creer une session Stripe Checkout pour souscrire/upgrader (manager) */
    createBillingCheckoutSession10(options = {}) {
      return request("POST", "/billing/checkout", options);
    },

    /** Creer une session Stripe Checkout pour souscrire/upgrader (manager) */
    createBillingCheckoutSession11(options = {}) {
      return request("POST", "/billing/checkout", options);
    },

    /** Lister les factures */
    getBillingInvoices(options = {}) {
      return request("GET", "/billing/invoices", options);
    },

    /** Lister les factures */
    getBillingInvoices2(options = {}) {
      return request("GET", "/billing/invoices", options);
    },

    /** Lister les factures */
    getBillingInvoices3(options = {}) {
      return request("GET", "/billing/invoices", options);
    },

    /** Lister les factures */
    getBillingInvoices4(options = {}) {
      return request("GET", "/billing/invoices", options);
    },

    /** Lister les factures */
    getBillingInvoices5(options = {}) {
      return request("GET", "/billing/invoices", options);
    },

    /** Lister les factures */
    getBillingInvoices6(options = {}) {
      return request("GET", "/billing/invoices", options);
    },

    /** Lister les factures */
    getBillingInvoices7(options = {}) {
      return request("GET", "/billing/invoices", options);
    },

    /** Lister les factures */
    getBillingInvoices8(options = {}) {
      return request("GET", "/billing/invoices", options);
    },

    /** Lister les factures */
    getBillingInvoices9(options = {}) {
      return request("GET", "/billing/invoices", options);
    },

    /** Lister les factures */
    getBillingInvoices10(options = {}) {
      return request("GET", "/billing/invoices", options);
    },

    /** Lister les factures */
    getBillingInvoices11(options = {}) {
      return request("GET", "/billing/invoices", options);
    },

    /** Voir une facture */
    getBillingInvoicesById(options = {}) {
      return request("GET", "/billing/invoices/{id}", options);
    },

    /** Voir une facture */
    getBillingInvoicesById2(options = {}) {
      return request("GET", "/billing/invoices/{id}", options);
    },

    /** Voir une facture */
    getBillingInvoicesById3(options = {}) {
      return request("GET", "/billing/invoices/{id}", options);
    },

    /** Voir une facture */
    getBillingInvoicesById4(options = {}) {
      return request("GET", "/billing/invoices/{id}", options);
    },

    /** Voir une facture */
    getBillingInvoicesById5(options = {}) {
      return request("GET", "/billing/invoices/{id}", options);
    },

    /** Voir une facture */
    getBillingInvoicesById6(options = {}) {
      return request("GET", "/billing/invoices/{id}", options);
    },

    /** Voir une facture */
    getBillingInvoicesById7(options = {}) {
      return request("GET", "/billing/invoices/{id}", options);
    },

    /** Voir une facture */
    getBillingInvoicesById8(options = {}) {
      return request("GET", "/billing/invoices/{id}", options);
    },

    /** Voir une facture */
    getBillingInvoicesById9(options = {}) {
      return request("GET", "/billing/invoices/{id}", options);
    },

    /** Voir une facture */
    getBillingInvoicesById10(options = {}) {
      return request("GET", "/billing/invoices/{id}", options);
    },

    /** Voir une facture */
    getBillingInvoicesById11(options = {}) {
      return request("GET", "/billing/invoices/{id}", options);
    },

    /** Telecharger facture PDF */
    getBillingInvoicesByIdPdf(options = {}) {
      return request("GET", "/billing/invoices/{id}/pdf", options);
    },

    /** Telecharger facture PDF */
    getBillingInvoicesByIdPdf2(options = {}) {
      return request("GET", "/billing/invoices/{id}/pdf", options);
    },

    /** Telecharger facture PDF */
    getBillingInvoicesByIdPdf3(options = {}) {
      return request("GET", "/billing/invoices/{id}/pdf", options);
    },

    /** Telecharger facture PDF */
    getBillingInvoicesByIdPdf4(options = {}) {
      return request("GET", "/billing/invoices/{id}/pdf", options);
    },

    /** Telecharger facture PDF */
    getBillingInvoicesByIdPdf5(options = {}) {
      return request("GET", "/billing/invoices/{id}/pdf", options);
    },

    /** Telecharger facture PDF */
    getBillingInvoicesByIdPdf6(options = {}) {
      return request("GET", "/billing/invoices/{id}/pdf", options);
    },

    /** Telecharger facture PDF */
    getBillingInvoicesByIdPdf7(options = {}) {
      return request("GET", "/billing/invoices/{id}/pdf", options);
    },

    /** Telecharger facture PDF */
    getBillingInvoicesByIdPdf8(options = {}) {
      return request("GET", "/billing/invoices/{id}/pdf", options);
    },

    /** Telecharger facture PDF */
    getBillingInvoicesByIdPdf9(options = {}) {
      return request("GET", "/billing/invoices/{id}/pdf", options);
    },

    /** Telecharger facture PDF */
    getBillingInvoicesByIdPdf10(options = {}) {
      return request("GET", "/billing/invoices/{id}/pdf", options);
    },

    /** Telecharger facture PDF */
    getBillingInvoicesByIdPdf11(options = {}) {
      return request("GET", "/billing/invoices/{id}/pdf", options);
    },

    /** Creer une session Stripe Customer Portal pour gerer l'abonnement (manager) */
    createBillingPortalSession(options = {}) {
      return request("POST", "/billing/portal", options);
    },

    /** Creer une session Stripe Customer Portal pour gerer l'abonnement (manager) */
    createBillingPortalSession2(options = {}) {
      return request("POST", "/billing/portal", options);
    },

    /** Creer une session Stripe Customer Portal pour gerer l'abonnement (manager) */
    createBillingPortalSession3(options = {}) {
      return request("POST", "/billing/portal", options);
    },

    /** Creer une session Stripe Customer Portal pour gerer l'abonnement (manager) */
    createBillingPortalSession4(options = {}) {
      return request("POST", "/billing/portal", options);
    },

    /** Creer une session Stripe Customer Portal pour gerer l'abonnement (manager) */
    createBillingPortalSession5(options = {}) {
      return request("POST", "/billing/portal", options);
    },

    /** Creer une session Stripe Customer Portal pour gerer l'abonnement (manager) */
    createBillingPortalSession6(options = {}) {
      return request("POST", "/billing/portal", options);
    },

    /** Creer une session Stripe Customer Portal pour gerer l'abonnement (manager) */
    createBillingPortalSession7(options = {}) {
      return request("POST", "/billing/portal", options);
    },

    /** Creer une session Stripe Customer Portal pour gerer l'abonnement (manager) */
    createBillingPortalSession8(options = {}) {
      return request("POST", "/billing/portal", options);
    },

    /** Creer une session Stripe Customer Portal pour gerer l'abonnement (manager) */
    createBillingPortalSession9(options = {}) {
      return request("POST", "/billing/portal", options);
    },

    /** Creer une session Stripe Customer Portal pour gerer l'abonnement (manager) */
    createBillingPortalSession10(options = {}) {
      return request("POST", "/billing/portal", options);
    },

    /** Creer une session Stripe Customer Portal pour gerer l'abonnement (manager) */
    createBillingPortalSession11(options = {}) {
      return request("POST", "/billing/portal", options);
    },

    /** Voir l'abonnement courant */
    getBillingSubscription(options = {}) {
      return request("GET", "/billing/subscription", options);
    },

    /** Voir l'abonnement courant */
    getBillingSubscription2(options = {}) {
      return request("GET", "/billing/subscription", options);
    },

    /** Voir l'abonnement courant */
    getBillingSubscription3(options = {}) {
      return request("GET", "/billing/subscription", options);
    },

    /** Voir l'abonnement courant */
    getBillingSubscription4(options = {}) {
      return request("GET", "/billing/subscription", options);
    },

    /** Voir l'abonnement courant */
    getBillingSubscription5(options = {}) {
      return request("GET", "/billing/subscription", options);
    },

    /** Voir l'abonnement courant */
    getBillingSubscription6(options = {}) {
      return request("GET", "/billing/subscription", options);
    },

    /** Voir l'abonnement courant */
    getBillingSubscription7(options = {}) {
      return request("GET", "/billing/subscription", options);
    },

    /** Voir l'abonnement courant */
    getBillingSubscription8(options = {}) {
      return request("GET", "/billing/subscription", options);
    },

    /** Voir l'abonnement courant */
    getBillingSubscription9(options = {}) {
      return request("GET", "/billing/subscription", options);
    },

    /** Voir l'abonnement courant */
    getBillingSubscription10(options = {}) {
      return request("GET", "/billing/subscription", options);
    },

    /** Voir l'abonnement courant */
    getBillingSubscription11(options = {}) {
      return request("GET", "/billing/subscription", options);
    },

    /** Annuler l'abonnement (manager) */
    cancelSubscription(options = {}) {
      return request("POST", "/billing/subscription/cancel", options);
    },

    /** Annuler l'abonnement (manager) */
    cancelSubscription2(options = {}) {
      return request("POST", "/billing/subscription/cancel", options);
    },

    /** Annuler l'abonnement (manager) */
    cancelSubscription3(options = {}) {
      return request("POST", "/billing/subscription/cancel", options);
    },

    /** Annuler l'abonnement (manager) */
    cancelSubscription4(options = {}) {
      return request("POST", "/billing/subscription/cancel", options);
    },

    /** Annuler l'abonnement (manager) */
    cancelSubscription5(options = {}) {
      return request("POST", "/billing/subscription/cancel", options);
    },

    /** Annuler l'abonnement (manager) */
    cancelSubscription6(options = {}) {
      return request("POST", "/billing/subscription/cancel", options);
    },

    /** Annuler l'abonnement (manager) */
    cancelSubscription7(options = {}) {
      return request("POST", "/billing/subscription/cancel", options);
    },

    /** Annuler l'abonnement (manager) */
    cancelSubscription8(options = {}) {
      return request("POST", "/billing/subscription/cancel", options);
    },

    /** Annuler l'abonnement (manager) */
    cancelSubscription9(options = {}) {
      return request("POST", "/billing/subscription/cancel", options);
    },

    /** Annuler l'abonnement (manager) */
    cancelSubscription10(options = {}) {
      return request("POST", "/billing/subscription/cancel", options);
    },

    /** Annuler l'abonnement (manager) */
    cancelSubscription11(options = {}) {
      return request("POST", "/billing/subscription/cancel", options);
    },

    /** Renouveler l'abonnement (manager) */
    renewSubscription(options = {}) {
      return request("POST", "/billing/subscription/renew", options);
    },

    /** Renouveler l'abonnement (manager) */
    renewSubscription2(options = {}) {
      return request("POST", "/billing/subscription/renew", options);
    },

    /** Renouveler l'abonnement (manager) */
    renewSubscription3(options = {}) {
      return request("POST", "/billing/subscription/renew", options);
    },

    /** Renouveler l'abonnement (manager) */
    renewSubscription4(options = {}) {
      return request("POST", "/billing/subscription/renew", options);
    },

    /** Renouveler l'abonnement (manager) */
    renewSubscription5(options = {}) {
      return request("POST", "/billing/subscription/renew", options);
    },

    /** Renouveler l'abonnement (manager) */
    renewSubscription6(options = {}) {
      return request("POST", "/billing/subscription/renew", options);
    },

    /** Renouveler l'abonnement (manager) */
    renewSubscription7(options = {}) {
      return request("POST", "/billing/subscription/renew", options);
    },

    /** Renouveler l'abonnement (manager) */
    renewSubscription8(options = {}) {
      return request("POST", "/billing/subscription/renew", options);
    },

    /** Renouveler l'abonnement (manager) */
    renewSubscription9(options = {}) {
      return request("POST", "/billing/subscription/renew", options);
    },

    /** Renouveler l'abonnement (manager) */
    renewSubscription10(options = {}) {
      return request("POST", "/billing/subscription/renew", options);
    },

    /** Renouveler l'abonnement (manager) */
    renewSubscription11(options = {}) {
      return request("POST", "/billing/subscription/renew", options);
    },

    /** Upgrader l'abonnement (manager) */
    upgradeSubscription(options = {}) {
      return request("POST", "/billing/subscription/upgrade", options);
    },

    /** Upgrader l'abonnement (manager) */
    upgradeSubscription2(options = {}) {
      return request("POST", "/billing/subscription/upgrade", options);
    },

    /** Upgrader l'abonnement (manager) */
    upgradeSubscription3(options = {}) {
      return request("POST", "/billing/subscription/upgrade", options);
    },

    /** Upgrader l'abonnement (manager) */
    upgradeSubscription4(options = {}) {
      return request("POST", "/billing/subscription/upgrade", options);
    },

    /** Upgrader l'abonnement (manager) */
    upgradeSubscription5(options = {}) {
      return request("POST", "/billing/subscription/upgrade", options);
    },

    /** Upgrader l'abonnement (manager) */
    upgradeSubscription6(options = {}) {
      return request("POST", "/billing/subscription/upgrade", options);
    },

    /** Upgrader l'abonnement (manager) */
    upgradeSubscription7(options = {}) {
      return request("POST", "/billing/subscription/upgrade", options);
    },

    /** Upgrader l'abonnement (manager) */
    upgradeSubscription8(options = {}) {
      return request("POST", "/billing/subscription/upgrade", options);
    },

    /** Upgrader l'abonnement (manager) */
    upgradeSubscription9(options = {}) {
      return request("POST", "/billing/subscription/upgrade", options);
    },

    /** Upgrader l'abonnement (manager) */
    upgradeSubscription10(options = {}) {
      return request("POST", "/billing/subscription/upgrade", options);
    },

    /** Upgrader l'abonnement (manager) */
    upgradeSubscription11(options = {}) {
      return request("POST", "/billing/subscription/upgrade", options);
    },

    /** Lister les demandes biometrie de la company */
    getBiometricEnrollmentRequests(options = {}) {
      return request("GET", "/biometric-enrollment-requests", options);
    },

    /** Lister les demandes biometrie de la company */
    getBiometricEnrollmentRequests2(options = {}) {
      return request("GET", "/biometric-enrollment-requests", options);
    },

    /** Lister les demandes biometrie de la company */
    getBiometricEnrollmentRequests3(options = {}) {
      return request("GET", "/biometric-enrollment-requests", options);
    },

    /** Lister les demandes biometrie de la company */
    getBiometricEnrollmentRequests4(options = {}) {
      return request("GET", "/biometric-enrollment-requests", options);
    },

    /** Lister les demandes biometrie de la company */
    getBiometricEnrollmentRequests5(options = {}) {
      return request("GET", "/biometric-enrollment-requests", options);
    },

    /** Lister les demandes biometrie de la company */
    getBiometricEnrollmentRequests6(options = {}) {
      return request("GET", "/biometric-enrollment-requests", options);
    },

    /** Lister les demandes biometrie de la company */
    getBiometricEnrollmentRequests7(options = {}) {
      return request("GET", "/biometric-enrollment-requests", options);
    },

    /** Lister les demandes biometrie de la company */
    getBiometricEnrollmentRequests8(options = {}) {
      return request("GET", "/biometric-enrollment-requests", options);
    },

    /** Approuver une demande biometrie */
    postBiometricEnrollmentRequestsByIdApprove(options = {}) {
      return request("POST", "/biometric-enrollment-requests/{id}/approve", options);
    },

    /** Approuver une demande biometrie */
    postBiometricEnrollmentRequestsByIdApprove2(options = {}) {
      return request("POST", "/biometric-enrollment-requests/{id}/approve", options);
    },

    /** Approuver une demande biometrie */
    postBiometricEnrollmentRequestsByIdApprove3(options = {}) {
      return request("POST", "/biometric-enrollment-requests/{id}/approve", options);
    },

    /** Approuver une demande biometrie */
    postBiometricEnrollmentRequestsByIdApprove4(options = {}) {
      return request("POST", "/biometric-enrollment-requests/{id}/approve", options);
    },

    /** Approuver une demande biometrie */
    postBiometricEnrollmentRequestsByIdApprove5(options = {}) {
      return request("POST", "/biometric-enrollment-requests/{id}/approve", options);
    },

    /** Approuver une demande biometrie */
    postBiometricEnrollmentRequestsByIdApprove6(options = {}) {
      return request("POST", "/biometric-enrollment-requests/{id}/approve", options);
    },

    /** Approuver une demande biometrie */
    postBiometricEnrollmentRequestsByIdApprove7(options = {}) {
      return request("POST", "/biometric-enrollment-requests/{id}/approve", options);
    },

    /** Approuver une demande biometrie */
    postBiometricEnrollmentRequestsByIdApprove8(options = {}) {
      return request("POST", "/biometric-enrollment-requests/{id}/approve", options);
    },

    /** Rejeter une demande biometrie */
    postBiometricEnrollmentRequestsByIdReject(options = {}) {
      return request("POST", "/biometric-enrollment-requests/{id}/reject", options);
    },

    /** Rejeter une demande biometrie */
    postBiometricEnrollmentRequestsByIdReject2(options = {}) {
      return request("POST", "/biometric-enrollment-requests/{id}/reject", options);
    },

    /** Rejeter une demande biometrie */
    postBiometricEnrollmentRequestsByIdReject3(options = {}) {
      return request("POST", "/biometric-enrollment-requests/{id}/reject", options);
    },

    /** Rejeter une demande biometrie */
    postBiometricEnrollmentRequestsByIdReject4(options = {}) {
      return request("POST", "/biometric-enrollment-requests/{id}/reject", options);
    },

    /** Rejeter une demande biometrie */
    postBiometricEnrollmentRequestsByIdReject5(options = {}) {
      return request("POST", "/biometric-enrollment-requests/{id}/reject", options);
    },

    /** Rejeter une demande biometrie */
    postBiometricEnrollmentRequestsByIdReject6(options = {}) {
      return request("POST", "/biometric-enrollment-requests/{id}/reject", options);
    },

    /** Rejeter une demande biometrie */
    postBiometricEnrollmentRequestsByIdReject7(options = {}) {
      return request("POST", "/biometric-enrollment-requests/{id}/reject", options);
    },

    /** Rejeter une demande biometrie */
    postBiometricEnrollmentRequestsByIdReject8(options = {}) {
      return request("POST", "/biometric-enrollment-requests/{id}/reject", options);
    },

    /** Lister les documents */
    getCabinetDocuments(options = {}) {
      return request("GET", "/cabinet/documents", options);
    },

    /** Lister les documents */
    getCabinetDocuments2(options = {}) {
      return request("GET", "/cabinet/documents", options);
    },

    /** Lister les documents */
    getCabinetDocuments3(options = {}) {
      return request("GET", "/cabinet/documents", options);
    },

    /** Lister les documents */
    getCabinetDocuments4(options = {}) {
      return request("GET", "/cabinet/documents", options);
    },

    /** Lister les documents */
    getCabinetDocuments5(options = {}) {
      return request("GET", "/cabinet/documents", options);
    },

    /** Lister les documents */
    getCabinetDocuments6(options = {}) {
      return request("GET", "/cabinet/documents", options);
    },

    /** Lister les documents */
    getCabinetDocuments7(options = {}) {
      return request("GET", "/cabinet/documents", options);
    },

    /** Lister les documents */
    getCabinetDocuments8(options = {}) {
      return request("GET", "/cabinet/documents", options);
    },

    /** Lister les documents */
    getCabinetDocuments9(options = {}) {
      return request("GET", "/cabinet/documents", options);
    },

    /** Lister les documents */
    getCabinetDocuments10(options = {}) {
      return request("GET", "/cabinet/documents", options);
    },

    /** Lister les documents */
    getCabinetDocuments11(options = {}) {
      return request("GET", "/cabinet/documents", options);
    },

    /** Uploader un document */
    postCabinetDocuments(options = {}) {
      return request("POST", "/cabinet/documents", options);
    },

    /** Uploader un document */
    postCabinetDocuments2(options = {}) {
      return request("POST", "/cabinet/documents", options);
    },

    /** Uploader un document */
    postCabinetDocuments3(options = {}) {
      return request("POST", "/cabinet/documents", options);
    },

    /** Uploader un document */
    postCabinetDocuments4(options = {}) {
      return request("POST", "/cabinet/documents", options);
    },

    /** Uploader un document */
    postCabinetDocuments5(options = {}) {
      return request("POST", "/cabinet/documents", options);
    },

    /** Uploader un document */
    postCabinetDocuments6(options = {}) {
      return request("POST", "/cabinet/documents", options);
    },

    /** Uploader un document */
    postCabinetDocuments7(options = {}) {
      return request("POST", "/cabinet/documents", options);
    },

    /** Uploader un document */
    postCabinetDocuments8(options = {}) {
      return request("POST", "/cabinet/documents", options);
    },

    /** Uploader un document */
    postCabinetDocuments9(options = {}) {
      return request("POST", "/cabinet/documents", options);
    },

    /** Uploader un document */
    postCabinetDocuments10(options = {}) {
      return request("POST", "/cabinet/documents", options);
    },

    /** Uploader un document */
    postCabinetDocuments11(options = {}) {
      return request("POST", "/cabinet/documents", options);
    },

    /** Supprimer un document */
    deleteCabinetDocumentsByCabinetDocument(options = {}) {
      return request("DELETE", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Supprimer un document */
    deleteCabinetDocumentsByCabinetDocument2(options = {}) {
      return request("DELETE", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Supprimer un document */
    deleteCabinetDocumentsByCabinetDocument3(options = {}) {
      return request("DELETE", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Supprimer un document */
    deleteCabinetDocumentsByCabinetDocument4(options = {}) {
      return request("DELETE", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Supprimer un document */
    deleteCabinetDocumentsByCabinetDocument5(options = {}) {
      return request("DELETE", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Supprimer un document */
    deleteCabinetDocumentsByCabinetDocument6(options = {}) {
      return request("DELETE", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Supprimer un document */
    deleteCabinetDocumentsByCabinetDocument7(options = {}) {
      return request("DELETE", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Supprimer un document */
    deleteCabinetDocumentsByCabinetDocument8(options = {}) {
      return request("DELETE", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Supprimer un document */
    deleteCabinetDocumentsByCabinetDocument9(options = {}) {
      return request("DELETE", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Supprimer un document */
    deleteCabinetDocumentsByCabinetDocument10(options = {}) {
      return request("DELETE", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Supprimer un document */
    deleteCabinetDocumentsByCabinetDocument11(options = {}) {
      return request("DELETE", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Voir un document */
    getCabinetDocumentsByCabinetDocument(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Voir un document */
    getCabinetDocumentsByCabinetDocument2(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Voir un document */
    getCabinetDocumentsByCabinetDocument3(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Voir un document */
    getCabinetDocumentsByCabinetDocument4(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Voir un document */
    getCabinetDocumentsByCabinetDocument5(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Voir un document */
    getCabinetDocumentsByCabinetDocument6(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Voir un document */
    getCabinetDocumentsByCabinetDocument7(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Voir un document */
    getCabinetDocumentsByCabinetDocument8(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Voir un document */
    getCabinetDocumentsByCabinetDocument9(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Voir un document */
    getCabinetDocumentsByCabinetDocument10(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Voir un document */
    getCabinetDocumentsByCabinetDocument11(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Modifier un document */
    putCabinetDocumentsByCabinetDocument(options = {}) {
      return request("PUT", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Modifier un document */
    putCabinetDocumentsByCabinetDocument2(options = {}) {
      return request("PUT", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Modifier un document */
    putCabinetDocumentsByCabinetDocument3(options = {}) {
      return request("PUT", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Modifier un document */
    putCabinetDocumentsByCabinetDocument4(options = {}) {
      return request("PUT", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Modifier un document */
    putCabinetDocumentsByCabinetDocument5(options = {}) {
      return request("PUT", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Modifier un document */
    putCabinetDocumentsByCabinetDocument6(options = {}) {
      return request("PUT", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Modifier un document */
    putCabinetDocumentsByCabinetDocument7(options = {}) {
      return request("PUT", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Modifier un document */
    putCabinetDocumentsByCabinetDocument8(options = {}) {
      return request("PUT", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Modifier un document */
    putCabinetDocumentsByCabinetDocument9(options = {}) {
      return request("PUT", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Modifier un document */
    putCabinetDocumentsByCabinetDocument10(options = {}) {
      return request("PUT", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Modifier un document */
    putCabinetDocumentsByCabinetDocument11(options = {}) {
      return request("PUT", "/cabinet/documents/{cabinetDocument}", options);
    },

    /** Telecharger un document */
    getCabinetDocumentsByCabinetDocumentDownload(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}/download", options);
    },

    /** Telecharger un document */
    getCabinetDocumentsByCabinetDocumentDownload2(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}/download", options);
    },

    /** Telecharger un document */
    getCabinetDocumentsByCabinetDocumentDownload3(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}/download", options);
    },

    /** Telecharger un document */
    getCabinetDocumentsByCabinetDocumentDownload4(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}/download", options);
    },

    /** Telecharger un document */
    getCabinetDocumentsByCabinetDocumentDownload5(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}/download", options);
    },

    /** Telecharger un document */
    getCabinetDocumentsByCabinetDocumentDownload6(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}/download", options);
    },

    /** Telecharger un document */
    getCabinetDocumentsByCabinetDocumentDownload7(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}/download", options);
    },

    /** Telecharger un document */
    getCabinetDocumentsByCabinetDocumentDownload8(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}/download", options);
    },

    /** Telecharger un document */
    getCabinetDocumentsByCabinetDocumentDownload9(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}/download", options);
    },

    /** Telecharger un document */
    getCabinetDocumentsByCabinetDocumentDownload10(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}/download", options);
    },

    /** Telecharger un document */
    getCabinetDocumentsByCabinetDocumentDownload11(options = {}) {
      return request("GET", "/cabinet/documents/{cabinetDocument}/download", options);
    },

    /** Deplacer un document */
    patchCabinetDocumentsByCabinetDocumentMove(options = {}) {
      return request("PATCH", "/cabinet/documents/{cabinetDocument}/move", options);
    },

    /** Deplacer un document */
    patchCabinetDocumentsByCabinetDocumentMove2(options = {}) {
      return request("PATCH", "/cabinet/documents/{cabinetDocument}/move", options);
    },

    /** Deplacer un document */
    patchCabinetDocumentsByCabinetDocumentMove3(options = {}) {
      return request("PATCH", "/cabinet/documents/{cabinetDocument}/move", options);
    },

    /** Deplacer un document */
    patchCabinetDocumentsByCabinetDocumentMove4(options = {}) {
      return request("PATCH", "/cabinet/documents/{cabinetDocument}/move", options);
    },

    /** Deplacer un document */
    patchCabinetDocumentsByCabinetDocumentMove5(options = {}) {
      return request("PATCH", "/cabinet/documents/{cabinetDocument}/move", options);
    },

    /** Deplacer un document */
    patchCabinetDocumentsByCabinetDocumentMove6(options = {}) {
      return request("PATCH", "/cabinet/documents/{cabinetDocument}/move", options);
    },

    /** Deplacer un document */
    patchCabinetDocumentsByCabinetDocumentMove7(options = {}) {
      return request("PATCH", "/cabinet/documents/{cabinetDocument}/move", options);
    },

    /** Deplacer un document */
    patchCabinetDocumentsByCabinetDocumentMove8(options = {}) {
      return request("PATCH", "/cabinet/documents/{cabinetDocument}/move", options);
    },

    /** Deplacer un document */
    patchCabinetDocumentsByCabinetDocumentMove9(options = {}) {
      return request("PATCH", "/cabinet/documents/{cabinetDocument}/move", options);
    },

    /** Deplacer un document */
    patchCabinetDocumentsByCabinetDocumentMove10(options = {}) {
      return request("PATCH", "/cabinet/documents/{cabinetDocument}/move", options);
    },

    /** Deplacer un document */
    patchCabinetDocumentsByCabinetDocumentMove11(options = {}) {
      return request("PATCH", "/cabinet/documents/{cabinetDocument}/move", options);
    },

    /** Lister les dossiers */
    getCabinetFolders(options = {}) {
      return request("GET", "/cabinet/folders", options);
    },

    /** Lister les dossiers */
    getCabinetFolders2(options = {}) {
      return request("GET", "/cabinet/folders", options);
    },

    /** Lister les dossiers */
    getCabinetFolders3(options = {}) {
      return request("GET", "/cabinet/folders", options);
    },

    /** Lister les dossiers */
    getCabinetFolders4(options = {}) {
      return request("GET", "/cabinet/folders", options);
    },

    /** Lister les dossiers */
    getCabinetFolders5(options = {}) {
      return request("GET", "/cabinet/folders", options);
    },

    /** Lister les dossiers */
    getCabinetFolders6(options = {}) {
      return request("GET", "/cabinet/folders", options);
    },

    /** Lister les dossiers */
    getCabinetFolders7(options = {}) {
      return request("GET", "/cabinet/folders", options);
    },

    /** Lister les dossiers */
    getCabinetFolders8(options = {}) {
      return request("GET", "/cabinet/folders", options);
    },

    /** Lister les dossiers */
    getCabinetFolders9(options = {}) {
      return request("GET", "/cabinet/folders", options);
    },

    /** Lister les dossiers */
    getCabinetFolders10(options = {}) {
      return request("GET", "/cabinet/folders", options);
    },

    /** Lister les dossiers */
    getCabinetFolders11(options = {}) {
      return request("GET", "/cabinet/folders", options);
    },

    /** Creer un dossier */
    postCabinetFolders(options = {}) {
      return request("POST", "/cabinet/folders", options);
    },

    /** Creer un dossier */
    postCabinetFolders2(options = {}) {
      return request("POST", "/cabinet/folders", options);
    },

    /** Creer un dossier */
    postCabinetFolders3(options = {}) {
      return request("POST", "/cabinet/folders", options);
    },

    /** Creer un dossier */
    postCabinetFolders4(options = {}) {
      return request("POST", "/cabinet/folders", options);
    },

    /** Creer un dossier */
    postCabinetFolders5(options = {}) {
      return request("POST", "/cabinet/folders", options);
    },

    /** Creer un dossier */
    postCabinetFolders6(options = {}) {
      return request("POST", "/cabinet/folders", options);
    },

    /** Creer un dossier */
    postCabinetFolders7(options = {}) {
      return request("POST", "/cabinet/folders", options);
    },

    /** Creer un dossier */
    postCabinetFolders8(options = {}) {
      return request("POST", "/cabinet/folders", options);
    },

    /** Creer un dossier */
    postCabinetFolders9(options = {}) {
      return request("POST", "/cabinet/folders", options);
    },

    /** Creer un dossier */
    postCabinetFolders10(options = {}) {
      return request("POST", "/cabinet/folders", options);
    },

    /** Creer un dossier */
    postCabinetFolders11(options = {}) {
      return request("POST", "/cabinet/folders", options);
    },

    /** Supprimer un dossier */
    deleteCabinetFoldersByCabinetFolder(options = {}) {
      return request("DELETE", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Supprimer un dossier */
    deleteCabinetFoldersByCabinetFolder2(options = {}) {
      return request("DELETE", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Supprimer un dossier */
    deleteCabinetFoldersByCabinetFolder3(options = {}) {
      return request("DELETE", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Supprimer un dossier */
    deleteCabinetFoldersByCabinetFolder4(options = {}) {
      return request("DELETE", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Supprimer un dossier */
    deleteCabinetFoldersByCabinetFolder5(options = {}) {
      return request("DELETE", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Supprimer un dossier */
    deleteCabinetFoldersByCabinetFolder6(options = {}) {
      return request("DELETE", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Supprimer un dossier */
    deleteCabinetFoldersByCabinetFolder7(options = {}) {
      return request("DELETE", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Supprimer un dossier */
    deleteCabinetFoldersByCabinetFolder8(options = {}) {
      return request("DELETE", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Supprimer un dossier */
    deleteCabinetFoldersByCabinetFolder9(options = {}) {
      return request("DELETE", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Supprimer un dossier */
    deleteCabinetFoldersByCabinetFolder10(options = {}) {
      return request("DELETE", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Supprimer un dossier */
    deleteCabinetFoldersByCabinetFolder11(options = {}) {
      return request("DELETE", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Voir un dossier */
    getCabinetFoldersByCabinetFolder(options = {}) {
      return request("GET", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Voir un dossier */
    getCabinetFoldersByCabinetFolder2(options = {}) {
      return request("GET", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Voir un dossier */
    getCabinetFoldersByCabinetFolder3(options = {}) {
      return request("GET", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Voir un dossier */
    getCabinetFoldersByCabinetFolder4(options = {}) {
      return request("GET", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Voir un dossier */
    getCabinetFoldersByCabinetFolder5(options = {}) {
      return request("GET", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Voir un dossier */
    getCabinetFoldersByCabinetFolder6(options = {}) {
      return request("GET", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Voir un dossier */
    getCabinetFoldersByCabinetFolder7(options = {}) {
      return request("GET", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Voir un dossier */
    getCabinetFoldersByCabinetFolder8(options = {}) {
      return request("GET", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Voir un dossier */
    getCabinetFoldersByCabinetFolder9(options = {}) {
      return request("GET", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Voir un dossier */
    getCabinetFoldersByCabinetFolder10(options = {}) {
      return request("GET", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Voir un dossier */
    getCabinetFoldersByCabinetFolder11(options = {}) {
      return request("GET", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Modifier un dossier */
    putCabinetFoldersByCabinetFolder(options = {}) {
      return request("PUT", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Modifier un dossier */
    putCabinetFoldersByCabinetFolder2(options = {}) {
      return request("PUT", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Modifier un dossier */
    putCabinetFoldersByCabinetFolder3(options = {}) {
      return request("PUT", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Modifier un dossier */
    putCabinetFoldersByCabinetFolder4(options = {}) {
      return request("PUT", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Modifier un dossier */
    putCabinetFoldersByCabinetFolder5(options = {}) {
      return request("PUT", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Modifier un dossier */
    putCabinetFoldersByCabinetFolder6(options = {}) {
      return request("PUT", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Modifier un dossier */
    putCabinetFoldersByCabinetFolder7(options = {}) {
      return request("PUT", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Modifier un dossier */
    putCabinetFoldersByCabinetFolder8(options = {}) {
      return request("PUT", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Modifier un dossier */
    putCabinetFoldersByCabinetFolder9(options = {}) {
      return request("PUT", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Modifier un dossier */
    putCabinetFoldersByCabinetFolder10(options = {}) {
      return request("PUT", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Modifier un dossier */
    putCabinetFoldersByCabinetFolder11(options = {}) {
      return request("PUT", "/cabinet/folders/{cabinetFolder}", options);
    },

    /** Acceder a un partage par jeton (lien public) */
    getCabinetSharedByToken(options = {}) {
      return request("GET", "/cabinet/shared/{token}", options);
    },

    /** Acceder a un partage par jeton (lien public) */
    getCabinetSharedByToken2(options = {}) {
      return request("GET", "/cabinet/shared/{token}", options);
    },

    /** Acceder a un partage par jeton (lien public) */
    getCabinetSharedByToken3(options = {}) {
      return request("GET", "/cabinet/shared/{token}", options);
    },

    /** Acceder a un partage par jeton (lien public) */
    getCabinetSharedByToken4(options = {}) {
      return request("GET", "/cabinet/shared/{token}", options);
    },

    /** Acceder a un partage par jeton (lien public) */
    getCabinetSharedByToken5(options = {}) {
      return request("GET", "/cabinet/shared/{token}", options);
    },

    /** Acceder a un partage par jeton (lien public) */
    getCabinetSharedByToken6(options = {}) {
      return request("GET", "/cabinet/shared/{token}", options);
    },

    /** Acceder a un partage par jeton (lien public) */
    getCabinetSharedByToken7(options = {}) {
      return request("GET", "/cabinet/shared/{token}", options);
    },

    /** Acceder a un partage par jeton (lien public) */
    getCabinetSharedByToken8(options = {}) {
      return request("GET", "/cabinet/shared/{token}", options);
    },

    /** Acceder a un partage par jeton (lien public) */
    getCabinetSharedByToken9(options = {}) {
      return request("GET", "/cabinet/shared/{token}", options);
    },

    /** Acceder a un partage par jeton (lien public) */
    getCabinetSharedByToken10(options = {}) {
      return request("GET", "/cabinet/shared/{token}", options);
    },

    /** Acceder a un partage par jeton (lien public) */
    getCabinetSharedByToken11(options = {}) {
      return request("GET", "/cabinet/shared/{token}", options);
    },

    /** Lister les partages du cabinet */
    getCabinetShares(options = {}) {
      return request("GET", "/cabinet/shares", options);
    },

    /** Lister les partages du cabinet */
    getCabinetShares2(options = {}) {
      return request("GET", "/cabinet/shares", options);
    },

    /** Lister les partages du cabinet */
    getCabinetShares3(options = {}) {
      return request("GET", "/cabinet/shares", options);
    },

    /** Lister les partages du cabinet */
    getCabinetShares4(options = {}) {
      return request("GET", "/cabinet/shares", options);
    },

    /** Lister les partages du cabinet */
    getCabinetShares5(options = {}) {
      return request("GET", "/cabinet/shares", options);
    },

    /** Lister les partages du cabinet */
    getCabinetShares6(options = {}) {
      return request("GET", "/cabinet/shares", options);
    },

    /** Lister les partages du cabinet */
    getCabinetShares7(options = {}) {
      return request("GET", "/cabinet/shares", options);
    },

    /** Lister les partages du cabinet */
    getCabinetShares8(options = {}) {
      return request("GET", "/cabinet/shares", options);
    },

    /** Lister les partages du cabinet */
    getCabinetShares9(options = {}) {
      return request("GET", "/cabinet/shares", options);
    },

    /** Lister les partages du cabinet */
    getCabinetShares10(options = {}) {
      return request("GET", "/cabinet/shares", options);
    },

    /** Lister les partages du cabinet */
    getCabinetShares11(options = {}) {
      return request("GET", "/cabinet/shares", options);
    },

    /** Creer un partage de dossier ou document */
    postCabinetShares(options = {}) {
      return request("POST", "/cabinet/shares", options);
    },

    /** Creer un partage de dossier ou document */
    postCabinetShares2(options = {}) {
      return request("POST", "/cabinet/shares", options);
    },

    /** Creer un partage de dossier ou document */
    postCabinetShares3(options = {}) {
      return request("POST", "/cabinet/shares", options);
    },

    /** Creer un partage de dossier ou document */
    postCabinetShares4(options = {}) {
      return request("POST", "/cabinet/shares", options);
    },

    /** Creer un partage de dossier ou document */
    postCabinetShares5(options = {}) {
      return request("POST", "/cabinet/shares", options);
    },

    /** Creer un partage de dossier ou document */
    postCabinetShares6(options = {}) {
      return request("POST", "/cabinet/shares", options);
    },

    /** Creer un partage de dossier ou document */
    postCabinetShares7(options = {}) {
      return request("POST", "/cabinet/shares", options);
    },

    /** Creer un partage de dossier ou document */
    postCabinetShares8(options = {}) {
      return request("POST", "/cabinet/shares", options);
    },

    /** Creer un partage de dossier ou document */
    postCabinetShares9(options = {}) {
      return request("POST", "/cabinet/shares", options);
    },

    /** Creer un partage de dossier ou document */
    postCabinetShares10(options = {}) {
      return request("POST", "/cabinet/shares", options);
    },

    /** Creer un partage de dossier ou document */
    postCabinetShares11(options = {}) {
      return request("POST", "/cabinet/shares", options);
    },

    /** Revoquer un partage */
    deleteCabinetSharesByCabinetShare(options = {}) {
      return request("DELETE", "/cabinet/shares/{cabinetShare}", options);
    },

    /** Revoquer un partage */
    deleteCabinetSharesByCabinetShare2(options = {}) {
      return request("DELETE", "/cabinet/shares/{cabinetShare}", options);
    },

    /** Revoquer un partage */
    deleteCabinetSharesByCabinetShare3(options = {}) {
      return request("DELETE", "/cabinet/shares/{cabinetShare}", options);
    },

    /** Revoquer un partage */
    deleteCabinetSharesByCabinetShare4(options = {}) {
      return request("DELETE", "/cabinet/shares/{cabinetShare}", options);
    },

    /** Revoquer un partage */
    deleteCabinetSharesByCabinetShare5(options = {}) {
      return request("DELETE", "/cabinet/shares/{cabinetShare}", options);
    },

    /** Revoquer un partage */
    deleteCabinetSharesByCabinetShare6(options = {}) {
      return request("DELETE", "/cabinet/shares/{cabinetShare}", options);
    },

    /** Revoquer un partage */
    deleteCabinetSharesByCabinetShare7(options = {}) {
      return request("DELETE", "/cabinet/shares/{cabinetShare}", options);
    },

    /** Revoquer un partage */
    deleteCabinetSharesByCabinetShare8(options = {}) {
      return request("DELETE", "/cabinet/shares/{cabinetShare}", options);
    },

    /** Revoquer un partage */
    deleteCabinetSharesByCabinetShare9(options = {}) {
      return request("DELETE", "/cabinet/shares/{cabinetShare}", options);
    },

    /** Revoquer un partage */
    deleteCabinetSharesByCabinetShare10(options = {}) {
      return request("DELETE", "/cabinet/shares/{cabinetShare}", options);
    },

    /** Revoquer un partage */
    deleteCabinetSharesByCabinetShare11(options = {}) {
      return request("DELETE", "/cabinet/shares/{cabinetShare}", options);
    },

    /** Statistiques de stockage du placard de l'employe courant */
    getCabinetStats(options = {}) {
      return request("GET", "/cabinet/stats", options);
    },

    /** Statistiques de stockage du placard de l'employe courant */
    getCabinetStats2(options = {}) {
      return request("GET", "/cabinet/stats", options);
    },

    /** Statistiques de stockage du placard de l'employe courant */
    getCabinetStats3(options = {}) {
      return request("GET", "/cabinet/stats", options);
    },

    /** Statistiques de stockage du placard de l'employe courant */
    getCabinetStats4(options = {}) {
      return request("GET", "/cabinet/stats", options);
    },

    /** Statistiques de stockage du placard de l'employe courant */
    getCabinetStats5(options = {}) {
      return request("GET", "/cabinet/stats", options);
    },

    /** Statistiques de stockage du placard de l'employe courant */
    getCabinetStats6(options = {}) {
      return request("GET", "/cabinet/stats", options);
    },

    /** Statistiques de stockage du placard de l'employe courant */
    getCabinetStats7(options = {}) {
      return request("GET", "/cabinet/stats", options);
    },

    /** Statistiques de stockage du placard de l'employe courant */
    getCabinetStats8(options = {}) {
      return request("GET", "/cabinet/stats", options);
    },

    /** Statistiques de stockage du placard de l'employe courant */
    getCabinetStats9(options = {}) {
      return request("GET", "/cabinet/stats", options);
    },

    /** Statistiques de stockage du placard de l'employe courant */
    getCabinetStats10(options = {}) {
      return request("GET", "/cabinet/stats", options);
    },

    /** Statistiques de stockage du placard de l'employe courant */
    getCabinetStats11(options = {}) {
      return request("GET", "/cabinet/stats", options);
    },

    /** Connecter un calendrier (google, outlook, caldav) */
    postCalendarConnect(options = {}) {
      return request("POST", "/calendar/connect", options);
    },

    /** Connecter un calendrier (google, outlook, caldav) */
    postCalendarConnect2(options = {}) {
      return request("POST", "/calendar/connect", options);
    },

    /** Connecter un calendrier (google, outlook, caldav) */
    postCalendarConnect3(options = {}) {
      return request("POST", "/calendar/connect", options);
    },

    /** Connecter un calendrier (google, outlook, caldav) */
    postCalendarConnect4(options = {}) {
      return request("POST", "/calendar/connect", options);
    },

    /** Connecter un calendrier (google, outlook, caldav) */
    postCalendarConnect5(options = {}) {
      return request("POST", "/calendar/connect", options);
    },

    /** Connecter un calendrier (google, outlook, caldav) */
    postCalendarConnect6(options = {}) {
      return request("POST", "/calendar/connect", options);
    },

    /** Connecter un calendrier (google, outlook, caldav) */
    postCalendarConnect7(options = {}) {
      return request("POST", "/calendar/connect", options);
    },

    /** Connecter un calendrier (google, outlook, caldav) */
    postCalendarConnect8(options = {}) {
      return request("POST", "/calendar/connect", options);
    },

    /** Connecter un calendrier (google, outlook, caldav) */
    postCalendarConnect9(options = {}) {
      return request("POST", "/calendar/connect", options);
    },

    /** Connecter un calendrier (google, outlook, caldav) */
    postCalendarConnect10(options = {}) {
      return request("POST", "/calendar/connect", options);
    },

    /** Connecter un calendrier (google, outlook, caldav) */
    postCalendarConnect11(options = {}) {
      return request("POST", "/calendar/connect", options);
    },

    /** Lister les connexions calendrier de l'employe */
    getCalendarConnections(options = {}) {
      return request("GET", "/calendar/connections", options);
    },

    /** Lister les connexions calendrier de l'employe */
    getCalendarConnections2(options = {}) {
      return request("GET", "/calendar/connections", options);
    },

    /** Lister les connexions calendrier de l'employe */
    getCalendarConnections3(options = {}) {
      return request("GET", "/calendar/connections", options);
    },

    /** Lister les connexions calendrier de l'employe */
    getCalendarConnections4(options = {}) {
      return request("GET", "/calendar/connections", options);
    },

    /** Lister les connexions calendrier de l'employe */
    getCalendarConnections5(options = {}) {
      return request("GET", "/calendar/connections", options);
    },

    /** Lister les connexions calendrier de l'employe */
    getCalendarConnections6(options = {}) {
      return request("GET", "/calendar/connections", options);
    },

    /** Lister les connexions calendrier de l'employe */
    getCalendarConnections7(options = {}) {
      return request("GET", "/calendar/connections", options);
    },

    /** Lister les connexions calendrier de l'employe */
    getCalendarConnections8(options = {}) {
      return request("GET", "/calendar/connections", options);
    },

    /** Lister les connexions calendrier de l'employe */
    getCalendarConnections9(options = {}) {
      return request("GET", "/calendar/connections", options);
    },

    /** Lister les connexions calendrier de l'employe */
    getCalendarConnections10(options = {}) {
      return request("GET", "/calendar/connections", options);
    },

    /** Lister les connexions calendrier de l'employe */
    getCalendarConnections11(options = {}) {
      return request("GET", "/calendar/connections", options);
    },

    /** Deconnecter un calendrier */
    deleteCalendarDisconnectByProvider(options = {}) {
      return request("DELETE", "/calendar/disconnect/{provider}", options);
    },

    /** Deconnecter un calendrier */
    deleteCalendarDisconnectByProvider2(options = {}) {
      return request("DELETE", "/calendar/disconnect/{provider}", options);
    },

    /** Deconnecter un calendrier */
    deleteCalendarDisconnectByProvider3(options = {}) {
      return request("DELETE", "/calendar/disconnect/{provider}", options);
    },

    /** Deconnecter un calendrier */
    deleteCalendarDisconnectByProvider4(options = {}) {
      return request("DELETE", "/calendar/disconnect/{provider}", options);
    },

    /** Deconnecter un calendrier */
    deleteCalendarDisconnectByProvider5(options = {}) {
      return request("DELETE", "/calendar/disconnect/{provider}", options);
    },

    /** Deconnecter un calendrier */
    deleteCalendarDisconnectByProvider6(options = {}) {
      return request("DELETE", "/calendar/disconnect/{provider}", options);
    },

    /** Deconnecter un calendrier */
    deleteCalendarDisconnectByProvider7(options = {}) {
      return request("DELETE", "/calendar/disconnect/{provider}", options);
    },

    /** Deconnecter un calendrier */
    deleteCalendarDisconnectByProvider8(options = {}) {
      return request("DELETE", "/calendar/disconnect/{provider}", options);
    },

    /** Deconnecter un calendrier */
    deleteCalendarDisconnectByProvider9(options = {}) {
      return request("DELETE", "/calendar/disconnect/{provider}", options);
    },

    /** Deconnecter un calendrier */
    deleteCalendarDisconnectByProvider10(options = {}) {
      return request("DELETE", "/calendar/disconnect/{provider}", options);
    },

    /** Deconnecter un calendrier */
    deleteCalendarDisconnectByProvider11(options = {}) {
      return request("DELETE", "/calendar/disconnect/{provider}", options);
    },

    /** Lister les evenements calendrier sur une periode */
    getCalendarEvents(options = {}) {
      return request("GET", "/calendar/events", options);
    },

    /** Lister les evenements calendrier sur une periode */
    getCalendarEvents2(options = {}) {
      return request("GET", "/calendar/events", options);
    },

    /** Lister les evenements calendrier sur une periode */
    getCalendarEvents3(options = {}) {
      return request("GET", "/calendar/events", options);
    },

    /** Lister les evenements calendrier sur une periode */
    getCalendarEvents4(options = {}) {
      return request("GET", "/calendar/events", options);
    },

    /** Lister les evenements calendrier sur une periode */
    getCalendarEvents5(options = {}) {
      return request("GET", "/calendar/events", options);
    },

    /** Lister les evenements calendrier sur une periode */
    getCalendarEvents6(options = {}) {
      return request("GET", "/calendar/events", options);
    },

    /** Lister les evenements calendrier sur une periode */
    getCalendarEvents7(options = {}) {
      return request("GET", "/calendar/events", options);
    },

    /** Lister les evenements calendrier sur une periode */
    getCalendarEvents8(options = {}) {
      return request("GET", "/calendar/events", options);
    },

    /** Lister les evenements calendrier sur une periode */
    getCalendarEvents9(options = {}) {
      return request("GET", "/calendar/events", options);
    },

    /** Lister les evenements calendrier sur une periode */
    getCalendarEvents10(options = {}) {
      return request("GET", "/calendar/events", options);
    },

    /** Lister les evenements calendrier sur une periode */
    getCalendarEvents11(options = {}) {
      return request("GET", "/calendar/events", options);
    },

    /** Synchroniser conges et formations vers le calendrier */
    postCalendarSync(options = {}) {
      return request("POST", "/calendar/sync", options);
    },

    /** Synchroniser conges et formations vers le calendrier */
    postCalendarSync2(options = {}) {
      return request("POST", "/calendar/sync", options);
    },

    /** Synchroniser conges et formations vers le calendrier */
    postCalendarSync3(options = {}) {
      return request("POST", "/calendar/sync", options);
    },

    /** Synchroniser conges et formations vers le calendrier */
    postCalendarSync4(options = {}) {
      return request("POST", "/calendar/sync", options);
    },

    /** Synchroniser conges et formations vers le calendrier */
    postCalendarSync5(options = {}) {
      return request("POST", "/calendar/sync", options);
    },

    /** Synchroniser conges et formations vers le calendrier */
    postCalendarSync6(options = {}) {
      return request("POST", "/calendar/sync", options);
    },

    /** Synchroniser conges et formations vers le calendrier */
    postCalendarSync7(options = {}) {
      return request("POST", "/calendar/sync", options);
    },

    /** Synchroniser conges et formations vers le calendrier */
    postCalendarSync8(options = {}) {
      return request("POST", "/calendar/sync", options);
    },

    /** Synchroniser conges et formations vers le calendrier */
    postCalendarSync9(options = {}) {
      return request("POST", "/calendar/sync", options);
    },

    /** Synchroniser conges et formations vers le calendrier */
    postCalendarSync10(options = {}) {
      return request("POST", "/calendar/sync", options);
    },

    /** Synchroniser conges et formations vers le calendrier */
    postCalendarSync11(options = {}) {
      return request("POST", "/calendar/sync", options);
    },

    /** Lister les cameras */
    getCameras(options = {}) {
      return request("GET", "/cameras", options);
    },

    /** Lister les cameras */
    getCameras2(options = {}) {
      return request("GET", "/cameras", options);
    },

    /** Lister les cameras */
    getCameras3(options = {}) {
      return request("GET", "/cameras", options);
    },

    /** Lister les cameras */
    getCameras4(options = {}) {
      return request("GET", "/cameras", options);
    },

    /** Lister les cameras */
    getCameras5(options = {}) {
      return request("GET", "/cameras", options);
    },

    /** Lister les cameras */
    getCameras6(options = {}) {
      return request("GET", "/cameras", options);
    },

    /** Lister les cameras */
    getCameras7(options = {}) {
      return request("GET", "/cameras", options);
    },

    /** Lister les cameras */
    getCameras8(options = {}) {
      return request("GET", "/cameras", options);
    },

    /** Creer une camera */
    postCameras(options = {}) {
      return request("POST", "/cameras", options);
    },

    /** Creer une camera */
    postCameras2(options = {}) {
      return request("POST", "/cameras", options);
    },

    /** Creer une camera */
    postCameras3(options = {}) {
      return request("POST", "/cameras", options);
    },

    /** Creer une camera */
    postCameras4(options = {}) {
      return request("POST", "/cameras", options);
    },

    /** Creer une camera */
    postCameras5(options = {}) {
      return request("POST", "/cameras", options);
    },

    /** Creer une camera */
    postCameras6(options = {}) {
      return request("POST", "/cameras", options);
    },

    /** Creer une camera */
    postCameras7(options = {}) {
      return request("POST", "/cameras", options);
    },

    /** Creer une camera */
    postCameras8(options = {}) {
      return request("POST", "/cameras", options);
    },

    /** Desactiver une camera */
    deleteCamerasByCamera(options = {}) {
      return request("DELETE", "/cameras/{camera}", options);
    },

    /** Desactiver une camera */
    deleteCamerasByCamera2(options = {}) {
      return request("DELETE", "/cameras/{camera}", options);
    },

    /** Desactiver une camera */
    deleteCamerasByCamera3(options = {}) {
      return request("DELETE", "/cameras/{camera}", options);
    },

    /** Desactiver une camera */
    deleteCamerasByCamera4(options = {}) {
      return request("DELETE", "/cameras/{camera}", options);
    },

    /** Desactiver une camera */
    deleteCamerasByCamera5(options = {}) {
      return request("DELETE", "/cameras/{camera}", options);
    },

    /** Desactiver une camera */
    deleteCamerasByCamera6(options = {}) {
      return request("DELETE", "/cameras/{camera}", options);
    },

    /** Desactiver une camera */
    deleteCamerasByCamera7(options = {}) {
      return request("DELETE", "/cameras/{camera}", options);
    },

    /** Desactiver une camera */
    deleteCamerasByCamera8(options = {}) {
      return request("DELETE", "/cameras/{camera}", options);
    },

    /** Voir une camera */
    getCamerasByCamera(options = {}) {
      return request("GET", "/cameras/{camera}", options);
    },

    /** Voir une camera */
    getCamerasByCamera2(options = {}) {
      return request("GET", "/cameras/{camera}", options);
    },

    /** Voir une camera */
    getCamerasByCamera3(options = {}) {
      return request("GET", "/cameras/{camera}", options);
    },

    /** Voir une camera */
    getCamerasByCamera4(options = {}) {
      return request("GET", "/cameras/{camera}", options);
    },

    /** Voir une camera */
    getCamerasByCamera5(options = {}) {
      return request("GET", "/cameras/{camera}", options);
    },

    /** Voir une camera */
    getCamerasByCamera6(options = {}) {
      return request("GET", "/cameras/{camera}", options);
    },

    /** Voir une camera */
    getCamerasByCamera7(options = {}) {
      return request("GET", "/cameras/{camera}", options);
    },

    /** Voir une camera */
    getCamerasByCamera8(options = {}) {
      return request("GET", "/cameras/{camera}", options);
    },

    /** Patch camera */
    patchCamerasByCamera(options = {}) {
      return request("PATCH", "/cameras/{camera}", options);
    },

    /** Patch camera */
    patchCamerasByCamera2(options = {}) {
      return request("PATCH", "/cameras/{camera}", options);
    },

    /** Patch camera */
    patchCamerasByCamera3(options = {}) {
      return request("PATCH", "/cameras/{camera}", options);
    },

    /** Patch camera */
    patchCamerasByCamera4(options = {}) {
      return request("PATCH", "/cameras/{camera}", options);
    },

    /** Patch camera */
    patchCamerasByCamera5(options = {}) {
      return request("PATCH", "/cameras/{camera}", options);
    },

    /** Patch camera */
    patchCamerasByCamera6(options = {}) {
      return request("PATCH", "/cameras/{camera}", options);
    },

    /** Patch camera */
    patchCamerasByCamera7(options = {}) {
      return request("PATCH", "/cameras/{camera}", options);
    },

    /** Patch camera */
    patchCamerasByCamera8(options = {}) {
      return request("PATCH", "/cameras/{camera}", options);
    },

    /** Mettre a jour une camera */
    putCamerasByCamera(options = {}) {
      return request("PUT", "/cameras/{camera}", options);
    },

    /** Mettre a jour une camera */
    putCamerasByCamera2(options = {}) {
      return request("PUT", "/cameras/{camera}", options);
    },

    /** Mettre a jour une camera */
    putCamerasByCamera3(options = {}) {
      return request("PUT", "/cameras/{camera}", options);
    },

    /** Mettre a jour une camera */
    putCamerasByCamera4(options = {}) {
      return request("PUT", "/cameras/{camera}", options);
    },

    /** Mettre a jour une camera */
    putCamerasByCamera5(options = {}) {
      return request("PUT", "/cameras/{camera}", options);
    },

    /** Mettre a jour une camera */
    putCamerasByCamera6(options = {}) {
      return request("PUT", "/cameras/{camera}", options);
    },

    /** Mettre a jour une camera */
    putCamerasByCamera7(options = {}) {
      return request("PUT", "/cameras/{camera}", options);
    },

    /** Mettre a jour une camera */
    putCamerasByCamera8(options = {}) {
      return request("PUT", "/cameras/{camera}", options);
    },

    /** Lister les logs d'acces camera */
    getCamerasByCameraAccessLogs(options = {}) {
      return request("GET", "/cameras/{camera}/access-logs", options);
    },

    /** Lister les logs d'acces camera */
    getCamerasByCameraAccessLogs2(options = {}) {
      return request("GET", "/cameras/{camera}/access-logs", options);
    },

    /** Lister les logs d'acces camera */
    getCamerasByCameraAccessLogs3(options = {}) {
      return request("GET", "/cameras/{camera}/access-logs", options);
    },

    /** Lister les logs d'acces camera */
    getCamerasByCameraAccessLogs4(options = {}) {
      return request("GET", "/cameras/{camera}/access-logs", options);
    },

    /** Lister les logs d'acces camera */
    getCamerasByCameraAccessLogs5(options = {}) {
      return request("GET", "/cameras/{camera}/access-logs", options);
    },

    /** Lister les logs d'acces camera */
    getCamerasByCameraAccessLogs6(options = {}) {
      return request("GET", "/cameras/{camera}/access-logs", options);
    },

    /** Lister les logs d'acces camera */
    getCamerasByCameraAccessLogs7(options = {}) {
      return request("GET", "/cameras/{camera}/access-logs", options);
    },

    /** Lister les logs d'acces camera */
    getCamerasByCameraAccessLogs8(options = {}) {
      return request("GET", "/cameras/{camera}/access-logs", options);
    },

    /** Lister les tokens de partage */
    getCamerasByCameraAccessTokens(options = {}) {
      return request("GET", "/cameras/{camera}/access-tokens", options);
    },

    /** Lister les tokens de partage */
    getCamerasByCameraAccessTokens2(options = {}) {
      return request("GET", "/cameras/{camera}/access-tokens", options);
    },

    /** Lister les tokens de partage */
    getCamerasByCameraAccessTokens3(options = {}) {
      return request("GET", "/cameras/{camera}/access-tokens", options);
    },

    /** Lister les tokens de partage */
    getCamerasByCameraAccessTokens4(options = {}) {
      return request("GET", "/cameras/{camera}/access-tokens", options);
    },

    /** Lister les tokens de partage */
    getCamerasByCameraAccessTokens5(options = {}) {
      return request("GET", "/cameras/{camera}/access-tokens", options);
    },

    /** Lister les tokens de partage */
    getCamerasByCameraAccessTokens6(options = {}) {
      return request("GET", "/cameras/{camera}/access-tokens", options);
    },

    /** Lister les tokens de partage */
    getCamerasByCameraAccessTokens7(options = {}) {
      return request("GET", "/cameras/{camera}/access-tokens", options);
    },

    /** Lister les tokens de partage */
    getCamerasByCameraAccessTokens8(options = {}) {
      return request("GET", "/cameras/{camera}/access-tokens", options);
    },

    /** Creer un token de partage */
    postCamerasByCameraAccessTokens(options = {}) {
      return request("POST", "/cameras/{camera}/access-tokens", options);
    },

    /** Creer un token de partage */
    postCamerasByCameraAccessTokens2(options = {}) {
      return request("POST", "/cameras/{camera}/access-tokens", options);
    },

    /** Creer un token de partage */
    postCamerasByCameraAccessTokens3(options = {}) {
      return request("POST", "/cameras/{camera}/access-tokens", options);
    },

    /** Creer un token de partage */
    postCamerasByCameraAccessTokens4(options = {}) {
      return request("POST", "/cameras/{camera}/access-tokens", options);
    },

    /** Creer un token de partage */
    postCamerasByCameraAccessTokens5(options = {}) {
      return request("POST", "/cameras/{camera}/access-tokens", options);
    },

    /** Creer un token de partage */
    postCamerasByCameraAccessTokens6(options = {}) {
      return request("POST", "/cameras/{camera}/access-tokens", options);
    },

    /** Creer un token de partage */
    postCamerasByCameraAccessTokens7(options = {}) {
      return request("POST", "/cameras/{camera}/access-tokens", options);
    },

    /** Creer un token de partage */
    postCamerasByCameraAccessTokens8(options = {}) {
      return request("POST", "/cameras/{camera}/access-tokens", options);
    },

    /** Revoquer un token de partage */
    deleteCamerasByCameraAccessTokensByToken(options = {}) {
      return request("DELETE", "/cameras/{camera}/access-tokens/{token}", options);
    },

    /** Revoquer un token de partage */
    deleteCamerasByCameraAccessTokensByToken2(options = {}) {
      return request("DELETE", "/cameras/{camera}/access-tokens/{token}", options);
    },

    /** Revoquer un token de partage */
    deleteCamerasByCameraAccessTokensByToken3(options = {}) {
      return request("DELETE", "/cameras/{camera}/access-tokens/{token}", options);
    },

    /** Revoquer un token de partage */
    deleteCamerasByCameraAccessTokensByToken4(options = {}) {
      return request("DELETE", "/cameras/{camera}/access-tokens/{token}", options);
    },

    /** Revoquer un token de partage */
    deleteCamerasByCameraAccessTokensByToken5(options = {}) {
      return request("DELETE", "/cameras/{camera}/access-tokens/{token}", options);
    },

    /** Revoquer un token de partage */
    deleteCamerasByCameraAccessTokensByToken6(options = {}) {
      return request("DELETE", "/cameras/{camera}/access-tokens/{token}", options);
    },

    /** Revoquer un token de partage */
    deleteCamerasByCameraAccessTokensByToken7(options = {}) {
      return request("DELETE", "/cameras/{camera}/access-tokens/{token}", options);
    },

    /** Revoquer un token de partage */
    deleteCamerasByCameraAccessTokensByToken8(options = {}) {
      return request("DELETE", "/cameras/{camera}/access-tokens/{token}", options);
    },

    /** Lister les permissions internes */
    getCamerasByCameraPermissions(options = {}) {
      return request("GET", "/cameras/{camera}/permissions", options);
    },

    /** Lister les permissions internes */
    getCamerasByCameraPermissions2(options = {}) {
      return request("GET", "/cameras/{camera}/permissions", options);
    },

    /** Lister les permissions internes */
    getCamerasByCameraPermissions3(options = {}) {
      return request("GET", "/cameras/{camera}/permissions", options);
    },

    /** Lister les permissions internes */
    getCamerasByCameraPermissions4(options = {}) {
      return request("GET", "/cameras/{camera}/permissions", options);
    },

    /** Lister les permissions internes */
    getCamerasByCameraPermissions5(options = {}) {
      return request("GET", "/cameras/{camera}/permissions", options);
    },

    /** Lister les permissions internes */
    getCamerasByCameraPermissions6(options = {}) {
      return request("GET", "/cameras/{camera}/permissions", options);
    },

    /** Lister les permissions internes */
    getCamerasByCameraPermissions7(options = {}) {
      return request("GET", "/cameras/{camera}/permissions", options);
    },

    /** Lister les permissions internes */
    getCamerasByCameraPermissions8(options = {}) {
      return request("GET", "/cameras/{camera}/permissions", options);
    },

    /** Creer ou remplacer une permission interne */
    postCamerasByCameraPermissions(options = {}) {
      return request("POST", "/cameras/{camera}/permissions", options);
    },

    /** Creer ou remplacer une permission interne */
    postCamerasByCameraPermissions2(options = {}) {
      return request("POST", "/cameras/{camera}/permissions", options);
    },

    /** Creer ou remplacer une permission interne */
    postCamerasByCameraPermissions3(options = {}) {
      return request("POST", "/cameras/{camera}/permissions", options);
    },

    /** Creer ou remplacer une permission interne */
    postCamerasByCameraPermissions4(options = {}) {
      return request("POST", "/cameras/{camera}/permissions", options);
    },

    /** Creer ou remplacer une permission interne */
    postCamerasByCameraPermissions5(options = {}) {
      return request("POST", "/cameras/{camera}/permissions", options);
    },

    /** Creer ou remplacer une permission interne */
    postCamerasByCameraPermissions6(options = {}) {
      return request("POST", "/cameras/{camera}/permissions", options);
    },

    /** Creer ou remplacer une permission interne */
    postCamerasByCameraPermissions7(options = {}) {
      return request("POST", "/cameras/{camera}/permissions", options);
    },

    /** Creer ou remplacer une permission interne */
    postCamerasByCameraPermissions8(options = {}) {
      return request("POST", "/cameras/{camera}/permissions", options);
    },

    /** Supprimer une permission interne */
    deleteCamerasByCameraPermissionsByPermission(options = {}) {
      return request("DELETE", "/cameras/{camera}/permissions/{permission}", options);
    },

    /** Supprimer une permission interne */
    deleteCamerasByCameraPermissionsByPermission2(options = {}) {
      return request("DELETE", "/cameras/{camera}/permissions/{permission}", options);
    },

    /** Supprimer une permission interne */
    deleteCamerasByCameraPermissionsByPermission3(options = {}) {
      return request("DELETE", "/cameras/{camera}/permissions/{permission}", options);
    },

    /** Supprimer une permission interne */
    deleteCamerasByCameraPermissionsByPermission4(options = {}) {
      return request("DELETE", "/cameras/{camera}/permissions/{permission}", options);
    },

    /** Supprimer une permission interne */
    deleteCamerasByCameraPermissionsByPermission5(options = {}) {
      return request("DELETE", "/cameras/{camera}/permissions/{permission}", options);
    },

    /** Supprimer une permission interne */
    deleteCamerasByCameraPermissionsByPermission6(options = {}) {
      return request("DELETE", "/cameras/{camera}/permissions/{permission}", options);
    },

    /** Supprimer une permission interne */
    deleteCamerasByCameraPermissionsByPermission7(options = {}) {
      return request("DELETE", "/cameras/{camera}/permissions/{permission}", options);
    },

    /** Supprimer une permission interne */
    deleteCamerasByCameraPermissionsByPermission8(options = {}) {
      return request("DELETE", "/cameras/{camera}/permissions/{permission}", options);
    },

    /** Regenerer un stream token client */
    getCamerasByCameraStreamToken(options = {}) {
      return request("GET", "/cameras/{camera}/stream-token", options);
    },

    /** Regenerer un stream token client */
    getCamerasByCameraStreamToken2(options = {}) {
      return request("GET", "/cameras/{camera}/stream-token", options);
    },

    /** Regenerer un stream token client */
    getCamerasByCameraStreamToken3(options = {}) {
      return request("GET", "/cameras/{camera}/stream-token", options);
    },

    /** Regenerer un stream token client */
    getCamerasByCameraStreamToken4(options = {}) {
      return request("GET", "/cameras/{camera}/stream-token", options);
    },

    /** Regenerer un stream token client */
    getCamerasByCameraStreamToken5(options = {}) {
      return request("GET", "/cameras/{camera}/stream-token", options);
    },

    /** Regenerer un stream token client */
    getCamerasByCameraStreamToken6(options = {}) {
      return request("GET", "/cameras/{camera}/stream-token", options);
    },

    /** Regenerer un stream token client */
    getCamerasByCameraStreamToken7(options = {}) {
      return request("GET", "/cameras/{camera}/stream-token", options);
    },

    /** Regenerer un stream token client */
    getCamerasByCameraStreamToken8(options = {}) {
      return request("GET", "/cameras/{camera}/stream-token", options);
    },

    /** Tester une URL RTSP */
    postCamerasTestRtsp(options = {}) {
      return request("POST", "/cameras/test-rtsp", options);
    },

    /** Tester une URL RTSP */
    postCamerasTestRtsp2(options = {}) {
      return request("POST", "/cameras/test-rtsp", options);
    },

    /** Tester une URL RTSP */
    postCamerasTestRtsp3(options = {}) {
      return request("POST", "/cameras/test-rtsp", options);
    },

    /** Tester une URL RTSP */
    postCamerasTestRtsp4(options = {}) {
      return request("POST", "/cameras/test-rtsp", options);
    },

    /** Tester une URL RTSP */
    postCamerasTestRtsp5(options = {}) {
      return request("POST", "/cameras/test-rtsp", options);
    },

    /** Tester une URL RTSP */
    postCamerasTestRtsp6(options = {}) {
      return request("POST", "/cameras/test-rtsp", options);
    },

    /** Tester une URL RTSP */
    postCamerasTestRtsp7(options = {}) {
      return request("POST", "/cameras/test-rtsp", options);
    },

    /** Tester une URL RTSP */
    postCamerasTestRtsp8(options = {}) {
      return request("POST", "/cameras/test-rtsp", options);
    },

    /** Lister les evenements de carriere (plans de carriere, issue #5259) */
    getCareerEvents(options = {}) {
      return request("GET", "/career-events", options);
    },

    /** Lister les evenements de carriere (plans de carriere, issue #5259) */
    getCareerEvents2(options = {}) {
      return request("GET", "/career-events", options);
    },

    /** Lister les evenements de carriere (plans de carriere, issue #5259) */
    getCareerEvents3(options = {}) {
      return request("GET", "/career-events", options);
    },

    /** Lister les evenements de carriere (plans de carriere, issue #5259) */
    getCareerEvents4(options = {}) {
      return request("GET", "/career-events", options);
    },

    /** Creer un evenement de carriere (manager) */
    postCareerEvents(options = {}) {
      return request("POST", "/career-events", options);
    },

    /** Creer un evenement de carriere (manager) */
    postCareerEvents2(options = {}) {
      return request("POST", "/career-events", options);
    },

    /** Creer un evenement de carriere (manager) */
    postCareerEvents3(options = {}) {
      return request("POST", "/career-events", options);
    },

    /** Creer un evenement de carriere (manager) */
    postCareerEvents4(options = {}) {
      return request("POST", "/career-events", options);
    },

    /** Supprimer un evenement de carriere (pending uniquement) */
    deleteCareerEventsByCareerEvent(options = {}) {
      return request("DELETE", "/career-events/{careerEvent}", options);
    },

    /** Supprimer un evenement de carriere (pending uniquement) */
    deleteCareerEventsByCareerEvent2(options = {}) {
      return request("DELETE", "/career-events/{careerEvent}", options);
    },

    /** Supprimer un evenement de carriere (pending uniquement) */
    deleteCareerEventsByCareerEvent3(options = {}) {
      return request("DELETE", "/career-events/{careerEvent}", options);
    },

    /** Supprimer un evenement de carriere (pending uniquement) */
    deleteCareerEventsByCareerEvent4(options = {}) {
      return request("DELETE", "/career-events/{careerEvent}", options);
    },

    /** Voir un evenement de carriere */
    getCareerEventsByCareerEvent(options = {}) {
      return request("GET", "/career-events/{careerEvent}", options);
    },

    /** Voir un evenement de carriere */
    getCareerEventsByCareerEvent2(options = {}) {
      return request("GET", "/career-events/{careerEvent}", options);
    },

    /** Voir un evenement de carriere */
    getCareerEventsByCareerEvent3(options = {}) {
      return request("GET", "/career-events/{careerEvent}", options);
    },

    /** Voir un evenement de carriere */
    getCareerEventsByCareerEvent4(options = {}) {
      return request("GET", "/career-events/{careerEvent}", options);
    },

    /** Modifier partiellement un evenement de carriere (pending uniquement) */
    patchCareerEventsByCareerEvent(options = {}) {
      return request("PATCH", "/career-events/{careerEvent}", options);
    },

    /** Modifier partiellement un evenement de carriere (pending uniquement) */
    patchCareerEventsByCareerEvent2(options = {}) {
      return request("PATCH", "/career-events/{careerEvent}", options);
    },

    /** Modifier partiellement un evenement de carriere (pending uniquement) */
    patchCareerEventsByCareerEvent3(options = {}) {
      return request("PATCH", "/career-events/{careerEvent}", options);
    },

    /** Modifier partiellement un evenement de carriere (pending uniquement) */
    patchCareerEventsByCareerEvent4(options = {}) {
      return request("PATCH", "/career-events/{careerEvent}", options);
    },

    /** Modifier un evenement de carriere (pending uniquement) */
    putCareerEventsByCareerEvent(options = {}) {
      return request("PUT", "/career-events/{careerEvent}", options);
    },

    /** Modifier un evenement de carriere (pending uniquement) */
    putCareerEventsByCareerEvent2(options = {}) {
      return request("PUT", "/career-events/{careerEvent}", options);
    },

    /** Modifier un evenement de carriere (pending uniquement) */
    putCareerEventsByCareerEvent3(options = {}) {
      return request("PUT", "/career-events/{careerEvent}", options);
    },

    /** Modifier un evenement de carriere (pending uniquement) */
    putCareerEventsByCareerEvent4(options = {}) {
      return request("PUT", "/career-events/{careerEvent}", options);
    },

    /** Appliquer un evenement approuve (approved → applied) — met a jour l'employe (poste/departement/salaire de base, impact paie) */
    putCareerEventsByCareerEventApply(options = {}) {
      return request("PUT", "/career-events/{careerEvent}/apply", options);
    },

    /** Appliquer un evenement approuve (approved → applied) — met a jour l'employe (poste/departement/salaire de base, impact paie) */
    putCareerEventsByCareerEventApply2(options = {}) {
      return request("PUT", "/career-events/{careerEvent}/apply", options);
    },

    /** Appliquer un evenement approuve (approved → applied) — met a jour l'employe (poste/departement/salaire de base, impact paie) */
    putCareerEventsByCareerEventApply3(options = {}) {
      return request("PUT", "/career-events/{careerEvent}/apply", options);
    },

    /** Appliquer un evenement approuve (approved → applied) — met a jour l'employe (poste/departement/salaire de base, impact paie) */
    putCareerEventsByCareerEventApply4(options = {}) {
      return request("PUT", "/career-events/{careerEvent}/apply", options);
    },

    /** Approuver un evenement de carriere (pending → approved) */
    putCareerEventsByCareerEventApprove(options = {}) {
      return request("PUT", "/career-events/{careerEvent}/approve", options);
    },

    /** Approuver un evenement de carriere (pending → approved) */
    putCareerEventsByCareerEventApprove2(options = {}) {
      return request("PUT", "/career-events/{careerEvent}/approve", options);
    },

    /** Approuver un evenement de carriere (pending → approved) */
    putCareerEventsByCareerEventApprove3(options = {}) {
      return request("PUT", "/career-events/{careerEvent}/approve", options);
    },

    /** Approuver un evenement de carriere (pending → approved) */
    putCareerEventsByCareerEventApprove4(options = {}) {
      return request("PUT", "/career-events/{careerEvent}/approve", options);
    },

    /** Rejeter un evenement de carriere (pending → rejected) */
    putCareerEventsByCareerEventReject(options = {}) {
      return request("PUT", "/career-events/{careerEvent}/reject", options);
    },

    /** Rejeter un evenement de carriere (pending → rejected) */
    putCareerEventsByCareerEventReject2(options = {}) {
      return request("PUT", "/career-events/{careerEvent}/reject", options);
    },

    /** Rejeter un evenement de carriere (pending → rejected) */
    putCareerEventsByCareerEventReject3(options = {}) {
      return request("PUT", "/career-events/{careerEvent}/reject", options);
    },

    /** Rejeter un evenement de carriere (pending → rejected) */
    putCareerEventsByCareerEventReject4(options = {}) {
      return request("PUT", "/career-events/{careerEvent}/reject", options);
    },

    /** Persister un evenement UX client tenant-scope */
    postClientEvents(options = {}) {
      return request("POST", "/client-events", options);
    },

    /** Persister un evenement UX client tenant-scope */
    postClientEvents2(options = {}) {
      return request("POST", "/client-events", options);
    },

    /** Persister un evenement UX client tenant-scope */
    postClientEvents3(options = {}) {
      return request("POST", "/client-events", options);
    },

    /** Persister un evenement UX client tenant-scope */
    postClientEvents4(options = {}) {
      return request("POST", "/client-events", options);
    },

    /** Persister un evenement UX client tenant-scope */
    postClientEvents5(options = {}) {
      return request("POST", "/client-events", options);
    },

    /** Analytics communication tenant */
    getCommunicationAnalytics(options = {}) {
      return request("GET", "/communication/analytics", options);
    },

    /** Analytics communication tenant */
    getCommunicationAnalytics2(options = {}) {
      return request("GET", "/communication/analytics", options);
    },

    /** Analytics communication tenant */
    getCommunicationAnalytics3(options = {}) {
      return request("GET", "/communication/analytics", options);
    },

    /** Analytics communication tenant */
    getCommunicationAnalytics4(options = {}) {
      return request("GET", "/communication/analytics", options);
    },

    /** Analytics communication tenant */
    getCommunicationAnalytics5(options = {}) {
      return request("GET", "/communication/analytics", options);
    },

    /** Analytics communication tenant */
    getCommunicationAnalytics6(options = {}) {
      return request("GET", "/communication/analytics", options);
    },

    /** Analytics communication tenant */
    getCommunicationAnalytics7(options = {}) {
      return request("GET", "/communication/analytics", options);
    },

    /** Analytics communication tenant */
    getCommunicationAnalytics8(options = {}) {
      return request("GET", "/communication/analytics", options);
    },

    /** Demandes de création d'entreprise de l'utilisateur */
    getCompanyRequests(options = {}) {
      return request("GET", "/company-requests", options);
    },

    /** Demandes de création d'entreprise de l'utilisateur */
    getCompanyRequests2(options = {}) {
      return request("GET", "/company-requests", options);
    },

    /** Demandes de création d'entreprise de l'utilisateur */
    getCompanyRequests3(options = {}) {
      return request("GET", "/company-requests", options);
    },

    /** Demandes de création d'entreprise de l'utilisateur */
    getCompanyRequests4(options = {}) {
      return request("GET", "/company-requests", options);
    },

    /** Demandes de création d'entreprise de l'utilisateur */
    getCompanyRequests5(options = {}) {
      return request("GET", "/company-requests", options);
    },

    /** Soumettre une demande de création d'entreprise */
    postCompanyRequests(options = {}) {
      return request("POST", "/company-requests", options);
    },

    /** Soumettre une demande de création d'entreprise */
    postCompanyRequests2(options = {}) {
      return request("POST", "/company-requests", options);
    },

    /** Soumettre une demande de création d'entreprise */
    postCompanyRequests3(options = {}) {
      return request("POST", "/company-requests", options);
    },

    /** Soumettre une demande de création d'entreprise */
    postCompanyRequests4(options = {}) {
      return request("POST", "/company-requests", options);
    },

    /** Soumettre une demande de création d'entreprise */
    postCompanyRequests5(options = {}) {
      return request("POST", "/company-requests", options);
    },

    /** Lire l'identite visuelle de l'entreprise courante */
    getCompanyBranding(options = {}) {
      return request("GET", "/company/branding", options);
    },

    /** Lire l'identite visuelle de l'entreprise courante */
    getCompanyBranding2(options = {}) {
      return request("GET", "/company/branding", options);
    },

    /** Lire l'identite visuelle de l'entreprise courante */
    getCompanyBranding3(options = {}) {
      return request("GET", "/company/branding", options);
    },

    /** Lire l'identite visuelle de l'entreprise courante */
    getCompanyBranding4(options = {}) {
      return request("GET", "/company/branding", options);
    },

    /** Lire l'identite visuelle de l'entreprise courante */
    getCompanyBranding5(options = {}) {
      return request("GET", "/company/branding", options);
    },

    /** Mettre a jour l'identite visuelle entreprise */
    patchCompanyBranding(options = {}) {
      return request("PATCH", "/company/branding", options);
    },

    /** Mettre a jour l'identite visuelle entreprise */
    patchCompanyBranding2(options = {}) {
      return request("PATCH", "/company/branding", options);
    },

    /** Mettre a jour l'identite visuelle entreprise */
    patchCompanyBranding3(options = {}) {
      return request("PATCH", "/company/branding", options);
    },

    /** Mettre a jour l'identite visuelle entreprise */
    patchCompanyBranding4(options = {}) {
      return request("PATCH", "/company/branding", options);
    },

    /** Mettre a jour l'identite visuelle entreprise */
    patchCompanyBranding5(options = {}) {
      return request("PATCH", "/company/branding", options);
    },

    /** Generer le QR onboarding entreprise */
    getCompanyQrOnboarding(options = {}) {
      return request("GET", "/company/qr-onboarding", options);
    },

    /** Generer le QR onboarding entreprise */
    getCompanyQrOnboarding2(options = {}) {
      return request("GET", "/company/qr-onboarding", options);
    },

    /** Generer le QR onboarding entreprise */
    getCompanyQrOnboarding3(options = {}) {
      return request("GET", "/company/qr-onboarding", options);
    },

    /** Generer le QR onboarding entreprise */
    getCompanyQrOnboarding4(options = {}) {
      return request("GET", "/company/qr-onboarding", options);
    },

    /** Generer le QR onboarding entreprise */
    getCompanyQrOnboarding5(options = {}) {
      return request("GET", "/company/qr-onboarding", options);
    },

    /** Creer un employe depuis QR */
    postCompanyQrOnboardingCreateEmployee(options = {}) {
      return request("POST", "/company/qr-onboarding/create-employee", options);
    },

    /** Creer un employe depuis QR */
    postCompanyQrOnboardingCreateEmployee2(options = {}) {
      return request("POST", "/company/qr-onboarding/create-employee", options);
    },

    /** Creer un employe depuis QR */
    postCompanyQrOnboardingCreateEmployee3(options = {}) {
      return request("POST", "/company/qr-onboarding/create-employee", options);
    },

    /** Creer un employe depuis QR */
    postCompanyQrOnboardingCreateEmployee4(options = {}) {
      return request("POST", "/company/qr-onboarding/create-employee", options);
    },

    /** Creer un employe depuis QR */
    postCompanyQrOnboardingCreateEmployee5(options = {}) {
      return request("POST", "/company/qr-onboarding/create-employee", options);
    },

    /** Lire un QR employe pour pre-remplir l'ajout manager */
    postCompanyQrOnboardingScanEmployee(options = {}) {
      return request("POST", "/company/qr-onboarding/scan-employee", options);
    },

    /** Lire un QR employe pour pre-remplir l'ajout manager */
    postCompanyQrOnboardingScanEmployee2(options = {}) {
      return request("POST", "/company/qr-onboarding/scan-employee", options);
    },

    /** Lire un QR employe pour pre-remplir l'ajout manager */
    postCompanyQrOnboardingScanEmployee3(options = {}) {
      return request("POST", "/company/qr-onboarding/scan-employee", options);
    },

    /** Lire un QR employe pour pre-remplir l'ajout manager */
    postCompanyQrOnboardingScanEmployee4(options = {}) {
      return request("POST", "/company/qr-onboarding/scan-employee", options);
    },

    /** Lire un QR employe pour pre-remplir l'ajout manager */
    postCompanyQrOnboardingScanEmployee5(options = {}) {
      return request("POST", "/company/qr-onboarding/scan-employee", options);
    },

    /** Roles de l'equipe (principal uniquement) */
    getCompanyTeamRoles(options = {}) {
      return request("GET", "/company/team-roles", options);
    },

    /** Roles de l'equipe (principal uniquement) */
    getCompanyTeamRoles2(options = {}) {
      return request("GET", "/company/team-roles", options);
    },

    /** Roles de l'equipe (principal uniquement) */
    getCompanyTeamRoles3(options = {}) {
      return request("GET", "/company/team-roles", options);
    },

    /** Roles de l'equipe (principal uniquement) */
    getCompanyTeamRoles4(options = {}) {
      return request("GET", "/company/team-roles", options);
    },

    /** Roles de l'equipe (principal uniquement) */
    getCompanyTeamRoles5(options = {}) {
      return request("GET", "/company/team-roles", options);
    },

    /** Roles de l'equipe (principal uniquement) */
    getCompanyTeamRoles6(options = {}) {
      return request("GET", "/company/team-roles", options);
    },

    /** Roles de l'equipe (principal uniquement) */
    getCompanyTeamRoles7(options = {}) {
      return request("GET", "/company/team-roles", options);
    },

    /** Roles de l'equipe (principal uniquement) */
    getCompanyTeamRoles8(options = {}) {
      return request("GET", "/company/team-roles", options);
    },

    /** Roles de l'equipe (principal uniquement) */
    getCompanyTeamRoles9(options = {}) {
      return request("GET", "/company/team-roles", options);
    },

    /** Roles de l'equipe (principal uniquement) */
    getCompanyTeamRoles10(options = {}) {
      return request("GET", "/company/team-roles", options);
    },

    /** Roles de l'equipe (principal uniquement) */
    getCompanyTeamRoles11(options = {}) {
      return request("GET", "/company/team-roles", options);
    },

    /** Lister les contrats */
    getContracts(options = {}) {
      return request("GET", "/contracts", options);
    },

    /** Lister les contrats */
    getContracts2(options = {}) {
      return request("GET", "/contracts", options);
    },

    /** Lister les contrats */
    getContracts3(options = {}) {
      return request("GET", "/contracts", options);
    },

    /** Lister les contrats */
    getContracts4(options = {}) {
      return request("GET", "/contracts", options);
    },

    /** Lister les contrats */
    getContracts5(options = {}) {
      return request("GET", "/contracts", options);
    },

    /** Lister les contrats */
    getContracts6(options = {}) {
      return request("GET", "/contracts", options);
    },

    /** Lister les contrats */
    getContracts7(options = {}) {
      return request("GET", "/contracts", options);
    },

    /** Lister les contrats */
    getContracts8(options = {}) {
      return request("GET", "/contracts", options);
    },

    /** Creer un contrat */
    postContracts(options = {}) {
      return request("POST", "/contracts", options);
    },

    /** Creer un contrat */
    postContracts2(options = {}) {
      return request("POST", "/contracts", options);
    },

    /** Creer un contrat */
    postContracts3(options = {}) {
      return request("POST", "/contracts", options);
    },

    /** Creer un contrat */
    postContracts4(options = {}) {
      return request("POST", "/contracts", options);
    },

    /** Creer un contrat */
    postContracts5(options = {}) {
      return request("POST", "/contracts", options);
    },

    /** Creer un contrat */
    postContracts6(options = {}) {
      return request("POST", "/contracts", options);
    },

    /** Creer un contrat */
    postContracts7(options = {}) {
      return request("POST", "/contracts", options);
    },

    /** Creer un contrat */
    postContracts8(options = {}) {
      return request("POST", "/contracts", options);
    },

    /** Voir un contrat */
    getContractsByContract(options = {}) {
      return request("GET", "/contracts/{contract}", options);
    },

    /** Voir un contrat */
    getContractsByContract2(options = {}) {
      return request("GET", "/contracts/{contract}", options);
    },

    /** Voir un contrat */
    getContractsByContract3(options = {}) {
      return request("GET", "/contracts/{contract}", options);
    },

    /** Voir un contrat */
    getContractsByContract4(options = {}) {
      return request("GET", "/contracts/{contract}", options);
    },

    /** Voir un contrat */
    getContractsByContract5(options = {}) {
      return request("GET", "/contracts/{contract}", options);
    },

    /** Voir un contrat */
    getContractsByContract6(options = {}) {
      return request("GET", "/contracts/{contract}", options);
    },

    /** Voir un contrat */
    getContractsByContract7(options = {}) {
      return request("GET", "/contracts/{contract}", options);
    },

    /** Voir un contrat */
    getContractsByContract8(options = {}) {
      return request("GET", "/contracts/{contract}", options);
    },

    /** Modifier un contrat */
    putContractsByContract(options = {}) {
      return request("PUT", "/contracts/{contract}", options);
    },

    /** Modifier un contrat */
    putContractsByContract2(options = {}) {
      return request("PUT", "/contracts/{contract}", options);
    },

    /** Modifier un contrat */
    putContractsByContract3(options = {}) {
      return request("PUT", "/contracts/{contract}", options);
    },

    /** Modifier un contrat */
    putContractsByContract4(options = {}) {
      return request("PUT", "/contracts/{contract}", options);
    },

    /** Modifier un contrat */
    putContractsByContract5(options = {}) {
      return request("PUT", "/contracts/{contract}", options);
    },

    /** Modifier un contrat */
    putContractsByContract6(options = {}) {
      return request("PUT", "/contracts/{contract}", options);
    },

    /** Modifier un contrat */
    putContractsByContract7(options = {}) {
      return request("PUT", "/contracts/{contract}", options);
    },

    /** Modifier un contrat */
    putContractsByContract8(options = {}) {
      return request("PUT", "/contracts/{contract}", options);
    },

    /** Activer un contrat draft */
    postContractsByContractActivate(options = {}) {
      return request("POST", "/contracts/{contract}/activate", options);
    },

    /** Activer un contrat draft */
    postContractsByContractActivate2(options = {}) {
      return request("POST", "/contracts/{contract}/activate", options);
    },

    /** Activer un contrat draft */
    postContractsByContractActivate3(options = {}) {
      return request("POST", "/contracts/{contract}/activate", options);
    },

    /** Activer un contrat draft */
    postContractsByContractActivate4(options = {}) {
      return request("POST", "/contracts/{contract}/activate", options);
    },

    /** Activer un contrat draft */
    postContractsByContractActivate5(options = {}) {
      return request("POST", "/contracts/{contract}/activate", options);
    },

    /** Activer un contrat draft */
    postContractsByContractActivate6(options = {}) {
      return request("POST", "/contracts/{contract}/activate", options);
    },

    /** Activer un contrat draft */
    postContractsByContractActivate7(options = {}) {
      return request("POST", "/contracts/{contract}/activate", options);
    },

    /** Activer un contrat draft */
    postContractsByContractActivate8(options = {}) {
      return request("POST", "/contracts/{contract}/activate", options);
    },

    /** Lister les avenants */
    getContractsByContractAmendments(options = {}) {
      return request("GET", "/contracts/{contract}/amendments", options);
    },

    /** Lister les avenants */
    getContractsByContractAmendments2(options = {}) {
      return request("GET", "/contracts/{contract}/amendments", options);
    },

    /** Lister les avenants */
    getContractsByContractAmendments3(options = {}) {
      return request("GET", "/contracts/{contract}/amendments", options);
    },

    /** Lister les avenants */
    getContractsByContractAmendments4(options = {}) {
      return request("GET", "/contracts/{contract}/amendments", options);
    },

    /** Lister les avenants */
    getContractsByContractAmendments5(options = {}) {
      return request("GET", "/contracts/{contract}/amendments", options);
    },

    /** Lister les avenants */
    getContractsByContractAmendments6(options = {}) {
      return request("GET", "/contracts/{contract}/amendments", options);
    },

    /** Lister les avenants */
    getContractsByContractAmendments7(options = {}) {
      return request("GET", "/contracts/{contract}/amendments", options);
    },

    /** Lister les avenants */
    getContractsByContractAmendments8(options = {}) {
      return request("GET", "/contracts/{contract}/amendments", options);
    },

    /** Creer un avenant */
    postContractsByContractAmendments(options = {}) {
      return request("POST", "/contracts/{contract}/amendments", options);
    },

    /** Creer un avenant */
    postContractsByContractAmendments2(options = {}) {
      return request("POST", "/contracts/{contract}/amendments", options);
    },

    /** Creer un avenant */
    postContractsByContractAmendments3(options = {}) {
      return request("POST", "/contracts/{contract}/amendments", options);
    },

    /** Creer un avenant */
    postContractsByContractAmendments4(options = {}) {
      return request("POST", "/contracts/{contract}/amendments", options);
    },

    /** Creer un avenant */
    postContractsByContractAmendments5(options = {}) {
      return request("POST", "/contracts/{contract}/amendments", options);
    },

    /** Creer un avenant */
    postContractsByContractAmendments6(options = {}) {
      return request("POST", "/contracts/{contract}/amendments", options);
    },

    /** Creer un avenant */
    postContractsByContractAmendments7(options = {}) {
      return request("POST", "/contracts/{contract}/amendments", options);
    },

    /** Creer un avenant */
    postContractsByContractAmendments8(options = {}) {
      return request("POST", "/contracts/{contract}/amendments", options);
    },

    /** Donnees PDF du contrat */
    getContractsByContractGeneratePdf(options = {}) {
      return request("GET", "/contracts/{contract}/generate-pdf", options);
    },

    /** Donnees PDF du contrat */
    getContractsByContractGeneratePdf2(options = {}) {
      return request("GET", "/contracts/{contract}/generate-pdf", options);
    },

    /** Donnees PDF du contrat */
    getContractsByContractGeneratePdf3(options = {}) {
      return request("GET", "/contracts/{contract}/generate-pdf", options);
    },

    /** Donnees PDF du contrat */
    getContractsByContractGeneratePdf4(options = {}) {
      return request("GET", "/contracts/{contract}/generate-pdf", options);
    },

    /** Donnees PDF du contrat */
    getContractsByContractGeneratePdf5(options = {}) {
      return request("GET", "/contracts/{contract}/generate-pdf", options);
    },

    /** Donnees PDF du contrat */
    getContractsByContractGeneratePdf6(options = {}) {
      return request("GET", "/contracts/{contract}/generate-pdf", options);
    },

    /** Donnees PDF du contrat */
    getContractsByContractGeneratePdf7(options = {}) {
      return request("GET", "/contracts/{contract}/generate-pdf", options);
    },

    /** Donnees PDF du contrat */
    getContractsByContractGeneratePdf8(options = {}) {
      return request("GET", "/contracts/{contract}/generate-pdf", options);
    },

    /** Alias PDF du contrat (issue #2226) */
    getContractsByContractPdf(options = {}) {
      return request("GET", "/contracts/{contract}/pdf", options);
    },

    /** Alias PDF du contrat (issue #2226) */
    getContractsByContractPdf2(options = {}) {
      return request("GET", "/contracts/{contract}/pdf", options);
    },

    /** Alias PDF du contrat (issue #2226) */
    getContractsByContractPdf3(options = {}) {
      return request("GET", "/contracts/{contract}/pdf", options);
    },

    /** Alias PDF du contrat (issue #2226) */
    getContractsByContractPdf4(options = {}) {
      return request("GET", "/contracts/{contract}/pdf", options);
    },

    /** Alias PDF du contrat (issue #2226) */
    getContractsByContractPdf5(options = {}) {
      return request("GET", "/contracts/{contract}/pdf", options);
    },

    /** Alias PDF du contrat (issue #2226) */
    getContractsByContractPdf6(options = {}) {
      return request("GET", "/contracts/{contract}/pdf", options);
    },

    /** Alias PDF du contrat (issue #2226) */
    getContractsByContractPdf7(options = {}) {
      return request("GET", "/contracts/{contract}/pdf", options);
    },

    /** Alias PDF du contrat (issue #2226) */
    getContractsByContractPdf8(options = {}) {
      return request("GET", "/contracts/{contract}/pdf", options);
    },

    /** Renouveler un contrat */
    postContractsByContractRenew(options = {}) {
      return request("POST", "/contracts/{contract}/renew", options);
    },

    /** Renouveler un contrat */
    postContractsByContractRenew2(options = {}) {
      return request("POST", "/contracts/{contract}/renew", options);
    },

    /** Renouveler un contrat */
    postContractsByContractRenew3(options = {}) {
      return request("POST", "/contracts/{contract}/renew", options);
    },

    /** Renouveler un contrat */
    postContractsByContractRenew4(options = {}) {
      return request("POST", "/contracts/{contract}/renew", options);
    },

    /** Renouveler un contrat */
    postContractsByContractRenew5(options = {}) {
      return request("POST", "/contracts/{contract}/renew", options);
    },

    /** Renouveler un contrat */
    postContractsByContractRenew6(options = {}) {
      return request("POST", "/contracts/{contract}/renew", options);
    },

    /** Renouveler un contrat */
    postContractsByContractRenew7(options = {}) {
      return request("POST", "/contracts/{contract}/renew", options);
    },

    /** Renouveler un contrat */
    postContractsByContractRenew8(options = {}) {
      return request("POST", "/contracts/{contract}/renew", options);
    },

    /** Signer explicitement un contrat (issue #5260) — idempotent */
    postContractsByContractSign(options = {}) {
      return request("POST", "/contracts/{contract}/sign", options);
    },

    /** Signer explicitement un contrat (issue #5260) — idempotent */
    postContractsByContractSign2(options = {}) {
      return request("POST", "/contracts/{contract}/sign", options);
    },

    /** Signer explicitement un contrat (issue #5260) — idempotent */
    postContractsByContractSign3(options = {}) {
      return request("POST", "/contracts/{contract}/sign", options);
    },

    /** Signer explicitement un contrat (issue #5260) — idempotent */
    postContractsByContractSign4(options = {}) {
      return request("POST", "/contracts/{contract}/sign", options);
    },

    /** Signer explicitement un contrat (issue #5260) — idempotent */
    postContractsByContractSign5(options = {}) {
      return request("POST", "/contracts/{contract}/sign", options);
    },

    /** Signer explicitement un contrat (issue #5260) — idempotent */
    postContractsByContractSign6(options = {}) {
      return request("POST", "/contracts/{contract}/sign", options);
    },

    /** Signer explicitement un contrat (issue #5260) — idempotent */
    postContractsByContractSign7(options = {}) {
      return request("POST", "/contracts/{contract}/sign", options);
    },

    /** Signer explicitement un contrat (issue #5260) — idempotent */
    postContractsByContractSign8(options = {}) {
      return request("POST", "/contracts/{contract}/sign", options);
    },

    /** Suspendre un contrat actif */
    postContractsByContractSuspend(options = {}) {
      return request("POST", "/contracts/{contract}/suspend", options);
    },

    /** Suspendre un contrat actif */
    postContractsByContractSuspend2(options = {}) {
      return request("POST", "/contracts/{contract}/suspend", options);
    },

    /** Suspendre un contrat actif */
    postContractsByContractSuspend3(options = {}) {
      return request("POST", "/contracts/{contract}/suspend", options);
    },

    /** Suspendre un contrat actif */
    postContractsByContractSuspend4(options = {}) {
      return request("POST", "/contracts/{contract}/suspend", options);
    },

    /** Suspendre un contrat actif */
    postContractsByContractSuspend5(options = {}) {
      return request("POST", "/contracts/{contract}/suspend", options);
    },

    /** Suspendre un contrat actif */
    postContractsByContractSuspend6(options = {}) {
      return request("POST", "/contracts/{contract}/suspend", options);
    },

    /** Suspendre un contrat actif */
    postContractsByContractSuspend7(options = {}) {
      return request("POST", "/contracts/{contract}/suspend", options);
    },

    /** Suspendre un contrat actif */
    postContractsByContractSuspend8(options = {}) {
      return request("POST", "/contracts/{contract}/suspend", options);
    },

    /** Resilier un contrat */
    postContractsByContractTerminate(options = {}) {
      return request("POST", "/contracts/{contract}/terminate", options);
    },

    /** Resilier un contrat */
    postContractsByContractTerminate2(options = {}) {
      return request("POST", "/contracts/{contract}/terminate", options);
    },

    /** Resilier un contrat */
    postContractsByContractTerminate3(options = {}) {
      return request("POST", "/contracts/{contract}/terminate", options);
    },

    /** Resilier un contrat */
    postContractsByContractTerminate4(options = {}) {
      return request("POST", "/contracts/{contract}/terminate", options);
    },

    /** Resilier un contrat */
    postContractsByContractTerminate5(options = {}) {
      return request("POST", "/contracts/{contract}/terminate", options);
    },

    /** Resilier un contrat */
    postContractsByContractTerminate6(options = {}) {
      return request("POST", "/contracts/{contract}/terminate", options);
    },

    /** Resilier un contrat */
    postContractsByContractTerminate7(options = {}) {
      return request("POST", "/contracts/{contract}/terminate", options);
    },

    /** Resilier un contrat */
    postContractsByContractTerminate8(options = {}) {
      return request("POST", "/contracts/{contract}/terminate", options);
    },

    /** Contrats expirant bientot */
    getContractsExpiring(options = {}) {
      return request("GET", "/contracts/expiring", options);
    },

    /** Contrats expirant bientot */
    getContractsExpiring2(options = {}) {
      return request("GET", "/contracts/expiring", options);
    },

    /** Contrats expirant bientot */
    getContractsExpiring3(options = {}) {
      return request("GET", "/contracts/expiring", options);
    },

    /** Contrats expirant bientot */
    getContractsExpiring4(options = {}) {
      return request("GET", "/contracts/expiring", options);
    },

    /** Contrats expirant bientot */
    getContractsExpiring5(options = {}) {
      return request("GET", "/contracts/expiring", options);
    },

    /** Contrats expirant bientot */
    getContractsExpiring6(options = {}) {
      return request("GET", "/contracts/expiring", options);
    },

    /** Contrats expirant bientot */
    getContractsExpiring7(options = {}) {
      return request("GET", "/contracts/expiring", options);
    },

    /** Contrats expirant bientot */
    getContractsExpiring8(options = {}) {
      return request("GET", "/contracts/expiring", options);
    },

    /** Modeles legaux de contrat par pays (issue #5260) — DZ/MA/TN/SN */
    getContractsTemplates(options = {}) {
      return request("GET", "/contracts/templates", options);
    },

    /** Modeles legaux de contrat par pays (issue #5260) — DZ/MA/TN/SN */
    getContractsTemplates2(options = {}) {
      return request("GET", "/contracts/templates", options);
    },

    /** Modeles legaux de contrat par pays (issue #5260) — DZ/MA/TN/SN */
    getContractsTemplates3(options = {}) {
      return request("GET", "/contracts/templates", options);
    },

    /** Modeles legaux de contrat par pays (issue #5260) — DZ/MA/TN/SN */
    getContractsTemplates4(options = {}) {
      return request("GET", "/contracts/templates", options);
    },

    /** Modeles legaux de contrat par pays (issue #5260) — DZ/MA/TN/SN */
    getContractsTemplates5(options = {}) {
      return request("GET", "/contracts/templates", options);
    },

    /** Modeles legaux de contrat par pays (issue #5260) — DZ/MA/TN/SN */
    getContractsTemplates6(options = {}) {
      return request("GET", "/contracts/templates", options);
    },

    /** Modeles legaux de contrat par pays (issue #5260) — DZ/MA/TN/SN */
    getContractsTemplates7(options = {}) {
      return request("GET", "/contracts/templates", options);
    },

    /** Modeles legaux de contrat par pays (issue #5260) — DZ/MA/TN/SN */
    getContractsTemplates8(options = {}) {
      return request("GET", "/contracts/templates", options);
    },

    /** Lister les conversations de l'employe courant */
    getConversations(options = {}) {
      return request("GET", "/conversations", options);
    },

    /** Lister les conversations de l'employe courant */
    getConversations2(options = {}) {
      return request("GET", "/conversations", options);
    },

    /** Lister les conversations de l'employe courant */
    getConversations3(options = {}) {
      return request("GET", "/conversations", options);
    },

    /** Lister les conversations de l'employe courant */
    getConversations4(options = {}) {
      return request("GET", "/conversations", options);
    },

    /** Lister les conversations de l'employe courant */
    getConversations5(options = {}) {
      return request("GET", "/conversations", options);
    },

    /** Lister les conversations de l'employe courant */
    getConversations6(options = {}) {
      return request("GET", "/conversations", options);
    },

    /** Lister les conversations de l'employe courant */
    getConversations7(options = {}) {
      return request("GET", "/conversations", options);
    },

    /** Lister les conversations de l'employe courant */
    getConversations8(options = {}) {
      return request("GET", "/conversations", options);
    },

    /** Ouvrir une conversation (fil de discussion) */
    postConversations(options = {}) {
      return request("POST", "/conversations", options);
    },

    /** Ouvrir une conversation (fil de discussion) */
    postConversations2(options = {}) {
      return request("POST", "/conversations", options);
    },

    /** Ouvrir une conversation (fil de discussion) */
    postConversations3(options = {}) {
      return request("POST", "/conversations", options);
    },

    /** Ouvrir une conversation (fil de discussion) */
    postConversations4(options = {}) {
      return request("POST", "/conversations", options);
    },

    /** Ouvrir une conversation (fil de discussion) */
    postConversations5(options = {}) {
      return request("POST", "/conversations", options);
    },

    /** Ouvrir une conversation (fil de discussion) */
    postConversations6(options = {}) {
      return request("POST", "/conversations", options);
    },

    /** Ouvrir une conversation (fil de discussion) */
    postConversations7(options = {}) {
      return request("POST", "/conversations", options);
    },

    /** Ouvrir une conversation (fil de discussion) */
    postConversations8(options = {}) {
      return request("POST", "/conversations", options);
    },

    /** Detail d'une conversation avec ses messages */
    getConversationsByThread(options = {}) {
      return request("GET", "/conversations/{thread}", options);
    },

    /** Detail d'une conversation avec ses messages */
    getConversationsByThread2(options = {}) {
      return request("GET", "/conversations/{thread}", options);
    },

    /** Detail d'une conversation avec ses messages */
    getConversationsByThread3(options = {}) {
      return request("GET", "/conversations/{thread}", options);
    },

    /** Detail d'une conversation avec ses messages */
    getConversationsByThread4(options = {}) {
      return request("GET", "/conversations/{thread}", options);
    },

    /** Detail d'une conversation avec ses messages */
    getConversationsByThread5(options = {}) {
      return request("GET", "/conversations/{thread}", options);
    },

    /** Detail d'une conversation avec ses messages */
    getConversationsByThread6(options = {}) {
      return request("GET", "/conversations/{thread}", options);
    },

    /** Detail d'une conversation avec ses messages */
    getConversationsByThread7(options = {}) {
      return request("GET", "/conversations/{thread}", options);
    },

    /** Detail d'une conversation avec ses messages */
    getConversationsByThread8(options = {}) {
      return request("GET", "/conversations/{thread}", options);
    },

    /** Repondre dans une conversation */
    postConversationsByThreadMessages(options = {}) {
      return request("POST", "/conversations/{thread}/messages", options);
    },

    /** Repondre dans une conversation */
    postConversationsByThreadMessages2(options = {}) {
      return request("POST", "/conversations/{thread}/messages", options);
    },

    /** Repondre dans une conversation */
    postConversationsByThreadMessages3(options = {}) {
      return request("POST", "/conversations/{thread}/messages", options);
    },

    /** Repondre dans une conversation */
    postConversationsByThreadMessages4(options = {}) {
      return request("POST", "/conversations/{thread}/messages", options);
    },

    /** Repondre dans une conversation */
    postConversationsByThreadMessages5(options = {}) {
      return request("POST", "/conversations/{thread}/messages", options);
    },

    /** Repondre dans une conversation */
    postConversationsByThreadMessages6(options = {}) {
      return request("POST", "/conversations/{thread}/messages", options);
    },

    /** Repondre dans une conversation */
    postConversationsByThreadMessages7(options = {}) {
      return request("POST", "/conversations/{thread}/messages", options);
    },

    /** Repondre dans une conversation */
    postConversationsByThreadMessages8(options = {}) {
      return request("POST", "/conversations/{thread}/messages", options);
    },

    /** Telecharger la piece jointe d'un message de conversation */
    getConversationsByThreadMessagesByMessageAttachment(options = {}) {
      return request("GET", "/conversations/{thread}/messages/{message}/attachment", options);
    },

    /** Telecharger la piece jointe d'un message de conversation */
    getConversationsByThreadMessagesByMessageAttachment2(options = {}) {
      return request("GET", "/conversations/{thread}/messages/{message}/attachment", options);
    },

    /** Telecharger la piece jointe d'un message de conversation */
    getConversationsByThreadMessagesByMessageAttachment3(options = {}) {
      return request("GET", "/conversations/{thread}/messages/{message}/attachment", options);
    },

    /** Telecharger la piece jointe d'un message de conversation */
    getConversationsByThreadMessagesByMessageAttachment4(options = {}) {
      return request("GET", "/conversations/{thread}/messages/{message}/attachment", options);
    },

    /** Telecharger la piece jointe d'un message de conversation */
    getConversationsByThreadMessagesByMessageAttachment5(options = {}) {
      return request("GET", "/conversations/{thread}/messages/{message}/attachment", options);
    },

    /** Telecharger la piece jointe d'un message de conversation */
    getConversationsByThreadMessagesByMessageAttachment6(options = {}) {
      return request("GET", "/conversations/{thread}/messages/{message}/attachment", options);
    },

    /** Telecharger la piece jointe d'un message de conversation */
    getConversationsByThreadMessagesByMessageAttachment7(options = {}) {
      return request("GET", "/conversations/{thread}/messages/{message}/attachment", options);
    },

    /** Telecharger la piece jointe d'un message de conversation */
    getConversationsByThreadMessagesByMessageAttachment8(options = {}) {
      return request("GET", "/conversations/{thread}/messages/{message}/attachment", options);
    },

    /** Simuler les cotisations sociales employe/employeur et l'impot sur le revenu pour un salaire brut donne, sans persister (manager) */
    simulateCotisations(options = {}) {
      return request("POST", "/cotisation-simulation", options);
    },

    /** Simuler les cotisations sociales employe/employeur et l'impot sur le revenu pour un salaire brut donne, sans persister (manager) */
    simulateCotisations2(options = {}) {
      return request("POST", "/cotisation-simulation", options);
    },

    /** Simuler les cotisations sociales employe/employeur et l'impot sur le revenu pour un salaire brut donne, sans persister (manager) */
    simulateCotisations3(options = {}) {
      return request("POST", "/cotisation-simulation", options);
    },

    /** Simuler les cotisations sociales employe/employeur et l'impot sur le revenu pour un salaire brut donne, sans persister (manager) */
    simulateCotisations4(options = {}) {
      return request("POST", "/cotisation-simulation", options);
    },

    /** Simuler les cotisations sociales employe/employeur et l'impot sur le revenu pour un salaire brut donne, sans persister (manager) */
    simulateCotisations5(options = {}) {
      return request("POST", "/cotisation-simulation", options);
    },

    /** Simuler les cotisations sociales employe/employeur et l'impot sur le revenu pour un salaire brut donne, sans persister (manager) */
    simulateCotisations6(options = {}) {
      return request("POST", "/cotisation-simulation", options);
    },

    /** Simuler les cotisations sociales employe/employeur et l'impot sur le revenu pour un salaire brut donne, sans persister (manager) */
    simulateCotisations7(options = {}) {
      return request("POST", "/cotisation-simulation", options);
    },

    /** Simuler les cotisations sociales employe/employeur et l'impot sur le revenu pour un salaire brut donne, sans persister (manager) */
    simulateCotisations8(options = {}) {
      return request("POST", "/cotisation-simulation", options);
    },

    /** Simuler les cotisations sociales employe/employeur et l'impot sur le revenu pour un salaire brut donne, sans persister (manager) */
    simulateCotisations9(options = {}) {
      return request("POST", "/cotisation-simulation", options);
    },

    /** Simuler les cotisations sociales employe/employeur et l'impot sur le revenu pour un salaire brut donne, sans persister (manager) */
    simulateCotisations10(options = {}) {
      return request("POST", "/cotisation-simulation", options);
    },

    /** Synthese dashboard admin (principal) */
    getDashboardAdmin(options = {}) {
      return request("GET", "/dashboard/admin", options);
    },

    /** Synthese dashboard admin (principal) */
    getDashboardAdmin2(options = {}) {
      return request("GET", "/dashboard/admin", options);
    },

    /** Synthese dashboard admin (principal) */
    getDashboardAdmin3(options = {}) {
      return request("GET", "/dashboard/admin", options);
    },

    /** Synthese dashboard admin (principal) */
    getDashboardAdmin4(options = {}) {
      return request("GET", "/dashboard/admin", options);
    },

    /** Synthese dashboard admin (principal) */
    getDashboardAdmin5(options = {}) {
      return request("GET", "/dashboard/admin", options);
    },

    /** Synthese dashboard admin (principal) */
    getDashboardAdmin6(options = {}) {
      return request("GET", "/dashboard/admin", options);
    },

    /** Synthese dashboard admin (principal) */
    getDashboardAdmin7(options = {}) {
      return request("GET", "/dashboard/admin", options);
    },

    /** Synthese dashboard admin (principal) */
    getDashboardAdmin8(options = {}) {
      return request("GET", "/dashboard/admin", options);
    },

    /** Synthese dashboard admin (principal) */
    getDashboardAdmin9(options = {}) {
      return request("GET", "/dashboard/admin", options);
    },

    /** Synthese dashboard admin (principal) */
    getDashboardAdmin10(options = {}) {
      return request("GET", "/dashboard/admin", options);
    },

    /** Synthese dashboard admin (principal) */
    getDashboardAdmin11(options = {}) {
      return request("GET", "/dashboard/admin", options);
    },

    /** Synthese dashboard comptable */
    getDashboardComptable(options = {}) {
      return request("GET", "/dashboard/comptable", options);
    },

    /** Synthese dashboard comptable */
    getDashboardComptable2(options = {}) {
      return request("GET", "/dashboard/comptable", options);
    },

    /** Synthese dashboard comptable */
    getDashboardComptable3(options = {}) {
      return request("GET", "/dashboard/comptable", options);
    },

    /** Synthese dashboard comptable */
    getDashboardComptable4(options = {}) {
      return request("GET", "/dashboard/comptable", options);
    },

    /** Synthese dashboard comptable */
    getDashboardComptable5(options = {}) {
      return request("GET", "/dashboard/comptable", options);
    },

    /** Synthese dashboard comptable */
    getDashboardComptable6(options = {}) {
      return request("GET", "/dashboard/comptable", options);
    },

    /** Synthese dashboard comptable */
    getDashboardComptable7(options = {}) {
      return request("GET", "/dashboard/comptable", options);
    },

    /** Synthese dashboard comptable */
    getDashboardComptable8(options = {}) {
      return request("GET", "/dashboard/comptable", options);
    },

    /** Synthese dashboard comptable */
    getDashboardComptable9(options = {}) {
      return request("GET", "/dashboard/comptable", options);
    },

    /** Synthese dashboard comptable */
    getDashboardComptable10(options = {}) {
      return request("GET", "/dashboard/comptable", options);
    },

    /** Synthese dashboard comptable */
    getDashboardComptable11(options = {}) {
      return request("GET", "/dashboard/comptable", options);
    },

    /** KPIs detailles */
    getDashboardKpi(options = {}) {
      return request("GET", "/dashboard/kpi", options);
    },

    /** KPIs detailles */
    getDashboardKpi2(options = {}) {
      return request("GET", "/dashboard/kpi", options);
    },

    /** KPIs detailles */
    getDashboardKpi3(options = {}) {
      return request("GET", "/dashboard/kpi", options);
    },

    /** KPIs detailles */
    getDashboardKpi4(options = {}) {
      return request("GET", "/dashboard/kpi", options);
    },

    /** KPIs detailles */
    getDashboardKpi5(options = {}) {
      return request("GET", "/dashboard/kpi", options);
    },

    /** KPIs detailles */
    getDashboardKpi6(options = {}) {
      return request("GET", "/dashboard/kpi", options);
    },

    /** KPIs detailles */
    getDashboardKpi7(options = {}) {
      return request("GET", "/dashboard/kpi", options);
    },

    /** KPIs detailles */
    getDashboardKpi8(options = {}) {
      return request("GET", "/dashboard/kpi", options);
    },

    /** KPIs detailles */
    getDashboardKpi9(options = {}) {
      return request("GET", "/dashboard/kpi", options);
    },

    /** KPIs detailles */
    getDashboardKpi10(options = {}) {
      return request("GET", "/dashboard/kpi", options);
    },

    /** KPIs detailles */
    getDashboardKpi11(options = {}) {
      return request("GET", "/dashboard/kpi", options);
    },

    /** Lire les signaux manager du jour */
    getDashboardManagerDigest(options = {}) {
      return request("GET", "/dashboard/manager-digest", options);
    },

    /** Lire les signaux manager du jour */
    getDashboardManagerDigest2(options = {}) {
      return request("GET", "/dashboard/manager-digest", options);
    },

    /** Lire les signaux manager du jour */
    getDashboardManagerDigest3(options = {}) {
      return request("GET", "/dashboard/manager-digest", options);
    },

    /** Lire les signaux manager du jour */
    getDashboardManagerDigest4(options = {}) {
      return request("GET", "/dashboard/manager-digest", options);
    },

    /** Lire les signaux manager du jour */
    getDashboardManagerDigest5(options = {}) {
      return request("GET", "/dashboard/manager-digest", options);
    },

    /** Synthese dashboard marketing */
    getDashboardMarketing(options = {}) {
      return request("GET", "/dashboard/marketing", options);
    },

    /** Synthese dashboard marketing */
    getDashboardMarketing2(options = {}) {
      return request("GET", "/dashboard/marketing", options);
    },

    /** Synthese dashboard marketing */
    getDashboardMarketing3(options = {}) {
      return request("GET", "/dashboard/marketing", options);
    },

    /** Synthese dashboard marketing */
    getDashboardMarketing4(options = {}) {
      return request("GET", "/dashboard/marketing", options);
    },

    /** Synthese dashboard marketing */
    getDashboardMarketing5(options = {}) {
      return request("GET", "/dashboard/marketing", options);
    },

    /** Synthese dashboard marketing */
    getDashboardMarketing6(options = {}) {
      return request("GET", "/dashboard/marketing", options);
    },

    /** Synthese dashboard marketing */
    getDashboardMarketing7(options = {}) {
      return request("GET", "/dashboard/marketing", options);
    },

    /** Synthese dashboard marketing */
    getDashboardMarketing8(options = {}) {
      return request("GET", "/dashboard/marketing", options);
    },

    /** Synthese dashboard marketing */
    getDashboardMarketing9(options = {}) {
      return request("GET", "/dashboard/marketing", options);
    },

    /** Synthese dashboard marketing */
    getDashboardMarketing10(options = {}) {
      return request("GET", "/dashboard/marketing", options);
    },

    /** Synthese dashboard marketing */
    getDashboardMarketing11(options = {}) {
      return request("GET", "/dashboard/marketing", options);
    },

    /** Activite recente */
    getDashboardRecentActivity(options = {}) {
      return request("GET", "/dashboard/recent-activity", options);
    },

    /** Activite recente */
    getDashboardRecentActivity2(options = {}) {
      return request("GET", "/dashboard/recent-activity", options);
    },

    /** Activite recente */
    getDashboardRecentActivity3(options = {}) {
      return request("GET", "/dashboard/recent-activity", options);
    },

    /** Activite recente */
    getDashboardRecentActivity4(options = {}) {
      return request("GET", "/dashboard/recent-activity", options);
    },

    /** Activite recente */
    getDashboardRecentActivity5(options = {}) {
      return request("GET", "/dashboard/recent-activity", options);
    },

    /** Activite recente */
    getDashboardRecentActivity6(options = {}) {
      return request("GET", "/dashboard/recent-activity", options);
    },

    /** Activite recente */
    getDashboardRecentActivity7(options = {}) {
      return request("GET", "/dashboard/recent-activity", options);
    },

    /** Activite recente */
    getDashboardRecentActivity8(options = {}) {
      return request("GET", "/dashboard/recent-activity", options);
    },

    /** Activite recente */
    getDashboardRecentActivity9(options = {}) {
      return request("GET", "/dashboard/recent-activity", options);
    },

    /** Activite recente */
    getDashboardRecentActivity10(options = {}) {
      return request("GET", "/dashboard/recent-activity", options);
    },

    /** Activite recente */
    getDashboardRecentActivity11(options = {}) {
      return request("GET", "/dashboard/recent-activity", options);
    },

    /** Synthese dashboard RH */
    getDashboardRh(options = {}) {
      return request("GET", "/dashboard/rh", options);
    },

    /** Synthese dashboard RH */
    getDashboardRh2(options = {}) {
      return request("GET", "/dashboard/rh", options);
    },

    /** Synthese dashboard RH */
    getDashboardRh3(options = {}) {
      return request("GET", "/dashboard/rh", options);
    },

    /** Synthese dashboard RH */
    getDashboardRh4(options = {}) {
      return request("GET", "/dashboard/rh", options);
    },

    /** Synthese dashboard RH */
    getDashboardRh5(options = {}) {
      return request("GET", "/dashboard/rh", options);
    },

    /** Synthese dashboard RH */
    getDashboardRh6(options = {}) {
      return request("GET", "/dashboard/rh", options);
    },

    /** Synthese dashboard RH */
    getDashboardRh7(options = {}) {
      return request("GET", "/dashboard/rh", options);
    },

    /** Synthese dashboard RH */
    getDashboardRh8(options = {}) {
      return request("GET", "/dashboard/rh", options);
    },

    /** Synthese dashboard RH */
    getDashboardRh9(options = {}) {
      return request("GET", "/dashboard/rh", options);
    },

    /** Synthese dashboard RH */
    getDashboardRh10(options = {}) {
      return request("GET", "/dashboard/rh", options);
    },

    /** Synthese dashboard RH */
    getDashboardRh11(options = {}) {
      return request("GET", "/dashboard/rh", options);
    },

    /** Resume dashboard */
    getDashboardSummary(options = {}) {
      return request("GET", "/dashboard/summary", options);
    },

    /** Resume dashboard */
    getDashboardSummary2(options = {}) {
      return request("GET", "/dashboard/summary", options);
    },

    /** Resume dashboard */
    getDashboardSummary3(options = {}) {
      return request("GET", "/dashboard/summary", options);
    },

    /** Resume dashboard */
    getDashboardSummary4(options = {}) {
      return request("GET", "/dashboard/summary", options);
    },

    /** Resume dashboard */
    getDashboardSummary5(options = {}) {
      return request("GET", "/dashboard/summary", options);
    },

    /** Resume dashboard */
    getDashboardSummary6(options = {}) {
      return request("GET", "/dashboard/summary", options);
    },

    /** Resume dashboard */
    getDashboardSummary7(options = {}) {
      return request("GET", "/dashboard/summary", options);
    },

    /** Resume dashboard */
    getDashboardSummary8(options = {}) {
      return request("GET", "/dashboard/summary", options);
    },

    /** Resume dashboard */
    getDashboardSummary9(options = {}) {
      return request("GET", "/dashboard/summary", options);
    },

    /** Resume dashboard */
    getDashboardSummary10(options = {}) {
      return request("GET", "/dashboard/summary", options);
    },

    /** Resume dashboard */
    getDashboardSummary11(options = {}) {
      return request("GET", "/dashboard/summary", options);
    },

    /** Comptes demo publics pour la documentation QA */
    getDemoUsers(options = {}) {
      return request("GET", "/demo-users", options);
    },

    /** Comptes demo publics pour la documentation QA */
    getDemoUsers2(options = {}) {
      return request("GET", "/demo-users", options);
    },

    /** Comptes demo publics pour la documentation QA */
    getDemoUsers3(options = {}) {
      return request("GET", "/demo-users", options);
    },

    /** Comptes demo publics pour la documentation QA */
    getDemoUsers4(options = {}) {
      return request("GET", "/demo-users", options);
    },

    /** Comptes demo publics pour la documentation QA */
    getDemoUsers5(options = {}) {
      return request("GET", "/demo-users", options);
    },

    /** Lister les départements du tenant */
    getDepartments(options = {}) {
      return request("GET", "/departments", options);
    },

    /** Lister les départements du tenant */
    getDepartments2(options = {}) {
      return request("GET", "/departments", options);
    },

    /** Lister les départements du tenant */
    getDepartments3(options = {}) {
      return request("GET", "/departments", options);
    },

    /** Lister les départements du tenant */
    getDepartments4(options = {}) {
      return request("GET", "/departments", options);
    },

    /** Lister les départements du tenant */
    getDepartments5(options = {}) {
      return request("GET", "/departments", options);
    },

    /** Lister les départements du tenant */
    getDepartments6(options = {}) {
      return request("GET", "/departments", options);
    },

    /** Lister les départements du tenant */
    getDepartments7(options = {}) {
      return request("GET", "/departments", options);
    },

    /** Lister les départements du tenant */
    getDepartments8(options = {}) {
      return request("GET", "/departments", options);
    },

    /** Créer un département */
    postDepartments(options = {}) {
      return request("POST", "/departments", options);
    },

    /** Créer un département */
    postDepartments2(options = {}) {
      return request("POST", "/departments", options);
    },

    /** Créer un département */
    postDepartments3(options = {}) {
      return request("POST", "/departments", options);
    },

    /** Créer un département */
    postDepartments4(options = {}) {
      return request("POST", "/departments", options);
    },

    /** Créer un département */
    postDepartments5(options = {}) {
      return request("POST", "/departments", options);
    },

    /** Créer un département */
    postDepartments6(options = {}) {
      return request("POST", "/departments", options);
    },

    /** Créer un département */
    postDepartments7(options = {}) {
      return request("POST", "/departments", options);
    },

    /** Créer un département */
    postDepartments8(options = {}) {
      return request("POST", "/departments", options);
    },

    /** Supprimer un département */
    deleteDepartmentsByDepartment(options = {}) {
      return request("DELETE", "/departments/{department}", options);
    },

    /** Supprimer un département */
    deleteDepartmentsByDepartment2(options = {}) {
      return request("DELETE", "/departments/{department}", options);
    },

    /** Supprimer un département */
    deleteDepartmentsByDepartment3(options = {}) {
      return request("DELETE", "/departments/{department}", options);
    },

    /** Supprimer un département */
    deleteDepartmentsByDepartment4(options = {}) {
      return request("DELETE", "/departments/{department}", options);
    },

    /** Supprimer un département */
    deleteDepartmentsByDepartment5(options = {}) {
      return request("DELETE", "/departments/{department}", options);
    },

    /** Supprimer un département */
    deleteDepartmentsByDepartment6(options = {}) {
      return request("DELETE", "/departments/{department}", options);
    },

    /** Supprimer un département */
    deleteDepartmentsByDepartment7(options = {}) {
      return request("DELETE", "/departments/{department}", options);
    },

    /** Supprimer un département */
    deleteDepartmentsByDepartment8(options = {}) {
      return request("DELETE", "/departments/{department}", options);
    },

    /** Détail d'un département */
    getDepartmentsByDepartment(options = {}) {
      return request("GET", "/departments/{department}", options);
    },

    /** Détail d'un département */
    getDepartmentsByDepartment2(options = {}) {
      return request("GET", "/departments/{department}", options);
    },

    /** Détail d'un département */
    getDepartmentsByDepartment3(options = {}) {
      return request("GET", "/departments/{department}", options);
    },

    /** Détail d'un département */
    getDepartmentsByDepartment4(options = {}) {
      return request("GET", "/departments/{department}", options);
    },

    /** Détail d'un département */
    getDepartmentsByDepartment5(options = {}) {
      return request("GET", "/departments/{department}", options);
    },

    /** Détail d'un département */
    getDepartmentsByDepartment6(options = {}) {
      return request("GET", "/departments/{department}", options);
    },

    /** Détail d'un département */
    getDepartmentsByDepartment7(options = {}) {
      return request("GET", "/departments/{department}", options);
    },

    /** Détail d'un département */
    getDepartmentsByDepartment8(options = {}) {
      return request("GET", "/departments/{department}", options);
    },

    /** Mise à jour partielle d'un département */
    patchDepartmentsByDepartment(options = {}) {
      return request("PATCH", "/departments/{department}", options);
    },

    /** Mise à jour partielle d'un département */
    patchDepartmentsByDepartment2(options = {}) {
      return request("PATCH", "/departments/{department}", options);
    },

    /** Mise à jour partielle d'un département */
    patchDepartmentsByDepartment3(options = {}) {
      return request("PATCH", "/departments/{department}", options);
    },

    /** Mise à jour partielle d'un département */
    patchDepartmentsByDepartment4(options = {}) {
      return request("PATCH", "/departments/{department}", options);
    },

    /** Mise à jour partielle d'un département */
    patchDepartmentsByDepartment5(options = {}) {
      return request("PATCH", "/departments/{department}", options);
    },

    /** Mise à jour partielle d'un département */
    patchDepartmentsByDepartment6(options = {}) {
      return request("PATCH", "/departments/{department}", options);
    },

    /** Mise à jour partielle d'un département */
    patchDepartmentsByDepartment7(options = {}) {
      return request("PATCH", "/departments/{department}", options);
    },

    /** Mise à jour partielle d'un département */
    patchDepartmentsByDepartment8(options = {}) {
      return request("PATCH", "/departments/{department}", options);
    },

    /** Mettre à jour un département */
    putDepartmentsByDepartment(options = {}) {
      return request("PUT", "/departments/{department}", options);
    },

    /** Mettre à jour un département */
    putDepartmentsByDepartment2(options = {}) {
      return request("PUT", "/departments/{department}", options);
    },

    /** Mettre à jour un département */
    putDepartmentsByDepartment3(options = {}) {
      return request("PUT", "/departments/{department}", options);
    },

    /** Mettre à jour un département */
    putDepartmentsByDepartment4(options = {}) {
      return request("PUT", "/departments/{department}", options);
    },

    /** Mettre à jour un département */
    putDepartmentsByDepartment5(options = {}) {
      return request("PUT", "/departments/{department}", options);
    },

    /** Mettre à jour un département */
    putDepartmentsByDepartment6(options = {}) {
      return request("PUT", "/departments/{department}", options);
    },

    /** Mettre à jour un département */
    putDepartmentsByDepartment7(options = {}) {
      return request("PUT", "/departments/{department}", options);
    },

    /** Mettre à jour un département */
    putDepartmentsByDepartment8(options = {}) {
      return request("PUT", "/departments/{department}", options);
    },

    /** Organigramme d'un departement (arbre department/teams/managers/employees, scope tenant) */
    getDepartmentsByDepartmentHierarchy(options = {}) {
      return request("GET", "/departments/{department}/hierarchy", options);
    },

    /** Organigramme d'un departement (arbre department/teams/managers/employees, scope tenant) */
    getDepartmentsByDepartmentHierarchy2(options = {}) {
      return request("GET", "/departments/{department}/hierarchy", options);
    },

    /** Organigramme d'un departement (arbre department/teams/managers/employees, scope tenant) */
    getDepartmentsByDepartmentHierarchy3(options = {}) {
      return request("GET", "/departments/{department}/hierarchy", options);
    },

    /** Organigramme d'un departement (arbre department/teams/managers/employees, scope tenant) */
    getDepartmentsByDepartmentHierarchy4(options = {}) {
      return request("GET", "/departments/{department}/hierarchy", options);
    },

    /** Organigramme d'un departement (arbre department/teams/managers/employees, scope tenant) */
    getDepartmentsByDepartmentHierarchy5(options = {}) {
      return request("GET", "/departments/{department}/hierarchy", options);
    },

    /** Organigramme d'un departement (arbre department/teams/managers/employees, scope tenant) */
    getDepartmentsByDepartmentHierarchy6(options = {}) {
      return request("GET", "/departments/{department}/hierarchy", options);
    },

    /** Organigramme d'un departement (arbre department/teams/managers/employees, scope tenant) */
    getDepartmentsByDepartmentHierarchy7(options = {}) {
      return request("GET", "/departments/{department}/hierarchy", options);
    },

    /** Organigramme d'un departement (arbre department/teams/managers/employees, scope tenant) */
    getDepartmentsByDepartmentHierarchy8(options = {}) {
      return request("GET", "/departments/{department}/hierarchy", options);
    },

    /** Supprimer le token FCM du device courant au logout */
    deleteDeviceTokens(options = {}) {
      return request("DELETE", "/device-tokens", options);
    },

    /** Supprimer le token FCM du device courant au logout */
    deleteDeviceTokens2(options = {}) {
      return request("DELETE", "/device-tokens", options);
    },

    /** Supprimer le token FCM du device courant au logout */
    deleteDeviceTokens3(options = {}) {
      return request("DELETE", "/device-tokens", options);
    },

    /** Supprimer le token FCM du device courant au logout */
    deleteDeviceTokens4(options = {}) {
      return request("DELETE", "/device-tokens", options);
    },

    /** Supprimer le token FCM du device courant au logout */
    deleteDeviceTokens5(options = {}) {
      return request("DELETE", "/device-tokens", options);
    },

    /** Supprimer le token FCM du device courant au logout */
    deleteDeviceTokens6(options = {}) {
      return request("DELETE", "/device-tokens", options);
    },

    /** Supprimer le token FCM du device courant au logout */
    deleteDeviceTokens7(options = {}) {
      return request("DELETE", "/device-tokens", options);
    },

    /** Supprimer le token FCM du device courant au logout */
    deleteDeviceTokens8(options = {}) {
      return request("DELETE", "/device-tokens", options);
    },

    /** Lister les tokens push actifs de l'utilisateur courant */
    getDeviceTokens(options = {}) {
      return request("GET", "/device-tokens", options);
    },

    /** Lister les tokens push actifs de l'utilisateur courant */
    getDeviceTokens2(options = {}) {
      return request("GET", "/device-tokens", options);
    },

    /** Lister les tokens push actifs de l'utilisateur courant */
    getDeviceTokens3(options = {}) {
      return request("GET", "/device-tokens", options);
    },

    /** Lister les tokens push actifs de l'utilisateur courant */
    getDeviceTokens4(options = {}) {
      return request("GET", "/device-tokens", options);
    },

    /** Lister les tokens push actifs de l'utilisateur courant */
    getDeviceTokens5(options = {}) {
      return request("GET", "/device-tokens", options);
    },

    /** Lister les tokens push actifs de l'utilisateur courant */
    getDeviceTokens6(options = {}) {
      return request("GET", "/device-tokens", options);
    },

    /** Lister les tokens push actifs de l'utilisateur courant */
    getDeviceTokens7(options = {}) {
      return request("GET", "/device-tokens", options);
    },

    /** Lister les tokens push actifs de l'utilisateur courant */
    getDeviceTokens8(options = {}) {
      return request("GET", "/device-tokens", options);
    },

    /** Enregistrer ou rafraichir le token FCM du device courant */
    postDeviceTokens(options = {}) {
      return request("POST", "/device-tokens", options);
    },

    /** Enregistrer ou rafraichir le token FCM du device courant */
    postDeviceTokens2(options = {}) {
      return request("POST", "/device-tokens", options);
    },

    /** Enregistrer ou rafraichir le token FCM du device courant */
    postDeviceTokens3(options = {}) {
      return request("POST", "/device-tokens", options);
    },

    /** Enregistrer ou rafraichir le token FCM du device courant */
    postDeviceTokens4(options = {}) {
      return request("POST", "/device-tokens", options);
    },

    /** Enregistrer ou rafraichir le token FCM du device courant */
    postDeviceTokens5(options = {}) {
      return request("POST", "/device-tokens", options);
    },

    /** Enregistrer ou rafraichir le token FCM du device courant */
    postDeviceTokens6(options = {}) {
      return request("POST", "/device-tokens", options);
    },

    /** Enregistrer ou rafraichir le token FCM du device courant */
    postDeviceTokens7(options = {}) {
      return request("POST", "/device-tokens", options);
    },

    /** Enregistrer ou rafraichir le token FCM du device courant */
    postDeviceTokens8(options = {}) {
      return request("POST", "/device-tokens", options);
    },

    /** Lister les nœuds edge du tenant */
    getEdge(options = {}) {
      return request("GET", "/edge", options);
    },

    /** Lister les nœuds edge du tenant */
    getEdge2(options = {}) {
      return request("GET", "/edge", options);
    },

    /** Lister les nœuds edge du tenant */
    getEdge3(options = {}) {
      return request("GET", "/edge", options);
    },

    /** Lister les nœuds edge du tenant */
    getEdge4(options = {}) {
      return request("GET", "/edge", options);
    },

    /** Lister les nœuds edge du tenant */
    getEdge5(options = {}) {
      return request("GET", "/edge", options);
    },

    /** Enregistrer un nœud edge */
    postEdge(options = {}) {
      return request("POST", "/edge", options);
    },

    /** Enregistrer un nœud edge */
    postEdge2(options = {}) {
      return request("POST", "/edge", options);
    },

    /** Enregistrer un nœud edge */
    postEdge3(options = {}) {
      return request("POST", "/edge", options);
    },

    /** Enregistrer un nœud edge */
    postEdge4(options = {}) {
      return request("POST", "/edge", options);
    },

    /** Enregistrer un nœud edge */
    postEdge5(options = {}) {
      return request("POST", "/edge", options);
    },

    /** Heartbeat d'un nœud edge (machine-to-cloud) */
    postEdgeNodeByNodeIdHeartbeat(options = {}) {
      return request("POST", "/edge-node/{nodeId}/heartbeat", options);
    },

    /** Heartbeat d'un nœud edge (machine-to-cloud) */
    postEdgeNodeByNodeIdHeartbeat2(options = {}) {
      return request("POST", "/edge-node/{nodeId}/heartbeat", options);
    },

    /** Heartbeat d'un nœud edge (machine-to-cloud) */
    postEdgeNodeByNodeIdHeartbeat3(options = {}) {
      return request("POST", "/edge-node/{nodeId}/heartbeat", options);
    },

    /** Heartbeat d'un nœud edge (machine-to-cloud) */
    postEdgeNodeByNodeIdHeartbeat4(options = {}) {
      return request("POST", "/edge-node/{nodeId}/heartbeat", options);
    },

    /** Heartbeat d'un nœud edge (machine-to-cloud) */
    postEdgeNodeByNodeIdHeartbeat5(options = {}) {
      return request("POST", "/edge-node/{nodeId}/heartbeat", options);
    },

    /** Recuperer le delta de synchronisation pour le nœud edge */
    getEdgeNodeByNodeIdPull(options = {}) {
      return request("GET", "/edge-node/{nodeId}/pull", options);
    },

    /** Recuperer le delta de synchronisation pour le nœud edge */
    getEdgeNodeByNodeIdPull2(options = {}) {
      return request("GET", "/edge-node/{nodeId}/pull", options);
    },

    /** Recuperer le delta de synchronisation pour le nœud edge */
    getEdgeNodeByNodeIdPull3(options = {}) {
      return request("GET", "/edge-node/{nodeId}/pull", options);
    },

    /** Recuperer le delta de synchronisation pour le nœud edge */
    getEdgeNodeByNodeIdPull4(options = {}) {
      return request("GET", "/edge-node/{nodeId}/pull", options);
    },

    /** Recuperer le delta de synchronisation pour le nœud edge */
    getEdgeNodeByNodeIdPull5(options = {}) {
      return request("GET", "/edge-node/{nodeId}/pull", options);
    },

    /** Pousser les donnees du nœud edge vers le cloud */
    postEdgeNodeByNodeIdPush(options = {}) {
      return request("POST", "/edge-node/{nodeId}/push", options);
    },

    /** Pousser les donnees du nœud edge vers le cloud */
    postEdgeNodeByNodeIdPush2(options = {}) {
      return request("POST", "/edge-node/{nodeId}/push", options);
    },

    /** Pousser les donnees du nœud edge vers le cloud */
    postEdgeNodeByNodeIdPush3(options = {}) {
      return request("POST", "/edge-node/{nodeId}/push", options);
    },

    /** Pousser les donnees du nœud edge vers le cloud */
    postEdgeNodeByNodeIdPush4(options = {}) {
      return request("POST", "/edge-node/{nodeId}/push", options);
    },

    /** Pousser les donnees du nœud edge vers le cloud */
    postEdgeNodeByNodeIdPush5(options = {}) {
      return request("POST", "/edge-node/{nodeId}/push", options);
    },

    /** Valider une licence edge (machine-to-cloud) */
    postEdgeNodeValidateLicense(options = {}) {
      return request("POST", "/edge-node/validate-license", options);
    },

    /** Valider une licence edge (machine-to-cloud) */
    postEdgeNodeValidateLicense2(options = {}) {
      return request("POST", "/edge-node/validate-license", options);
    },

    /** Valider une licence edge (machine-to-cloud) */
    postEdgeNodeValidateLicense3(options = {}) {
      return request("POST", "/edge-node/validate-license", options);
    },

    /** Valider une licence edge (machine-to-cloud) */
    postEdgeNodeValidateLicense4(options = {}) {
      return request("POST", "/edge-node/validate-license", options);
    },

    /** Valider une licence edge (machine-to-cloud) */
    postEdgeNodeValidateLicense5(options = {}) {
      return request("POST", "/edge-node/validate-license", options);
    },

    /** Detail d'un nœud edge du tenant */
    getEdgeByNodeId(options = {}) {
      return request("GET", "/edge/{nodeId}", options);
    },

    /** Detail d'un nœud edge du tenant */
    getEdgeByNodeId2(options = {}) {
      return request("GET", "/edge/{nodeId}", options);
    },

    /** Detail d'un nœud edge du tenant */
    getEdgeByNodeId3(options = {}) {
      return request("GET", "/edge/{nodeId}", options);
    },

    /** Detail d'un nœud edge du tenant */
    getEdgeByNodeId4(options = {}) {
      return request("GET", "/edge/{nodeId}", options);
    },

    /** Detail d'un nœud edge du tenant */
    getEdgeByNodeId5(options = {}) {
      return request("GET", "/edge/{nodeId}", options);
    },

    /** Emettre une licence pour un nœud edge */
    postEdgeByNodeIdLicense(options = {}) {
      return request("POST", "/edge/{nodeId}/license", options);
    },

    /** Emettre une licence pour un nœud edge */
    postEdgeByNodeIdLicense2(options = {}) {
      return request("POST", "/edge/{nodeId}/license", options);
    },

    /** Emettre une licence pour un nœud edge */
    postEdgeByNodeIdLicense3(options = {}) {
      return request("POST", "/edge/{nodeId}/license", options);
    },

    /** Emettre une licence pour un nœud edge */
    postEdgeByNodeIdLicense4(options = {}) {
      return request("POST", "/edge/{nodeId}/license", options);
    },

    /** Emettre une licence pour un nœud edge */
    postEdgeByNodeIdLicense5(options = {}) {
      return request("POST", "/edge/{nodeId}/license", options);
    },

    /** Declencher la synchronisation d'un nœud edge */
    postEdgeByNodeIdSync(options = {}) {
      return request("POST", "/edge/{nodeId}/sync", options);
    },

    /** Declencher la synchronisation d'un nœud edge */
    postEdgeByNodeIdSync2(options = {}) {
      return request("POST", "/edge/{nodeId}/sync", options);
    },

    /** Declencher la synchronisation d'un nœud edge */
    postEdgeByNodeIdSync3(options = {}) {
      return request("POST", "/edge/{nodeId}/sync", options);
    },

    /** Declencher la synchronisation d'un nœud edge */
    postEdgeByNodeIdSync4(options = {}) {
      return request("POST", "/edge/{nodeId}/sync", options);
    },

    /** Declencher la synchronisation d'un nœud edge */
    postEdgeByNodeIdSync5(options = {}) {
      return request("POST", "/edge/{nodeId}/sync", options);
    },

    /** Caddyfile.edge de reference pour le nœud edge (public) */
    getEdgeDownloadCaddyfileEdge(options = {}) {
      return request("GET", "/edge/download/Caddyfile.edge", options);
    },

    /** Caddyfile.edge de reference pour le nœud edge (public) */
    getEdgeDownloadCaddyfileEdge2(options = {}) {
      return request("GET", "/edge/download/Caddyfile.edge", options);
    },

    /** Caddyfile.edge de reference pour le nœud edge (public) */
    getEdgeDownloadCaddyfileEdge3(options = {}) {
      return request("GET", "/edge/download/Caddyfile.edge", options);
    },

    /** Caddyfile.edge de reference pour le nœud edge (public) */
    getEdgeDownloadCaddyfileEdge4(options = {}) {
      return request("GET", "/edge/download/Caddyfile.edge", options);
    },

    /** Caddyfile.edge de reference pour le nœud edge (public) */
    getEdgeDownloadCaddyfileEdge5(options = {}) {
      return request("GET", "/edge/download/Caddyfile.edge", options);
    },

    /** Somme SHA-256 de Caddyfile.edge (public) */
    getEdgeDownloadCaddyfileEdgeSha256(options = {}) {
      return request("GET", "/edge/download/Caddyfile.edge.sha256", options);
    },

    /** Somme SHA-256 de Caddyfile.edge (public) */
    getEdgeDownloadCaddyfileEdgeSha2562(options = {}) {
      return request("GET", "/edge/download/Caddyfile.edge.sha256", options);
    },

    /** Somme SHA-256 de Caddyfile.edge (public) */
    getEdgeDownloadCaddyfileEdgeSha2563(options = {}) {
      return request("GET", "/edge/download/Caddyfile.edge.sha256", options);
    },

    /** Somme SHA-256 de Caddyfile.edge (public) */
    getEdgeDownloadCaddyfileEdgeSha2564(options = {}) {
      return request("GET", "/edge/download/Caddyfile.edge.sha256", options);
    },

    /** Somme SHA-256 de Caddyfile.edge (public) */
    getEdgeDownloadCaddyfileEdgeSha2565(options = {}) {
      return request("GET", "/edge/download/Caddyfile.edge.sha256", options);
    },

    /** docker-compose.yml de reference pour le nœud edge (public) */
    getEdgeDownloadDockerComposeYml(options = {}) {
      return request("GET", "/edge/download/docker-compose.yml", options);
    },

    /** docker-compose.yml de reference pour le nœud edge (public) */
    getEdgeDownloadDockerComposeYml2(options = {}) {
      return request("GET", "/edge/download/docker-compose.yml", options);
    },

    /** docker-compose.yml de reference pour le nœud edge (public) */
    getEdgeDownloadDockerComposeYml3(options = {}) {
      return request("GET", "/edge/download/docker-compose.yml", options);
    },

    /** docker-compose.yml de reference pour le nœud edge (public) */
    getEdgeDownloadDockerComposeYml4(options = {}) {
      return request("GET", "/edge/download/docker-compose.yml", options);
    },

    /** docker-compose.yml de reference pour le nœud edge (public) */
    getEdgeDownloadDockerComposeYml5(options = {}) {
      return request("GET", "/edge/download/docker-compose.yml", options);
    },

    /** Somme SHA-256 de docker-compose.yml edge (public) */
    getEdgeDownloadDockerComposeYmlSha256(options = {}) {
      return request("GET", "/edge/download/docker-compose.yml.sha256", options);
    },

    /** Somme SHA-256 de docker-compose.yml edge (public) */
    getEdgeDownloadDockerComposeYmlSha2562(options = {}) {
      return request("GET", "/edge/download/docker-compose.yml.sha256", options);
    },

    /** Somme SHA-256 de docker-compose.yml edge (public) */
    getEdgeDownloadDockerComposeYmlSha2563(options = {}) {
      return request("GET", "/edge/download/docker-compose.yml.sha256", options);
    },

    /** Somme SHA-256 de docker-compose.yml edge (public) */
    getEdgeDownloadDockerComposeYmlSha2564(options = {}) {
      return request("GET", "/edge/download/docker-compose.yml.sha256", options);
    },

    /** Somme SHA-256 de docker-compose.yml edge (public) */
    getEdgeDownloadDockerComposeYmlSha2565(options = {}) {
      return request("GET", "/edge/download/docker-compose.yml.sha256", options);
    },

    /** Exemple de fichier d'environnement pour le nœud edge (public) */
    getEdgeDownloadEnvExample(options = {}) {
      return request("GET", "/edge/download/env-example", options);
    },

    /** Exemple de fichier d'environnement pour le nœud edge (public) */
    getEdgeDownloadEnvExample2(options = {}) {
      return request("GET", "/edge/download/env-example", options);
    },

    /** Exemple de fichier d'environnement pour le nœud edge (public) */
    getEdgeDownloadEnvExample3(options = {}) {
      return request("GET", "/edge/download/env-example", options);
    },

    /** Exemple de fichier d'environnement pour le nœud edge (public) */
    getEdgeDownloadEnvExample4(options = {}) {
      return request("GET", "/edge/download/env-example", options);
    },

    /** Exemple de fichier d'environnement pour le nœud edge (public) */
    getEdgeDownloadEnvExample5(options = {}) {
      return request("GET", "/edge/download/env-example", options);
    },

    /** Sommes SHA-256 des artefacts edge (public) */
    getEdgeDownloadSha256Txt(options = {}) {
      return request("GET", "/edge/download/sha256.txt", options);
    },

    /** Sommes SHA-256 des artefacts edge (public) */
    getEdgeDownloadSha256Txt2(options = {}) {
      return request("GET", "/edge/download/sha256.txt", options);
    },

    /** Sommes SHA-256 des artefacts edge (public) */
    getEdgeDownloadSha256Txt3(options = {}) {
      return request("GET", "/edge/download/sha256.txt", options);
    },

    /** Sommes SHA-256 des artefacts edge (public) */
    getEdgeDownloadSha256Txt4(options = {}) {
      return request("GET", "/edge/download/sha256.txt", options);
    },

    /** Sommes SHA-256 des artefacts edge (public) */
    getEdgeDownloadSha256Txt5(options = {}) {
      return request("GET", "/edge/download/sha256.txt", options);
    },

    /** Healthcheck edge (public, machine-to-cloud) */
    getEdgeHealth(options = {}) {
      return request("GET", "/edge/health", options);
    },

    /** Healthcheck edge (public, machine-to-cloud) */
    getEdgeHealth2(options = {}) {
      return request("GET", "/edge/health", options);
    },

    /** Healthcheck edge (public, machine-to-cloud) */
    getEdgeHealth3(options = {}) {
      return request("GET", "/edge/health", options);
    },

    /** Healthcheck edge (public, machine-to-cloud) */
    getEdgeHealth4(options = {}) {
      return request("GET", "/edge/health", options);
    },

    /** Healthcheck edge (public, machine-to-cloud) */
    getEdgeHealth5(options = {}) {
      return request("GET", "/edge/health", options);
    },

    /** Heartbeat machine-to-cloud d'un nœud edge (public) */
    postEdgeHeartbeat(options = {}) {
      return request("POST", "/edge/heartbeat", options);
    },

    /** Heartbeat machine-to-cloud d'un nœud edge (public) */
    postEdgeHeartbeat2(options = {}) {
      return request("POST", "/edge/heartbeat", options);
    },

    /** Heartbeat machine-to-cloud d'un nœud edge (public) */
    postEdgeHeartbeat3(options = {}) {
      return request("POST", "/edge/heartbeat", options);
    },

    /** Heartbeat machine-to-cloud d'un nœud edge (public) */
    postEdgeHeartbeat4(options = {}) {
      return request("POST", "/edge/heartbeat", options);
    },

    /** Heartbeat machine-to-cloud d'un nœud edge (public) */
    postEdgeHeartbeat5(options = {}) {
      return request("POST", "/edge/heartbeat", options);
    },

    /** Script d'installation du nœud edge (public) */
    getEdgeInstallSh(options = {}) {
      return request("GET", "/edge/install.sh", options);
    },

    /** Script d'installation du nœud edge (public) */
    getEdgeInstallSh2(options = {}) {
      return request("GET", "/edge/install.sh", options);
    },

    /** Script d'installation du nœud edge (public) */
    getEdgeInstallSh3(options = {}) {
      return request("GET", "/edge/install.sh", options);
    },

    /** Script d'installation du nœud edge (public) */
    getEdgeInstallSh4(options = {}) {
      return request("GET", "/edge/install.sh", options);
    },

    /** Script d'installation du nœud edge (public) */
    getEdgeInstallSh5(options = {}) {
      return request("GET", "/edge/install.sh", options);
    },

    /** Cle publique de verification des licences edge (public) */
    getEdgeLicensePublicKey(options = {}) {
      return request("GET", "/edge/license-public-key", options);
    },

    /** Cle publique de verification des licences edge (public) */
    getEdgeLicensePublicKey2(options = {}) {
      return request("GET", "/edge/license-public-key", options);
    },

    /** Cle publique de verification des licences edge (public) */
    getEdgeLicensePublicKey3(options = {}) {
      return request("GET", "/edge/license-public-key", options);
    },

    /** Cle publique de verification des licences edge (public) */
    getEdgeLicensePublicKey4(options = {}) {
      return request("GET", "/edge/license-public-key", options);
    },

    /** Cle publique de verification des licences edge (public) */
    getEdgeLicensePublicKey5(options = {}) {
      return request("GET", "/edge/license-public-key", options);
    },

    /** Readiness probe du nœud edge (schéma SQLite) */
    getEdgeReadiness(options = {}) {
      return request("GET", "/edge/readiness", options);
    },

    /** Readiness probe du nœud edge (schéma SQLite) */
    getEdgeReadiness2(options = {}) {
      return request("GET", "/edge/readiness", options);
    },

    /** Readiness probe du nœud edge (schéma SQLite) */
    getEdgeReadiness3(options = {}) {
      return request("GET", "/edge/readiness", options);
    },

    /** Readiness probe du nœud edge (schéma SQLite) */
    getEdgeReadiness4(options = {}) {
      return request("GET", "/edge/readiness", options);
    },

    /** Readiness probe du nœud edge (schéma SQLite) */
    getEdgeReadiness5(options = {}) {
      return request("GET", "/edge/readiness", options);
    },

    /** Lister les documents des dossiers employes (checklist G3) */
    getEmployeeDocuments(options = {}) {
      return request("GET", "/employee-documents", options);
    },

    /** Lister les documents des dossiers employes (checklist G3) */
    getEmployeeDocuments2(options = {}) {
      return request("GET", "/employee-documents", options);
    },

    /** Lister les documents des dossiers employes (checklist G3) */
    getEmployeeDocuments3(options = {}) {
      return request("GET", "/employee-documents", options);
    },

    /** Lister les documents des dossiers employes (checklist G3) */
    getEmployeeDocuments4(options = {}) {
      return request("GET", "/employee-documents", options);
    },

    /** Enregistrer un document du dossier employe */
    postEmployeeDocuments(options = {}) {
      return request("POST", "/employee-documents", options);
    },

    /** Enregistrer un document du dossier employe */
    postEmployeeDocuments2(options = {}) {
      return request("POST", "/employee-documents", options);
    },

    /** Enregistrer un document du dossier employe */
    postEmployeeDocuments3(options = {}) {
      return request("POST", "/employee-documents", options);
    },

    /** Enregistrer un document du dossier employe */
    postEmployeeDocuments4(options = {}) {
      return request("POST", "/employee-documents", options);
    },

    /** Supprimer un document du dossier employe */
    deleteEmployeeDocumentsByEmployeeDocument(options = {}) {
      return request("DELETE", "/employee-documents/{employeeDocument}", options);
    },

    /** Supprimer un document du dossier employe */
    deleteEmployeeDocumentsByEmployeeDocument2(options = {}) {
      return request("DELETE", "/employee-documents/{employeeDocument}", options);
    },

    /** Supprimer un document du dossier employe */
    deleteEmployeeDocumentsByEmployeeDocument3(options = {}) {
      return request("DELETE", "/employee-documents/{employeeDocument}", options);
    },

    /** Supprimer un document du dossier employe */
    deleteEmployeeDocumentsByEmployeeDocument4(options = {}) {
      return request("DELETE", "/employee-documents/{employeeDocument}", options);
    },

    /** Lire un document du dossier employe */
    getEmployeeDocumentsByEmployeeDocument(options = {}) {
      return request("GET", "/employee-documents/{employeeDocument}", options);
    },

    /** Lire un document du dossier employe */
    getEmployeeDocumentsByEmployeeDocument2(options = {}) {
      return request("GET", "/employee-documents/{employeeDocument}", options);
    },

    /** Lire un document du dossier employe */
    getEmployeeDocumentsByEmployeeDocument3(options = {}) {
      return request("GET", "/employee-documents/{employeeDocument}", options);
    },

    /** Lire un document du dossier employe */
    getEmployeeDocumentsByEmployeeDocument4(options = {}) {
      return request("GET", "/employee-documents/{employeeDocument}", options);
    },

    /** Patch un document du dossier employe */
    patchEmployeeDocumentsByEmployeeDocument(options = {}) {
      return request("PATCH", "/employee-documents/{employeeDocument}", options);
    },

    /** Patch un document du dossier employe */
    patchEmployeeDocumentsByEmployeeDocument2(options = {}) {
      return request("PATCH", "/employee-documents/{employeeDocument}", options);
    },

    /** Patch un document du dossier employe */
    patchEmployeeDocumentsByEmployeeDocument3(options = {}) {
      return request("PATCH", "/employee-documents/{employeeDocument}", options);
    },

    /** Patch un document du dossier employe */
    patchEmployeeDocumentsByEmployeeDocument4(options = {}) {
      return request("PATCH", "/employee-documents/{employeeDocument}", options);
    },

    /** Mettre a jour un document du dossier employe */
    putEmployeeDocumentsByEmployeeDocument(options = {}) {
      return request("PUT", "/employee-documents/{employeeDocument}", options);
    },

    /** Mettre a jour un document du dossier employe */
    putEmployeeDocumentsByEmployeeDocument2(options = {}) {
      return request("PUT", "/employee-documents/{employeeDocument}", options);
    },

    /** Mettre a jour un document du dossier employe */
    putEmployeeDocumentsByEmployeeDocument3(options = {}) {
      return request("PUT", "/employee-documents/{employeeDocument}", options);
    },

    /** Mettre a jour un document du dossier employe */
    putEmployeeDocumentsByEmployeeDocument4(options = {}) {
      return request("PUT", "/employee-documents/{employeeDocument}", options);
    },

    /** Lister les employes */
    getEmployees(options = {}) {
      return request("GET", "/employees", options);
    },

    /** Lister les employes */
    getEmployees2(options = {}) {
      return request("GET", "/employees", options);
    },

    /** Lister les employes */
    getEmployees3(options = {}) {
      return request("GET", "/employees", options);
    },

    /** Lister les employes */
    getEmployees4(options = {}) {
      return request("GET", "/employees", options);
    },

    /** Lister les employes */
    getEmployees5(options = {}) {
      return request("GET", "/employees", options);
    },

    /** Creer un employe */
    postEmployees(options = {}) {
      return request("POST", "/employees", options);
    },

    /** Creer un employe */
    postEmployees2(options = {}) {
      return request("POST", "/employees", options);
    },

    /** Creer un employe */
    postEmployees3(options = {}) {
      return request("POST", "/employees", options);
    },

    /** Creer un employe */
    postEmployees4(options = {}) {
      return request("POST", "/employees", options);
    },

    /** Creer un employe */
    postEmployees5(options = {}) {
      return request("POST", "/employees", options);
    },

    /** Voir un employe */
    getEmployeesByEmployee(options = {}) {
      return request("GET", "/employees/{employee}", options);
    },

    /** Voir un employe */
    getEmployeesByEmployee2(options = {}) {
      return request("GET", "/employees/{employee}", options);
    },

    /** Voir un employe */
    getEmployeesByEmployee3(options = {}) {
      return request("GET", "/employees/{employee}", options);
    },

    /** Voir un employe */
    getEmployeesByEmployee4(options = {}) {
      return request("GET", "/employees/{employee}", options);
    },

    /** Voir un employe */
    getEmployeesByEmployee5(options = {}) {
      return request("GET", "/employees/{employee}", options);
    },

    /** Patch employe */
    patchEmployeesByEmployee(options = {}) {
      return request("PATCH", "/employees/{employee}", options);
    },

    /** Patch employe */
    patchEmployeesByEmployee2(options = {}) {
      return request("PATCH", "/employees/{employee}", options);
    },

    /** Patch employe */
    patchEmployeesByEmployee3(options = {}) {
      return request("PATCH", "/employees/{employee}", options);
    },

    /** Patch employe */
    patchEmployeesByEmployee4(options = {}) {
      return request("PATCH", "/employees/{employee}", options);
    },

    /** Patch employe */
    patchEmployeesByEmployee5(options = {}) {
      return request("PATCH", "/employees/{employee}", options);
    },

    /** Mettre a jour un employe */
    putEmployeesByEmployee(options = {}) {
      return request("PUT", "/employees/{employee}", options);
    },

    /** Mettre a jour un employe */
    putEmployeesByEmployee2(options = {}) {
      return request("PUT", "/employees/{employee}", options);
    },

    /** Mettre a jour un employe */
    putEmployeesByEmployee3(options = {}) {
      return request("PUT", "/employees/{employee}", options);
    },

    /** Mettre a jour un employe */
    putEmployeesByEmployee4(options = {}) {
      return request("PUT", "/employees/{employee}", options);
    },

    /** Mettre a jour un employe */
    putEmployeesByEmployee5(options = {}) {
      return request("PUT", "/employees/{employee}", options);
    },

    /** Archiver un employe */
    postEmployeesByEmployeeArchive(options = {}) {
      return request("POST", "/employees/{employee}/archive", options);
    },

    /** Archiver un employe */
    postEmployeesByEmployeeArchive2(options = {}) {
      return request("POST", "/employees/{employee}/archive", options);
    },

    /** Archiver un employe */
    postEmployeesByEmployeeArchive3(options = {}) {
      return request("POST", "/employees/{employee}/archive", options);
    },

    /** Archiver un employe */
    postEmployeesByEmployeeArchive4(options = {}) {
      return request("POST", "/employees/{employee}/archive", options);
    },

    /** Archiver un employe */
    postEmployeesByEmployeeArchive5(options = {}) {
      return request("POST", "/employees/{employee}/archive", options);
    },

    /** Assigner un role manager a un employe (principal) */
    postEmployeesByEmployeeAssignRole(options = {}) {
      return request("POST", "/employees/{employee}/assign-role", options);
    },

    /** Assigner un role manager a un employe (principal) */
    postEmployeesByEmployeeAssignRole2(options = {}) {
      return request("POST", "/employees/{employee}/assign-role", options);
    },

    /** Assigner un role manager a un employe (principal) */
    postEmployeesByEmployeeAssignRole3(options = {}) {
      return request("POST", "/employees/{employee}/assign-role", options);
    },

    /** Assigner un role manager a un employe (principal) */
    postEmployeesByEmployeeAssignRole4(options = {}) {
      return request("POST", "/employees/{employee}/assign-role", options);
    },

    /** Assigner un role manager a un employe (principal) */
    postEmployeesByEmployeeAssignRole5(options = {}) {
      return request("POST", "/employees/{employee}/assign-role", options);
    },

    /** Assigner un role manager a un employe (principal) */
    postEmployeesByEmployeeAssignRole6(options = {}) {
      return request("POST", "/employees/{employee}/assign-role", options);
    },

    /** Assigner un role manager a un employe (principal) */
    postEmployeesByEmployeeAssignRole7(options = {}) {
      return request("POST", "/employees/{employee}/assign-role", options);
    },

    /** Assigner un role manager a un employe (principal) */
    postEmployeesByEmployeeAssignRole8(options = {}) {
      return request("POST", "/employees/{employee}/assign-role", options);
    },

    /** Assigner un role manager a un employe (principal) */
    postEmployeesByEmployeeAssignRole9(options = {}) {
      return request("POST", "/employees/{employee}/assign-role", options);
    },

    /** Assigner un role manager a un employe (principal) */
    postEmployeesByEmployeeAssignRole10(options = {}) {
      return request("POST", "/employees/{employee}/assign-role", options);
    },

    /** Assigner un role manager a un employe (principal) */
    postEmployeesByEmployeeAssignRole11(options = {}) {
      return request("POST", "/employees/{employee}/assign-role", options);
    },

    /** Solde de congés d'un employé (manager/RH) */
    getEmployeesByEmployeeBalance(options = {}) {
      return request("GET", "/employees/{employee}/balance", options);
    },

    /** Solde de congés d'un employé (manager/RH) */
    getEmployeesByEmployeeBalance2(options = {}) {
      return request("GET", "/employees/{employee}/balance", options);
    },

    /** Solde de congés d'un employé (manager/RH) */
    getEmployeesByEmployeeBalance3(options = {}) {
      return request("GET", "/employees/{employee}/balance", options);
    },

    /** Solde de congés d'un employé (manager/RH) */
    getEmployeesByEmployeeBalance4(options = {}) {
      return request("GET", "/employees/{employee}/balance", options);
    },

    /** Solde de congés d'un employé (manager/RH) */
    getEmployeesByEmployeeBalance5(options = {}) {
      return request("GET", "/employees/{employee}/balance", options);
    },

    /** Solde de congés d'un employé (manager/RH) */
    getEmployeesByEmployeeBalance6(options = {}) {
      return request("GET", "/employees/{employee}/balance", options);
    },

    /** Solde de congés d'un employé (manager/RH) */
    getEmployeesByEmployeeBalance7(options = {}) {
      return request("GET", "/employees/{employee}/balance", options);
    },

    /** Solde de congés d'un employé (manager/RH) */
    getEmployeesByEmployeeBalance8(options = {}) {
      return request("GET", "/employees/{employee}/balance", options);
    },

    /** Solde de congés d'un employé (manager/RH) */
    getEmployeesByEmployeeBalance9(options = {}) {
      return request("GET", "/employees/{employee}/balance", options);
    },

    /** Solde de congés d'un employé (manager/RH) */
    getEmployeesByEmployeeBalance10(options = {}) {
      return request("GET", "/employees/{employee}/balance", options);
    },

    /** Solde de congés d'un employé (manager/RH) */
    getEmployeesByEmployeeBalance11(options = {}) {
      return request("GET", "/employees/{employee}/balance", options);
    },

    /** Certificat de travail PDF (fin de contrat, F-08) */
    getEmployeesByEmployeeCertificateOfEmployment(options = {}) {
      return request("GET", "/employees/{employee}/certificate-of-employment", options);
    },

    /** Certificat de travail PDF (fin de contrat, F-08) */
    getEmployeesByEmployeeCertificateOfEmployment2(options = {}) {
      return request("GET", "/employees/{employee}/certificate-of-employment", options);
    },

    /** Certificat de travail PDF (fin de contrat, F-08) */
    getEmployeesByEmployeeCertificateOfEmployment3(options = {}) {
      return request("GET", "/employees/{employee}/certificate-of-employment", options);
    },

    /** Certificat de travail PDF (fin de contrat, F-08) */
    getEmployeesByEmployeeCertificateOfEmployment4(options = {}) {
      return request("GET", "/employees/{employee}/certificate-of-employment", options);
    },

    /** Certificat de travail PDF (fin de contrat, F-08) */
    getEmployeesByEmployeeCertificateOfEmployment5(options = {}) {
      return request("GET", "/employees/{employee}/certificate-of-employment", options);
    },

    /** Resume journalier d'un employe */
    getEmployeesByEmployeeDailySummary(options = {}) {
      return request("GET", "/employees/{employee}/daily-summary", options);
    },

    /** Resume journalier d'un employe */
    getEmployeesByEmployeeDailySummary2(options = {}) {
      return request("GET", "/employees/{employee}/daily-summary", options);
    },

    /** Resume journalier d'un employe */
    getEmployeesByEmployeeDailySummary3(options = {}) {
      return request("GET", "/employees/{employee}/daily-summary", options);
    },

    /** Resume journalier d'un employe */
    getEmployeesByEmployeeDailySummary4(options = {}) {
      return request("GET", "/employees/{employee}/daily-summary", options);
    },

    /** Resume journalier d'un employe */
    getEmployeesByEmployeeDailySummary5(options = {}) {
      return request("GET", "/employees/{employee}/daily-summary", options);
    },

    /** Lire le depart d'un employe (manager : entreprise ; employe : le sien) */
    getEmployeesByEmployeeDeparture(options = {}) {
      return request("GET", "/employees/{employee}/departure", options);
    },

    /** Enregistrer le depart d'un employe (offboarding, issue #5324) */
    postEmployeesByEmployeeDeparture(options = {}) {
      return request("POST", "/employees/{employee}/departure", options);
    },

    /** Récapitulatif du préavis légal (issue #5325, G2) */
    getEmployeesByEmployeeDepartureNotice(options = {}) {
      return request("GET", "/employees/{employee}/departure/notice", options);
    },

    /** Récapitulatif du préavis légal (issue #5325, G2) */
    getEmployeesByEmployeeDepartureNotice2(options = {}) {
      return request("GET", "/employees/{employee}/departure/notice", options);
    },

    /** Récapitulatif du préavis légal (issue #5325, G2) */
    getEmployeesByEmployeeDepartureNotice3(options = {}) {
      return request("GET", "/employees/{employee}/departure/notice", options);
    },

    /** Récapitulatif du préavis légal (issue #5325, G2) */
    getEmployeesByEmployeeDepartureNotice4(options = {}) {
      return request("GET", "/employees/{employee}/departure/notice", options);
    },

    /** Récapitulatif du préavis légal (issue #5325, G2) */
    getEmployeesByEmployeeDepartureNotice5(options = {}) {
      return request("GET", "/employees/{employee}/departure/notice", options);
    },

    /** Solde de tout compte (fin de contrat, F-08) */
    getEmployeesByEmployeeEndOfContract(options = {}) {
      return request("GET", "/employees/{employee}/end-of-contract", options);
    },

    /** Solde de tout compte (fin de contrat, F-08) */
    getEmployeesByEmployeeEndOfContract2(options = {}) {
      return request("GET", "/employees/{employee}/end-of-contract", options);
    },

    /** Solde de tout compte (fin de contrat, F-08) */
    getEmployeesByEmployeeEndOfContract3(options = {}) {
      return request("GET", "/employees/{employee}/end-of-contract", options);
    },

    /** Solde de tout compte (fin de contrat, F-08) */
    getEmployeesByEmployeeEndOfContract4(options = {}) {
      return request("GET", "/employees/{employee}/end-of-contract", options);
    },

    /** Solde de tout compte (fin de contrat, F-08) */
    getEmployeesByEmployeeEndOfContract5(options = {}) {
      return request("GET", "/employees/{employee}/end-of-contract", options);
    },

    /** Journal de paie d'un employé (manager/RH) */
    getEmployeesByEmployeeLedger(options = {}) {
      return request("GET", "/employees/{employee}/ledger", options);
    },

    /** Journal de paie d'un employé (manager/RH) */
    getEmployeesByEmployeeLedger2(options = {}) {
      return request("GET", "/employees/{employee}/ledger", options);
    },

    /** Journal de paie d'un employé (manager/RH) */
    getEmployeesByEmployeeLedger3(options = {}) {
      return request("GET", "/employees/{employee}/ledger", options);
    },

    /** Journal de paie d'un employé (manager/RH) */
    getEmployeesByEmployeeLedger4(options = {}) {
      return request("GET", "/employees/{employee}/ledger", options);
    },

    /** Journal de paie d'un employé (manager/RH) */
    getEmployeesByEmployeeLedger5(options = {}) {
      return request("GET", "/employees/{employee}/ledger", options);
    },

    /** Journal de paie d'un employé (manager/RH) */
    getEmployeesByEmployeeLedger6(options = {}) {
      return request("GET", "/employees/{employee}/ledger", options);
    },

    /** Journal de paie d'un employé (manager/RH) */
    getEmployeesByEmployeeLedger7(options = {}) {
      return request("GET", "/employees/{employee}/ledger", options);
    },

    /** Journal de paie d'un employé (manager/RH) */
    getEmployeesByEmployeeLedger8(options = {}) {
      return request("GET", "/employees/{employee}/ledger", options);
    },

    /** Journal de paie d'un employé (manager/RH) */
    getEmployeesByEmployeeLedger9(options = {}) {
      return request("GET", "/employees/{employee}/ledger", options);
    },

    /** Journal de paie d'un employé (manager/RH) */
    getEmployeesByEmployeeLedger10(options = {}) {
      return request("GET", "/employees/{employee}/ledger", options);
    },

    /** Journal de paie d'un employé (manager/RH) */
    getEmployeesByEmployeeLedger11(options = {}) {
      return request("GET", "/employees/{employee}/ledger", options);
    },

    /** Estimation sur periode */
    getEmployeesByEmployeeQuickEstimate(options = {}) {
      return request("GET", "/employees/{employee}/quick-estimate", options);
    },

    /** Estimation sur periode */
    getEmployeesByEmployeeQuickEstimate2(options = {}) {
      return request("GET", "/employees/{employee}/quick-estimate", options);
    },

    /** Estimation sur periode */
    getEmployeesByEmployeeQuickEstimate3(options = {}) {
      return request("GET", "/employees/{employee}/quick-estimate", options);
    },

    /** Estimation sur periode */
    getEmployeesByEmployeeQuickEstimate4(options = {}) {
      return request("GET", "/employees/{employee}/quick-estimate", options);
    },

    /** Estimation sur periode */
    getEmployeesByEmployeeQuickEstimate5(options = {}) {
      return request("GET", "/employees/{employee}/quick-estimate", options);
    },

    /** Telecharger le PDF d'estimation */
    getEmployeesByEmployeeReceipt(options = {}) {
      return request("GET", "/employees/{employee}/receipt", options);
    },

    /** Telecharger le PDF d'estimation */
    getEmployeesByEmployeeReceipt2(options = {}) {
      return request("GET", "/employees/{employee}/receipt", options);
    },

    /** Telecharger le PDF d'estimation */
    getEmployeesByEmployeeReceipt3(options = {}) {
      return request("GET", "/employees/{employee}/receipt", options);
    },

    /** Telecharger le PDF d'estimation */
    getEmployeesByEmployeeReceipt4(options = {}) {
      return request("GET", "/employees/{employee}/receipt", options);
    },

    /** Telecharger le PDF d'estimation */
    getEmployeesByEmployeeReceipt5(options = {}) {
      return request("GET", "/employees/{employee}/receipt", options);
    },

    /** Importer des employes par CSV */
    postEmployeesImport(options = {}) {
      return request("POST", "/employees/import", options);
    },

    /** Importer des employes par CSV */
    postEmployeesImport2(options = {}) {
      return request("POST", "/employees/import", options);
    },

    /** Importer des employes par CSV */
    postEmployeesImport3(options = {}) {
      return request("POST", "/employees/import", options);
    },

    /** Importer des employes par CSV */
    postEmployeesImport4(options = {}) {
      return request("POST", "/employees/import", options);
    },

    /** Importer des employes par CSV */
    postEmployeesImport5(options = {}) {
      return request("POST", "/employees/import", options);
    },

    /** Importer des employes par CSV */
    postEmployeesImport6(options = {}) {
      return request("POST", "/employees/import", options);
    },

    /** Importer des employes par CSV */
    postEmployeesImport7(options = {}) {
      return request("POST", "/employees/import", options);
    },

    /** Importer des employes par CSV */
    postEmployeesImport8(options = {}) {
      return request("POST", "/employees/import", options);
    },

    /** Importer des employes par CSV */
    postEmployeesImport9(options = {}) {
      return request("POST", "/employees/import", options);
    },

    /** Importer des employes par CSV */
    postEmployeesImport10(options = {}) {
      return request("POST", "/employees/import", options);
    },

    /** Template CSV import */
    getEmployeesImportTemplate(options = {}) {
      return request("GET", "/employees/import-template", options);
    },

    /** Template CSV import */
    getEmployeesImportTemplate2(options = {}) {
      return request("GET", "/employees/import-template", options);
    },

    /** Template CSV import */
    getEmployeesImportTemplate3(options = {}) {
      return request("GET", "/employees/import-template", options);
    },

    /** Template CSV import */
    getEmployeesImportTemplate4(options = {}) {
      return request("GET", "/employees/import-template", options);
    },

    /** Template CSV import */
    getEmployeesImportTemplate5(options = {}) {
      return request("GET", "/employees/import-template", options);
    },

    /** Template CSV import */
    getEmployeesImportTemplate6(options = {}) {
      return request("GET", "/employees/import-template", options);
    },

    /** Template CSV import */
    getEmployeesImportTemplate7(options = {}) {
      return request("GET", "/employees/import-template", options);
    },

    /** Template CSV import */
    getEmployeesImportTemplate8(options = {}) {
      return request("GET", "/employees/import-template", options);
    },

    /** Template CSV import */
    getEmployeesImportTemplate9(options = {}) {
      return request("GET", "/employees/import-template", options);
    },

    /** Template CSV import */
    getEmployeesImportTemplate10(options = {}) {
      return request("GET", "/employees/import-template", options);
    },

    /** Manager — lie un utilisateur ordinaire à un employé du tenant */
    postEmployeesLinkUser(options = {}) {
      return request("POST", "/employees/link-user", options);
    },

    /** Manager — lie un utilisateur ordinaire à un employé du tenant */
    postEmployeesLinkUser2(options = {}) {
      return request("POST", "/employees/link-user", options);
    },

    /** Manager — lie un utilisateur ordinaire à un employé du tenant */
    postEmployeesLinkUser3(options = {}) {
      return request("POST", "/employees/link-user", options);
    },

    /** Manager — lie un utilisateur ordinaire à un employé du tenant */
    postEmployeesLinkUser4(options = {}) {
      return request("POST", "/employees/link-user", options);
    },

    /** Manager — lie un utilisateur ordinaire à un employé du tenant */
    postEmployeesLinkUser5(options = {}) {
      return request("POST", "/employees/link-user", options);
    },

    /** Lister les evaluations */
    getEvaluations(options = {}) {
      return request("GET", "/evaluations", options);
    },

    /** Lister les evaluations */
    getEvaluations2(options = {}) {
      return request("GET", "/evaluations", options);
    },

    /** Lister les evaluations */
    getEvaluations3(options = {}) {
      return request("GET", "/evaluations", options);
    },

    /** Lister les evaluations */
    getEvaluations4(options = {}) {
      return request("GET", "/evaluations", options);
    },

    /** Lister les evaluations */
    getEvaluations5(options = {}) {
      return request("GET", "/evaluations", options);
    },

    /** Lister les evaluations */
    getEvaluations6(options = {}) {
      return request("GET", "/evaluations", options);
    },

    /** Lister les evaluations */
    getEvaluations7(options = {}) {
      return request("GET", "/evaluations", options);
    },

    /** Lister les evaluations */
    getEvaluations8(options = {}) {
      return request("GET", "/evaluations", options);
    },

    /** Creer une evaluation */
    postEvaluations(options = {}) {
      return request("POST", "/evaluations", options);
    },

    /** Creer une evaluation */
    postEvaluations2(options = {}) {
      return request("POST", "/evaluations", options);
    },

    /** Creer une evaluation */
    postEvaluations3(options = {}) {
      return request("POST", "/evaluations", options);
    },

    /** Creer une evaluation */
    postEvaluations4(options = {}) {
      return request("POST", "/evaluations", options);
    },

    /** Creer une evaluation */
    postEvaluations5(options = {}) {
      return request("POST", "/evaluations", options);
    },

    /** Creer une evaluation */
    postEvaluations6(options = {}) {
      return request("POST", "/evaluations", options);
    },

    /** Creer une evaluation */
    postEvaluations7(options = {}) {
      return request("POST", "/evaluations", options);
    },

    /** Creer une evaluation */
    postEvaluations8(options = {}) {
      return request("POST", "/evaluations", options);
    },

    /** Supprimer une evaluation */
    deleteEvaluationsByEvaluation(options = {}) {
      return request("DELETE", "/evaluations/{evaluation}", options);
    },

    /** Supprimer une evaluation */
    deleteEvaluationsByEvaluation2(options = {}) {
      return request("DELETE", "/evaluations/{evaluation}", options);
    },

    /** Supprimer une evaluation */
    deleteEvaluationsByEvaluation3(options = {}) {
      return request("DELETE", "/evaluations/{evaluation}", options);
    },

    /** Supprimer une evaluation */
    deleteEvaluationsByEvaluation4(options = {}) {
      return request("DELETE", "/evaluations/{evaluation}", options);
    },

    /** Supprimer une evaluation */
    deleteEvaluationsByEvaluation5(options = {}) {
      return request("DELETE", "/evaluations/{evaluation}", options);
    },

    /** Supprimer une evaluation */
    deleteEvaluationsByEvaluation6(options = {}) {
      return request("DELETE", "/evaluations/{evaluation}", options);
    },

    /** Supprimer une evaluation */
    deleteEvaluationsByEvaluation7(options = {}) {
      return request("DELETE", "/evaluations/{evaluation}", options);
    },

    /** Supprimer une evaluation */
    deleteEvaluationsByEvaluation8(options = {}) {
      return request("DELETE", "/evaluations/{evaluation}", options);
    },

    /** Voir une evaluation */
    getEvaluationsByEvaluation(options = {}) {
      return request("GET", "/evaluations/{evaluation}", options);
    },

    /** Voir une evaluation */
    getEvaluationsByEvaluation2(options = {}) {
      return request("GET", "/evaluations/{evaluation}", options);
    },

    /** Voir une evaluation */
    getEvaluationsByEvaluation3(options = {}) {
      return request("GET", "/evaluations/{evaluation}", options);
    },

    /** Voir une evaluation */
    getEvaluationsByEvaluation4(options = {}) {
      return request("GET", "/evaluations/{evaluation}", options);
    },

    /** Voir une evaluation */
    getEvaluationsByEvaluation5(options = {}) {
      return request("GET", "/evaluations/{evaluation}", options);
    },

    /** Voir une evaluation */
    getEvaluationsByEvaluation6(options = {}) {
      return request("GET", "/evaluations/{evaluation}", options);
    },

    /** Voir une evaluation */
    getEvaluationsByEvaluation7(options = {}) {
      return request("GET", "/evaluations/{evaluation}", options);
    },

    /** Voir une evaluation */
    getEvaluationsByEvaluation8(options = {}) {
      return request("GET", "/evaluations/{evaluation}", options);
    },

    /** Mettre a jour partiellement une evaluation */
    patchEvaluationsByEvaluation(options = {}) {
      return request("PATCH", "/evaluations/{evaluation}", options);
    },

    /** Mettre a jour partiellement une evaluation */
    patchEvaluationsByEvaluation2(options = {}) {
      return request("PATCH", "/evaluations/{evaluation}", options);
    },

    /** Mettre a jour partiellement une evaluation */
    patchEvaluationsByEvaluation3(options = {}) {
      return request("PATCH", "/evaluations/{evaluation}", options);
    },

    /** Mettre a jour partiellement une evaluation */
    patchEvaluationsByEvaluation4(options = {}) {
      return request("PATCH", "/evaluations/{evaluation}", options);
    },

    /** Mettre a jour partiellement une evaluation */
    patchEvaluationsByEvaluation5(options = {}) {
      return request("PATCH", "/evaluations/{evaluation}", options);
    },

    /** Mettre a jour partiellement une evaluation */
    patchEvaluationsByEvaluation6(options = {}) {
      return request("PATCH", "/evaluations/{evaluation}", options);
    },

    /** Mettre a jour partiellement une evaluation */
    patchEvaluationsByEvaluation7(options = {}) {
      return request("PATCH", "/evaluations/{evaluation}", options);
    },

    /** Mettre a jour partiellement une evaluation */
    patchEvaluationsByEvaluation8(options = {}) {
      return request("PATCH", "/evaluations/{evaluation}", options);
    },

    /** Mettre a jour une evaluation */
    putEvaluationsByEvaluation(options = {}) {
      return request("PUT", "/evaluations/{evaluation}", options);
    },

    /** Mettre a jour une evaluation */
    putEvaluationsByEvaluation2(options = {}) {
      return request("PUT", "/evaluations/{evaluation}", options);
    },

    /** Mettre a jour une evaluation */
    putEvaluationsByEvaluation3(options = {}) {
      return request("PUT", "/evaluations/{evaluation}", options);
    },

    /** Mettre a jour une evaluation */
    putEvaluationsByEvaluation4(options = {}) {
      return request("PUT", "/evaluations/{evaluation}", options);
    },

    /** Mettre a jour une evaluation */
    putEvaluationsByEvaluation5(options = {}) {
      return request("PUT", "/evaluations/{evaluation}", options);
    },

    /** Mettre a jour une evaluation */
    putEvaluationsByEvaluation6(options = {}) {
      return request("PUT", "/evaluations/{evaluation}", options);
    },

    /** Mettre a jour une evaluation */
    putEvaluationsByEvaluation7(options = {}) {
      return request("PUT", "/evaluations/{evaluation}", options);
    },

    /** Mettre a jour une evaluation */
    putEvaluationsByEvaluation8(options = {}) {
      return request("PUT", "/evaluations/{evaluation}", options);
    },

    /** Accuser reception d'une evaluation */
    putEvaluationsByEvaluationAcknowledge(options = {}) {
      return request("PUT", "/evaluations/{evaluation}/acknowledge", options);
    },

    /** Accuser reception d'une evaluation */
    putEvaluationsByEvaluationAcknowledge2(options = {}) {
      return request("PUT", "/evaluations/{evaluation}/acknowledge", options);
    },

    /** Accuser reception d'une evaluation */
    putEvaluationsByEvaluationAcknowledge3(options = {}) {
      return request("PUT", "/evaluations/{evaluation}/acknowledge", options);
    },

    /** Accuser reception d'une evaluation */
    putEvaluationsByEvaluationAcknowledge4(options = {}) {
      return request("PUT", "/evaluations/{evaluation}/acknowledge", options);
    },

    /** Accuser reception d'une evaluation */
    putEvaluationsByEvaluationAcknowledge5(options = {}) {
      return request("PUT", "/evaluations/{evaluation}/acknowledge", options);
    },

    /** Accuser reception d'une evaluation */
    putEvaluationsByEvaluationAcknowledge6(options = {}) {
      return request("PUT", "/evaluations/{evaluation}/acknowledge", options);
    },

    /** Accuser reception d'une evaluation */
    putEvaluationsByEvaluationAcknowledge7(options = {}) {
      return request("PUT", "/evaluations/{evaluation}/acknowledge", options);
    },

    /** Accuser reception d'une evaluation */
    putEvaluationsByEvaluationAcknowledge8(options = {}) {
      return request("PUT", "/evaluations/{evaluation}/acknowledge", options);
    },

    /** Soumettre une evaluation */
    putEvaluationsByEvaluationSubmit(options = {}) {
      return request("PUT", "/evaluations/{evaluation}/submit", options);
    },

    /** Soumettre une evaluation */
    putEvaluationsByEvaluationSubmit2(options = {}) {
      return request("PUT", "/evaluations/{evaluation}/submit", options);
    },

    /** Soumettre une evaluation */
    putEvaluationsByEvaluationSubmit3(options = {}) {
      return request("PUT", "/evaluations/{evaluation}/submit", options);
    },

    /** Soumettre une evaluation */
    putEvaluationsByEvaluationSubmit4(options = {}) {
      return request("PUT", "/evaluations/{evaluation}/submit", options);
    },

    /** Soumettre une evaluation */
    putEvaluationsByEvaluationSubmit5(options = {}) {
      return request("PUT", "/evaluations/{evaluation}/submit", options);
    },

    /** Soumettre une evaluation */
    putEvaluationsByEvaluationSubmit6(options = {}) {
      return request("PUT", "/evaluations/{evaluation}/submit", options);
    },

    /** Soumettre une evaluation */
    putEvaluationsByEvaluationSubmit7(options = {}) {
      return request("PUT", "/evaluations/{evaluation}/submit", options);
    },

    /** Soumettre une evaluation */
    putEvaluationsByEvaluationSubmit8(options = {}) {
      return request("PUT", "/evaluations/{evaluation}/submit", options);
    },

    /** Lister les notes de frais */
    getExpenseClaims(options = {}) {
      return request("GET", "/expense-claims", options);
    },

    /** Lister les notes de frais */
    getExpenseClaims2(options = {}) {
      return request("GET", "/expense-claims", options);
    },

    /** Lister les notes de frais */
    getExpenseClaims3(options = {}) {
      return request("GET", "/expense-claims", options);
    },

    /** Lister les notes de frais */
    getExpenseClaims4(options = {}) {
      return request("GET", "/expense-claims", options);
    },

    /** Lister les notes de frais */
    getExpenseClaims5(options = {}) {
      return request("GET", "/expense-claims", options);
    },

    /** Lister les notes de frais */
    getExpenseClaims6(options = {}) {
      return request("GET", "/expense-claims", options);
    },

    /** Lister les notes de frais */
    getExpenseClaims7(options = {}) {
      return request("GET", "/expense-claims", options);
    },

    /** Lister les notes de frais */
    getExpenseClaims8(options = {}) {
      return request("GET", "/expense-claims", options);
    },

    /** Lister les notes de frais */
    getExpenseClaims9(options = {}) {
      return request("GET", "/expense-claims", options);
    },

    /** Lister les notes de frais */
    getExpenseClaims10(options = {}) {
      return request("GET", "/expense-claims", options);
    },

    /** Lister les notes de frais */
    getExpenseClaims11(options = {}) {
      return request("GET", "/expense-claims", options);
    },

    /** Creer une note de frais */
    postExpenseClaims(options = {}) {
      return request("POST", "/expense-claims", options);
    },

    /** Creer une note de frais */
    postExpenseClaims2(options = {}) {
      return request("POST", "/expense-claims", options);
    },

    /** Creer une note de frais */
    postExpenseClaims3(options = {}) {
      return request("POST", "/expense-claims", options);
    },

    /** Creer une note de frais */
    postExpenseClaims4(options = {}) {
      return request("POST", "/expense-claims", options);
    },

    /** Creer une note de frais */
    postExpenseClaims5(options = {}) {
      return request("POST", "/expense-claims", options);
    },

    /** Creer une note de frais */
    postExpenseClaims6(options = {}) {
      return request("POST", "/expense-claims", options);
    },

    /** Creer une note de frais */
    postExpenseClaims7(options = {}) {
      return request("POST", "/expense-claims", options);
    },

    /** Creer une note de frais */
    postExpenseClaims8(options = {}) {
      return request("POST", "/expense-claims", options);
    },

    /** Creer une note de frais */
    postExpenseClaims9(options = {}) {
      return request("POST", "/expense-claims", options);
    },

    /** Creer une note de frais */
    postExpenseClaims10(options = {}) {
      return request("POST", "/expense-claims", options);
    },

    /** Creer une note de frais */
    postExpenseClaims11(options = {}) {
      return request("POST", "/expense-claims", options);
    },

    /** Supprimer une note de frais */
    deleteExpenseClaimsByExpenseClaim(options = {}) {
      return request("DELETE", "/expense-claims/{expenseClaim}", options);
    },

    /** Supprimer une note de frais */
    deleteExpenseClaimsByExpenseClaim2(options = {}) {
      return request("DELETE", "/expense-claims/{expenseClaim}", options);
    },

    /** Supprimer une note de frais */
    deleteExpenseClaimsByExpenseClaim3(options = {}) {
      return request("DELETE", "/expense-claims/{expenseClaim}", options);
    },

    /** Supprimer une note de frais */
    deleteExpenseClaimsByExpenseClaim4(options = {}) {
      return request("DELETE", "/expense-claims/{expenseClaim}", options);
    },

    /** Supprimer une note de frais */
    deleteExpenseClaimsByExpenseClaim5(options = {}) {
      return request("DELETE", "/expense-claims/{expenseClaim}", options);
    },

    /** Supprimer une note de frais */
    deleteExpenseClaimsByExpenseClaim6(options = {}) {
      return request("DELETE", "/expense-claims/{expenseClaim}", options);
    },

    /** Supprimer une note de frais */
    deleteExpenseClaimsByExpenseClaim7(options = {}) {
      return request("DELETE", "/expense-claims/{expenseClaim}", options);
    },

    /** Supprimer une note de frais */
    deleteExpenseClaimsByExpenseClaim8(options = {}) {
      return request("DELETE", "/expense-claims/{expenseClaim}", options);
    },

    /** Supprimer une note de frais */
    deleteExpenseClaimsByExpenseClaim9(options = {}) {
      return request("DELETE", "/expense-claims/{expenseClaim}", options);
    },

    /** Supprimer une note de frais */
    deleteExpenseClaimsByExpenseClaim10(options = {}) {
      return request("DELETE", "/expense-claims/{expenseClaim}", options);
    },

    /** Supprimer une note de frais */
    deleteExpenseClaimsByExpenseClaim11(options = {}) {
      return request("DELETE", "/expense-claims/{expenseClaim}", options);
    },

    /** Voir une note de frais */
    getExpenseClaimsByExpenseClaim(options = {}) {
      return request("GET", "/expense-claims/{expenseClaim}", options);
    },

    /** Voir une note de frais */
    getExpenseClaimsByExpenseClaim2(options = {}) {
      return request("GET", "/expense-claims/{expenseClaim}", options);
    },

    /** Voir une note de frais */
    getExpenseClaimsByExpenseClaim3(options = {}) {
      return request("GET", "/expense-claims/{expenseClaim}", options);
    },

    /** Voir une note de frais */
    getExpenseClaimsByExpenseClaim4(options = {}) {
      return request("GET", "/expense-claims/{expenseClaim}", options);
    },

    /** Voir une note de frais */
    getExpenseClaimsByExpenseClaim5(options = {}) {
      return request("GET", "/expense-claims/{expenseClaim}", options);
    },

    /** Voir une note de frais */
    getExpenseClaimsByExpenseClaim6(options = {}) {
      return request("GET", "/expense-claims/{expenseClaim}", options);
    },

    /** Voir une note de frais */
    getExpenseClaimsByExpenseClaim7(options = {}) {
      return request("GET", "/expense-claims/{expenseClaim}", options);
    },

    /** Voir une note de frais */
    getExpenseClaimsByExpenseClaim8(options = {}) {
      return request("GET", "/expense-claims/{expenseClaim}", options);
    },

    /** Voir une note de frais */
    getExpenseClaimsByExpenseClaim9(options = {}) {
      return request("GET", "/expense-claims/{expenseClaim}", options);
    },

    /** Voir une note de frais */
    getExpenseClaimsByExpenseClaim10(options = {}) {
      return request("GET", "/expense-claims/{expenseClaim}", options);
    },

    /** Voir une note de frais */
    getExpenseClaimsByExpenseClaim11(options = {}) {
      return request("GET", "/expense-claims/{expenseClaim}", options);
    },

    /** Modifier une note de frais */
    putExpenseClaimsByExpenseClaim(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}", options);
    },

    /** Modifier une note de frais */
    putExpenseClaimsByExpenseClaim2(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}", options);
    },

    /** Modifier une note de frais */
    putExpenseClaimsByExpenseClaim3(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}", options);
    },

    /** Modifier une note de frais */
    putExpenseClaimsByExpenseClaim4(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}", options);
    },

    /** Modifier une note de frais */
    putExpenseClaimsByExpenseClaim5(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}", options);
    },

    /** Modifier une note de frais */
    putExpenseClaimsByExpenseClaim6(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}", options);
    },

    /** Modifier une note de frais */
    putExpenseClaimsByExpenseClaim7(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}", options);
    },

    /** Modifier une note de frais */
    putExpenseClaimsByExpenseClaim8(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}", options);
    },

    /** Modifier une note de frais */
    putExpenseClaimsByExpenseClaim9(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}", options);
    },

    /** Modifier une note de frais */
    putExpenseClaimsByExpenseClaim10(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}", options);
    },

    /** Modifier une note de frais */
    putExpenseClaimsByExpenseClaim11(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}", options);
    },

    /** Ecritures comptables d'une note de frais approuvee (#5235) */
    getExpenseClaimsByExpenseClaimAccountingEntries(options = {}) {
      return request("GET", "/expense-claims/{expenseClaim}/accounting-entries", options);
    },

    /** Regenerer les ecritures comptables d'une note (comptable, idempotent) */
    postExpenseClaimsByExpenseClaimAccountingEntriesRegenerate(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/accounting-entries/regenerate", options);
    },

    /** Approuver une note de frais */
    postExpenseClaimsByExpenseClaimApprove(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais */
    postExpenseClaimsByExpenseClaimApprove2(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais */
    postExpenseClaimsByExpenseClaimApprove3(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais */
    postExpenseClaimsByExpenseClaimApprove4(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais */
    postExpenseClaimsByExpenseClaimApprove5(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais */
    postExpenseClaimsByExpenseClaimApprove6(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais */
    postExpenseClaimsByExpenseClaimApprove7(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais */
    postExpenseClaimsByExpenseClaimApprove8(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais */
    postExpenseClaimsByExpenseClaimApprove9(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais */
    postExpenseClaimsByExpenseClaimApprove10(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais */
    postExpenseClaimsByExpenseClaimApprove11(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimApprove(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimApprove2(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimApprove3(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimApprove4(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimApprove5(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimApprove6(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimApprove7(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimApprove8(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimApprove9(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimApprove10(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Approuver une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimApprove11(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/approve", options);
    },

    /** Rejeter une note de frais */
    postExpenseClaimsByExpenseClaimReject(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Rejeter une note de frais */
    postExpenseClaimsByExpenseClaimReject2(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Rejeter une note de frais */
    postExpenseClaimsByExpenseClaimReject3(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Rejeter une note de frais */
    postExpenseClaimsByExpenseClaimReject4(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Rejeter une note de frais */
    postExpenseClaimsByExpenseClaimReject5(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Rejeter une note de frais */
    postExpenseClaimsByExpenseClaimReject6(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Rejeter une note de frais */
    postExpenseClaimsByExpenseClaimReject7(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Rejeter une note de frais */
    postExpenseClaimsByExpenseClaimReject8(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Rejeter une note de frais */
    postExpenseClaimsByExpenseClaimReject9(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Rejeter une note de frais */
    postExpenseClaimsByExpenseClaimReject10(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Rejeter une note de frais */
    postExpenseClaimsByExpenseClaimReject11(options = {}) {
      return request("POST", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Refuser une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimReject(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Refuser une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimReject2(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Refuser une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimReject3(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Refuser une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimReject4(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Refuser une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimReject5(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Refuser une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimReject6(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Refuser une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimReject7(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Refuser une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimReject8(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Refuser une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimReject9(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Refuser une note de frais (déprécié — utiliser POST) */
    putExpenseClaimsByExpenseClaimReject10(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Rejeter une note de frais (deprecie) */
    putExpenseClaimsByExpenseClaimReject11(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/reject", options);
    },

    /** Soumettre une note de frais */
    putExpenseClaimsByExpenseClaimSubmit(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/submit", options);
    },

    /** Soumettre une note de frais */
    putExpenseClaimsByExpenseClaimSubmit2(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/submit", options);
    },

    /** Soumettre une note de frais */
    putExpenseClaimsByExpenseClaimSubmit3(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/submit", options);
    },

    /** Soumettre une note de frais */
    putExpenseClaimsByExpenseClaimSubmit4(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/submit", options);
    },

    /** Soumettre une note de frais */
    putExpenseClaimsByExpenseClaimSubmit5(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/submit", options);
    },

    /** Soumettre une note de frais */
    putExpenseClaimsByExpenseClaimSubmit6(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/submit", options);
    },

    /** Soumettre une note de frais */
    putExpenseClaimsByExpenseClaimSubmit7(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/submit", options);
    },

    /** Soumettre une note de frais */
    putExpenseClaimsByExpenseClaimSubmit8(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/submit", options);
    },

    /** Soumettre une note de frais */
    putExpenseClaimsByExpenseClaimSubmit9(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/submit", options);
    },

    /** Soumettre une note de frais */
    putExpenseClaimsByExpenseClaimSubmit10(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/submit", options);
    },

    /** Soumettre une note de frais */
    putExpenseClaimsByExpenseClaimSubmit11(options = {}) {
      return request("PUT", "/expense-claims/{expenseClaim}/submit", options);
    },

    /** Exporter les absences */
    getExportAbsences(options = {}) {
      return request("GET", "/export/absences", options);
    },

    /** Exporter les absences */
    getExportAbsences2(options = {}) {
      return request("GET", "/export/absences", options);
    },

    /** Exporter les absences */
    getExportAbsences3(options = {}) {
      return request("GET", "/export/absences", options);
    },

    /** Exporter les absences */
    getExportAbsences4(options = {}) {
      return request("GET", "/export/absences", options);
    },

    /** Exporter les absences */
    getExportAbsences5(options = {}) {
      return request("GET", "/export/absences", options);
    },

    /** Exporter les absences */
    getExportAbsences6(options = {}) {
      return request("GET", "/export/absences", options);
    },

    /** Exporter les absences */
    getExportAbsences7(options = {}) {
      return request("GET", "/export/absences", options);
    },

    /** Exporter les absences */
    getExportAbsences8(options = {}) {
      return request("GET", "/export/absences", options);
    },

    /** Exporter les absences */
    getExportAbsences9(options = {}) {
      return request("GET", "/export/absences", options);
    },

    /** Exporter les absences */
    getExportAbsences10(options = {}) {
      return request("GET", "/export/absences", options);
    },

    /** Exporter les absences */
    getExportAbsences11(options = {}) {
      return request("GET", "/export/absences", options);
    },

    /** Exporter l'ordre de virement (OD) */
    getExportAccountingOd(options = {}) {
      return request("GET", "/export/accounting-od", options);
    },

    /** Exporter l'ordre de virement (OD) */
    getExportAccountingOd2(options = {}) {
      return request("GET", "/export/accounting-od", options);
    },

    /** Exporter l'ordre de virement (OD) */
    getExportAccountingOd3(options = {}) {
      return request("GET", "/export/accounting-od", options);
    },

    /** Exporter l'ordre de virement (OD) */
    getExportAccountingOd4(options = {}) {
      return request("GET", "/export/accounting-od", options);
    },

    /** Exporter l'ordre de virement (OD) */
    getExportAccountingOd5(options = {}) {
      return request("GET", "/export/accounting-od", options);
    },

    /** Exporter l'ordre de virement (OD) */
    getExportAccountingOd6(options = {}) {
      return request("GET", "/export/accounting-od", options);
    },

    /** Exporter l'ordre de virement (OD) */
    getExportAccountingOd7(options = {}) {
      return request("GET", "/export/accounting-od", options);
    },

    /** Exporter l'ordre de virement (OD) */
    getExportAccountingOd8(options = {}) {
      return request("GET", "/export/accounting-od", options);
    },

    /** Exporter l'ordre de virement (OD) */
    getExportAccountingOd9(options = {}) {
      return request("GET", "/export/accounting-od", options);
    },

    /** Exporter l'ordre de virement (OD) */
    getExportAccountingOd10(options = {}) {
      return request("GET", "/export/accounting-od", options);
    },

    /** Exporter l'ordre de virement (OD) */
    getExportAccountingOd11(options = {}) {
      return request("GET", "/export/accounting-od", options);
    },

    /** Exporter le pointage */
    getExportAttendance(options = {}) {
      return request("GET", "/export/attendance", options);
    },

    /** Exporter le pointage */
    getExportAttendance2(options = {}) {
      return request("GET", "/export/attendance", options);
    },

    /** Exporter le pointage */
    getExportAttendance3(options = {}) {
      return request("GET", "/export/attendance", options);
    },

    /** Exporter le pointage */
    getExportAttendance4(options = {}) {
      return request("GET", "/export/attendance", options);
    },

    /** Exporter le pointage */
    getExportAttendance5(options = {}) {
      return request("GET", "/export/attendance", options);
    },

    /** Exporter le pointage */
    getExportAttendance6(options = {}) {
      return request("GET", "/export/attendance", options);
    },

    /** Exporter le pointage */
    getExportAttendance7(options = {}) {
      return request("GET", "/export/attendance", options);
    },

    /** Exporter le pointage */
    getExportAttendance8(options = {}) {
      return request("GET", "/export/attendance", options);
    },

    /** Exporter le pointage */
    getExportAttendance9(options = {}) {
      return request("GET", "/export/attendance", options);
    },

    /** Exporter le pointage */
    getExportAttendance10(options = {}) {
      return request("GET", "/export/attendance", options);
    },

    /** Exporter le pointage */
    getExportAttendance11(options = {}) {
      return request("GET", "/export/attendance", options);
    },

    /** Exporter les contrats */
    getExportContracts(options = {}) {
      return request("GET", "/export/contracts", options);
    },

    /** Exporter les contrats */
    getExportContracts2(options = {}) {
      return request("GET", "/export/contracts", options);
    },

    /** Exporter les contrats */
    getExportContracts3(options = {}) {
      return request("GET", "/export/contracts", options);
    },

    /** Exporter les contrats */
    getExportContracts4(options = {}) {
      return request("GET", "/export/contracts", options);
    },

    /** Exporter les contrats */
    getExportContracts5(options = {}) {
      return request("GET", "/export/contracts", options);
    },

    /** Exporter les contrats */
    getExportContracts6(options = {}) {
      return request("GET", "/export/contracts", options);
    },

    /** Exporter les contrats */
    getExportContracts7(options = {}) {
      return request("GET", "/export/contracts", options);
    },

    /** Exporter les contrats */
    getExportContracts8(options = {}) {
      return request("GET", "/export/contracts", options);
    },

    /** Exporter les contrats */
    getExportContracts9(options = {}) {
      return request("GET", "/export/contracts", options);
    },

    /** Exporter les contrats */
    getExportContracts10(options = {}) {
      return request("GET", "/export/contracts", options);
    },

    /** Exporter les contrats */
    getExportContracts11(options = {}) {
      return request("GET", "/export/contracts", options);
    },

    /** Exporter les employes */
    getExportEmployees(options = {}) {
      return request("GET", "/export/employees", options);
    },

    /** Exporter les employes */
    getExportEmployees2(options = {}) {
      return request("GET", "/export/employees", options);
    },

    /** Exporter les employes */
    getExportEmployees3(options = {}) {
      return request("GET", "/export/employees", options);
    },

    /** Exporter les employes */
    getExportEmployees4(options = {}) {
      return request("GET", "/export/employees", options);
    },

    /** Exporter les employes */
    getExportEmployees5(options = {}) {
      return request("GET", "/export/employees", options);
    },

    /** Exporter les employes */
    getExportEmployees6(options = {}) {
      return request("GET", "/export/employees", options);
    },

    /** Exporter les employes */
    getExportEmployees7(options = {}) {
      return request("GET", "/export/employees", options);
    },

    /** Exporter les employes */
    getExportEmployees8(options = {}) {
      return request("GET", "/export/employees", options);
    },

    /** Exporter les employes */
    getExportEmployees9(options = {}) {
      return request("GET", "/export/employees", options);
    },

    /** Exporter les employes */
    getExportEmployees10(options = {}) {
      return request("GET", "/export/employees", options);
    },

    /** Exporter les employes */
    getExportEmployees11(options = {}) {
      return request("GET", "/export/employees", options);
    },

    /** Historique des exports du portail manager (issue #2199) - manager uniquement */
    exportHistory(options = {}) {
      return request("GET", "/export/history", options);
    },

    /** Historique des exports du portail manager (issue #2199) - manager uniquement */
    exportHistory2(options = {}) {
      return request("GET", "/export/history", options);
    },

    /** Historique des exports du portail manager (issue #2199) - manager uniquement */
    exportHistory3(options = {}) {
      return request("GET", "/export/history", options);
    },

    /** Historique des exports du portail manager (issue #2199) - manager uniquement */
    exportHistory4(options = {}) {
      return request("GET", "/export/history", options);
    },

    /** Historique des exports du portail manager (issue #2199) - manager uniquement */
    exportHistory5(options = {}) {
      return request("GET", "/export/history", options);
    },

    /** Historique des exports du portail manager (issue #2199) - manager uniquement */
    exportHistory6(options = {}) {
      return request("GET", "/export/history", options);
    },

    /** Historique des exports du portail manager (issue #2199) - manager uniquement */
    exportHistory7(options = {}) {
      return request("GET", "/export/history", options);
    },

    /** Historique des exports du portail manager (issue #2199) - manager uniquement */
    exportHistory8(options = {}) {
      return request("GET", "/export/history", options);
    },

    /** Historique des exports du portail manager (issue #2199) - manager uniquement */
    exportHistory9(options = {}) {
      return request("GET", "/export/history", options);
    },

    /** Historique des exports du portail manager (issue #2199) - manager uniquement */
    exportHistory10(options = {}) {
      return request("GET", "/export/history", options);
    },

    /** Exporter les bulletins de paie de l'entreprise (JSON ou CSV encode en JSON) - manager uniquement */
    exportPaySlips(options = {}) {
      return request("GET", "/export/pay-slips", options);
    },

    /** Exporter les bulletins de paie de l'entreprise (JSON ou CSV encode en JSON) - manager uniquement */
    exportPaySlips2(options = {}) {
      return request("GET", "/export/pay-slips", options);
    },

    /** Exporter les bulletins de paie de l'entreprise (JSON ou CSV encode en JSON) - manager uniquement */
    exportPaySlips3(options = {}) {
      return request("GET", "/export/pay-slips", options);
    },

    /** Exporter les bulletins de paie de l'entreprise (JSON ou CSV encode en JSON) - manager uniquement */
    exportPaySlips4(options = {}) {
      return request("GET", "/export/pay-slips", options);
    },

    /** Exporter les bulletins de paie de l'entreprise (JSON ou CSV encode en JSON) - manager uniquement */
    exportPaySlips5(options = {}) {
      return request("GET", "/export/pay-slips", options);
    },

    /** Exporter les bulletins de paie de l'entreprise (JSON ou CSV encode en JSON) - manager uniquement */
    exportPaySlips6(options = {}) {
      return request("GET", "/export/pay-slips", options);
    },

    /** Exporter les bulletins de paie de l'entreprise (JSON ou CSV encode en JSON) - manager uniquement */
    exportPaySlips7(options = {}) {
      return request("GET", "/export/pay-slips", options);
    },

    /** Exporter les bulletins de paie de l'entreprise (JSON ou CSV encode en JSON) - manager uniquement */
    exportPaySlips8(options = {}) {
      return request("GET", "/export/pay-slips", options);
    },

    /** Exporter les bulletins de paie de l'entreprise (JSON ou CSV encode en JSON) - manager uniquement */
    exportPaySlips9(options = {}) {
      return request("GET", "/export/pay-slips", options);
    },

    /** Exporter les bulletins de paie de l'entreprise (JSON ou CSV encode en JSON) - manager uniquement */
    exportPaySlips10(options = {}) {
      return request("GET", "/export/pay-slips", options);
    },

    /** Exporter le journal de paie */
    getExportPayrollJournal(options = {}) {
      return request("GET", "/export/payroll-journal", options);
    },

    /** Exporter le journal de paie */
    getExportPayrollJournal2(options = {}) {
      return request("GET", "/export/payroll-journal", options);
    },

    /** Exporter le journal de paie */
    getExportPayrollJournal3(options = {}) {
      return request("GET", "/export/payroll-journal", options);
    },

    /** Exporter le journal de paie */
    getExportPayrollJournal4(options = {}) {
      return request("GET", "/export/payroll-journal", options);
    },

    /** Exporter le journal de paie */
    getExportPayrollJournal5(options = {}) {
      return request("GET", "/export/payroll-journal", options);
    },

    /** Exporter le journal de paie */
    getExportPayrollJournal6(options = {}) {
      return request("GET", "/export/payroll-journal", options);
    },

    /** Exporter le journal de paie */
    getExportPayrollJournal7(options = {}) {
      return request("GET", "/export/payroll-journal", options);
    },

    /** Exporter le journal de paie */
    getExportPayrollJournal8(options = {}) {
      return request("GET", "/export/payroll-journal", options);
    },

    /** Exporter le journal de paie */
    getExportPayrollJournal9(options = {}) {
      return request("GET", "/export/payroll-journal", options);
    },

    /** Exporter le journal de paie */
    getExportPayrollJournal10(options = {}) {
      return request("GET", "/export/payroll-journal", options);
    },

    /** Exporter le journal de paie */
    getExportPayrollJournal11(options = {}) {
      return request("GET", "/export/payroll-journal", options);
    },

    /** Exporter le grand livre de paie */
    getExportPayrollLedger(options = {}) {
      return request("GET", "/export/payroll-ledger", options);
    },

    /** Exporter le grand livre de paie */
    getExportPayrollLedger2(options = {}) {
      return request("GET", "/export/payroll-ledger", options);
    },

    /** Exporter le grand livre de paie */
    getExportPayrollLedger3(options = {}) {
      return request("GET", "/export/payroll-ledger", options);
    },

    /** Exporter le grand livre de paie */
    getExportPayrollLedger4(options = {}) {
      return request("GET", "/export/payroll-ledger", options);
    },

    /** Exporter le grand livre de paie */
    getExportPayrollLedger5(options = {}) {
      return request("GET", "/export/payroll-ledger", options);
    },

    /** Exporter le grand livre de paie */
    getExportPayrollLedger6(options = {}) {
      return request("GET", "/export/payroll-ledger", options);
    },

    /** Exporter le grand livre de paie */
    getExportPayrollLedger7(options = {}) {
      return request("GET", "/export/payroll-ledger", options);
    },

    /** Exporter le grand livre de paie */
    getExportPayrollLedger8(options = {}) {
      return request("GET", "/export/payroll-ledger", options);
    },

    /** Exporter le grand livre de paie */
    getExportPayrollLedger9(options = {}) {
      return request("GET", "/export/payroll-ledger", options);
    },

    /** Exporter le grand livre de paie */
    getExportPayrollLedger10(options = {}) {
      return request("GET", "/export/payroll-ledger", options);
    },

    /** Exporter le grand livre de paie */
    getExportPayrollLedger11(options = {}) {
      return request("GET", "/export/payroll-ledger", options);
    },

    /** Exporter les formations */
    getExportTraining(options = {}) {
      return request("GET", "/export/training", options);
    },

    /** Exporter les formations */
    getExportTraining2(options = {}) {
      return request("GET", "/export/training", options);
    },

    /** Exporter les formations */
    getExportTraining3(options = {}) {
      return request("GET", "/export/training", options);
    },

    /** Exporter les formations */
    getExportTraining4(options = {}) {
      return request("GET", "/export/training", options);
    },

    /** Exporter les formations */
    getExportTraining5(options = {}) {
      return request("GET", "/export/training", options);
    },

    /** Exporter les formations */
    getExportTraining6(options = {}) {
      return request("GET", "/export/training", options);
    },

    /** Exporter les formations */
    getExportTraining7(options = {}) {
      return request("GET", "/export/training", options);
    },

    /** Exporter les formations */
    getExportTraining8(options = {}) {
      return request("GET", "/export/training", options);
    },

    /** Exporter les formations */
    getExportTraining9(options = {}) {
      return request("GET", "/export/training", options);
    },

    /** Exporter les formations */
    getExportTraining10(options = {}) {
      return request("GET", "/export/training", options);
    },

    /** Exporter les formations */
    getExportTraining11(options = {}) {
      return request("GET", "/export/training", options);
    },

    /** Exporter la flotte */
    getExportVehicles(options = {}) {
      return request("GET", "/export/vehicles", options);
    },

    /** Exporter la flotte */
    getExportVehicles2(options = {}) {
      return request("GET", "/export/vehicles", options);
    },

    /** Exporter la flotte */
    getExportVehicles3(options = {}) {
      return request("GET", "/export/vehicles", options);
    },

    /** Exporter la flotte */
    getExportVehicles4(options = {}) {
      return request("GET", "/export/vehicles", options);
    },

    /** Exporter la flotte */
    getExportVehicles5(options = {}) {
      return request("GET", "/export/vehicles", options);
    },

    /** Exporter la flotte */
    getExportVehicles6(options = {}) {
      return request("GET", "/export/vehicles", options);
    },

    /** Exporter la flotte */
    getExportVehicles7(options = {}) {
      return request("GET", "/export/vehicles", options);
    },

    /** Exporter la flotte */
    getExportVehicles8(options = {}) {
      return request("GET", "/export/vehicles", options);
    },

    /** Exporter la flotte */
    getExportVehicles9(options = {}) {
      return request("GET", "/export/vehicles", options);
    },

    /** Exporter la flotte */
    getExportVehicles10(options = {}) {
      return request("GET", "/export/vehicles", options);
    },

    /** Exporter la flotte */
    getExportVehicles11(options = {}) {
      return request("GET", "/export/vehicles", options);
    },

    /** Verifier si une fonctionnalite est activee pour le plan de l'entreprise courante */
    checkFeatureFlag(options = {}) {
      return request("GET", "/feature-flags/check/{featureKey}", options);
    },

    /** Verifier si une fonctionnalite est activee pour le plan de l'entreprise courante */
    checkFeatureFlag2(options = {}) {
      return request("GET", "/feature-flags/check/{featureKey}", options);
    },

    /** Verifier si une fonctionnalite est activee pour le plan de l'entreprise courante */
    checkFeatureFlag3(options = {}) {
      return request("GET", "/feature-flags/check/{featureKey}", options);
    },

    /** Verifier si une fonctionnalite est activee pour le plan de l'entreprise courante */
    checkFeatureFlag4(options = {}) {
      return request("GET", "/feature-flags/check/{featureKey}", options);
    },

    /** Verifier si une fonctionnalite est activee pour le plan de l'entreprise courante */
    checkFeatureFlag5(options = {}) {
      return request("GET", "/feature-flags/check/{featureKey}", options);
    },

    /** Verifier si une fonctionnalite est activee pour le plan de l'entreprise courante */
    checkFeatureFlag6(options = {}) {
      return request("GET", "/feature-flags/check/{featureKey}", options);
    },

    /** Verifier si une fonctionnalite est activee pour le plan de l'entreprise courante */
    checkFeatureFlag7(options = {}) {
      return request("GET", "/feature-flags/check/{featureKey}", options);
    },

    /** Verifier si une fonctionnalite est activee pour le plan de l'entreprise courante */
    checkFeatureFlag8(options = {}) {
      return request("GET", "/feature-flags/check/{featureKey}", options);
    },

    /** Verifier si une fonctionnalite est activee pour le plan de l'entreprise courante */
    checkFeatureFlag9(options = {}) {
      return request("GET", "/feature-flags/check/{featureKey}", options);
    },

    /** Verifier si une fonctionnalite est activee pour le plan de l'entreprise courante */
    checkFeatureFlag10(options = {}) {
      return request("GET", "/feature-flags/check/{featureKey}", options);
    },

    /** Verifier si une fonctionnalite est activee pour le plan de l'entreprise courante */
    checkFeatureFlag11(options = {}) {
      return request("GET", "/feature-flags/check/{featureKey}", options);
    },

    /** Voir la matrice complete feature x plan */
    getFeatureFlagMatrix(options = {}) {
      return request("GET", "/feature-flags/matrix", options);
    },

    /** Voir la matrice complete feature x plan */
    getFeatureFlagMatrix2(options = {}) {
      return request("GET", "/feature-flags/matrix", options);
    },

    /** Voir la matrice complete feature x plan */
    getFeatureFlagMatrix3(options = {}) {
      return request("GET", "/feature-flags/matrix", options);
    },

    /** Voir la matrice complete feature x plan */
    getFeatureFlagMatrix4(options = {}) {
      return request("GET", "/feature-flags/matrix", options);
    },

    /** Voir la matrice complete feature x plan */
    getFeatureFlagMatrix5(options = {}) {
      return request("GET", "/feature-flags/matrix", options);
    },

    /** Voir la matrice complete feature x plan */
    getFeatureFlagMatrix6(options = {}) {
      return request("GET", "/feature-flags/matrix", options);
    },

    /** Voir la matrice complete feature x plan */
    getFeatureFlagMatrix7(options = {}) {
      return request("GET", "/feature-flags/matrix", options);
    },

    /** Voir la matrice complete feature x plan */
    getFeatureFlagMatrix8(options = {}) {
      return request("GET", "/feature-flags/matrix", options);
    },

    /** Voir la matrice complete feature x plan */
    getFeatureFlagMatrix9(options = {}) {
      return request("GET", "/feature-flags/matrix", options);
    },

    /** Voir la matrice complete feature x plan */
    getFeatureFlagMatrix10(options = {}) {
      return request("GET", "/feature-flags/matrix", options);
    },

    /** Voir la matrice complete feature x plan */
    getFeatureFlagMatrix11(options = {}) {
      return request("GET", "/feature-flags/matrix", options);
    },

    /** Voir le detail d'une fonctionnalite du registre */
    getFeatureByKey(options = {}) {
      return request("GET", "/features/{key}", options);
    },

    /** Voir le detail d'une fonctionnalite du registre */
    getFeatureByKey2(options = {}) {
      return request("GET", "/features/{key}", options);
    },

    /** Voir le detail d'une fonctionnalite du registre */
    getFeatureByKey3(options = {}) {
      return request("GET", "/features/{key}", options);
    },

    /** Voir le detail d'une fonctionnalite du registre */
    getFeatureByKey4(options = {}) {
      return request("GET", "/features/{key}", options);
    },

    /** Voir le detail d'une fonctionnalite du registre */
    getFeatureByKey5(options = {}) {
      return request("GET", "/features/{key}", options);
    },

    /** Voir le detail d'une fonctionnalite du registre */
    getFeatureByKey6(options = {}) {
      return request("GET", "/features/{key}", options);
    },

    /** Voir le detail d'une fonctionnalite du registre */
    getFeatureByKey7(options = {}) {
      return request("GET", "/features/{key}", options);
    },

    /** Voir le detail d'une fonctionnalite du registre */
    getFeatureByKey8(options = {}) {
      return request("GET", "/features/{key}", options);
    },

    /** Voir le detail d'une fonctionnalite du registre */
    getFeatureByKey9(options = {}) {
      return request("GET", "/features/{key}", options);
    },

    /** Voir le detail d'une fonctionnalite du registre */
    getFeatureByKey10(options = {}) {
      return request("GET", "/features/{key}", options);
    },

    /** Voir le detail d'une fonctionnalite du registre */
    getFeatureByKey11(options = {}) {
      return request("GET", "/features/{key}", options);
    },

    /** Statistiques du registre de fonctionnalites (admin) */
    getFeatureRegistryStatistics(options = {}) {
      return request("GET", "/features/admin/statistics", options);
    },

    /** Statistiques du registre de fonctionnalites (admin) */
    getFeatureRegistryStatistics2(options = {}) {
      return request("GET", "/features/admin/statistics", options);
    },

    /** Statistiques du registre de fonctionnalites (admin) */
    getFeatureRegistryStatistics3(options = {}) {
      return request("GET", "/features/admin/statistics", options);
    },

    /** Statistiques du registre de fonctionnalites (admin) */
    getFeatureRegistryStatistics4(options = {}) {
      return request("GET", "/features/admin/statistics", options);
    },

    /** Statistiques du registre de fonctionnalites (admin) */
    getFeatureRegistryStatistics5(options = {}) {
      return request("GET", "/features/admin/statistics", options);
    },

    /** Statistiques du registre de fonctionnalites (admin) */
    getFeatureRegistryStatistics6(options = {}) {
      return request("GET", "/features/admin/statistics", options);
    },

    /** Statistiques du registre de fonctionnalites (admin) */
    getFeatureRegistryStatistics7(options = {}) {
      return request("GET", "/features/admin/statistics", options);
    },

    /** Statistiques du registre de fonctionnalites (admin) */
    getFeatureRegistryStatistics8(options = {}) {
      return request("GET", "/features/admin/statistics", options);
    },

    /** Statistiques du registre de fonctionnalites (admin) */
    getFeatureRegistryStatistics9(options = {}) {
      return request("GET", "/features/admin/statistics", options);
    },

    /** Statistiques du registre de fonctionnalites (admin) */
    getFeatureRegistryStatistics10(options = {}) {
      return request("GET", "/features/admin/statistics", options);
    },

    /** Statistiques du registre de fonctionnalites (admin) */
    getFeatureRegistryStatistics11(options = {}) {
      return request("GET", "/features/admin/statistics", options);
    },

    /** Synchroniser manuellement le registre de fonctionnalites (admin) */
    synchronizeFeatureRegistry(options = {}) {
      return request("POST", "/features/admin/synchronize", options);
    },

    /** Synchroniser manuellement le registre de fonctionnalites (admin) */
    synchronizeFeatureRegistry2(options = {}) {
      return request("POST", "/features/admin/synchronize", options);
    },

    /** Synchroniser manuellement le registre de fonctionnalites (admin) */
    synchronizeFeatureRegistry3(options = {}) {
      return request("POST", "/features/admin/synchronize", options);
    },

    /** Synchroniser manuellement le registre de fonctionnalites (admin) */
    synchronizeFeatureRegistry4(options = {}) {
      return request("POST", "/features/admin/synchronize", options);
    },

    /** Synchroniser manuellement le registre de fonctionnalites (admin) */
    synchronizeFeatureRegistry5(options = {}) {
      return request("POST", "/features/admin/synchronize", options);
    },

    /** Synchroniser manuellement le registre de fonctionnalites (admin) */
    synchronizeFeatureRegistry6(options = {}) {
      return request("POST", "/features/admin/synchronize", options);
    },

    /** Synchroniser manuellement le registre de fonctionnalites (admin) */
    synchronizeFeatureRegistry7(options = {}) {
      return request("POST", "/features/admin/synchronize", options);
    },

    /** Synchroniser manuellement le registre de fonctionnalites (admin) */
    synchronizeFeatureRegistry8(options = {}) {
      return request("POST", "/features/admin/synchronize", options);
    },

    /** Synchroniser manuellement le registre de fonctionnalites (admin) */
    synchronizeFeatureRegistry9(options = {}) {
      return request("POST", "/features/admin/synchronize", options);
    },

    /** Synchroniser manuellement le registre de fonctionnalites (admin) */
    synchronizeFeatureRegistry10(options = {}) {
      return request("POST", "/features/admin/synchronize", options);
    },

    /** Synchroniser manuellement le registre de fonctionnalites (admin) */
    synchronizeFeatureRegistry11(options = {}) {
      return request("POST", "/features/admin/synchronize", options);
    },

    /** Lister les fonctionnalites compatibles avec une version mobile donnee */
    getCompatibleFeatures(options = {}) {
      return request("GET", "/features/compatible/{version}", options);
    },

    /** Lister les fonctionnalites compatibles avec une version mobile donnee */
    getCompatibleFeatures2(options = {}) {
      return request("GET", "/features/compatible/{version}", options);
    },

    /** Lister les fonctionnalites compatibles avec une version mobile donnee */
    getCompatibleFeatures3(options = {}) {
      return request("GET", "/features/compatible/{version}", options);
    },

    /** Lister les fonctionnalites compatibles avec une version mobile donnee */
    getCompatibleFeatures4(options = {}) {
      return request("GET", "/features/compatible/{version}", options);
    },

    /** Lister les fonctionnalites compatibles avec une version mobile donnee */
    getCompatibleFeatures5(options = {}) {
      return request("GET", "/features/compatible/{version}", options);
    },

    /** Lister les fonctionnalites compatibles avec une version mobile donnee */
    getCompatibleFeatures6(options = {}) {
      return request("GET", "/features/compatible/{version}", options);
    },

    /** Lister les fonctionnalites compatibles avec une version mobile donnee */
    getCompatibleFeatures7(options = {}) {
      return request("GET", "/features/compatible/{version}", options);
    },

    /** Lister les fonctionnalites compatibles avec une version mobile donnee */
    getCompatibleFeatures8(options = {}) {
      return request("GET", "/features/compatible/{version}", options);
    },

    /** Lister les fonctionnalites compatibles avec une version mobile donnee */
    getCompatibleFeatures9(options = {}) {
      return request("GET", "/features/compatible/{version}", options);
    },

    /** Lister les fonctionnalites compatibles avec une version mobile donnee */
    getCompatibleFeatures10(options = {}) {
      return request("GET", "/features/compatible/{version}", options);
    },

    /** Lister les fonctionnalites compatibles avec une version mobile donnee */
    getCompatibleFeatures11(options = {}) {
      return request("GET", "/features/compatible/{version}", options);
    },

    /** Obtenir le manifeste des fonctionnalites disponibles pour l'utilisateur courant */
    getFeatureManifest(options = {}) {
      return request("GET", "/features/manifest", options);
    },

    /** Obtenir le manifeste des fonctionnalites disponibles pour l'utilisateur courant */
    getFeatureManifest2(options = {}) {
      return request("GET", "/features/manifest", options);
    },

    /** Obtenir le manifeste des fonctionnalites disponibles pour l'utilisateur courant */
    getFeatureManifest3(options = {}) {
      return request("GET", "/features/manifest", options);
    },

    /** Obtenir le manifeste des fonctionnalites disponibles pour l'utilisateur courant */
    getFeatureManifest4(options = {}) {
      return request("GET", "/features/manifest", options);
    },

    /** Obtenir le manifeste des fonctionnalites disponibles pour l'utilisateur courant */
    getFeatureManifest5(options = {}) {
      return request("GET", "/features/manifest", options);
    },

    /** Obtenir le manifeste des fonctionnalites disponibles pour l'utilisateur courant */
    getFeatureManifest6(options = {}) {
      return request("GET", "/features/manifest", options);
    },

    /** Obtenir le manifeste des fonctionnalites disponibles pour l'utilisateur courant */
    getFeatureManifest7(options = {}) {
      return request("GET", "/features/manifest", options);
    },

    /** Obtenir le manifeste des fonctionnalites disponibles pour l'utilisateur courant */
    getFeatureManifest8(options = {}) {
      return request("GET", "/features/manifest", options);
    },

    /** Obtenir le manifeste des fonctionnalites disponibles pour l'utilisateur courant */
    getFeatureManifest9(options = {}) {
      return request("GET", "/features/manifest", options);
    },

    /** Obtenir le manifeste des fonctionnalites disponibles pour l'utilisateur courant */
    getFeatureManifest10(options = {}) {
      return request("GET", "/features/manifest", options);
    },

    /** Obtenir le manifeste des fonctionnalites disponibles pour l'utilisateur courant */
    getFeatureManifest11(options = {}) {
      return request("GET", "/features/manifest", options);
    },

    /** Carte temps reel des vehicules */
    getFleetLiveMap(options = {}) {
      return request("GET", "/fleet/live-map", options);
    },

    /** Carte temps reel des vehicules */
    getFleetLiveMap2(options = {}) {
      return request("GET", "/fleet/live-map", options);
    },

    /** Carte temps reel des vehicules */
    getFleetLiveMap3(options = {}) {
      return request("GET", "/fleet/live-map", options);
    },

    /** Carte temps reel des vehicules */
    getFleetLiveMap4(options = {}) {
      return request("GET", "/fleet/live-map", options);
    },

    /** Carte temps reel des vehicules */
    getFleetLiveMap5(options = {}) {
      return request("GET", "/fleet/live-map", options);
    },

    /** Carte temps reel des vehicules */
    getFleetLiveMap6(options = {}) {
      return request("GET", "/fleet/live-map", options);
    },

    /** Carte temps reel des vehicules */
    getFleetLiveMap7(options = {}) {
      return request("GET", "/fleet/live-map", options);
    },

    /** Carte temps reel des vehicules */
    getFleetLiveMap8(options = {}) {
      return request("GET", "/fleet/live-map", options);
    },

    /** Vue d'ensemble de la flotte */
    getFleetOverview(options = {}) {
      return request("GET", "/fleet/overview", options);
    },

    /** Vue d'ensemble de la flotte */
    getFleetOverview2(options = {}) {
      return request("GET", "/fleet/overview", options);
    },

    /** Vue d'ensemble de la flotte */
    getFleetOverview3(options = {}) {
      return request("GET", "/fleet/overview", options);
    },

    /** Vue d'ensemble de la flotte */
    getFleetOverview4(options = {}) {
      return request("GET", "/fleet/overview", options);
    },

    /** Vue d'ensemble de la flotte */
    getFleetOverview5(options = {}) {
      return request("GET", "/fleet/overview", options);
    },

    /** Vue d'ensemble de la flotte */
    getFleetOverview6(options = {}) {
      return request("GET", "/fleet/overview", options);
    },

    /** Vue d'ensemble de la flotte */
    getFleetOverview7(options = {}) {
      return request("GET", "/fleet/overview", options);
    },

    /** Vue d'ensemble de la flotte */
    getFleetOverview8(options = {}) {
      return request("GET", "/fleet/overview", options);
    },

    /** Rapport consommation carburant */
    getFleetReportsFuel(options = {}) {
      return request("GET", "/fleet/reports/fuel", options);
    },

    /** Rapport consommation carburant */
    getFleetReportsFuel2(options = {}) {
      return request("GET", "/fleet/reports/fuel", options);
    },

    /** Rapport consommation carburant */
    getFleetReportsFuel3(options = {}) {
      return request("GET", "/fleet/reports/fuel", options);
    },

    /** Rapport consommation carburant */
    getFleetReportsFuel4(options = {}) {
      return request("GET", "/fleet/reports/fuel", options);
    },

    /** Rapport consommation carburant */
    getFleetReportsFuel5(options = {}) {
      return request("GET", "/fleet/reports/fuel", options);
    },

    /** Rapport consommation carburant */
    getFleetReportsFuel6(options = {}) {
      return request("GET", "/fleet/reports/fuel", options);
    },

    /** Rapport consommation carburant */
    getFleetReportsFuel7(options = {}) {
      return request("GET", "/fleet/reports/fuel", options);
    },

    /** Rapport consommation carburant */
    getFleetReportsFuel8(options = {}) {
      return request("GET", "/fleet/reports/fuel", options);
    },

    /** Vehicules necessitant une maintenance */
    getFleetReportsMaintenanceDue(options = {}) {
      return request("GET", "/fleet/reports/maintenance-due", options);
    },

    /** Vehicules necessitant une maintenance */
    getFleetReportsMaintenanceDue2(options = {}) {
      return request("GET", "/fleet/reports/maintenance-due", options);
    },

    /** Vehicules necessitant une maintenance */
    getFleetReportsMaintenanceDue3(options = {}) {
      return request("GET", "/fleet/reports/maintenance-due", options);
    },

    /** Vehicules necessitant une maintenance */
    getFleetReportsMaintenanceDue4(options = {}) {
      return request("GET", "/fleet/reports/maintenance-due", options);
    },

    /** Vehicules necessitant une maintenance */
    getFleetReportsMaintenanceDue5(options = {}) {
      return request("GET", "/fleet/reports/maintenance-due", options);
    },

    /** Vehicules necessitant une maintenance */
    getFleetReportsMaintenanceDue6(options = {}) {
      return request("GET", "/fleet/reports/maintenance-due", options);
    },

    /** Vehicules necessitant une maintenance */
    getFleetReportsMaintenanceDue7(options = {}) {
      return request("GET", "/fleet/reports/maintenance-due", options);
    },

    /** Vehicules necessitant une maintenance */
    getFleetReportsMaintenanceDue8(options = {}) {
      return request("GET", "/fleet/reports/maintenance-due", options);
    },

    /** Rapport kilometrage */
    getFleetReportsMileage(options = {}) {
      return request("GET", "/fleet/reports/mileage", options);
    },

    /** Rapport kilometrage */
    getFleetReportsMileage2(options = {}) {
      return request("GET", "/fleet/reports/mileage", options);
    },

    /** Rapport kilometrage */
    getFleetReportsMileage3(options = {}) {
      return request("GET", "/fleet/reports/mileage", options);
    },

    /** Rapport kilometrage */
    getFleetReportsMileage4(options = {}) {
      return request("GET", "/fleet/reports/mileage", options);
    },

    /** Rapport kilometrage */
    getFleetReportsMileage5(options = {}) {
      return request("GET", "/fleet/reports/mileage", options);
    },

    /** Rapport kilometrage */
    getFleetReportsMileage6(options = {}) {
      return request("GET", "/fleet/reports/mileage", options);
    },

    /** Rapport kilometrage */
    getFleetReportsMileage7(options = {}) {
      return request("GET", "/fleet/reports/mileage", options);
    },

    /** Rapport kilometrage */
    getFleetReportsMileage8(options = {}) {
      return request("GET", "/fleet/reports/mileage", options);
    },

    /** Candidature partenaire */
    postGrowthPartnerApply(options = {}) {
      return request("POST", "/growth/partner/apply", options);
    },

    /** Candidature partenaire */
    postGrowthPartnerApply2(options = {}) {
      return request("POST", "/growth/partner/apply", options);
    },

    /** Candidature partenaire */
    postGrowthPartnerApply3(options = {}) {
      return request("POST", "/growth/partner/apply", options);
    },

    /** Candidature partenaire */
    postGrowthPartnerApply4(options = {}) {
      return request("POST", "/growth/partner/apply", options);
    },

    /** Candidature partenaire */
    postGrowthPartnerApply5(options = {}) {
      return request("POST", "/growth/partner/apply", options);
    },

    /** Candidature partenaire */
    postGrowthPartnerApply6(options = {}) {
      return request("POST", "/growth/partner/apply", options);
    },

    /** Candidature partenaire */
    postGrowthPartnerApply7(options = {}) {
      return request("POST", "/growth/partner/apply", options);
    },

    /** Candidature partenaire */
    postGrowthPartnerApply8(options = {}) {
      return request("POST", "/growth/partner/apply", options);
    },

    /** Candidature partenaire */
    postGrowthPartnerApply9(options = {}) {
      return request("POST", "/growth/partner/apply", options);
    },

    /** Candidature partenaire */
    postGrowthPartnerApply10(options = {}) {
      return request("POST", "/growth/partner/apply", options);
    },

    /** Candidature partenaire */
    postGrowthPartnerApply11(options = {}) {
      return request("POST", "/growth/partner/apply", options);
    },

    /** Entreprises parrainées par le partenaire */
    getGrowthPartnerCompanies(options = {}) {
      return request("GET", "/growth/partner/companies", options);
    },

    /** Entreprises parrainées par le partenaire */
    getGrowthPartnerCompanies2(options = {}) {
      return request("GET", "/growth/partner/companies", options);
    },

    /** Entreprises parrainées par le partenaire */
    getGrowthPartnerCompanies3(options = {}) {
      return request("GET", "/growth/partner/companies", options);
    },

    /** Entreprises parrainées par le partenaire */
    getGrowthPartnerCompanies4(options = {}) {
      return request("GET", "/growth/partner/companies", options);
    },

    /** Entreprises parrainées par le partenaire */
    getGrowthPartnerCompanies5(options = {}) {
      return request("GET", "/growth/partner/companies", options);
    },

    /** Entreprises parrainées par le partenaire */
    getGrowthPartnerCompanies6(options = {}) {
      return request("GET", "/growth/partner/companies", options);
    },

    /** Entreprises parrainées par le partenaire */
    getGrowthPartnerCompanies7(options = {}) {
      return request("GET", "/growth/partner/companies", options);
    },

    /** Entreprises parrainées par le partenaire */
    getGrowthPartnerCompanies8(options = {}) {
      return request("GET", "/growth/partner/companies", options);
    },

    /** Entreprises parrainées par le partenaire */
    getGrowthPartnerCompanies9(options = {}) {
      return request("GET", "/growth/partner/companies", options);
    },

    /** Entreprises parrainées par le partenaire */
    getGrowthPartnerCompanies10(options = {}) {
      return request("GET", "/growth/partner/companies", options);
    },

    /** Entreprises parrainées par le partenaire */
    getGrowthPartnerCompanies11(options = {}) {
      return request("GET", "/growth/partner/companies", options);
    },

    /** Tableau de bord de l'espace partenaire */
    getGrowthPartnerDashboard(options = {}) {
      return request("GET", "/growth/partner/dashboard", options);
    },

    /** Tableau de bord de l'espace partenaire */
    getGrowthPartnerDashboard2(options = {}) {
      return request("GET", "/growth/partner/dashboard", options);
    },

    /** Tableau de bord de l'espace partenaire */
    getGrowthPartnerDashboard3(options = {}) {
      return request("GET", "/growth/partner/dashboard", options);
    },

    /** Tableau de bord de l'espace partenaire */
    getGrowthPartnerDashboard4(options = {}) {
      return request("GET", "/growth/partner/dashboard", options);
    },

    /** Tableau de bord de l'espace partenaire */
    getGrowthPartnerDashboard5(options = {}) {
      return request("GET", "/growth/partner/dashboard", options);
    },

    /** Tableau de bord de l'espace partenaire */
    getGrowthPartnerDashboard6(options = {}) {
      return request("GET", "/growth/partner/dashboard", options);
    },

    /** Tableau de bord de l'espace partenaire */
    getGrowthPartnerDashboard7(options = {}) {
      return request("GET", "/growth/partner/dashboard", options);
    },

    /** Tableau de bord de l'espace partenaire */
    getGrowthPartnerDashboard8(options = {}) {
      return request("GET", "/growth/partner/dashboard", options);
    },

    /** Tableau de bord de l'espace partenaire */
    getGrowthPartnerDashboard9(options = {}) {
      return request("GET", "/growth/partner/dashboard", options);
    },

    /** Tableau de bord de l'espace partenaire */
    getGrowthPartnerDashboard10(options = {}) {
      return request("GET", "/growth/partner/dashboard", options);
    },

    /** Tableau de bord de l'espace partenaire */
    getGrowthPartnerDashboard11(options = {}) {
      return request("GET", "/growth/partner/dashboard", options);
    },

    /** Demande de versement commission */
    postGrowthPartnerPayout(options = {}) {
      return request("POST", "/growth/partner/payout", options);
    },

    /** Demande de versement commission */
    postGrowthPartnerPayout2(options = {}) {
      return request("POST", "/growth/partner/payout", options);
    },

    /** Demande de versement commission */
    postGrowthPartnerPayout3(options = {}) {
      return request("POST", "/growth/partner/payout", options);
    },

    /** Demande de versement commission */
    postGrowthPartnerPayout4(options = {}) {
      return request("POST", "/growth/partner/payout", options);
    },

    /** Demande de versement commission */
    postGrowthPartnerPayout5(options = {}) {
      return request("POST", "/growth/partner/payout", options);
    },

    /** Demande de versement commission */
    postGrowthPartnerPayout6(options = {}) {
      return request("POST", "/growth/partner/payout", options);
    },

    /** Demande de versement commission */
    postGrowthPartnerPayout7(options = {}) {
      return request("POST", "/growth/partner/payout", options);
    },

    /** Demande de versement commission */
    postGrowthPartnerPayout8(options = {}) {
      return request("POST", "/growth/partner/payout", options);
    },

    /** Demande de versement commission */
    postGrowthPartnerPayout9(options = {}) {
      return request("POST", "/growth/partner/payout", options);
    },

    /** Demande de versement commission */
    postGrowthPartnerPayout10(options = {}) {
      return request("POST", "/growth/partner/payout", options);
    },

    /** Demande de versement commission */
    postGrowthPartnerPayout11(options = {}) {
      return request("POST", "/growth/partner/payout", options);
    },

    /** Statistiques partenaire connecté */
    getGrowthPartnerStats(options = {}) {
      return request("GET", "/growth/partner/stats", options);
    },

    /** Statistiques partenaire connecté */
    getGrowthPartnerStats2(options = {}) {
      return request("GET", "/growth/partner/stats", options);
    },

    /** Statistiques partenaire connecté */
    getGrowthPartnerStats3(options = {}) {
      return request("GET", "/growth/partner/stats", options);
    },

    /** Statistiques partenaire connecté */
    getGrowthPartnerStats4(options = {}) {
      return request("GET", "/growth/partner/stats", options);
    },

    /** Statistiques partenaire connecté */
    getGrowthPartnerStats5(options = {}) {
      return request("GET", "/growth/partner/stats", options);
    },

    /** Statistiques partenaire connecté */
    getGrowthPartnerStats6(options = {}) {
      return request("GET", "/growth/partner/stats", options);
    },

    /** Statistiques partenaire connecté */
    getGrowthPartnerStats7(options = {}) {
      return request("GET", "/growth/partner/stats", options);
    },

    /** Statistiques partenaire connecté */
    getGrowthPartnerStats8(options = {}) {
      return request("GET", "/growth/partner/stats", options);
    },

    /** Statistiques partenaire connecté */
    getGrowthPartnerStats9(options = {}) {
      return request("GET", "/growth/partner/stats", options);
    },

    /** Statistiques partenaire connecté */
    getGrowthPartnerStats10(options = {}) {
      return request("GET", "/growth/partner/stats", options);
    },

    /** Statistiques partenaire connecté */
    getGrowthPartnerStats11(options = {}) {
      return request("GET", "/growth/partner/stats", options);
    },

    /** Healthcheck live + ready */
    getHealth(options = {}) {
      return request("GET", "/health", options);
    },

    /** Healthcheck live + ready */
    getHealth2(options = {}) {
      return request("GET", "/health", options);
    },

    /** Healthcheck live + ready */
    getHealth3(options = {}) {
      return request("GET", "/health", options);
    },

    /** Healthcheck live + ready */
    getHealth4(options = {}) {
      return request("GET", "/health", options);
    },

    /** Healthcheck live + ready */
    getHealth5(options = {}) {
      return request("GET", "/health", options);
    },

    /** Liveness probe — processus API vivant (sans dépendances) */
    getHealthLive(options = {}) {
      return request("GET", "/health/live", options);
    },

    /** Liveness probe — processus API vivant (sans dépendances) */
    getHealthLive2(options = {}) {
      return request("GET", "/health/live", options);
    },

    /** Liveness probe — processus API vivant (sans dépendances) */
    getHealthLive3(options = {}) {
      return request("GET", "/health/live", options);
    },

    /** Liveness probe — processus API vivant (sans dépendances) */
    getHealthLive4(options = {}) {
      return request("GET", "/health/live", options);
    },

    /** Liveness probe — processus API vivant (sans dépendances) */
    getHealthLive5(options = {}) {
      return request("GET", "/health/live", options);
    },

    /** Readiness probe — base de données disponible */
    getHealthReady(options = {}) {
      return request("GET", "/health/ready", options);
    },

    /** Readiness probe — base de données disponible */
    getHealthReady2(options = {}) {
      return request("GET", "/health/ready", options);
    },

    /** Readiness probe — base de données disponible */
    getHealthReady3(options = {}) {
      return request("GET", "/health/ready", options);
    },

    /** Readiness probe — base de données disponible */
    getHealthReady4(options = {}) {
      return request("GET", "/health/ready", options);
    },

    /** Readiness probe — base de données disponible */
    getHealthReady5(options = {}) {
      return request("GET", "/health/ready", options);
    },

    /** Dashboard RH — indicateurs et actions en attente */
    getHrDashboard(options = {}) {
      return request("GET", "/hr/dashboard", options);
    },

    /** Dashboard RH — indicateurs et actions en attente */
    getHrDashboard2(options = {}) {
      return request("GET", "/hr/dashboard", options);
    },

    /** Dashboard RH — indicateurs et actions en attente */
    getHrDashboard3(options = {}) {
      return request("GET", "/hr/dashboard", options);
    },

    /** Dashboard RH — indicateurs et actions en attente */
    getHrDashboard4(options = {}) {
      return request("GET", "/hr/dashboard", options);
    },

    /** Dashboard RH — indicateurs et actions en attente */
    getHrDashboard5(options = {}) {
      return request("GET", "/hr/dashboard", options);
    },

    /** Liste des employés (scope RH) */
    getHrEmployees(options = {}) {
      return request("GET", "/hr/employees", options);
    },

    /** Liste des employés (scope RH) */
    getHrEmployees2(options = {}) {
      return request("GET", "/hr/employees", options);
    },

    /** Liste des employés (scope RH) */
    getHrEmployees3(options = {}) {
      return request("GET", "/hr/employees", options);
    },

    /** Liste des employés (scope RH) */
    getHrEmployees4(options = {}) {
      return request("GET", "/hr/employees", options);
    },

    /** Liste des employés (scope RH) */
    getHrEmployees5(options = {}) {
      return request("GET", "/hr/employees", options);
    },

    /** Ajoute un employé (scope RH — sans changement de rôle manager) */
    postHrEmployees(options = {}) {
      return request("POST", "/hr/employees", options);
    },

    /** Ajoute un employé (scope RH — sans changement de rôle manager) */
    postHrEmployees2(options = {}) {
      return request("POST", "/hr/employees", options);
    },

    /** Ajoute un employé (scope RH — sans changement de rôle manager) */
    postHrEmployees3(options = {}) {
      return request("POST", "/hr/employees", options);
    },

    /** Ajoute un employé (scope RH — sans changement de rôle manager) */
    postHrEmployees4(options = {}) {
      return request("POST", "/hr/employees", options);
    },

    /** Ajoute un employé (scope RH — sans changement de rôle manager) */
    postHrEmployees5(options = {}) {
      return request("POST", "/hr/employees", options);
    },

    /** Détail d'un employé (scope RH) */
    getHrEmployeesByEmployee(options = {}) {
      return request("GET", "/hr/employees/{employee}", options);
    },

    /** Détail d'un employé (scope RH) */
    getHrEmployeesByEmployee2(options = {}) {
      return request("GET", "/hr/employees/{employee}", options);
    },

    /** Détail d'un employé (scope RH) */
    getHrEmployeesByEmployee3(options = {}) {
      return request("GET", "/hr/employees/{employee}", options);
    },

    /** Détail d'un employé (scope RH) */
    getHrEmployeesByEmployee4(options = {}) {
      return request("GET", "/hr/employees/{employee}", options);
    },

    /** Détail d'un employé (scope RH) */
    getHrEmployeesByEmployee5(options = {}) {
      return request("GET", "/hr/employees/{employee}", options);
    },

    /** Modifie un employé (scope RH — sans changement de rôle manager) */
    patchHrEmployeesByEmployee(options = {}) {
      return request("PATCH", "/hr/employees/{employee}", options);
    },

    /** Modifie un employé (scope RH — sans changement de rôle manager) */
    patchHrEmployeesByEmployee2(options = {}) {
      return request("PATCH", "/hr/employees/{employee}", options);
    },

    /** Modifie un employé (scope RH — sans changement de rôle manager) */
    patchHrEmployeesByEmployee3(options = {}) {
      return request("PATCH", "/hr/employees/{employee}", options);
    },

    /** Modifie un employé (scope RH — sans changement de rôle manager) */
    patchHrEmployeesByEmployee4(options = {}) {
      return request("PATCH", "/hr/employees/{employee}", options);
    },

    /** Modifie un employé (scope RH — sans changement de rôle manager) */
    patchHrEmployeesByEmployee5(options = {}) {
      return request("PATCH", "/hr/employees/{employee}", options);
    },

    /** Profil de l'utilisateur RH connecté (manager_role rh/principal) */
    getHrMe(options = {}) {
      return request("GET", "/hr/me", options);
    },

    /** Profil de l'utilisateur RH connecté (manager_role rh/principal) */
    getHrMe2(options = {}) {
      return request("GET", "/hr/me", options);
    },

    /** Profil de l'utilisateur RH connecté (manager_role rh/principal) */
    getHrMe3(options = {}) {
      return request("GET", "/hr/me", options);
    },

    /** Profil de l'utilisateur RH connecté (manager_role rh/principal) */
    getHrMe4(options = {}) {
      return request("GET", "/hr/me", options);
    },

    /** Profil de l'utilisateur RH connecté (manager_role rh/principal) */
    getHrMe5(options = {}) {
      return request("GET", "/hr/me", options);
    },

    /** Vue d'ensemble de l'équipe (effectifs, répartition) */
    getHrTeamOverview(options = {}) {
      return request("GET", "/hr/team-overview", options);
    },

    /** Vue d'ensemble de l'équipe (effectifs, répartition) */
    getHrTeamOverview2(options = {}) {
      return request("GET", "/hr/team-overview", options);
    },

    /** Vue d'ensemble de l'équipe (effectifs, répartition) */
    getHrTeamOverview3(options = {}) {
      return request("GET", "/hr/team-overview", options);
    },

    /** Vue d'ensemble de l'équipe (effectifs, répartition) */
    getHrTeamOverview4(options = {}) {
      return request("GET", "/hr/team-overview", options);
    },

    /** Vue d'ensemble de l'équipe (effectifs, répartition) */
    getHrTeamOverview5(options = {}) {
      return request("GET", "/hr/team-overview", options);
    },

    /** Catalogue de traductions complet */
    getI18nCatalog(options = {}) {
      return request("GET", "/i18n/catalog", options);
    },

    /** Catalogue de traductions complet */
    getI18nCatalog2(options = {}) {
      return request("GET", "/i18n/catalog", options);
    },

    /** Catalogue de traductions complet */
    getI18nCatalog3(options = {}) {
      return request("GET", "/i18n/catalog", options);
    },

    /** Catalogue de traductions complet */
    getI18nCatalog4(options = {}) {
      return request("GET", "/i18n/catalog", options);
    },

    /** Catalogue de traductions complet */
    getI18nCatalog5(options = {}) {
      return request("GET", "/i18n/catalog", options);
    },

    /** Catalogue de traductions complet */
    getI18nCatalog6(options = {}) {
      return request("GET", "/i18n/catalog", options);
    },

    /** Catalogue de traductions complet */
    getI18nCatalog7(options = {}) {
      return request("GET", "/i18n/catalog", options);
    },

    /** Catalogue de traductions complet */
    getI18nCatalog8(options = {}) {
      return request("GET", "/i18n/catalog", options);
    },

    /** Catalogue de traductions complet */
    getI18nCatalog9(options = {}) {
      return request("GET", "/i18n/catalog", options);
    },

    /** Catalogue de traductions complet */
    getI18nCatalog10(options = {}) {
      return request("GET", "/i18n/catalog", options);
    },

    /** Catalogue de traductions complet */
    getI18nCatalog11(options = {}) {
      return request("GET", "/i18n/catalog", options);
    },

    /** Traductions pour une locale */
    getI18nCatalogByLocale(options = {}) {
      return request("GET", "/i18n/catalog/{locale}", options);
    },

    /** Traductions pour une locale */
    getI18nCatalogByLocale2(options = {}) {
      return request("GET", "/i18n/catalog/{locale}", options);
    },

    /** Traductions pour une locale */
    getI18nCatalogByLocale3(options = {}) {
      return request("GET", "/i18n/catalog/{locale}", options);
    },

    /** Traductions pour une locale */
    getI18nCatalogByLocale4(options = {}) {
      return request("GET", "/i18n/catalog/{locale}", options);
    },

    /** Traductions pour une locale */
    getI18nCatalogByLocale5(options = {}) {
      return request("GET", "/i18n/catalog/{locale}", options);
    },

    /** Traductions pour une locale */
    getI18nCatalogByLocale6(options = {}) {
      return request("GET", "/i18n/catalog/{locale}", options);
    },

    /** Traductions pour une locale */
    getI18nCatalogByLocale7(options = {}) {
      return request("GET", "/i18n/catalog/{locale}", options);
    },

    /** Traductions pour une locale */
    getI18nCatalogByLocale8(options = {}) {
      return request("GET", "/i18n/catalog/{locale}", options);
    },

    /** Traductions pour une locale */
    getI18nCatalogByLocale9(options = {}) {
      return request("GET", "/i18n/catalog/{locale}", options);
    },

    /** Traductions pour une locale */
    getI18nCatalogByLocale10(options = {}) {
      return request("GET", "/i18n/catalog/{locale}", options);
    },

    /** Traductions pour une locale */
    getI18nCatalogByLocale11(options = {}) {
      return request("GET", "/i18n/catalog/{locale}", options);
    },

    /** Verification interne MediaMTX */
    getInternalCameraTokenVerify(options = {}) {
      return request("GET", "/internal/camera-token/verify", options);
    },

    /** Verification interne MediaMTX */
    getInternalCameraTokenVerify2(options = {}) {
      return request("GET", "/internal/camera-token/verify", options);
    },

    /** Verification interne MediaMTX */
    getInternalCameraTokenVerify3(options = {}) {
      return request("GET", "/internal/camera-token/verify", options);
    },

    /** Verification interne MediaMTX */
    getInternalCameraTokenVerify4(options = {}) {
      return request("GET", "/internal/camera-token/verify", options);
    },

    /** Verification interne MediaMTX */
    getInternalCameraTokenVerify5(options = {}) {
      return request("GET", "/internal/camera-token/verify", options);
    },

    /** Verification interne MediaMTX */
    getInternalCameraTokenVerify6(options = {}) {
      return request("GET", "/internal/camera-token/verify", options);
    },

    /** Verification interne MediaMTX */
    getInternalCameraTokenVerify7(options = {}) {
      return request("GET", "/internal/camera-token/verify", options);
    },

    /** Verification interne MediaMTX */
    getInternalCameraTokenVerify8(options = {}) {
      return request("GET", "/internal/camera-token/verify", options);
    },

    /** Lister les invitations de l'entreprise */
    getInvitations(options = {}) {
      return request("GET", "/invitations", options);
    },

    /** Lister les invitations de l'entreprise */
    getInvitations2(options = {}) {
      return request("GET", "/invitations", options);
    },

    /** Lister les invitations de l'entreprise */
    getInvitations3(options = {}) {
      return request("GET", "/invitations", options);
    },

    /** Lister les invitations de l'entreprise */
    getInvitations4(options = {}) {
      return request("GET", "/invitations", options);
    },

    /** Lister les invitations de l'entreprise */
    getInvitations5(options = {}) {
      return request("GET", "/invitations", options);
    },

    /** Lister les invitations de l'entreprise */
    getInvitations6(options = {}) {
      return request("GET", "/invitations", options);
    },

    /** Lister les invitations de l'entreprise */
    getInvitations7(options = {}) {
      return request("GET", "/invitations", options);
    },

    /** Lister les invitations de l'entreprise */
    getInvitations8(options = {}) {
      return request("GET", "/invitations", options);
    },

    /** Renvoyer une invitation */
    postInvitationsByInvitationResend(options = {}) {
      return request("POST", "/invitations/{invitation}/resend", options);
    },

    /** Renvoyer une invitation */
    postInvitationsByInvitationResend2(options = {}) {
      return request("POST", "/invitations/{invitation}/resend", options);
    },

    /** Renvoyer une invitation */
    postInvitationsByInvitationResend3(options = {}) {
      return request("POST", "/invitations/{invitation}/resend", options);
    },

    /** Renvoyer une invitation */
    postInvitationsByInvitationResend4(options = {}) {
      return request("POST", "/invitations/{invitation}/resend", options);
    },

    /** Renvoyer une invitation */
    postInvitationsByInvitationResend5(options = {}) {
      return request("POST", "/invitations/{invitation}/resend", options);
    },

    /** Renvoyer une invitation */
    postInvitationsByInvitationResend6(options = {}) {
      return request("POST", "/invitations/{invitation}/resend", options);
    },

    /** Renvoyer une invitation */
    postInvitationsByInvitationResend7(options = {}) {
      return request("POST", "/invitations/{invitation}/resend", options);
    },

    /** Renvoyer une invitation */
    postInvitationsByInvitationResend8(options = {}) {
      return request("POST", "/invitations/{invitation}/resend", options);
    },

    /** Enregistrer un kiosk */
    postKiosks(options = {}) {
      return request("POST", "/kiosks", options);
    },

    /** Enregistrer un kiosk */
    postKiosks2(options = {}) {
      return request("POST", "/kiosks", options);
    },

    /** Enregistrer un kiosk */
    postKiosks3(options = {}) {
      return request("POST", "/kiosks", options);
    },

    /** Enregistrer un kiosk */
    postKiosks4(options = {}) {
      return request("POST", "/kiosks", options);
    },

    /** Enregistrer un kiosk */
    postKiosks5(options = {}) {
      return request("POST", "/kiosks", options);
    },

    /** Enregistrer un kiosk */
    postKiosks6(options = {}) {
      return request("POST", "/kiosks", options);
    },

    /** Enregistrer un kiosk */
    postKiosks7(options = {}) {
      return request("POST", "/kiosks", options);
    },

    /** Enregistrer un kiosk */
    postKiosks8(options = {}) {
      return request("POST", "/kiosks", options);
    },

    /** Annonces du kiosk (device token) */
    getKiosksByDeviceCodeAnnouncements(options = {}) {
      return request("GET", "/kiosks/{deviceCode}/announcements", options);
    },

    /** Annonces du kiosk (device token) */
    getKiosksByDeviceCodeAnnouncements2(options = {}) {
      return request("GET", "/kiosks/{deviceCode}/announcements", options);
    },

    /** Annonces du kiosk (device token) */
    getKiosksByDeviceCodeAnnouncements3(options = {}) {
      return request("GET", "/kiosks/{deviceCode}/announcements", options);
    },

    /** Annonces du kiosk (device token) */
    getKiosksByDeviceCodeAnnouncements4(options = {}) {
      return request("GET", "/kiosks/{deviceCode}/announcements", options);
    },

    /** Annonces du kiosk (device token) */
    getKiosksByDeviceCodeAnnouncements5(options = {}) {
      return request("GET", "/kiosks/{deviceCode}/announcements", options);
    },

    /** Annonces du kiosk (device token) */
    getKiosksByDeviceCodeAnnouncements6(options = {}) {
      return request("GET", "/kiosks/{deviceCode}/announcements", options);
    },

    /** Annonces du kiosk (device token) */
    getKiosksByDeviceCodeAnnouncements7(options = {}) {
      return request("GET", "/kiosks/{deviceCode}/announcements", options);
    },

    /** Annonces du kiosk (device token) */
    getKiosksByDeviceCodeAnnouncements8(options = {}) {
      return request("GET", "/kiosks/{deviceCode}/announcements", options);
    },

    /** Infos employe pour ecran kiosk (device token) */
    postKiosksByDeviceCodeEmployeeInfo(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/employee-info", options);
    },

    /** Infos employe pour ecran kiosk (device token) */
    postKiosksByDeviceCodeEmployeeInfo2(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/employee-info", options);
    },

    /** Infos employe pour ecran kiosk (device token) */
    postKiosksByDeviceCodeEmployeeInfo3(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/employee-info", options);
    },

    /** Infos employe pour ecran kiosk (device token) */
    postKiosksByDeviceCodeEmployeeInfo4(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/employee-info", options);
    },

    /** Infos employe pour ecran kiosk (device token) */
    postKiosksByDeviceCodeEmployeeInfo5(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/employee-info", options);
    },

    /** Infos employe pour ecran kiosk (device token) */
    postKiosksByDeviceCodeEmployeeInfo6(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/employee-info", options);
    },

    /** Infos employe pour ecran kiosk (device token) */
    postKiosksByDeviceCodeEmployeeInfo7(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/employee-info", options);
    },

    /** Infos employe pour ecran kiosk (device token) */
    postKiosksByDeviceCodeEmployeeInfo8(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/employee-info", options);
    },

    /** Solde conges employe pour kiosk (device token) */
    postKiosksByDeviceCodeLeaveBalance(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/leave-balance", options);
    },

    /** Solde conges employe pour kiosk (device token) */
    postKiosksByDeviceCodeLeaveBalance2(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/leave-balance", options);
    },

    /** Solde conges employe pour kiosk (device token) */
    postKiosksByDeviceCodeLeaveBalance3(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/leave-balance", options);
    },

    /** Solde conges employe pour kiosk (device token) */
    postKiosksByDeviceCodeLeaveBalance4(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/leave-balance", options);
    },

    /** Solde conges employe pour kiosk (device token) */
    postKiosksByDeviceCodeLeaveBalance5(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/leave-balance", options);
    },

    /** Solde conges employe pour kiosk (device token) */
    postKiosksByDeviceCodeLeaveBalance6(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/leave-balance", options);
    },

    /** Solde conges employe pour kiosk (device token) */
    postKiosksByDeviceCodeLeaveBalance7(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/leave-balance", options);
    },

    /** Solde conges employe pour kiosk (device token) */
    postKiosksByDeviceCodeLeaveBalance8(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/leave-balance", options);
    },

    /** Pointage via kiosk */
    postKiosksByDeviceCodePunch(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/punch", options);
    },

    /** Pointage via kiosk */
    postKiosksByDeviceCodePunch2(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/punch", options);
    },

    /** Pointage via kiosk */
    postKiosksByDeviceCodePunch3(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/punch", options);
    },

    /** Pointage via kiosk */
    postKiosksByDeviceCodePunch4(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/punch", options);
    },

    /** Pointage via kiosk */
    postKiosksByDeviceCodePunch5(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/punch", options);
    },

    /** Pointage via kiosk */
    postKiosksByDeviceCodePunch6(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/punch", options);
    },

    /** Pointage via kiosk */
    postKiosksByDeviceCodePunch7(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/punch", options);
    },

    /** Pointage via kiosk */
    postKiosksByDeviceCodePunch8(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/punch", options);
    },

    /** Pointage QR via kiosk (device token) */
    postKiosksByDeviceCodeQrPunch(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/qr-punch", options);
    },

    /** Pointage QR via kiosk (device token) */
    postKiosksByDeviceCodeQrPunch2(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/qr-punch", options);
    },

    /** Pointage QR via kiosk (device token) */
    postKiosksByDeviceCodeQrPunch3(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/qr-punch", options);
    },

    /** Pointage QR via kiosk (device token) */
    postKiosksByDeviceCodeQrPunch4(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/qr-punch", options);
    },

    /** Pointage QR via kiosk (device token) */
    postKiosksByDeviceCodeQrPunch5(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/qr-punch", options);
    },

    /** Pointage QR via kiosk (device token) */
    postKiosksByDeviceCodeQrPunch6(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/qr-punch", options);
    },

    /** Pointage QR via kiosk (device token) */
    postKiosksByDeviceCodeQrPunch7(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/qr-punch", options);
    },

    /** Pointage QR via kiosk (device token) */
    postKiosksByDeviceCodeQrPunch8(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/qr-punch", options);
    },

    /** Roster biometrie d'un kiosk */
    getKiosksByDeviceCodeRoster(options = {}) {
      return request("GET", "/kiosks/{deviceCode}/roster", options);
    },

    /** Roster biometrie d'un kiosk */
    getKiosksByDeviceCodeRoster2(options = {}) {
      return request("GET", "/kiosks/{deviceCode}/roster", options);
    },

    /** Roster biometrie d'un kiosk */
    getKiosksByDeviceCodeRoster3(options = {}) {
      return request("GET", "/kiosks/{deviceCode}/roster", options);
    },

    /** Roster biometrie d'un kiosk */
    getKiosksByDeviceCodeRoster4(options = {}) {
      return request("GET", "/kiosks/{deviceCode}/roster", options);
    },

    /** Roster biometrie d'un kiosk */
    getKiosksByDeviceCodeRoster5(options = {}) {
      return request("GET", "/kiosks/{deviceCode}/roster", options);
    },

    /** Roster biometrie d'un kiosk */
    getKiosksByDeviceCodeRoster6(options = {}) {
      return request("GET", "/kiosks/{deviceCode}/roster", options);
    },

    /** Roster biometrie d'un kiosk */
    getKiosksByDeviceCodeRoster7(options = {}) {
      return request("GET", "/kiosks/{deviceCode}/roster", options);
    },

    /** Roster biometrie d'un kiosk */
    getKiosksByDeviceCodeRoster8(options = {}) {
      return request("GET", "/kiosks/{deviceCode}/roster", options);
    },

    /** Synchroniser des evenements offline */
    postKiosksByDeviceCodeSync(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/sync", options);
    },

    /** Synchroniser des evenements offline */
    postKiosksByDeviceCodeSync2(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/sync", options);
    },

    /** Synchroniser des evenements offline */
    postKiosksByDeviceCodeSync3(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/sync", options);
    },

    /** Synchroniser des evenements offline */
    postKiosksByDeviceCodeSync4(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/sync", options);
    },

    /** Synchroniser des evenements offline */
    postKiosksByDeviceCodeSync5(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/sync", options);
    },

    /** Synchroniser des evenements offline */
    postKiosksByDeviceCodeSync6(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/sync", options);
    },

    /** Synchroniser des evenements offline */
    postKiosksByDeviceCodeSync7(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/sync", options);
    },

    /** Synchroniser des evenements offline */
    postKiosksByDeviceCodeSync8(options = {}) {
      return request("POST", "/kiosks/{deviceCode}/sync", options);
    },

    /** Lire le score de readiness lancement du tenant */
    getLaunchReadiness(options = {}) {
      return request("GET", "/launch-readiness", options);
    },

    /** Lire le score de readiness lancement du tenant */
    getLaunchReadiness2(options = {}) {
      return request("GET", "/launch-readiness", options);
    },

    /** Lire le score de readiness lancement du tenant */
    getLaunchReadiness3(options = {}) {
      return request("GET", "/launch-readiness", options);
    },

    /** Lire le score de readiness lancement du tenant */
    getLaunchReadiness4(options = {}) {
      return request("GET", "/launch-readiness", options);
    },

    /** Lire le score de readiness lancement du tenant */
    getLaunchReadiness5(options = {}) {
      return request("GET", "/launch-readiness", options);
    },

    /** Lister les accruals */
    getLeaveAccruals(options = {}) {
      return request("GET", "/leave-accruals", options);
    },

    /** Lister les accruals */
    getLeaveAccruals2(options = {}) {
      return request("GET", "/leave-accruals", options);
    },

    /** Lister les accruals */
    getLeaveAccruals3(options = {}) {
      return request("GET", "/leave-accruals", options);
    },

    /** Lister les accruals */
    getLeaveAccruals4(options = {}) {
      return request("GET", "/leave-accruals", options);
    },

    /** Lister les accruals */
    getLeaveAccruals5(options = {}) {
      return request("GET", "/leave-accruals", options);
    },

    /** Lister les accruals */
    getLeaveAccruals6(options = {}) {
      return request("GET", "/leave-accruals", options);
    },

    /** Lister les accruals */
    getLeaveAccruals7(options = {}) {
      return request("GET", "/leave-accruals", options);
    },

    /** Lister les accruals */
    getLeaveAccruals8(options = {}) {
      return request("GET", "/leave-accruals", options);
    },

    /** Creer un accrual */
    postLeaveAccruals(options = {}) {
      return request("POST", "/leave-accruals", options);
    },

    /** Creer un accrual */
    postLeaveAccruals2(options = {}) {
      return request("POST", "/leave-accruals", options);
    },

    /** Creer un accrual */
    postLeaveAccruals3(options = {}) {
      return request("POST", "/leave-accruals", options);
    },

    /** Creer un accrual */
    postLeaveAccruals4(options = {}) {
      return request("POST", "/leave-accruals", options);
    },

    /** Creer un accrual */
    postLeaveAccruals5(options = {}) {
      return request("POST", "/leave-accruals", options);
    },

    /** Creer un accrual */
    postLeaveAccruals6(options = {}) {
      return request("POST", "/leave-accruals", options);
    },

    /** Creer un accrual */
    postLeaveAccruals7(options = {}) {
      return request("POST", "/leave-accruals", options);
    },

    /** Creer un accrual */
    postLeaveAccruals8(options = {}) {
      return request("POST", "/leave-accruals", options);
    },

    /** Soldes de conges globaux (manager) */
    getLeaveBalances(options = {}) {
      return request("GET", "/leave-balances", options);
    },

    /** Soldes de conges globaux (manager) */
    getLeaveBalances2(options = {}) {
      return request("GET", "/leave-balances", options);
    },

    /** Soldes de conges globaux (manager) */
    getLeaveBalances3(options = {}) {
      return request("GET", "/leave-balances", options);
    },

    /** Soldes de conges globaux (manager) */
    getLeaveBalances4(options = {}) {
      return request("GET", "/leave-balances", options);
    },

    /** Soldes de conges globaux (manager) */
    getLeaveBalances5(options = {}) {
      return request("GET", "/leave-balances", options);
    },

    /** Soldes de conges globaux (manager) */
    getLeaveBalances6(options = {}) {
      return request("GET", "/leave-balances", options);
    },

    /** Soldes de conges globaux (manager) */
    getLeaveBalances7(options = {}) {
      return request("GET", "/leave-balances", options);
    },

    /** Soldes de conges globaux (manager) */
    getLeaveBalances8(options = {}) {
      return request("GET", "/leave-balances", options);
    },

    /** Lister les politiques de conges */
    getLeavePolicies(options = {}) {
      return request("GET", "/leave-policies", options);
    },

    /** Lister les politiques de conges */
    getLeavePolicies2(options = {}) {
      return request("GET", "/leave-policies", options);
    },

    /** Lister les politiques de conges */
    getLeavePolicies3(options = {}) {
      return request("GET", "/leave-policies", options);
    },

    /** Lister les politiques de conges */
    getLeavePolicies4(options = {}) {
      return request("GET", "/leave-policies", options);
    },

    /** Lister les politiques de conges */
    getLeavePolicies5(options = {}) {
      return request("GET", "/leave-policies", options);
    },

    /** Lister les politiques de conges */
    getLeavePolicies6(options = {}) {
      return request("GET", "/leave-policies", options);
    },

    /** Lister les politiques de conges */
    getLeavePolicies7(options = {}) {
      return request("GET", "/leave-policies", options);
    },

    /** Lister les politiques de conges */
    getLeavePolicies8(options = {}) {
      return request("GET", "/leave-policies", options);
    },

    /** Creer une politique de conges */
    postLeavePolicies(options = {}) {
      return request("POST", "/leave-policies", options);
    },

    /** Creer une politique de conges */
    postLeavePolicies2(options = {}) {
      return request("POST", "/leave-policies", options);
    },

    /** Creer une politique de conges */
    postLeavePolicies3(options = {}) {
      return request("POST", "/leave-policies", options);
    },

    /** Creer une politique de conges */
    postLeavePolicies4(options = {}) {
      return request("POST", "/leave-policies", options);
    },

    /** Creer une politique de conges */
    postLeavePolicies5(options = {}) {
      return request("POST", "/leave-policies", options);
    },

    /** Creer une politique de conges */
    postLeavePolicies6(options = {}) {
      return request("POST", "/leave-policies", options);
    },

    /** Creer une politique de conges */
    postLeavePolicies7(options = {}) {
      return request("POST", "/leave-policies", options);
    },

    /** Creer une politique de conges */
    postLeavePolicies8(options = {}) {
      return request("POST", "/leave-policies", options);
    },

    /** Supprimer une politique */
    deleteLeavePoliciesByLeavePolicy(options = {}) {
      return request("DELETE", "/leave-policies/{leavePolicy}", options);
    },

    /** Supprimer une politique */
    deleteLeavePoliciesByLeavePolicy2(options = {}) {
      return request("DELETE", "/leave-policies/{leavePolicy}", options);
    },

    /** Supprimer une politique */
    deleteLeavePoliciesByLeavePolicy3(options = {}) {
      return request("DELETE", "/leave-policies/{leavePolicy}", options);
    },

    /** Supprimer une politique */
    deleteLeavePoliciesByLeavePolicy4(options = {}) {
      return request("DELETE", "/leave-policies/{leavePolicy}", options);
    },

    /** Supprimer une politique */
    deleteLeavePoliciesByLeavePolicy5(options = {}) {
      return request("DELETE", "/leave-policies/{leavePolicy}", options);
    },

    /** Supprimer une politique */
    deleteLeavePoliciesByLeavePolicy6(options = {}) {
      return request("DELETE", "/leave-policies/{leavePolicy}", options);
    },

    /** Supprimer une politique */
    deleteLeavePoliciesByLeavePolicy7(options = {}) {
      return request("DELETE", "/leave-policies/{leavePolicy}", options);
    },

    /** Supprimer une politique */
    deleteLeavePoliciesByLeavePolicy8(options = {}) {
      return request("DELETE", "/leave-policies/{leavePolicy}", options);
    },

    /** Voir une politique */
    getLeavePoliciesByLeavePolicy(options = {}) {
      return request("GET", "/leave-policies/{leavePolicy}", options);
    },

    /** Voir une politique */
    getLeavePoliciesByLeavePolicy2(options = {}) {
      return request("GET", "/leave-policies/{leavePolicy}", options);
    },

    /** Voir une politique */
    getLeavePoliciesByLeavePolicy3(options = {}) {
      return request("GET", "/leave-policies/{leavePolicy}", options);
    },

    /** Voir une politique */
    getLeavePoliciesByLeavePolicy4(options = {}) {
      return request("GET", "/leave-policies/{leavePolicy}", options);
    },

    /** Voir une politique */
    getLeavePoliciesByLeavePolicy5(options = {}) {
      return request("GET", "/leave-policies/{leavePolicy}", options);
    },

    /** Voir une politique */
    getLeavePoliciesByLeavePolicy6(options = {}) {
      return request("GET", "/leave-policies/{leavePolicy}", options);
    },

    /** Voir une politique */
    getLeavePoliciesByLeavePolicy7(options = {}) {
      return request("GET", "/leave-policies/{leavePolicy}", options);
    },

    /** Voir une politique */
    getLeavePoliciesByLeavePolicy8(options = {}) {
      return request("GET", "/leave-policies/{leavePolicy}", options);
    },

    /** Modifier une politique */
    putLeavePoliciesByLeavePolicy(options = {}) {
      return request("PUT", "/leave-policies/{leavePolicy}", options);
    },

    /** Modifier une politique */
    putLeavePoliciesByLeavePolicy2(options = {}) {
      return request("PUT", "/leave-policies/{leavePolicy}", options);
    },

    /** Modifier une politique */
    putLeavePoliciesByLeavePolicy3(options = {}) {
      return request("PUT", "/leave-policies/{leavePolicy}", options);
    },

    /** Modifier une politique */
    putLeavePoliciesByLeavePolicy4(options = {}) {
      return request("PUT", "/leave-policies/{leavePolicy}", options);
    },

    /** Modifier une politique */
    putLeavePoliciesByLeavePolicy5(options = {}) {
      return request("PUT", "/leave-policies/{leavePolicy}", options);
    },

    /** Modifier une politique */
    putLeavePoliciesByLeavePolicy6(options = {}) {
      return request("PUT", "/leave-policies/{leavePolicy}", options);
    },

    /** Modifier une politique */
    putLeavePoliciesByLeavePolicy7(options = {}) {
      return request("PUT", "/leave-policies/{leavePolicy}", options);
    },

    /** Modifier une politique */
    putLeavePoliciesByLeavePolicy8(options = {}) {
      return request("PUT", "/leave-policies/{leavePolicy}", options);
    },

    /** Lister les prets */
    getLoans(options = {}) {
      return request("GET", "/loans", options);
    },

    /** Lister les prets */
    getLoans2(options = {}) {
      return request("GET", "/loans", options);
    },

    /** Lister les prets */
    getLoans3(options = {}) {
      return request("GET", "/loans", options);
    },

    /** Lister les prets */
    getLoans4(options = {}) {
      return request("GET", "/loans", options);
    },

    /** Lister les prets */
    getLoans5(options = {}) {
      return request("GET", "/loans", options);
    },

    /** Lister les prets */
    getLoans6(options = {}) {
      return request("GET", "/loans", options);
    },

    /** Lister les prets */
    getLoans7(options = {}) {
      return request("GET", "/loans", options);
    },

    /** Lister les prets */
    getLoans8(options = {}) {
      return request("GET", "/loans", options);
    },

    /** Lister les prets */
    getLoans9(options = {}) {
      return request("GET", "/loans", options);
    },

    /** Lister les prets */
    getLoans10(options = {}) {
      return request("GET", "/loans", options);
    },

    /** Lister les prets */
    getLoans11(options = {}) {
      return request("GET", "/loans", options);
    },

    /** Demander un pret */
    postLoans(options = {}) {
      return request("POST", "/loans", options);
    },

    /** Demander un pret */
    postLoans2(options = {}) {
      return request("POST", "/loans", options);
    },

    /** Demander un pret */
    postLoans3(options = {}) {
      return request("POST", "/loans", options);
    },

    /** Demander un pret */
    postLoans4(options = {}) {
      return request("POST", "/loans", options);
    },

    /** Demander un pret */
    postLoans5(options = {}) {
      return request("POST", "/loans", options);
    },

    /** Demander un pret */
    postLoans6(options = {}) {
      return request("POST", "/loans", options);
    },

    /** Demander un pret */
    postLoans7(options = {}) {
      return request("POST", "/loans", options);
    },

    /** Demander un pret */
    postLoans8(options = {}) {
      return request("POST", "/loans", options);
    },

    /** Demander un pret */
    postLoans9(options = {}) {
      return request("POST", "/loans", options);
    },

    /** Demander un pret */
    postLoans10(options = {}) {
      return request("POST", "/loans", options);
    },

    /** Demander un pret */
    postLoans11(options = {}) {
      return request("POST", "/loans", options);
    },

    /** Voir un pret */
    getLoansByEmployeeLoan(options = {}) {
      return request("GET", "/loans/{employeeLoan}", options);
    },

    /** Voir un pret */
    getLoansByEmployeeLoan2(options = {}) {
      return request("GET", "/loans/{employeeLoan}", options);
    },

    /** Voir un pret */
    getLoansByEmployeeLoan3(options = {}) {
      return request("GET", "/loans/{employeeLoan}", options);
    },

    /** Voir un pret */
    getLoansByEmployeeLoan4(options = {}) {
      return request("GET", "/loans/{employeeLoan}", options);
    },

    /** Voir un pret */
    getLoansByEmployeeLoan5(options = {}) {
      return request("GET", "/loans/{employeeLoan}", options);
    },

    /** Voir un pret */
    getLoansByEmployeeLoan6(options = {}) {
      return request("GET", "/loans/{employeeLoan}", options);
    },

    /** Voir un pret */
    getLoansByEmployeeLoan7(options = {}) {
      return request("GET", "/loans/{employeeLoan}", options);
    },

    /** Voir un pret */
    getLoansByEmployeeLoan8(options = {}) {
      return request("GET", "/loans/{employeeLoan}", options);
    },

    /** Voir un pret */
    getLoansByEmployeeLoan9(options = {}) {
      return request("GET", "/loans/{employeeLoan}", options);
    },

    /** Voir un pret */
    getLoansByEmployeeLoan10(options = {}) {
      return request("GET", "/loans/{employeeLoan}", options);
    },

    /** Voir un pret */
    getLoansByEmployeeLoan11(options = {}) {
      return request("GET", "/loans/{employeeLoan}", options);
    },

    /** Approuver un pret */
    postLoansByEmployeeLoanApprove(options = {}) {
      return request("POST", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un pret */
    postLoansByEmployeeLoanApprove2(options = {}) {
      return request("POST", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un pret */
    postLoansByEmployeeLoanApprove3(options = {}) {
      return request("POST", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un pret */
    postLoansByEmployeeLoanApprove4(options = {}) {
      return request("POST", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un pret */
    postLoansByEmployeeLoanApprove5(options = {}) {
      return request("POST", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un pret */
    postLoansByEmployeeLoanApprove6(options = {}) {
      return request("POST", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un pret */
    postLoansByEmployeeLoanApprove7(options = {}) {
      return request("POST", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un pret */
    postLoansByEmployeeLoanApprove8(options = {}) {
      return request("POST", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un pret */
    postLoansByEmployeeLoanApprove9(options = {}) {
      return request("POST", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un pret */
    postLoansByEmployeeLoanApprove10(options = {}) {
      return request("POST", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un pret */
    postLoansByEmployeeLoanApprove11(options = {}) {
      return request("POST", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanApprove(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanApprove2(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanApprove3(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanApprove4(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanApprove5(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanApprove6(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanApprove7(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanApprove8(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanApprove9(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanApprove10(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/approve", options);
    },

    /** Approuver un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanApprove11(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/approve", options);
    },

    /** Decaisser un pret */
    postLoansByEmployeeLoanDisburse(options = {}) {
      return request("POST", "/loans/{employeeLoan}/disburse", options);
    },

    /** Decaisser un pret */
    postLoansByEmployeeLoanDisburse2(options = {}) {
      return request("POST", "/loans/{employeeLoan}/disburse", options);
    },

    /** Decaisser un pret */
    postLoansByEmployeeLoanDisburse3(options = {}) {
      return request("POST", "/loans/{employeeLoan}/disburse", options);
    },

    /** Decaisser un pret */
    postLoansByEmployeeLoanDisburse4(options = {}) {
      return request("POST", "/loans/{employeeLoan}/disburse", options);
    },

    /** Decaisser un pret */
    postLoansByEmployeeLoanDisburse5(options = {}) {
      return request("POST", "/loans/{employeeLoan}/disburse", options);
    },

    /** Decaisser un pret */
    postLoansByEmployeeLoanDisburse6(options = {}) {
      return request("POST", "/loans/{employeeLoan}/disburse", options);
    },

    /** Decaisser un pret */
    postLoansByEmployeeLoanDisburse7(options = {}) {
      return request("POST", "/loans/{employeeLoan}/disburse", options);
    },

    /** Decaisser un pret */
    postLoansByEmployeeLoanDisburse8(options = {}) {
      return request("POST", "/loans/{employeeLoan}/disburse", options);
    },

    /** Decaisser un pret */
    postLoansByEmployeeLoanDisburse9(options = {}) {
      return request("POST", "/loans/{employeeLoan}/disburse", options);
    },

    /** Decaisser un pret */
    postLoansByEmployeeLoanDisburse10(options = {}) {
      return request("POST", "/loans/{employeeLoan}/disburse", options);
    },

    /** Decaisser un pret */
    postLoansByEmployeeLoanDisburse11(options = {}) {
      return request("POST", "/loans/{employeeLoan}/disburse", options);
    },

    /** Décaisser un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanDisburse(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/disburse", options);
    },

    /** Décaisser un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanDisburse2(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/disburse", options);
    },

    /** Décaisser un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanDisburse3(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/disburse", options);
    },

    /** Décaisser un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanDisburse4(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/disburse", options);
    },

    /** Décaisser un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanDisburse5(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/disburse", options);
    },

    /** Décaisser un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanDisburse6(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/disburse", options);
    },

    /** Décaisser un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanDisburse7(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/disburse", options);
    },

    /** Décaisser un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanDisburse8(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/disburse", options);
    },

    /** Décaisser un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanDisburse9(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/disburse", options);
    },

    /** Décaisser un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanDisburse10(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/disburse", options);
    },

    /** Décaisser un prêt (déprécié — utiliser POST) */
    putLoansByEmployeeLoanDisburse11(options = {}) {
      return request("PUT", "/loans/{employeeLoan}/disburse", options);
    },

    /** Persister un lead marketing (vitrine) */
    postMarketingLeads(options = {}) {
      return request("POST", "/marketing/leads", options);
    },

    /** Persister un lead marketing (vitrine) */
    postMarketingLeads2(options = {}) {
      return request("POST", "/marketing/leads", options);
    },

    /** Persister un lead marketing (vitrine) */
    postMarketingLeads3(options = {}) {
      return request("POST", "/marketing/leads", options);
    },

    /** Persister un lead marketing (vitrine) */
    postMarketingLeads4(options = {}) {
      return request("POST", "/marketing/leads", options);
    },

    /** Persister un lead marketing (vitrine) */
    postMarketingLeads5(options = {}) {
      return request("POST", "/marketing/leads", options);
    },

    /** Persister un lead marketing (vitrine) */
    postMarketingLeads6(options = {}) {
      return request("POST", "/marketing/leads", options);
    },

    /** Persister un lead marketing (vitrine) */
    postMarketingLeads7(options = {}) {
      return request("POST", "/marketing/leads", options);
    },

    /** Persister un lead marketing (vitrine) */
    postMarketingLeads8(options = {}) {
      return request("POST", "/marketing/leads", options);
    },

    /** Persister un lead marketing (vitrine) */
    postMarketingLeads9(options = {}) {
      return request("POST", "/marketing/leads", options);
    },

    /** Persister un lead marketing (vitrine) */
    postMarketingLeads10(options = {}) {
      return request("POST", "/marketing/leads", options);
    },

    /** Persister un lead marketing (vitrine) */
    postMarketingLeads11(options = {}) {
      return request("POST", "/marketing/leads", options);
    },

    /** Lire le contact comptabilite cree depuis un lead qualifie */
    getMarketingLeadsByLeadContact(options = {}) {
      return request("GET", "/marketing/leads/{lead}/contact", options);
    },

    /** Lire le contact comptabilite cree depuis un lead qualifie */
    getMarketingLeadsByLeadContact2(options = {}) {
      return request("GET", "/marketing/leads/{lead}/contact", options);
    },

    /** Lire le contact comptabilite cree depuis un lead qualifie */
    getMarketingLeadsByLeadContact3(options = {}) {
      return request("GET", "/marketing/leads/{lead}/contact", options);
    },

    /** Lire le contact comptabilite cree depuis un lead qualifie */
    getMarketingLeadsByLeadContact4(options = {}) {
      return request("GET", "/marketing/leads/{lead}/contact", options);
    },

    /** Lire le contact comptabilite cree depuis un lead qualifie */
    getMarketingLeadsByLeadContact5(options = {}) {
      return request("GET", "/marketing/leads/{lead}/contact", options);
    },

    /** Lire le contact comptabilite cree depuis un lead qualifie */
    getMarketingLeadsByLeadContact6(options = {}) {
      return request("GET", "/marketing/leads/{lead}/contact", options);
    },

    /** Lire le contact comptabilite cree depuis un lead qualifie */
    getMarketingLeadsByLeadContact7(options = {}) {
      return request("GET", "/marketing/leads/{lead}/contact", options);
    },

    /** Qualifier un lead et creer le contact comptabilite associe */
    postMarketingLeadsByLeadQualify(options = {}) {
      return request("POST", "/marketing/leads/{lead}/qualify", options);
    },

    /** Qualifier un lead et creer le contact comptabilite associe */
    postMarketingLeadsByLeadQualify2(options = {}) {
      return request("POST", "/marketing/leads/{lead}/qualify", options);
    },

    /** Qualifier un lead et creer le contact comptabilite associe */
    postMarketingLeadsByLeadQualify3(options = {}) {
      return request("POST", "/marketing/leads/{lead}/qualify", options);
    },

    /** Qualifier un lead et creer le contact comptabilite associe */
    postMarketingLeadsByLeadQualify4(options = {}) {
      return request("POST", "/marketing/leads/{lead}/qualify", options);
    },

    /** Qualifier un lead et creer le contact comptabilite associe */
    postMarketingLeadsByLeadQualify5(options = {}) {
      return request("POST", "/marketing/leads/{lead}/qualify", options);
    },

    /** Qualifier un lead et creer le contact comptabilite associe */
    postMarketingLeadsByLeadQualify6(options = {}) {
      return request("POST", "/marketing/leads/{lead}/qualify", options);
    },

    /** Qualifier un lead et creer le contact comptabilite associe */
    postMarketingLeadsByLeadQualify7(options = {}) {
      return request("POST", "/marketing/leads/{lead}/qualify", options);
    },

    /** Lister les publications (alias court de /social-posts) */
    getMarketingPosts(options = {}) {
      return request("GET", "/marketing/posts", options);
    },

    /** Lister les publications (alias court de /social-posts) */
    getMarketingPosts2(options = {}) {
      return request("GET", "/marketing/posts", options);
    },

    /** Lister les publications (alias court de /social-posts) */
    getMarketingPosts3(options = {}) {
      return request("GET", "/marketing/posts", options);
    },

    /** Lister les publications (alias court de /social-posts) */
    getMarketingPosts4(options = {}) {
      return request("GET", "/marketing/posts", options);
    },

    /** Lister les publications (alias court de /social-posts) */
    getMarketingPosts5(options = {}) {
      return request("GET", "/marketing/posts", options);
    },

    /** Lister les publications (alias court de /social-posts) */
    getMarketingPosts6(options = {}) {
      return request("GET", "/marketing/posts", options);
    },

    /** Lister les publications (alias court de /social-posts) */
    getMarketingPosts7(options = {}) {
      return request("GET", "/marketing/posts", options);
    },

    /** Lister les publications (alias court de /social-posts) */
    getMarketingPosts8(options = {}) {
      return request("GET", "/marketing/posts", options);
    },

    /** Lister les publications (alias court de /social-posts) */
    getMarketingPosts9(options = {}) {
      return request("GET", "/marketing/posts", options);
    },

    /** Lister les publications (alias court de /social-posts) */
    getMarketingPosts10(options = {}) {
      return request("GET", "/marketing/posts", options);
    },

    /** Lister les publications (alias court de /social-posts) */
    getMarketingPosts11(options = {}) {
      return request("GET", "/marketing/posts", options);
    },

    /** Créer une publication (alias court de /social-posts) */
    postMarketingPosts(options = {}) {
      return request("POST", "/marketing/posts", options);
    },

    /** Créer une publication (alias court de /social-posts) */
    postMarketingPosts2(options = {}) {
      return request("POST", "/marketing/posts", options);
    },

    /** Créer une publication (alias court de /social-posts) */
    postMarketingPosts3(options = {}) {
      return request("POST", "/marketing/posts", options);
    },

    /** Créer une publication (alias court de /social-posts) */
    postMarketingPosts4(options = {}) {
      return request("POST", "/marketing/posts", options);
    },

    /** Créer une publication (alias court de /social-posts) */
    postMarketingPosts5(options = {}) {
      return request("POST", "/marketing/posts", options);
    },

    /** Créer une publication (alias court de /social-posts) */
    postMarketingPosts6(options = {}) {
      return request("POST", "/marketing/posts", options);
    },

    /** Créer une publication (alias court de /social-posts) */
    postMarketingPosts7(options = {}) {
      return request("POST", "/marketing/posts", options);
    },

    /** Créer une publication (alias court de /social-posts) */
    postMarketingPosts8(options = {}) {
      return request("POST", "/marketing/posts", options);
    },

    /** Créer une publication (alias court de /social-posts) */
    postMarketingPosts9(options = {}) {
      return request("POST", "/marketing/posts", options);
    },

    /** Créer une publication (alias court de /social-posts) */
    postMarketingPosts10(options = {}) {
      return request("POST", "/marketing/posts", options);
    },

    /** Créer une publication (alias court de /social-posts) */
    postMarketingPosts11(options = {}) {
      return request("POST", "/marketing/posts", options);
    },

    /** Supprimer une publication (alias court) */
    deleteMarketingPostsBySocialPost(options = {}) {
      return request("DELETE", "/marketing/posts/{socialPost}", options);
    },

    /** Supprimer une publication (alias court) */
    deleteMarketingPostsBySocialPost2(options = {}) {
      return request("DELETE", "/marketing/posts/{socialPost}", options);
    },

    /** Supprimer une publication (alias court) */
    deleteMarketingPostsBySocialPost3(options = {}) {
      return request("DELETE", "/marketing/posts/{socialPost}", options);
    },

    /** Supprimer une publication (alias court) */
    deleteMarketingPostsBySocialPost4(options = {}) {
      return request("DELETE", "/marketing/posts/{socialPost}", options);
    },

    /** Supprimer une publication (alias court) */
    deleteMarketingPostsBySocialPost5(options = {}) {
      return request("DELETE", "/marketing/posts/{socialPost}", options);
    },

    /** Supprimer une publication (alias court) */
    deleteMarketingPostsBySocialPost6(options = {}) {
      return request("DELETE", "/marketing/posts/{socialPost}", options);
    },

    /** Supprimer une publication (alias court) */
    deleteMarketingPostsBySocialPost7(options = {}) {
      return request("DELETE", "/marketing/posts/{socialPost}", options);
    },

    /** Supprimer une publication (alias court) */
    deleteMarketingPostsBySocialPost8(options = {}) {
      return request("DELETE", "/marketing/posts/{socialPost}", options);
    },

    /** Supprimer une publication (alias court) */
    deleteMarketingPostsBySocialPost9(options = {}) {
      return request("DELETE", "/marketing/posts/{socialPost}", options);
    },

    /** Supprimer une publication (alias court) */
    deleteMarketingPostsBySocialPost10(options = {}) {
      return request("DELETE", "/marketing/posts/{socialPost}", options);
    },

    /** Supprimer une publication (alias court) */
    deleteMarketingPostsBySocialPost11(options = {}) {
      return request("DELETE", "/marketing/posts/{socialPost}", options);
    },

    /** Détail d'une publication (alias court) */
    getMarketingPostsBySocialPost(options = {}) {
      return request("GET", "/marketing/posts/{socialPost}", options);
    },

    /** Détail d'une publication (alias court) */
    getMarketingPostsBySocialPost2(options = {}) {
      return request("GET", "/marketing/posts/{socialPost}", options);
    },

    /** Détail d'une publication (alias court) */
    getMarketingPostsBySocialPost3(options = {}) {
      return request("GET", "/marketing/posts/{socialPost}", options);
    },

    /** Détail d'une publication (alias court) */
    getMarketingPostsBySocialPost4(options = {}) {
      return request("GET", "/marketing/posts/{socialPost}", options);
    },

    /** Détail d'une publication (alias court) */
    getMarketingPostsBySocialPost5(options = {}) {
      return request("GET", "/marketing/posts/{socialPost}", options);
    },

    /** Détail d'une publication (alias court) */
    getMarketingPostsBySocialPost6(options = {}) {
      return request("GET", "/marketing/posts/{socialPost}", options);
    },

    /** Détail d'une publication (alias court) */
    getMarketingPostsBySocialPost7(options = {}) {
      return request("GET", "/marketing/posts/{socialPost}", options);
    },

    /** Détail d'une publication (alias court) */
    getMarketingPostsBySocialPost8(options = {}) {
      return request("GET", "/marketing/posts/{socialPost}", options);
    },

    /** Détail d'une publication (alias court) */
    getMarketingPostsBySocialPost9(options = {}) {
      return request("GET", "/marketing/posts/{socialPost}", options);
    },

    /** Détail d'une publication (alias court) */
    getMarketingPostsBySocialPost10(options = {}) {
      return request("GET", "/marketing/posts/{socialPost}", options);
    },

    /** Détail d'une publication (alias court) */
    getMarketingPostsBySocialPost11(options = {}) {
      return request("GET", "/marketing/posts/{socialPost}", options);
    },

    /** Mettre à jour une publication (alias court) */
    patchMarketingPostsBySocialPost(options = {}) {
      return request("PATCH", "/marketing/posts/{socialPost}", options);
    },

    /** Mettre à jour une publication (alias court) */
    patchMarketingPostsBySocialPost2(options = {}) {
      return request("PATCH", "/marketing/posts/{socialPost}", options);
    },

    /** Mettre à jour une publication (alias court) */
    patchMarketingPostsBySocialPost3(options = {}) {
      return request("PATCH", "/marketing/posts/{socialPost}", options);
    },

    /** Mettre à jour une publication (alias court) */
    patchMarketingPostsBySocialPost4(options = {}) {
      return request("PATCH", "/marketing/posts/{socialPost}", options);
    },

    /** Mettre à jour une publication (alias court) */
    patchMarketingPostsBySocialPost5(options = {}) {
      return request("PATCH", "/marketing/posts/{socialPost}", options);
    },

    /** Mettre à jour une publication (alias court) */
    patchMarketingPostsBySocialPost6(options = {}) {
      return request("PATCH", "/marketing/posts/{socialPost}", options);
    },

    /** Mettre à jour une publication (alias court) */
    patchMarketingPostsBySocialPost7(options = {}) {
      return request("PATCH", "/marketing/posts/{socialPost}", options);
    },

    /** Mettre à jour une publication (alias court) */
    patchMarketingPostsBySocialPost8(options = {}) {
      return request("PATCH", "/marketing/posts/{socialPost}", options);
    },

    /** Mettre à jour une publication (alias court) */
    patchMarketingPostsBySocialPost9(options = {}) {
      return request("PATCH", "/marketing/posts/{socialPost}", options);
    },

    /** Mettre à jour une publication (alias court) */
    patchMarketingPostsBySocialPost10(options = {}) {
      return request("PATCH", "/marketing/posts/{socialPost}", options);
    },

    /** Mettre à jour une publication (alias court) */
    patchMarketingPostsBySocialPost11(options = {}) {
      return request("PATCH", "/marketing/posts/{socialPost}", options);
    },

    /** Publier une publication planifiée (alias court) */
    postMarketingPostsBySocialPostPublish(options = {}) {
      return request("POST", "/marketing/posts/{socialPost}/publish", options);
    },

    /** Publier une publication planifiée (alias court) */
    postMarketingPostsBySocialPostPublish2(options = {}) {
      return request("POST", "/marketing/posts/{socialPost}/publish", options);
    },

    /** Publier une publication planifiée (alias court) */
    postMarketingPostsBySocialPostPublish3(options = {}) {
      return request("POST", "/marketing/posts/{socialPost}/publish", options);
    },

    /** Publier une publication planifiée (alias court) */
    postMarketingPostsBySocialPostPublish4(options = {}) {
      return request("POST", "/marketing/posts/{socialPost}/publish", options);
    },

    /** Publier une publication planifiée (alias court) */
    postMarketingPostsBySocialPostPublish5(options = {}) {
      return request("POST", "/marketing/posts/{socialPost}/publish", options);
    },

    /** Publier une publication planifiée (alias court) */
    postMarketingPostsBySocialPostPublish6(options = {}) {
      return request("POST", "/marketing/posts/{socialPost}/publish", options);
    },

    /** Publier une publication planifiée (alias court) */
    postMarketingPostsBySocialPostPublish7(options = {}) {
      return request("POST", "/marketing/posts/{socialPost}/publish", options);
    },

    /** Publier une publication planifiée (alias court) */
    postMarketingPostsBySocialPostPublish8(options = {}) {
      return request("POST", "/marketing/posts/{socialPost}/publish", options);
    },

    /** Publier une publication planifiée (alias court) */
    postMarketingPostsBySocialPostPublish9(options = {}) {
      return request("POST", "/marketing/posts/{socialPost}/publish", options);
    },

    /** Publier une publication planifiée (alias court) */
    postMarketingPostsBySocialPostPublish10(options = {}) {
      return request("POST", "/marketing/posts/{socialPost}/publish", options);
    },

    /** Publier une publication planifiée (alias court) */
    postMarketingPostsBySocialPostPublish11(options = {}) {
      return request("POST", "/marketing/posts/{socialPost}/publish", options);
    },

    /** Compte social connecte du tenant */
    getMarketingSocialAccount(options = {}) {
      return request("GET", "/marketing/social-account", options);
    },

    /** Compte social connecte du tenant */
    getMarketingSocialAccount2(options = {}) {
      return request("GET", "/marketing/social-account", options);
    },

    /** Compte social connecte du tenant */
    getMarketingSocialAccount3(options = {}) {
      return request("GET", "/marketing/social-account", options);
    },

    /** Compte social connecte du tenant */
    getMarketingSocialAccount4(options = {}) {
      return request("GET", "/marketing/social-account", options);
    },

    /** Compte social connecte du tenant */
    getMarketingSocialAccount5(options = {}) {
      return request("GET", "/marketing/social-account", options);
    },

    /** Compte social connecte du tenant */
    getMarketingSocialAccount6(options = {}) {
      return request("GET", "/marketing/social-account", options);
    },

    /** Compte social connecte du tenant */
    getMarketingSocialAccount7(options = {}) {
      return request("GET", "/marketing/social-account", options);
    },

    /** Compte social connecte du tenant */
    getMarketingSocialAccount8(options = {}) {
      return request("GET", "/marketing/social-account", options);
    },

    /** Compte social connecte du tenant */
    getMarketingSocialAccount9(options = {}) {
      return request("GET", "/marketing/social-account", options);
    },

    /** Compte social connecte du tenant */
    getMarketingSocialAccount10(options = {}) {
      return request("GET", "/marketing/social-account", options);
    },

    /** Compte social connecte du tenant */
    getMarketingSocialAccount11(options = {}) {
      return request("GET", "/marketing/social-account", options);
    },

    /** Connecter un compte social (agregateur Ayrshare) */
    postMarketingSocialAccountConnect(options = {}) {
      return request("POST", "/marketing/social-account/connect", options);
    },

    /** Connecter un compte social (agregateur Ayrshare) */
    postMarketingSocialAccountConnect2(options = {}) {
      return request("POST", "/marketing/social-account/connect", options);
    },

    /** Connecter un compte social (agregateur Ayrshare) */
    postMarketingSocialAccountConnect3(options = {}) {
      return request("POST", "/marketing/social-account/connect", options);
    },

    /** Connecter un compte social (agregateur Ayrshare) */
    postMarketingSocialAccountConnect4(options = {}) {
      return request("POST", "/marketing/social-account/connect", options);
    },

    /** Connecter un compte social (agregateur Ayrshare) */
    postMarketingSocialAccountConnect5(options = {}) {
      return request("POST", "/marketing/social-account/connect", options);
    },

    /** Connecter un compte social (agregateur Ayrshare) */
    postMarketingSocialAccountConnect6(options = {}) {
      return request("POST", "/marketing/social-account/connect", options);
    },

    /** Connecter un compte social (agregateur Ayrshare) */
    postMarketingSocialAccountConnect7(options = {}) {
      return request("POST", "/marketing/social-account/connect", options);
    },

    /** Connecter un compte social (agregateur Ayrshare) */
    postMarketingSocialAccountConnect8(options = {}) {
      return request("POST", "/marketing/social-account/connect", options);
    },

    /** Connecter un compte social (agregateur Ayrshare) */
    postMarketingSocialAccountConnect9(options = {}) {
      return request("POST", "/marketing/social-account/connect", options);
    },

    /** Connecter un compte social (agregateur Ayrshare) */
    postMarketingSocialAccountConnect10(options = {}) {
      return request("POST", "/marketing/social-account/connect", options);
    },

    /** Connecter un compte social (agregateur Ayrshare) */
    postMarketingSocialAccountConnect11(options = {}) {
      return request("POST", "/marketing/social-account/connect", options);
    },

    /** Deconnecter le compte social du tenant */
    postMarketingSocialAccountDisconnect(options = {}) {
      return request("POST", "/marketing/social-account/disconnect", options);
    },

    /** Deconnecter le compte social du tenant */
    postMarketingSocialAccountDisconnect2(options = {}) {
      return request("POST", "/marketing/social-account/disconnect", options);
    },

    /** Deconnecter le compte social du tenant */
    postMarketingSocialAccountDisconnect3(options = {}) {
      return request("POST", "/marketing/social-account/disconnect", options);
    },

    /** Deconnecter le compte social du tenant */
    postMarketingSocialAccountDisconnect4(options = {}) {
      return request("POST", "/marketing/social-account/disconnect", options);
    },

    /** Deconnecter le compte social du tenant */
    postMarketingSocialAccountDisconnect5(options = {}) {
      return request("POST", "/marketing/social-account/disconnect", options);
    },

    /** Deconnecter le compte social du tenant */
    postMarketingSocialAccountDisconnect6(options = {}) {
      return request("POST", "/marketing/social-account/disconnect", options);
    },

    /** Deconnecter le compte social du tenant */
    postMarketingSocialAccountDisconnect7(options = {}) {
      return request("POST", "/marketing/social-account/disconnect", options);
    },

    /** Deconnecter le compte social du tenant */
    postMarketingSocialAccountDisconnect8(options = {}) {
      return request("POST", "/marketing/social-account/disconnect", options);
    },

    /** Deconnecter le compte social du tenant */
    postMarketingSocialAccountDisconnect9(options = {}) {
      return request("POST", "/marketing/social-account/disconnect", options);
    },

    /** Deconnecter le compte social du tenant */
    postMarketingSocialAccountDisconnect10(options = {}) {
      return request("POST", "/marketing/social-account/disconnect", options);
    },

    /** Deconnecter le compte social du tenant */
    postMarketingSocialAccountDisconnect11(options = {}) {
      return request("POST", "/marketing/social-account/disconnect", options);
    },

    /** Comptes sociaux connectés (alias pluriel de /social-account) */
    getMarketingSocialAccounts(options = {}) {
      return request("GET", "/marketing/social-accounts", options);
    },

    /** Comptes sociaux connectés (alias pluriel de /social-account) */
    getMarketingSocialAccounts2(options = {}) {
      return request("GET", "/marketing/social-accounts", options);
    },

    /** Comptes sociaux connectés (alias pluriel de /social-account) */
    getMarketingSocialAccounts3(options = {}) {
      return request("GET", "/marketing/social-accounts", options);
    },

    /** Comptes sociaux connectés (alias pluriel de /social-account) */
    getMarketingSocialAccounts4(options = {}) {
      return request("GET", "/marketing/social-accounts", options);
    },

    /** Comptes sociaux connectés (alias pluriel de /social-account) */
    getMarketingSocialAccounts5(options = {}) {
      return request("GET", "/marketing/social-accounts", options);
    },

    /** Comptes sociaux connectés (alias pluriel de /social-account) */
    getMarketingSocialAccounts6(options = {}) {
      return request("GET", "/marketing/social-accounts", options);
    },

    /** Comptes sociaux connectés (alias pluriel de /social-account) */
    getMarketingSocialAccounts7(options = {}) {
      return request("GET", "/marketing/social-accounts", options);
    },

    /** Comptes sociaux connectés (alias pluriel de /social-account) */
    getMarketingSocialAccounts8(options = {}) {
      return request("GET", "/marketing/social-accounts", options);
    },

    /** Comptes sociaux connectés (alias pluriel de /social-account) */
    getMarketingSocialAccounts9(options = {}) {
      return request("GET", "/marketing/social-accounts", options);
    },

    /** Comptes sociaux connectés (alias pluriel de /social-account) */
    getMarketingSocialAccounts10(options = {}) {
      return request("GET", "/marketing/social-accounts", options);
    },

    /** Comptes sociaux connectés (alias pluriel de /social-account) */
    getMarketingSocialAccounts11(options = {}) {
      return request("GET", "/marketing/social-accounts", options);
    },

    /** Connecter un compte social (alias pluriel) */
    postMarketingSocialAccountsConnect(options = {}) {
      return request("POST", "/marketing/social-accounts/connect", options);
    },

    /** Connecter un compte social (alias pluriel) */
    postMarketingSocialAccountsConnect2(options = {}) {
      return request("POST", "/marketing/social-accounts/connect", options);
    },

    /** Connecter un compte social (alias pluriel) */
    postMarketingSocialAccountsConnect3(options = {}) {
      return request("POST", "/marketing/social-accounts/connect", options);
    },

    /** Connecter un compte social (alias pluriel) */
    postMarketingSocialAccountsConnect4(options = {}) {
      return request("POST", "/marketing/social-accounts/connect", options);
    },

    /** Connecter un compte social (alias pluriel) */
    postMarketingSocialAccountsConnect5(options = {}) {
      return request("POST", "/marketing/social-accounts/connect", options);
    },

    /** Connecter un compte social (alias pluriel) */
    postMarketingSocialAccountsConnect6(options = {}) {
      return request("POST", "/marketing/social-accounts/connect", options);
    },

    /** Connecter un compte social (alias pluriel) */
    postMarketingSocialAccountsConnect7(options = {}) {
      return request("POST", "/marketing/social-accounts/connect", options);
    },

    /** Connecter un compte social (alias pluriel) */
    postMarketingSocialAccountsConnect8(options = {}) {
      return request("POST", "/marketing/social-accounts/connect", options);
    },

    /** Connecter un compte social (alias pluriel) */
    postMarketingSocialAccountsConnect9(options = {}) {
      return request("POST", "/marketing/social-accounts/connect", options);
    },

    /** Connecter un compte social (alias pluriel) */
    postMarketingSocialAccountsConnect10(options = {}) {
      return request("POST", "/marketing/social-accounts/connect", options);
    },

    /** Connecter un compte social (alias pluriel) */
    postMarketingSocialAccountsConnect11(options = {}) {
      return request("POST", "/marketing/social-accounts/connect", options);
    },

    /** Déconnecter un compte social (alias pluriel) */
    postMarketingSocialAccountsDisconnect(options = {}) {
      return request("POST", "/marketing/social-accounts/disconnect", options);
    },

    /** Déconnecter un compte social (alias pluriel) */
    postMarketingSocialAccountsDisconnect2(options = {}) {
      return request("POST", "/marketing/social-accounts/disconnect", options);
    },

    /** Déconnecter un compte social (alias pluriel) */
    postMarketingSocialAccountsDisconnect3(options = {}) {
      return request("POST", "/marketing/social-accounts/disconnect", options);
    },

    /** Déconnecter un compte social (alias pluriel) */
    postMarketingSocialAccountsDisconnect4(options = {}) {
      return request("POST", "/marketing/social-accounts/disconnect", options);
    },

    /** Déconnecter un compte social (alias pluriel) */
    postMarketingSocialAccountsDisconnect5(options = {}) {
      return request("POST", "/marketing/social-accounts/disconnect", options);
    },

    /** Déconnecter un compte social (alias pluriel) */
    postMarketingSocialAccountsDisconnect6(options = {}) {
      return request("POST", "/marketing/social-accounts/disconnect", options);
    },

    /** Déconnecter un compte social (alias pluriel) */
    postMarketingSocialAccountsDisconnect7(options = {}) {
      return request("POST", "/marketing/social-accounts/disconnect", options);
    },

    /** Déconnecter un compte social (alias pluriel) */
    postMarketingSocialAccountsDisconnect8(options = {}) {
      return request("POST", "/marketing/social-accounts/disconnect", options);
    },

    /** Déconnecter un compte social (alias pluriel) */
    postMarketingSocialAccountsDisconnect9(options = {}) {
      return request("POST", "/marketing/social-accounts/disconnect", options);
    },

    /** Déconnecter un compte social (alias pluriel) */
    postMarketingSocialAccountsDisconnect10(options = {}) {
      return request("POST", "/marketing/social-accounts/disconnect", options);
    },

    /** Déconnecter un compte social (alias pluriel) */
    postMarketingSocialAccountsDisconnect11(options = {}) {
      return request("POST", "/marketing/social-accounts/disconnect", options);
    },

    /** Lister les publications reseaux sociaux du tenant */
    getMarketingSocialPosts(options = {}) {
      return request("GET", "/marketing/social-posts", options);
    },

    /** Lister les publications reseaux sociaux du tenant */
    getMarketingSocialPosts2(options = {}) {
      return request("GET", "/marketing/social-posts", options);
    },

    /** Lister les publications reseaux sociaux du tenant */
    getMarketingSocialPosts3(options = {}) {
      return request("GET", "/marketing/social-posts", options);
    },

    /** Lister les publications reseaux sociaux du tenant */
    getMarketingSocialPosts4(options = {}) {
      return request("GET", "/marketing/social-posts", options);
    },

    /** Lister les publications reseaux sociaux du tenant */
    getMarketingSocialPosts5(options = {}) {
      return request("GET", "/marketing/social-posts", options);
    },

    /** Lister les publications reseaux sociaux du tenant */
    getMarketingSocialPosts6(options = {}) {
      return request("GET", "/marketing/social-posts", options);
    },

    /** Lister les publications reseaux sociaux du tenant */
    getMarketingSocialPosts7(options = {}) {
      return request("GET", "/marketing/social-posts", options);
    },

    /** Lister les publications reseaux sociaux du tenant */
    getMarketingSocialPosts8(options = {}) {
      return request("GET", "/marketing/social-posts", options);
    },

    /** Lister les publications reseaux sociaux du tenant */
    getMarketingSocialPosts9(options = {}) {
      return request("GET", "/marketing/social-posts", options);
    },

    /** Lister les publications reseaux sociaux du tenant */
    getMarketingSocialPosts10(options = {}) {
      return request("GET", "/marketing/social-posts", options);
    },

    /** Lister les publications reseaux sociaux du tenant */
    getMarketingSocialPosts11(options = {}) {
      return request("GET", "/marketing/social-posts", options);
    },

    /** Creer une publication (brouillon, ou planifiee/publiee si scheduled_at fourni) */
    postMarketingSocialPosts(options = {}) {
      return request("POST", "/marketing/social-posts", options);
    },

    /** Creer une publication (brouillon, ou planifiee/publiee si scheduled_at fourni) */
    postMarketingSocialPosts2(options = {}) {
      return request("POST", "/marketing/social-posts", options);
    },

    /** Creer une publication (brouillon, ou planifiee/publiee si scheduled_at fourni) */
    postMarketingSocialPosts3(options = {}) {
      return request("POST", "/marketing/social-posts", options);
    },

    /** Creer une publication (brouillon, ou planifiee/publiee si scheduled_at fourni) */
    postMarketingSocialPosts4(options = {}) {
      return request("POST", "/marketing/social-posts", options);
    },

    /** Creer une publication (brouillon, ou planifiee/publiee si scheduled_at fourni) */
    postMarketingSocialPosts5(options = {}) {
      return request("POST", "/marketing/social-posts", options);
    },

    /** Creer une publication (brouillon, ou planifiee/publiee si scheduled_at fourni) */
    postMarketingSocialPosts6(options = {}) {
      return request("POST", "/marketing/social-posts", options);
    },

    /** Creer une publication (brouillon, ou planifiee/publiee si scheduled_at fourni) */
    postMarketingSocialPosts7(options = {}) {
      return request("POST", "/marketing/social-posts", options);
    },

    /** Creer une publication (brouillon, ou planifiee/publiee si scheduled_at fourni) */
    postMarketingSocialPosts8(options = {}) {
      return request("POST", "/marketing/social-posts", options);
    },

    /** Creer une publication (brouillon, ou planifiee/publiee si scheduled_at fourni) */
    postMarketingSocialPosts9(options = {}) {
      return request("POST", "/marketing/social-posts", options);
    },

    /** Creer une publication (brouillon, ou planifiee/publiee si scheduled_at fourni) */
    postMarketingSocialPosts10(options = {}) {
      return request("POST", "/marketing/social-posts", options);
    },

    /** Creer une publication (brouillon, ou planifiee/publiee si scheduled_at fourni) */
    postMarketingSocialPosts11(options = {}) {
      return request("POST", "/marketing/social-posts", options);
    },

    /** Supprimer une publication (draft uniquement) */
    deleteMarketingSocialPostsBySocialPost(options = {}) {
      return request("DELETE", "/marketing/social-posts/{socialPost}", options);
    },

    /** Supprimer une publication (draft uniquement) */
    deleteMarketingSocialPostsBySocialPost2(options = {}) {
      return request("DELETE", "/marketing/social-posts/{socialPost}", options);
    },

    /** Supprimer une publication (draft uniquement) */
    deleteMarketingSocialPostsBySocialPost3(options = {}) {
      return request("DELETE", "/marketing/social-posts/{socialPost}", options);
    },

    /** Supprimer une publication (draft uniquement) */
    deleteMarketingSocialPostsBySocialPost4(options = {}) {
      return request("DELETE", "/marketing/social-posts/{socialPost}", options);
    },

    /** Supprimer une publication (draft uniquement) */
    deleteMarketingSocialPostsBySocialPost5(options = {}) {
      return request("DELETE", "/marketing/social-posts/{socialPost}", options);
    },

    /** Supprimer une publication (draft uniquement) */
    deleteMarketingSocialPostsBySocialPost6(options = {}) {
      return request("DELETE", "/marketing/social-posts/{socialPost}", options);
    },

    /** Supprimer une publication (draft uniquement) */
    deleteMarketingSocialPostsBySocialPost7(options = {}) {
      return request("DELETE", "/marketing/social-posts/{socialPost}", options);
    },

    /** Supprimer une publication (draft uniquement) */
    deleteMarketingSocialPostsBySocialPost8(options = {}) {
      return request("DELETE", "/marketing/social-posts/{socialPost}", options);
    },

    /** Supprimer une publication (draft uniquement) */
    deleteMarketingSocialPostsBySocialPost9(options = {}) {
      return request("DELETE", "/marketing/social-posts/{socialPost}", options);
    },

    /** Supprimer une publication (draft uniquement) */
    deleteMarketingSocialPostsBySocialPost10(options = {}) {
      return request("DELETE", "/marketing/social-posts/{socialPost}", options);
    },

    /** Supprimer une publication (draft uniquement) */
    deleteMarketingSocialPostsBySocialPost11(options = {}) {
      return request("DELETE", "/marketing/social-posts/{socialPost}", options);
    },

    /** Detail d'une publication */
    getMarketingSocialPostsBySocialPost(options = {}) {
      return request("GET", "/marketing/social-posts/{socialPost}", options);
    },

    /** Detail d'une publication */
    getMarketingSocialPostsBySocialPost2(options = {}) {
      return request("GET", "/marketing/social-posts/{socialPost}", options);
    },

    /** Detail d'une publication */
    getMarketingSocialPostsBySocialPost3(options = {}) {
      return request("GET", "/marketing/social-posts/{socialPost}", options);
    },

    /** Detail d'une publication */
    getMarketingSocialPostsBySocialPost4(options = {}) {
      return request("GET", "/marketing/social-posts/{socialPost}", options);
    },

    /** Detail d'une publication */
    getMarketingSocialPostsBySocialPost5(options = {}) {
      return request("GET", "/marketing/social-posts/{socialPost}", options);
    },

    /** Detail d'une publication */
    getMarketingSocialPostsBySocialPost6(options = {}) {
      return request("GET", "/marketing/social-posts/{socialPost}", options);
    },

    /** Detail d'une publication */
    getMarketingSocialPostsBySocialPost7(options = {}) {
      return request("GET", "/marketing/social-posts/{socialPost}", options);
    },

    /** Detail d'une publication */
    getMarketingSocialPostsBySocialPost8(options = {}) {
      return request("GET", "/marketing/social-posts/{socialPost}", options);
    },

    /** Detail d'une publication */
    getMarketingSocialPostsBySocialPost9(options = {}) {
      return request("GET", "/marketing/social-posts/{socialPost}", options);
    },

    /** Detail d'une publication */
    getMarketingSocialPostsBySocialPost10(options = {}) {
      return request("GET", "/marketing/social-posts/{socialPost}", options);
    },

    /** Detail d'une publication */
    getMarketingSocialPostsBySocialPost11(options = {}) {
      return request("GET", "/marketing/social-posts/{socialPost}", options);
    },

    /** Modifier une publication (draft uniquement) */
    patchMarketingSocialPostsBySocialPost(options = {}) {
      return request("PATCH", "/marketing/social-posts/{socialPost}", options);
    },

    /** Modifier une publication (draft uniquement) */
    patchMarketingSocialPostsBySocialPost2(options = {}) {
      return request("PATCH", "/marketing/social-posts/{socialPost}", options);
    },

    /** Modifier une publication (draft uniquement) */
    patchMarketingSocialPostsBySocialPost3(options = {}) {
      return request("PATCH", "/marketing/social-posts/{socialPost}", options);
    },

    /** Modifier une publication (draft uniquement) */
    patchMarketingSocialPostsBySocialPost4(options = {}) {
      return request("PATCH", "/marketing/social-posts/{socialPost}", options);
    },

    /** Modifier une publication (draft uniquement) */
    patchMarketingSocialPostsBySocialPost5(options = {}) {
      return request("PATCH", "/marketing/social-posts/{socialPost}", options);
    },

    /** Modifier une publication (draft uniquement) */
    patchMarketingSocialPostsBySocialPost6(options = {}) {
      return request("PATCH", "/marketing/social-posts/{socialPost}", options);
    },

    /** Modifier une publication (draft uniquement) */
    patchMarketingSocialPostsBySocialPost7(options = {}) {
      return request("PATCH", "/marketing/social-posts/{socialPost}", options);
    },

    /** Modifier une publication (draft uniquement) */
    patchMarketingSocialPostsBySocialPost8(options = {}) {
      return request("PATCH", "/marketing/social-posts/{socialPost}", options);
    },

    /** Modifier une publication (draft uniquement) */
    patchMarketingSocialPostsBySocialPost9(options = {}) {
      return request("PATCH", "/marketing/social-posts/{socialPost}", options);
    },

    /** Modifier une publication (draft uniquement) */
    patchMarketingSocialPostsBySocialPost10(options = {}) {
      return request("PATCH", "/marketing/social-posts/{socialPost}", options);
    },

    /** Modifier une publication (draft uniquement) */
    patchMarketingSocialPostsBySocialPost11(options = {}) {
      return request("PATCH", "/marketing/social-posts/{socialPost}", options);
    },

    /** Publier immediatement ou (re)planifier une publication */
    postMarketingSocialPostsBySocialPostPublish(options = {}) {
      return request("POST", "/marketing/social-posts/{socialPost}/publish", options);
    },

    /** Publier immediatement ou (re)planifier une publication */
    postMarketingSocialPostsBySocialPostPublish2(options = {}) {
      return request("POST", "/marketing/social-posts/{socialPost}/publish", options);
    },

    /** Publier immediatement ou (re)planifier une publication */
    postMarketingSocialPostsBySocialPostPublish3(options = {}) {
      return request("POST", "/marketing/social-posts/{socialPost}/publish", options);
    },

    /** Publier immediatement ou (re)planifier une publication */
    postMarketingSocialPostsBySocialPostPublish4(options = {}) {
      return request("POST", "/marketing/social-posts/{socialPost}/publish", options);
    },

    /** Publier immediatement ou (re)planifier une publication */
    postMarketingSocialPostsBySocialPostPublish5(options = {}) {
      return request("POST", "/marketing/social-posts/{socialPost}/publish", options);
    },

    /** Publier immediatement ou (re)planifier une publication */
    postMarketingSocialPostsBySocialPostPublish6(options = {}) {
      return request("POST", "/marketing/social-posts/{socialPost}/publish", options);
    },

    /** Publier immediatement ou (re)planifier une publication */
    postMarketingSocialPostsBySocialPostPublish7(options = {}) {
      return request("POST", "/marketing/social-posts/{socialPost}/publish", options);
    },

    /** Publier immediatement ou (re)planifier une publication */
    postMarketingSocialPostsBySocialPostPublish8(options = {}) {
      return request("POST", "/marketing/social-posts/{socialPost}/publish", options);
    },

    /** Publier immediatement ou (re)planifier une publication */
    postMarketingSocialPostsBySocialPostPublish9(options = {}) {
      return request("POST", "/marketing/social-posts/{socialPost}/publish", options);
    },

    /** Publier immediatement ou (re)planifier une publication */
    postMarketingSocialPostsBySocialPostPublish10(options = {}) {
      return request("POST", "/marketing/social-posts/{socialPost}/publish", options);
    },

    /** Publier immediatement ou (re)planifier une publication */
    postMarketingSocialPostsBySocialPostPublish11(options = {}) {
      return request("POST", "/marketing/social-posts/{socialPost}/publish", options);
    },

    /** Anomalies de pointage de l'employé courant */
    getMeAttendanceAnomalies(options = {}) {
      return request("GET", "/me/attendance-anomalies", options);
    },

    /** Anomalies de pointage de l'employé courant */
    getMeAttendanceAnomalies2(options = {}) {
      return request("GET", "/me/attendance-anomalies", options);
    },

    /** Anomalies de pointage de l'employé courant */
    getMeAttendanceAnomalies3(options = {}) {
      return request("GET", "/me/attendance-anomalies", options);
    },

    /** Anomalies de pointage de l'employé courant */
    getMeAttendanceAnomalies4(options = {}) {
      return request("GET", "/me/attendance-anomalies", options);
    },

    /** Anomalies de pointage de l'employé courant */
    getMeAttendanceAnomalies5(options = {}) {
      return request("GET", "/me/attendance-anomalies", options);
    },

    /** Anomalies de pointage de l'employé courant */
    getMeAttendanceAnomalies6(options = {}) {
      return request("GET", "/me/attendance-anomalies", options);
    },

    /** Anomalies de pointage de l'employé courant */
    getMeAttendanceAnomalies7(options = {}) {
      return request("GET", "/me/attendance-anomalies", options);
    },

    /** Anomalies de pointage de l'employé courant */
    getMeAttendanceAnomalies8(options = {}) {
      return request("GET", "/me/attendance-anomalies", options);
    },

    /** Anomalies de pointage de l'employé courant */
    getMeAttendanceAnomalies9(options = {}) {
      return request("GET", "/me/attendance-anomalies", options);
    },

    /** Anomalies de pointage de l'employé courant */
    getMeAttendanceAnomalies10(options = {}) {
      return request("GET", "/me/attendance-anomalies", options);
    },

    /** Anomalies de pointage de l'employé courant */
    getMeAttendanceAnomalies11(options = {}) {
      return request("GET", "/me/attendance-anomalies", options);
    },

    /** Mon solde de paie courant */
    getMeBalance(options = {}) {
      return request("GET", "/me/balance", options);
    },

    /** Mon solde de paie courant */
    getMeBalance2(options = {}) {
      return request("GET", "/me/balance", options);
    },

    /** Mon solde de paie courant */
    getMeBalance3(options = {}) {
      return request("GET", "/me/balance", options);
    },

    /** Mon solde de paie courant */
    getMeBalance4(options = {}) {
      return request("GET", "/me/balance", options);
    },

    /** Mon solde de paie courant */
    getMeBalance5(options = {}) {
      return request("GET", "/me/balance", options);
    },

    /** Mon solde de paie courant */
    getMeBalance6(options = {}) {
      return request("GET", "/me/balance", options);
    },

    /** Mon solde de paie courant */
    getMeBalance7(options = {}) {
      return request("GET", "/me/balance", options);
    },

    /** Mon solde de paie courant */
    getMeBalance8(options = {}) {
      return request("GET", "/me/balance", options);
    },

    /** Mon solde de paie courant */
    getMeBalance9(options = {}) {
      return request("GET", "/me/balance", options);
    },

    /** Mon solde de paie courant */
    getMeBalance10(options = {}) {
      return request("GET", "/me/balance", options);
    },

    /** Lire le parcours professionnel durable de l'employe courant */
    getMeCareer(options = {}) {
      return request("GET", "/me/career", options);
    },

    /** Lire le parcours professionnel durable de l'employe courant */
    getMeCareer2(options = {}) {
      return request("GET", "/me/career", options);
    },

    /** Lire le parcours professionnel durable de l'employe courant */
    getMeCareer3(options = {}) {
      return request("GET", "/me/career", options);
    },

    /** Lire le parcours professionnel durable de l'employe courant */
    getMeCareer4(options = {}) {
      return request("GET", "/me/career", options);
    },

    /** Lire le parcours professionnel durable de l'employe courant */
    getMeCareer5(options = {}) {
      return request("GET", "/me/career", options);
    },

    /** Soumettre une demande d'integration via QR entreprise */
    postMeCompanyQrScan(options = {}) {
      return request("POST", "/me/company-qr/scan", options);
    },

    /** Soumettre une demande d'integration via QR entreprise */
    postMeCompanyQrScan2(options = {}) {
      return request("POST", "/me/company-qr/scan", options);
    },

    /** Soumettre une demande d'integration via QR entreprise */
    postMeCompanyQrScan3(options = {}) {
      return request("POST", "/me/company-qr/scan", options);
    },

    /** Soumettre une demande d'integration via QR entreprise */
    postMeCompanyQrScan4(options = {}) {
      return request("POST", "/me/company-qr/scan", options);
    },

    /** Soumettre une demande d'integration via QR entreprise */
    postMeCompanyQrScan5(options = {}) {
      return request("POST", "/me/company-qr/scan", options);
    },

    /** Contrat actif de l'employé connecté */
    getMeContract(options = {}) {
      return request("GET", "/me/contract", options);
    },

    /** Contrat actif de l'employé connecté */
    getMeContract2(options = {}) {
      return request("GET", "/me/contract", options);
    },

    /** Contrat actif de l'employé connecté */
    getMeContract3(options = {}) {
      return request("GET", "/me/contract", options);
    },

    /** Contrat actif de l'employé connecté */
    getMeContract4(options = {}) {
      return request("GET", "/me/contract", options);
    },

    /** Contrat actif de l'employé connecté */
    getMeContract5(options = {}) {
      return request("GET", "/me/contract", options);
    },

    /** Contrat actif de l'employé connecté */
    getMeContract6(options = {}) {
      return request("GET", "/me/contract", options);
    },

    /** Contrat actif de l'employé connecté */
    getMeContract7(options = {}) {
      return request("GET", "/me/contract", options);
    },

    /** Contrat actif de l'employé connecté */
    getMeContract8(options = {}) {
      return request("GET", "/me/contract", options);
    },

    /** Contrat actif de l'employé connecté */
    getMeContract9(options = {}) {
      return request("GET", "/me/contract", options);
    },

    /** Contrat actif de l'employé connecté */
    getMeContract10(options = {}) {
      return request("GET", "/me/contract", options);
    },

    /** Contrat actif de l'employé connecté */
    getMeContract11(options = {}) {
      return request("GET", "/me/contract", options);
    },

    /** Mes contrats */
    getMeContracts(options = {}) {
      return request("GET", "/me/contracts", options);
    },

    /** Mes contrats */
    getMeContracts2(options = {}) {
      return request("GET", "/me/contracts", options);
    },

    /** Mes contrats */
    getMeContracts3(options = {}) {
      return request("GET", "/me/contracts", options);
    },

    /** Mes contrats */
    getMeContracts4(options = {}) {
      return request("GET", "/me/contracts", options);
    },

    /** Mes contrats */
    getMeContracts5(options = {}) {
      return request("GET", "/me/contracts", options);
    },

    /** Mes contrats */
    getMeContracts6(options = {}) {
      return request("GET", "/me/contracts", options);
    },

    /** Mes contrats */
    getMeContracts7(options = {}) {
      return request("GET", "/me/contracts", options);
    },

    /** Mes contrats */
    getMeContracts8(options = {}) {
      return request("GET", "/me/contracts", options);
    },

    /** Resume journalier utilisateur courant */
    getMeDailySummary(options = {}) {
      return request("GET", "/me/daily-summary", options);
    },

    /** Resume journalier utilisateur courant */
    getMeDailySummary2(options = {}) {
      return request("GET", "/me/daily-summary", options);
    },

    /** Resume journalier utilisateur courant */
    getMeDailySummary3(options = {}) {
      return request("GET", "/me/daily-summary", options);
    },

    /** Resume journalier utilisateur courant */
    getMeDailySummary4(options = {}) {
      return request("GET", "/me/daily-summary", options);
    },

    /** Resume journalier utilisateur courant */
    getMeDailySummary5(options = {}) {
      return request("GET", "/me/daily-summary", options);
    },

    /** Mon depart (self-service, issue #5324) */
    getMeDeparture(options = {}) {
      return request("GET", "/me/departure", options);
    },

    /** Lire les documents de SON dossier employe (self-service) */
    getMeDocuments(options = {}) {
      return request("GET", "/me/documents", options);
    },

    /** Mes soldes de conges */
    getMeLeaveBalances(options = {}) {
      return request("GET", "/me/leave-balances", options);
    },

    /** Mes soldes de conges */
    getMeLeaveBalances2(options = {}) {
      return request("GET", "/me/leave-balances", options);
    },

    /** Mes soldes de conges */
    getMeLeaveBalances3(options = {}) {
      return request("GET", "/me/leave-balances", options);
    },

    /** Mes soldes de conges */
    getMeLeaveBalances4(options = {}) {
      return request("GET", "/me/leave-balances", options);
    },

    /** Mes soldes de conges */
    getMeLeaveBalances5(options = {}) {
      return request("GET", "/me/leave-balances", options);
    },

    /** Mes soldes de conges */
    getMeLeaveBalances6(options = {}) {
      return request("GET", "/me/leave-balances", options);
    },

    /** Mes soldes de conges */
    getMeLeaveBalances7(options = {}) {
      return request("GET", "/me/leave-balances", options);
    },

    /** Mes soldes de conges */
    getMeLeaveBalances8(options = {}) {
      return request("GET", "/me/leave-balances", options);
    },

    /** Journal de paie de l'employé courant */
    getMeLedger(options = {}) {
      return request("GET", "/me/ledger", options);
    },

    /** Journal de paie de l'employé courant */
    getMeLedger2(options = {}) {
      return request("GET", "/me/ledger", options);
    },

    /** Journal de paie de l'employé courant */
    getMeLedger3(options = {}) {
      return request("GET", "/me/ledger", options);
    },

    /** Journal de paie de l'employé courant */
    getMeLedger4(options = {}) {
      return request("GET", "/me/ledger", options);
    },

    /** Journal de paie de l'employé courant */
    getMeLedger5(options = {}) {
      return request("GET", "/me/ledger", options);
    },

    /** Journal de paie de l'employé courant */
    getMeLedger6(options = {}) {
      return request("GET", "/me/ledger", options);
    },

    /** Journal de paie de l'employé courant */
    getMeLedger7(options = {}) {
      return request("GET", "/me/ledger", options);
    },

    /** Journal de paie de l'employé courant */
    getMeLedger8(options = {}) {
      return request("GET", "/me/ledger", options);
    },

    /** Journal de paie de l'employé courant */
    getMeLedger9(options = {}) {
      return request("GET", "/me/ledger", options);
    },

    /** Journal de paie de l'employé courant */
    getMeLedger10(options = {}) {
      return request("GET", "/me/ledger", options);
    },

    /** Journal de paie de l'employé courant */
    getMeLedger11(options = {}) {
      return request("GET", "/me/ledger", options);
    },

    /** Mes prets */
    getMeLoans(options = {}) {
      return request("GET", "/me/loans", options);
    },

    /** Mes prets */
    getMeLoans2(options = {}) {
      return request("GET", "/me/loans", options);
    },

    /** Mes prets */
    getMeLoans3(options = {}) {
      return request("GET", "/me/loans", options);
    },

    /** Mes prets */
    getMeLoans4(options = {}) {
      return request("GET", "/me/loans", options);
    },

    /** Mes prets */
    getMeLoans5(options = {}) {
      return request("GET", "/me/loans", options);
    },

    /** Mes prets */
    getMeLoans6(options = {}) {
      return request("GET", "/me/loans", options);
    },

    /** Mes prets */
    getMeLoans7(options = {}) {
      return request("GET", "/me/loans", options);
    },

    /** Mes prets */
    getMeLoans8(options = {}) {
      return request("GET", "/me/loans", options);
    },

    /** Mes prets */
    getMeLoans9(options = {}) {
      return request("GET", "/me/loans", options);
    },

    /** Mes prets */
    getMeLoans10(options = {}) {
      return request("GET", "/me/loans", options);
    },

    /** Mes remboursements de pret */
    getMeLoansByLoanRepayments(options = {}) {
      return request("GET", "/me/loans/{loan}/repayments", options);
    },

    /** Mes remboursements de pret */
    getMeLoansByLoanRepayments2(options = {}) {
      return request("GET", "/me/loans/{loan}/repayments", options);
    },

    /** Mes remboursements de pret */
    getMeLoansByLoanRepayments3(options = {}) {
      return request("GET", "/me/loans/{loan}/repayments", options);
    },

    /** Mes remboursements de pret */
    getMeLoansByLoanRepayments4(options = {}) {
      return request("GET", "/me/loans/{loan}/repayments", options);
    },

    /** Mes remboursements de pret */
    getMeLoansByLoanRepayments5(options = {}) {
      return request("GET", "/me/loans/{loan}/repayments", options);
    },

    /** Mes remboursements de pret */
    getMeLoansByLoanRepayments6(options = {}) {
      return request("GET", "/me/loans/{loan}/repayments", options);
    },

    /** Mes remboursements de pret */
    getMeLoansByLoanRepayments7(options = {}) {
      return request("GET", "/me/loans/{loan}/repayments", options);
    },

    /** Mes remboursements de pret */
    getMeLoansByLoanRepayments8(options = {}) {
      return request("GET", "/me/loans/{loan}/repayments", options);
    },

    /** Mes remboursements de pret */
    getMeLoansByLoanRepayments9(options = {}) {
      return request("GET", "/me/loans/{loan}/repayments", options);
    },

    /** Mes remboursements de pret */
    getMeLoansByLoanRepayments10(options = {}) {
      return request("GET", "/me/loans/{loan}/repayments", options);
    },

    /** Resume mensuel utilisateur courant */
    getMeMonthlySummary(options = {}) {
      return request("GET", "/me/monthly-summary", options);
    },

    /** Resume mensuel utilisateur courant */
    getMeMonthlySummary2(options = {}) {
      return request("GET", "/me/monthly-summary", options);
    },

    /** Resume mensuel utilisateur courant */
    getMeMonthlySummary3(options = {}) {
      return request("GET", "/me/monthly-summary", options);
    },

    /** Resume mensuel utilisateur courant */
    getMeMonthlySummary4(options = {}) {
      return request("GET", "/me/monthly-summary", options);
    },

    /** Resume mensuel utilisateur courant */
    getMeMonthlySummary5(options = {}) {
      return request("GET", "/me/monthly-summary", options);
    },

    /** Mes bulletins de paie */
    getMePaySlips(options = {}) {
      return request("GET", "/me/pay-slips", options);
    },

    /** Mes bulletins de paie */
    getMePaySlips2(options = {}) {
      return request("GET", "/me/pay-slips", options);
    },

    /** Mes bulletins de paie */
    getMePaySlips3(options = {}) {
      return request("GET", "/me/pay-slips", options);
    },

    /** Mes bulletins de paie */
    getMePaySlips4(options = {}) {
      return request("GET", "/me/pay-slips", options);
    },

    /** Mes bulletins de paie */
    getMePaySlips5(options = {}) {
      return request("GET", "/me/pay-slips", options);
    },

    /** Mes bulletins de paie */
    getMePaySlips6(options = {}) {
      return request("GET", "/me/pay-slips", options);
    },

    /** Mes bulletins de paie */
    getMePaySlips7(options = {}) {
      return request("GET", "/me/pay-slips", options);
    },

    /** Mes bulletins de paie */
    getMePaySlips8(options = {}) {
      return request("GET", "/me/pay-slips", options);
    },

    /** Mes bulletins de paie */
    getMePaySlips9(options = {}) {
      return request("GET", "/me/pay-slips", options);
    },

    /** Mes bulletins de paie */
    getMePaySlips10(options = {}) {
      return request("GET", "/me/pay-slips", options);
    },

    /** Voir un de mes bulletins de paie */
    showMyPaySlip(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}", options);
    },

    /** Voir un de mes bulletins de paie */
    showMyPaySlip2(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}", options);
    },

    /** Voir un de mes bulletins de paie */
    showMyPaySlip3(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}", options);
    },

    /** Voir un de mes bulletins de paie */
    showMyPaySlip4(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}", options);
    },

    /** Voir un de mes bulletins de paie */
    showMyPaySlip5(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}", options);
    },

    /** Voir un de mes bulletins de paie */
    showMyPaySlip6(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}", options);
    },

    /** Voir un de mes bulletins de paie */
    showMyPaySlip7(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}", options);
    },

    /** Voir un de mes bulletins de paie */
    showMyPaySlip8(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}", options);
    },

    /** Voir un de mes bulletins de paie */
    showMyPaySlip9(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}", options);
    },

    /** Voir un de mes bulletins de paie */
    showMyPaySlip10(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}", options);
    },

    /** Telecharger mon bulletin archive dans le Cabinet (PDF) */
    archivedMyPaySlipDocument(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/document", options);
    },

    /** Telecharger mon bulletin archive dans le Cabinet (PDF) */
    archivedMyPaySlipDocument2(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/document", options);
    },

    /** Telecharger mon bulletin archive dans le Cabinet (PDF) */
    archivedMyPaySlipDocument3(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/document", options);
    },

    /** Telecharger mon bulletin archive dans le Cabinet (PDF) */
    archivedMyPaySlipDocument4(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/document", options);
    },

    /** Telecharger mon bulletin archive dans le Cabinet (PDF) */
    archivedMyPaySlipDocument5(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/document", options);
    },

    /** Telecharger mon bulletin archive dans le Cabinet (PDF) */
    archivedMyPaySlipDocument6(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/document", options);
    },

    /** Telecharger mon bulletin archive dans le Cabinet (PDF) */
    archivedMyPaySlipDocument7(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/document", options);
    },

    /** Telecharger mon bulletin archive dans le Cabinet (PDF) */
    archivedMyPaySlipDocument8(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/document", options);
    },

    /** Telecharger mon bulletin archive dans le Cabinet (PDF) */
    archivedMyPaySlipDocument9(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/document", options);
    },

    /** Telecharger mon bulletin archive dans le Cabinet (PDF) */
    archivedMyPaySlipDocument10(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/document", options);
    },

    /** Telecharger un de mes bulletins de paie en PDF */
    downloadMyPaySlipPdf(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/pdf", options);
    },

    /** Telecharger un de mes bulletins de paie en PDF */
    downloadMyPaySlipPdf2(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/pdf", options);
    },

    /** Telecharger un de mes bulletins de paie en PDF */
    downloadMyPaySlipPdf3(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/pdf", options);
    },

    /** Telecharger un de mes bulletins de paie en PDF */
    downloadMyPaySlipPdf4(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/pdf", options);
    },

    /** Telecharger un de mes bulletins de paie en PDF */
    downloadMyPaySlipPdf5(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/pdf", options);
    },

    /** Telecharger un de mes bulletins de paie en PDF */
    downloadMyPaySlipPdf6(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/pdf", options);
    },

    /** Telecharger un de mes bulletins de paie en PDF */
    downloadMyPaySlipPdf7(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/pdf", options);
    },

    /** Telecharger un de mes bulletins de paie en PDF */
    downloadMyPaySlipPdf8(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/pdf", options);
    },

    /** Telecharger un de mes bulletins de paie en PDF */
    downloadMyPaySlipPdf9(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/pdf", options);
    },

    /** Telecharger un de mes bulletins de paie en PDF */
    downloadMyPaySlipPdf10(options = {}) {
      return request("GET", "/me/pay-slips/{paySlip}/pdf", options);
    },

    /** Mes documents de paiement */
    getMePaymentDocuments(options = {}) {
      return request("GET", "/me/payment-documents", options);
    },

    /** Mes documents de paiement */
    getMePaymentDocuments2(options = {}) {
      return request("GET", "/me/payment-documents", options);
    },

    /** Mes documents de paiement */
    getMePaymentDocuments3(options = {}) {
      return request("GET", "/me/payment-documents", options);
    },

    /** Mes documents de paiement */
    getMePaymentDocuments4(options = {}) {
      return request("GET", "/me/payment-documents", options);
    },

    /** Mes documents de paiement */
    getMePaymentDocuments5(options = {}) {
      return request("GET", "/me/payment-documents", options);
    },

    /** Mes documents de paiement */
    getMePaymentDocuments6(options = {}) {
      return request("GET", "/me/payment-documents", options);
    },

    /** Mes documents de paiement */
    getMePaymentDocuments7(options = {}) {
      return request("GET", "/me/payment-documents", options);
    },

    /** Mes documents de paiement */
    getMePaymentDocuments8(options = {}) {
      return request("GET", "/me/payment-documents", options);
    },

    /** Mes documents de paiement */
    getMePaymentDocuments9(options = {}) {
      return request("GET", "/me/payment-documents", options);
    },

    /** Mes documents de paiement */
    getMePaymentDocuments10(options = {}) {
      return request("GET", "/me/payment-documents", options);
    },

    /** Telecharger un document de paiement */
    getMePaymentDocumentsByPaymentDocumentDownload(options = {}) {
      return request("GET", "/me/payment-documents/{paymentDocument}/download", options);
    },

    /** Telecharger un document de paiement */
    getMePaymentDocumentsByPaymentDocumentDownload2(options = {}) {
      return request("GET", "/me/payment-documents/{paymentDocument}/download", options);
    },

    /** Telecharger un document de paiement */
    getMePaymentDocumentsByPaymentDocumentDownload3(options = {}) {
      return request("GET", "/me/payment-documents/{paymentDocument}/download", options);
    },

    /** Telecharger un document de paiement */
    getMePaymentDocumentsByPaymentDocumentDownload4(options = {}) {
      return request("GET", "/me/payment-documents/{paymentDocument}/download", options);
    },

    /** Telecharger un document de paiement */
    getMePaymentDocumentsByPaymentDocumentDownload5(options = {}) {
      return request("GET", "/me/payment-documents/{paymentDocument}/download", options);
    },

    /** Telecharger un document de paiement */
    getMePaymentDocumentsByPaymentDocumentDownload6(options = {}) {
      return request("GET", "/me/payment-documents/{paymentDocument}/download", options);
    },

    /** Telecharger un document de paiement */
    getMePaymentDocumentsByPaymentDocumentDownload7(options = {}) {
      return request("GET", "/me/payment-documents/{paymentDocument}/download", options);
    },

    /** Telecharger un document de paiement */
    getMePaymentDocumentsByPaymentDocumentDownload8(options = {}) {
      return request("GET", "/me/payment-documents/{paymentDocument}/download", options);
    },

    /** Telecharger un document de paiement */
    getMePaymentDocumentsByPaymentDocumentDownload9(options = {}) {
      return request("GET", "/me/payment-documents/{paymentDocument}/download", options);
    },

    /** Telecharger un document de paiement */
    getMePaymentDocumentsByPaymentDocumentDownload10(options = {}) {
      return request("GET", "/me/payment-documents/{paymentDocument}/download", options);
    },

    /** Generer le QR profil employe */
    getMeQrProfile(options = {}) {
      return request("GET", "/me/qr-profile", options);
    },

    /** Generer le QR profil employe */
    getMeQrProfile2(options = {}) {
      return request("GET", "/me/qr-profile", options);
    },

    /** Generer le QR profil employe */
    getMeQrProfile3(options = {}) {
      return request("GET", "/me/qr-profile", options);
    },

    /** Generer le QR profil employe */
    getMeQrProfile4(options = {}) {
      return request("GET", "/me/qr-profile", options);
    },

    /** Generer le QR profil employe */
    getMeQrProfile5(options = {}) {
      return request("GET", "/me/qr-profile", options);
    },

    /** Estimation utilisateur courant */
    getMeQuickEstimate(options = {}) {
      return request("GET", "/me/quick-estimate", options);
    },

    /** Estimation utilisateur courant */
    getMeQuickEstimate2(options = {}) {
      return request("GET", "/me/quick-estimate", options);
    },

    /** Estimation utilisateur courant */
    getMeQuickEstimate3(options = {}) {
      return request("GET", "/me/quick-estimate", options);
    },

    /** Estimation utilisateur courant */
    getMeQuickEstimate4(options = {}) {
      return request("GET", "/me/quick-estimate", options);
    },

    /** Estimation utilisateur courant */
    getMeQuickEstimate5(options = {}) {
      return request("GET", "/me/quick-estimate", options);
    },

    /** Mes inscriptions formation (alias mobile employee) */
    getMeTrainingEnrollments(options = {}) {
      return request("GET", "/me/training-enrollments", options);
    },

    /** Mes inscriptions formation (alias mobile employee) */
    getMeTrainingEnrollments2(options = {}) {
      return request("GET", "/me/training-enrollments", options);
    },

    /** Mes inscriptions formation (alias mobile employee) */
    getMeTrainingEnrollments3(options = {}) {
      return request("GET", "/me/training-enrollments", options);
    },

    /** Mes inscriptions formation (alias mobile employee) */
    getMeTrainingEnrollments4(options = {}) {
      return request("GET", "/me/training-enrollments", options);
    },

    /** Mes inscriptions formation (alias mobile employee) */
    getMeTrainingEnrollments5(options = {}) {
      return request("GET", "/me/training-enrollments", options);
    },

    /** Mes inscriptions formation (alias mobile employee) */
    getMeTrainingEnrollments6(options = {}) {
      return request("GET", "/me/training-enrollments", options);
    },

    /** Mes inscriptions formation (alias mobile employee) */
    getMeTrainingEnrollments7(options = {}) {
      return request("GET", "/me/training-enrollments", options);
    },

    /** Mes inscriptions formation (alias mobile employee) */
    getMeTrainingEnrollments8(options = {}) {
      return request("GET", "/me/training-enrollments", options);
    },

    /** Mes inscriptions formation (alias mobile employee) */
    getMeTrainingEnrollments9(options = {}) {
      return request("GET", "/me/training-enrollments", options);
    },

    /** Mes inscriptions formation (alias mobile employee) */
    getMeTrainingEnrollments10(options = {}) {
      return request("GET", "/me/training-enrollments", options);
    },

    /** Mes formations */
    getMeTrainings(options = {}) {
      return request("GET", "/me/trainings", options);
    },

    /** Mes formations */
    getMeTrainings2(options = {}) {
      return request("GET", "/me/trainings", options);
    },

    /** Mes formations */
    getMeTrainings3(options = {}) {
      return request("GET", "/me/trainings", options);
    },

    /** Mes formations */
    getMeTrainings4(options = {}) {
      return request("GET", "/me/trainings", options);
    },

    /** Mes formations */
    getMeTrainings5(options = {}) {
      return request("GET", "/me/trainings", options);
    },

    /** Mes formations */
    getMeTrainings6(options = {}) {
      return request("GET", "/me/trainings", options);
    },

    /** Mes formations */
    getMeTrainings7(options = {}) {
      return request("GET", "/me/trainings", options);
    },

    /** Mes formations */
    getMeTrainings8(options = {}) {
      return request("GET", "/me/trainings", options);
    },

    /** Mes formations */
    getMeTrainings9(options = {}) {
      return request("GET", "/me/trainings", options);
    },

    /** Mes formations */
    getMeTrainings10(options = {}) {
      return request("GET", "/me/trainings", options);
    },

    /** Auto-inscription a une formation */
    postMeTrainingsBySessionEnroll(options = {}) {
      return request("POST", "/me/trainings/{session}/enroll", options);
    },

    /** Auto-inscription a une formation */
    postMeTrainingsBySessionEnroll2(options = {}) {
      return request("POST", "/me/trainings/{session}/enroll", options);
    },

    /** Auto-inscription a une formation */
    postMeTrainingsBySessionEnroll3(options = {}) {
      return request("POST", "/me/trainings/{session}/enroll", options);
    },

    /** Auto-inscription a une formation */
    postMeTrainingsBySessionEnroll4(options = {}) {
      return request("POST", "/me/trainings/{session}/enroll", options);
    },

    /** Auto-inscription a une formation */
    postMeTrainingsBySessionEnroll5(options = {}) {
      return request("POST", "/me/trainings/{session}/enroll", options);
    },

    /** Auto-inscription a une formation */
    postMeTrainingsBySessionEnroll6(options = {}) {
      return request("POST", "/me/trainings/{session}/enroll", options);
    },

    /** Auto-inscription a une formation */
    postMeTrainingsBySessionEnroll7(options = {}) {
      return request("POST", "/me/trainings/{session}/enroll", options);
    },

    /** Auto-inscription a une formation */
    postMeTrainingsBySessionEnroll8(options = {}) {
      return request("POST", "/me/trainings/{session}/enroll", options);
    },

    /** Auto-inscription a une formation */
    postMeTrainingsBySessionEnroll9(options = {}) {
      return request("POST", "/me/trainings/{session}/enroll", options);
    },

    /** Auto-inscription a une formation */
    postMeTrainingsBySessionEnroll10(options = {}) {
      return request("POST", "/me/trainings/{session}/enroll", options);
    },

    /** Vehicules assignes a l'employe connecte (mobile) */
    getMeVehicles(options = {}) {
      return request("GET", "/me/vehicles", options);
    },

    /** Vehicules assignes a l'employe connecte (mobile) */
    getMeVehicles2(options = {}) {
      return request("GET", "/me/vehicles", options);
    },

    /** Vehicules assignes a l'employe connecte (mobile) */
    getMeVehicles3(options = {}) {
      return request("GET", "/me/vehicles", options);
    },

    /** Vehicules assignes a l'employe connecte (mobile) */
    getMeVehicles4(options = {}) {
      return request("GET", "/me/vehicles", options);
    },

    /** Vehicules assignes a l'employe connecte (mobile) */
    getMeVehicles5(options = {}) {
      return request("GET", "/me/vehicles", options);
    },

    /** Vehicules assignes a l'employe connecte (mobile) */
    getMeVehicles6(options = {}) {
      return request("GET", "/me/vehicles", options);
    },

    /** Vehicules assignes a l'employe connecte (mobile) */
    getMeVehicles7(options = {}) {
      return request("GET", "/me/vehicles", options);
    },

    /** Vehicules assignes a l'employe connecte (mobile) */
    getMeVehicles8(options = {}) {
      return request("GET", "/me/vehicles", options);
    },

    /** Métriques plateforme (super-admin) */
    getMetrics(options = {}) {
      return request("GET", "/metrics", options);
    },

    /** Métriques plateforme (super-admin) */
    getMetrics2(options = {}) {
      return request("GET", "/metrics", options);
    },

    /** Métriques plateforme (super-admin) */
    getMetrics3(options = {}) {
      return request("GET", "/metrics", options);
    },

    /** Métriques plateforme (super-admin) */
    getMetrics4(options = {}) {
      return request("GET", "/metrics", options);
    },

    /** Métriques plateforme (super-admin) */
    getMetrics5(options = {}) {
      return request("GET", "/metrics", options);
    },

    /** Lire les preferences de notification de l'utilisateur courant */
    getNotificationPreferences(options = {}) {
      return request("GET", "/notification-preferences", options);
    },

    /** Lire les preferences de notification de l'utilisateur courant */
    getNotificationPreferences2(options = {}) {
      return request("GET", "/notification-preferences", options);
    },

    /** Lire les preferences de notification de l'utilisateur courant */
    getNotificationPreferences3(options = {}) {
      return request("GET", "/notification-preferences", options);
    },

    /** Lire les preferences de notification de l'utilisateur courant */
    getNotificationPreferences4(options = {}) {
      return request("GET", "/notification-preferences", options);
    },

    /** Lire les preferences de notification de l'utilisateur courant */
    getNotificationPreferences5(options = {}) {
      return request("GET", "/notification-preferences", options);
    },

    /** Lire les preferences de notification de l'utilisateur courant */
    getNotificationPreferences6(options = {}) {
      return request("GET", "/notification-preferences", options);
    },

    /** Lire les preferences de notification de l'utilisateur courant */
    getNotificationPreferences7(options = {}) {
      return request("GET", "/notification-preferences", options);
    },

    /** Lire les preferences de notification de l'utilisateur courant */
    getNotificationPreferences8(options = {}) {
      return request("GET", "/notification-preferences", options);
    },

    /** Mettre a jour les preferences de notification de l'utilisateur courant */
    patchNotificationPreferences(options = {}) {
      return request("PATCH", "/notification-preferences", options);
    },

    /** Mettre a jour les preferences de notification de l'utilisateur courant */
    patchNotificationPreferences2(options = {}) {
      return request("PATCH", "/notification-preferences", options);
    },

    /** Mettre a jour les preferences de notification de l'utilisateur courant */
    patchNotificationPreferences3(options = {}) {
      return request("PATCH", "/notification-preferences", options);
    },

    /** Mettre a jour les preferences de notification de l'utilisateur courant */
    patchNotificationPreferences4(options = {}) {
      return request("PATCH", "/notification-preferences", options);
    },

    /** Mettre a jour les preferences de notification de l'utilisateur courant */
    patchNotificationPreferences5(options = {}) {
      return request("PATCH", "/notification-preferences", options);
    },

    /** Mettre a jour les preferences de notification de l'utilisateur courant */
    patchNotificationPreferences6(options = {}) {
      return request("PATCH", "/notification-preferences", options);
    },

    /** Mettre a jour les preferences de notification de l'utilisateur courant */
    patchNotificationPreferences7(options = {}) {
      return request("PATCH", "/notification-preferences", options);
    },

    /** Mettre a jour les preferences de notification de l'utilisateur courant */
    patchNotificationPreferences8(options = {}) {
      return request("PATCH", "/notification-preferences", options);
    },

    /** Lister les notifications de l'utilisateur courant */
    getNotifications(options = {}) {
      return request("GET", "/notifications", options);
    },

    /** Lister les notifications de l'utilisateur courant */
    getNotifications2(options = {}) {
      return request("GET", "/notifications", options);
    },

    /** Lister les notifications de l'utilisateur courant */
    getNotifications3(options = {}) {
      return request("GET", "/notifications", options);
    },

    /** Lister les notifications de l'utilisateur courant */
    getNotifications4(options = {}) {
      return request("GET", "/notifications", options);
    },

    /** Lister les notifications de l'utilisateur courant */
    getNotifications5(options = {}) {
      return request("GET", "/notifications", options);
    },

    /** Lister les notifications de l'utilisateur courant */
    getNotifications6(options = {}) {
      return request("GET", "/notifications", options);
    },

    /** Lister les notifications de l'utilisateur courant */
    getNotifications7(options = {}) {
      return request("GET", "/notifications", options);
    },

    /** Lister les notifications de l'utilisateur courant */
    getNotifications8(options = {}) {
      return request("GET", "/notifications", options);
    },

    /** Supprimer une notification */
    deleteNotificationsByNotification(options = {}) {
      return request("DELETE", "/notifications/{notification}", options);
    },

    /** Supprimer une notification */
    deleteNotificationsByNotification2(options = {}) {
      return request("DELETE", "/notifications/{notification}", options);
    },

    /** Supprimer une notification */
    deleteNotificationsByNotification3(options = {}) {
      return request("DELETE", "/notifications/{notification}", options);
    },

    /** Supprimer une notification */
    deleteNotificationsByNotification4(options = {}) {
      return request("DELETE", "/notifications/{notification}", options);
    },

    /** Supprimer une notification */
    deleteNotificationsByNotification5(options = {}) {
      return request("DELETE", "/notifications/{notification}", options);
    },

    /** Supprimer une notification */
    deleteNotificationsByNotification6(options = {}) {
      return request("DELETE", "/notifications/{notification}", options);
    },

    /** Supprimer une notification */
    deleteNotificationsByNotification7(options = {}) {
      return request("DELETE", "/notifications/{notification}", options);
    },

    /** Supprimer une notification */
    deleteNotificationsByNotification8(options = {}) {
      return request("DELETE", "/notifications/{notification}", options);
    },

    /** Marquer une notification comme lue */
    patchNotificationsByNotificationRead(options = {}) {
      return request("PATCH", "/notifications/{notification}/read", options);
    },

    /** Marquer une notification comme lue */
    patchNotificationsByNotificationRead2(options = {}) {
      return request("PATCH", "/notifications/{notification}/read", options);
    },

    /** Marquer une notification comme lue */
    patchNotificationsByNotificationRead3(options = {}) {
      return request("PATCH", "/notifications/{notification}/read", options);
    },

    /** Marquer une notification comme lue */
    patchNotificationsByNotificationRead4(options = {}) {
      return request("PATCH", "/notifications/{notification}/read", options);
    },

    /** Marquer une notification comme lue */
    patchNotificationsByNotificationRead5(options = {}) {
      return request("PATCH", "/notifications/{notification}/read", options);
    },

    /** Marquer une notification comme lue */
    patchNotificationsByNotificationRead6(options = {}) {
      return request("PATCH", "/notifications/{notification}/read", options);
    },

    /** Marquer une notification comme lue */
    patchNotificationsByNotificationRead7(options = {}) {
      return request("PATCH", "/notifications/{notification}/read", options);
    },

    /** Marquer une notification comme lue */
    patchNotificationsByNotificationRead8(options = {}) {
      return request("PATCH", "/notifications/{notification}/read", options);
    },

    /** Marquer toutes les notifications comme lues */
    postNotificationsMarkAllRead(options = {}) {
      return request("POST", "/notifications/mark-all-read", options);
    },

    /** Marquer toutes les notifications comme lues */
    postNotificationsMarkAllRead2(options = {}) {
      return request("POST", "/notifications/mark-all-read", options);
    },

    /** Marquer toutes les notifications comme lues */
    postNotificationsMarkAllRead3(options = {}) {
      return request("POST", "/notifications/mark-all-read", options);
    },

    /** Marquer toutes les notifications comme lues */
    postNotificationsMarkAllRead4(options = {}) {
      return request("POST", "/notifications/mark-all-read", options);
    },

    /** Marquer toutes les notifications comme lues */
    postNotificationsMarkAllRead5(options = {}) {
      return request("POST", "/notifications/mark-all-read", options);
    },

    /** Marquer toutes les notifications comme lues */
    postNotificationsMarkAllRead6(options = {}) {
      return request("POST", "/notifications/mark-all-read", options);
    },

    /** Marquer toutes les notifications comme lues */
    postNotificationsMarkAllRead7(options = {}) {
      return request("POST", "/notifications/mark-all-read", options);
    },

    /** Marquer toutes les notifications comme lues */
    postNotificationsMarkAllRead8(options = {}) {
      return request("POST", "/notifications/mark-all-read", options);
    },

    /** Marquer toutes les notifications comme lues */
    postNotificationsMarkAllRead9(options = {}) {
      return request("POST", "/notifications/mark-all-read", options);
    },

    /** Marquer toutes les notifications comme lues */
    postNotificationsMarkAllRead10(options = {}) {
      return request("POST", "/notifications/mark-all-read", options);
    },

    /** Marquer toutes les notifications comme lues */
    postNotificationsMarkAllRead11(options = {}) {
      return request("POST", "/notifications/mark-all-read", options);
    },

    /** Marquer toutes les notifications comme lues */
    postNotificationsReadAll(options = {}) {
      return request("POST", "/notifications/read-all", options);
    },

    /** Marquer toutes les notifications comme lues */
    postNotificationsReadAll2(options = {}) {
      return request("POST", "/notifications/read-all", options);
    },

    /** Marquer toutes les notifications comme lues */
    postNotificationsReadAll3(options = {}) {
      return request("POST", "/notifications/read-all", options);
    },

    /** Marquer toutes les notifications comme lues */
    postNotificationsReadAll4(options = {}) {
      return request("POST", "/notifications/read-all", options);
    },

    /** Marquer toutes les notifications comme lues */
    postNotificationsReadAll5(options = {}) {
      return request("POST", "/notifications/read-all", options);
    },

    /** Marquer toutes les notifications comme lues */
    postNotificationsReadAll6(options = {}) {
      return request("POST", "/notifications/read-all", options);
    },

    /** Marquer toutes les notifications comme lues */
    postNotificationsReadAll7(options = {}) {
      return request("POST", "/notifications/read-all", options);
    },

    /** Marquer toutes les notifications comme lues */
    postNotificationsReadAll8(options = {}) {
      return request("POST", "/notifications/read-all", options);
    },

    /** Émettre un token SSE temporaire pour le flux temps réel */
    postNotificationsSseToken(options = {}) {
      return request("POST", "/notifications/sse-token", options);
    },

    /** Émettre un token SSE temporaire pour le flux temps réel */
    postNotificationsSseToken2(options = {}) {
      return request("POST", "/notifications/sse-token", options);
    },

    /** Émettre un token SSE temporaire pour le flux temps réel */
    postNotificationsSseToken3(options = {}) {
      return request("POST", "/notifications/sse-token", options);
    },

    /** Émettre un token SSE temporaire pour le flux temps réel */
    postNotificationsSseToken4(options = {}) {
      return request("POST", "/notifications/sse-token", options);
    },

    /** Émettre un token SSE temporaire pour le flux temps réel */
    postNotificationsSseToken5(options = {}) {
      return request("POST", "/notifications/sse-token", options);
    },

    /** Émettre un token SSE temporaire pour le flux temps réel */
    postNotificationsSseToken6(options = {}) {
      return request("POST", "/notifications/sse-token", options);
    },

    /** Émettre un token SSE temporaire pour le flux temps réel */
    postNotificationsSseToken7(options = {}) {
      return request("POST", "/notifications/sse-token", options);
    },

    /** Émettre un token SSE temporaire pour le flux temps réel */
    postNotificationsSseToken8(options = {}) {
      return request("POST", "/notifications/sse-token", options);
    },

    /** Émettre un token SSE temporaire pour le flux temps réel */
    postNotificationsSseToken9(options = {}) {
      return request("POST", "/notifications/sse-token", options);
    },

    /** Émettre un token SSE temporaire pour le flux temps réel */
    postNotificationsSseToken10(options = {}) {
      return request("POST", "/notifications/sse-token", options);
    },

    /** Émettre un token SSE temporaire pour le flux temps réel */
    postNotificationsSseToken11(options = {}) {
      return request("POST", "/notifications/sse-token", options);
    },

    /** Flux SSE temps réel des notifications (employee/manager) */
    getNotificationsStream(options = {}) {
      return request("GET", "/notifications/stream", options);
    },

    /** Flux SSE temps réel des notifications (employee/manager) */
    getNotificationsStream2(options = {}) {
      return request("GET", "/notifications/stream", options);
    },

    /** Flux SSE temps réel des notifications (employee/manager) */
    getNotificationsStream3(options = {}) {
      return request("GET", "/notifications/stream", options);
    },

    /** Flux SSE temps réel des notifications (employee/manager) */
    getNotificationsStream4(options = {}) {
      return request("GET", "/notifications/stream", options);
    },

    /** Flux SSE temps réel des notifications (employee/manager) */
    getNotificationsStream5(options = {}) {
      return request("GET", "/notifications/stream", options);
    },

    /** Flux SSE temps réel des notifications (employee/manager) */
    getNotificationsStream6(options = {}) {
      return request("GET", "/notifications/stream", options);
    },

    /** Flux SSE temps réel des notifications (employee/manager) */
    getNotificationsStream7(options = {}) {
      return request("GET", "/notifications/stream", options);
    },

    /** Flux SSE temps réel des notifications (employee/manager) */
    getNotificationsStream8(options = {}) {
      return request("GET", "/notifications/stream", options);
    },

    /** Flux SSE temps réel des notifications (employee/manager) */
    getNotificationsStream9(options = {}) {
      return request("GET", "/notifications/stream", options);
    },

    /** Flux SSE temps réel des notifications (employee/manager) */
    getNotificationsStream10(options = {}) {
      return request("GET", "/notifications/stream", options);
    },

    /** Flux SSE temps réel des notifications (employee/manager) */
    getNotificationsStream11(options = {}) {
      return request("GET", "/notifications/stream", options);
    },

    /** Notifications non lues de l'employe */
    getNotificationsUnread(options = {}) {
      return request("GET", "/notifications/unread", options);
    },

    /** Notifications non lues de l'employe */
    getNotificationsUnread2(options = {}) {
      return request("GET", "/notifications/unread", options);
    },

    /** Notifications non lues de l'employe */
    getNotificationsUnread3(options = {}) {
      return request("GET", "/notifications/unread", options);
    },

    /** Notifications non lues de l'employe */
    getNotificationsUnread4(options = {}) {
      return request("GET", "/notifications/unread", options);
    },

    /** Notifications non lues de l'employe */
    getNotificationsUnread5(options = {}) {
      return request("GET", "/notifications/unread", options);
    },

    /** Notifications non lues de l'employe */
    getNotificationsUnread6(options = {}) {
      return request("GET", "/notifications/unread", options);
    },

    /** Notifications non lues de l'employe */
    getNotificationsUnread7(options = {}) {
      return request("GET", "/notifications/unread", options);
    },

    /** Notifications non lues de l'employe */
    getNotificationsUnread8(options = {}) {
      return request("GET", "/notifications/unread", options);
    },

    /** Notifications non lues de l'employe */
    getNotificationsUnread9(options = {}) {
      return request("GET", "/notifications/unread", options);
    },

    /** Notifications non lues de l'employe */
    getNotificationsUnread10(options = {}) {
      return request("GET", "/notifications/unread", options);
    },

    /** Notifications non lues de l'employe */
    getNotificationsUnread11(options = {}) {
      return request("GET", "/notifications/unread", options);
    },

    /** Marquer une étape complète (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeyComplete(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape complète (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeyComplete2(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape complète (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeyComplete3(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape complète (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeyComplete4(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape complète (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeyComplete5(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape complète (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeyComplete6(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape complète (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeyComplete7(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape complète (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeyComplete8(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape complète (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeyComplete9(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape complète (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeyComplete10(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape complète (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeyComplete11(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape comme complète */
    postOnboardingSetupByStepKeyComplete(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape comme complète */
    postOnboardingSetupByStepKeyComplete2(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape comme complète */
    postOnboardingSetupByStepKeyComplete3(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape comme complète */
    postOnboardingSetupByStepKeyComplete4(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape comme complète */
    postOnboardingSetupByStepKeyComplete5(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape comme complète */
    postOnboardingSetupByStepKeyComplete6(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape comme complète */
    postOnboardingSetupByStepKeyComplete7(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape comme complète */
    postOnboardingSetupByStepKeyComplete8(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape comme complète */
    postOnboardingSetupByStepKeyComplete9(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape comme complète */
    postOnboardingSetupByStepKeyComplete10(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Marquer une étape comme complète */
    postOnboardingSetupByStepKeyComplete11(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/complete", options);
    },

    /** Passer une étape (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeySkip(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Passer une étape (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeySkip2(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Passer une étape (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeySkip3(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Passer une étape (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeySkip4(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Passer une étape (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeySkip5(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Passer une étape (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeySkip6(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Passer une étape (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeySkip7(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Passer une étape (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeySkip8(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Passer une étape (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeySkip9(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Passer une étape (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeySkip10(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Passer une étape (déprécié — utiliser POST) */
    patchOnboardingSetupByStepKeySkip11(options = {}) {
      return request("PATCH", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Ignorer une étape d'onboarding */
    postOnboardingSetupByStepKeySkip(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Ignorer une étape d'onboarding */
    postOnboardingSetupByStepKeySkip2(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Ignorer une étape d'onboarding */
    postOnboardingSetupByStepKeySkip3(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Ignorer une étape d'onboarding */
    postOnboardingSetupByStepKeySkip4(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Ignorer une étape d'onboarding */
    postOnboardingSetupByStepKeySkip5(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Ignorer une étape d'onboarding */
    postOnboardingSetupByStepKeySkip6(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Ignorer une étape d'onboarding */
    postOnboardingSetupByStepKeySkip7(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Ignorer une étape d'onboarding */
    postOnboardingSetupByStepKeySkip8(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Ignorer une étape d'onboarding */
    postOnboardingSetupByStepKeySkip9(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Ignorer une étape d'onboarding */
    postOnboardingSetupByStepKeySkip10(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Ignorer une étape d'onboarding */
    postOnboardingSetupByStepKeySkip11(options = {}) {
      return request("POST", "/onboarding-setup/{stepKey}/skip", options);
    },

    /** Récupérer la checklist d'onboarding */
    getOnboardingSetupChecklist(options = {}) {
      return request("GET", "/onboarding-setup/checklist", options);
    },

    /** Récupérer la checklist d'onboarding */
    getOnboardingSetupChecklist2(options = {}) {
      return request("GET", "/onboarding-setup/checklist", options);
    },

    /** Récupérer la checklist d'onboarding */
    getOnboardingSetupChecklist3(options = {}) {
      return request("GET", "/onboarding-setup/checklist", options);
    },

    /** Récupérer la checklist d'onboarding */
    getOnboardingSetupChecklist4(options = {}) {
      return request("GET", "/onboarding-setup/checklist", options);
    },

    /** Récupérer la checklist d'onboarding */
    getOnboardingSetupChecklist5(options = {}) {
      return request("GET", "/onboarding-setup/checklist", options);
    },

    /** Récupérer la checklist d'onboarding */
    getOnboardingSetupChecklist6(options = {}) {
      return request("GET", "/onboarding-setup/checklist", options);
    },

    /** Récupérer la checklist d'onboarding */
    getOnboardingSetupChecklist7(options = {}) {
      return request("GET", "/onboarding-setup/checklist", options);
    },

    /** Récupérer la checklist d'onboarding */
    getOnboardingSetupChecklist8(options = {}) {
      return request("GET", "/onboarding-setup/checklist", options);
    },

    /** Récupérer la checklist d'onboarding */
    getOnboardingSetupChecklist9(options = {}) {
      return request("GET", "/onboarding-setup/checklist", options);
    },

    /** Récupérer la checklist d'onboarding */
    getOnboardingSetupChecklist10(options = {}) {
      return request("GET", "/onboarding-setup/checklist", options);
    },

    /** Récupérer la checklist d'onboarding */
    getOnboardingSetupChecklist11(options = {}) {
      return request("GET", "/onboarding-setup/checklist", options);
    },

    /** Progression globale de l'onboarding */
    getOnboardingSetupProgress(options = {}) {
      return request("GET", "/onboarding-setup/progress", options);
    },

    /** Progression globale de l'onboarding */
    getOnboardingSetupProgress2(options = {}) {
      return request("GET", "/onboarding-setup/progress", options);
    },

    /** Progression globale de l'onboarding */
    getOnboardingSetupProgress3(options = {}) {
      return request("GET", "/onboarding-setup/progress", options);
    },

    /** Progression globale de l'onboarding */
    getOnboardingSetupProgress4(options = {}) {
      return request("GET", "/onboarding-setup/progress", options);
    },

    /** Progression globale de l'onboarding */
    getOnboardingSetupProgress5(options = {}) {
      return request("GET", "/onboarding-setup/progress", options);
    },

    /** Progression globale de l'onboarding */
    getOnboardingSetupProgress6(options = {}) {
      return request("GET", "/onboarding-setup/progress", options);
    },

    /** Progression globale de l'onboarding */
    getOnboardingSetupProgress7(options = {}) {
      return request("GET", "/onboarding-setup/progress", options);
    },

    /** Progression globale de l'onboarding */
    getOnboardingSetupProgress8(options = {}) {
      return request("GET", "/onboarding-setup/progress", options);
    },

    /** Progression globale de l'onboarding */
    getOnboardingSetupProgress9(options = {}) {
      return request("GET", "/onboarding-setup/progress", options);
    },

    /** Progression globale de l'onboarding */
    getOnboardingSetupProgress10(options = {}) {
      return request("GET", "/onboarding-setup/progress", options);
    },

    /** Progression globale de l'onboarding */
    getOnboardingSetupProgress11(options = {}) {
      return request("GET", "/onboarding-setup/progress", options);
    },

    /** Checklist d'onboarding de l'employé authentifié */
    getOnboardingChecklist(options = {}) {
      return request("GET", "/onboarding/checklist", options);
    },

    /** Checklist d'onboarding de l'employé authentifié */
    getOnboardingChecklist2(options = {}) {
      return request("GET", "/onboarding/checklist", options);
    },

    /** Checklist d'onboarding de l'employé authentifié */
    getOnboardingChecklist3(options = {}) {
      return request("GET", "/onboarding/checklist", options);
    },

    /** Checklist d'onboarding de l'employé authentifié */
    getOnboardingChecklist4(options = {}) {
      return request("GET", "/onboarding/checklist", options);
    },

    /** Checklist d'onboarding de l'employé authentifié */
    getOnboardingChecklist5(options = {}) {
      return request("GET", "/onboarding/checklist", options);
    },

    /** Consulter une invitation d'onboarding (token public) */
    getOnboardingInvitationByToken(options = {}) {
      return request("GET", "/onboarding/invitation/{token}", options);
    },

    /** Consulter une invitation d'onboarding (token public) */
    getOnboardingInvitationByToken2(options = {}) {
      return request("GET", "/onboarding/invitation/{token}", options);
    },

    /** Consulter une invitation d'onboarding (token public) */
    getOnboardingInvitationByToken3(options = {}) {
      return request("GET", "/onboarding/invitation/{token}", options);
    },

    /** Consulter une invitation d'onboarding (token public) */
    getOnboardingInvitationByToken4(options = {}) {
      return request("GET", "/onboarding/invitation/{token}", options);
    },

    /** Consulter une invitation d'onboarding (token public) */
    getOnboardingInvitationByToken5(options = {}) {
      return request("GET", "/onboarding/invitation/{token}", options);
    },

    /** Activer une invitation (définir le mot de passe) */
    postOnboardingInvitationByTokenActivate(options = {}) {
      return request("POST", "/onboarding/invitation/{token}/activate", options);
    },

    /** Activer une invitation (définir le mot de passe) */
    postOnboardingInvitationByTokenActivate2(options = {}) {
      return request("POST", "/onboarding/invitation/{token}/activate", options);
    },

    /** Activer une invitation (définir le mot de passe) */
    postOnboardingInvitationByTokenActivate3(options = {}) {
      return request("POST", "/onboarding/invitation/{token}/activate", options);
    },

    /** Activer une invitation (définir le mot de passe) */
    postOnboardingInvitationByTokenActivate4(options = {}) {
      return request("POST", "/onboarding/invitation/{token}/activate", options);
    },

    /** Activer une invitation (définir le mot de passe) */
    postOnboardingInvitationByTokenActivate5(options = {}) {
      return request("POST", "/onboarding/invitation/{token}/activate", options);
    },

    /** Organigramme complet */
    getOrgChart(options = {}) {
      return request("GET", "/org-chart", options);
    },

    /** Organigramme complet */
    getOrgChart2(options = {}) {
      return request("GET", "/org-chart", options);
    },

    /** Organigramme complet */
    getOrgChart3(options = {}) {
      return request("GET", "/org-chart", options);
    },

    /** Organigramme complet */
    getOrgChart4(options = {}) {
      return request("GET", "/org-chart", options);
    },

    /** Organigramme complet */
    getOrgChart5(options = {}) {
      return request("GET", "/org-chart", options);
    },

    /** Organigramme complet */
    getOrgChart6(options = {}) {
      return request("GET", "/org-chart", options);
    },

    /** Organigramme complet */
    getOrgChart7(options = {}) {
      return request("GET", "/org-chart", options);
    },

    /** Organigramme complet */
    getOrgChart8(options = {}) {
      return request("GET", "/org-chart", options);
    },

    /** Organigramme complet */
    getOrgChart9(options = {}) {
      return request("GET", "/org-chart", options);
    },

    /** Organigramme complet */
    getOrgChart10(options = {}) {
      return request("GET", "/org-chart", options);
    },

    /** Organigramme complet */
    getOrgChart11(options = {}) {
      return request("GET", "/org-chart", options);
    },

    /** Chaine hierarchique */
    getOrgChartByEmployeeIdManagerChain(options = {}) {
      return request("GET", "/org-chart/{employeeId}/manager-chain", options);
    },

    /** Chaine hierarchique */
    getOrgChartByEmployeeIdManagerChain2(options = {}) {
      return request("GET", "/org-chart/{employeeId}/manager-chain", options);
    },

    /** Chaine hierarchique */
    getOrgChartByEmployeeIdManagerChain3(options = {}) {
      return request("GET", "/org-chart/{employeeId}/manager-chain", options);
    },

    /** Chaine hierarchique */
    getOrgChartByEmployeeIdManagerChain4(options = {}) {
      return request("GET", "/org-chart/{employeeId}/manager-chain", options);
    },

    /** Chaine hierarchique */
    getOrgChartByEmployeeIdManagerChain5(options = {}) {
      return request("GET", "/org-chart/{employeeId}/manager-chain", options);
    },

    /** Chaine hierarchique */
    getOrgChartByEmployeeIdManagerChain6(options = {}) {
      return request("GET", "/org-chart/{employeeId}/manager-chain", options);
    },

    /** Chaine hierarchique */
    getOrgChartByEmployeeIdManagerChain7(options = {}) {
      return request("GET", "/org-chart/{employeeId}/manager-chain", options);
    },

    /** Chaine hierarchique */
    getOrgChartByEmployeeIdManagerChain8(options = {}) {
      return request("GET", "/org-chart/{employeeId}/manager-chain", options);
    },

    /** Chaine hierarchique */
    getOrgChartByEmployeeIdManagerChain9(options = {}) {
      return request("GET", "/org-chart/{employeeId}/manager-chain", options);
    },

    /** Chaine hierarchique */
    getOrgChartByEmployeeIdManagerChain10(options = {}) {
      return request("GET", "/org-chart/{employeeId}/manager-chain", options);
    },

    /** Chaine hierarchique */
    getOrgChartByEmployeeIdManagerChain11(options = {}) {
      return request("GET", "/org-chart/{employeeId}/manager-chain", options);
    },

    /** Subordonnes d'un employe */
    getOrgChartByEmployeeIdSubordinates(options = {}) {
      return request("GET", "/org-chart/{employeeId}/subordinates", options);
    },

    /** Subordonnes d'un employe */
    getOrgChartByEmployeeIdSubordinates2(options = {}) {
      return request("GET", "/org-chart/{employeeId}/subordinates", options);
    },

    /** Subordonnes d'un employe */
    getOrgChartByEmployeeIdSubordinates3(options = {}) {
      return request("GET", "/org-chart/{employeeId}/subordinates", options);
    },

    /** Subordonnes d'un employe */
    getOrgChartByEmployeeIdSubordinates4(options = {}) {
      return request("GET", "/org-chart/{employeeId}/subordinates", options);
    },

    /** Subordonnes d'un employe */
    getOrgChartByEmployeeIdSubordinates5(options = {}) {
      return request("GET", "/org-chart/{employeeId}/subordinates", options);
    },

    /** Subordonnes d'un employe */
    getOrgChartByEmployeeIdSubordinates6(options = {}) {
      return request("GET", "/org-chart/{employeeId}/subordinates", options);
    },

    /** Subordonnes d'un employe */
    getOrgChartByEmployeeIdSubordinates7(options = {}) {
      return request("GET", "/org-chart/{employeeId}/subordinates", options);
    },

    /** Subordonnes d'un employe */
    getOrgChartByEmployeeIdSubordinates8(options = {}) {
      return request("GET", "/org-chart/{employeeId}/subordinates", options);
    },

    /** Subordonnes d'un employe */
    getOrgChartByEmployeeIdSubordinates9(options = {}) {
      return request("GET", "/org-chart/{employeeId}/subordinates", options);
    },

    /** Subordonnes d'un employe */
    getOrgChartByEmployeeIdSubordinates10(options = {}) {
      return request("GET", "/org-chart/{employeeId}/subordinates", options);
    },

    /** Subordonnes d'un employe */
    getOrgChartByEmployeeIdSubordinates11(options = {}) {
      return request("GET", "/org-chart/{employeeId}/subordinates", options);
    },

    /** Lister les bulletins du tenant (manager) */
    getPaySlips(options = {}) {
      return request("GET", "/pay-slips", options);
    },

    /** Lister les bulletins du tenant (manager) */
    getPaySlips2(options = {}) {
      return request("GET", "/pay-slips", options);
    },

    /** Lister les bulletins du tenant (manager) */
    getPaySlips3(options = {}) {
      return request("GET", "/pay-slips", options);
    },

    /** Lister les bulletins du tenant (manager) */
    getPaySlips4(options = {}) {
      return request("GET", "/pay-slips", options);
    },

    /** Lister les bulletins du tenant (manager) */
    getPaySlips5(options = {}) {
      return request("GET", "/pay-slips", options);
    },

    /** Voir un bulletin de paie */
    getPaySlipsByPaySlip(options = {}) {
      return request("GET", "/pay-slips/{paySlip}", options);
    },

    /** Voir un bulletin de paie */
    getPaySlipsByPaySlip2(options = {}) {
      return request("GET", "/pay-slips/{paySlip}", options);
    },

    /** Voir un bulletin de paie */
    getPaySlipsByPaySlip3(options = {}) {
      return request("GET", "/pay-slips/{paySlip}", options);
    },

    /** Voir un bulletin de paie */
    getPaySlipsByPaySlip4(options = {}) {
      return request("GET", "/pay-slips/{paySlip}", options);
    },

    /** Voir un bulletin de paie */
    getPaySlipsByPaySlip5(options = {}) {
      return request("GET", "/pay-slips/{paySlip}", options);
    },

    /** Voir un bulletin de paie */
    getPaySlipsByPaySlip6(options = {}) {
      return request("GET", "/pay-slips/{paySlip}", options);
    },

    /** Voir un bulletin de paie */
    getPaySlipsByPaySlip7(options = {}) {
      return request("GET", "/pay-slips/{paySlip}", options);
    },

    /** Voir un bulletin de paie */
    getPaySlipsByPaySlip8(options = {}) {
      return request("GET", "/pay-slips/{paySlip}", options);
    },

    /** Voir un bulletin de paie */
    getPaySlipsByPaySlip9(options = {}) {
      return request("GET", "/pay-slips/{paySlip}", options);
    },

    /** Voir un bulletin de paie */
    getPaySlipsByPaySlip10(options = {}) {
      return request("GET", "/pay-slips/{paySlip}", options);
    },

    /** Telecharger bulletin PDF */
    getPaySlipsByPaySlipPdf(options = {}) {
      return request("GET", "/pay-slips/{paySlip}/pdf", options);
    },

    /** Telecharger bulletin PDF */
    getPaySlipsByPaySlipPdf2(options = {}) {
      return request("GET", "/pay-slips/{paySlip}/pdf", options);
    },

    /** Telecharger bulletin PDF */
    getPaySlipsByPaySlipPdf3(options = {}) {
      return request("GET", "/pay-slips/{paySlip}/pdf", options);
    },

    /** Telecharger bulletin PDF */
    getPaySlipsByPaySlipPdf4(options = {}) {
      return request("GET", "/pay-slips/{paySlip}/pdf", options);
    },

    /** Telecharger bulletin PDF */
    getPaySlipsByPaySlipPdf5(options = {}) {
      return request("GET", "/pay-slips/{paySlip}/pdf", options);
    },

    /** Telecharger bulletin PDF */
    getPaySlipsByPaySlipPdf6(options = {}) {
      return request("GET", "/pay-slips/{paySlip}/pdf", options);
    },

    /** Telecharger bulletin PDF */
    getPaySlipsByPaySlipPdf7(options = {}) {
      return request("GET", "/pay-slips/{paySlip}/pdf", options);
    },

    /** Telecharger bulletin PDF */
    getPaySlipsByPaySlipPdf8(options = {}) {
      return request("GET", "/pay-slips/{paySlip}/pdf", options);
    },

    /** Telecharger bulletin PDF */
    getPaySlipsByPaySlipPdf9(options = {}) {
      return request("GET", "/pay-slips/{paySlip}/pdf", options);
    },

    /** Telecharger bulletin PDF */
    getPaySlipsByPaySlipPdf10(options = {}) {
      return request("GET", "/pay-slips/{paySlip}/pdf", options);
    },

    /** Lister les lots de paiement */
    getPaymentBatches(options = {}) {
      return request("GET", "/payment-batches", options);
    },

    /** Lister les lots de paiement */
    getPaymentBatches2(options = {}) {
      return request("GET", "/payment-batches", options);
    },

    /** Lister les lots de paiement */
    getPaymentBatches3(options = {}) {
      return request("GET", "/payment-batches", options);
    },

    /** Lister les lots de paiement */
    getPaymentBatches4(options = {}) {
      return request("GET", "/payment-batches", options);
    },

    /** Lister les lots de paiement */
    getPaymentBatches5(options = {}) {
      return request("GET", "/payment-batches", options);
    },

    /** Lister les lots de paiement */
    getPaymentBatches6(options = {}) {
      return request("GET", "/payment-batches", options);
    },

    /** Lister les lots de paiement */
    getPaymentBatches7(options = {}) {
      return request("GET", "/payment-batches", options);
    },

    /** Lister les lots de paiement */
    getPaymentBatches8(options = {}) {
      return request("GET", "/payment-batches", options);
    },

    /** Lister les lots de paiement */
    getPaymentBatches9(options = {}) {
      return request("GET", "/payment-batches", options);
    },

    /** Lister les lots de paiement */
    getPaymentBatches10(options = {}) {
      return request("GET", "/payment-batches", options);
    },

    /** Creer un lot de paiement depuis un cycle paie */
    postPaymentBatches(options = {}) {
      return request("POST", "/payment-batches", options);
    },

    /** Creer un lot de paiement depuis un cycle paie */
    postPaymentBatches2(options = {}) {
      return request("POST", "/payment-batches", options);
    },

    /** Creer un lot de paiement depuis un cycle paie */
    postPaymentBatches3(options = {}) {
      return request("POST", "/payment-batches", options);
    },

    /** Creer un lot de paiement depuis un cycle paie */
    postPaymentBatches4(options = {}) {
      return request("POST", "/payment-batches", options);
    },

    /** Creer un lot de paiement depuis un cycle paie */
    postPaymentBatches5(options = {}) {
      return request("POST", "/payment-batches", options);
    },

    /** Creer un lot de paiement depuis un cycle paie */
    postPaymentBatches6(options = {}) {
      return request("POST", "/payment-batches", options);
    },

    /** Creer un lot de paiement depuis un cycle paie */
    postPaymentBatches7(options = {}) {
      return request("POST", "/payment-batches", options);
    },

    /** Creer un lot de paiement depuis un cycle paie */
    postPaymentBatches8(options = {}) {
      return request("POST", "/payment-batches", options);
    },

    /** Creer un lot de paiement depuis un cycle paie */
    postPaymentBatches9(options = {}) {
      return request("POST", "/payment-batches", options);
    },

    /** Creer un lot de paiement depuis un cycle paie */
    postPaymentBatches10(options = {}) {
      return request("POST", "/payment-batches", options);
    },

    /** Detail d'un lot de paiement */
    getPaymentBatchesByPaymentBatch(options = {}) {
      return request("GET", "/payment-batches/{paymentBatch}", options);
    },

    /** Detail d'un lot de paiement */
    getPaymentBatchesByPaymentBatch2(options = {}) {
      return request("GET", "/payment-batches/{paymentBatch}", options);
    },

    /** Detail d'un lot de paiement */
    getPaymentBatchesByPaymentBatch3(options = {}) {
      return request("GET", "/payment-batches/{paymentBatch}", options);
    },

    /** Detail d'un lot de paiement */
    getPaymentBatchesByPaymentBatch4(options = {}) {
      return request("GET", "/payment-batches/{paymentBatch}", options);
    },

    /** Detail d'un lot de paiement */
    getPaymentBatchesByPaymentBatch5(options = {}) {
      return request("GET", "/payment-batches/{paymentBatch}", options);
    },

    /** Detail d'un lot de paiement */
    getPaymentBatchesByPaymentBatch6(options = {}) {
      return request("GET", "/payment-batches/{paymentBatch}", options);
    },

    /** Detail d'un lot de paiement */
    getPaymentBatchesByPaymentBatch7(options = {}) {
      return request("GET", "/payment-batches/{paymentBatch}", options);
    },

    /** Detail d'un lot de paiement */
    getPaymentBatchesByPaymentBatch8(options = {}) {
      return request("GET", "/payment-batches/{paymentBatch}", options);
    },

    /** Detail d'un lot de paiement */
    getPaymentBatchesByPaymentBatch9(options = {}) {
      return request("GET", "/payment-batches/{paymentBatch}", options);
    },

    /** Detail d'un lot de paiement */
    getPaymentBatchesByPaymentBatch10(options = {}) {
      return request("GET", "/payment-batches/{paymentBatch}", options);
    },

    /** Declarer un lot de paiement comme envoye */
    postPaymentBatchesByPaymentBatchMarkPaid(options = {}) {
      return request("POST", "/payment-batches/{paymentBatch}/mark-paid", options);
    },

    /** Declarer un lot de paiement comme envoye */
    postPaymentBatchesByPaymentBatchMarkPaid2(options = {}) {
      return request("POST", "/payment-batches/{paymentBatch}/mark-paid", options);
    },

    /** Declarer un lot de paiement comme envoye */
    postPaymentBatchesByPaymentBatchMarkPaid3(options = {}) {
      return request("POST", "/payment-batches/{paymentBatch}/mark-paid", options);
    },

    /** Declarer un lot de paiement comme envoye */
    postPaymentBatchesByPaymentBatchMarkPaid4(options = {}) {
      return request("POST", "/payment-batches/{paymentBatch}/mark-paid", options);
    },

    /** Declarer un lot de paiement comme envoye */
    postPaymentBatchesByPaymentBatchMarkPaid5(options = {}) {
      return request("POST", "/payment-batches/{paymentBatch}/mark-paid", options);
    },

    /** Declarer un lot de paiement comme envoye */
    postPaymentBatchesByPaymentBatchMarkPaid6(options = {}) {
      return request("POST", "/payment-batches/{paymentBatch}/mark-paid", options);
    },

    /** Declarer un lot de paiement comme envoye */
    postPaymentBatchesByPaymentBatchMarkPaid7(options = {}) {
      return request("POST", "/payment-batches/{paymentBatch}/mark-paid", options);
    },

    /** Declarer un lot de paiement comme envoye */
    postPaymentBatchesByPaymentBatchMarkPaid8(options = {}) {
      return request("POST", "/payment-batches/{paymentBatch}/mark-paid", options);
    },

    /** Declarer un lot de paiement comme envoye */
    postPaymentBatchesByPaymentBatchMarkPaid9(options = {}) {
      return request("POST", "/payment-batches/{paymentBatch}/mark-paid", options);
    },

    /** Declarer un lot de paiement comme envoye */
    postPaymentBatchesByPaymentBatchMarkPaid10(options = {}) {
      return request("POST", "/payment-batches/{paymentBatch}/mark-paid", options);
    },

    /** Confirmer la reception d'un paiement */
    postPaymentConfirmationsByPaymentItemConfirm(options = {}) {
      return request("POST", "/payment-confirmations/{paymentItem}/confirm", options);
    },

    /** Confirmer la reception d'un paiement */
    postPaymentConfirmationsByPaymentItemConfirm2(options = {}) {
      return request("POST", "/payment-confirmations/{paymentItem}/confirm", options);
    },

    /** Confirmer la reception d'un paiement */
    postPaymentConfirmationsByPaymentItemConfirm3(options = {}) {
      return request("POST", "/payment-confirmations/{paymentItem}/confirm", options);
    },

    /** Confirmer la reception d'un paiement */
    postPaymentConfirmationsByPaymentItemConfirm4(options = {}) {
      return request("POST", "/payment-confirmations/{paymentItem}/confirm", options);
    },

    /** Confirmer la reception d'un paiement */
    postPaymentConfirmationsByPaymentItemConfirm5(options = {}) {
      return request("POST", "/payment-confirmations/{paymentItem}/confirm", options);
    },

    /** Confirmer la reception d'un paiement */
    postPaymentConfirmationsByPaymentItemConfirm6(options = {}) {
      return request("POST", "/payment-confirmations/{paymentItem}/confirm", options);
    },

    /** Confirmer la reception d'un paiement */
    postPaymentConfirmationsByPaymentItemConfirm7(options = {}) {
      return request("POST", "/payment-confirmations/{paymentItem}/confirm", options);
    },

    /** Confirmer la reception d'un paiement */
    postPaymentConfirmationsByPaymentItemConfirm8(options = {}) {
      return request("POST", "/payment-confirmations/{paymentItem}/confirm", options);
    },

    /** Confirmer la reception d'un paiement */
    postPaymentConfirmationsByPaymentItemConfirm9(options = {}) {
      return request("POST", "/payment-confirmations/{paymentItem}/confirm", options);
    },

    /** Confirmer la reception d'un paiement */
    postPaymentConfirmationsByPaymentItemConfirm10(options = {}) {
      return request("POST", "/payment-confirmations/{paymentItem}/confirm", options);
    },

    /** Lister les ordres de virement du tenant (pagine) */
    getPaymentOrders(options = {}) {
      return request("GET", "/payment-orders", options);
    },

    /** Detail d'un ordre de virement */
    getPaymentOrdersByPaymentOrder(options = {}) {
      return request("GET", "/payment-orders/{paymentOrder}", options);
    },

    /** Executer un ordre de virement prepare (comptable) */
    postPaymentOrdersByPaymentOrderExecute(options = {}) {
      return request("POST", "/payment-orders/{paymentOrder}/execute", options);
    },

    /** Rapprocher un ordre de virement execute (comptable) */
    postPaymentOrdersByPaymentOrderReconcile(options = {}) {
      return request("POST", "/payment-orders/{paymentOrder}/reconcile", options);
    },

    /** Documents de paiement d.un cycle paie */
    getPaymentsByPayrollRunDocuments(options = {}) {
      return request("GET", "/payments/{payrollRun}/documents", options);
    },

    /** Documents de paiement d.un cycle paie */
    getPaymentsByPayrollRunDocuments2(options = {}) {
      return request("GET", "/payments/{payrollRun}/documents", options);
    },

    /** Documents de paiement d.un cycle paie */
    getPaymentsByPayrollRunDocuments3(options = {}) {
      return request("GET", "/payments/{payrollRun}/documents", options);
    },

    /** Documents de paiement d.un cycle paie */
    getPaymentsByPayrollRunDocuments4(options = {}) {
      return request("GET", "/payments/{payrollRun}/documents", options);
    },

    /** Documents de paiement d.un cycle paie */
    getPaymentsByPayrollRunDocuments5(options = {}) {
      return request("GET", "/payments/{payrollRun}/documents", options);
    },

    /** Documents de paiement d.un cycle paie */
    getPaymentsByPayrollRunDocuments6(options = {}) {
      return request("GET", "/payments/{payrollRun}/documents", options);
    },

    /** Documents de paiement d.un cycle paie */
    getPaymentsByPayrollRunDocuments7(options = {}) {
      return request("GET", "/payments/{payrollRun}/documents", options);
    },

    /** Documents de paiement d.un cycle paie */
    getPaymentsByPayrollRunDocuments8(options = {}) {
      return request("GET", "/payments/{payrollRun}/documents", options);
    },

    /** Documents de paiement d.un cycle paie */
    getPaymentsByPayrollRunDocuments9(options = {}) {
      return request("GET", "/payments/{payrollRun}/documents", options);
    },

    /** Documents de paiement d.un cycle paie */
    getPaymentsByPayrollRunDocuments10(options = {}) {
      return request("GET", "/payments/{payrollRun}/documents", options);
    },

    /** Lister les sessions de paie */
    getPayrollRuns(options = {}) {
      return request("GET", "/payroll-runs", options);
    },

    /** Lister les sessions de paie */
    getPayrollRuns2(options = {}) {
      return request("GET", "/payroll-runs", options);
    },

    /** Lister les sessions de paie */
    getPayrollRuns3(options = {}) {
      return request("GET", "/payroll-runs", options);
    },

    /** Lister les sessions de paie */
    getPayrollRuns4(options = {}) {
      return request("GET", "/payroll-runs", options);
    },

    /** Lister les sessions de paie */
    getPayrollRuns5(options = {}) {
      return request("GET", "/payroll-runs", options);
    },

    /** Lister les sessions de paie */
    getPayrollRuns6(options = {}) {
      return request("GET", "/payroll-runs", options);
    },

    /** Lister les sessions de paie */
    getPayrollRuns7(options = {}) {
      return request("GET", "/payroll-runs", options);
    },

    /** Lister les sessions de paie */
    getPayrollRuns8(options = {}) {
      return request("GET", "/payroll-runs", options);
    },

    /** Lister les sessions de paie */
    getPayrollRuns9(options = {}) {
      return request("GET", "/payroll-runs", options);
    },

    /** Lister les sessions de paie */
    getPayrollRuns10(options = {}) {
      return request("GET", "/payroll-runs", options);
    },

    /** Creer une session de paie */
    postPayrollRuns(options = {}) {
      return request("POST", "/payroll-runs", options);
    },

    /** Creer une session de paie */
    postPayrollRuns2(options = {}) {
      return request("POST", "/payroll-runs", options);
    },

    /** Creer une session de paie */
    postPayrollRuns3(options = {}) {
      return request("POST", "/payroll-runs", options);
    },

    /** Creer une session de paie */
    postPayrollRuns4(options = {}) {
      return request("POST", "/payroll-runs", options);
    },

    /** Creer une session de paie */
    postPayrollRuns5(options = {}) {
      return request("POST", "/payroll-runs", options);
    },

    /** Creer une session de paie */
    postPayrollRuns6(options = {}) {
      return request("POST", "/payroll-runs", options);
    },

    /** Creer une session de paie */
    postPayrollRuns7(options = {}) {
      return request("POST", "/payroll-runs", options);
    },

    /** Creer une session de paie */
    postPayrollRuns8(options = {}) {
      return request("POST", "/payroll-runs", options);
    },

    /** Creer une session de paie */
    postPayrollRuns9(options = {}) {
      return request("POST", "/payroll-runs", options);
    },

    /** Creer une session de paie */
    postPayrollRuns10(options = {}) {
      return request("POST", "/payroll-runs", options);
    },

    /** Voir une session de paie */
    getPayrollRunsByPayrollRun(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}", options);
    },

    /** Voir une session de paie */
    getPayrollRunsByPayrollRun2(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}", options);
    },

    /** Voir une session de paie */
    getPayrollRunsByPayrollRun3(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}", options);
    },

    /** Voir une session de paie */
    getPayrollRunsByPayrollRun4(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}", options);
    },

    /** Voir une session de paie */
    getPayrollRunsByPayrollRun5(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}", options);
    },

    /** Voir une session de paie */
    getPayrollRunsByPayrollRun6(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}", options);
    },

    /** Voir une session de paie */
    getPayrollRunsByPayrollRun7(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}", options);
    },

    /** Voir une session de paie */
    getPayrollRunsByPayrollRun8(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}", options);
    },

    /** Voir une session de paie */
    getPayrollRunsByPayrollRun9(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}", options);
    },

    /** Voir une session de paie */
    getPayrollRunsByPayrollRun10(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}", options);
    },

    /** Ecrires comptables d'un run de paie valide (#5239) */
    getPayrollRunsByPayrollRunAccountingEntries(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/accounting-entries", options);
    },

    /** Regenerer les ecritures comptables d'un run (comptable, idempotent) */
    postPayrollRunsByPayrollRunAccountingEntriesRegenerate(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/accounting-entries/regenerate", options);
    },

    /** Rapport d'anomalies pre-cloture (F-20) */
    getPayrollRunsByPayrollRunAnomalies(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/anomalies", options);
    },

    /** Rapport d'anomalies pre-cloture (F-20) */
    getPayrollRunsByPayrollRunAnomalies2(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/anomalies", options);
    },

    /** Rapport d'anomalies pre-cloture (F-20) */
    getPayrollRunsByPayrollRunAnomalies3(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/anomalies", options);
    },

    /** Rapport d'anomalies pre-cloture (F-20) */
    getPayrollRunsByPayrollRunAnomalies4(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/anomalies", options);
    },

    /** Rapport d'anomalies pre-cloture (F-20) */
    getPayrollRunsByPayrollRunAnomalies5(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/anomalies", options);
    },

    /** Generer un export bancaire pour un run valide (alias chemin) */
    postPayrollRunsByPayrollRunBankExport(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bank-export", options);
    },

    /** Generer un export bancaire pour un run valide (alias chemin) */
    postPayrollRunsByPayrollRunBankExport2(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bank-export", options);
    },

    /** Generer un export bancaire pour un run valide (alias chemin) */
    postPayrollRunsByPayrollRunBankExport3(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bank-export", options);
    },

    /** Generer un export bancaire pour un run valide (alias chemin) */
    postPayrollRunsByPayrollRunBankExport4(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bank-export", options);
    },

    /** Generer un export bancaire pour un run valide (alias chemin) */
    postPayrollRunsByPayrollRunBankExport5(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bank-export", options);
    },

    /** Generer un export bancaire pour un run valide (alias chemin) */
    postPayrollRunsByPayrollRunBankExport6(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bank-export", options);
    },

    /** Generer un export bancaire pour un run valide (alias chemin) */
    postPayrollRunsByPayrollRunBankExport7(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bank-export", options);
    },

    /** Generer un export bancaire pour un run valide (alias chemin) */
    postPayrollRunsByPayrollRunBankExport8(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bank-export", options);
    },

    /** Generer un export bancaire pour un run valide (alias chemin) */
    postPayrollRunsByPayrollRunBankExport9(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bank-export", options);
    },

    /** Generer un export bancaire pour un run valide (alias chemin) */
    postPayrollRunsByPayrollRunBankExport10(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bank-export", options);
    },

    /** Bordereau de paie d'un run (totaux par cotisation + recap, #5243) */
    getPayrollRunsByPayrollRunBordereau(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/bordereau", options);
    },

    /** Lancer le paiement groupé du run (virements) */
    postPayrollRunsByPayrollRunBulkPay(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bulk-pay", options);
    },

    /** Lancer le paiement groupé du run (virements) */
    postPayrollRunsByPayrollRunBulkPay2(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bulk-pay", options);
    },

    /** Lancer le paiement groupé du run (virements) */
    postPayrollRunsByPayrollRunBulkPay3(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bulk-pay", options);
    },

    /** Lancer le paiement groupé du run (virements) */
    postPayrollRunsByPayrollRunBulkPay4(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bulk-pay", options);
    },

    /** Lancer le paiement groupé du run (virements) */
    postPayrollRunsByPayrollRunBulkPay5(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bulk-pay", options);
    },

    /** Lancer le paiement groupé du run (virements) */
    postPayrollRunsByPayrollRunBulkPay6(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bulk-pay", options);
    },

    /** Lancer le paiement groupé du run (virements) */
    postPayrollRunsByPayrollRunBulkPay7(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bulk-pay", options);
    },

    /** Lancer le paiement groupé du run (virements) */
    postPayrollRunsByPayrollRunBulkPay8(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bulk-pay", options);
    },

    /** Lancer le paiement groupé du run (virements) */
    postPayrollRunsByPayrollRunBulkPay9(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bulk-pay", options);
    },

    /** Lancer le paiement groupé du run (virements) */
    postPayrollRunsByPayrollRunBulkPay10(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bulk-pay", options);
    },

    /** Lancer le paiement groupé du run (virements) */
    postPayrollRunsByPayrollRunBulkPay11(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/bulk-pay", options);
    },

    /** Statut du paiement groupé du run */
    getPayrollRunsByPayrollRunBulkPayStatus(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/bulk-pay/status", options);
    },

    /** Statut du paiement groupé du run */
    getPayrollRunsByPayrollRunBulkPayStatus2(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/bulk-pay/status", options);
    },

    /** Statut du paiement groupé du run */
    getPayrollRunsByPayrollRunBulkPayStatus3(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/bulk-pay/status", options);
    },

    /** Statut du paiement groupé du run */
    getPayrollRunsByPayrollRunBulkPayStatus4(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/bulk-pay/status", options);
    },

    /** Statut du paiement groupé du run */
    getPayrollRunsByPayrollRunBulkPayStatus5(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/bulk-pay/status", options);
    },

    /** Statut du paiement groupé du run */
    getPayrollRunsByPayrollRunBulkPayStatus6(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/bulk-pay/status", options);
    },

    /** Statut du paiement groupé du run */
    getPayrollRunsByPayrollRunBulkPayStatus7(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/bulk-pay/status", options);
    },

    /** Statut du paiement groupé du run */
    getPayrollRunsByPayrollRunBulkPayStatus8(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/bulk-pay/status", options);
    },

    /** Statut du paiement groupé du run */
    getPayrollRunsByPayrollRunBulkPayStatus9(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/bulk-pay/status", options);
    },

    /** Statut du paiement groupé du run */
    getPayrollRunsByPayrollRunBulkPayStatus10(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/bulk-pay/status", options);
    },

    /** Statut du paiement groupé du run */
    getPayrollRunsByPayrollRunBulkPayStatus11(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/bulk-pay/status", options);
    },

    /** Calculer la paie */
    postPayrollRunsByPayrollRunCalculate(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/calculate", options);
    },

    /** Calculer la paie */
    postPayrollRunsByPayrollRunCalculate2(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/calculate", options);
    },

    /** Calculer la paie */
    postPayrollRunsByPayrollRunCalculate3(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/calculate", options);
    },

    /** Calculer la paie */
    postPayrollRunsByPayrollRunCalculate4(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/calculate", options);
    },

    /** Calculer la paie */
    postPayrollRunsByPayrollRunCalculate5(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/calculate", options);
    },

    /** Calculer la paie */
    postPayrollRunsByPayrollRunCalculate6(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/calculate", options);
    },

    /** Calculer la paie */
    postPayrollRunsByPayrollRunCalculate7(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/calculate", options);
    },

    /** Calculer la paie */
    postPayrollRunsByPayrollRunCalculate8(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/calculate", options);
    },

    /** Calculer la paie */
    postPayrollRunsByPayrollRunCalculate9(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/calculate", options);
    },

    /** Calculer la paie */
    postPayrollRunsByPayrollRunCalculate10(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/calculate", options);
    },

    /** Annuler une session de paie */
    postPayrollRunsByPayrollRunCancel(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/cancel", options);
    },

    /** Annuler une session de paie */
    postPayrollRunsByPayrollRunCancel2(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/cancel", options);
    },

    /** Annuler une session de paie */
    postPayrollRunsByPayrollRunCancel3(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/cancel", options);
    },

    /** Annuler une session de paie */
    postPayrollRunsByPayrollRunCancel4(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/cancel", options);
    },

    /** Annuler une session de paie */
    postPayrollRunsByPayrollRunCancel5(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/cancel", options);
    },

    /** Annuler une session de paie */
    postPayrollRunsByPayrollRunCancel6(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/cancel", options);
    },

    /** Annuler une session de paie */
    postPayrollRunsByPayrollRunCancel7(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/cancel", options);
    },

    /** Annuler une session de paie */
    postPayrollRunsByPayrollRunCancel8(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/cancel", options);
    },

    /** Annuler une session de paie */
    postPayrollRunsByPayrollRunCancel9(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/cancel", options);
    },

    /** Annuler une session de paie */
    postPayrollRunsByPayrollRunCancel10(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/cancel", options);
    },

    /** Déclaration CNPS mensuelle Cameroun — format DAS CSV (CEMAC/CM #1823) */
    downloadCnpsCmDeclaration(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnps-cm", options);
    },

    /** Déclaration CNPS mensuelle Cameroun — format DAS CSV (CEMAC/CM #1823) */
    downloadCnpsCmDeclaration2(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnps-cm", options);
    },

    /** Déclaration CNPS mensuelle Cameroun — format DAS CSV (CEMAC/CM #1823) */
    downloadCnpsCmDeclaration3(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnps-cm", options);
    },

    /** Déclaration CNPS mensuelle Cameroun — format DAS CSV (CEMAC/CM #1823) */
    downloadCnpsCmDeclaration4(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnps-cm", options);
    },

    /** Déclaration CNPS mensuelle Cameroun — format DAS CSV (CEMAC/CM #1823) */
    downloadCnpsCmDeclaration5(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnps-cm", options);
    },

    /** Déclaration CNPS mensuelle Cameroun — format DAS CSV (CEMAC/CM #1823) */
    downloadCnpsCmDeclaration6(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnps-cm", options);
    },

    /** Déclaration CNPS mensuelle Cameroun — format DAS CSV (CEMAC/CM #1823) */
    downloadCnpsCmDeclaration7(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnps-cm", options);
    },

    /** Déclaration CNPS mensuelle Cameroun — format DAS CSV (CEMAC/CM #1823) */
    downloadCnpsCmDeclaration8(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnps-cm", options);
    },

    /** Déclaration CNPS mensuelle Cameroun — format DAS CSV (CEMAC/CM #1823) */
    downloadCnpsCmDeclaration9(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnps-cm", options);
    },

    /** Déclaration CNPS mensuelle Cameroun — format DAS CSV (CEMAC/CM #1823) */
    downloadCnpsCmDeclaration10(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnps-cm", options);
    },

    /** Déclaration CNSS mensuelle Burkina Faso — CSV (CEDEAO #2158) */
    downloadCnssBfDeclaration(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-bf", options);
    },

    /** Déclaration CNSS mensuelle Burkina Faso — CSV (CEDEAO #2158) */
    downloadCnssBfDeclaration2(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-bf", options);
    },

    /** Déclaration CNSS mensuelle Burkina Faso — CSV (CEDEAO #2158) */
    downloadCnssBfDeclaration3(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-bf", options);
    },

    /** Déclaration CNSS mensuelle Burkina Faso — CSV (CEDEAO #2158) */
    downloadCnssBfDeclaration4(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-bf", options);
    },

    /** Déclaration CNSS mensuelle Burkina Faso — CSV (CEDEAO #2158) */
    downloadCnssBfDeclaration5(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-bf", options);
    },

    /** Déclaration CNSS mensuelle Burkina Faso — CSV (CEDEAO #2158) */
    downloadCnssBfDeclaration6(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-bf", options);
    },

    /** Déclaration CNSS mensuelle Burkina Faso — CSV (CEDEAO #2158) */
    downloadCnssBfDeclaration7(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-bf", options);
    },

    /** Déclaration CNSS mensuelle Burkina Faso — CSV (CEDEAO #2158) */
    downloadCnssBfDeclaration8(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-bf", options);
    },

    /** Déclaration CNSS mensuelle Burkina Faso — CSV (CEDEAO #2158) */
    downloadCnssBfDeclaration9(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-bf", options);
    },

    /** Déclaration CNSS mensuelle Burkina Faso — CSV (CEDEAO #2158) */
    downloadCnssBfDeclaration10(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-bf", options);
    },

    /** Declaration CNSS mensuelle Congo — CSV (CEMAC/CG #2155) */
    downloadCnssCgDeclaration(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-cg", options);
    },

    /** Declaration CNSS mensuelle Congo — CSV (CEMAC/CG #2155) */
    downloadCnssCgDeclaration2(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-cg", options);
    },

    /** Declaration CNSS mensuelle Congo — CSV (CEMAC/CG #2155) */
    downloadCnssCgDeclaration3(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-cg", options);
    },

    /** Declaration CNSS mensuelle Congo — CSV (CEMAC/CG #2155) */
    downloadCnssCgDeclaration4(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-cg", options);
    },

    /** Declaration CNSS mensuelle Congo — CSV (CEMAC/CG #2155) */
    downloadCnssCgDeclaration5(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-cg", options);
    },

    /** Declaration CNSS mensuelle Congo — CSV (CEMAC/CG #2155) */
    downloadCnssCgDeclaration6(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-cg", options);
    },

    /** Declaration CNSS mensuelle Congo — CSV (CEMAC/CG #2155) */
    downloadCnssCgDeclaration7(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-cg", options);
    },

    /** Declaration CNSS mensuelle Congo — CSV (CEMAC/CG #2155) */
    downloadCnssCgDeclaration8(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-cg", options);
    },

    /** Declaration CNSS mensuelle Congo — CSV (CEMAC/CG #2155) */
    downloadCnssCgDeclaration9(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-cg", options);
    },

    /** Declaration CNSS mensuelle Congo — CSV (CEMAC/CG #2155) */
    downloadCnssCgDeclaration10(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-cg", options);
    },

    /** Déclaration CNSS mensuelle Côte d'Ivoire — CSV (CEDEAO #1830) */
    downloadCnssCiDeclaration(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ci", options);
    },

    /** Déclaration CNSS mensuelle Côte d'Ivoire — CSV (CEDEAO #1830) */
    downloadCnssCiDeclaration2(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ci", options);
    },

    /** Déclaration CNSS mensuelle Côte d'Ivoire — CSV (CEDEAO #1830) */
    downloadCnssCiDeclaration3(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ci", options);
    },

    /** Déclaration CNSS mensuelle Côte d'Ivoire — CSV (CEDEAO #1830) */
    downloadCnssCiDeclaration4(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ci", options);
    },

    /** Déclaration CNSS mensuelle Côte d'Ivoire — CSV (CEDEAO #1830) */
    downloadCnssCiDeclaration5(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ci", options);
    },

    /** Déclaration CNSS mensuelle Côte d'Ivoire — CSV (CEDEAO #1830) */
    downloadCnssCiDeclaration6(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ci", options);
    },

    /** Déclaration CNSS mensuelle Côte d'Ivoire — CSV (CEDEAO #1830) */
    downloadCnssCiDeclaration7(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ci", options);
    },

    /** Déclaration CNSS mensuelle Côte d'Ivoire — CSV (CEDEAO #1830) */
    downloadCnssCiDeclaration8(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ci", options);
    },

    /** Déclaration CNSS mensuelle Côte d'Ivoire — CSV (CEDEAO #1830) */
    downloadCnssCiDeclaration9(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ci", options);
    },

    /** Déclaration CNSS mensuelle Côte d'Ivoire — CSV (CEDEAO #1830) */
    downloadCnssCiDeclaration10(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ci", options);
    },

    /** Declaration CNSS mensuelle Gabon — CSV (CEMAC/GA #2155) */
    downloadCnssGaDeclaration(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ga", options);
    },

    /** Declaration CNSS mensuelle Gabon — CSV (CEMAC/GA #2155) */
    downloadCnssGaDeclaration2(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ga", options);
    },

    /** Declaration CNSS mensuelle Gabon — CSV (CEMAC/GA #2155) */
    downloadCnssGaDeclaration3(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ga", options);
    },

    /** Declaration CNSS mensuelle Gabon — CSV (CEMAC/GA #2155) */
    downloadCnssGaDeclaration4(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ga", options);
    },

    /** Declaration CNSS mensuelle Gabon — CSV (CEMAC/GA #2155) */
    downloadCnssGaDeclaration5(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ga", options);
    },

    /** Declaration CNSS mensuelle Gabon — CSV (CEMAC/GA #2155) */
    downloadCnssGaDeclaration6(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ga", options);
    },

    /** Declaration CNSS mensuelle Gabon — CSV (CEMAC/GA #2155) */
    downloadCnssGaDeclaration7(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ga", options);
    },

    /** Declaration CNSS mensuelle Gabon — CSV (CEMAC/GA #2155) */
    downloadCnssGaDeclaration8(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ga", options);
    },

    /** Declaration CNSS mensuelle Gabon — CSV (CEMAC/GA #2155) */
    downloadCnssGaDeclaration9(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ga", options);
    },

    /** Declaration CNSS mensuelle Gabon — CSV (CEMAC/GA #2155) */
    downloadCnssGaDeclaration10(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/cnss-ga", options);
    },

    /** Déclaration INPS mensuelle Mali — CSV (CEDEAO #2158) */
    downloadInpsMlDeclaration(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/inps-ml", options);
    },

    /** Déclaration INPS mensuelle Mali — CSV (CEDEAO #2158) */
    downloadInpsMlDeclaration2(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/inps-ml", options);
    },

    /** Déclaration INPS mensuelle Mali — CSV (CEDEAO #2158) */
    downloadInpsMlDeclaration3(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/inps-ml", options);
    },

    /** Déclaration INPS mensuelle Mali — CSV (CEDEAO #2158) */
    downloadInpsMlDeclaration4(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/inps-ml", options);
    },

    /** Déclaration INPS mensuelle Mali — CSV (CEDEAO #2158) */
    downloadInpsMlDeclaration5(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/inps-ml", options);
    },

    /** Déclaration INPS mensuelle Mali — CSV (CEDEAO #2158) */
    downloadInpsMlDeclaration6(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/inps-ml", options);
    },

    /** Déclaration INPS mensuelle Mali — CSV (CEDEAO #2158) */
    downloadInpsMlDeclaration7(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/inps-ml", options);
    },

    /** Déclaration INPS mensuelle Mali — CSV (CEDEAO #2158) */
    downloadInpsMlDeclaration8(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/inps-ml", options);
    },

    /** Déclaration INPS mensuelle Mali — CSV (CEDEAO #2158) */
    downloadInpsMlDeclaration9(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/inps-ml", options);
    },

    /** Déclaration INPS mensuelle Mali — CSV (CEDEAO #2158) */
    downloadInpsMlDeclaration10(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/inps-ml", options);
    },

    /** Déclaration IPRES/CSS mensuelle Sénégal — CSV (CEDEAO #1830) */
    downloadIpresSnDeclaration(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/ipres-sn", options);
    },

    /** Déclaration IPRES/CSS mensuelle Sénégal — CSV (CEDEAO #1830) */
    downloadIpresSnDeclaration2(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/ipres-sn", options);
    },

    /** Déclaration IPRES/CSS mensuelle Sénégal — CSV (CEDEAO #1830) */
    downloadIpresSnDeclaration3(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/ipres-sn", options);
    },

    /** Déclaration IPRES/CSS mensuelle Sénégal — CSV (CEDEAO #1830) */
    downloadIpresSnDeclaration4(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/ipres-sn", options);
    },

    /** Déclaration IPRES/CSS mensuelle Sénégal — CSV (CEDEAO #1830) */
    downloadIpresSnDeclaration5(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/ipres-sn", options);
    },

    /** Déclaration IPRES/CSS mensuelle Sénégal — CSV (CEDEAO #1830) */
    downloadIpresSnDeclaration6(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/ipres-sn", options);
    },

    /** Déclaration IPRES/CSS mensuelle Sénégal — CSV (CEDEAO #1830) */
    downloadIpresSnDeclaration7(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/ipres-sn", options);
    },

    /** Déclaration IPRES/CSS mensuelle Sénégal — CSV (CEDEAO #1830) */
    downloadIpresSnDeclaration8(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/ipres-sn", options);
    },

    /** Déclaration IPRES/CSS mensuelle Sénégal — CSV (CEDEAO #1830) */
    downloadIpresSnDeclaration9(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/ipres-sn", options);
    },

    /** Déclaration IPRES/CSS mensuelle Sénégal — CSV (CEDEAO #1830) */
    downloadIpresSnDeclaration10(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/declarations/ipres-sn", options);
    },

    /** Export comptable du run (CSV/Excel streamé) */
    getPayrollRunsByPayrollRunExport(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/export", options);
    },

    /** Export comptable du run (CSV/Excel streamé) */
    getPayrollRunsByPayrollRunExport2(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/export", options);
    },

    /** Export comptable du run (CSV/Excel streamé) */
    getPayrollRunsByPayrollRunExport3(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/export", options);
    },

    /** Export comptable du run (CSV/Excel streamé) */
    getPayrollRunsByPayrollRunExport4(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/export", options);
    },

    /** Export comptable du run (CSV/Excel streamé) */
    getPayrollRunsByPayrollRunExport5(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/export", options);
    },

    /** Export comptable du run (CSV/Excel streamé) */
    getPayrollRunsByPayrollRunExport6(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/export", options);
    },

    /** Export comptable du run (CSV/Excel streamé) */
    getPayrollRunsByPayrollRunExport7(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/export", options);
    },

    /** Export comptable du run (CSV/Excel streamé) */
    getPayrollRunsByPayrollRunExport8(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/export", options);
    },

    /** Export comptable du run (CSV/Excel streamé) */
    getPayrollRunsByPayrollRunExport9(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/export", options);
    },

    /** Export comptable du run (CSV/Excel streamé) */
    getPayrollRunsByPayrollRunExport10(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/export", options);
    },

    /** Export comptable du run (CSV/Excel streamé) */
    getPayrollRunsByPayrollRunExport11(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/export", options);
    },

    /** Journal de paie mensuel CSV (F-10) */
    getPayrollRunsByPayrollRunJournal(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/journal", options);
    },

    /** Journal de paie mensuel CSV (F-10) */
    getPayrollRunsByPayrollRunJournal2(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/journal", options);
    },

    /** Journal de paie mensuel CSV (F-10) */
    getPayrollRunsByPayrollRunJournal3(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/journal", options);
    },

    /** Journal de paie mensuel CSV (F-10) */
    getPayrollRunsByPayrollRunJournal4(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/journal", options);
    },

    /** Journal de paie mensuel CSV (F-10) */
    getPayrollRunsByPayrollRunJournal5(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/journal", options);
    },

    /** Verrouiller la session de paie (cloture comptable, F-11) */
    postPayrollRunsByPayrollRunLock(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/lock", options);
    },

    /** Verrouiller la session de paie (cloture comptable, F-11) */
    postPayrollRunsByPayrollRunLock2(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/lock", options);
    },

    /** Verrouiller la session de paie (cloture comptable, F-11) */
    postPayrollRunsByPayrollRunLock3(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/lock", options);
    },

    /** Verrouiller la session de paie (cloture comptable, F-11) */
    postPayrollRunsByPayrollRunLock4(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/lock", options);
    },

    /** Verrouiller la session de paie (cloture comptable, F-11) */
    postPayrollRunsByPayrollRunLock5(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/lock", options);
    },

    /** Lister les bulletins de paie d'une session */
    listPayrollRunPaySlips(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/pay-slips", options);
    },

    /** Lister les bulletins de paie d'une session */
    listPayrollRunPaySlips2(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/pay-slips", options);
    },

    /** Lister les bulletins de paie d'une session */
    listPayrollRunPaySlips3(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/pay-slips", options);
    },

    /** Lister les bulletins de paie d'une session */
    listPayrollRunPaySlips4(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/pay-slips", options);
    },

    /** Lister les bulletins de paie d'une session */
    listPayrollRunPaySlips5(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/pay-slips", options);
    },

    /** Lister les bulletins de paie d'une session */
    listPayrollRunPaySlips6(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/pay-slips", options);
    },

    /** Lister les bulletins de paie d'une session */
    listPayrollRunPaySlips7(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/pay-slips", options);
    },

    /** Lister les bulletins de paie d'une session */
    listPayrollRunPaySlips8(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/pay-slips", options);
    },

    /** Lister les bulletins de paie d'une session */
    listPayrollRunPaySlips9(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/pay-slips", options);
    },

    /** Lister les bulletins de paie d'une session */
    listPayrollRunPaySlips10(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/pay-slips", options);
    },

    /** Preparer un ordre de virement pour un run valide (comptable) */
    postPayrollRunsByPayrollRunPaymentOrder(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/payment-order", options);
    },

    /** Lister les régularisations d'un run (DZ-DEPTH #1818) */
    listPayrollRunRegularizations(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/regularizations", options);
    },

    /** Lister les régularisations d'un run (DZ-DEPTH #1818) */
    listPayrollRunRegularizations2(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/regularizations", options);
    },

    /** Lister les régularisations d'un run (DZ-DEPTH #1818) */
    listPayrollRunRegularizations3(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/regularizations", options);
    },

    /** Lister les régularisations d'un run (DZ-DEPTH #1818) */
    listPayrollRunRegularizations4(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/regularizations", options);
    },

    /** Lister les régularisations d'un run (DZ-DEPTH #1818) */
    listPayrollRunRegularizations5(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/regularizations", options);
    },

    /** Lister les régularisations d'un run (DZ-DEPTH #1818) */
    listPayrollRunRegularizations6(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/regularizations", options);
    },

    /** Lister les régularisations d'un run (DZ-DEPTH #1818) */
    listPayrollRunRegularizations7(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/regularizations", options);
    },

    /** Lister les régularisations d'un run (DZ-DEPTH #1818) */
    listPayrollRunRegularizations8(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/regularizations", options);
    },

    /** Lister les régularisations d'un run (DZ-DEPTH #1818) */
    listPayrollRunRegularizations9(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/regularizations", options);
    },

    /** Lister les régularisations d'un run (DZ-DEPTH #1818) */
    listPayrollRunRegularizations10(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/regularizations", options);
    },

    /** Créer un run de régularisation (DZ-DEPTH #1818) */
    regularizePayrollRun(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/regularize", options);
    },

    /** Créer un run de régularisation (DZ-DEPTH #1818) */
    regularizePayrollRun2(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/regularize", options);
    },

    /** Créer un run de régularisation (DZ-DEPTH #1818) */
    regularizePayrollRun3(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/regularize", options);
    },

    /** Créer un run de régularisation (DZ-DEPTH #1818) */
    regularizePayrollRun4(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/regularize", options);
    },

    /** Créer un run de régularisation (DZ-DEPTH #1818) */
    regularizePayrollRun5(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/regularize", options);
    },

    /** Créer un run de régularisation (DZ-DEPTH #1818) */
    regularizePayrollRun6(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/regularize", options);
    },

    /** Créer un run de régularisation (DZ-DEPTH #1818) */
    regularizePayrollRun7(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/regularize", options);
    },

    /** Créer un run de régularisation (DZ-DEPTH #1818) */
    regularizePayrollRun8(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/regularize", options);
    },

    /** Créer un run de régularisation (DZ-DEPTH #1818) */
    regularizePayrollRun9(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/regularize", options);
    },

    /** Créer un run de régularisation (DZ-DEPTH #1818) */
    regularizePayrollRun10(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/regularize", options);
    },

    /** Envoyer les bulletins du run aux employés */
    postPayrollRunsByPayrollRunSendSlips(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/send-slips", options);
    },

    /** Envoyer les bulletins du run aux employés */
    postPayrollRunsByPayrollRunSendSlips2(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/send-slips", options);
    },

    /** Envoyer les bulletins du run aux employés */
    postPayrollRunsByPayrollRunSendSlips3(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/send-slips", options);
    },

    /** Envoyer les bulletins du run aux employés */
    postPayrollRunsByPayrollRunSendSlips4(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/send-slips", options);
    },

    /** Envoyer les bulletins du run aux employés */
    postPayrollRunsByPayrollRunSendSlips5(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/send-slips", options);
    },

    /** Envoyer les bulletins du run aux employés */
    postPayrollRunsByPayrollRunSendSlips6(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/send-slips", options);
    },

    /** Envoyer les bulletins du run aux employés */
    postPayrollRunsByPayrollRunSendSlips7(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/send-slips", options);
    },

    /** Envoyer les bulletins du run aux employés */
    postPayrollRunsByPayrollRunSendSlips8(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/send-slips", options);
    },

    /** Envoyer les bulletins du run aux employés */
    postPayrollRunsByPayrollRunSendSlips9(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/send-slips", options);
    },

    /** Envoyer les bulletins du run aux employés */
    postPayrollRunsByPayrollRunSendSlips10(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/send-slips", options);
    },

    /** Envoyer les bulletins du run aux employés */
    postPayrollRunsByPayrollRunSendSlips11(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/send-slips", options);
    },

    /** Resume de la session de paie */
    getPayrollRunsByPayrollRunSummary(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/summary", options);
    },

    /** Resume de la session de paie */
    getPayrollRunsByPayrollRunSummary2(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/summary", options);
    },

    /** Resume de la session de paie */
    getPayrollRunsByPayrollRunSummary3(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/summary", options);
    },

    /** Resume de la session de paie */
    getPayrollRunsByPayrollRunSummary4(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/summary", options);
    },

    /** Resume de la session de paie */
    getPayrollRunsByPayrollRunSummary5(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/summary", options);
    },

    /** Resume de la session de paie */
    getPayrollRunsByPayrollRunSummary6(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/summary", options);
    },

    /** Resume de la session de paie */
    getPayrollRunsByPayrollRunSummary7(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/summary", options);
    },

    /** Resume de la session de paie */
    getPayrollRunsByPayrollRunSummary8(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/summary", options);
    },

    /** Resume de la session de paie */
    getPayrollRunsByPayrollRunSummary9(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/summary", options);
    },

    /** Resume de la session de paie */
    getPayrollRunsByPayrollRunSummary10(options = {}) {
      return request("GET", "/payroll-runs/{payrollRun}/summary", options);
    },

    /** Deverrouiller une session cloturee (raison obligatoire, F-11) */
    postPayrollRunsByPayrollRunUnlock(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/unlock", options);
    },

    /** Deverrouiller une session cloturee (raison obligatoire, F-11) */
    postPayrollRunsByPayrollRunUnlock2(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/unlock", options);
    },

    /** Deverrouiller une session cloturee (raison obligatoire, F-11) */
    postPayrollRunsByPayrollRunUnlock3(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/unlock", options);
    },

    /** Deverrouiller une session cloturee (raison obligatoire, F-11) */
    postPayrollRunsByPayrollRunUnlock4(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/unlock", options);
    },

    /** Deverrouiller une session cloturee (raison obligatoire, F-11) */
    postPayrollRunsByPayrollRunUnlock5(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/unlock", options);
    },

    /** Valider la session de paie */
    postPayrollRunsByPayrollRunValidate(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/validate", options);
    },

    /** Valider la session de paie */
    postPayrollRunsByPayrollRunValidate2(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/validate", options);
    },

    /** Valider la session de paie */
    postPayrollRunsByPayrollRunValidate3(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/validate", options);
    },

    /** Valider la session de paie */
    postPayrollRunsByPayrollRunValidate4(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/validate", options);
    },

    /** Valider la session de paie */
    postPayrollRunsByPayrollRunValidate5(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/validate", options);
    },

    /** Valider la session de paie */
    postPayrollRunsByPayrollRunValidate6(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/validate", options);
    },

    /** Valider la session de paie */
    postPayrollRunsByPayrollRunValidate7(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/validate", options);
    },

    /** Valider la session de paie */
    postPayrollRunsByPayrollRunValidate8(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/validate", options);
    },

    /** Valider la session de paie */
    postPayrollRunsByPayrollRunValidate9(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/validate", options);
    },

    /** Valider la session de paie */
    postPayrollRunsByPayrollRunValidate10(options = {}) {
      return request("POST", "/payroll-runs/{payrollRun}/validate", options);
    },

    /** Lister les enregistrements d'audit des calculs de paie (manager principal/RH) — issue #1874 */
    listPayrollAudits(options = {}) {
      return request("GET", "/payroll/audit", options);
    },

    /** Lister les enregistrements d'audit des calculs de paie (manager principal/RH) — issue #1874 */
    listPayrollAudits2(options = {}) {
      return request("GET", "/payroll/audit", options);
    },

    /** Lister les enregistrements d'audit des calculs de paie (manager principal/RH) — issue #1874 */
    listPayrollAudits3(options = {}) {
      return request("GET", "/payroll/audit", options);
    },

    /** Lister les enregistrements d'audit des calculs de paie (manager principal/RH) — issue #1874 */
    listPayrollAudits4(options = {}) {
      return request("GET", "/payroll/audit", options);
    },

    /** Lister les enregistrements d'audit des calculs de paie (manager principal/RH) — issue #1874 */
    listPayrollAudits5(options = {}) {
      return request("GET", "/payroll/audit", options);
    },

    /** Lister les enregistrements d'audit des calculs de paie (manager principal/RH) — issue #1874 */
    listPayrollAudits6(options = {}) {
      return request("GET", "/payroll/audit", options);
    },

    /** Lister les enregistrements d'audit des calculs de paie (manager principal/RH) — issue #1874 */
    listPayrollAudits7(options = {}) {
      return request("GET", "/payroll/audit", options);
    },

    /** Lister les enregistrements d'audit des calculs de paie (manager principal/RH) — issue #1874 */
    listPayrollAudits8(options = {}) {
      return request("GET", "/payroll/audit", options);
    },

    /** Lister les enregistrements d'audit des calculs de paie (manager principal/RH) — issue #1874 */
    listPayrollAudits9(options = {}) {
      return request("GET", "/payroll/audit", options);
    },

    /** Lister les enregistrements d'audit des calculs de paie (manager principal/RH) — issue #1874 */
    listPayrollAudits10(options = {}) {
      return request("GET", "/payroll/audit", options);
    },

    /** Détail d'un enregistrement d'audit de calcul de paie (manager principal/RH) — issue #1874 */
    getPayrollAudit(options = {}) {
      return request("GET", "/payroll/audit/{correlationId}", options);
    },

    /** Détail d'un enregistrement d'audit de calcul de paie (manager principal/RH) — issue #1874 */
    getPayrollAudit2(options = {}) {
      return request("GET", "/payroll/audit/{correlationId}", options);
    },

    /** Détail d'un enregistrement d'audit de calcul de paie (manager principal/RH) — issue #1874 */
    getPayrollAudit3(options = {}) {
      return request("GET", "/payroll/audit/{correlationId}", options);
    },

    /** Détail d'un enregistrement d'audit de calcul de paie (manager principal/RH) — issue #1874 */
    getPayrollAudit4(options = {}) {
      return request("GET", "/payroll/audit/{correlationId}", options);
    },

    /** Détail d'un enregistrement d'audit de calcul de paie (manager principal/RH) — issue #1874 */
    getPayrollAudit5(options = {}) {
      return request("GET", "/payroll/audit/{correlationId}", options);
    },

    /** Détail d'un enregistrement d'audit de calcul de paie (manager principal/RH) — issue #1874 */
    getPayrollAudit6(options = {}) {
      return request("GET", "/payroll/audit/{correlationId}", options);
    },

    /** Détail d'un enregistrement d'audit de calcul de paie (manager principal/RH) — issue #1874 */
    getPayrollAudit7(options = {}) {
      return request("GET", "/payroll/audit/{correlationId}", options);
    },

    /** Détail d'un enregistrement d'audit de calcul de paie (manager principal/RH) — issue #1874 */
    getPayrollAudit8(options = {}) {
      return request("GET", "/payroll/audit/{correlationId}", options);
    },

    /** Détail d'un enregistrement d'audit de calcul de paie (manager principal/RH) — issue #1874 */
    getPayrollAudit9(options = {}) {
      return request("GET", "/payroll/audit/{correlationId}", options);
    },

    /** Détail d'un enregistrement d'audit de calcul de paie (manager principal/RH) — issue #1874 */
    getPayrollAudit10(options = {}) {
      return request("GET", "/payroll/audit/{correlationId}", options);
    },

    /** Réglages du cycle de paie (société) */
    getPayrollCycleSettings(options = {}) {
      return request("GET", "/payroll/cycle-settings", options);
    },

    /** Réglages du cycle de paie (société) */
    getPayrollCycleSettings2(options = {}) {
      return request("GET", "/payroll/cycle-settings", options);
    },

    /** Réglages du cycle de paie (société) */
    getPayrollCycleSettings3(options = {}) {
      return request("GET", "/payroll/cycle-settings", options);
    },

    /** Réglages du cycle de paie (société) */
    getPayrollCycleSettings4(options = {}) {
      return request("GET", "/payroll/cycle-settings", options);
    },

    /** Réglages du cycle de paie (société) */
    getPayrollCycleSettings5(options = {}) {
      return request("GET", "/payroll/cycle-settings", options);
    },

    /** Réglages du cycle de paie (société) */
    getPayrollCycleSettings6(options = {}) {
      return request("GET", "/payroll/cycle-settings", options);
    },

    /** Réglages du cycle de paie (société) */
    getPayrollCycleSettings7(options = {}) {
      return request("GET", "/payroll/cycle-settings", options);
    },

    /** Réglages du cycle de paie (société) */
    getPayrollCycleSettings8(options = {}) {
      return request("GET", "/payroll/cycle-settings", options);
    },

    /** Réglages du cycle de paie (société) */
    getPayrollCycleSettings9(options = {}) {
      return request("GET", "/payroll/cycle-settings", options);
    },

    /** Réglages du cycle de paie (société) */
    getPayrollCycleSettings10(options = {}) {
      return request("GET", "/payroll/cycle-settings", options);
    },

    /** Réglages du cycle de paie (société) */
    getPayrollCycleSettings11(options = {}) {
      return request("GET", "/payroll/cycle-settings", options);
    },

    /** Mettre à jour les réglages du cycle de paie */
    putPayrollCycleSettings(options = {}) {
      return request("PUT", "/payroll/cycle-settings", options);
    },

    /** Mettre à jour les réglages du cycle de paie */
    putPayrollCycleSettings2(options = {}) {
      return request("PUT", "/payroll/cycle-settings", options);
    },

    /** Mettre à jour les réglages du cycle de paie */
    putPayrollCycleSettings3(options = {}) {
      return request("PUT", "/payroll/cycle-settings", options);
    },

    /** Mettre à jour les réglages du cycle de paie */
    putPayrollCycleSettings4(options = {}) {
      return request("PUT", "/payroll/cycle-settings", options);
    },

    /** Mettre à jour les réglages du cycle de paie */
    putPayrollCycleSettings5(options = {}) {
      return request("PUT", "/payroll/cycle-settings", options);
    },

    /** Mettre à jour les réglages du cycle de paie */
    putPayrollCycleSettings6(options = {}) {
      return request("PUT", "/payroll/cycle-settings", options);
    },

    /** Mettre à jour les réglages du cycle de paie */
    putPayrollCycleSettings7(options = {}) {
      return request("PUT", "/payroll/cycle-settings", options);
    },

    /** Mettre à jour les réglages du cycle de paie */
    putPayrollCycleSettings8(options = {}) {
      return request("PUT", "/payroll/cycle-settings", options);
    },

    /** Mettre à jour les réglages du cycle de paie */
    putPayrollCycleSettings9(options = {}) {
      return request("PUT", "/payroll/cycle-settings", options);
    },

    /** Mettre à jour les réglages du cycle de paie */
    putPayrollCycleSettings10(options = {}) {
      return request("PUT", "/payroll/cycle-settings", options);
    },

    /** Mettre à jour les réglages du cycle de paie */
    putPayrollCycleSettings11(options = {}) {
      return request("PUT", "/payroll/cycle-settings", options);
    },

    /** Historique des cycles de paie */
    getPayrollCycles(options = {}) {
      return request("GET", "/payroll/cycles", options);
    },

    /** Historique des cycles de paie */
    getPayrollCycles2(options = {}) {
      return request("GET", "/payroll/cycles", options);
    },

    /** Historique des cycles de paie */
    getPayrollCycles3(options = {}) {
      return request("GET", "/payroll/cycles", options);
    },

    /** Historique des cycles de paie */
    getPayrollCycles4(options = {}) {
      return request("GET", "/payroll/cycles", options);
    },

    /** Historique des cycles de paie */
    getPayrollCycles5(options = {}) {
      return request("GET", "/payroll/cycles", options);
    },

    /** Historique des cycles de paie */
    getPayrollCycles6(options = {}) {
      return request("GET", "/payroll/cycles", options);
    },

    /** Historique des cycles de paie */
    getPayrollCycles7(options = {}) {
      return request("GET", "/payroll/cycles", options);
    },

    /** Historique des cycles de paie */
    getPayrollCycles8(options = {}) {
      return request("GET", "/payroll/cycles", options);
    },

    /** Historique des cycles de paie */
    getPayrollCycles9(options = {}) {
      return request("GET", "/payroll/cycles", options);
    },

    /** Historique des cycles de paie */
    getPayrollCycles10(options = {}) {
      return request("GET", "/payroll/cycles", options);
    },

    /** Historique des cycles de paie */
    getPayrollCycles11(options = {}) {
      return request("GET", "/payroll/cycles", options);
    },

    /** Cycle de paie courant */
    getPayrollCyclesCurrent(options = {}) {
      return request("GET", "/payroll/cycles/current", options);
    },

    /** Cycle de paie courant */
    getPayrollCyclesCurrent2(options = {}) {
      return request("GET", "/payroll/cycles/current", options);
    },

    /** Cycle de paie courant */
    getPayrollCyclesCurrent3(options = {}) {
      return request("GET", "/payroll/cycles/current", options);
    },

    /** Cycle de paie courant */
    getPayrollCyclesCurrent4(options = {}) {
      return request("GET", "/payroll/cycles/current", options);
    },

    /** Cycle de paie courant */
    getPayrollCyclesCurrent5(options = {}) {
      return request("GET", "/payroll/cycles/current", options);
    },

    /** Cycle de paie courant */
    getPayrollCyclesCurrent6(options = {}) {
      return request("GET", "/payroll/cycles/current", options);
    },

    /** Cycle de paie courant */
    getPayrollCyclesCurrent7(options = {}) {
      return request("GET", "/payroll/cycles/current", options);
    },

    /** Cycle de paie courant */
    getPayrollCyclesCurrent8(options = {}) {
      return request("GET", "/payroll/cycles/current", options);
    },

    /** Cycle de paie courant */
    getPayrollCyclesCurrent9(options = {}) {
      return request("GET", "/payroll/cycles/current", options);
    },

    /** Cycle de paie courant */
    getPayrollCyclesCurrent10(options = {}) {
      return request("GET", "/payroll/cycles/current", options);
    },

    /** Cycle de paie courant */
    getPayrollCyclesCurrent11(options = {}) {
      return request("GET", "/payroll/cycles/current", options);
    },

    /** Previsualiser un cycle de paie candidat */
    getPayrollCyclesPreview(options = {}) {
      return request("GET", "/payroll/cycles/preview", options);
    },

    /** Previsualiser un cycle de paie candidat */
    getPayrollCyclesPreview2(options = {}) {
      return request("GET", "/payroll/cycles/preview", options);
    },

    /** Previsualiser un cycle de paie candidat */
    getPayrollCyclesPreview3(options = {}) {
      return request("GET", "/payroll/cycles/preview", options);
    },

    /** Previsualiser un cycle de paie candidat */
    getPayrollCyclesPreview4(options = {}) {
      return request("GET", "/payroll/cycles/preview", options);
    },

    /** Previsualiser un cycle de paie candidat */
    getPayrollCyclesPreview5(options = {}) {
      return request("GET", "/payroll/cycles/preview", options);
    },

    /** Previsualiser un cycle de paie candidat */
    getPayrollCyclesPreview6(options = {}) {
      return request("GET", "/payroll/cycles/preview", options);
    },

    /** Previsualiser un cycle de paie candidat */
    getPayrollCyclesPreview7(options = {}) {
      return request("GET", "/payroll/cycles/preview", options);
    },

    /** Previsualiser un cycle de paie candidat */
    getPayrollCyclesPreview8(options = {}) {
      return request("GET", "/payroll/cycles/preview", options);
    },

    /** Previsualiser un cycle de paie candidat */
    getPayrollCyclesPreview9(options = {}) {
      return request("GET", "/payroll/cycles/preview", options);
    },

    /** Previsualiser un cycle de paie candidat */
    getPayrollCyclesPreview10(options = {}) {
      return request("GET", "/payroll/cycles/preview", options);
    },

    /** Resume mobile des soldes paie equipe */
    getPayrollMobileSummary(options = {}) {
      return request("GET", "/payroll/mobile-summary", options);
    },

    /** Resume mobile des soldes paie equipe */
    getPayrollMobileSummary2(options = {}) {
      return request("GET", "/payroll/mobile-summary", options);
    },

    /** Resume mobile des soldes paie equipe */
    getPayrollMobileSummary3(options = {}) {
      return request("GET", "/payroll/mobile-summary", options);
    },

    /** Resume mobile des soldes paie equipe */
    getPayrollMobileSummary4(options = {}) {
      return request("GET", "/payroll/mobile-summary", options);
    },

    /** Resume mobile des soldes paie equipe */
    getPayrollMobileSummary5(options = {}) {
      return request("GET", "/payroll/mobile-summary", options);
    },

    /** Resume mobile des soldes paie equipe */
    getPayrollMobileSummary6(options = {}) {
      return request("GET", "/payroll/mobile-summary", options);
    },

    /** Resume mobile des soldes paie equipe */
    getPayrollMobileSummary7(options = {}) {
      return request("GET", "/payroll/mobile-summary", options);
    },

    /** Resume mobile des soldes paie equipe */
    getPayrollMobileSummary8(options = {}) {
      return request("GET", "/payroll/mobile-summary", options);
    },

    /** Resume mobile des soldes paie equipe */
    getPayrollMobileSummary9(options = {}) {
      return request("GET", "/payroll/mobile-summary", options);
    },

    /** Resume mobile des soldes paie equipe */
    getPayrollMobileSummary10(options = {}) {
      return request("GET", "/payroll/mobile-summary", options);
    },

    /** Simuler l'impact d'un barème (manager, dry-run) — issue #1814 */
    simulatePayroll(options = {}) {
      return request("POST", "/payroll/simulate", options);
    },

    /** Simuler l'impact d'un barème (manager, dry-run) — issue #1814 */
    simulatePayroll2(options = {}) {
      return request("POST", "/payroll/simulate", options);
    },

    /** Simuler l'impact d'un barème (manager, dry-run) — issue #1814 */
    simulatePayroll3(options = {}) {
      return request("POST", "/payroll/simulate", options);
    },

    /** Simuler l'impact d'un barème (manager, dry-run) — issue #1814 */
    simulatePayroll4(options = {}) {
      return request("POST", "/payroll/simulate", options);
    },

    /** Simuler l'impact d'un barème (manager, dry-run) — issue #1814 */
    simulatePayroll5(options = {}) {
      return request("POST", "/payroll/simulate", options);
    },

    /** Simuler l'impact d'un barème (manager, dry-run) — issue #1814 */
    simulatePayroll6(options = {}) {
      return request("POST", "/payroll/simulate", options);
    },

    /** Simuler l'impact d'un barème (manager, dry-run) — issue #1814 */
    simulatePayroll7(options = {}) {
      return request("POST", "/payroll/simulate", options);
    },

    /** Simuler l'impact d'un barème (manager, dry-run) — issue #1814 */
    simulatePayroll8(options = {}) {
      return request("POST", "/payroll/simulate", options);
    },

    /** Simuler l'impact d'un barème (manager, dry-run) — issue #1814 */
    simulatePayroll9(options = {}) {
      return request("POST", "/payroll/simulate", options);
    },

    /** Simuler l'impact d'un barème (manager, dry-run) — issue #1814 */
    simulatePayroll10(options = {}) {
      return request("POST", "/payroll/simulate", options);
    },

    /** Liste des campagnes de paie (tenant) */
    getPayrolls(options = {}) {
      return request("GET", "/payrolls", options);
    },

    /** Liste des campagnes de paie (tenant) */
    getPayrolls2(options = {}) {
      return request("GET", "/payrolls", options);
    },

    /** Liste des campagnes de paie (tenant) */
    getPayrolls3(options = {}) {
      return request("GET", "/payrolls", options);
    },

    /** Liste des campagnes de paie (tenant) */
    getPayrolls4(options = {}) {
      return request("GET", "/payrolls", options);
    },

    /** Liste des campagnes de paie (tenant) */
    getPayrolls5(options = {}) {
      return request("GET", "/payrolls", options);
    },

    /** Crée une campagne de paie */
    postPayrolls(options = {}) {
      return request("POST", "/payrolls", options);
    },

    /** Crée une campagne de paie */
    postPayrolls2(options = {}) {
      return request("POST", "/payrolls", options);
    },

    /** Crée une campagne de paie */
    postPayrolls3(options = {}) {
      return request("POST", "/payrolls", options);
    },

    /** Crée une campagne de paie */
    postPayrolls4(options = {}) {
      return request("POST", "/payrolls", options);
    },

    /** Crée une campagne de paie */
    postPayrolls5(options = {}) {
      return request("POST", "/payrolls", options);
    },

    /** Supprimer un run de paie (manager) */
    deletePayrollsByPayroll(options = {}) {
      return request("DELETE", "/payrolls/{payroll}", options);
    },

    /** Supprimer un run de paie (manager) */
    deletePayrollsByPayroll2(options = {}) {
      return request("DELETE", "/payrolls/{payroll}", options);
    },

    /** Supprimer un run de paie (manager) */
    deletePayrollsByPayroll3(options = {}) {
      return request("DELETE", "/payrolls/{payroll}", options);
    },

    /** Supprimer un run de paie (manager) */
    deletePayrollsByPayroll4(options = {}) {
      return request("DELETE", "/payrolls/{payroll}", options);
    },

    /** Supprimer un run de paie (manager) */
    deletePayrollsByPayroll5(options = {}) {
      return request("DELETE", "/payrolls/{payroll}", options);
    },

    /** Supprimer un run de paie (manager) */
    deletePayrollsByPayroll6(options = {}) {
      return request("DELETE", "/payrolls/{payroll}", options);
    },

    /** Supprimer un run de paie (manager) */
    deletePayrollsByPayroll7(options = {}) {
      return request("DELETE", "/payrolls/{payroll}", options);
    },

    /** Supprimer un run de paie (manager) */
    deletePayrollsByPayroll8(options = {}) {
      return request("DELETE", "/payrolls/{payroll}", options);
    },

    /** Supprimer un run de paie (manager) */
    deletePayrollsByPayroll9(options = {}) {
      return request("DELETE", "/payrolls/{payroll}", options);
    },

    /** Supprimer un run de paie (manager) */
    deletePayrollsByPayroll10(options = {}) {
      return request("DELETE", "/payrolls/{payroll}", options);
    },

    /** Supprimer un run de paie (manager) */
    deletePayrollsByPayroll11(options = {}) {
      return request("DELETE", "/payrolls/{payroll}", options);
    },

    /** Détail d'un run de paie */
    getPayrollsByPayroll(options = {}) {
      return request("GET", "/payrolls/{payroll}", options);
    },

    /** Détail d'un run de paie */
    getPayrollsByPayroll2(options = {}) {
      return request("GET", "/payrolls/{payroll}", options);
    },

    /** Détail d'un run de paie */
    getPayrollsByPayroll3(options = {}) {
      return request("GET", "/payrolls/{payroll}", options);
    },

    /** Détail d'un run de paie */
    getPayrollsByPayroll4(options = {}) {
      return request("GET", "/payrolls/{payroll}", options);
    },

    /** Détail d'un run de paie */
    getPayrollsByPayroll5(options = {}) {
      return request("GET", "/payrolls/{payroll}", options);
    },

    /** Détail d'un run de paie */
    getPayrollsByPayroll6(options = {}) {
      return request("GET", "/payrolls/{payroll}", options);
    },

    /** Détail d'un run de paie */
    getPayrollsByPayroll7(options = {}) {
      return request("GET", "/payrolls/{payroll}", options);
    },

    /** Détail d'un run de paie */
    getPayrollsByPayroll8(options = {}) {
      return request("GET", "/payrolls/{payroll}", options);
    },

    /** Détail d'un run de paie */
    getPayrollsByPayroll9(options = {}) {
      return request("GET", "/payrolls/{payroll}", options);
    },

    /** Détail d'un run de paie */
    getPayrollsByPayroll10(options = {}) {
      return request("GET", "/payrolls/{payroll}", options);
    },

    /** Détail d'un run de paie */
    getPayrollsByPayroll11(options = {}) {
      return request("GET", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour partiellement un run de paie (manager) */
    patchPayrollsByPayroll(options = {}) {
      return request("PATCH", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour partiellement un run de paie (manager) */
    patchPayrollsByPayroll2(options = {}) {
      return request("PATCH", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour partiellement un run de paie (manager) */
    patchPayrollsByPayroll3(options = {}) {
      return request("PATCH", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour partiellement un run de paie (manager) */
    patchPayrollsByPayroll4(options = {}) {
      return request("PATCH", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour partiellement un run de paie (manager) */
    patchPayrollsByPayroll5(options = {}) {
      return request("PATCH", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour partiellement un run de paie (manager) */
    patchPayrollsByPayroll6(options = {}) {
      return request("PATCH", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour partiellement un run de paie (manager) */
    patchPayrollsByPayroll7(options = {}) {
      return request("PATCH", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour partiellement un run de paie (manager) */
    patchPayrollsByPayroll8(options = {}) {
      return request("PATCH", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour partiellement un run de paie (manager) */
    patchPayrollsByPayroll9(options = {}) {
      return request("PATCH", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour partiellement un run de paie (manager) */
    patchPayrollsByPayroll10(options = {}) {
      return request("PATCH", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour partiellement un run de paie (manager) */
    patchPayrollsByPayroll11(options = {}) {
      return request("PATCH", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour un run de paie (manager) */
    putPayrollsByPayroll(options = {}) {
      return request("PUT", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour un run de paie (manager) */
    putPayrollsByPayroll2(options = {}) {
      return request("PUT", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour un run de paie (manager) */
    putPayrollsByPayroll3(options = {}) {
      return request("PUT", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour un run de paie (manager) */
    putPayrollsByPayroll4(options = {}) {
      return request("PUT", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour un run de paie (manager) */
    putPayrollsByPayroll5(options = {}) {
      return request("PUT", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour un run de paie (manager) */
    putPayrollsByPayroll6(options = {}) {
      return request("PUT", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour un run de paie (manager) */
    putPayrollsByPayroll7(options = {}) {
      return request("PUT", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour un run de paie (manager) */
    putPayrollsByPayroll8(options = {}) {
      return request("PUT", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour un run de paie (manager) */
    putPayrollsByPayroll9(options = {}) {
      return request("PUT", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour un run de paie (manager) */
    putPayrollsByPayroll10(options = {}) {
      return request("PUT", "/payrolls/{payroll}", options);
    },

    /** Mettre à jour un run de paie (manager) */
    putPayrollsByPayroll11(options = {}) {
      return request("PUT", "/payrolls/{payroll}", options);
    },

    /** Valider un run de paie (manager, même société) */
    postPayrollsByPayrollValidate(options = {}) {
      return request("POST", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (manager, même société) */
    postPayrollsByPayrollValidate2(options = {}) {
      return request("POST", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (manager, même société) */
    postPayrollsByPayrollValidate3(options = {}) {
      return request("POST", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (manager, même société) */
    postPayrollsByPayrollValidate4(options = {}) {
      return request("POST", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (manager, même société) */
    postPayrollsByPayrollValidate5(options = {}) {
      return request("POST", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (manager, même société) */
    postPayrollsByPayrollValidate6(options = {}) {
      return request("POST", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (manager, même société) */
    postPayrollsByPayrollValidate7(options = {}) {
      return request("POST", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (manager, même société) */
    postPayrollsByPayrollValidate8(options = {}) {
      return request("POST", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (manager, même société) */
    postPayrollsByPayrollValidate9(options = {}) {
      return request("POST", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (manager, même société) */
    postPayrollsByPayrollValidate10(options = {}) {
      return request("POST", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (manager, même société) */
    postPayrollsByPayrollValidate11(options = {}) {
      return request("POST", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (déprécié — utiliser POST) */
    putPayrollsByPayrollValidate(options = {}) {
      return request("PUT", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (déprécié — utiliser POST) */
    putPayrollsByPayrollValidate2(options = {}) {
      return request("PUT", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (déprécié — utiliser POST) */
    putPayrollsByPayrollValidate3(options = {}) {
      return request("PUT", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (déprécié — utiliser POST) */
    putPayrollsByPayrollValidate4(options = {}) {
      return request("PUT", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (déprécié — utiliser POST) */
    putPayrollsByPayrollValidate5(options = {}) {
      return request("PUT", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (déprécié — utiliser POST) */
    putPayrollsByPayrollValidate6(options = {}) {
      return request("PUT", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (déprécié — utiliser POST) */
    putPayrollsByPayrollValidate7(options = {}) {
      return request("PUT", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (déprécié — utiliser POST) */
    putPayrollsByPayrollValidate8(options = {}) {
      return request("PUT", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (déprécié — utiliser POST) */
    putPayrollsByPayrollValidate9(options = {}) {
      return request("PUT", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (déprécié — utiliser POST) */
    putPayrollsByPayrollValidate10(options = {}) {
      return request("PUT", "/payrolls/{payroll}/validate", options);
    },

    /** Valider un run de paie (déprécié — utiliser POST) */
    putPayrollsByPayrollValidate11(options = {}) {
      return request("PUT", "/payrolls/{payroll}/validate", options);
    },

    /** Suggestions de rééquilibrage des équipes */
    getPlanningShiftRebalancing(options = {}) {
      return request("GET", "/planning/shift-rebalancing", options);
    },

    /** Suggestions de rééquilibrage des équipes */
    getPlanningShiftRebalancing2(options = {}) {
      return request("GET", "/planning/shift-rebalancing", options);
    },

    /** Suggestions de rééquilibrage des équipes */
    getPlanningShiftRebalancing3(options = {}) {
      return request("GET", "/planning/shift-rebalancing", options);
    },

    /** Suggestions de rééquilibrage des équipes */
    getPlanningShiftRebalancing4(options = {}) {
      return request("GET", "/planning/shift-rebalancing", options);
    },

    /** Suggestions de rééquilibrage des équipes */
    getPlanningShiftRebalancing5(options = {}) {
      return request("GET", "/planning/shift-rebalancing", options);
    },

    /** Suggestions de rééquilibrage des équipes */
    getPlanningShiftRebalancing6(options = {}) {
      return request("GET", "/planning/shift-rebalancing", options);
    },

    /** Suggestions de rééquilibrage des équipes */
    getPlanningShiftRebalancing7(options = {}) {
      return request("GET", "/planning/shift-rebalancing", options);
    },

    /** Suggestions de rééquilibrage des équipes */
    getPlanningShiftRebalancing8(options = {}) {
      return request("GET", "/planning/shift-rebalancing", options);
    },

    /** Suggestions de rééquilibrage des équipes */
    getPlanningShiftRebalancing9(options = {}) {
      return request("GET", "/planning/shift-rebalancing", options);
    },

    /** Suggestions de rééquilibrage des équipes */
    getPlanningShiftRebalancing10(options = {}) {
      return request("GET", "/planning/shift-rebalancing", options);
    },

    /** Suggestions de rééquilibrage des équipes */
    getPlanningShiftRebalancing11(options = {}) {
      return request("GET", "/planning/shift-rebalancing", options);
    },

    /** Optimisation hebdomadaire du planning (suggestions) */
    getPlanningWeeklyOptimization(options = {}) {
      return request("GET", "/planning/weekly-optimization", options);
    },

    /** Optimisation hebdomadaire du planning (suggestions) */
    getPlanningWeeklyOptimization2(options = {}) {
      return request("GET", "/planning/weekly-optimization", options);
    },

    /** Optimisation hebdomadaire du planning (suggestions) */
    getPlanningWeeklyOptimization3(options = {}) {
      return request("GET", "/planning/weekly-optimization", options);
    },

    /** Optimisation hebdomadaire du planning (suggestions) */
    getPlanningWeeklyOptimization4(options = {}) {
      return request("GET", "/planning/weekly-optimization", options);
    },

    /** Optimisation hebdomadaire du planning (suggestions) */
    getPlanningWeeklyOptimization5(options = {}) {
      return request("GET", "/planning/weekly-optimization", options);
    },

    /** Optimisation hebdomadaire du planning (suggestions) */
    getPlanningWeeklyOptimization6(options = {}) {
      return request("GET", "/planning/weekly-optimization", options);
    },

    /** Optimisation hebdomadaire du planning (suggestions) */
    getPlanningWeeklyOptimization7(options = {}) {
      return request("GET", "/planning/weekly-optimization", options);
    },

    /** Optimisation hebdomadaire du planning (suggestions) */
    getPlanningWeeklyOptimization8(options = {}) {
      return request("GET", "/planning/weekly-optimization", options);
    },

    /** Optimisation hebdomadaire du planning (suggestions) */
    getPlanningWeeklyOptimization9(options = {}) {
      return request("GET", "/planning/weekly-optimization", options);
    },

    /** Optimisation hebdomadaire du planning (suggestions) */
    getPlanningWeeklyOptimization10(options = {}) {
      return request("GET", "/planning/weekly-optimization", options);
    },

    /** Optimisation hebdomadaire du planning (suggestions) */
    getPlanningWeeklyOptimization11(options = {}) {
      return request("GET", "/planning/weekly-optimization", options);
    },

    /** Lister les annonces plateforme (super-admin) */
    getPlatformAnnouncements(options = {}) {
      return request("GET", "/platform/announcements", options);
    },

    /** Lister les annonces plateforme (super-admin) */
    getPlatformAnnouncements2(options = {}) {
      return request("GET", "/platform/announcements", options);
    },

    /** Lister les annonces plateforme (super-admin) */
    getPlatformAnnouncements3(options = {}) {
      return request("GET", "/platform/announcements", options);
    },

    /** Lister les annonces plateforme (super-admin) */
    getPlatformAnnouncements4(options = {}) {
      return request("GET", "/platform/announcements", options);
    },

    /** Lister les annonces plateforme (super-admin) */
    getPlatformAnnouncements5(options = {}) {
      return request("GET", "/platform/announcements", options);
    },

    /** Lister les annonces plateforme (super-admin) */
    getPlatformAnnouncements6(options = {}) {
      return request("GET", "/platform/announcements", options);
    },

    /** Lister les annonces plateforme (super-admin) */
    getPlatformAnnouncements7(options = {}) {
      return request("GET", "/platform/announcements", options);
    },

    /** Lister les annonces plateforme (super-admin) */
    getPlatformAnnouncements8(options = {}) {
      return request("GET", "/platform/announcements", options);
    },

    /** Lister les annonces plateforme (super-admin) */
    getPlatformAnnouncements9(options = {}) {
      return request("GET", "/platform/announcements", options);
    },

    /** Lister les annonces plateforme (super-admin) */
    getPlatformAnnouncements10(options = {}) {
      return request("GET", "/platform/announcements", options);
    },

    /** Lister les annonces plateforme (super-admin) */
    getPlatformAnnouncements11(options = {}) {
      return request("GET", "/platform/announcements", options);
    },

    /** Creer une annonce plateforme (maintenance, feature, incident, action requise) */
    postPlatformAnnouncements(options = {}) {
      return request("POST", "/platform/announcements", options);
    },

    /** Creer une annonce plateforme (maintenance, feature, incident, action requise) */
    postPlatformAnnouncements2(options = {}) {
      return request("POST", "/platform/announcements", options);
    },

    /** Creer une annonce plateforme (maintenance, feature, incident, action requise) */
    postPlatformAnnouncements3(options = {}) {
      return request("POST", "/platform/announcements", options);
    },

    /** Creer une annonce plateforme (maintenance, feature, incident, action requise) */
    postPlatformAnnouncements4(options = {}) {
      return request("POST", "/platform/announcements", options);
    },

    /** Creer une annonce plateforme (maintenance, feature, incident, action requise) */
    postPlatformAnnouncements5(options = {}) {
      return request("POST", "/platform/announcements", options);
    },

    /** Creer une annonce plateforme (maintenance, feature, incident, action requise) */
    postPlatformAnnouncements6(options = {}) {
      return request("POST", "/platform/announcements", options);
    },

    /** Creer une annonce plateforme (maintenance, feature, incident, action requise) */
    postPlatformAnnouncements7(options = {}) {
      return request("POST", "/platform/announcements", options);
    },

    /** Creer une annonce plateforme (maintenance, feature, incident, action requise) */
    postPlatformAnnouncements8(options = {}) {
      return request("POST", "/platform/announcements", options);
    },

    /** Creer une annonce plateforme (maintenance, feature, incident, action requise) */
    postPlatformAnnouncements9(options = {}) {
      return request("POST", "/platform/announcements", options);
    },

    /** Creer une annonce plateforme (maintenance, feature, incident, action requise) */
    postPlatformAnnouncements10(options = {}) {
      return request("POST", "/platform/announcements", options);
    },

    /** Creer une annonce plateforme (maintenance, feature, incident, action requise) */
    postPlatformAnnouncements11(options = {}) {
      return request("POST", "/platform/announcements", options);
    },

    /** Supprimer une annonce plateforme */
    deletePlatformAnnouncementsByAnnouncement(options = {}) {
      return request("DELETE", "/platform/announcements/{announcement}", options);
    },

    /** Supprimer une annonce plateforme */
    deletePlatformAnnouncementsByAnnouncement2(options = {}) {
      return request("DELETE", "/platform/announcements/{announcement}", options);
    },

    /** Supprimer une annonce plateforme */
    deletePlatformAnnouncementsByAnnouncement3(options = {}) {
      return request("DELETE", "/platform/announcements/{announcement}", options);
    },

    /** Supprimer une annonce plateforme */
    deletePlatformAnnouncementsByAnnouncement4(options = {}) {
      return request("DELETE", "/platform/announcements/{announcement}", options);
    },

    /** Supprimer une annonce plateforme */
    deletePlatformAnnouncementsByAnnouncement5(options = {}) {
      return request("DELETE", "/platform/announcements/{announcement}", options);
    },

    /** Supprimer une annonce plateforme */
    deletePlatformAnnouncementsByAnnouncement6(options = {}) {
      return request("DELETE", "/platform/announcements/{announcement}", options);
    },

    /** Supprimer une annonce plateforme */
    deletePlatformAnnouncementsByAnnouncement7(options = {}) {
      return request("DELETE", "/platform/announcements/{announcement}", options);
    },

    /** Supprimer une annonce plateforme */
    deletePlatformAnnouncementsByAnnouncement8(options = {}) {
      return request("DELETE", "/platform/announcements/{announcement}", options);
    },

    /** Supprimer une annonce plateforme */
    deletePlatformAnnouncementsByAnnouncement9(options = {}) {
      return request("DELETE", "/platform/announcements/{announcement}", options);
    },

    /** Supprimer une annonce plateforme */
    deletePlatformAnnouncementsByAnnouncement10(options = {}) {
      return request("DELETE", "/platform/announcements/{announcement}", options);
    },

    /** Supprimer une annonce plateforme */
    deletePlatformAnnouncementsByAnnouncement11(options = {}) {
      return request("DELETE", "/platform/announcements/{announcement}", options);
    },

    /** Detail d'une annonce plateforme */
    getPlatformAnnouncementsByAnnouncement(options = {}) {
      return request("GET", "/platform/announcements/{announcement}", options);
    },

    /** Detail d'une annonce plateforme */
    getPlatformAnnouncementsByAnnouncement2(options = {}) {
      return request("GET", "/platform/announcements/{announcement}", options);
    },

    /** Detail d'une annonce plateforme */
    getPlatformAnnouncementsByAnnouncement3(options = {}) {
      return request("GET", "/platform/announcements/{announcement}", options);
    },

    /** Detail d'une annonce plateforme */
    getPlatformAnnouncementsByAnnouncement4(options = {}) {
      return request("GET", "/platform/announcements/{announcement}", options);
    },

    /** Detail d'une annonce plateforme */
    getPlatformAnnouncementsByAnnouncement5(options = {}) {
      return request("GET", "/platform/announcements/{announcement}", options);
    },

    /** Detail d'une annonce plateforme */
    getPlatformAnnouncementsByAnnouncement6(options = {}) {
      return request("GET", "/platform/announcements/{announcement}", options);
    },

    /** Detail d'une annonce plateforme */
    getPlatformAnnouncementsByAnnouncement7(options = {}) {
      return request("GET", "/platform/announcements/{announcement}", options);
    },

    /** Detail d'une annonce plateforme */
    getPlatformAnnouncementsByAnnouncement8(options = {}) {
      return request("GET", "/platform/announcements/{announcement}", options);
    },

    /** Detail d'une annonce plateforme */
    getPlatformAnnouncementsByAnnouncement9(options = {}) {
      return request("GET", "/platform/announcements/{announcement}", options);
    },

    /** Detail d'une annonce plateforme */
    getPlatformAnnouncementsByAnnouncement10(options = {}) {
      return request("GET", "/platform/announcements/{announcement}", options);
    },

    /** Detail d'une annonce plateforme */
    getPlatformAnnouncementsByAnnouncement11(options = {}) {
      return request("GET", "/platform/announcements/{announcement}", options);
    },

    /** Désactiver la 2FA (mot de passe requis) */
    postPlatformAuth2faDisable(options = {}) {
      return request("POST", "/platform/auth/2fa/disable", options);
    },

    /** Désactiver la 2FA (mot de passe requis) */
    postPlatformAuth2faDisable2(options = {}) {
      return request("POST", "/platform/auth/2fa/disable", options);
    },

    /** Désactiver la 2FA (mot de passe requis) */
    postPlatformAuth2faDisable3(options = {}) {
      return request("POST", "/platform/auth/2fa/disable", options);
    },

    /** Désactiver la 2FA (mot de passe requis) */
    postPlatformAuth2faDisable4(options = {}) {
      return request("POST", "/platform/auth/2fa/disable", options);
    },

    /** Désactiver la 2FA (mot de passe requis) */
    postPlatformAuth2faDisable5(options = {}) {
      return request("POST", "/platform/auth/2fa/disable", options);
    },

    /** Activer la 2FA avec le code de confirmation */
    postPlatformAuth2faEnable(options = {}) {
      return request("POST", "/platform/auth/2fa/enable", options);
    },

    /** Activer la 2FA avec le code de confirmation */
    postPlatformAuth2faEnable2(options = {}) {
      return request("POST", "/platform/auth/2fa/enable", options);
    },

    /** Activer la 2FA avec le code de confirmation */
    postPlatformAuth2faEnable3(options = {}) {
      return request("POST", "/platform/auth/2fa/enable", options);
    },

    /** Activer la 2FA avec le code de confirmation */
    postPlatformAuth2faEnable4(options = {}) {
      return request("POST", "/platform/auth/2fa/enable", options);
    },

    /** Activer la 2FA avec le code de confirmation */
    postPlatformAuth2faEnable5(options = {}) {
      return request("POST", "/platform/auth/2fa/enable", options);
    },

    /** Démarrer la configuration 2FA (secret TOTP) */
    postPlatformAuth2faSetup(options = {}) {
      return request("POST", "/platform/auth/2fa/setup", options);
    },

    /** Démarrer la configuration 2FA (secret TOTP) */
    postPlatformAuth2faSetup2(options = {}) {
      return request("POST", "/platform/auth/2fa/setup", options);
    },

    /** Démarrer la configuration 2FA (secret TOTP) */
    postPlatformAuth2faSetup3(options = {}) {
      return request("POST", "/platform/auth/2fa/setup", options);
    },

    /** Démarrer la configuration 2FA (secret TOTP) */
    postPlatformAuth2faSetup4(options = {}) {
      return request("POST", "/platform/auth/2fa/setup", options);
    },

    /** Démarrer la configuration 2FA (secret TOTP) */
    postPlatformAuth2faSetup5(options = {}) {
      return request("POST", "/platform/auth/2fa/setup", options);
    },

    /** Changer le mot de passe du super-admin */
    postPlatformAuthChangePassword(options = {}) {
      return request("POST", "/platform/auth/change-password", options);
    },

    /** Changer le mot de passe du super-admin */
    postPlatformAuthChangePassword2(options = {}) {
      return request("POST", "/platform/auth/change-password", options);
    },

    /** Changer le mot de passe du super-admin */
    postPlatformAuthChangePassword3(options = {}) {
      return request("POST", "/platform/auth/change-password", options);
    },

    /** Changer le mot de passe du super-admin */
    postPlatformAuthChangePassword4(options = {}) {
      return request("POST", "/platform/auth/change-password", options);
    },

    /** Changer le mot de passe du super-admin */
    postPlatformAuthChangePassword5(options = {}) {
      return request("POST", "/platform/auth/change-password", options);
    },

    /** Connexion super-admin */
    postPlatformAuthLogin(options = {}) {
      return request("POST", "/platform/auth/login", options);
    },

    /** Connexion super-admin */
    postPlatformAuthLogin2(options = {}) {
      return request("POST", "/platform/auth/login", options);
    },

    /** Connexion super-admin */
    postPlatformAuthLogin3(options = {}) {
      return request("POST", "/platform/auth/login", options);
    },

    /** Connexion super-admin */
    postPlatformAuthLogin4(options = {}) {
      return request("POST", "/platform/auth/login", options);
    },

    /** Connexion super-admin */
    postPlatformAuthLogin5(options = {}) {
      return request("POST", "/platform/auth/login", options);
    },

    /** Deconnexion super-admin */
    postPlatformAuthLogout(options = {}) {
      return request("POST", "/platform/auth/logout", options);
    },

    /** Deconnexion super-admin */
    postPlatformAuthLogout2(options = {}) {
      return request("POST", "/platform/auth/logout", options);
    },

    /** Deconnexion super-admin */
    postPlatformAuthLogout3(options = {}) {
      return request("POST", "/platform/auth/logout", options);
    },

    /** Deconnexion super-admin */
    postPlatformAuthLogout4(options = {}) {
      return request("POST", "/platform/auth/logout", options);
    },

    /** Deconnexion super-admin */
    postPlatformAuthLogout5(options = {}) {
      return request("POST", "/platform/auth/logout", options);
    },

    /** Super-admin courant */
    getPlatformAuthMe(options = {}) {
      return request("GET", "/platform/auth/me", options);
    },

    /** Super-admin courant */
    getPlatformAuthMe2(options = {}) {
      return request("GET", "/platform/auth/me", options);
    },

    /** Super-admin courant */
    getPlatformAuthMe3(options = {}) {
      return request("GET", "/platform/auth/me", options);
    },

    /** Super-admin courant */
    getPlatformAuthMe4(options = {}) {
      return request("GET", "/platform/auth/me", options);
    },

    /** Super-admin courant */
    getPlatformAuthMe5(options = {}) {
      return request("GET", "/platform/auth/me", options);
    },

    /** Mettre à jour le profil du super-admin */
    patchPlatformAuthProfile(options = {}) {
      return request("PATCH", "/platform/auth/profile", options);
    },

    /** Mettre à jour le profil du super-admin */
    patchPlatformAuthProfile2(options = {}) {
      return request("PATCH", "/platform/auth/profile", options);
    },

    /** Mettre à jour le profil du super-admin */
    patchPlatformAuthProfile3(options = {}) {
      return request("PATCH", "/platform/auth/profile", options);
    },

    /** Mettre à jour le profil du super-admin */
    patchPlatformAuthProfile4(options = {}) {
      return request("PATCH", "/platform/auth/profile", options);
    },

    /** Mettre à jour le profil du super-admin */
    patchPlatformAuthProfile5(options = {}) {
      return request("PATCH", "/platform/auth/profile", options);
    },

    /** Lister les societes */
    getPlatformCompanies(options = {}) {
      return request("GET", "/platform/companies", options);
    },

    /** Lister les societes */
    getPlatformCompanies2(options = {}) {
      return request("GET", "/platform/companies", options);
    },

    /** Lister les societes */
    getPlatformCompanies3(options = {}) {
      return request("GET", "/platform/companies", options);
    },

    /** Lister les societes */
    getPlatformCompanies4(options = {}) {
      return request("GET", "/platform/companies", options);
    },

    /** Lister les societes */
    getPlatformCompanies5(options = {}) {
      return request("GET", "/platform/companies", options);
    },

    /** Creer une societe shared */
    postPlatformCompanies(options = {}) {
      return request("POST", "/platform/companies", options);
    },

    /** Creer une societe shared */
    postPlatformCompanies2(options = {}) {
      return request("POST", "/platform/companies", options);
    },

    /** Creer une societe shared */
    postPlatformCompanies3(options = {}) {
      return request("POST", "/platform/companies", options);
    },

    /** Creer une societe shared */
    postPlatformCompanies4(options = {}) {
      return request("POST", "/platform/companies", options);
    },

    /** Creer une societe shared */
    postPlatformCompanies5(options = {}) {
      return request("POST", "/platform/companies", options);
    },

    /** Reformer/choisir le pays legal d'un tenant (invariant 9) */
    patchPlatformCompaniesByCompanyCountry(options = {}) {
      return request("PATCH", "/platform/companies/{company}/country", options);
    },

    /** Reformer/choisir le pays legal d'un tenant (invariant 9) */
    patchPlatformCompaniesByCompanyCountry2(options = {}) {
      return request("PATCH", "/platform/companies/{company}/country", options);
    },

    /** Reformer/choisir le pays legal d'un tenant (invariant 9) */
    patchPlatformCompaniesByCompanyCountry3(options = {}) {
      return request("PATCH", "/platform/companies/{company}/country", options);
    },

    /** Reformer/choisir le pays legal d'un tenant (invariant 9) */
    patchPlatformCompaniesByCompanyCountry4(options = {}) {
      return request("PATCH", "/platform/companies/{company}/country", options);
    },

    /** Reformer/choisir le pays legal d'un tenant (invariant 9) */
    patchPlatformCompaniesByCompanyCountry5(options = {}) {
      return request("PATCH", "/platform/companies/{company}/country", options);
    },

    /** Lire les feature flags d'une societe */
    getPlatformCompaniesByCompanyFeatures(options = {}) {
      return request("GET", "/platform/companies/{company}/features", options);
    },

    /** Lire les feature flags d'une societe */
    getPlatformCompaniesByCompanyFeatures2(options = {}) {
      return request("GET", "/platform/companies/{company}/features", options);
    },

    /** Lire les feature flags d'une societe */
    getPlatformCompaniesByCompanyFeatures3(options = {}) {
      return request("GET", "/platform/companies/{company}/features", options);
    },

    /** Lire les feature flags d'une societe */
    getPlatformCompaniesByCompanyFeatures4(options = {}) {
      return request("GET", "/platform/companies/{company}/features", options);
    },

    /** Lire les feature flags d'une societe */
    getPlatformCompaniesByCompanyFeatures5(options = {}) {
      return request("GET", "/platform/companies/{company}/features", options);
    },

    /** Mettre a jour les feature flags d'une societe */
    patchPlatformCompaniesByCompanyFeatures(options = {}) {
      return request("PATCH", "/platform/companies/{company}/features", options);
    },

    /** Mettre a jour les feature flags d'une societe */
    patchPlatformCompaniesByCompanyFeatures2(options = {}) {
      return request("PATCH", "/platform/companies/{company}/features", options);
    },

    /** Mettre a jour les feature flags d'une societe */
    patchPlatformCompaniesByCompanyFeatures3(options = {}) {
      return request("PATCH", "/platform/companies/{company}/features", options);
    },

    /** Mettre a jour les feature flags d'une societe */
    patchPlatformCompaniesByCompanyFeatures4(options = {}) {
      return request("PATCH", "/platform/companies/{company}/features", options);
    },

    /** Mettre a jour les feature flags d'une societe */
    patchPlatformCompaniesByCompanyFeatures5(options = {}) {
      return request("PATCH", "/platform/companies/{company}/features", options);
    },

    /** Health detaille d'une societe */
    getPlatformCompaniesByCompanyHealth(options = {}) {
      return request("GET", "/platform/companies/{company}/health", options);
    },

    /** Health detaille d'une societe */
    getPlatformCompaniesByCompanyHealth2(options = {}) {
      return request("GET", "/platform/companies/{company}/health", options);
    },

    /** Health detaille d'une societe */
    getPlatformCompaniesByCompanyHealth3(options = {}) {
      return request("GET", "/platform/companies/{company}/health", options);
    },

    /** Health detaille d'une societe */
    getPlatformCompaniesByCompanyHealth4(options = {}) {
      return request("GET", "/platform/companies/{company}/health", options);
    },

    /** Health detaille d'une societe */
    getPlatformCompaniesByCompanyHealth5(options = {}) {
      return request("GET", "/platform/companies/{company}/health", options);
    },

    /** Lire l'abonnement plateforme d'une societe */
    getPlatformCompaniesByCompanySubscription(options = {}) {
      return request("GET", "/platform/companies/{company}/subscription", options);
    },

    /** Lire l'abonnement plateforme d'une societe */
    getPlatformCompaniesByCompanySubscription2(options = {}) {
      return request("GET", "/platform/companies/{company}/subscription", options);
    },

    /** Lire l'abonnement plateforme d'une societe */
    getPlatformCompaniesByCompanySubscription3(options = {}) {
      return request("GET", "/platform/companies/{company}/subscription", options);
    },

    /** Lire l'abonnement plateforme d'une societe */
    getPlatformCompaniesByCompanySubscription4(options = {}) {
      return request("GET", "/platform/companies/{company}/subscription", options);
    },

    /** Lire l'abonnement plateforme d'une societe */
    getPlatformCompaniesByCompanySubscription5(options = {}) {
      return request("GET", "/platform/companies/{company}/subscription", options);
    },

    /** Mettre a jour l'abonnement plateforme d'une societe */
    patchPlatformCompaniesByCompanySubscription(options = {}) {
      return request("PATCH", "/platform/companies/{company}/subscription", options);
    },

    /** Mettre a jour l'abonnement plateforme d'une societe */
    patchPlatformCompaniesByCompanySubscription2(options = {}) {
      return request("PATCH", "/platform/companies/{company}/subscription", options);
    },

    /** Mettre a jour l'abonnement plateforme d'une societe */
    patchPlatformCompaniesByCompanySubscription3(options = {}) {
      return request("PATCH", "/platform/companies/{company}/subscription", options);
    },

    /** Mettre a jour l'abonnement plateforme d'une societe */
    patchPlatformCompaniesByCompanySubscription4(options = {}) {
      return request("PATCH", "/platform/companies/{company}/subscription", options);
    },

    /** Mettre a jour l'abonnement plateforme d'une societe */
    patchPlatformCompaniesByCompanySubscription5(options = {}) {
      return request("PATCH", "/platform/companies/{company}/subscription", options);
    },

    /** Vue portefeuille health des societes */
    getPlatformCompaniesHealth(options = {}) {
      return request("GET", "/platform/companies/health", options);
    },

    /** Vue portefeuille health des societes */
    getPlatformCompaniesHealth2(options = {}) {
      return request("GET", "/platform/companies/health", options);
    },

    /** Vue portefeuille health des societes */
    getPlatformCompaniesHealth3(options = {}) {
      return request("GET", "/platform/companies/health", options);
    },

    /** Vue portefeuille health des societes */
    getPlatformCompaniesHealth4(options = {}) {
      return request("GET", "/platform/companies/health", options);
    },

    /** Vue portefeuille health des societes */
    getPlatformCompaniesHealth5(options = {}) {
      return request("GET", "/platform/companies/health", options);
    },

    /** Lister les demandes de creation de societe */
    getPlatformCompanyRequests(options = {}) {
      return request("GET", "/platform/company-requests", options);
    },

    /** Lister les demandes de creation de societe */
    getPlatformCompanyRequests2(options = {}) {
      return request("GET", "/platform/company-requests", options);
    },

    /** Lister les demandes de creation de societe */
    getPlatformCompanyRequests3(options = {}) {
      return request("GET", "/platform/company-requests", options);
    },

    /** Lister les demandes de creation de societe */
    getPlatformCompanyRequests4(options = {}) {
      return request("GET", "/platform/company-requests", options);
    },

    /** Lister les demandes de creation de societe */
    getPlatformCompanyRequests5(options = {}) {
      return request("GET", "/platform/company-requests", options);
    },

    /** Lire une demande de creation de societe */
    getPlatformCompanyRequestsById(options = {}) {
      return request("GET", "/platform/company-requests/{id}", options);
    },

    /** Lire une demande de creation de societe */
    getPlatformCompanyRequestsById2(options = {}) {
      return request("GET", "/platform/company-requests/{id}", options);
    },

    /** Lire une demande de creation de societe */
    getPlatformCompanyRequestsById3(options = {}) {
      return request("GET", "/platform/company-requests/{id}", options);
    },

    /** Lire une demande de creation de societe */
    getPlatformCompanyRequestsById4(options = {}) {
      return request("GET", "/platform/company-requests/{id}", options);
    },

    /** Lire une demande de creation de societe */
    getPlatformCompanyRequestsById5(options = {}) {
      return request("GET", "/platform/company-requests/{id}", options);
    },

    /** Approuver ou rejeter une demande de creation de societe */
    patchPlatformCompanyRequestsById(options = {}) {
      return request("PATCH", "/platform/company-requests/{id}", options);
    },

    /** Approuver ou rejeter une demande de creation de societe */
    patchPlatformCompanyRequestsById2(options = {}) {
      return request("PATCH", "/platform/company-requests/{id}", options);
    },

    /** Approuver ou rejeter une demande de creation de societe */
    patchPlatformCompanyRequestsById3(options = {}) {
      return request("PATCH", "/platform/company-requests/{id}", options);
    },

    /** Approuver ou rejeter une demande de creation de societe */
    patchPlatformCompanyRequestsById4(options = {}) {
      return request("PATCH", "/platform/company-requests/{id}", options);
    },

    /** Approuver ou rejeter une demande de creation de societe */
    patchPlatformCompanyRequestsById5(options = {}) {
      return request("PATCH", "/platform/company-requests/{id}", options);
    },

    /** Lister les pays supportes et leurs defaults */
    getPlatformCountryDefaults(options = {}) {
      return request("GET", "/platform/country-defaults", options);
    },

    /** Lister les pays supportes et leurs defaults */
    getPlatformCountryDefaults2(options = {}) {
      return request("GET", "/platform/country-defaults", options);
    },

    /** Lister les pays supportes et leurs defaults */
    getPlatformCountryDefaults3(options = {}) {
      return request("GET", "/platform/country-defaults", options);
    },

    /** Lister les pays supportes et leurs defaults */
    getPlatformCountryDefaults4(options = {}) {
      return request("GET", "/platform/country-defaults", options);
    },

    /** Lister les pays supportes et leurs defaults */
    getPlatformCountryDefaults5(options = {}) {
      return request("GET", "/platform/country-defaults", options);
    },

    /** Pipeline CRM des demandes clients (leads, essais, actifs, rejetes) */
    getPlatformCrmPipeline(options = {}) {
      return request("GET", "/platform/crm/pipeline", options);
    },

    /** Pipeline CRM des demandes clients (leads, essais, actifs, rejetes) */
    getPlatformCrmPipeline2(options = {}) {
      return request("GET", "/platform/crm/pipeline", options);
    },

    /** Pipeline CRM des demandes clients (leads, essais, actifs, rejetes) */
    getPlatformCrmPipeline3(options = {}) {
      return request("GET", "/platform/crm/pipeline", options);
    },

    /** Pipeline CRM des demandes clients (leads, essais, actifs, rejetes) */
    getPlatformCrmPipeline4(options = {}) {
      return request("GET", "/platform/crm/pipeline", options);
    },

    /** Pipeline CRM des demandes clients (leads, essais, actifs, rejetes) */
    getPlatformCrmPipeline5(options = {}) {
      return request("GET", "/platform/crm/pipeline", options);
    },

    /** Pipeline CRM des demandes clients (leads, essais, actifs, rejetes) */
    getPlatformCrmPipeline6(options = {}) {
      return request("GET", "/platform/crm/pipeline", options);
    },

    /** Pipeline CRM des demandes clients (leads, essais, actifs, rejetes) */
    getPlatformCrmPipeline7(options = {}) {
      return request("GET", "/platform/crm/pipeline", options);
    },

    /** Pipeline CRM des demandes clients (leads, essais, actifs, rejetes) */
    getPlatformCrmPipeline8(options = {}) {
      return request("GET", "/platform/crm/pipeline", options);
    },

    /** Pipeline CRM des demandes clients (leads, essais, actifs, rejetes) */
    getPlatformCrmPipeline9(options = {}) {
      return request("GET", "/platform/crm/pipeline", options);
    },

    /** Pipeline CRM des demandes clients (leads, essais, actifs, rejetes) */
    getPlatformCrmPipeline10(options = {}) {
      return request("GET", "/platform/crm/pipeline", options);
    },

    /** Pipeline CRM des demandes clients (leads, essais, actifs, rejetes) */
    getPlatformCrmPipeline11(options = {}) {
      return request("GET", "/platform/crm/pipeline", options);
    },

    /** Lister les nœuds edge de la plateforme (super-admin) */
    getPlatformEdgeNodes(options = {}) {
      return request("GET", "/platform/edge/nodes", options);
    },

    /** Lister les nœuds edge de la plateforme (super-admin) */
    getPlatformEdgeNodes2(options = {}) {
      return request("GET", "/platform/edge/nodes", options);
    },

    /** Lister les nœuds edge de la plateforme (super-admin) */
    getPlatformEdgeNodes3(options = {}) {
      return request("GET", "/platform/edge/nodes", options);
    },

    /** Lister les nœuds edge de la plateforme (super-admin) */
    getPlatformEdgeNodes4(options = {}) {
      return request("GET", "/platform/edge/nodes", options);
    },

    /** Lister les nœuds edge de la plateforme (super-admin) */
    getPlatformEdgeNodes5(options = {}) {
      return request("GET", "/platform/edge/nodes", options);
    },

    /** Lister les nœuds edge de la plateforme (super-admin) */
    getPlatformEdgeNodes6(options = {}) {
      return request("GET", "/platform/edge/nodes", options);
    },

    /** Lister les nœuds edge de la plateforme (super-admin) */
    getPlatformEdgeNodes7(options = {}) {
      return request("GET", "/platform/edge/nodes", options);
    },

    /** Lister les nœuds edge de la plateforme (super-admin) */
    getPlatformEdgeNodes8(options = {}) {
      return request("GET", "/platform/edge/nodes", options);
    },

    /** Lister les nœuds edge de la plateforme (super-admin) */
    getPlatformEdgeNodes9(options = {}) {
      return request("GET", "/platform/edge/nodes", options);
    },

    /** Lister les nœuds edge de la plateforme (super-admin) */
    getPlatformEdgeNodes10(options = {}) {
      return request("GET", "/platform/edge/nodes", options);
    },

    /** Lister les nœuds edge de la plateforme (super-admin) */
    getPlatformEdgeNodes11(options = {}) {
      return request("GET", "/platform/edge/nodes", options);
    },

    /** Révoquer un nœud edge (super-admin) */
    deletePlatformEdgeNodesByNodeId(options = {}) {
      return request("DELETE", "/platform/edge/nodes/{nodeId}", options);
    },

    /** Révoquer un nœud edge (super-admin) */
    deletePlatformEdgeNodesByNodeId2(options = {}) {
      return request("DELETE", "/platform/edge/nodes/{nodeId}", options);
    },

    /** Révoquer un nœud edge (super-admin) */
    deletePlatformEdgeNodesByNodeId3(options = {}) {
      return request("DELETE", "/platform/edge/nodes/{nodeId}", options);
    },

    /** Révoquer un nœud edge (super-admin) */
    deletePlatformEdgeNodesByNodeId4(options = {}) {
      return request("DELETE", "/platform/edge/nodes/{nodeId}", options);
    },

    /** Révoquer un nœud edge (super-admin) */
    deletePlatformEdgeNodesByNodeId5(options = {}) {
      return request("DELETE", "/platform/edge/nodes/{nodeId}", options);
    },

    /** Révoquer un nœud edge (super-admin) */
    deletePlatformEdgeNodesByNodeId6(options = {}) {
      return request("DELETE", "/platform/edge/nodes/{nodeId}", options);
    },

    /** Révoquer un nœud edge (super-admin) */
    deletePlatformEdgeNodesByNodeId7(options = {}) {
      return request("DELETE", "/platform/edge/nodes/{nodeId}", options);
    },

    /** Révoquer un nœud edge (super-admin) */
    deletePlatformEdgeNodesByNodeId8(options = {}) {
      return request("DELETE", "/platform/edge/nodes/{nodeId}", options);
    },

    /** Révoquer un nœud edge (super-admin) */
    deletePlatformEdgeNodesByNodeId9(options = {}) {
      return request("DELETE", "/platform/edge/nodes/{nodeId}", options);
    },

    /** Révoquer un nœud edge (super-admin) */
    deletePlatformEdgeNodesByNodeId10(options = {}) {
      return request("DELETE", "/platform/edge/nodes/{nodeId}", options);
    },

    /** Révoquer un nœud edge (super-admin) */
    deletePlatformEdgeNodesByNodeId11(options = {}) {
      return request("DELETE", "/platform/edge/nodes/{nodeId}", options);
    },

    /** Forcer la synchronisation d'un nœud edge (super-admin) */
    postPlatformEdgeNodesByNodeIdSync(options = {}) {
      return request("POST", "/platform/edge/nodes/{nodeId}/sync", options);
    },

    /** Forcer la synchronisation d'un nœud edge (super-admin) */
    postPlatformEdgeNodesByNodeIdSync2(options = {}) {
      return request("POST", "/platform/edge/nodes/{nodeId}/sync", options);
    },

    /** Forcer la synchronisation d'un nœud edge (super-admin) */
    postPlatformEdgeNodesByNodeIdSync3(options = {}) {
      return request("POST", "/platform/edge/nodes/{nodeId}/sync", options);
    },

    /** Forcer la synchronisation d'un nœud edge (super-admin) */
    postPlatformEdgeNodesByNodeIdSync4(options = {}) {
      return request("POST", "/platform/edge/nodes/{nodeId}/sync", options);
    },

    /** Forcer la synchronisation d'un nœud edge (super-admin) */
    postPlatformEdgeNodesByNodeIdSync5(options = {}) {
      return request("POST", "/platform/edge/nodes/{nodeId}/sync", options);
    },

    /** Forcer la synchronisation d'un nœud edge (super-admin) */
    postPlatformEdgeNodesByNodeIdSync6(options = {}) {
      return request("POST", "/platform/edge/nodes/{nodeId}/sync", options);
    },

    /** Forcer la synchronisation d'un nœud edge (super-admin) */
    postPlatformEdgeNodesByNodeIdSync7(options = {}) {
      return request("POST", "/platform/edge/nodes/{nodeId}/sync", options);
    },

    /** Forcer la synchronisation d'un nœud edge (super-admin) */
    postPlatformEdgeNodesByNodeIdSync8(options = {}) {
      return request("POST", "/platform/edge/nodes/{nodeId}/sync", options);
    },

    /** Forcer la synchronisation d'un nœud edge (super-admin) */
    postPlatformEdgeNodesByNodeIdSync9(options = {}) {
      return request("POST", "/platform/edge/nodes/{nodeId}/sync", options);
    },

    /** Forcer la synchronisation d'un nœud edge (super-admin) */
    postPlatformEdgeNodesByNodeIdSync10(options = {}) {
      return request("POST", "/platform/edge/nodes/{nodeId}/sync", options);
    },

    /** Forcer la synchronisation d'un nœud edge (super-admin) */
    postPlatformEdgeNodesByNodeIdSync11(options = {}) {
      return request("POST", "/platform/edge/nodes/{nodeId}/sync", options);
    },

    /** Historique des opérations growth (super-admin) */
    getPlatformGrowthHistory(options = {}) {
      return request("GET", "/platform/growth/history", options);
    },

    /** Historique des opérations growth (super-admin) */
    getPlatformGrowthHistory2(options = {}) {
      return request("GET", "/platform/growth/history", options);
    },

    /** Historique des opérations growth (super-admin) */
    getPlatformGrowthHistory3(options = {}) {
      return request("GET", "/platform/growth/history", options);
    },

    /** Historique des opérations growth (super-admin) */
    getPlatformGrowthHistory4(options = {}) {
      return request("GET", "/platform/growth/history", options);
    },

    /** Historique des opérations growth (super-admin) */
    getPlatformGrowthHistory5(options = {}) {
      return request("GET", "/platform/growth/history", options);
    },

    /** Historique des opérations growth (super-admin) */
    getPlatformGrowthHistory6(options = {}) {
      return request("GET", "/platform/growth/history", options);
    },

    /** Historique des opérations growth (super-admin) */
    getPlatformGrowthHistory7(options = {}) {
      return request("GET", "/platform/growth/history", options);
    },

    /** Historique des opérations growth (super-admin) */
    getPlatformGrowthHistory8(options = {}) {
      return request("GET", "/platform/growth/history", options);
    },

    /** Historique des opérations growth (super-admin) */
    getPlatformGrowthHistory9(options = {}) {
      return request("GET", "/platform/growth/history", options);
    },

    /** Historique des opérations growth (super-admin) */
    getPlatformGrowthHistory10(options = {}) {
      return request("GET", "/platform/growth/history", options);
    },

    /** Historique des opérations growth (super-admin) */
    getPlatformGrowthHistory11(options = {}) {
      return request("GET", "/platform/growth/history", options);
    },

    /** Lister les partenaires (admin) */
    getPlatformGrowthPartners(options = {}) {
      return request("GET", "/platform/growth/partners", options);
    },

    /** Lister les partenaires (admin) */
    getPlatformGrowthPartners2(options = {}) {
      return request("GET", "/platform/growth/partners", options);
    },

    /** Lister les partenaires (admin) */
    getPlatformGrowthPartners3(options = {}) {
      return request("GET", "/platform/growth/partners", options);
    },

    /** Lister les partenaires (admin) */
    getPlatformGrowthPartners4(options = {}) {
      return request("GET", "/platform/growth/partners", options);
    },

    /** Lister les partenaires (admin) */
    getPlatformGrowthPartners5(options = {}) {
      return request("GET", "/platform/growth/partners", options);
    },

    /** Lister les partenaires (admin) */
    getPlatformGrowthPartners6(options = {}) {
      return request("GET", "/platform/growth/partners", options);
    },

    /** Lister les partenaires (admin) */
    getPlatformGrowthPartners7(options = {}) {
      return request("GET", "/platform/growth/partners", options);
    },

    /** Lister les partenaires (admin) */
    getPlatformGrowthPartners8(options = {}) {
      return request("GET", "/platform/growth/partners", options);
    },

    /** Lister les partenaires (admin) */
    getPlatformGrowthPartners9(options = {}) {
      return request("GET", "/platform/growth/partners", options);
    },

    /** Lister les partenaires (admin) */
    getPlatformGrowthPartners10(options = {}) {
      return request("GET", "/platform/growth/partners", options);
    },

    /** Lister les partenaires (admin) */
    getPlatformGrowthPartners11(options = {}) {
      return request("GET", "/platform/growth/partners", options);
    },

    /** Mettre à jour le statut de candidature d'un partenaire (super-admin) */
    patchPlatformGrowthPartnersByPartnerApplication(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/application", options);
    },

    /** Mettre à jour le statut de candidature d'un partenaire (super-admin) */
    patchPlatformGrowthPartnersByPartnerApplication2(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/application", options);
    },

    /** Mettre à jour le statut de candidature d'un partenaire (super-admin) */
    patchPlatformGrowthPartnersByPartnerApplication3(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/application", options);
    },

    /** Mettre à jour le statut de candidature d'un partenaire (super-admin) */
    patchPlatformGrowthPartnersByPartnerApplication4(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/application", options);
    },

    /** Mettre à jour le statut de candidature d'un partenaire (super-admin) */
    patchPlatformGrowthPartnersByPartnerApplication5(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/application", options);
    },

    /** Mettre à jour le statut de candidature d'un partenaire (super-admin) */
    patchPlatformGrowthPartnersByPartnerApplication6(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/application", options);
    },

    /** Mettre à jour le statut de candidature d'un partenaire (super-admin) */
    patchPlatformGrowthPartnersByPartnerApplication7(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/application", options);
    },

    /** Mettre à jour le statut de candidature d'un partenaire (super-admin) */
    patchPlatformGrowthPartnersByPartnerApplication8(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/application", options);
    },

    /** Mettre à jour le statut de candidature d'un partenaire (super-admin) */
    patchPlatformGrowthPartnersByPartnerApplication9(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/application", options);
    },

    /** Mettre à jour le statut de candidature d'un partenaire (super-admin) */
    patchPlatformGrowthPartnersByPartnerApplication10(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/application", options);
    },

    /** Mettre à jour le statut de candidature d'un partenaire (super-admin) */
    patchPlatformGrowthPartnersByPartnerApplication11(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/application", options);
    },

    /** Mettre à jour le taux de commission d'un partenaire */
    patchPlatformGrowthPartnersByPartnerRate(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/rate", options);
    },

    /** Mettre à jour le taux de commission d'un partenaire */
    patchPlatformGrowthPartnersByPartnerRate2(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/rate", options);
    },

    /** Mettre à jour le taux de commission d'un partenaire */
    patchPlatformGrowthPartnersByPartnerRate3(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/rate", options);
    },

    /** Mettre à jour le taux de commission d'un partenaire */
    patchPlatformGrowthPartnersByPartnerRate4(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/rate", options);
    },

    /** Mettre à jour le taux de commission d'un partenaire */
    patchPlatformGrowthPartnersByPartnerRate5(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/rate", options);
    },

    /** Mettre à jour le taux de commission d'un partenaire */
    patchPlatformGrowthPartnersByPartnerRate6(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/rate", options);
    },

    /** Mettre à jour le taux de commission d'un partenaire */
    patchPlatformGrowthPartnersByPartnerRate7(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/rate", options);
    },

    /** Mettre à jour le taux de commission d'un partenaire */
    patchPlatformGrowthPartnersByPartnerRate8(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/rate", options);
    },

    /** Mettre à jour le taux de commission d'un partenaire */
    patchPlatformGrowthPartnersByPartnerRate9(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/rate", options);
    },

    /** Mettre à jour le taux de commission d'un partenaire */
    patchPlatformGrowthPartnersByPartnerRate10(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/rate", options);
    },

    /** Mettre à jour le taux de commission d'un partenaire */
    patchPlatformGrowthPartnersByPartnerRate11(options = {}) {
      return request("PATCH", "/platform/growth/partners/{partner}/rate", options);
    },

    /** Lister les demandes de versement */
    getPlatformGrowthPayouts(options = {}) {
      return request("GET", "/platform/growth/payouts", options);
    },

    /** Lister les demandes de versement */
    getPlatformGrowthPayouts2(options = {}) {
      return request("GET", "/platform/growth/payouts", options);
    },

    /** Lister les demandes de versement */
    getPlatformGrowthPayouts3(options = {}) {
      return request("GET", "/platform/growth/payouts", options);
    },

    /** Lister les demandes de versement */
    getPlatformGrowthPayouts4(options = {}) {
      return request("GET", "/platform/growth/payouts", options);
    },

    /** Lister les demandes de versement */
    getPlatformGrowthPayouts5(options = {}) {
      return request("GET", "/platform/growth/payouts", options);
    },

    /** Lister les demandes de versement */
    getPlatformGrowthPayouts6(options = {}) {
      return request("GET", "/platform/growth/payouts", options);
    },

    /** Lister les demandes de versement */
    getPlatformGrowthPayouts7(options = {}) {
      return request("GET", "/platform/growth/payouts", options);
    },

    /** Lister les demandes de versement */
    getPlatformGrowthPayouts8(options = {}) {
      return request("GET", "/platform/growth/payouts", options);
    },

    /** Lister les demandes de versement */
    getPlatformGrowthPayouts9(options = {}) {
      return request("GET", "/platform/growth/payouts", options);
    },

    /** Lister les demandes de versement */
    getPlatformGrowthPayouts10(options = {}) {
      return request("GET", "/platform/growth/payouts", options);
    },

    /** Lister les demandes de versement */
    getPlatformGrowthPayouts11(options = {}) {
      return request("GET", "/platform/growth/payouts", options);
    },

    /** Mettre à jour le statut d'un payout partenaire (super-admin) */
    patchPlatformGrowthPayoutsByPayout(options = {}) {
      return request("PATCH", "/platform/growth/payouts/{payout}", options);
    },

    /** Mettre à jour le statut d'un payout partenaire (super-admin) */
    patchPlatformGrowthPayoutsByPayout2(options = {}) {
      return request("PATCH", "/platform/growth/payouts/{payout}", options);
    },

    /** Mettre à jour le statut d'un payout partenaire (super-admin) */
    patchPlatformGrowthPayoutsByPayout3(options = {}) {
      return request("PATCH", "/platform/growth/payouts/{payout}", options);
    },

    /** Mettre à jour le statut d'un payout partenaire (super-admin) */
    patchPlatformGrowthPayoutsByPayout4(options = {}) {
      return request("PATCH", "/platform/growth/payouts/{payout}", options);
    },

    /** Mettre à jour le statut d'un payout partenaire (super-admin) */
    patchPlatformGrowthPayoutsByPayout5(options = {}) {
      return request("PATCH", "/platform/growth/payouts/{payout}", options);
    },

    /** Mettre à jour le statut d'un payout partenaire (super-admin) */
    patchPlatformGrowthPayoutsByPayout6(options = {}) {
      return request("PATCH", "/platform/growth/payouts/{payout}", options);
    },

    /** Mettre à jour le statut d'un payout partenaire (super-admin) */
    patchPlatformGrowthPayoutsByPayout7(options = {}) {
      return request("PATCH", "/platform/growth/payouts/{payout}", options);
    },

    /** Mettre à jour le statut d'un payout partenaire (super-admin) */
    patchPlatformGrowthPayoutsByPayout8(options = {}) {
      return request("PATCH", "/platform/growth/payouts/{payout}", options);
    },

    /** Mettre à jour le statut d'un payout partenaire (super-admin) */
    patchPlatformGrowthPayoutsByPayout9(options = {}) {
      return request("PATCH", "/platform/growth/payouts/{payout}", options);
    },

    /** Mettre à jour le statut d'un payout partenaire (super-admin) */
    patchPlatformGrowthPayoutsByPayout10(options = {}) {
      return request("PATCH", "/platform/growth/payouts/{payout}", options);
    },

    /** Mettre à jour le statut d'un payout partenaire (super-admin) */
    patchPlatformGrowthPayoutsByPayout11(options = {}) {
      return request("PATCH", "/platform/growth/payouts/{payout}", options);
    },

    /** Lister les sessions d'impersonation (super-admin) */
    getPlatformImpersonations(options = {}) {
      return request("GET", "/platform/impersonations", options);
    },

    /** Lister les sessions d'impersonation (super-admin) */
    getPlatformImpersonations2(options = {}) {
      return request("GET", "/platform/impersonations", options);
    },

    /** Lister les sessions d'impersonation (super-admin) */
    getPlatformImpersonations3(options = {}) {
      return request("GET", "/platform/impersonations", options);
    },

    /** Lister les sessions d'impersonation (super-admin) */
    getPlatformImpersonations4(options = {}) {
      return request("GET", "/platform/impersonations", options);
    },

    /** Lister les sessions d'impersonation (super-admin) */
    getPlatformImpersonations5(options = {}) {
      return request("GET", "/platform/impersonations", options);
    },

    /** Lister les sessions d'impersonation (super-admin) */
    getPlatformImpersonations6(options = {}) {
      return request("GET", "/platform/impersonations", options);
    },

    /** Lister les sessions d'impersonation (super-admin) */
    getPlatformImpersonations7(options = {}) {
      return request("GET", "/platform/impersonations", options);
    },

    /** Lister les sessions d'impersonation (super-admin) */
    getPlatformImpersonations8(options = {}) {
      return request("GET", "/platform/impersonations", options);
    },

    /** Lister les sessions d'impersonation (super-admin) */
    getPlatformImpersonations9(options = {}) {
      return request("GET", "/platform/impersonations", options);
    },

    /** Lister les sessions d'impersonation (super-admin) */
    getPlatformImpersonations10(options = {}) {
      return request("GET", "/platform/impersonations", options);
    },

    /** Lister les sessions d'impersonation (super-admin) */
    getPlatformImpersonations11(options = {}) {
      return request("GET", "/platform/impersonations", options);
    },

    /** Creer une session d'impersonation (login en tant que) */
    postPlatformImpersonations(options = {}) {
      return request("POST", "/platform/impersonations", options);
    },

    /** Creer une session d'impersonation (login en tant que) */
    postPlatformImpersonations2(options = {}) {
      return request("POST", "/platform/impersonations", options);
    },

    /** Creer une session d'impersonation (login en tant que) */
    postPlatformImpersonations3(options = {}) {
      return request("POST", "/platform/impersonations", options);
    },

    /** Creer une session d'impersonation (login en tant que) */
    postPlatformImpersonations4(options = {}) {
      return request("POST", "/platform/impersonations", options);
    },

    /** Creer une session d'impersonation (login en tant que) */
    postPlatformImpersonations5(options = {}) {
      return request("POST", "/platform/impersonations", options);
    },

    /** Creer une session d'impersonation (login en tant que) */
    postPlatformImpersonations6(options = {}) {
      return request("POST", "/platform/impersonations", options);
    },

    /** Creer une session d'impersonation (login en tant que) */
    postPlatformImpersonations7(options = {}) {
      return request("POST", "/platform/impersonations", options);
    },

    /** Creer une session d'impersonation (login en tant que) */
    postPlatformImpersonations8(options = {}) {
      return request("POST", "/platform/impersonations", options);
    },

    /** Creer une session d'impersonation (login en tant que) */
    postPlatformImpersonations9(options = {}) {
      return request("POST", "/platform/impersonations", options);
    },

    /** Creer une session d'impersonation (login en tant que) */
    postPlatformImpersonations10(options = {}) {
      return request("POST", "/platform/impersonations", options);
    },

    /** Creer une session d'impersonation (login en tant que) */
    postPlatformImpersonations11(options = {}) {
      return request("POST", "/platform/impersonations", options);
    },

    /** Terminer une session d'impersonation */
    deletePlatformImpersonationsBySession(options = {}) {
      return request("DELETE", "/platform/impersonations/{session}", options);
    },

    /** Terminer une session d'impersonation */
    deletePlatformImpersonationsBySession2(options = {}) {
      return request("DELETE", "/platform/impersonations/{session}", options);
    },

    /** Terminer une session d'impersonation */
    deletePlatformImpersonationsBySession3(options = {}) {
      return request("DELETE", "/platform/impersonations/{session}", options);
    },

    /** Terminer une session d'impersonation */
    deletePlatformImpersonationsBySession4(options = {}) {
      return request("DELETE", "/platform/impersonations/{session}", options);
    },

    /** Terminer une session d'impersonation */
    deletePlatformImpersonationsBySession5(options = {}) {
      return request("DELETE", "/platform/impersonations/{session}", options);
    },

    /** Terminer une session d'impersonation */
    deletePlatformImpersonationsBySession6(options = {}) {
      return request("DELETE", "/platform/impersonations/{session}", options);
    },

    /** Terminer une session d'impersonation */
    deletePlatformImpersonationsBySession7(options = {}) {
      return request("DELETE", "/platform/impersonations/{session}", options);
    },

    /** Terminer une session d'impersonation */
    deletePlatformImpersonationsBySession8(options = {}) {
      return request("DELETE", "/platform/impersonations/{session}", options);
    },

    /** Terminer une session d'impersonation */
    deletePlatformImpersonationsBySession9(options = {}) {
      return request("DELETE", "/platform/impersonations/{session}", options);
    },

    /** Terminer une session d'impersonation */
    deletePlatformImpersonationsBySession10(options = {}) {
      return request("DELETE", "/platform/impersonations/{session}", options);
    },

    /** Terminer une session d'impersonation */
    deletePlatformImpersonationsBySession11(options = {}) {
      return request("DELETE", "/platform/impersonations/{session}", options);
    },

    /** Agregats business du cockpit super-admin */
    getPlatformMetricsOverview(options = {}) {
      return request("GET", "/platform/metrics/overview", options);
    },

    /** Agregats business du cockpit super-admin */
    getPlatformMetricsOverview2(options = {}) {
      return request("GET", "/platform/metrics/overview", options);
    },

    /** Agregats business du cockpit super-admin */
    getPlatformMetricsOverview3(options = {}) {
      return request("GET", "/platform/metrics/overview", options);
    },

    /** Agregats business du cockpit super-admin */
    getPlatformMetricsOverview4(options = {}) {
      return request("GET", "/platform/metrics/overview", options);
    },

    /** Agregats business du cockpit super-admin */
    getPlatformMetricsOverview5(options = {}) {
      return request("GET", "/platform/metrics/overview", options);
    },

    /** Taux d'echec de notifications cross-tenant sur 24h (super-admin) */
    getPlatformObservabilityNotifications(options = {}) {
      return request("GET", "/platform/observability/notifications", options);
    },

    /** Taux d'echec de notifications cross-tenant sur 24h (super-admin) */
    getPlatformObservabilityNotifications2(options = {}) {
      return request("GET", "/platform/observability/notifications", options);
    },

    /** Taux d'echec de notifications cross-tenant sur 24h (super-admin) */
    getPlatformObservabilityNotifications3(options = {}) {
      return request("GET", "/platform/observability/notifications", options);
    },

    /** Taux d'echec de notifications cross-tenant sur 24h (super-admin) */
    getPlatformObservabilityNotifications4(options = {}) {
      return request("GET", "/platform/observability/notifications", options);
    },

    /** Taux d'echec de notifications cross-tenant sur 24h (super-admin) */
    getPlatformObservabilityNotifications5(options = {}) {
      return request("GET", "/platform/observability/notifications", options);
    },

    /** Taux d'echec de notifications cross-tenant sur 24h (super-admin) */
    getPlatformObservabilityNotifications6(options = {}) {
      return request("GET", "/platform/observability/notifications", options);
    },

    /** Taux d'echec de notifications cross-tenant sur 24h (super-admin) */
    getPlatformObservabilityNotifications7(options = {}) {
      return request("GET", "/platform/observability/notifications", options);
    },

    /** Taux d'echec de notifications cross-tenant sur 24h (super-admin) */
    getPlatformObservabilityNotifications8(options = {}) {
      return request("GET", "/platform/observability/notifications", options);
    },

    /** Taux d'echec de notifications cross-tenant sur 24h (super-admin) */
    getPlatformObservabilityNotifications9(options = {}) {
      return request("GET", "/platform/observability/notifications", options);
    },

    /** Taux d'echec de notifications cross-tenant sur 24h (super-admin) */
    getPlatformObservabilityNotifications10(options = {}) {
      return request("GET", "/platform/observability/notifications", options);
    },

    /** Taux d'echec de notifications cross-tenant sur 24h (super-admin) */
    getPlatformObservabilityNotifications11(options = {}) {
      return request("GET", "/platform/observability/notifications", options);
    },

    /** Profondeur des queues Redis et jobs echoues (super-admin) */
    getPlatformObservabilityQueues(options = {}) {
      return request("GET", "/platform/observability/queues", options);
    },

    /** Profondeur des queues Redis et jobs echoues (super-admin) */
    getPlatformObservabilityQueues2(options = {}) {
      return request("GET", "/platform/observability/queues", options);
    },

    /** Profondeur des queues Redis et jobs echoues (super-admin) */
    getPlatformObservabilityQueues3(options = {}) {
      return request("GET", "/platform/observability/queues", options);
    },

    /** Profondeur des queues Redis et jobs echoues (super-admin) */
    getPlatformObservabilityQueues4(options = {}) {
      return request("GET", "/platform/observability/queues", options);
    },

    /** Profondeur des queues Redis et jobs echoues (super-admin) */
    getPlatformObservabilityQueues5(options = {}) {
      return request("GET", "/platform/observability/queues", options);
    },

    /** Profondeur des queues Redis et jobs echoues (super-admin) */
    getPlatformObservabilityQueues6(options = {}) {
      return request("GET", "/platform/observability/queues", options);
    },

    /** Profondeur des queues Redis et jobs echoues (super-admin) */
    getPlatformObservabilityQueues7(options = {}) {
      return request("GET", "/platform/observability/queues", options);
    },

    /** Profondeur des queues Redis et jobs echoues (super-admin) */
    getPlatformObservabilityQueues8(options = {}) {
      return request("GET", "/platform/observability/queues", options);
    },

    /** Profondeur des queues Redis et jobs echoues (super-admin) */
    getPlatformObservabilityQueues9(options = {}) {
      return request("GET", "/platform/observability/queues", options);
    },

    /** Profondeur des queues Redis et jobs echoues (super-admin) */
    getPlatformObservabilityQueues10(options = {}) {
      return request("GET", "/platform/observability/queues", options);
    },

    /** Profondeur des queues Redis et jobs echoues (super-admin) */
    getPlatformObservabilityQueues11(options = {}) {
      return request("GET", "/platform/observability/queues", options);
    },

    /** Lister le catalogue des plans SaaS */
    getPlatformPlans(options = {}) {
      return request("GET", "/platform/plans", options);
    },

    /** Lister le catalogue des plans SaaS */
    getPlatformPlans2(options = {}) {
      return request("GET", "/platform/plans", options);
    },

    /** Lister le catalogue des plans SaaS */
    getPlatformPlans3(options = {}) {
      return request("GET", "/platform/plans", options);
    },

    /** Lister le catalogue des plans SaaS */
    getPlatformPlans4(options = {}) {
      return request("GET", "/platform/plans", options);
    },

    /** Lister le catalogue des plans SaaS */
    getPlatformPlans5(options = {}) {
      return request("GET", "/platform/plans", options);
    },

    /** Lister tous les tickets de support (super-admin, cross-tenant) */
    getPlatformSupportTickets(options = {}) {
      return request("GET", "/platform/support-tickets", options);
    },

    /** Lister tous les tickets de support (super-admin, cross-tenant) */
    getPlatformSupportTickets2(options = {}) {
      return request("GET", "/platform/support-tickets", options);
    },

    /** Lister tous les tickets de support (super-admin, cross-tenant) */
    getPlatformSupportTickets3(options = {}) {
      return request("GET", "/platform/support-tickets", options);
    },

    /** Lister tous les tickets de support (super-admin, cross-tenant) */
    getPlatformSupportTickets4(options = {}) {
      return request("GET", "/platform/support-tickets", options);
    },

    /** Lister tous les tickets de support (super-admin, cross-tenant) */
    getPlatformSupportTickets5(options = {}) {
      return request("GET", "/platform/support-tickets", options);
    },

    /** Lister tous les tickets de support (super-admin, cross-tenant) */
    getPlatformSupportTickets6(options = {}) {
      return request("GET", "/platform/support-tickets", options);
    },

    /** Lister tous les tickets de support (super-admin, cross-tenant) */
    getPlatformSupportTickets7(options = {}) {
      return request("GET", "/platform/support-tickets", options);
    },

    /** Lister tous les tickets de support (super-admin, cross-tenant) */
    getPlatformSupportTickets8(options = {}) {
      return request("GET", "/platform/support-tickets", options);
    },

    /** Lister tous les tickets de support (super-admin, cross-tenant) */
    getPlatformSupportTickets9(options = {}) {
      return request("GET", "/platform/support-tickets", options);
    },

    /** Lister tous les tickets de support (super-admin, cross-tenant) */
    getPlatformSupportTickets10(options = {}) {
      return request("GET", "/platform/support-tickets", options);
    },

    /** Lister tous les tickets de support (super-admin, cross-tenant) */
    getPlatformSupportTickets11(options = {}) {
      return request("GET", "/platform/support-tickets", options);
    },

    /** Detail d'un ticket de support (super-admin) */
    getPlatformSupportTicketsBySupportTicket(options = {}) {
      return request("GET", "/platform/support-tickets/{supportTicket}", options);
    },

    /** Detail d'un ticket de support (super-admin) */
    getPlatformSupportTicketsBySupportTicket2(options = {}) {
      return request("GET", "/platform/support-tickets/{supportTicket}", options);
    },

    /** Detail d'un ticket de support (super-admin) */
    getPlatformSupportTicketsBySupportTicket3(options = {}) {
      return request("GET", "/platform/support-tickets/{supportTicket}", options);
    },

    /** Detail d'un ticket de support (super-admin) */
    getPlatformSupportTicketsBySupportTicket4(options = {}) {
      return request("GET", "/platform/support-tickets/{supportTicket}", options);
    },

    /** Detail d'un ticket de support (super-admin) */
    getPlatformSupportTicketsBySupportTicket5(options = {}) {
      return request("GET", "/platform/support-tickets/{supportTicket}", options);
    },

    /** Detail d'un ticket de support (super-admin) */
    getPlatformSupportTicketsBySupportTicket6(options = {}) {
      return request("GET", "/platform/support-tickets/{supportTicket}", options);
    },

    /** Detail d'un ticket de support (super-admin) */
    getPlatformSupportTicketsBySupportTicket7(options = {}) {
      return request("GET", "/platform/support-tickets/{supportTicket}", options);
    },

    /** Detail d'un ticket de support (super-admin) */
    getPlatformSupportTicketsBySupportTicket8(options = {}) {
      return request("GET", "/platform/support-tickets/{supportTicket}", options);
    },

    /** Detail d'un ticket de support (super-admin) */
    getPlatformSupportTicketsBySupportTicket9(options = {}) {
      return request("GET", "/platform/support-tickets/{supportTicket}", options);
    },

    /** Detail d'un ticket de support (super-admin) */
    getPlatformSupportTicketsBySupportTicket10(options = {}) {
      return request("GET", "/platform/support-tickets/{supportTicket}", options);
    },

    /** Detail d'un ticket de support (super-admin) */
    getPlatformSupportTicketsBySupportTicket11(options = {}) {
      return request("GET", "/platform/support-tickets/{supportTicket}", options);
    },

    /** Repondre a un ticket (super-admin) */
    postPlatformSupportTicketsBySupportTicketReply(options = {}) {
      return request("POST", "/platform/support-tickets/{supportTicket}/reply", options);
    },

    /** Repondre a un ticket (super-admin) */
    postPlatformSupportTicketsBySupportTicketReply2(options = {}) {
      return request("POST", "/platform/support-tickets/{supportTicket}/reply", options);
    },

    /** Repondre a un ticket (super-admin) */
    postPlatformSupportTicketsBySupportTicketReply3(options = {}) {
      return request("POST", "/platform/support-tickets/{supportTicket}/reply", options);
    },

    /** Repondre a un ticket (super-admin) */
    postPlatformSupportTicketsBySupportTicketReply4(options = {}) {
      return request("POST", "/platform/support-tickets/{supportTicket}/reply", options);
    },

    /** Repondre a un ticket (super-admin) */
    postPlatformSupportTicketsBySupportTicketReply5(options = {}) {
      return request("POST", "/platform/support-tickets/{supportTicket}/reply", options);
    },

    /** Repondre a un ticket (super-admin) */
    postPlatformSupportTicketsBySupportTicketReply6(options = {}) {
      return request("POST", "/platform/support-tickets/{supportTicket}/reply", options);
    },

    /** Repondre a un ticket (super-admin) */
    postPlatformSupportTicketsBySupportTicketReply7(options = {}) {
      return request("POST", "/platform/support-tickets/{supportTicket}/reply", options);
    },

    /** Repondre a un ticket (super-admin) */
    postPlatformSupportTicketsBySupportTicketReply8(options = {}) {
      return request("POST", "/platform/support-tickets/{supportTicket}/reply", options);
    },

    /** Repondre a un ticket (super-admin) */
    postPlatformSupportTicketsBySupportTicketReply9(options = {}) {
      return request("POST", "/platform/support-tickets/{supportTicket}/reply", options);
    },

    /** Repondre a un ticket (super-admin) */
    postPlatformSupportTicketsBySupportTicketReply10(options = {}) {
      return request("POST", "/platform/support-tickets/{supportTicket}/reply", options);
    },

    /** Repondre a un ticket (super-admin) */
    postPlatformSupportTicketsBySupportTicketReply11(options = {}) {
      return request("POST", "/platform/support-tickets/{supportTicket}/reply", options);
    },

    /** Trier un ticket (statut, priorite, assignation) */
    patchPlatformSupportTicketsBySupportTicketTriage(options = {}) {
      return request("PATCH", "/platform/support-tickets/{supportTicket}/triage", options);
    },

    /** Trier un ticket (statut, priorite, assignation) */
    patchPlatformSupportTicketsBySupportTicketTriage2(options = {}) {
      return request("PATCH", "/platform/support-tickets/{supportTicket}/triage", options);
    },

    /** Trier un ticket (statut, priorite, assignation) */
    patchPlatformSupportTicketsBySupportTicketTriage3(options = {}) {
      return request("PATCH", "/platform/support-tickets/{supportTicket}/triage", options);
    },

    /** Trier un ticket (statut, priorite, assignation) */
    patchPlatformSupportTicketsBySupportTicketTriage4(options = {}) {
      return request("PATCH", "/platform/support-tickets/{supportTicket}/triage", options);
    },

    /** Trier un ticket (statut, priorite, assignation) */
    patchPlatformSupportTicketsBySupportTicketTriage5(options = {}) {
      return request("PATCH", "/platform/support-tickets/{supportTicket}/triage", options);
    },

    /** Trier un ticket (statut, priorite, assignation) */
    patchPlatformSupportTicketsBySupportTicketTriage6(options = {}) {
      return request("PATCH", "/platform/support-tickets/{supportTicket}/triage", options);
    },

    /** Trier un ticket (statut, priorite, assignation) */
    patchPlatformSupportTicketsBySupportTicketTriage7(options = {}) {
      return request("PATCH", "/platform/support-tickets/{supportTicket}/triage", options);
    },

    /** Trier un ticket (statut, priorite, assignation) */
    patchPlatformSupportTicketsBySupportTicketTriage8(options = {}) {
      return request("PATCH", "/platform/support-tickets/{supportTicket}/triage", options);
    },

    /** Trier un ticket (statut, priorite, assignation) */
    patchPlatformSupportTicketsBySupportTicketTriage9(options = {}) {
      return request("PATCH", "/platform/support-tickets/{supportTicket}/triage", options);
    },

    /** Trier un ticket (statut, priorite, assignation) */
    patchPlatformSupportTicketsBySupportTicketTriage10(options = {}) {
      return request("PATCH", "/platform/support-tickets/{supportTicket}/triage", options);
    },

    /** Trier un ticket (statut, priorite, assignation) */
    patchPlatformSupportTicketsBySupportTicketTriage11(options = {}) {
      return request("PATCH", "/platform/support-tickets/{supportTicket}/triage", options);
    },

    /** Lister les utilisateurs plateforme (super-admins) */
    getPlatformUsers(options = {}) {
      return request("GET", "/platform/users", options);
    },

    /** Lister les utilisateurs plateforme (super-admins) */
    getPlatformUsers2(options = {}) {
      return request("GET", "/platform/users", options);
    },

    /** Lister les utilisateurs plateforme (super-admins) */
    getPlatformUsers3(options = {}) {
      return request("GET", "/platform/users", options);
    },

    /** Lister les utilisateurs plateforme (super-admins) */
    getPlatformUsers4(options = {}) {
      return request("GET", "/platform/users", options);
    },

    /** Lister les utilisateurs plateforme (super-admins) */
    getPlatformUsers5(options = {}) {
      return request("GET", "/platform/users", options);
    },

    /** Creer un utilisateur plateforme */
    postPlatformUsers(options = {}) {
      return request("POST", "/platform/users", options);
    },

    /** Creer un utilisateur plateforme */
    postPlatformUsers2(options = {}) {
      return request("POST", "/platform/users", options);
    },

    /** Creer un utilisateur plateforme */
    postPlatformUsers3(options = {}) {
      return request("POST", "/platform/users", options);
    },

    /** Creer un utilisateur plateforme */
    postPlatformUsers4(options = {}) {
      return request("POST", "/platform/users", options);
    },

    /** Creer un utilisateur plateforme */
    postPlatformUsers5(options = {}) {
      return request("POST", "/platform/users", options);
    },

    /** Desactiver un utilisateur plateforme (jamais de suppression physique) */
    deletePlatformUsersByUser(options = {}) {
      return request("DELETE", "/platform/users/{user}", options);
    },

    /** Desactiver un utilisateur plateforme (jamais de suppression physique) */
    deletePlatformUsersByUser2(options = {}) {
      return request("DELETE", "/platform/users/{user}", options);
    },

    /** Desactiver un utilisateur plateforme (jamais de suppression physique) */
    deletePlatformUsersByUser3(options = {}) {
      return request("DELETE", "/platform/users/{user}", options);
    },

    /** Desactiver un utilisateur plateforme (jamais de suppression physique) */
    deletePlatformUsersByUser4(options = {}) {
      return request("DELETE", "/platform/users/{user}", options);
    },

    /** Desactiver un utilisateur plateforme (jamais de suppression physique) */
    deletePlatformUsersByUser5(options = {}) {
      return request("DELETE", "/platform/users/{user}", options);
    },

    /** Detail d'un utilisateur plateforme */
    getPlatformUsersByUser(options = {}) {
      return request("GET", "/platform/users/{user}", options);
    },

    /** Detail d'un utilisateur plateforme */
    getPlatformUsersByUser2(options = {}) {
      return request("GET", "/platform/users/{user}", options);
    },

    /** Detail d'un utilisateur plateforme */
    getPlatformUsersByUser3(options = {}) {
      return request("GET", "/platform/users/{user}", options);
    },

    /** Detail d'un utilisateur plateforme */
    getPlatformUsersByUser4(options = {}) {
      return request("GET", "/platform/users/{user}", options);
    },

    /** Detail d'un utilisateur plateforme */
    getPlatformUsersByUser5(options = {}) {
      return request("GET", "/platform/users/{user}", options);
    },

    /** Mettre a jour un utilisateur plateforme */
    patchPlatformUsersByUser(options = {}) {
      return request("PATCH", "/platform/users/{user}", options);
    },

    /** Mettre a jour un utilisateur plateforme */
    patchPlatformUsersByUser2(options = {}) {
      return request("PATCH", "/platform/users/{user}", options);
    },

    /** Mettre a jour un utilisateur plateforme */
    patchPlatformUsersByUser3(options = {}) {
      return request("PATCH", "/platform/users/{user}", options);
    },

    /** Mettre a jour un utilisateur plateforme */
    patchPlatformUsersByUser4(options = {}) {
      return request("PATCH", "/platform/users/{user}", options);
    },

    /** Mettre a jour un utilisateur plateforme */
    patchPlatformUsersByUser5(options = {}) {
      return request("PATCH", "/platform/users/{user}", options);
    },

    /** Activer un utilisateur plateforme */
    postPlatformUsersByUserActivate(options = {}) {
      return request("POST", "/platform/users/{user}/activate", options);
    },

    /** Activer un utilisateur plateforme */
    postPlatformUsersByUserActivate2(options = {}) {
      return request("POST", "/platform/users/{user}/activate", options);
    },

    /** Activer un utilisateur plateforme */
    postPlatformUsersByUserActivate3(options = {}) {
      return request("POST", "/platform/users/{user}/activate", options);
    },

    /** Activer un utilisateur plateforme */
    postPlatformUsersByUserActivate4(options = {}) {
      return request("POST", "/platform/users/{user}/activate", options);
    },

    /** Activer un utilisateur plateforme */
    postPlatformUsersByUserActivate5(options = {}) {
      return request("POST", "/platform/users/{user}/activate", options);
    },

    /** Desactiver un utilisateur plateforme */
    postPlatformUsersByUserDeactivate(options = {}) {
      return request("POST", "/platform/users/{user}/deactivate", options);
    },

    /** Desactiver un utilisateur plateforme */
    postPlatformUsersByUserDeactivate2(options = {}) {
      return request("POST", "/platform/users/{user}/deactivate", options);
    },

    /** Desactiver un utilisateur plateforme */
    postPlatformUsersByUserDeactivate3(options = {}) {
      return request("POST", "/platform/users/{user}/deactivate", options);
    },

    /** Desactiver un utilisateur plateforme */
    postPlatformUsersByUserDeactivate4(options = {}) {
      return request("POST", "/platform/users/{user}/deactivate", options);
    },

    /** Desactiver un utilisateur plateforme */
    postPlatformUsersByUserDeactivate5(options = {}) {
      return request("POST", "/platform/users/{user}/deactivate", options);
    },

    /** Suspendre un utilisateur plateforme */
    postPlatformUsersByUserSuspend(options = {}) {
      return request("POST", "/platform/users/{user}/suspend", options);
    },

    /** Suspendre un utilisateur plateforme */
    postPlatformUsersByUserSuspend2(options = {}) {
      return request("POST", "/platform/users/{user}/suspend", options);
    },

    /** Suspendre un utilisateur plateforme */
    postPlatformUsersByUserSuspend3(options = {}) {
      return request("POST", "/platform/users/{user}/suspend", options);
    },

    /** Suspendre un utilisateur plateforme */
    postPlatformUsersByUserSuspend4(options = {}) {
      return request("POST", "/platform/users/{user}/suspend", options);
    },

    /** Suspendre un utilisateur plateforme */
    postPlatformUsersByUserSuspend5(options = {}) {
      return request("POST", "/platform/users/{user}/suspend", options);
    },

    /** Lister les postes */
    getPositions(options = {}) {
      return request("GET", "/positions", options);
    },

    /** Lister les postes */
    getPositions2(options = {}) {
      return request("GET", "/positions", options);
    },

    /** Lister les postes */
    getPositions3(options = {}) {
      return request("GET", "/positions", options);
    },

    /** Lister les postes */
    getPositions4(options = {}) {
      return request("GET", "/positions", options);
    },

    /** Lister les postes */
    getPositions5(options = {}) {
      return request("GET", "/positions", options);
    },

    /** Lister les postes */
    getPositions6(options = {}) {
      return request("GET", "/positions", options);
    },

    /** Lister les postes */
    getPositions7(options = {}) {
      return request("GET", "/positions", options);
    },

    /** Lister les postes */
    getPositions8(options = {}) {
      return request("GET", "/positions", options);
    },

    /** Creer un poste */
    postPositions(options = {}) {
      return request("POST", "/positions", options);
    },

    /** Creer un poste */
    postPositions2(options = {}) {
      return request("POST", "/positions", options);
    },

    /** Creer un poste */
    postPositions3(options = {}) {
      return request("POST", "/positions", options);
    },

    /** Creer un poste */
    postPositions4(options = {}) {
      return request("POST", "/positions", options);
    },

    /** Creer un poste */
    postPositions5(options = {}) {
      return request("POST", "/positions", options);
    },

    /** Creer un poste */
    postPositions6(options = {}) {
      return request("POST", "/positions", options);
    },

    /** Creer un poste */
    postPositions7(options = {}) {
      return request("POST", "/positions", options);
    },

    /** Creer un poste */
    postPositions8(options = {}) {
      return request("POST", "/positions", options);
    },

    /** Supprimer un poste */
    deletePositionsByPosition(options = {}) {
      return request("DELETE", "/positions/{position}", options);
    },

    /** Supprimer un poste */
    deletePositionsByPosition2(options = {}) {
      return request("DELETE", "/positions/{position}", options);
    },

    /** Supprimer un poste */
    deletePositionsByPosition3(options = {}) {
      return request("DELETE", "/positions/{position}", options);
    },

    /** Supprimer un poste */
    deletePositionsByPosition4(options = {}) {
      return request("DELETE", "/positions/{position}", options);
    },

    /** Supprimer un poste */
    deletePositionsByPosition5(options = {}) {
      return request("DELETE", "/positions/{position}", options);
    },

    /** Supprimer un poste */
    deletePositionsByPosition6(options = {}) {
      return request("DELETE", "/positions/{position}", options);
    },

    /** Supprimer un poste */
    deletePositionsByPosition7(options = {}) {
      return request("DELETE", "/positions/{position}", options);
    },

    /** Supprimer un poste */
    deletePositionsByPosition8(options = {}) {
      return request("DELETE", "/positions/{position}", options);
    },

    /** Voir un poste */
    getPositionsByPosition(options = {}) {
      return request("GET", "/positions/{position}", options);
    },

    /** Voir un poste */
    getPositionsByPosition2(options = {}) {
      return request("GET", "/positions/{position}", options);
    },

    /** Voir un poste */
    getPositionsByPosition3(options = {}) {
      return request("GET", "/positions/{position}", options);
    },

    /** Voir un poste */
    getPositionsByPosition4(options = {}) {
      return request("GET", "/positions/{position}", options);
    },

    /** Voir un poste */
    getPositionsByPosition5(options = {}) {
      return request("GET", "/positions/{position}", options);
    },

    /** Voir un poste */
    getPositionsByPosition6(options = {}) {
      return request("GET", "/positions/{position}", options);
    },

    /** Voir un poste */
    getPositionsByPosition7(options = {}) {
      return request("GET", "/positions/{position}", options);
    },

    /** Voir un poste */
    getPositionsByPosition8(options = {}) {
      return request("GET", "/positions/{position}", options);
    },

    /** Mettre à jour un poste (manager) */
    patchPositionsByPosition(options = {}) {
      return request("PATCH", "/positions/{position}", options);
    },

    /** Mettre à jour un poste (manager) */
    patchPositionsByPosition2(options = {}) {
      return request("PATCH", "/positions/{position}", options);
    },

    /** Mettre à jour un poste (manager) */
    patchPositionsByPosition3(options = {}) {
      return request("PATCH", "/positions/{position}", options);
    },

    /** Mettre à jour un poste (manager) */
    patchPositionsByPosition4(options = {}) {
      return request("PATCH", "/positions/{position}", options);
    },

    /** Mettre à jour un poste (manager) */
    patchPositionsByPosition5(options = {}) {
      return request("PATCH", "/positions/{position}", options);
    },

    /** Mettre à jour un poste (manager) */
    patchPositionsByPosition6(options = {}) {
      return request("PATCH", "/positions/{position}", options);
    },

    /** Mettre à jour un poste (manager) */
    patchPositionsByPosition7(options = {}) {
      return request("PATCH", "/positions/{position}", options);
    },

    /** Mettre à jour un poste (manager) */
    patchPositionsByPosition8(options = {}) {
      return request("PATCH", "/positions/{position}", options);
    },

    /** Modifier un poste */
    putPositionsByPosition(options = {}) {
      return request("PUT", "/positions/{position}", options);
    },

    /** Modifier un poste */
    putPositionsByPosition2(options = {}) {
      return request("PUT", "/positions/{position}", options);
    },

    /** Modifier un poste */
    putPositionsByPosition3(options = {}) {
      return request("PUT", "/positions/{position}", options);
    },

    /** Modifier un poste */
    putPositionsByPosition4(options = {}) {
      return request("PUT", "/positions/{position}", options);
    },

    /** Modifier un poste */
    putPositionsByPosition5(options = {}) {
      return request("PUT", "/positions/{position}", options);
    },

    /** Modifier un poste */
    putPositionsByPosition6(options = {}) {
      return request("PUT", "/positions/{position}", options);
    },

    /** Modifier un poste */
    putPositionsByPosition7(options = {}) {
      return request("PUT", "/positions/{position}", options);
    },

    /** Modifier un poste */
    putPositionsByPosition8(options = {}) {
      return request("PUT", "/positions/{position}", options);
    },

    /** Predire les risques d'absenteisme */
    getPredictionsAbsenteeism(options = {}) {
      return request("GET", "/predictions/absenteeism", options);
    },

    /** Predire les risques d'absenteisme */
    getPredictionsAbsenteeism2(options = {}) {
      return request("GET", "/predictions/absenteeism", options);
    },

    /** Predire les risques d'absenteisme */
    getPredictionsAbsenteeism3(options = {}) {
      return request("GET", "/predictions/absenteeism", options);
    },

    /** Predire les risques d'absenteisme */
    getPredictionsAbsenteeism4(options = {}) {
      return request("GET", "/predictions/absenteeism", options);
    },

    /** Predire les risques d'absenteisme */
    getPredictionsAbsenteeism5(options = {}) {
      return request("GET", "/predictions/absenteeism", options);
    },

    /** Predire les risques d'absenteisme */
    getPredictionsAbsenteeism6(options = {}) {
      return request("GET", "/predictions/absenteeism", options);
    },

    /** Predire les risques d'absenteisme */
    getPredictionsAbsenteeism7(options = {}) {
      return request("GET", "/predictions/absenteeism", options);
    },

    /** Predire les risques d'absenteisme */
    getPredictionsAbsenteeism8(options = {}) {
      return request("GET", "/predictions/absenteeism", options);
    },

    /** Predire les risques d'absenteisme */
    getPredictionsAbsenteeism9(options = {}) {
      return request("GET", "/predictions/absenteeism", options);
    },

    /** Predire les risques d'absenteisme */
    getPredictionsAbsenteeism10(options = {}) {
      return request("GET", "/predictions/absenteeism", options);
    },

    /** Predire les risques d'absenteisme */
    getPredictionsAbsenteeism11(options = {}) {
      return request("GET", "/predictions/absenteeism", options);
    },

    /** Notifications RH proactives generees par l'IA */
    getPredictionsNotifications(options = {}) {
      return request("GET", "/predictions/notifications", options);
    },

    /** Notifications RH proactives generees par l'IA */
    getPredictionsNotifications2(options = {}) {
      return request("GET", "/predictions/notifications", options);
    },

    /** Notifications RH proactives generees par l'IA */
    getPredictionsNotifications3(options = {}) {
      return request("GET", "/predictions/notifications", options);
    },

    /** Notifications RH proactives generees par l'IA */
    getPredictionsNotifications4(options = {}) {
      return request("GET", "/predictions/notifications", options);
    },

    /** Notifications RH proactives generees par l'IA */
    getPredictionsNotifications5(options = {}) {
      return request("GET", "/predictions/notifications", options);
    },

    /** Notifications RH proactives generees par l'IA */
    getPredictionsNotifications6(options = {}) {
      return request("GET", "/predictions/notifications", options);
    },

    /** Notifications RH proactives generees par l'IA */
    getPredictionsNotifications7(options = {}) {
      return request("GET", "/predictions/notifications", options);
    },

    /** Notifications RH proactives generees par l'IA */
    getPredictionsNotifications8(options = {}) {
      return request("GET", "/predictions/notifications", options);
    },

    /** Notifications RH proactives generees par l'IA */
    getPredictionsNotifications9(options = {}) {
      return request("GET", "/predictions/notifications", options);
    },

    /** Notifications RH proactives generees par l'IA */
    getPredictionsNotifications10(options = {}) {
      return request("GET", "/predictions/notifications", options);
    },

    /** Notifications RH proactives generees par l'IA */
    getPredictionsNotifications11(options = {}) {
      return request("GET", "/predictions/notifications", options);
    },

    /** Predire le risque de turnover par employe */
    getPredictionsTurnover(options = {}) {
      return request("GET", "/predictions/turnover", options);
    },

    /** Predire le risque de turnover par employe */
    getPredictionsTurnover2(options = {}) {
      return request("GET", "/predictions/turnover", options);
    },

    /** Predire le risque de turnover par employe */
    getPredictionsTurnover3(options = {}) {
      return request("GET", "/predictions/turnover", options);
    },

    /** Predire le risque de turnover par employe */
    getPredictionsTurnover4(options = {}) {
      return request("GET", "/predictions/turnover", options);
    },

    /** Predire le risque de turnover par employe */
    getPredictionsTurnover5(options = {}) {
      return request("GET", "/predictions/turnover", options);
    },

    /** Predire le risque de turnover par employe */
    getPredictionsTurnover6(options = {}) {
      return request("GET", "/predictions/turnover", options);
    },

    /** Predire le risque de turnover par employe */
    getPredictionsTurnover7(options = {}) {
      return request("GET", "/predictions/turnover", options);
    },

    /** Predire le risque de turnover par employe */
    getPredictionsTurnover8(options = {}) {
      return request("GET", "/predictions/turnover", options);
    },

    /** Predire le risque de turnover par employe */
    getPredictionsTurnover9(options = {}) {
      return request("GET", "/predictions/turnover", options);
    },

    /** Predire le risque de turnover par employe */
    getPredictionsTurnover10(options = {}) {
      return request("GET", "/predictions/turnover", options);
    },

    /** Predire le risque de turnover par employe */
    getPredictionsTurnover11(options = {}) {
      return request("GET", "/predictions/turnover", options);
    },

    /** Enregistrer ou retirer le consentement biometrique */
    patchPrivacyBiometricConsent(options = {}) {
      return request("PATCH", "/privacy/biometric-consent", options);
    },

    /** Enregistrer ou retirer le consentement biometrique */
    patchPrivacyBiometricConsent2(options = {}) {
      return request("PATCH", "/privacy/biometric-consent", options);
    },

    /** Enregistrer ou retirer le consentement biometrique */
    patchPrivacyBiometricConsent3(options = {}) {
      return request("PATCH", "/privacy/biometric-consent", options);
    },

    /** Enregistrer ou retirer le consentement biometrique */
    patchPrivacyBiometricConsent4(options = {}) {
      return request("PATCH", "/privacy/biometric-consent", options);
    },

    /** Enregistrer ou retirer le consentement biometrique */
    patchPrivacyBiometricConsent5(options = {}) {
      return request("PATCH", "/privacy/biometric-consent", options);
    },

    /** Creer une demande de suppression non destructive */
    postPrivacyDeletionRequest(options = {}) {
      return request("POST", "/privacy/deletion-request", options);
    },

    /** Creer une demande de suppression non destructive */
    postPrivacyDeletionRequest2(options = {}) {
      return request("POST", "/privacy/deletion-request", options);
    },

    /** Creer une demande de suppression non destructive */
    postPrivacyDeletionRequest3(options = {}) {
      return request("POST", "/privacy/deletion-request", options);
    },

    /** Creer une demande de suppression non destructive */
    postPrivacyDeletionRequest4(options = {}) {
      return request("POST", "/privacy/deletion-request", options);
    },

    /** Creer une demande de suppression non destructive */
    postPrivacyDeletionRequest5(options = {}) {
      return request("POST", "/privacy/deletion-request", options);
    },

    /** Exporter les donnees personnelles de l'employe authentifie */
    getPrivacyExport(options = {}) {
      return request("GET", "/privacy/export", options);
    },

    /** Exporter les donnees personnelles de l'employe authentifie */
    getPrivacyExport2(options = {}) {
      return request("GET", "/privacy/export", options);
    },

    /** Exporter les donnees personnelles de l'employe authentifie */
    getPrivacyExport3(options = {}) {
      return request("GET", "/privacy/export", options);
    },

    /** Exporter les donnees personnelles de l'employe authentifie */
    getPrivacyExport4(options = {}) {
      return request("GET", "/privacy/export", options);
    },

    /** Exporter les donnees personnelles de l'employe authentifie */
    getPrivacyExport5(options = {}) {
      return request("GET", "/privacy/export", options);
    },

    /** Lister les projets */
    getProjects(options = {}) {
      return request("GET", "/projects", options);
    },

    /** Lister les projets */
    getProjects2(options = {}) {
      return request("GET", "/projects", options);
    },

    /** Lister les projets */
    getProjects3(options = {}) {
      return request("GET", "/projects", options);
    },

    /** Lister les projets */
    getProjects4(options = {}) {
      return request("GET", "/projects", options);
    },

    /** Lister les projets */
    getProjects5(options = {}) {
      return request("GET", "/projects", options);
    },

    /** Lister les projets */
    getProjects6(options = {}) {
      return request("GET", "/projects", options);
    },

    /** Lister les projets */
    getProjects7(options = {}) {
      return request("GET", "/projects", options);
    },

    /** Lister les projets */
    getProjects8(options = {}) {
      return request("GET", "/projects", options);
    },

    /** Creer un projet */
    postProjects(options = {}) {
      return request("POST", "/projects", options);
    },

    /** Creer un projet */
    postProjects2(options = {}) {
      return request("POST", "/projects", options);
    },

    /** Creer un projet */
    postProjects3(options = {}) {
      return request("POST", "/projects", options);
    },

    /** Creer un projet */
    postProjects4(options = {}) {
      return request("POST", "/projects", options);
    },

    /** Creer un projet */
    postProjects5(options = {}) {
      return request("POST", "/projects", options);
    },

    /** Creer un projet */
    postProjects6(options = {}) {
      return request("POST", "/projects", options);
    },

    /** Creer un projet */
    postProjects7(options = {}) {
      return request("POST", "/projects", options);
    },

    /** Creer un projet */
    postProjects8(options = {}) {
      return request("POST", "/projects", options);
    },

    /** Supprimer un projet */
    deleteProjectsByProject(options = {}) {
      return request("DELETE", "/projects/{project}", options);
    },

    /** Supprimer un projet */
    deleteProjectsByProject2(options = {}) {
      return request("DELETE", "/projects/{project}", options);
    },

    /** Supprimer un projet */
    deleteProjectsByProject3(options = {}) {
      return request("DELETE", "/projects/{project}", options);
    },

    /** Supprimer un projet */
    deleteProjectsByProject4(options = {}) {
      return request("DELETE", "/projects/{project}", options);
    },

    /** Supprimer un projet */
    deleteProjectsByProject5(options = {}) {
      return request("DELETE", "/projects/{project}", options);
    },

    /** Supprimer un projet */
    deleteProjectsByProject6(options = {}) {
      return request("DELETE", "/projects/{project}", options);
    },

    /** Supprimer un projet */
    deleteProjectsByProject7(options = {}) {
      return request("DELETE", "/projects/{project}", options);
    },

    /** Supprimer un projet */
    deleteProjectsByProject8(options = {}) {
      return request("DELETE", "/projects/{project}", options);
    },

    /** Voir un projet */
    getProjectsByProject(options = {}) {
      return request("GET", "/projects/{project}", options);
    },

    /** Voir un projet */
    getProjectsByProject2(options = {}) {
      return request("GET", "/projects/{project}", options);
    },

    /** Voir un projet */
    getProjectsByProject3(options = {}) {
      return request("GET", "/projects/{project}", options);
    },

    /** Voir un projet */
    getProjectsByProject4(options = {}) {
      return request("GET", "/projects/{project}", options);
    },

    /** Voir un projet */
    getProjectsByProject5(options = {}) {
      return request("GET", "/projects/{project}", options);
    },

    /** Voir un projet */
    getProjectsByProject6(options = {}) {
      return request("GET", "/projects/{project}", options);
    },

    /** Voir un projet */
    getProjectsByProject7(options = {}) {
      return request("GET", "/projects/{project}", options);
    },

    /** Voir un projet */
    getProjectsByProject8(options = {}) {
      return request("GET", "/projects/{project}", options);
    },

    /** Mettre a jour partiellement un projet */
    patchProjectsByProject(options = {}) {
      return request("PATCH", "/projects/{project}", options);
    },

    /** Mettre a jour partiellement un projet */
    patchProjectsByProject2(options = {}) {
      return request("PATCH", "/projects/{project}", options);
    },

    /** Mettre a jour partiellement un projet */
    patchProjectsByProject3(options = {}) {
      return request("PATCH", "/projects/{project}", options);
    },

    /** Mettre a jour partiellement un projet */
    patchProjectsByProject4(options = {}) {
      return request("PATCH", "/projects/{project}", options);
    },

    /** Mettre a jour partiellement un projet */
    patchProjectsByProject5(options = {}) {
      return request("PATCH", "/projects/{project}", options);
    },

    /** Mettre a jour partiellement un projet */
    patchProjectsByProject6(options = {}) {
      return request("PATCH", "/projects/{project}", options);
    },

    /** Mettre a jour partiellement un projet */
    patchProjectsByProject7(options = {}) {
      return request("PATCH", "/projects/{project}", options);
    },

    /** Mettre a jour partiellement un projet */
    patchProjectsByProject8(options = {}) {
      return request("PATCH", "/projects/{project}", options);
    },

    /** Mettre a jour un projet */
    putProjectsByProject(options = {}) {
      return request("PUT", "/projects/{project}", options);
    },

    /** Mettre a jour un projet */
    putProjectsByProject2(options = {}) {
      return request("PUT", "/projects/{project}", options);
    },

    /** Mettre a jour un projet */
    putProjectsByProject3(options = {}) {
      return request("PUT", "/projects/{project}", options);
    },

    /** Mettre a jour un projet */
    putProjectsByProject4(options = {}) {
      return request("PUT", "/projects/{project}", options);
    },

    /** Mettre a jour un projet */
    putProjectsByProject5(options = {}) {
      return request("PUT", "/projects/{project}", options);
    },

    /** Mettre a jour un projet */
    putProjectsByProject6(options = {}) {
      return request("PUT", "/projects/{project}", options);
    },

    /** Mettre a jour un projet */
    putProjectsByProject7(options = {}) {
      return request("PUT", "/projects/{project}", options);
    },

    /** Mettre a jour un projet */
    putProjectsByProject8(options = {}) {
      return request("PUT", "/projects/{project}", options);
    },

    /** Lister les jours fériés du pays (manager principal : nationaux + entreprise) */
    listPublicHolidays(options = {}) {
      return request("GET", "/public-holidays", options);
    },

    /** Lister les jours fériés du pays (manager principal : nationaux + entreprise) */
    listPublicHolidays2(options = {}) {
      return request("GET", "/public-holidays", options);
    },

    /** Lister les jours fériés du pays (manager principal : nationaux + entreprise) */
    listPublicHolidays3(options = {}) {
      return request("GET", "/public-holidays", options);
    },

    /** Lister les jours fériés du pays (manager principal : nationaux + entreprise) */
    listPublicHolidays4(options = {}) {
      return request("GET", "/public-holidays", options);
    },

    /** Lister les jours fériés du pays (manager principal : nationaux + entreprise) */
    listPublicHolidays5(options = {}) {
      return request("GET", "/public-holidays", options);
    },

    /** Lister les jours fériés du pays (manager principal : nationaux + entreprise) */
    listPublicHolidays6(options = {}) {
      return request("GET", "/public-holidays", options);
    },

    /** Lister les jours fériés du pays (manager principal : nationaux + entreprise) */
    listPublicHolidays7(options = {}) {
      return request("GET", "/public-holidays", options);
    },

    /** Lister les jours fériés du pays (manager principal : nationaux + entreprise) */
    listPublicHolidays8(options = {}) {
      return request("GET", "/public-holidays", options);
    },

    /** Lister les jours fériés du pays (manager principal : nationaux + entreprise) */
    listPublicHolidays9(options = {}) {
      return request("GET", "/public-holidays", options);
    },

    /** Lister les jours fériés du pays (manager principal : nationaux + entreprise) */
    listPublicHolidays10(options = {}) {
      return request("GET", "/public-holidays", options);
    },

    /** Créer un jour férié d'entreprise (manager principal) */
    storePublicHoliday(options = {}) {
      return request("POST", "/public-holidays", options);
    },

    /** Créer un jour férié d'entreprise (manager principal) */
    storePublicHoliday2(options = {}) {
      return request("POST", "/public-holidays", options);
    },

    /** Créer un jour férié d'entreprise (manager principal) */
    storePublicHoliday3(options = {}) {
      return request("POST", "/public-holidays", options);
    },

    /** Créer un jour férié d'entreprise (manager principal) */
    storePublicHoliday4(options = {}) {
      return request("POST", "/public-holidays", options);
    },

    /** Créer un jour férié d'entreprise (manager principal) */
    storePublicHoliday5(options = {}) {
      return request("POST", "/public-holidays", options);
    },

    /** Créer un jour férié d'entreprise (manager principal) */
    storePublicHoliday6(options = {}) {
      return request("POST", "/public-holidays", options);
    },

    /** Créer un jour férié d'entreprise (manager principal) */
    storePublicHoliday7(options = {}) {
      return request("POST", "/public-holidays", options);
    },

    /** Créer un jour férié d'entreprise (manager principal) */
    storePublicHoliday8(options = {}) {
      return request("POST", "/public-holidays", options);
    },

    /** Créer un jour férié d'entreprise (manager principal) */
    storePublicHoliday9(options = {}) {
      return request("POST", "/public-holidays", options);
    },

    /** Créer un jour férié d'entreprise (manager principal) */
    storePublicHoliday10(options = {}) {
      return request("POST", "/public-holidays", options);
    },

    /** Supprimer un jour férié d'entreprise (manager principal) */
    destroyPublicHoliday(options = {}) {
      return request("DELETE", "/public-holidays/{publicHoliday}", options);
    },

    /** Supprimer un jour férié d'entreprise (manager principal) */
    destroyPublicHoliday2(options = {}) {
      return request("DELETE", "/public-holidays/{publicHoliday}", options);
    },

    /** Supprimer un jour férié d'entreprise (manager principal) */
    destroyPublicHoliday3(options = {}) {
      return request("DELETE", "/public-holidays/{publicHoliday}", options);
    },

    /** Supprimer un jour férié d'entreprise (manager principal) */
    destroyPublicHoliday4(options = {}) {
      return request("DELETE", "/public-holidays/{publicHoliday}", options);
    },

    /** Supprimer un jour férié d'entreprise (manager principal) */
    destroyPublicHoliday5(options = {}) {
      return request("DELETE", "/public-holidays/{publicHoliday}", options);
    },

    /** Supprimer un jour férié d'entreprise (manager principal) */
    destroyPublicHoliday6(options = {}) {
      return request("DELETE", "/public-holidays/{publicHoliday}", options);
    },

    /** Supprimer un jour férié d'entreprise (manager principal) */
    destroyPublicHoliday7(options = {}) {
      return request("DELETE", "/public-holidays/{publicHoliday}", options);
    },

    /** Supprimer un jour férié d'entreprise (manager principal) */
    destroyPublicHoliday8(options = {}) {
      return request("DELETE", "/public-holidays/{publicHoliday}", options);
    },

    /** Supprimer un jour férié d'entreprise (manager principal) */
    destroyPublicHoliday9(options = {}) {
      return request("DELETE", "/public-holidays/{publicHoliday}", options);
    },

    /** Supprimer un jour férié d'entreprise (manager principal) */
    destroyPublicHoliday10(options = {}) {
      return request("DELETE", "/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié d'entreprise (manager principal) */
    updatePublicHoliday(options = {}) {
      return request("PUT", "/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié d'entreprise (manager principal) */
    updatePublicHoliday2(options = {}) {
      return request("PUT", "/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié d'entreprise (manager principal) */
    updatePublicHoliday3(options = {}) {
      return request("PUT", "/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié d'entreprise (manager principal) */
    updatePublicHoliday4(options = {}) {
      return request("PUT", "/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié d'entreprise (manager principal) */
    updatePublicHoliday5(options = {}) {
      return request("PUT", "/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié d'entreprise (manager principal) */
    updatePublicHoliday6(options = {}) {
      return request("PUT", "/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié d'entreprise (manager principal) */
    updatePublicHoliday7(options = {}) {
      return request("PUT", "/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié d'entreprise (manager principal) */
    updatePublicHoliday8(options = {}) {
      return request("PUT", "/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié d'entreprise (manager principal) */
    updatePublicHoliday9(options = {}) {
      return request("PUT", "/public-holidays/{publicHoliday}", options);
    },

    /** Modifier un jour férié d'entreprise (manager principal) */
    updatePublicHoliday10(options = {}) {
      return request("PUT", "/public-holidays/{publicHoliday}", options);
    },

    /** Offres d'emploi publiques d'une entreprise */
    getPublicCareersByCompanySlug(options = {}) {
      return request("GET", "/public/careers/{companySlug}", options);
    },

    /** Offres d'emploi publiques d'une entreprise */
    getPublicCareersByCompanySlug2(options = {}) {
      return request("GET", "/public/careers/{companySlug}", options);
    },

    /** Offres d'emploi publiques d'une entreprise */
    getPublicCareersByCompanySlug3(options = {}) {
      return request("GET", "/public/careers/{companySlug}", options);
    },

    /** Offres d'emploi publiques d'une entreprise */
    getPublicCareersByCompanySlug4(options = {}) {
      return request("GET", "/public/careers/{companySlug}", options);
    },

    /** Offres d'emploi publiques d'une entreprise */
    getPublicCareersByCompanySlug5(options = {}) {
      return request("GET", "/public/careers/{companySlug}", options);
    },

    /** Flux XML des offres (agrégateurs) */
    getPublicCareersByCompanySlugFeedXml(options = {}) {
      return request("GET", "/public/careers/{companySlug}/feed.xml", options);
    },

    /** Flux XML des offres (agrégateurs) */
    getPublicCareersByCompanySlugFeedXml2(options = {}) {
      return request("GET", "/public/careers/{companySlug}/feed.xml", options);
    },

    /** Flux XML des offres (agrégateurs) */
    getPublicCareersByCompanySlugFeedXml3(options = {}) {
      return request("GET", "/public/careers/{companySlug}/feed.xml", options);
    },

    /** Flux XML des offres (agrégateurs) */
    getPublicCareersByCompanySlugFeedXml4(options = {}) {
      return request("GET", "/public/careers/{companySlug}/feed.xml", options);
    },

    /** Flux XML des offres (agrégateurs) */
    getPublicCareersByCompanySlugFeedXml5(options = {}) {
      return request("GET", "/public/careers/{companySlug}/feed.xml", options);
    },

    /** Détail d'une offre publique */
    getPublicCareersByCompanySlugJobsByJobPosting(options = {}) {
      return request("GET", "/public/careers/{companySlug}/jobs/{jobPosting}", options);
    },

    /** Détail d'une offre publique */
    getPublicCareersByCompanySlugJobsByJobPosting2(options = {}) {
      return request("GET", "/public/careers/{companySlug}/jobs/{jobPosting}", options);
    },

    /** Détail d'une offre publique */
    getPublicCareersByCompanySlugJobsByJobPosting3(options = {}) {
      return request("GET", "/public/careers/{companySlug}/jobs/{jobPosting}", options);
    },

    /** Détail d'une offre publique */
    getPublicCareersByCompanySlugJobsByJobPosting4(options = {}) {
      return request("GET", "/public/careers/{companySlug}/jobs/{jobPosting}", options);
    },

    /** Détail d'une offre publique */
    getPublicCareersByCompanySlugJobsByJobPosting5(options = {}) {
      return request("GET", "/public/careers/{companySlug}/jobs/{jobPosting}", options);
    },

    /** Postuler à une offre publique (anti-doublon par email, #3860) */
    postPublicCareersByCompanySlugJobsByJobPostingApply(options = {}) {
      return request("POST", "/public/careers/{companySlug}/jobs/{jobPosting}/apply", options);
    },

    /** Postuler à une offre publique (anti-doublon par email, #3860) */
    postPublicCareersByCompanySlugJobsByJobPostingApply2(options = {}) {
      return request("POST", "/public/careers/{companySlug}/jobs/{jobPosting}/apply", options);
    },

    /** Postuler à une offre publique (anti-doublon par email, #3860) */
    postPublicCareersByCompanySlugJobsByJobPostingApply3(options = {}) {
      return request("POST", "/public/careers/{companySlug}/jobs/{jobPosting}/apply", options);
    },

    /** Postuler à une offre publique (anti-doublon par email, #3860) */
    postPublicCareersByCompanySlugJobsByJobPostingApply4(options = {}) {
      return request("POST", "/public/careers/{companySlug}/jobs/{jobPosting}/apply", options);
    },

    /** Postuler à une offre publique (anti-doublon par email, #3860) */
    postPublicCareersByCompanySlugJobsByJobPostingApply5(options = {}) {
      return request("POST", "/public/careers/{companySlug}/jobs/{jobPosting}/apply", options);
    },

    /** Envoyer une notification push de test a un employe */
    postPushNotificationsSend(options = {}) {
      return request("POST", "/push-notifications/send", options);
    },

    /** Envoyer une notification push de test a un employe */
    postPushNotificationsSend2(options = {}) {
      return request("POST", "/push-notifications/send", options);
    },

    /** Envoyer une notification push de test a un employe */
    postPushNotificationsSend3(options = {}) {
      return request("POST", "/push-notifications/send", options);
    },

    /** Envoyer une notification push de test a un employe */
    postPushNotificationsSend4(options = {}) {
      return request("POST", "/push-notifications/send", options);
    },

    /** Envoyer une notification push de test a un employe */
    postPushNotificationsSend5(options = {}) {
      return request("POST", "/push-notifications/send", options);
    },

    /** Envoyer une notification push de test a un employe */
    postPushNotificationsSend6(options = {}) {
      return request("POST", "/push-notifications/send", options);
    },

    /** Envoyer une notification push de test a un employe */
    postPushNotificationsSend7(options = {}) {
      return request("POST", "/push-notifications/send", options);
    },

    /** Envoyer une notification push de test a un employe */
    postPushNotificationsSend8(options = {}) {
      return request("POST", "/push-notifications/send", options);
    },

    /** Supprimer une candidature (manager) */
    deleteRecruitmentApplicantsByApplicant(options = {}) {
      return request("DELETE", "/recruitment/applicants/{applicant}", options);
    },

    /** Supprimer une candidature (manager) */
    deleteRecruitmentApplicantsByApplicant2(options = {}) {
      return request("DELETE", "/recruitment/applicants/{applicant}", options);
    },

    /** Supprimer une candidature (manager) */
    deleteRecruitmentApplicantsByApplicant3(options = {}) {
      return request("DELETE", "/recruitment/applicants/{applicant}", options);
    },

    /** Supprimer une candidature (manager) */
    deleteRecruitmentApplicantsByApplicant4(options = {}) {
      return request("DELETE", "/recruitment/applicants/{applicant}", options);
    },

    /** Supprimer une candidature (manager) */
    deleteRecruitmentApplicantsByApplicant5(options = {}) {
      return request("DELETE", "/recruitment/applicants/{applicant}", options);
    },

    /** Supprimer une candidature (manager) */
    deleteRecruitmentApplicantsByApplicant6(options = {}) {
      return request("DELETE", "/recruitment/applicants/{applicant}", options);
    },

    /** Supprimer une candidature (manager) */
    deleteRecruitmentApplicantsByApplicant7(options = {}) {
      return request("DELETE", "/recruitment/applicants/{applicant}", options);
    },

    /** Supprimer une candidature (manager) */
    deleteRecruitmentApplicantsByApplicant8(options = {}) {
      return request("DELETE", "/recruitment/applicants/{applicant}", options);
    },

    /** Supprimer une candidature (manager) */
    deleteRecruitmentApplicantsByApplicant9(options = {}) {
      return request("DELETE", "/recruitment/applicants/{applicant}", options);
    },

    /** Supprimer une candidature (manager) */
    deleteRecruitmentApplicantsByApplicant10(options = {}) {
      return request("DELETE", "/recruitment/applicants/{applicant}", options);
    },

    /** Supprimer une candidature (manager) */
    deleteRecruitmentApplicantsByApplicant11(options = {}) {
      return request("DELETE", "/recruitment/applicants/{applicant}", options);
    },

    /** Détail d'une candidature (manager) */
    getRecruitmentApplicantsByApplicant(options = {}) {
      return request("GET", "/recruitment/applicants/{applicant}", options);
    },

    /** Détail d'une candidature (manager) */
    getRecruitmentApplicantsByApplicant2(options = {}) {
      return request("GET", "/recruitment/applicants/{applicant}", options);
    },

    /** Détail d'une candidature (manager) */
    getRecruitmentApplicantsByApplicant3(options = {}) {
      return request("GET", "/recruitment/applicants/{applicant}", options);
    },

    /** Détail d'une candidature (manager) */
    getRecruitmentApplicantsByApplicant4(options = {}) {
      return request("GET", "/recruitment/applicants/{applicant}", options);
    },

    /** Détail d'une candidature (manager) */
    getRecruitmentApplicantsByApplicant5(options = {}) {
      return request("GET", "/recruitment/applicants/{applicant}", options);
    },

    /** Détail d'une candidature (manager) */
    getRecruitmentApplicantsByApplicant6(options = {}) {
      return request("GET", "/recruitment/applicants/{applicant}", options);
    },

    /** Détail d'une candidature (manager) */
    getRecruitmentApplicantsByApplicant7(options = {}) {
      return request("GET", "/recruitment/applicants/{applicant}", options);
    },

    /** Détail d'une candidature (manager) */
    getRecruitmentApplicantsByApplicant8(options = {}) {
      return request("GET", "/recruitment/applicants/{applicant}", options);
    },

    /** Détail d'une candidature (manager) */
    getRecruitmentApplicantsByApplicant9(options = {}) {
      return request("GET", "/recruitment/applicants/{applicant}", options);
    },

    /** Détail d'une candidature (manager) */
    getRecruitmentApplicantsByApplicant10(options = {}) {
      return request("GET", "/recruitment/applicants/{applicant}", options);
    },

    /** Détail d'une candidature (manager) */
    getRecruitmentApplicantsByApplicant11(options = {}) {
      return request("GET", "/recruitment/applicants/{applicant}", options);
    },

    /** Modifier un candidat */
    putRecruitmentApplicantsByApplicant(options = {}) {
      return request("PUT", "/recruitment/applicants/{applicant}", options);
    },

    /** Modifier un candidat */
    putRecruitmentApplicantsByApplicant2(options = {}) {
      return request("PUT", "/recruitment/applicants/{applicant}", options);
    },

    /** Modifier un candidat */
    putRecruitmentApplicantsByApplicant3(options = {}) {
      return request("PUT", "/recruitment/applicants/{applicant}", options);
    },

    /** Modifier un candidat */
    putRecruitmentApplicantsByApplicant4(options = {}) {
      return request("PUT", "/recruitment/applicants/{applicant}", options);
    },

    /** Modifier un candidat */
    putRecruitmentApplicantsByApplicant5(options = {}) {
      return request("PUT", "/recruitment/applicants/{applicant}", options);
    },

    /** Modifier un candidat */
    putRecruitmentApplicantsByApplicant6(options = {}) {
      return request("PUT", "/recruitment/applicants/{applicant}", options);
    },

    /** Modifier un candidat */
    putRecruitmentApplicantsByApplicant7(options = {}) {
      return request("PUT", "/recruitment/applicants/{applicant}", options);
    },

    /** Modifier un candidat */
    putRecruitmentApplicantsByApplicant8(options = {}) {
      return request("PUT", "/recruitment/applicants/{applicant}", options);
    },

    /** Modifier un candidat */
    putRecruitmentApplicantsByApplicant9(options = {}) {
      return request("PUT", "/recruitment/applicants/{applicant}", options);
    },

    /** Modifier un candidat */
    putRecruitmentApplicantsByApplicant10(options = {}) {
      return request("PUT", "/recruitment/applicants/{applicant}", options);
    },

    /** Modifier un candidat */
    putRecruitmentApplicantsByApplicant11(options = {}) {
      return request("PUT", "/recruitment/applicants/{applicant}", options);
    },

    /** Planifier un entretien */
    postRecruitmentApplicantsByApplicantInterviews(options = {}) {
      return request("POST", "/recruitment/applicants/{applicant}/interviews", options);
    },

    /** Planifier un entretien */
    postRecruitmentApplicantsByApplicantInterviews2(options = {}) {
      return request("POST", "/recruitment/applicants/{applicant}/interviews", options);
    },

    /** Planifier un entretien */
    postRecruitmentApplicantsByApplicantInterviews3(options = {}) {
      return request("POST", "/recruitment/applicants/{applicant}/interviews", options);
    },

    /** Planifier un entretien */
    postRecruitmentApplicantsByApplicantInterviews4(options = {}) {
      return request("POST", "/recruitment/applicants/{applicant}/interviews", options);
    },

    /** Planifier un entretien */
    postRecruitmentApplicantsByApplicantInterviews5(options = {}) {
      return request("POST", "/recruitment/applicants/{applicant}/interviews", options);
    },

    /** Planifier un entretien */
    postRecruitmentApplicantsByApplicantInterviews6(options = {}) {
      return request("POST", "/recruitment/applicants/{applicant}/interviews", options);
    },

    /** Planifier un entretien */
    postRecruitmentApplicantsByApplicantInterviews7(options = {}) {
      return request("POST", "/recruitment/applicants/{applicant}/interviews", options);
    },

    /** Planifier un entretien */
    postRecruitmentApplicantsByApplicantInterviews8(options = {}) {
      return request("POST", "/recruitment/applicants/{applicant}/interviews", options);
    },

    /** Planifier un entretien */
    postRecruitmentApplicantsByApplicantInterviews9(options = {}) {
      return request("POST", "/recruitment/applicants/{applicant}/interviews", options);
    },

    /** Planifier un entretien */
    postRecruitmentApplicantsByApplicantInterviews10(options = {}) {
      return request("POST", "/recruitment/applicants/{applicant}/interviews", options);
    },

    /** Planifier un entretien */
    postRecruitmentApplicantsByApplicantInterviews11(options = {}) {
      return request("POST", "/recruitment/applicants/{applicant}/interviews", options);
    },

    /** Changer le statut d'une candidature (principal/rh) */
    patchRecruitmentApplicantsByIdStatus(options = {}) {
      return request("PATCH", "/recruitment/applicants/{id}/status", options);
    },

    /** Changer le statut d'une candidature (principal/rh) */
    patchRecruitmentApplicantsByIdStatus2(options = {}) {
      return request("PATCH", "/recruitment/applicants/{id}/status", options);
    },

    /** Changer le statut d'une candidature (principal/rh) */
    patchRecruitmentApplicantsByIdStatus3(options = {}) {
      return request("PATCH", "/recruitment/applicants/{id}/status", options);
    },

    /** Changer le statut d'une candidature (principal/rh) */
    patchRecruitmentApplicantsByIdStatus4(options = {}) {
      return request("PATCH", "/recruitment/applicants/{id}/status", options);
    },

    /** Changer le statut d'une candidature (principal/rh) */
    patchRecruitmentApplicantsByIdStatus5(options = {}) {
      return request("PATCH", "/recruitment/applicants/{id}/status", options);
    },

    /** Changer le statut d'une candidature (principal/rh) */
    patchRecruitmentApplicantsByIdStatus6(options = {}) {
      return request("PATCH", "/recruitment/applicants/{id}/status", options);
    },

    /** Changer le statut d'une candidature (principal/rh) */
    patchRecruitmentApplicantsByIdStatus7(options = {}) {
      return request("PATCH", "/recruitment/applicants/{id}/status", options);
    },

    /** Changer le statut d'une candidature (principal/rh) */
    patchRecruitmentApplicantsByIdStatus8(options = {}) {
      return request("PATCH", "/recruitment/applicants/{id}/status", options);
    },

    /** Changer le statut d'une candidature (principal/rh) */
    patchRecruitmentApplicantsByIdStatus9(options = {}) {
      return request("PATCH", "/recruitment/applicants/{id}/status", options);
    },

    /** Changer le statut d'une candidature (principal/rh) */
    patchRecruitmentApplicantsByIdStatus10(options = {}) {
      return request("PATCH", "/recruitment/applicants/{id}/status", options);
    },

    /** Changer le statut d'une candidature (principal/rh) */
    patchRecruitmentApplicantsByIdStatus11(options = {}) {
      return request("PATCH", "/recruitment/applicants/{id}/status", options);
    },

    /** Saisir le feedback d'un entretien (le clôture) */
    patchRecruitmentInterviewsByIdFeedback(options = {}) {
      return request("PATCH", "/recruitment/interviews/{id}/feedback", options);
    },

    /** Saisir le feedback d'un entretien (le clôture) */
    patchRecruitmentInterviewsByIdFeedback2(options = {}) {
      return request("PATCH", "/recruitment/interviews/{id}/feedback", options);
    },

    /** Saisir le feedback d'un entretien (le clôture) */
    patchRecruitmentInterviewsByIdFeedback3(options = {}) {
      return request("PATCH", "/recruitment/interviews/{id}/feedback", options);
    },

    /** Saisir le feedback d'un entretien (le clôture) */
    patchRecruitmentInterviewsByIdFeedback4(options = {}) {
      return request("PATCH", "/recruitment/interviews/{id}/feedback", options);
    },

    /** Supprimer un entretien (manager) */
    deleteRecruitmentInterviewsByInterview(options = {}) {
      return request("DELETE", "/recruitment/interviews/{interview}", options);
    },

    /** Supprimer un entretien (manager) */
    deleteRecruitmentInterviewsByInterview2(options = {}) {
      return request("DELETE", "/recruitment/interviews/{interview}", options);
    },

    /** Supprimer un entretien (manager) */
    deleteRecruitmentInterviewsByInterview3(options = {}) {
      return request("DELETE", "/recruitment/interviews/{interview}", options);
    },

    /** Supprimer un entretien (manager) */
    deleteRecruitmentInterviewsByInterview4(options = {}) {
      return request("DELETE", "/recruitment/interviews/{interview}", options);
    },

    /** Supprimer un entretien (manager) */
    deleteRecruitmentInterviewsByInterview5(options = {}) {
      return request("DELETE", "/recruitment/interviews/{interview}", options);
    },

    /** Supprimer un entretien (manager) */
    deleteRecruitmentInterviewsByInterview6(options = {}) {
      return request("DELETE", "/recruitment/interviews/{interview}", options);
    },

    /** Supprimer un entretien (manager) */
    deleteRecruitmentInterviewsByInterview7(options = {}) {
      return request("DELETE", "/recruitment/interviews/{interview}", options);
    },

    /** Supprimer un entretien (manager) */
    deleteRecruitmentInterviewsByInterview8(options = {}) {
      return request("DELETE", "/recruitment/interviews/{interview}", options);
    },

    /** Supprimer un entretien (manager) */
    deleteRecruitmentInterviewsByInterview9(options = {}) {
      return request("DELETE", "/recruitment/interviews/{interview}", options);
    },

    /** Supprimer un entretien (manager) */
    deleteRecruitmentInterviewsByInterview10(options = {}) {
      return request("DELETE", "/recruitment/interviews/{interview}", options);
    },

    /** Supprimer un entretien (manager) */
    deleteRecruitmentInterviewsByInterview11(options = {}) {
      return request("DELETE", "/recruitment/interviews/{interview}", options);
    },

    /** Modifier un entretien */
    putRecruitmentInterviewsByInterview(options = {}) {
      return request("PUT", "/recruitment/interviews/{interview}", options);
    },

    /** Modifier un entretien */
    putRecruitmentInterviewsByInterview2(options = {}) {
      return request("PUT", "/recruitment/interviews/{interview}", options);
    },

    /** Modifier un entretien */
    putRecruitmentInterviewsByInterview3(options = {}) {
      return request("PUT", "/recruitment/interviews/{interview}", options);
    },

    /** Modifier un entretien */
    putRecruitmentInterviewsByInterview4(options = {}) {
      return request("PUT", "/recruitment/interviews/{interview}", options);
    },

    /** Modifier un entretien */
    putRecruitmentInterviewsByInterview5(options = {}) {
      return request("PUT", "/recruitment/interviews/{interview}", options);
    },

    /** Modifier un entretien */
    putRecruitmentInterviewsByInterview6(options = {}) {
      return request("PUT", "/recruitment/interviews/{interview}", options);
    },

    /** Modifier un entretien */
    putRecruitmentInterviewsByInterview7(options = {}) {
      return request("PUT", "/recruitment/interviews/{interview}", options);
    },

    /** Modifier un entretien */
    putRecruitmentInterviewsByInterview8(options = {}) {
      return request("PUT", "/recruitment/interviews/{interview}", options);
    },

    /** Modifier un entretien */
    putRecruitmentInterviewsByInterview9(options = {}) {
      return request("PUT", "/recruitment/interviews/{interview}", options);
    },

    /** Modifier un entretien */
    putRecruitmentInterviewsByInterview10(options = {}) {
      return request("PUT", "/recruitment/interviews/{interview}", options);
    },

    /** Modifier un entretien */
    putRecruitmentInterviewsByInterview11(options = {}) {
      return request("PUT", "/recruitment/interviews/{interview}", options);
    },

    /** Saisir le feedback d'un entretien (le clôture) */
    patchRecruitmentInterviewsByInterviewFeedback(options = {}) {
      return request("PATCH", "/recruitment/interviews/{interview}/feedback", options);
    },

    /** Saisir le feedback d'un entretien (le clôture) */
    patchRecruitmentInterviewsByInterviewFeedback2(options = {}) {
      return request("PATCH", "/recruitment/interviews/{interview}/feedback", options);
    },

    /** Saisir le feedback d'un entretien (le clôture) */
    patchRecruitmentInterviewsByInterviewFeedback3(options = {}) {
      return request("PATCH", "/recruitment/interviews/{interview}/feedback", options);
    },

    /** Saisir le feedback d'un entretien (le clôture) */
    patchRecruitmentInterviewsByInterviewFeedback4(options = {}) {
      return request("PATCH", "/recruitment/interviews/{interview}/feedback", options);
    },

    /** Saisir le feedback d'un entretien (le clôture) */
    patchRecruitmentInterviewsByInterviewFeedback5(options = {}) {
      return request("PATCH", "/recruitment/interviews/{interview}/feedback", options);
    },

    /** Saisir le feedback d'un entretien (le clôture) */
    patchRecruitmentInterviewsByInterviewFeedback6(options = {}) {
      return request("PATCH", "/recruitment/interviews/{interview}/feedback", options);
    },

    /** Saisir le feedback d'un entretien (le clôture) */
    patchRecruitmentInterviewsByInterviewFeedback7(options = {}) {
      return request("PATCH", "/recruitment/interviews/{interview}/feedback", options);
    },

    /** Lister les offres d'emploi */
    getRecruitmentJobs(options = {}) {
      return request("GET", "/recruitment/jobs", options);
    },

    /** Lister les offres d'emploi */
    getRecruitmentJobs2(options = {}) {
      return request("GET", "/recruitment/jobs", options);
    },

    /** Lister les offres d'emploi */
    getRecruitmentJobs3(options = {}) {
      return request("GET", "/recruitment/jobs", options);
    },

    /** Lister les offres d'emploi */
    getRecruitmentJobs4(options = {}) {
      return request("GET", "/recruitment/jobs", options);
    },

    /** Lister les offres d'emploi */
    getRecruitmentJobs5(options = {}) {
      return request("GET", "/recruitment/jobs", options);
    },

    /** Lister les offres d'emploi */
    getRecruitmentJobs6(options = {}) {
      return request("GET", "/recruitment/jobs", options);
    },

    /** Lister les offres d'emploi */
    getRecruitmentJobs7(options = {}) {
      return request("GET", "/recruitment/jobs", options);
    },

    /** Lister les offres d'emploi */
    getRecruitmentJobs8(options = {}) {
      return request("GET", "/recruitment/jobs", options);
    },

    /** Lister les offres d'emploi */
    getRecruitmentJobs9(options = {}) {
      return request("GET", "/recruitment/jobs", options);
    },

    /** Lister les offres d'emploi */
    getRecruitmentJobs10(options = {}) {
      return request("GET", "/recruitment/jobs", options);
    },

    /** Lister les offres d'emploi */
    getRecruitmentJobs11(options = {}) {
      return request("GET", "/recruitment/jobs", options);
    },

    /** Creer une offre d'emploi */
    postRecruitmentJobs(options = {}) {
      return request("POST", "/recruitment/jobs", options);
    },

    /** Creer une offre d'emploi */
    postRecruitmentJobs2(options = {}) {
      return request("POST", "/recruitment/jobs", options);
    },

    /** Creer une offre d'emploi */
    postRecruitmentJobs3(options = {}) {
      return request("POST", "/recruitment/jobs", options);
    },

    /** Creer une offre d'emploi */
    postRecruitmentJobs4(options = {}) {
      return request("POST", "/recruitment/jobs", options);
    },

    /** Creer une offre d'emploi */
    postRecruitmentJobs5(options = {}) {
      return request("POST", "/recruitment/jobs", options);
    },

    /** Creer une offre d'emploi */
    postRecruitmentJobs6(options = {}) {
      return request("POST", "/recruitment/jobs", options);
    },

    /** Creer une offre d'emploi */
    postRecruitmentJobs7(options = {}) {
      return request("POST", "/recruitment/jobs", options);
    },

    /** Creer une offre d'emploi */
    postRecruitmentJobs8(options = {}) {
      return request("POST", "/recruitment/jobs", options);
    },

    /** Creer une offre d'emploi */
    postRecruitmentJobs9(options = {}) {
      return request("POST", "/recruitment/jobs", options);
    },

    /** Creer une offre d'emploi */
    postRecruitmentJobs10(options = {}) {
      return request("POST", "/recruitment/jobs", options);
    },

    /** Creer une offre d'emploi */
    postRecruitmentJobs11(options = {}) {
      return request("POST", "/recruitment/jobs", options);
    },

    /** Supprimer une offre d'emploi (manager) */
    deleteRecruitmentJobsByJobPosting(options = {}) {
      return request("DELETE", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Supprimer une offre d'emploi (manager) */
    deleteRecruitmentJobsByJobPosting2(options = {}) {
      return request("DELETE", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Supprimer une offre d'emploi (manager) */
    deleteRecruitmentJobsByJobPosting3(options = {}) {
      return request("DELETE", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Supprimer une offre d'emploi (manager) */
    deleteRecruitmentJobsByJobPosting4(options = {}) {
      return request("DELETE", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Supprimer une offre d'emploi (manager) */
    deleteRecruitmentJobsByJobPosting5(options = {}) {
      return request("DELETE", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Supprimer une offre d'emploi (manager) */
    deleteRecruitmentJobsByJobPosting6(options = {}) {
      return request("DELETE", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Supprimer une offre d'emploi (manager) */
    deleteRecruitmentJobsByJobPosting7(options = {}) {
      return request("DELETE", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Supprimer une offre d'emploi (manager) */
    deleteRecruitmentJobsByJobPosting8(options = {}) {
      return request("DELETE", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Supprimer une offre d'emploi (manager) */
    deleteRecruitmentJobsByJobPosting9(options = {}) {
      return request("DELETE", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Supprimer une offre d'emploi (manager) */
    deleteRecruitmentJobsByJobPosting10(options = {}) {
      return request("DELETE", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Supprimer une offre d'emploi (manager) */
    deleteRecruitmentJobsByJobPosting11(options = {}) {
      return request("DELETE", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Voir une offre */
    getRecruitmentJobsByJobPosting(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Voir une offre */
    getRecruitmentJobsByJobPosting2(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Voir une offre */
    getRecruitmentJobsByJobPosting3(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Voir une offre */
    getRecruitmentJobsByJobPosting4(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Voir une offre */
    getRecruitmentJobsByJobPosting5(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Voir une offre */
    getRecruitmentJobsByJobPosting6(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Voir une offre */
    getRecruitmentJobsByJobPosting7(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Voir une offre */
    getRecruitmentJobsByJobPosting8(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Voir une offre */
    getRecruitmentJobsByJobPosting9(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Voir une offre */
    getRecruitmentJobsByJobPosting10(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Voir une offre */
    getRecruitmentJobsByJobPosting11(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Modifier une offre */
    putRecruitmentJobsByJobPosting(options = {}) {
      return request("PUT", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Modifier une offre */
    putRecruitmentJobsByJobPosting2(options = {}) {
      return request("PUT", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Modifier une offre */
    putRecruitmentJobsByJobPosting3(options = {}) {
      return request("PUT", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Modifier une offre */
    putRecruitmentJobsByJobPosting4(options = {}) {
      return request("PUT", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Modifier une offre */
    putRecruitmentJobsByJobPosting5(options = {}) {
      return request("PUT", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Modifier une offre */
    putRecruitmentJobsByJobPosting6(options = {}) {
      return request("PUT", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Modifier une offre */
    putRecruitmentJobsByJobPosting7(options = {}) {
      return request("PUT", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Modifier une offre */
    putRecruitmentJobsByJobPosting8(options = {}) {
      return request("PUT", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Modifier une offre */
    putRecruitmentJobsByJobPosting9(options = {}) {
      return request("PUT", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Modifier une offre */
    putRecruitmentJobsByJobPosting10(options = {}) {
      return request("PUT", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Modifier une offre */
    putRecruitmentJobsByJobPosting11(options = {}) {
      return request("PUT", "/recruitment/jobs/{jobPosting}", options);
    },

    /** Lister les candidats */
    getRecruitmentJobsByJobPostingApplicants(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Lister les candidats */
    getRecruitmentJobsByJobPostingApplicants2(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Lister les candidats */
    getRecruitmentJobsByJobPostingApplicants3(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Lister les candidats */
    getRecruitmentJobsByJobPostingApplicants4(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Lister les candidats */
    getRecruitmentJobsByJobPostingApplicants5(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Lister les candidats */
    getRecruitmentJobsByJobPostingApplicants6(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Lister les candidats */
    getRecruitmentJobsByJobPostingApplicants7(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Lister les candidats */
    getRecruitmentJobsByJobPostingApplicants8(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Lister les candidats */
    getRecruitmentJobsByJobPostingApplicants9(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Lister les candidats */
    getRecruitmentJobsByJobPostingApplicants10(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Lister les candidats */
    getRecruitmentJobsByJobPostingApplicants11(options = {}) {
      return request("GET", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Ajouter un candidat */
    postRecruitmentJobsByJobPostingApplicants(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Ajouter un candidat */
    postRecruitmentJobsByJobPostingApplicants2(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Ajouter un candidat */
    postRecruitmentJobsByJobPostingApplicants3(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Ajouter un candidat */
    postRecruitmentJobsByJobPostingApplicants4(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Ajouter un candidat */
    postRecruitmentJobsByJobPostingApplicants5(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Ajouter un candidat */
    postRecruitmentJobsByJobPostingApplicants6(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Ajouter un candidat */
    postRecruitmentJobsByJobPostingApplicants7(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Ajouter un candidat */
    postRecruitmentJobsByJobPostingApplicants8(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Ajouter un candidat */
    postRecruitmentJobsByJobPostingApplicants9(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Ajouter un candidat */
    postRecruitmentJobsByJobPostingApplicants10(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Ajouter un candidat */
    postRecruitmentJobsByJobPostingApplicants11(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/applicants", options);
    },

    /** Cloturer une offre */
    postRecruitmentJobsByJobPostingClose(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/close", options);
    },

    /** Cloturer une offre */
    postRecruitmentJobsByJobPostingClose2(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/close", options);
    },

    /** Cloturer une offre */
    postRecruitmentJobsByJobPostingClose3(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/close", options);
    },

    /** Cloturer une offre */
    postRecruitmentJobsByJobPostingClose4(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/close", options);
    },

    /** Cloturer une offre */
    postRecruitmentJobsByJobPostingClose5(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/close", options);
    },

    /** Cloturer une offre */
    postRecruitmentJobsByJobPostingClose6(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/close", options);
    },

    /** Cloturer une offre */
    postRecruitmentJobsByJobPostingClose7(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/close", options);
    },

    /** Cloturer une offre */
    postRecruitmentJobsByJobPostingClose8(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/close", options);
    },

    /** Cloturer une offre */
    postRecruitmentJobsByJobPostingClose9(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/close", options);
    },

    /** Cloturer une offre */
    postRecruitmentJobsByJobPostingClose10(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/close", options);
    },

    /** Cloturer une offre */
    postRecruitmentJobsByJobPostingClose11(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/close", options);
    },

    /** Publier une offre */
    postRecruitmentJobsByJobPostingPublish(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/publish", options);
    },

    /** Publier une offre */
    postRecruitmentJobsByJobPostingPublish2(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/publish", options);
    },

    /** Publier une offre */
    postRecruitmentJobsByJobPostingPublish3(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/publish", options);
    },

    /** Publier une offre */
    postRecruitmentJobsByJobPostingPublish4(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/publish", options);
    },

    /** Publier une offre */
    postRecruitmentJobsByJobPostingPublish5(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/publish", options);
    },

    /** Publier une offre */
    postRecruitmentJobsByJobPostingPublish6(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/publish", options);
    },

    /** Publier une offre */
    postRecruitmentJobsByJobPostingPublish7(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/publish", options);
    },

    /** Publier une offre */
    postRecruitmentJobsByJobPostingPublish8(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/publish", options);
    },

    /** Publier une offre */
    postRecruitmentJobsByJobPostingPublish9(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/publish", options);
    },

    /** Publier une offre */
    postRecruitmentJobsByJobPostingPublish10(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/publish", options);
    },

    /** Publier une offre */
    postRecruitmentJobsByJobPostingPublish11(options = {}) {
      return request("POST", "/recruitment/jobs/{jobPosting}/publish", options);
    },

    /** Absentéisme du mois/année */
    getReportsAbsenteeism(options = {}) {
      return request("GET", "/reports/absenteeism", options);
    },

    /** Absentéisme du mois/année */
    getReportsAbsenteeism2(options = {}) {
      return request("GET", "/reports/absenteeism", options);
    },

    /** Absentéisme du mois/année */
    getReportsAbsenteeism3(options = {}) {
      return request("GET", "/reports/absenteeism", options);
    },

    /** Absentéisme du mois/année */
    getReportsAbsenteeism4(options = {}) {
      return request("GET", "/reports/absenteeism", options);
    },

    /** Absentéisme du mois/année */
    getReportsAbsenteeism5(options = {}) {
      return request("GET", "/reports/absenteeism", options);
    },

    /** Absentéisme du mois/année */
    getReportsAbsenteeism6(options = {}) {
      return request("GET", "/reports/absenteeism", options);
    },

    /** Absentéisme du mois/année */
    getReportsAbsenteeism7(options = {}) {
      return request("GET", "/reports/absenteeism", options);
    },

    /** Absentéisme du mois/année */
    getReportsAbsenteeism8(options = {}) {
      return request("GET", "/reports/absenteeism", options);
    },

    /** Analyse des coûts (masse salariale par département) */
    getReportsCostAnalysis(options = {}) {
      return request("GET", "/reports/cost-analysis", options);
    },

    /** Analyse des coûts (masse salariale par département) */
    getReportsCostAnalysis2(options = {}) {
      return request("GET", "/reports/cost-analysis", options);
    },

    /** Analyse des coûts (masse salariale par département) */
    getReportsCostAnalysis3(options = {}) {
      return request("GET", "/reports/cost-analysis", options);
    },

    /** Analyse des coûts (masse salariale par département) */
    getReportsCostAnalysis4(options = {}) {
      return request("GET", "/reports/cost-analysis", options);
    },

    /** Analyse des coûts (masse salariale par département) */
    getReportsCostAnalysis5(options = {}) {
      return request("GET", "/reports/cost-analysis", options);
    },

    /** Analyse des coûts (masse salariale par département) */
    getReportsCostAnalysis6(options = {}) {
      return request("GET", "/reports/cost-analysis", options);
    },

    /** Analyse des coûts (masse salariale par département) */
    getReportsCostAnalysis7(options = {}) {
      return request("GET", "/reports/cost-analysis", options);
    },

    /** Analyse des coûts (masse salariale par département) */
    getReportsCostAnalysis8(options = {}) {
      return request("GET", "/reports/cost-analysis", options);
    },

    /** Ventilation démographique des employés */
    getReportsDemographics(options = {}) {
      return request("GET", "/reports/demographics", options);
    },

    /** Ventilation démographique des employés */
    getReportsDemographics2(options = {}) {
      return request("GET", "/reports/demographics", options);
    },

    /** Ventilation démographique des employés */
    getReportsDemographics3(options = {}) {
      return request("GET", "/reports/demographics", options);
    },

    /** Ventilation démographique des employés */
    getReportsDemographics4(options = {}) {
      return request("GET", "/reports/demographics", options);
    },

    /** Ventilation démographique des employés */
    getReportsDemographics5(options = {}) {
      return request("GET", "/reports/demographics", options);
    },

    /** Ventilation démographique des employés */
    getReportsDemographics6(options = {}) {
      return request("GET", "/reports/demographics", options);
    },

    /** Ventilation démographique des employés */
    getReportsDemographics7(options = {}) {
      return request("GET", "/reports/demographics", options);
    },

    /** Ventilation démographique des employés */
    getReportsDemographics8(options = {}) {
      return request("GET", "/reports/demographics", options);
    },

    /** Effectif par département, type de contrat et genre */
    getReportsHeadcount(options = {}) {
      return request("GET", "/reports/headcount", options);
    },

    /** Effectif par département, type de contrat et genre */
    getReportsHeadcount2(options = {}) {
      return request("GET", "/reports/headcount", options);
    },

    /** Effectif par département, type de contrat et genre */
    getReportsHeadcount3(options = {}) {
      return request("GET", "/reports/headcount", options);
    },

    /** Effectif par département, type de contrat et genre */
    getReportsHeadcount4(options = {}) {
      return request("GET", "/reports/headcount", options);
    },

    /** Effectif par département, type de contrat et genre */
    getReportsHeadcount5(options = {}) {
      return request("GET", "/reports/headcount", options);
    },

    /** Effectif par département, type de contrat et genre */
    getReportsHeadcount6(options = {}) {
      return request("GET", "/reports/headcount", options);
    },

    /** Effectif par département, type de contrat et genre */
    getReportsHeadcount7(options = {}) {
      return request("GET", "/reports/headcount", options);
    },

    /** Effectif par département, type de contrat et genre */
    getReportsHeadcount8(options = {}) {
      return request("GET", "/reports/headcount", options);
    },

    /** Synthèse des avances/prêts employés */
    getReportsLoanSummary(options = {}) {
      return request("GET", "/reports/loan-summary", options);
    },

    /** Synthèse des avances/prêts employés */
    getReportsLoanSummary2(options = {}) {
      return request("GET", "/reports/loan-summary", options);
    },

    /** Synthèse des avances/prêts employés */
    getReportsLoanSummary3(options = {}) {
      return request("GET", "/reports/loan-summary", options);
    },

    /** Synthèse des avances/prêts employés */
    getReportsLoanSummary4(options = {}) {
      return request("GET", "/reports/loan-summary", options);
    },

    /** Synthèse des avances/prêts employés */
    getReportsLoanSummary5(options = {}) {
      return request("GET", "/reports/loan-summary", options);
    },

    /** Synthèse des avances/prêts employés */
    getReportsLoanSummary6(options = {}) {
      return request("GET", "/reports/loan-summary", options);
    },

    /** Synthèse des avances/prêts employés */
    getReportsLoanSummary7(options = {}) {
      return request("GET", "/reports/loan-summary", options);
    },

    /** Synthèse des avances/prêts employés */
    getReportsLoanSummary8(options = {}) {
      return request("GET", "/reports/loan-summary", options);
    },

    /** Heures supplémentaires du mois/année */
    getReportsOvertime(options = {}) {
      return request("GET", "/reports/overtime", options);
    },

    /** Heures supplémentaires du mois/année */
    getReportsOvertime2(options = {}) {
      return request("GET", "/reports/overtime", options);
    },

    /** Heures supplémentaires du mois/année */
    getReportsOvertime3(options = {}) {
      return request("GET", "/reports/overtime", options);
    },

    /** Heures supplémentaires du mois/année */
    getReportsOvertime4(options = {}) {
      return request("GET", "/reports/overtime", options);
    },

    /** Heures supplémentaires du mois/année */
    getReportsOvertime5(options = {}) {
      return request("GET", "/reports/overtime", options);
    },

    /** Heures supplémentaires du mois/année */
    getReportsOvertime6(options = {}) {
      return request("GET", "/reports/overtime", options);
    },

    /** Heures supplémentaires du mois/année */
    getReportsOvertime7(options = {}) {
      return request("GET", "/reports/overtime", options);
    },

    /** Heures supplémentaires du mois/année */
    getReportsOvertime8(options = {}) {
      return request("GET", "/reports/overtime", options);
    },

    /** Synthèse paie du mois/année */
    getReportsPayrollSummary(options = {}) {
      return request("GET", "/reports/payroll-summary", options);
    },

    /** Synthèse paie du mois/année */
    getReportsPayrollSummary2(options = {}) {
      return request("GET", "/reports/payroll-summary", options);
    },

    /** Synthèse paie du mois/année */
    getReportsPayrollSummary3(options = {}) {
      return request("GET", "/reports/payroll-summary", options);
    },

    /** Synthèse paie du mois/année */
    getReportsPayrollSummary4(options = {}) {
      return request("GET", "/reports/payroll-summary", options);
    },

    /** Synthèse paie du mois/année */
    getReportsPayrollSummary5(options = {}) {
      return request("GET", "/reports/payroll-summary", options);
    },

    /** Synthèse paie du mois/année */
    getReportsPayrollSummary6(options = {}) {
      return request("GET", "/reports/payroll-summary", options);
    },

    /** Synthèse paie du mois/année */
    getReportsPayrollSummary7(options = {}) {
      return request("GET", "/reports/payroll-summary", options);
    },

    /** Synthèse paie du mois/année */
    getReportsPayrollSummary8(options = {}) {
      return request("GET", "/reports/payroll-summary", options);
    },

    /** Pipeline recrutement (candidatures par statut) */
    getReportsRecruitmentPipeline(options = {}) {
      return request("GET", "/reports/recruitment-pipeline", options);
    },

    /** Pipeline recrutement (candidatures par statut) */
    getReportsRecruitmentPipeline2(options = {}) {
      return request("GET", "/reports/recruitment-pipeline", options);
    },

    /** Pipeline recrutement (candidatures par statut) */
    getReportsRecruitmentPipeline3(options = {}) {
      return request("GET", "/reports/recruitment-pipeline", options);
    },

    /** Pipeline recrutement (candidatures par statut) */
    getReportsRecruitmentPipeline4(options = {}) {
      return request("GET", "/reports/recruitment-pipeline", options);
    },

    /** Pipeline recrutement (candidatures par statut) */
    getReportsRecruitmentPipeline5(options = {}) {
      return request("GET", "/reports/recruitment-pipeline", options);
    },

    /** Pipeline recrutement (candidatures par statut) */
    getReportsRecruitmentPipeline6(options = {}) {
      return request("GET", "/reports/recruitment-pipeline", options);
    },

    /** Pipeline recrutement (candidatures par statut) */
    getReportsRecruitmentPipeline7(options = {}) {
      return request("GET", "/reports/recruitment-pipeline", options);
    },

    /** Pipeline recrutement (candidatures par statut) */
    getReportsRecruitmentPipeline8(options = {}) {
      return request("GET", "/reports/recruitment-pipeline", options);
    },

    /** Taux de complétion des formations */
    getReportsTrainingCompletion(options = {}) {
      return request("GET", "/reports/training-completion", options);
    },

    /** Taux de complétion des formations */
    getReportsTrainingCompletion2(options = {}) {
      return request("GET", "/reports/training-completion", options);
    },

    /** Taux de complétion des formations */
    getReportsTrainingCompletion3(options = {}) {
      return request("GET", "/reports/training-completion", options);
    },

    /** Taux de complétion des formations */
    getReportsTrainingCompletion4(options = {}) {
      return request("GET", "/reports/training-completion", options);
    },

    /** Taux de complétion des formations */
    getReportsTrainingCompletion5(options = {}) {
      return request("GET", "/reports/training-completion", options);
    },

    /** Taux de complétion des formations */
    getReportsTrainingCompletion6(options = {}) {
      return request("GET", "/reports/training-completion", options);
    },

    /** Taux de complétion des formations */
    getReportsTrainingCompletion7(options = {}) {
      return request("GET", "/reports/training-completion", options);
    },

    /** Taux de complétion des formations */
    getReportsTrainingCompletion8(options = {}) {
      return request("GET", "/reports/training-completion", options);
    },

    /** Taux de rotation (turnover) sur N mois */
    getReportsTurnover(options = {}) {
      return request("GET", "/reports/turnover", options);
    },

    /** Taux de rotation (turnover) sur N mois */
    getReportsTurnover2(options = {}) {
      return request("GET", "/reports/turnover", options);
    },

    /** Taux de rotation (turnover) sur N mois */
    getReportsTurnover3(options = {}) {
      return request("GET", "/reports/turnover", options);
    },

    /** Taux de rotation (turnover) sur N mois */
    getReportsTurnover4(options = {}) {
      return request("GET", "/reports/turnover", options);
    },

    /** Taux de rotation (turnover) sur N mois */
    getReportsTurnover5(options = {}) {
      return request("GET", "/reports/turnover", options);
    },

    /** Taux de rotation (turnover) sur N mois */
    getReportsTurnover6(options = {}) {
      return request("GET", "/reports/turnover", options);
    },

    /** Taux de rotation (turnover) sur N mois */
    getReportsTurnover7(options = {}) {
      return request("GET", "/reports/turnover", options);
    },

    /** Taux de rotation (turnover) sur N mois */
    getReportsTurnover8(options = {}) {
      return request("GET", "/reports/turnover", options);
    },

    /** Lister les avances sur salaire */
    getSalaryAdvances(options = {}) {
      return request("GET", "/salary-advances", options);
    },

    /** Lister les avances sur salaire */
    getSalaryAdvances2(options = {}) {
      return request("GET", "/salary-advances", options);
    },

    /** Lister les avances sur salaire */
    getSalaryAdvances3(options = {}) {
      return request("GET", "/salary-advances", options);
    },

    /** Lister les avances sur salaire */
    getSalaryAdvances4(options = {}) {
      return request("GET", "/salary-advances", options);
    },

    /** Lister les avances sur salaire */
    getSalaryAdvances5(options = {}) {
      return request("GET", "/salary-advances", options);
    },

    /** Lister les avances sur salaire */
    getSalaryAdvances6(options = {}) {
      return request("GET", "/salary-advances", options);
    },

    /** Lister les avances sur salaire */
    getSalaryAdvances7(options = {}) {
      return request("GET", "/salary-advances", options);
    },

    /** Lister les avances sur salaire */
    getSalaryAdvances8(options = {}) {
      return request("GET", "/salary-advances", options);
    },

    /** Demander une avance sur salaire */
    postSalaryAdvances(options = {}) {
      return request("POST", "/salary-advances", options);
    },

    /** Demander une avance sur salaire */
    postSalaryAdvances2(options = {}) {
      return request("POST", "/salary-advances", options);
    },

    /** Demander une avance sur salaire */
    postSalaryAdvances3(options = {}) {
      return request("POST", "/salary-advances", options);
    },

    /** Demander une avance sur salaire */
    postSalaryAdvances4(options = {}) {
      return request("POST", "/salary-advances", options);
    },

    /** Demander une avance sur salaire */
    postSalaryAdvances5(options = {}) {
      return request("POST", "/salary-advances", options);
    },

    /** Demander une avance sur salaire */
    postSalaryAdvances6(options = {}) {
      return request("POST", "/salary-advances", options);
    },

    /** Demander une avance sur salaire */
    postSalaryAdvances7(options = {}) {
      return request("POST", "/salary-advances", options);
    },

    /** Demander une avance sur salaire */
    postSalaryAdvances8(options = {}) {
      return request("POST", "/salary-advances", options);
    },

    /** Annuler une avance (employe proprietaire) */
    deleteSalaryAdvancesBySalaryAdvance(options = {}) {
      return request("DELETE", "/salary-advances/{salaryAdvance}", options);
    },

    /** Annuler une avance (employe proprietaire) */
    deleteSalaryAdvancesBySalaryAdvance2(options = {}) {
      return request("DELETE", "/salary-advances/{salaryAdvance}", options);
    },

    /** Annuler une avance (employe proprietaire) */
    deleteSalaryAdvancesBySalaryAdvance3(options = {}) {
      return request("DELETE", "/salary-advances/{salaryAdvance}", options);
    },

    /** Annuler une avance (employe proprietaire) */
    deleteSalaryAdvancesBySalaryAdvance4(options = {}) {
      return request("DELETE", "/salary-advances/{salaryAdvance}", options);
    },

    /** Annuler une avance (employe proprietaire) */
    deleteSalaryAdvancesBySalaryAdvance5(options = {}) {
      return request("DELETE", "/salary-advances/{salaryAdvance}", options);
    },

    /** Annuler une avance (employe proprietaire) */
    deleteSalaryAdvancesBySalaryAdvance6(options = {}) {
      return request("DELETE", "/salary-advances/{salaryAdvance}", options);
    },

    /** Annuler une avance (employe proprietaire) */
    deleteSalaryAdvancesBySalaryAdvance7(options = {}) {
      return request("DELETE", "/salary-advances/{salaryAdvance}", options);
    },

    /** Annuler une avance (employe proprietaire) */
    deleteSalaryAdvancesBySalaryAdvance8(options = {}) {
      return request("DELETE", "/salary-advances/{salaryAdvance}", options);
    },

    /** Voir une avance */
    getSalaryAdvancesBySalaryAdvance(options = {}) {
      return request("GET", "/salary-advances/{salaryAdvance}", options);
    },

    /** Voir une avance */
    getSalaryAdvancesBySalaryAdvance2(options = {}) {
      return request("GET", "/salary-advances/{salaryAdvance}", options);
    },

    /** Voir une avance */
    getSalaryAdvancesBySalaryAdvance3(options = {}) {
      return request("GET", "/salary-advances/{salaryAdvance}", options);
    },

    /** Voir une avance */
    getSalaryAdvancesBySalaryAdvance4(options = {}) {
      return request("GET", "/salary-advances/{salaryAdvance}", options);
    },

    /** Voir une avance */
    getSalaryAdvancesBySalaryAdvance5(options = {}) {
      return request("GET", "/salary-advances/{salaryAdvance}", options);
    },

    /** Voir une avance */
    getSalaryAdvancesBySalaryAdvance6(options = {}) {
      return request("GET", "/salary-advances/{salaryAdvance}", options);
    },

    /** Voir une avance */
    getSalaryAdvancesBySalaryAdvance7(options = {}) {
      return request("GET", "/salary-advances/{salaryAdvance}", options);
    },

    /** Voir une avance */
    getSalaryAdvancesBySalaryAdvance8(options = {}) {
      return request("GET", "/salary-advances/{salaryAdvance}", options);
    },

    /** Approuver une avance (manager) */
    postSalaryAdvancesBySalaryAdvanceApprove(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/approve", options);
    },

    /** Approuver une avance (manager) */
    postSalaryAdvancesBySalaryAdvanceApprove2(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/approve", options);
    },

    /** Approuver une avance (manager) */
    postSalaryAdvancesBySalaryAdvanceApprove3(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/approve", options);
    },

    /** Approuver une avance (manager) */
    postSalaryAdvancesBySalaryAdvanceApprove4(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/approve", options);
    },

    /** Approuver une avance (manager) */
    postSalaryAdvancesBySalaryAdvanceApprove5(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/approve", options);
    },

    /** Approuver une avance (manager) */
    postSalaryAdvancesBySalaryAdvanceApprove6(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/approve", options);
    },

    /** Approuver une avance (manager) */
    postSalaryAdvancesBySalaryAdvanceApprove7(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/approve", options);
    },

    /** Approuver une avance (manager) */
    postSalaryAdvancesBySalaryAdvanceApprove8(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/approve", options);
    },

    /** Approuver une avance sur salaire (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceApprove(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/approve", options);
    },

    /** Approuver une avance sur salaire (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceApprove2(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/approve", options);
    },

    /** Approuver une avance sur salaire (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceApprove3(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/approve", options);
    },

    /** Approuver une avance sur salaire (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceApprove4(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/approve", options);
    },

    /** Approuver une avance sur salaire (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceApprove5(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/approve", options);
    },

    /** Approuver une avance sur salaire (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceApprove6(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/approve", options);
    },

    /** Approuver une avance sur salaire (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceApprove7(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/approve", options);
    },

    /** Approuver une avance sur salaire (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceApprove8(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/approve", options);
    },

    /** Confirmer la reception de l'avance (employe) */
    putSalaryAdvancesBySalaryAdvanceConfirmReceived(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/confirm-received", options);
    },

    /** Confirmer la reception de l'avance (employe) */
    putSalaryAdvancesBySalaryAdvanceConfirmReceived2(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/confirm-received", options);
    },

    /** Confirmer la reception de l'avance (employe) */
    putSalaryAdvancesBySalaryAdvanceConfirmReceived3(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/confirm-received", options);
    },

    /** Confirmer la reception de l'avance (employe) */
    putSalaryAdvancesBySalaryAdvanceConfirmReceived4(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/confirm-received", options);
    },

    /** Confirmer la reception de l'avance (employe) */
    putSalaryAdvancesBySalaryAdvanceConfirmReceived5(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/confirm-received", options);
    },

    /** Confirmer la reception de l'avance (employe) */
    putSalaryAdvancesBySalaryAdvanceConfirmReceived6(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/confirm-received", options);
    },

    /** Confirmer la reception de l'avance (employe) */
    putSalaryAdvancesBySalaryAdvanceConfirmReceived7(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/confirm-received", options);
    },

    /** Confirmer la reception de l'avance (employe) */
    putSalaryAdvancesBySalaryAdvanceConfirmReceived8(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/confirm-received", options);
    },

    /** Ouvrir une reclamation sur une avance dont le paiement a ete declare mais non recu (employe) */
    disputeSalaryAdvance(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/dispute", options);
    },

    /** Ouvrir une reclamation sur une avance dont le paiement a ete declare mais non recu (employe) */
    disputeSalaryAdvance2(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/dispute", options);
    },

    /** Ouvrir une reclamation sur une avance dont le paiement a ete declare mais non recu (employe) */
    disputeSalaryAdvance3(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/dispute", options);
    },

    /** Ouvrir une reclamation sur une avance dont le paiement a ete declare mais non recu (employe) */
    disputeSalaryAdvance4(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/dispute", options);
    },

    /** Ouvrir une reclamation sur une avance dont le paiement a ete declare mais non recu (employe) */
    disputeSalaryAdvance5(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/dispute", options);
    },

    /** Ouvrir une reclamation sur une avance dont le paiement a ete declare mais non recu (employe) */
    disputeSalaryAdvance6(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/dispute", options);
    },

    /** Ouvrir une reclamation sur une avance dont le paiement a ete declare mais non recu (employe) */
    disputeSalaryAdvance7(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/dispute", options);
    },

    /** Ouvrir une reclamation sur une avance dont le paiement a ete declare mais non recu (employe) */
    disputeSalaryAdvance8(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/dispute", options);
    },

    /** Valider une avance avant paiement (manager/RH) */
    postSalaryAdvancesBySalaryAdvanceManagerApprove(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/manager-approve", options);
    },

    /** Valider une avance avant paiement (manager/RH) */
    postSalaryAdvancesBySalaryAdvanceManagerApprove2(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/manager-approve", options);
    },

    /** Valider une avance avant paiement (manager/RH) */
    postSalaryAdvancesBySalaryAdvanceManagerApprove3(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/manager-approve", options);
    },

    /** Valider une avance avant paiement (manager/RH) */
    postSalaryAdvancesBySalaryAdvanceManagerApprove4(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/manager-approve", options);
    },

    /** Valider une avance avant paiement (manager/RH) */
    postSalaryAdvancesBySalaryAdvanceManagerApprove5(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/manager-approve", options);
    },

    /** Valider une avance avant paiement (manager/RH) */
    postSalaryAdvancesBySalaryAdvanceManagerApprove6(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/manager-approve", options);
    },

    /** Valider une avance avant paiement (manager/RH) */
    postSalaryAdvancesBySalaryAdvanceManagerApprove7(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/manager-approve", options);
    },

    /** Valider une avance avant paiement (manager/RH) */
    postSalaryAdvancesBySalaryAdvanceManagerApprove8(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/manager-approve", options);
    },

    /** Validation manager d'une avance (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceManagerApprove(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/manager-approve", options);
    },

    /** Validation manager d'une avance (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceManagerApprove2(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/manager-approve", options);
    },

    /** Validation manager d'une avance (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceManagerApprove3(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/manager-approve", options);
    },

    /** Validation manager d'une avance (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceManagerApprove4(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/manager-approve", options);
    },

    /** Validation manager d'une avance (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceManagerApprove5(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/manager-approve", options);
    },

    /** Validation manager d'une avance (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceManagerApprove6(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/manager-approve", options);
    },

    /** Validation manager d'une avance (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceManagerApprove7(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/manager-approve", options);
    },

    /** Validation manager d'une avance (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceManagerApprove8(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/manager-approve", options);
    },

    /** Declarer l'avance comme envoyee (manager/RH) */
    putSalaryAdvancesBySalaryAdvanceMarkPaid(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/mark-paid", options);
    },

    /** Declarer l'avance comme envoyee (manager/RH) */
    putSalaryAdvancesBySalaryAdvanceMarkPaid2(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/mark-paid", options);
    },

    /** Declarer l'avance comme envoyee (manager/RH) */
    putSalaryAdvancesBySalaryAdvanceMarkPaid3(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/mark-paid", options);
    },

    /** Declarer l'avance comme envoyee (manager/RH) */
    putSalaryAdvancesBySalaryAdvanceMarkPaid4(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/mark-paid", options);
    },

    /** Declarer l'avance comme envoyee (manager/RH) */
    putSalaryAdvancesBySalaryAdvanceMarkPaid5(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/mark-paid", options);
    },

    /** Declarer l'avance comme envoyee (manager/RH) */
    putSalaryAdvancesBySalaryAdvanceMarkPaid6(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/mark-paid", options);
    },

    /** Declarer l'avance comme envoyee (manager/RH) */
    putSalaryAdvancesBySalaryAdvanceMarkPaid7(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/mark-paid", options);
    },

    /** Declarer l'avance comme envoyee (manager/RH) */
    putSalaryAdvancesBySalaryAdvanceMarkPaid8(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/mark-paid", options);
    },

    /** Telecharger le justificatif joint a une demande d'avance (employe proprietaire ou manager) */
    downloadSalaryAdvanceProof(options = {}) {
      return request("GET", "/salary-advances/{salaryAdvance}/proof", options);
    },

    /** Telecharger le justificatif joint a une demande d'avance (employe proprietaire ou manager) */
    downloadSalaryAdvanceProof2(options = {}) {
      return request("GET", "/salary-advances/{salaryAdvance}/proof", options);
    },

    /** Telecharger le justificatif joint a une demande d'avance (employe proprietaire ou manager) */
    downloadSalaryAdvanceProof3(options = {}) {
      return request("GET", "/salary-advances/{salaryAdvance}/proof", options);
    },

    /** Telecharger le justificatif joint a une demande d'avance (employe proprietaire ou manager) */
    downloadSalaryAdvanceProof4(options = {}) {
      return request("GET", "/salary-advances/{salaryAdvance}/proof", options);
    },

    /** Telecharger le justificatif joint a une demande d'avance (employe proprietaire ou manager) */
    downloadSalaryAdvanceProof5(options = {}) {
      return request("GET", "/salary-advances/{salaryAdvance}/proof", options);
    },

    /** Telecharger le justificatif joint a une demande d'avance (employe proprietaire ou manager) */
    downloadSalaryAdvanceProof6(options = {}) {
      return request("GET", "/salary-advances/{salaryAdvance}/proof", options);
    },

    /** Telecharger le justificatif joint a une demande d'avance (employe proprietaire ou manager) */
    downloadSalaryAdvanceProof7(options = {}) {
      return request("GET", "/salary-advances/{salaryAdvance}/proof", options);
    },

    /** Telecharger le justificatif joint a une demande d'avance (employe proprietaire ou manager) */
    downloadSalaryAdvanceProof8(options = {}) {
      return request("GET", "/salary-advances/{salaryAdvance}/proof", options);
    },

    /** Rejeter une avance (manager) */
    postSalaryAdvancesBySalaryAdvanceReject(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/reject", options);
    },

    /** Rejeter une avance (manager) */
    postSalaryAdvancesBySalaryAdvanceReject2(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/reject", options);
    },

    /** Rejeter une avance (manager) */
    postSalaryAdvancesBySalaryAdvanceReject3(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/reject", options);
    },

    /** Rejeter une avance (manager) */
    postSalaryAdvancesBySalaryAdvanceReject4(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/reject", options);
    },

    /** Rejeter une avance (manager) */
    postSalaryAdvancesBySalaryAdvanceReject5(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/reject", options);
    },

    /** Rejeter une avance (manager) */
    postSalaryAdvancesBySalaryAdvanceReject6(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/reject", options);
    },

    /** Rejeter une avance (manager) */
    postSalaryAdvancesBySalaryAdvanceReject7(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/reject", options);
    },

    /** Rejeter une avance (manager) */
    postSalaryAdvancesBySalaryAdvanceReject8(options = {}) {
      return request("POST", "/salary-advances/{salaryAdvance}/reject", options);
    },

    /** Refuser une avance sur salaire (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceReject(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/reject", options);
    },

    /** Refuser une avance sur salaire (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceReject2(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/reject", options);
    },

    /** Refuser une avance sur salaire (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceReject3(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/reject", options);
    },

    /** Refuser une avance sur salaire (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceReject4(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/reject", options);
    },

    /** Refuser une avance sur salaire (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceReject5(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/reject", options);
    },

    /** Refuser une avance sur salaire (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceReject6(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/reject", options);
    },

    /** Refuser une avance sur salaire (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceReject7(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/reject", options);
    },

    /** Refuser une avance sur salaire (déprécié — utiliser POST) */
    putSalaryAdvancesBySalaryAdvanceReject8(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/reject", options);
    },

    /** Resoudre une reclamation sur une avance (manager/RH) */
    resolveSalaryAdvanceDispute(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/resolve-dispute", options);
    },

    /** Resoudre une reclamation sur une avance (manager/RH) */
    resolveSalaryAdvanceDispute2(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/resolve-dispute", options);
    },

    /** Resoudre une reclamation sur une avance (manager/RH) */
    resolveSalaryAdvanceDispute3(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/resolve-dispute", options);
    },

    /** Resoudre une reclamation sur une avance (manager/RH) */
    resolveSalaryAdvanceDispute4(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/resolve-dispute", options);
    },

    /** Resoudre une reclamation sur une avance (manager/RH) */
    resolveSalaryAdvanceDispute5(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/resolve-dispute", options);
    },

    /** Resoudre une reclamation sur une avance (manager/RH) */
    resolveSalaryAdvanceDispute6(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/resolve-dispute", options);
    },

    /** Resoudre une reclamation sur une avance (manager/RH) */
    resolveSalaryAdvanceDispute7(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/resolve-dispute", options);
    },

    /** Resoudre une reclamation sur une avance (manager/RH) */
    resolveSalaryAdvanceDispute8(options = {}) {
      return request("PUT", "/salary-advances/{salaryAdvance}/resolve-dispute", options);
    },

    /** Lister les composants de salaire (manager) */
    listSalaryComponents(options = {}) {
      return request("GET", "/salary-components", options);
    },

    /** Lister les composants de salaire (manager) */
    listSalaryComponents2(options = {}) {
      return request("GET", "/salary-components", options);
    },

    /** Lister les composants de salaire (manager) */
    listSalaryComponents3(options = {}) {
      return request("GET", "/salary-components", options);
    },

    /** Lister les composants de salaire (manager) */
    listSalaryComponents4(options = {}) {
      return request("GET", "/salary-components", options);
    },

    /** Lister les composants de salaire (manager) */
    listSalaryComponents5(options = {}) {
      return request("GET", "/salary-components", options);
    },

    /** Lister les composants de salaire (manager) */
    listSalaryComponents6(options = {}) {
      return request("GET", "/salary-components", options);
    },

    /** Lister les composants de salaire (manager) */
    listSalaryComponents7(options = {}) {
      return request("GET", "/salary-components", options);
    },

    /** Lister les composants de salaire (manager) */
    listSalaryComponents8(options = {}) {
      return request("GET", "/salary-components", options);
    },

    /** Lister les composants de salaire (manager) */
    listSalaryComponents9(options = {}) {
      return request("GET", "/salary-components", options);
    },

    /** Lister les composants de salaire (manager) */
    listSalaryComponents10(options = {}) {
      return request("GET", "/salary-components", options);
    },

    /** Creer un composant de salaire (manager) */
    createSalaryComponent(options = {}) {
      return request("POST", "/salary-components", options);
    },

    /** Creer un composant de salaire (manager) */
    createSalaryComponent2(options = {}) {
      return request("POST", "/salary-components", options);
    },

    /** Creer un composant de salaire (manager) */
    createSalaryComponent3(options = {}) {
      return request("POST", "/salary-components", options);
    },

    /** Creer un composant de salaire (manager) */
    createSalaryComponent4(options = {}) {
      return request("POST", "/salary-components", options);
    },

    /** Creer un composant de salaire (manager) */
    createSalaryComponent5(options = {}) {
      return request("POST", "/salary-components", options);
    },

    /** Creer un composant de salaire (manager) */
    createSalaryComponent6(options = {}) {
      return request("POST", "/salary-components", options);
    },

    /** Creer un composant de salaire (manager) */
    createSalaryComponent7(options = {}) {
      return request("POST", "/salary-components", options);
    },

    /** Creer un composant de salaire (manager) */
    createSalaryComponent8(options = {}) {
      return request("POST", "/salary-components", options);
    },

    /** Creer un composant de salaire (manager) */
    createSalaryComponent9(options = {}) {
      return request("POST", "/salary-components", options);
    },

    /** Creer un composant de salaire (manager) */
    createSalaryComponent10(options = {}) {
      return request("POST", "/salary-components", options);
    },

    /** Supprimer un composant de salaire (manager) */
    deleteSalaryComponent(options = {}) {
      return request("DELETE", "/salary-components/{salaryComponent}", options);
    },

    /** Supprimer un composant de salaire (manager) */
    deleteSalaryComponent2(options = {}) {
      return request("DELETE", "/salary-components/{salaryComponent}", options);
    },

    /** Supprimer un composant de salaire (manager) */
    deleteSalaryComponent3(options = {}) {
      return request("DELETE", "/salary-components/{salaryComponent}", options);
    },

    /** Supprimer un composant de salaire (manager) */
    deleteSalaryComponent4(options = {}) {
      return request("DELETE", "/salary-components/{salaryComponent}", options);
    },

    /** Supprimer un composant de salaire (manager) */
    deleteSalaryComponent5(options = {}) {
      return request("DELETE", "/salary-components/{salaryComponent}", options);
    },

    /** Supprimer un composant de salaire (manager) */
    deleteSalaryComponent6(options = {}) {
      return request("DELETE", "/salary-components/{salaryComponent}", options);
    },

    /** Supprimer un composant de salaire (manager) */
    deleteSalaryComponent7(options = {}) {
      return request("DELETE", "/salary-components/{salaryComponent}", options);
    },

    /** Supprimer un composant de salaire (manager) */
    deleteSalaryComponent8(options = {}) {
      return request("DELETE", "/salary-components/{salaryComponent}", options);
    },

    /** Supprimer un composant de salaire (manager) */
    deleteSalaryComponent9(options = {}) {
      return request("DELETE", "/salary-components/{salaryComponent}", options);
    },

    /** Supprimer un composant de salaire (manager) */
    deleteSalaryComponent10(options = {}) {
      return request("DELETE", "/salary-components/{salaryComponent}", options);
    },

    /** Voir un composant de salaire (manager) */
    showSalaryComponent(options = {}) {
      return request("GET", "/salary-components/{salaryComponent}", options);
    },

    /** Voir un composant de salaire (manager) */
    showSalaryComponent2(options = {}) {
      return request("GET", "/salary-components/{salaryComponent}", options);
    },

    /** Voir un composant de salaire (manager) */
    showSalaryComponent3(options = {}) {
      return request("GET", "/salary-components/{salaryComponent}", options);
    },

    /** Voir un composant de salaire (manager) */
    showSalaryComponent4(options = {}) {
      return request("GET", "/salary-components/{salaryComponent}", options);
    },

    /** Voir un composant de salaire (manager) */
    showSalaryComponent5(options = {}) {
      return request("GET", "/salary-components/{salaryComponent}", options);
    },

    /** Voir un composant de salaire (manager) */
    showSalaryComponent6(options = {}) {
      return request("GET", "/salary-components/{salaryComponent}", options);
    },

    /** Voir un composant de salaire (manager) */
    showSalaryComponent7(options = {}) {
      return request("GET", "/salary-components/{salaryComponent}", options);
    },

    /** Voir un composant de salaire (manager) */
    showSalaryComponent8(options = {}) {
      return request("GET", "/salary-components/{salaryComponent}", options);
    },

    /** Voir un composant de salaire (manager) */
    showSalaryComponent9(options = {}) {
      return request("GET", "/salary-components/{salaryComponent}", options);
    },

    /** Voir un composant de salaire (manager) */
    showSalaryComponent10(options = {}) {
      return request("GET", "/salary-components/{salaryComponent}", options);
    },

    /** Modifier un composant de salaire (manager) */
    updateSalaryComponent(options = {}) {
      return request("PUT", "/salary-components/{salaryComponent}", options);
    },

    /** Modifier un composant de salaire (manager) */
    updateSalaryComponent2(options = {}) {
      return request("PUT", "/salary-components/{salaryComponent}", options);
    },

    /** Modifier un composant de salaire (manager) */
    updateSalaryComponent3(options = {}) {
      return request("PUT", "/salary-components/{salaryComponent}", options);
    },

    /** Modifier un composant de salaire (manager) */
    updateSalaryComponent4(options = {}) {
      return request("PUT", "/salary-components/{salaryComponent}", options);
    },

    /** Modifier un composant de salaire (manager) */
    updateSalaryComponent5(options = {}) {
      return request("PUT", "/salary-components/{salaryComponent}", options);
    },

    /** Modifier un composant de salaire (manager) */
    updateSalaryComponent6(options = {}) {
      return request("PUT", "/salary-components/{salaryComponent}", options);
    },

    /** Modifier un composant de salaire (manager) */
    updateSalaryComponent7(options = {}) {
      return request("PUT", "/salary-components/{salaryComponent}", options);
    },

    /** Modifier un composant de salaire (manager) */
    updateSalaryComponent8(options = {}) {
      return request("PUT", "/salary-components/{salaryComponent}", options);
    },

    /** Modifier un composant de salaire (manager) */
    updateSalaryComponent9(options = {}) {
      return request("PUT", "/salary-components/{salaryComponent}", options);
    },

    /** Modifier un composant de salaire (manager) */
    updateSalaryComponent10(options = {}) {
      return request("PUT", "/salary-components/{salaryComponent}", options);
    },

    /** Lister les structures salariales (manager) */
    listSalaryStructures(options = {}) {
      return request("GET", "/salary-structures", options);
    },

    /** Lister les structures salariales (manager) */
    listSalaryStructures2(options = {}) {
      return request("GET", "/salary-structures", options);
    },

    /** Lister les structures salariales (manager) */
    listSalaryStructures3(options = {}) {
      return request("GET", "/salary-structures", options);
    },

    /** Lister les structures salariales (manager) */
    listSalaryStructures4(options = {}) {
      return request("GET", "/salary-structures", options);
    },

    /** Lister les structures salariales (manager) */
    listSalaryStructures5(options = {}) {
      return request("GET", "/salary-structures", options);
    },

    /** Lister les structures salariales (manager) */
    listSalaryStructures6(options = {}) {
      return request("GET", "/salary-structures", options);
    },

    /** Lister les structures salariales (manager) */
    listSalaryStructures7(options = {}) {
      return request("GET", "/salary-structures", options);
    },

    /** Lister les structures salariales (manager) */
    listSalaryStructures8(options = {}) {
      return request("GET", "/salary-structures", options);
    },

    /** Lister les structures salariales (manager) */
    listSalaryStructures9(options = {}) {
      return request("GET", "/salary-structures", options);
    },

    /** Lister les structures salariales (manager) */
    listSalaryStructures10(options = {}) {
      return request("GET", "/salary-structures", options);
    },

    /** Creer une structure salariale (manager) */
    createSalaryStructure(options = {}) {
      return request("POST", "/salary-structures", options);
    },

    /** Creer une structure salariale (manager) */
    createSalaryStructure2(options = {}) {
      return request("POST", "/salary-structures", options);
    },

    /** Creer une structure salariale (manager) */
    createSalaryStructure3(options = {}) {
      return request("POST", "/salary-structures", options);
    },

    /** Creer une structure salariale (manager) */
    createSalaryStructure4(options = {}) {
      return request("POST", "/salary-structures", options);
    },

    /** Creer une structure salariale (manager) */
    createSalaryStructure5(options = {}) {
      return request("POST", "/salary-structures", options);
    },

    /** Creer une structure salariale (manager) */
    createSalaryStructure6(options = {}) {
      return request("POST", "/salary-structures", options);
    },

    /** Creer une structure salariale (manager) */
    createSalaryStructure7(options = {}) {
      return request("POST", "/salary-structures", options);
    },

    /** Creer une structure salariale (manager) */
    createSalaryStructure8(options = {}) {
      return request("POST", "/salary-structures", options);
    },

    /** Creer une structure salariale (manager) */
    createSalaryStructure9(options = {}) {
      return request("POST", "/salary-structures", options);
    },

    /** Creer une structure salariale (manager) */
    createSalaryStructure10(options = {}) {
      return request("POST", "/salary-structures", options);
    },

    /** Supprimer une structure salariale (manager) */
    deleteSalaryStructure(options = {}) {
      return request("DELETE", "/salary-structures/{salaryStructure}", options);
    },

    /** Supprimer une structure salariale (manager) */
    deleteSalaryStructure2(options = {}) {
      return request("DELETE", "/salary-structures/{salaryStructure}", options);
    },

    /** Supprimer une structure salariale (manager) */
    deleteSalaryStructure3(options = {}) {
      return request("DELETE", "/salary-structures/{salaryStructure}", options);
    },

    /** Supprimer une structure salariale (manager) */
    deleteSalaryStructure4(options = {}) {
      return request("DELETE", "/salary-structures/{salaryStructure}", options);
    },

    /** Supprimer une structure salariale (manager) */
    deleteSalaryStructure5(options = {}) {
      return request("DELETE", "/salary-structures/{salaryStructure}", options);
    },

    /** Supprimer une structure salariale (manager) */
    deleteSalaryStructure6(options = {}) {
      return request("DELETE", "/salary-structures/{salaryStructure}", options);
    },

    /** Supprimer une structure salariale (manager) */
    deleteSalaryStructure7(options = {}) {
      return request("DELETE", "/salary-structures/{salaryStructure}", options);
    },

    /** Supprimer une structure salariale (manager) */
    deleteSalaryStructure8(options = {}) {
      return request("DELETE", "/salary-structures/{salaryStructure}", options);
    },

    /** Supprimer une structure salariale (manager) */
    deleteSalaryStructure9(options = {}) {
      return request("DELETE", "/salary-structures/{salaryStructure}", options);
    },

    /** Supprimer une structure salariale (manager) */
    deleteSalaryStructure10(options = {}) {
      return request("DELETE", "/salary-structures/{salaryStructure}", options);
    },

    /** Voir une structure salariale avec ses composants (manager) */
    showSalaryStructure(options = {}) {
      return request("GET", "/salary-structures/{salaryStructure}", options);
    },

    /** Voir une structure salariale avec ses composants (manager) */
    showSalaryStructure2(options = {}) {
      return request("GET", "/salary-structures/{salaryStructure}", options);
    },

    /** Voir une structure salariale avec ses composants (manager) */
    showSalaryStructure3(options = {}) {
      return request("GET", "/salary-structures/{salaryStructure}", options);
    },

    /** Voir une structure salariale avec ses composants (manager) */
    showSalaryStructure4(options = {}) {
      return request("GET", "/salary-structures/{salaryStructure}", options);
    },

    /** Voir une structure salariale avec ses composants (manager) */
    showSalaryStructure5(options = {}) {
      return request("GET", "/salary-structures/{salaryStructure}", options);
    },

    /** Voir une structure salariale avec ses composants (manager) */
    showSalaryStructure6(options = {}) {
      return request("GET", "/salary-structures/{salaryStructure}", options);
    },

    /** Voir une structure salariale avec ses composants (manager) */
    showSalaryStructure7(options = {}) {
      return request("GET", "/salary-structures/{salaryStructure}", options);
    },

    /** Voir une structure salariale avec ses composants (manager) */
    showSalaryStructure8(options = {}) {
      return request("GET", "/salary-structures/{salaryStructure}", options);
    },

    /** Voir une structure salariale avec ses composants (manager) */
    showSalaryStructure9(options = {}) {
      return request("GET", "/salary-structures/{salaryStructure}", options);
    },

    /** Voir une structure salariale avec ses composants (manager) */
    showSalaryStructure10(options = {}) {
      return request("GET", "/salary-structures/{salaryStructure}", options);
    },

    /** Modifier une structure salariale (manager) */
    updateSalaryStructure(options = {}) {
      return request("PUT", "/salary-structures/{salaryStructure}", options);
    },

    /** Modifier une structure salariale (manager) */
    updateSalaryStructure2(options = {}) {
      return request("PUT", "/salary-structures/{salaryStructure}", options);
    },

    /** Modifier une structure salariale (manager) */
    updateSalaryStructure3(options = {}) {
      return request("PUT", "/salary-structures/{salaryStructure}", options);
    },

    /** Modifier une structure salariale (manager) */
    updateSalaryStructure4(options = {}) {
      return request("PUT", "/salary-structures/{salaryStructure}", options);
    },

    /** Modifier une structure salariale (manager) */
    updateSalaryStructure5(options = {}) {
      return request("PUT", "/salary-structures/{salaryStructure}", options);
    },

    /** Modifier une structure salariale (manager) */
    updateSalaryStructure6(options = {}) {
      return request("PUT", "/salary-structures/{salaryStructure}", options);
    },

    /** Modifier une structure salariale (manager) */
    updateSalaryStructure7(options = {}) {
      return request("PUT", "/salary-structures/{salaryStructure}", options);
    },

    /** Modifier une structure salariale (manager) */
    updateSalaryStructure8(options = {}) {
      return request("PUT", "/salary-structures/{salaryStructure}", options);
    },

    /** Modifier une structure salariale (manager) */
    updateSalaryStructure9(options = {}) {
      return request("PUT", "/salary-structures/{salaryStructure}", options);
    },

    /** Modifier une structure salariale (manager) */
    updateSalaryStructure10(options = {}) {
      return request("PUT", "/salary-structures/{salaryStructure}", options);
    },

    /** Lister les horaires de l'entreprise */
    getSchedules(options = {}) {
      return request("GET", "/schedules", options);
    },

    /** Lister les horaires de l'entreprise */
    getSchedules2(options = {}) {
      return request("GET", "/schedules", options);
    },

    /** Lister les horaires de l'entreprise */
    getSchedules3(options = {}) {
      return request("GET", "/schedules", options);
    },

    /** Lister les horaires de l'entreprise */
    getSchedules4(options = {}) {
      return request("GET", "/schedules", options);
    },

    /** Lister les horaires de l'entreprise */
    getSchedules5(options = {}) {
      return request("GET", "/schedules", options);
    },

    /** Creer un horaire de travail */
    postSchedules(options = {}) {
      return request("POST", "/schedules", options);
    },

    /** Creer un horaire de travail */
    postSchedules2(options = {}) {
      return request("POST", "/schedules", options);
    },

    /** Creer un horaire de travail */
    postSchedules3(options = {}) {
      return request("POST", "/schedules", options);
    },

    /** Creer un horaire de travail */
    postSchedules4(options = {}) {
      return request("POST", "/schedules", options);
    },

    /** Creer un horaire de travail */
    postSchedules5(options = {}) {
      return request("POST", "/schedules", options);
    },

    /** Supprimer un horaire non defaut */
    deleteSchedulesBySchedule(options = {}) {
      return request("DELETE", "/schedules/{schedule}", options);
    },

    /** Supprimer un horaire non defaut */
    deleteSchedulesBySchedule2(options = {}) {
      return request("DELETE", "/schedules/{schedule}", options);
    },

    /** Supprimer un horaire non defaut */
    deleteSchedulesBySchedule3(options = {}) {
      return request("DELETE", "/schedules/{schedule}", options);
    },

    /** Supprimer un horaire non defaut */
    deleteSchedulesBySchedule4(options = {}) {
      return request("DELETE", "/schedules/{schedule}", options);
    },

    /** Supprimer un horaire non defaut */
    deleteSchedulesBySchedule5(options = {}) {
      return request("DELETE", "/schedules/{schedule}", options);
    },

    /** Détail d'un planning */
    getSchedulesBySchedule(options = {}) {
      return request("GET", "/schedules/{schedule}", options);
    },

    /** Détail d'un planning */
    getSchedulesBySchedule2(options = {}) {
      return request("GET", "/schedules/{schedule}", options);
    },

    /** Détail d'un planning */
    getSchedulesBySchedule3(options = {}) {
      return request("GET", "/schedules/{schedule}", options);
    },

    /** Détail d'un planning */
    getSchedulesBySchedule4(options = {}) {
      return request("GET", "/schedules/{schedule}", options);
    },

    /** Détail d'un planning */
    getSchedulesBySchedule5(options = {}) {
      return request("GET", "/schedules/{schedule}", options);
    },

    /** Mettre à jour un planning (manager — sécurité #2217) */
    patchSchedulesBySchedule(options = {}) {
      return request("PATCH", "/schedules/{schedule}", options);
    },

    /** Mettre à jour un planning (manager — sécurité #2217) */
    patchSchedulesBySchedule2(options = {}) {
      return request("PATCH", "/schedules/{schedule}", options);
    },

    /** Mettre à jour un planning (manager — sécurité #2217) */
    patchSchedulesBySchedule3(options = {}) {
      return request("PATCH", "/schedules/{schedule}", options);
    },

    /** Mettre à jour un planning (manager — sécurité #2217) */
    patchSchedulesBySchedule4(options = {}) {
      return request("PATCH", "/schedules/{schedule}", options);
    },

    /** Mettre à jour un planning (manager — sécurité #2217) */
    patchSchedulesBySchedule5(options = {}) {
      return request("PATCH", "/schedules/{schedule}", options);
    },

    /** Modifier un horaire */
    putSchedulesBySchedule(options = {}) {
      return request("PUT", "/schedules/{schedule}", options);
    },

    /** Modifier un horaire */
    putSchedulesBySchedule2(options = {}) {
      return request("PUT", "/schedules/{schedule}", options);
    },

    /** Modifier un horaire */
    putSchedulesBySchedule3(options = {}) {
      return request("PUT", "/schedules/{schedule}", options);
    },

    /** Modifier un horaire */
    putSchedulesBySchedule4(options = {}) {
      return request("PUT", "/schedules/{schedule}", options);
    },

    /** Modifier un horaire */
    putSchedulesBySchedule5(options = {}) {
      return request("PUT", "/schedules/{schedule}", options);
    },

    /** Affecter un horaire/regle entreprise a des employes */
    postSchedulesByScheduleAssignEmployees(options = {}) {
      return request("POST", "/schedules/{schedule}/assign-employees", options);
    },

    /** Affecter un horaire/regle entreprise a des employes */
    postSchedulesByScheduleAssignEmployees2(options = {}) {
      return request("POST", "/schedules/{schedule}/assign-employees", options);
    },

    /** Affecter un horaire/regle entreprise a des employes */
    postSchedulesByScheduleAssignEmployees3(options = {}) {
      return request("POST", "/schedules/{schedule}/assign-employees", options);
    },

    /** Affecter un horaire/regle entreprise a des employes */
    postSchedulesByScheduleAssignEmployees4(options = {}) {
      return request("POST", "/schedules/{schedule}/assign-employees", options);
    },

    /** Affecter un horaire/regle entreprise a des employes */
    postSchedulesByScheduleAssignEmployees5(options = {}) {
      return request("POST", "/schedules/{schedule}/assign-employees", options);
    },

    /** Lister les sites */
    getSites(options = {}) {
      return request("GET", "/sites", options);
    },

    /** Lister les sites */
    getSites2(options = {}) {
      return request("GET", "/sites", options);
    },

    /** Lister les sites */
    getSites3(options = {}) {
      return request("GET", "/sites", options);
    },

    /** Lister les sites */
    getSites4(options = {}) {
      return request("GET", "/sites", options);
    },

    /** Lister les sites */
    getSites5(options = {}) {
      return request("GET", "/sites", options);
    },

    /** Lister les sites */
    getSites6(options = {}) {
      return request("GET", "/sites", options);
    },

    /** Lister les sites */
    getSites7(options = {}) {
      return request("GET", "/sites", options);
    },

    /** Lister les sites */
    getSites8(options = {}) {
      return request("GET", "/sites", options);
    },

    /** Creer un site */
    postSites(options = {}) {
      return request("POST", "/sites", options);
    },

    /** Creer un site */
    postSites2(options = {}) {
      return request("POST", "/sites", options);
    },

    /** Creer un site */
    postSites3(options = {}) {
      return request("POST", "/sites", options);
    },

    /** Creer un site */
    postSites4(options = {}) {
      return request("POST", "/sites", options);
    },

    /** Creer un site */
    postSites5(options = {}) {
      return request("POST", "/sites", options);
    },

    /** Creer un site */
    postSites6(options = {}) {
      return request("POST", "/sites", options);
    },

    /** Creer un site */
    postSites7(options = {}) {
      return request("POST", "/sites", options);
    },

    /** Creer un site */
    postSites8(options = {}) {
      return request("POST", "/sites", options);
    },

    /** Supprimer un site */
    deleteSitesBySite(options = {}) {
      return request("DELETE", "/sites/{site}", options);
    },

    /** Supprimer un site */
    deleteSitesBySite2(options = {}) {
      return request("DELETE", "/sites/{site}", options);
    },

    /** Supprimer un site */
    deleteSitesBySite3(options = {}) {
      return request("DELETE", "/sites/{site}", options);
    },

    /** Supprimer un site */
    deleteSitesBySite4(options = {}) {
      return request("DELETE", "/sites/{site}", options);
    },

    /** Supprimer un site */
    deleteSitesBySite5(options = {}) {
      return request("DELETE", "/sites/{site}", options);
    },

    /** Supprimer un site */
    deleteSitesBySite6(options = {}) {
      return request("DELETE", "/sites/{site}", options);
    },

    /** Supprimer un site */
    deleteSitesBySite7(options = {}) {
      return request("DELETE", "/sites/{site}", options);
    },

    /** Supprimer un site */
    deleteSitesBySite8(options = {}) {
      return request("DELETE", "/sites/{site}", options);
    },

    /** Voir un site */
    getSitesBySite(options = {}) {
      return request("GET", "/sites/{site}", options);
    },

    /** Voir un site */
    getSitesBySite2(options = {}) {
      return request("GET", "/sites/{site}", options);
    },

    /** Voir un site */
    getSitesBySite3(options = {}) {
      return request("GET", "/sites/{site}", options);
    },

    /** Voir un site */
    getSitesBySite4(options = {}) {
      return request("GET", "/sites/{site}", options);
    },

    /** Voir un site */
    getSitesBySite5(options = {}) {
      return request("GET", "/sites/{site}", options);
    },

    /** Voir un site */
    getSitesBySite6(options = {}) {
      return request("GET", "/sites/{site}", options);
    },

    /** Voir un site */
    getSitesBySite7(options = {}) {
      return request("GET", "/sites/{site}", options);
    },

    /** Voir un site */
    getSitesBySite8(options = {}) {
      return request("GET", "/sites/{site}", options);
    },

    /** Mettre à jour un site (manager) */
    patchSitesBySite(options = {}) {
      return request("PATCH", "/sites/{site}", options);
    },

    /** Mettre à jour un site (manager) */
    patchSitesBySite2(options = {}) {
      return request("PATCH", "/sites/{site}", options);
    },

    /** Mettre à jour un site (manager) */
    patchSitesBySite3(options = {}) {
      return request("PATCH", "/sites/{site}", options);
    },

    /** Mettre à jour un site (manager) */
    patchSitesBySite4(options = {}) {
      return request("PATCH", "/sites/{site}", options);
    },

    /** Mettre à jour un site (manager) */
    patchSitesBySite5(options = {}) {
      return request("PATCH", "/sites/{site}", options);
    },

    /** Mettre à jour un site (manager) */
    patchSitesBySite6(options = {}) {
      return request("PATCH", "/sites/{site}", options);
    },

    /** Mettre à jour un site (manager) */
    patchSitesBySite7(options = {}) {
      return request("PATCH", "/sites/{site}", options);
    },

    /** Mettre à jour un site (manager) */
    patchSitesBySite8(options = {}) {
      return request("PATCH", "/sites/{site}", options);
    },

    /** Modifier un site */
    putSitesBySite(options = {}) {
      return request("PUT", "/sites/{site}", options);
    },

    /** Modifier un site */
    putSitesBySite2(options = {}) {
      return request("PUT", "/sites/{site}", options);
    },

    /** Modifier un site */
    putSitesBySite3(options = {}) {
      return request("PUT", "/sites/{site}", options);
    },

    /** Modifier un site */
    putSitesBySite4(options = {}) {
      return request("PUT", "/sites/{site}", options);
    },

    /** Modifier un site */
    putSitesBySite5(options = {}) {
      return request("PUT", "/sites/{site}", options);
    },

    /** Modifier un site */
    putSitesBySite6(options = {}) {
      return request("PUT", "/sites/{site}", options);
    },

    /** Modifier un site */
    putSitesBySite7(options = {}) {
      return request("PUT", "/sites/{site}", options);
    },

    /** Modifier un site */
    putSitesBySite8(options = {}) {
      return request("PUT", "/sites/{site}", options);
    },

    /** Lire la configuration de mode active pour l'employe connecte */
    getSmartAttendanceConfig(options = {}) {
      return request("GET", "/smart-attendance/config", options);
    },

    /** Lire la configuration de mode active pour l'employe connecte */
    getSmartAttendanceConfig2(options = {}) {
      return request("GET", "/smart-attendance/config", options);
    },

    /** Lire la configuration de mode active pour l'employe connecte */
    getSmartAttendanceConfig3(options = {}) {
      return request("GET", "/smart-attendance/config", options);
    },

    /** Lire la configuration de mode active pour l'employe connecte */
    getSmartAttendanceConfig4(options = {}) {
      return request("GET", "/smart-attendance/config", options);
    },

    /** Lire la configuration de mode active pour l'employe connecte */
    getSmartAttendanceConfig5(options = {}) {
      return request("GET", "/smart-attendance/config", options);
    },

    /** Lire la configuration de mode active pour l'employe connecte */
    getSmartAttendanceConfig6(options = {}) {
      return request("GET", "/smart-attendance/config", options);
    },

    /** Lire la configuration de mode active pour l'employe connecte */
    getSmartAttendanceConfig7(options = {}) {
      return request("GET", "/smart-attendance/config", options);
    },

    /** Lire la configuration de mode active pour l'employe connecte */
    getSmartAttendanceConfig8(options = {}) {
      return request("GET", "/smart-attendance/config", options);
    },

    /** Lire la configuration de mode active pour l'employe connecte */
    getSmartAttendanceConfig9(options = {}) {
      return request("GET", "/smart-attendance/config", options);
    },

    /** Lire la configuration de mode active pour l'employe connecte */
    getSmartAttendanceConfig10(options = {}) {
      return request("GET", "/smart-attendance/config", options);
    },

    /** Lire la configuration de mode active pour l'employe connecte */
    getSmartAttendanceConfig11(options = {}) {
      return request("GET", "/smart-attendance/config", options);
    },

    /** Statistiques du jour — Smart Attendance (manager/RH) */
    getSmartAttendanceDashboard(options = {}) {
      return request("GET", "/smart-attendance/dashboard", options);
    },

    /** Statistiques du jour — Smart Attendance (manager/RH) */
    getSmartAttendanceDashboard2(options = {}) {
      return request("GET", "/smart-attendance/dashboard", options);
    },

    /** Statistiques du jour — Smart Attendance (manager/RH) */
    getSmartAttendanceDashboard3(options = {}) {
      return request("GET", "/smart-attendance/dashboard", options);
    },

    /** Statistiques du jour — Smart Attendance (manager/RH) */
    getSmartAttendanceDashboard4(options = {}) {
      return request("GET", "/smart-attendance/dashboard", options);
    },

    /** Statistiques du jour — Smart Attendance (manager/RH) */
    getSmartAttendanceDashboard5(options = {}) {
      return request("GET", "/smart-attendance/dashboard", options);
    },

    /** Statistiques du jour — Smart Attendance (manager/RH) */
    getSmartAttendanceDashboard6(options = {}) {
      return request("GET", "/smart-attendance/dashboard", options);
    },

    /** Statistiques du jour — Smart Attendance (manager/RH) */
    getSmartAttendanceDashboard7(options = {}) {
      return request("GET", "/smart-attendance/dashboard", options);
    },

    /** Statistiques du jour — Smart Attendance (manager/RH) */
    getSmartAttendanceDashboard8(options = {}) {
      return request("GET", "/smart-attendance/dashboard", options);
    },

    /** Statistiques du jour — Smart Attendance (manager/RH) */
    getSmartAttendanceDashboard9(options = {}) {
      return request("GET", "/smart-attendance/dashboard", options);
    },

    /** Statistiques du jour — Smart Attendance (manager/RH) */
    getSmartAttendanceDashboard10(options = {}) {
      return request("GET", "/smart-attendance/dashboard", options);
    },

    /** Statistiques du jour — Smart Attendance (manager/RH) */
    getSmartAttendanceDashboard11(options = {}) {
      return request("GET", "/smart-attendance/dashboard", options);
    },

    /** Préférence mode géolocalisation d'un employé (manager/RH) */
    getSmartAttendanceEmployeesByEmployeeIdPreference(options = {}) {
      return request("GET", "/smart-attendance/employees/{employeeId}/preference", options);
    },

    /** Préférence mode géolocalisation d'un employé (manager/RH) */
    getSmartAttendanceEmployeesByEmployeeIdPreference2(options = {}) {
      return request("GET", "/smart-attendance/employees/{employeeId}/preference", options);
    },

    /** Préférence mode géolocalisation d'un employé (manager/RH) */
    getSmartAttendanceEmployeesByEmployeeIdPreference3(options = {}) {
      return request("GET", "/smart-attendance/employees/{employeeId}/preference", options);
    },

    /** Préférence mode géolocalisation d'un employé (manager/RH) */
    getSmartAttendanceEmployeesByEmployeeIdPreference4(options = {}) {
      return request("GET", "/smart-attendance/employees/{employeeId}/preference", options);
    },

    /** Préférence mode géolocalisation d'un employé (manager/RH) */
    getSmartAttendanceEmployeesByEmployeeIdPreference5(options = {}) {
      return request("GET", "/smart-attendance/employees/{employeeId}/preference", options);
    },

    /** Préférence mode géolocalisation d'un employé (manager/RH) */
    getSmartAttendanceEmployeesByEmployeeIdPreference6(options = {}) {
      return request("GET", "/smart-attendance/employees/{employeeId}/preference", options);
    },

    /** Préférence mode géolocalisation d'un employé (manager/RH) */
    getSmartAttendanceEmployeesByEmployeeIdPreference7(options = {}) {
      return request("GET", "/smart-attendance/employees/{employeeId}/preference", options);
    },

    /** Préférence mode géolocalisation d'un employé (manager/RH) */
    getSmartAttendanceEmployeesByEmployeeIdPreference8(options = {}) {
      return request("GET", "/smart-attendance/employees/{employeeId}/preference", options);
    },

    /** Préférence mode géolocalisation d'un employé (manager/RH) */
    getSmartAttendanceEmployeesByEmployeeIdPreference9(options = {}) {
      return request("GET", "/smart-attendance/employees/{employeeId}/preference", options);
    },

    /** Préférence mode géolocalisation d'un employé (manager/RH) */
    getSmartAttendanceEmployeesByEmployeeIdPreference10(options = {}) {
      return request("GET", "/smart-attendance/employees/{employeeId}/preference", options);
    },

    /** Préférence mode géolocalisation d'un employé (manager/RH) */
    getSmartAttendanceEmployeesByEmployeeIdPreference11(options = {}) {
      return request("GET", "/smart-attendance/employees/{employeeId}/preference", options);
    },

    /** Envoyer un événement géographique (entrée/sortie de zone) */
    postSmartAttendanceGeoEvents(options = {}) {
      return request("POST", "/smart-attendance/geo-events", options);
    },

    /** Envoyer un événement géographique (entrée/sortie de zone) */
    postSmartAttendanceGeoEvents2(options = {}) {
      return request("POST", "/smart-attendance/geo-events", options);
    },

    /** Envoyer un événement géographique (entrée/sortie de zone) */
    postSmartAttendanceGeoEvents3(options = {}) {
      return request("POST", "/smart-attendance/geo-events", options);
    },

    /** Envoyer un événement géographique (entrée/sortie de zone) */
    postSmartAttendanceGeoEvents4(options = {}) {
      return request("POST", "/smart-attendance/geo-events", options);
    },

    /** Envoyer un événement géographique (entrée/sortie de zone) */
    postSmartAttendanceGeoEvents5(options = {}) {
      return request("POST", "/smart-attendance/geo-events", options);
    },

    /** Envoyer un événement géographique (entrée/sortie de zone) */
    postSmartAttendanceGeoEvents6(options = {}) {
      return request("POST", "/smart-attendance/geo-events", options);
    },

    /** Envoyer un événement géographique (entrée/sortie de zone) */
    postSmartAttendanceGeoEvents7(options = {}) {
      return request("POST", "/smart-attendance/geo-events", options);
    },

    /** Envoyer un événement géographique (entrée/sortie de zone) */
    postSmartAttendanceGeoEvents8(options = {}) {
      return request("POST", "/smart-attendance/geo-events", options);
    },

    /** Envoyer un événement géographique (entrée/sortie de zone) */
    postSmartAttendanceGeoEvents9(options = {}) {
      return request("POST", "/smart-attendance/geo-events", options);
    },

    /** Envoyer un événement géographique (entrée/sortie de zone) */
    postSmartAttendanceGeoEvents10(options = {}) {
      return request("POST", "/smart-attendance/geo-events", options);
    },

    /** Envoyer un événement géographique (entrée/sortie de zone) */
    postSmartAttendanceGeoEvents11(options = {}) {
      return request("POST", "/smart-attendance/geo-events", options);
    },

    /** Parametres du mode de pointage de l'entreprise */
    getSmartAttendanceModeSettings(options = {}) {
      return request("GET", "/smart-attendance/mode-settings", options);
    },

    /** Parametres du mode de pointage de l'entreprise */
    getSmartAttendanceModeSettings2(options = {}) {
      return request("GET", "/smart-attendance/mode-settings", options);
    },

    /** Parametres du mode de pointage de l'entreprise */
    getSmartAttendanceModeSettings3(options = {}) {
      return request("GET", "/smart-attendance/mode-settings", options);
    },

    /** Parametres du mode de pointage de l'entreprise */
    getSmartAttendanceModeSettings4(options = {}) {
      return request("GET", "/smart-attendance/mode-settings", options);
    },

    /** Parametres du mode de pointage de l'entreprise */
    getSmartAttendanceModeSettings5(options = {}) {
      return request("GET", "/smart-attendance/mode-settings", options);
    },

    /** Parametres du mode de pointage de l'entreprise */
    getSmartAttendanceModeSettings6(options = {}) {
      return request("GET", "/smart-attendance/mode-settings", options);
    },

    /** Parametres du mode de pointage de l'entreprise */
    getSmartAttendanceModeSettings7(options = {}) {
      return request("GET", "/smart-attendance/mode-settings", options);
    },

    /** Parametres du mode de pointage de l'entreprise */
    getSmartAttendanceModeSettings8(options = {}) {
      return request("GET", "/smart-attendance/mode-settings", options);
    },

    /** Parametres du mode de pointage de l'entreprise */
    getSmartAttendanceModeSettings9(options = {}) {
      return request("GET", "/smart-attendance/mode-settings", options);
    },

    /** Parametres du mode de pointage de l'entreprise */
    getSmartAttendanceModeSettings10(options = {}) {
      return request("GET", "/smart-attendance/mode-settings", options);
    },

    /** Parametres du mode de pointage de l'entreprise */
    getSmartAttendanceModeSettings11(options = {}) {
      return request("GET", "/smart-attendance/mode-settings", options);
    },

    /** Configurer le mode de pointage (principal) */
    putSmartAttendanceModeSettings(options = {}) {
      return request("PUT", "/smart-attendance/mode-settings", options);
    },

    /** Configurer le mode de pointage (principal) */
    putSmartAttendanceModeSettings2(options = {}) {
      return request("PUT", "/smart-attendance/mode-settings", options);
    },

    /** Configurer le mode de pointage (principal) */
    putSmartAttendanceModeSettings3(options = {}) {
      return request("PUT", "/smart-attendance/mode-settings", options);
    },

    /** Configurer le mode de pointage (principal) */
    putSmartAttendanceModeSettings4(options = {}) {
      return request("PUT", "/smart-attendance/mode-settings", options);
    },

    /** Configurer le mode de pointage (principal) */
    putSmartAttendanceModeSettings5(options = {}) {
      return request("PUT", "/smart-attendance/mode-settings", options);
    },

    /** Configurer le mode de pointage (principal) */
    putSmartAttendanceModeSettings6(options = {}) {
      return request("PUT", "/smart-attendance/mode-settings", options);
    },

    /** Configurer le mode de pointage (principal) */
    putSmartAttendanceModeSettings7(options = {}) {
      return request("PUT", "/smart-attendance/mode-settings", options);
    },

    /** Configurer le mode de pointage (principal) */
    putSmartAttendanceModeSettings8(options = {}) {
      return request("PUT", "/smart-attendance/mode-settings", options);
    },

    /** Configurer le mode de pointage (principal) */
    putSmartAttendanceModeSettings9(options = {}) {
      return request("PUT", "/smart-attendance/mode-settings", options);
    },

    /** Configurer le mode de pointage (principal) */
    putSmartAttendanceModeSettings10(options = {}) {
      return request("PUT", "/smart-attendance/mode-settings", options);
    },

    /** Configurer le mode de pointage (principal) */
    putSmartAttendanceModeSettings11(options = {}) {
      return request("PUT", "/smart-attendance/mode-settings", options);
    },

    /** Sessions GPS de l'employé courant */
    getSmartAttendanceMySessions(options = {}) {
      return request("GET", "/smart-attendance/my-sessions", options);
    },

    /** Sessions GPS de l'employé courant */
    getSmartAttendanceMySessions2(options = {}) {
      return request("GET", "/smart-attendance/my-sessions", options);
    },

    /** Sessions GPS de l'employé courant */
    getSmartAttendanceMySessions3(options = {}) {
      return request("GET", "/smart-attendance/my-sessions", options);
    },

    /** Sessions GPS de l'employé courant */
    getSmartAttendanceMySessions4(options = {}) {
      return request("GET", "/smart-attendance/my-sessions", options);
    },

    /** Sessions GPS de l'employé courant */
    getSmartAttendanceMySessions5(options = {}) {
      return request("GET", "/smart-attendance/my-sessions", options);
    },

    /** Sessions GPS de l'employé courant */
    getSmartAttendanceMySessions6(options = {}) {
      return request("GET", "/smart-attendance/my-sessions", options);
    },

    /** Sessions GPS de l'employé courant */
    getSmartAttendanceMySessions7(options = {}) {
      return request("GET", "/smart-attendance/my-sessions", options);
    },

    /** Sessions GPS de l'employé courant */
    getSmartAttendanceMySessions8(options = {}) {
      return request("GET", "/smart-attendance/my-sessions", options);
    },

    /** Sessions GPS de l'employé courant */
    getSmartAttendanceMySessions9(options = {}) {
      return request("GET", "/smart-attendance/my-sessions", options);
    },

    /** Sessions GPS de l'employé courant */
    getSmartAttendanceMySessions10(options = {}) {
      return request("GET", "/smart-attendance/my-sessions", options);
    },

    /** Sessions GPS de l'employé courant */
    getSmartAttendanceMySessions11(options = {}) {
      return request("GET", "/smart-attendance/my-sessions", options);
    },

    /** Mettre à jour les préférences de pointage */
    putSmartAttendancePreferences(options = {}) {
      return request("PUT", "/smart-attendance/preferences", options);
    },

    /** Mettre à jour les préférences de pointage */
    putSmartAttendancePreferences2(options = {}) {
      return request("PUT", "/smart-attendance/preferences", options);
    },

    /** Mettre à jour les préférences de pointage */
    putSmartAttendancePreferences3(options = {}) {
      return request("PUT", "/smart-attendance/preferences", options);
    },

    /** Mettre à jour les préférences de pointage */
    putSmartAttendancePreferences4(options = {}) {
      return request("PUT", "/smart-attendance/preferences", options);
    },

    /** Mettre à jour les préférences de pointage */
    putSmartAttendancePreferences5(options = {}) {
      return request("PUT", "/smart-attendance/preferences", options);
    },

    /** Mettre à jour les préférences de pointage */
    putSmartAttendancePreferences6(options = {}) {
      return request("PUT", "/smart-attendance/preferences", options);
    },

    /** Mettre à jour les préférences de pointage */
    putSmartAttendancePreferences7(options = {}) {
      return request("PUT", "/smart-attendance/preferences", options);
    },

    /** Mettre à jour les préférences de pointage */
    putSmartAttendancePreferences8(options = {}) {
      return request("PUT", "/smart-attendance/preferences", options);
    },

    /** Mettre à jour les préférences de pointage */
    putSmartAttendancePreferences9(options = {}) {
      return request("PUT", "/smart-attendance/preferences", options);
    },

    /** Mettre à jour les préférences de pointage */
    putSmartAttendancePreferences10(options = {}) {
      return request("PUT", "/smart-attendance/preferences", options);
    },

    /** Mettre à jour les préférences de pointage */
    putSmartAttendancePreferences11(options = {}) {
      return request("PUT", "/smart-attendance/preferences", options);
    },

    /** Lister les sessions GPS */
    getSmartAttendanceSessions(options = {}) {
      return request("GET", "/smart-attendance/sessions", options);
    },

    /** Lister les sessions GPS */
    getSmartAttendanceSessions2(options = {}) {
      return request("GET", "/smart-attendance/sessions", options);
    },

    /** Lister les sessions GPS */
    getSmartAttendanceSessions3(options = {}) {
      return request("GET", "/smart-attendance/sessions", options);
    },

    /** Lister les sessions GPS */
    getSmartAttendanceSessions4(options = {}) {
      return request("GET", "/smart-attendance/sessions", options);
    },

    /** Lister les sessions GPS */
    getSmartAttendanceSessions5(options = {}) {
      return request("GET", "/smart-attendance/sessions", options);
    },

    /** Lister les sessions GPS */
    getSmartAttendanceSessions6(options = {}) {
      return request("GET", "/smart-attendance/sessions", options);
    },

    /** Lister les sessions GPS */
    getSmartAttendanceSessions7(options = {}) {
      return request("GET", "/smart-attendance/sessions", options);
    },

    /** Lister les sessions GPS */
    getSmartAttendanceSessions8(options = {}) {
      return request("GET", "/smart-attendance/sessions", options);
    },

    /** Lister les sessions GPS */
    getSmartAttendanceSessions9(options = {}) {
      return request("GET", "/smart-attendance/sessions", options);
    },

    /** Lister les sessions GPS */
    getSmartAttendanceSessions10(options = {}) {
      return request("GET", "/smart-attendance/sessions", options);
    },

    /** Lister les sessions GPS */
    getSmartAttendanceSessions11(options = {}) {
      return request("GET", "/smart-attendance/sessions", options);
    },

    /** Détail d'une session GPS */
    getSmartAttendanceSessionsById(options = {}) {
      return request("GET", "/smart-attendance/sessions/{id}", options);
    },

    /** Détail d'une session GPS */
    getSmartAttendanceSessionsById2(options = {}) {
      return request("GET", "/smart-attendance/sessions/{id}", options);
    },

    /** Détail d'une session GPS */
    getSmartAttendanceSessionsById3(options = {}) {
      return request("GET", "/smart-attendance/sessions/{id}", options);
    },

    /** Détail d'une session GPS */
    getSmartAttendanceSessionsById4(options = {}) {
      return request("GET", "/smart-attendance/sessions/{id}", options);
    },

    /** Détail d'une session GPS */
    getSmartAttendanceSessionsById5(options = {}) {
      return request("GET", "/smart-attendance/sessions/{id}", options);
    },

    /** Détail d'une session GPS */
    getSmartAttendanceSessionsById6(options = {}) {
      return request("GET", "/smart-attendance/sessions/{id}", options);
    },

    /** Détail d'une session GPS */
    getSmartAttendanceSessionsById7(options = {}) {
      return request("GET", "/smart-attendance/sessions/{id}", options);
    },

    /** Détail d'une session GPS */
    getSmartAttendanceSessionsById8(options = {}) {
      return request("GET", "/smart-attendance/sessions/{id}", options);
    },

    /** Détail d'une session GPS */
    getSmartAttendanceSessionsById9(options = {}) {
      return request("GET", "/smart-attendance/sessions/{id}", options);
    },

    /** Détail d'une session GPS */
    getSmartAttendanceSessionsById10(options = {}) {
      return request("GET", "/smart-attendance/sessions/{id}", options);
    },

    /** Détail d'une session GPS */
    getSmartAttendanceSessionsById11(options = {}) {
      return request("GET", "/smart-attendance/sessions/{id}", options);
    },

    /** Approuver une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdApprove(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/approve", options);
    },

    /** Approuver une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdApprove2(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/approve", options);
    },

    /** Approuver une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdApprove3(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/approve", options);
    },

    /** Approuver une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdApprove4(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/approve", options);
    },

    /** Approuver une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdApprove5(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/approve", options);
    },

    /** Approuver une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdApprove6(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/approve", options);
    },

    /** Approuver une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdApprove7(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/approve", options);
    },

    /** Approuver une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdApprove8(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/approve", options);
    },

    /** Approuver une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdApprove9(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/approve", options);
    },

    /** Approuver une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdApprove10(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/approve", options);
    },

    /** Approuver une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdApprove11(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/approve", options);
    },

    /** Rejeter une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdReject(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/reject", options);
    },

    /** Rejeter une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdReject2(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/reject", options);
    },

    /** Rejeter une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdReject3(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/reject", options);
    },

    /** Rejeter une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdReject4(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/reject", options);
    },

    /** Rejeter une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdReject5(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/reject", options);
    },

    /** Rejeter une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdReject6(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/reject", options);
    },

    /** Rejeter une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdReject7(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/reject", options);
    },

    /** Rejeter une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdReject8(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/reject", options);
    },

    /** Rejeter une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdReject9(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/reject", options);
    },

    /** Rejeter une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdReject10(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/reject", options);
    },

    /** Rejeter une session GPS (manager/RH) */
    postSmartAttendanceSessionsByIdReject11(options = {}) {
      return request("POST", "/smart-attendance/sessions/{id}/reject", options);
    },

    /** Lister les regles de cotisations sociales (manager) */
    listSocialContributions(options = {}) {
      return request("GET", "/social-contributions", options);
    },

    /** Lister les regles de cotisations sociales (manager) */
    listSocialContributions2(options = {}) {
      return request("GET", "/social-contributions", options);
    },

    /** Lister les regles de cotisations sociales (manager) */
    listSocialContributions3(options = {}) {
      return request("GET", "/social-contributions", options);
    },

    /** Lister les regles de cotisations sociales (manager) */
    listSocialContributions4(options = {}) {
      return request("GET", "/social-contributions", options);
    },

    /** Lister les regles de cotisations sociales (manager) */
    listSocialContributions5(options = {}) {
      return request("GET", "/social-contributions", options);
    },

    /** Lister les regles de cotisations sociales (manager) */
    listSocialContributions6(options = {}) {
      return request("GET", "/social-contributions", options);
    },

    /** Lister les regles de cotisations sociales (manager) */
    listSocialContributions7(options = {}) {
      return request("GET", "/social-contributions", options);
    },

    /** Lister les regles de cotisations sociales (manager) */
    listSocialContributions8(options = {}) {
      return request("GET", "/social-contributions", options);
    },

    /** Lister les regles de cotisations sociales (manager) */
    listSocialContributions9(options = {}) {
      return request("GET", "/social-contributions", options);
    },

    /** Lister les regles de cotisations sociales (manager) */
    listSocialContributions10(options = {}) {
      return request("GET", "/social-contributions", options);
    },

    /** Creer une regle de cotisation sociale (manager) */
    createSocialContribution(options = {}) {
      return request("POST", "/social-contributions", options);
    },

    /** Creer une regle de cotisation sociale (manager) */
    createSocialContribution2(options = {}) {
      return request("POST", "/social-contributions", options);
    },

    /** Creer une regle de cotisation sociale (manager) */
    createSocialContribution3(options = {}) {
      return request("POST", "/social-contributions", options);
    },

    /** Creer une regle de cotisation sociale (manager) */
    createSocialContribution4(options = {}) {
      return request("POST", "/social-contributions", options);
    },

    /** Creer une regle de cotisation sociale (manager) */
    createSocialContribution5(options = {}) {
      return request("POST", "/social-contributions", options);
    },

    /** Creer une regle de cotisation sociale (manager) */
    createSocialContribution6(options = {}) {
      return request("POST", "/social-contributions", options);
    },

    /** Creer une regle de cotisation sociale (manager) */
    createSocialContribution7(options = {}) {
      return request("POST", "/social-contributions", options);
    },

    /** Creer une regle de cotisation sociale (manager) */
    createSocialContribution8(options = {}) {
      return request("POST", "/social-contributions", options);
    },

    /** Creer une regle de cotisation sociale (manager) */
    createSocialContribution9(options = {}) {
      return request("POST", "/social-contributions", options);
    },

    /** Creer une regle de cotisation sociale (manager) */
    createSocialContribution10(options = {}) {
      return request("POST", "/social-contributions", options);
    },

    /** Supprimer une regle de cotisation sociale (manager) */
    deleteSocialContribution(options = {}) {
      return request("DELETE", "/social-contributions/{socialContribution}", options);
    },

    /** Supprimer une regle de cotisation sociale (manager) */
    deleteSocialContribution2(options = {}) {
      return request("DELETE", "/social-contributions/{socialContribution}", options);
    },

    /** Supprimer une regle de cotisation sociale (manager) */
    deleteSocialContribution3(options = {}) {
      return request("DELETE", "/social-contributions/{socialContribution}", options);
    },

    /** Supprimer une regle de cotisation sociale (manager) */
    deleteSocialContribution4(options = {}) {
      return request("DELETE", "/social-contributions/{socialContribution}", options);
    },

    /** Supprimer une regle de cotisation sociale (manager) */
    deleteSocialContribution5(options = {}) {
      return request("DELETE", "/social-contributions/{socialContribution}", options);
    },

    /** Supprimer une regle de cotisation sociale (manager) */
    deleteSocialContribution6(options = {}) {
      return request("DELETE", "/social-contributions/{socialContribution}", options);
    },

    /** Supprimer une regle de cotisation sociale (manager) */
    deleteSocialContribution7(options = {}) {
      return request("DELETE", "/social-contributions/{socialContribution}", options);
    },

    /** Supprimer une regle de cotisation sociale (manager) */
    deleteSocialContribution8(options = {}) {
      return request("DELETE", "/social-contributions/{socialContribution}", options);
    },

    /** Supprimer une regle de cotisation sociale (manager) */
    deleteSocialContribution9(options = {}) {
      return request("DELETE", "/social-contributions/{socialContribution}", options);
    },

    /** Supprimer une regle de cotisation sociale (manager) */
    deleteSocialContribution10(options = {}) {
      return request("DELETE", "/social-contributions/{socialContribution}", options);
    },

    /** Modifier une regle de cotisation sociale (manager) */
    updateSocialContribution(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}", options);
    },

    /** Modifier une regle de cotisation sociale (manager) */
    updateSocialContribution2(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}", options);
    },

    /** Modifier une regle de cotisation sociale (manager) */
    updateSocialContribution3(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}", options);
    },

    /** Modifier une regle de cotisation sociale (manager) */
    updateSocialContribution4(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}", options);
    },

    /** Modifier une regle de cotisation sociale (manager) */
    updateSocialContribution5(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}", options);
    },

    /** Modifier une regle de cotisation sociale (manager) */
    updateSocialContribution6(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}", options);
    },

    /** Modifier une regle de cotisation sociale (manager) */
    updateSocialContribution7(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}", options);
    },

    /** Modifier une regle de cotisation sociale (manager) */
    updateSocialContribution8(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}", options);
    },

    /** Modifier une regle de cotisation sociale (manager) */
    updateSocialContribution9(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}", options);
    },

    /** Modifier une regle de cotisation sociale (manager) */
    updateSocialContribution10(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}", options);
    },

    /** Historique immuable des modifications d'une cotisation (manager) — issue #1813 */
    socialContributionHistory(options = {}) {
      return request("GET", "/social-contributions/{socialContribution}/history", options);
    },

    /** Historique immuable des modifications d'une cotisation (manager) — issue #1813 */
    socialContributionHistory2(options = {}) {
      return request("GET", "/social-contributions/{socialContribution}/history", options);
    },

    /** Historique immuable des modifications d'une cotisation (manager) — issue #1813 */
    socialContributionHistory3(options = {}) {
      return request("GET", "/social-contributions/{socialContribution}/history", options);
    },

    /** Historique immuable des modifications d'une cotisation (manager) — issue #1813 */
    socialContributionHistory4(options = {}) {
      return request("GET", "/social-contributions/{socialContribution}/history", options);
    },

    /** Historique immuable des modifications d'une cotisation (manager) — issue #1813 */
    socialContributionHistory5(options = {}) {
      return request("GET", "/social-contributions/{socialContribution}/history", options);
    },

    /** Historique immuable des modifications d'une cotisation (manager) — issue #1813 */
    socialContributionHistory6(options = {}) {
      return request("GET", "/social-contributions/{socialContribution}/history", options);
    },

    /** Historique immuable des modifications d'une cotisation (manager) — issue #1813 */
    socialContributionHistory7(options = {}) {
      return request("GET", "/social-contributions/{socialContribution}/history", options);
    },

    /** Historique immuable des modifications d'une cotisation (manager) — issue #1813 */
    socialContributionHistory8(options = {}) {
      return request("GET", "/social-contributions/{socialContribution}/history", options);
    },

    /** Historique immuable des modifications d'une cotisation (manager) — issue #1813 */
    socialContributionHistory9(options = {}) {
      return request("GET", "/social-contributions/{socialContribution}/history", options);
    },

    /** Historique immuable des modifications d'une cotisation (manager) — issue #1813 */
    socialContributionHistory10(options = {}) {
      return request("GET", "/social-contributions/{socialContribution}/history", options);
    },

    /** Soumettre une regle de cotisation pour validation (manager) — issue #1813 */
    submitSocialContribution(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}/submit", options);
    },

    /** Soumettre une regle de cotisation pour validation (manager) — issue #1813 */
    submitSocialContribution2(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}/submit", options);
    },

    /** Soumettre une regle de cotisation pour validation (manager) — issue #1813 */
    submitSocialContribution3(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}/submit", options);
    },

    /** Soumettre une regle de cotisation pour validation (manager) — issue #1813 */
    submitSocialContribution4(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}/submit", options);
    },

    /** Soumettre une regle de cotisation pour validation (manager) — issue #1813 */
    submitSocialContribution5(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}/submit", options);
    },

    /** Soumettre une regle de cotisation pour validation (manager) — issue #1813 */
    submitSocialContribution6(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}/submit", options);
    },

    /** Soumettre une regle de cotisation pour validation (manager) — issue #1813 */
    submitSocialContribution7(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}/submit", options);
    },

    /** Soumettre une regle de cotisation pour validation (manager) — issue #1813 */
    submitSocialContribution8(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}/submit", options);
    },

    /** Soumettre une regle de cotisation pour validation (manager) — issue #1813 */
    submitSocialContribution9(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}/submit", options);
    },

    /** Soumettre une regle de cotisation pour validation (manager) — issue #1813 */
    submitSocialContribution10(options = {}) {
      return request("PUT", "/social-contributions/{socialContribution}/submit", options);
    },

    /** Generer la declaration trimestrielle CNAS (Algerie) pour tous les employes actifs */
    generateCnasDzDeclaration(options = {}) {
      return request("POST", "/social-declarations/cnas-dz", options);
    },

    /** Generer la declaration trimestrielle CNAS (Algerie) pour tous les employes actifs */
    generateCnasDzDeclaration2(options = {}) {
      return request("POST", "/social-declarations/cnas-dz", options);
    },

    /** Generer la declaration trimestrielle CNAS (Algerie) pour tous les employes actifs */
    generateCnasDzDeclaration3(options = {}) {
      return request("POST", "/social-declarations/cnas-dz", options);
    },

    /** Generer la declaration trimestrielle CNAS (Algerie) pour tous les employes actifs */
    generateCnasDzDeclaration4(options = {}) {
      return request("POST", "/social-declarations/cnas-dz", options);
    },

    /** Generer la declaration trimestrielle CNAS (Algerie) pour tous les employes actifs */
    generateCnasDzDeclaration5(options = {}) {
      return request("POST", "/social-declarations/cnas-dz", options);
    },

    /** Generer la declaration trimestrielle CNAS (Algerie) pour tous les employes actifs */
    generateCnasDzDeclaration6(options = {}) {
      return request("POST", "/social-declarations/cnas-dz", options);
    },

    /** Generer la declaration trimestrielle CNAS (Algerie) pour tous les employes actifs */
    generateCnasDzDeclaration7(options = {}) {
      return request("POST", "/social-declarations/cnas-dz", options);
    },

    /** Generer la declaration trimestrielle CNAS (Algerie) pour tous les employes actifs */
    generateCnasDzDeclaration8(options = {}) {
      return request("POST", "/social-declarations/cnas-dz", options);
    },

    /** Generer la declaration trimestrielle CNAS (Algerie) pour tous les employes actifs */
    generateCnasDzDeclaration9(options = {}) {
      return request("POST", "/social-declarations/cnas-dz", options);
    },

    /** Generer la declaration trimestrielle CNAS (Algerie) pour tous les employes actifs */
    generateCnasDzDeclaration10(options = {}) {
      return request("POST", "/social-declarations/cnas-dz", options);
    },

    /** Generer la declaration trimestrielle CNSS (Maroc) pour tous les employes actifs */
    generateCnssMaDeclaration(options = {}) {
      return request("POST", "/social-declarations/cnss-ma", options);
    },

    /** Generer la declaration trimestrielle CNSS (Maroc) pour tous les employes actifs */
    generateCnssMaDeclaration2(options = {}) {
      return request("POST", "/social-declarations/cnss-ma", options);
    },

    /** Generer la declaration trimestrielle CNSS (Maroc) pour tous les employes actifs */
    generateCnssMaDeclaration3(options = {}) {
      return request("POST", "/social-declarations/cnss-ma", options);
    },

    /** Generer la declaration trimestrielle CNSS (Maroc) pour tous les employes actifs */
    generateCnssMaDeclaration4(options = {}) {
      return request("POST", "/social-declarations/cnss-ma", options);
    },

    /** Generer la declaration trimestrielle CNSS (Maroc) pour tous les employes actifs */
    generateCnssMaDeclaration5(options = {}) {
      return request("POST", "/social-declarations/cnss-ma", options);
    },

    /** Generer la declaration trimestrielle CNSS (Maroc) pour tous les employes actifs */
    generateCnssMaDeclaration6(options = {}) {
      return request("POST", "/social-declarations/cnss-ma", options);
    },

    /** Generer la declaration trimestrielle CNSS (Maroc) pour tous les employes actifs */
    generateCnssMaDeclaration7(options = {}) {
      return request("POST", "/social-declarations/cnss-ma", options);
    },

    /** Generer la declaration trimestrielle CNSS (Maroc) pour tous les employes actifs */
    generateCnssMaDeclaration8(options = {}) {
      return request("POST", "/social-declarations/cnss-ma", options);
    },

    /** Generer la declaration trimestrielle CNSS (Maroc) pour tous les employes actifs */
    generateCnssMaDeclaration9(options = {}) {
      return request("POST", "/social-declarations/cnss-ma", options);
    },

    /** Generer la declaration trimestrielle CNSS (Maroc) pour tous les employes actifs */
    generateCnssMaDeclaration10(options = {}) {
      return request("POST", "/social-declarations/cnss-ma", options);
    },

    /** Generer la declaration annuelle des salaires DAS (Algerie, #5243) */
    generateDasDzDeclaration(options = {}) {
      return request("POST", "/social-declarations/das-dz", options);
    },

    /** Generer la declaration annuelle des salaires DAS (Algerie, #5243) */
    generateDasDzDeclaration2(options = {}) {
      return request("POST", "/social-declarations/das-dz", options);
    },

    /** Generer la declaration annuelle des salaires DAS (Algerie, #5243) */
    generateDasDzDeclaration3(options = {}) {
      return request("POST", "/social-declarations/das-dz", options);
    },

    /** Generer la declaration annuelle des salaires DAS (Algerie, #5243) */
    generateDasDzDeclaration4(options = {}) {
      return request("POST", "/social-declarations/das-dz", options);
    },

    /** Generer la declaration annuelle des salaires DAS (Algerie, #5243) */
    generateDasDzDeclaration5(options = {}) {
      return request("POST", "/social-declarations/das-dz", options);
    },

    /** Generer la declaration annuelle des salaires DAS (Algerie, #5243) */
    generateDasDzDeclaration6(options = {}) {
      return request("POST", "/social-declarations/das-dz", options);
    },

    /** Generer la DSN (Declaration Sociale Nominative, France) pour un mois donne */
    generateDsnFrDeclaration(options = {}) {
      return request("POST", "/social-declarations/dsn-fr", options);
    },

    /** Generer la DSN (Declaration Sociale Nominative, France) pour un mois donne */
    generateDsnFrDeclaration2(options = {}) {
      return request("POST", "/social-declarations/dsn-fr", options);
    },

    /** Generer la DSN (Declaration Sociale Nominative, France) pour un mois donne */
    generateDsnFrDeclaration3(options = {}) {
      return request("POST", "/social-declarations/dsn-fr", options);
    },

    /** Generer la DSN (Declaration Sociale Nominative, France) pour un mois donne */
    generateDsnFrDeclaration4(options = {}) {
      return request("POST", "/social-declarations/dsn-fr", options);
    },

    /** Generer la DSN (Declaration Sociale Nominative, France) pour un mois donne */
    generateDsnFrDeclaration5(options = {}) {
      return request("POST", "/social-declarations/dsn-fr", options);
    },

    /** Generer la DSN (Declaration Sociale Nominative, France) pour un mois donne */
    generateDsnFrDeclaration6(options = {}) {
      return request("POST", "/social-declarations/dsn-fr", options);
    },

    /** Generer la DSN (Declaration Sociale Nominative, France) pour un mois donne */
    generateDsnFrDeclaration7(options = {}) {
      return request("POST", "/social-declarations/dsn-fr", options);
    },

    /** Generer la DSN (Declaration Sociale Nominative, France) pour un mois donne */
    generateDsnFrDeclaration8(options = {}) {
      return request("POST", "/social-declarations/dsn-fr", options);
    },

    /** Generer la DSN (Declaration Sociale Nominative, France) pour un mois donne */
    generateDsnFrDeclaration9(options = {}) {
      return request("POST", "/social-declarations/dsn-fr", options);
    },

    /** Generer la DSN (Declaration Sociale Nominative, France) pour un mois donne */
    generateDsnFrDeclaration10(options = {}) {
      return request("POST", "/social-declarations/dsn-fr", options);
    },

    /** Configurer le SSO de l'entreprise */
    postSsoConfigure(options = {}) {
      return request("POST", "/sso/configure", options);
    },

    /** Configurer le SSO de l'entreprise */
    postSsoConfigure2(options = {}) {
      return request("POST", "/sso/configure", options);
    },

    /** Configurer le SSO de l'entreprise */
    postSsoConfigure3(options = {}) {
      return request("POST", "/sso/configure", options);
    },

    /** Configurer le SSO de l'entreprise */
    postSsoConfigure4(options = {}) {
      return request("POST", "/sso/configure", options);
    },

    /** Configurer le SSO de l'entreprise */
    postSsoConfigure5(options = {}) {
      return request("POST", "/sso/configure", options);
    },

    /** Configurer le SSO de l'entreprise */
    postSsoConfigure6(options = {}) {
      return request("POST", "/sso/configure", options);
    },

    /** Configurer le SSO de l'entreprise */
    postSsoConfigure7(options = {}) {
      return request("POST", "/sso/configure", options);
    },

    /** Configurer le SSO de l'entreprise */
    postSsoConfigure8(options = {}) {
      return request("POST", "/sso/configure", options);
    },

    /** Configurer le SSO de l'entreprise */
    postSsoConfigure9(options = {}) {
      return request("POST", "/sso/configure", options);
    },

    /** Configurer le SSO de l'entreprise */
    postSsoConfigure10(options = {}) {
      return request("POST", "/sso/configure", options);
    },

    /** Configurer le SSO de l'entreprise */
    postSsoConfigure11(options = {}) {
      return request("POST", "/sso/configure", options);
    },

    /** Desactiver le SSO de l'entreprise */
    deleteSsoDisable(options = {}) {
      return request("DELETE", "/sso/disable", options);
    },

    /** Desactiver le SSO de l'entreprise */
    deleteSsoDisable2(options = {}) {
      return request("DELETE", "/sso/disable", options);
    },

    /** Desactiver le SSO de l'entreprise */
    deleteSsoDisable3(options = {}) {
      return request("DELETE", "/sso/disable", options);
    },

    /** Desactiver le SSO de l'entreprise */
    deleteSsoDisable4(options = {}) {
      return request("DELETE", "/sso/disable", options);
    },

    /** Desactiver le SSO de l'entreprise */
    deleteSsoDisable5(options = {}) {
      return request("DELETE", "/sso/disable", options);
    },

    /** Desactiver le SSO de l'entreprise */
    deleteSsoDisable6(options = {}) {
      return request("DELETE", "/sso/disable", options);
    },

    /** Desactiver le SSO de l'entreprise */
    deleteSsoDisable7(options = {}) {
      return request("DELETE", "/sso/disable", options);
    },

    /** Desactiver le SSO de l'entreprise */
    deleteSsoDisable8(options = {}) {
      return request("DELETE", "/sso/disable", options);
    },

    /** Desactiver le SSO de l'entreprise */
    deleteSsoDisable9(options = {}) {
      return request("DELETE", "/sso/disable", options);
    },

    /** Desactiver le SSO de l'entreprise */
    deleteSsoDisable10(options = {}) {
      return request("DELETE", "/sso/disable", options);
    },

    /** Desactiver le SSO de l'entreprise */
    deleteSsoDisable11(options = {}) {
      return request("DELETE", "/sso/disable", options);
    },

    /** Demarrer le flux OpenID Connect (SP -> IdP) */
    getSsoOidcByCompanyIdAuthorize(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/authorize", options);
    },

    /** Demarrer le flux OpenID Connect (SP -> IdP) */
    getSsoOidcByCompanyIdAuthorize2(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/authorize", options);
    },

    /** Demarrer le flux OpenID Connect (SP -> IdP) */
    getSsoOidcByCompanyIdAuthorize3(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/authorize", options);
    },

    /** Demarrer le flux OpenID Connect (SP -> IdP) */
    getSsoOidcByCompanyIdAuthorize4(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/authorize", options);
    },

    /** Demarrer le flux OpenID Connect (SP -> IdP) */
    getSsoOidcByCompanyIdAuthorize5(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/authorize", options);
    },

    /** Demarrer le flux OpenID Connect (SP -> IdP) */
    getSsoOidcByCompanyIdAuthorize6(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/authorize", options);
    },

    /** Demarrer le flux OpenID Connect (SP -> IdP) */
    getSsoOidcByCompanyIdAuthorize7(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/authorize", options);
    },

    /** Demarrer le flux OpenID Connect (SP -> IdP) */
    getSsoOidcByCompanyIdAuthorize8(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/authorize", options);
    },

    /** Demarrer le flux OpenID Connect (SP -> IdP) */
    getSsoOidcByCompanyIdAuthorize9(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/authorize", options);
    },

    /** Demarrer le flux OpenID Connect (SP -> IdP) */
    getSsoOidcByCompanyIdAuthorize10(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/authorize", options);
    },

    /** Demarrer le flux OpenID Connect (SP -> IdP) */
    getSsoOidcByCompanyIdAuthorize11(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/authorize", options);
    },

    /** Callback OpenID Connect (IdP -> SP) */
    getSsoOidcByCompanyIdCallback(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/callback", options);
    },

    /** Callback OpenID Connect (IdP -> SP) */
    getSsoOidcByCompanyIdCallback2(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/callback", options);
    },

    /** Callback OpenID Connect (IdP -> SP) */
    getSsoOidcByCompanyIdCallback3(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/callback", options);
    },

    /** Callback OpenID Connect (IdP -> SP) */
    getSsoOidcByCompanyIdCallback4(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/callback", options);
    },

    /** Callback OpenID Connect (IdP -> SP) */
    getSsoOidcByCompanyIdCallback5(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/callback", options);
    },

    /** Callback OpenID Connect (IdP -> SP) */
    getSsoOidcByCompanyIdCallback6(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/callback", options);
    },

    /** Callback OpenID Connect (IdP -> SP) */
    getSsoOidcByCompanyIdCallback7(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/callback", options);
    },

    /** Callback OpenID Connect (IdP -> SP) */
    getSsoOidcByCompanyIdCallback8(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/callback", options);
    },

    /** Callback OpenID Connect (IdP -> SP) */
    getSsoOidcByCompanyIdCallback9(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/callback", options);
    },

    /** Callback OpenID Connect (IdP -> SP) */
    getSsoOidcByCompanyIdCallback10(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/callback", options);
    },

    /** Callback OpenID Connect (IdP -> SP) */
    getSsoOidcByCompanyIdCallback11(options = {}) {
      return request("GET", "/sso/oidc/{companyId}/callback", options);
    },

    /** Lister les fournisseurs SSO supportes */
    getSsoProviders(options = {}) {
      return request("GET", "/sso/providers", options);
    },

    /** Lister les fournisseurs SSO supportes */
    getSsoProviders2(options = {}) {
      return request("GET", "/sso/providers", options);
    },

    /** Lister les fournisseurs SSO supportes */
    getSsoProviders3(options = {}) {
      return request("GET", "/sso/providers", options);
    },

    /** Lister les fournisseurs SSO supportes */
    getSsoProviders4(options = {}) {
      return request("GET", "/sso/providers", options);
    },

    /** Lister les fournisseurs SSO supportes */
    getSsoProviders5(options = {}) {
      return request("GET", "/sso/providers", options);
    },

    /** Lister les fournisseurs SSO supportes */
    getSsoProviders6(options = {}) {
      return request("GET", "/sso/providers", options);
    },

    /** Lister les fournisseurs SSO supportes */
    getSsoProviders7(options = {}) {
      return request("GET", "/sso/providers", options);
    },

    /** Lister les fournisseurs SSO supportes */
    getSsoProviders8(options = {}) {
      return request("GET", "/sso/providers", options);
    },

    /** Lister les fournisseurs SSO supportes */
    getSsoProviders9(options = {}) {
      return request("GET", "/sso/providers", options);
    },

    /** Lister les fournisseurs SSO supportes */
    getSsoProviders10(options = {}) {
      return request("GET", "/sso/providers", options);
    },

    /** Lister les fournisseurs SSO supportes */
    getSsoProviders11(options = {}) {
      return request("GET", "/sso/providers", options);
    },

    /** Callback SAML 2.0 (IdP -> SP) */
    postSsoSamlByCompanyIdCallback(options = {}) {
      return request("POST", "/sso/saml/{companyId}/callback", options);
    },

    /** Callback SAML 2.0 (IdP -> SP) */
    postSsoSamlByCompanyIdCallback2(options = {}) {
      return request("POST", "/sso/saml/{companyId}/callback", options);
    },

    /** Callback SAML 2.0 (IdP -> SP) */
    postSsoSamlByCompanyIdCallback3(options = {}) {
      return request("POST", "/sso/saml/{companyId}/callback", options);
    },

    /** Callback SAML 2.0 (IdP -> SP) */
    postSsoSamlByCompanyIdCallback4(options = {}) {
      return request("POST", "/sso/saml/{companyId}/callback", options);
    },

    /** Callback SAML 2.0 (IdP -> SP) */
    postSsoSamlByCompanyIdCallback5(options = {}) {
      return request("POST", "/sso/saml/{companyId}/callback", options);
    },

    /** Callback SAML 2.0 (IdP -> SP) */
    postSsoSamlByCompanyIdCallback6(options = {}) {
      return request("POST", "/sso/saml/{companyId}/callback", options);
    },

    /** Callback SAML 2.0 (IdP -> SP) */
    postSsoSamlByCompanyIdCallback7(options = {}) {
      return request("POST", "/sso/saml/{companyId}/callback", options);
    },

    /** Callback SAML 2.0 (IdP -> SP) */
    postSsoSamlByCompanyIdCallback8(options = {}) {
      return request("POST", "/sso/saml/{companyId}/callback", options);
    },

    /** Callback SAML 2.0 (IdP -> SP) */
    postSsoSamlByCompanyIdCallback9(options = {}) {
      return request("POST", "/sso/saml/{companyId}/callback", options);
    },

    /** Callback SAML 2.0 (IdP -> SP) */
    postSsoSamlByCompanyIdCallback10(options = {}) {
      return request("POST", "/sso/saml/{companyId}/callback", options);
    },

    /** Callback SAML 2.0 (IdP -> SP) */
    postSsoSamlByCompanyIdCallback11(options = {}) {
      return request("POST", "/sso/saml/{companyId}/callback", options);
    },

    /** Statut SSO de l'entreprise */
    getSsoStatus(options = {}) {
      return request("GET", "/sso/status", options);
    },

    /** Statut SSO de l'entreprise */
    getSsoStatus2(options = {}) {
      return request("GET", "/sso/status", options);
    },

    /** Statut SSO de l'entreprise */
    getSsoStatus3(options = {}) {
      return request("GET", "/sso/status", options);
    },

    /** Statut SSO de l'entreprise */
    getSsoStatus4(options = {}) {
      return request("GET", "/sso/status", options);
    },

    /** Statut SSO de l'entreprise */
    getSsoStatus5(options = {}) {
      return request("GET", "/sso/status", options);
    },

    /** Statut SSO de l'entreprise */
    getSsoStatus6(options = {}) {
      return request("GET", "/sso/status", options);
    },

    /** Statut SSO de l'entreprise */
    getSsoStatus7(options = {}) {
      return request("GET", "/sso/status", options);
    },

    /** Statut SSO de l'entreprise */
    getSsoStatus8(options = {}) {
      return request("GET", "/sso/status", options);
    },

    /** Statut SSO de l'entreprise */
    getSsoStatus9(options = {}) {
      return request("GET", "/sso/status", options);
    },

    /** Statut SSO de l'entreprise */
    getSsoStatus10(options = {}) {
      return request("GET", "/sso/status", options);
    },

    /** Statut SSO de l'entreprise */
    getSsoStatus11(options = {}) {
      return request("GET", "/sso/status", options);
    },

    /** Lister les tickets de support ouverts par le tenant */
    getSupportTickets(options = {}) {
      return request("GET", "/support-tickets", options);
    },

    /** Lister les tickets de support ouverts par le tenant */
    getSupportTickets2(options = {}) {
      return request("GET", "/support-tickets", options);
    },

    /** Lister les tickets de support ouverts par le tenant */
    getSupportTickets3(options = {}) {
      return request("GET", "/support-tickets", options);
    },

    /** Lister les tickets de support ouverts par le tenant */
    getSupportTickets4(options = {}) {
      return request("GET", "/support-tickets", options);
    },

    /** Lister les tickets de support ouverts par le tenant */
    getSupportTickets5(options = {}) {
      return request("GET", "/support-tickets", options);
    },

    /** Lister les tickets de support ouverts par le tenant */
    getSupportTickets6(options = {}) {
      return request("GET", "/support-tickets", options);
    },

    /** Lister les tickets de support ouverts par le tenant */
    getSupportTickets7(options = {}) {
      return request("GET", "/support-tickets", options);
    },

    /** Lister les tickets de support ouverts par le tenant */
    getSupportTickets8(options = {}) {
      return request("GET", "/support-tickets", options);
    },

    /** Ouvrir un ticket de support (employe/manager) */
    postSupportTickets(options = {}) {
      return request("POST", "/support-tickets", options);
    },

    /** Ouvrir un ticket de support (employe/manager) */
    postSupportTickets2(options = {}) {
      return request("POST", "/support-tickets", options);
    },

    /** Ouvrir un ticket de support (employe/manager) */
    postSupportTickets3(options = {}) {
      return request("POST", "/support-tickets", options);
    },

    /** Ouvrir un ticket de support (employe/manager) */
    postSupportTickets4(options = {}) {
      return request("POST", "/support-tickets", options);
    },

    /** Ouvrir un ticket de support (employe/manager) */
    postSupportTickets5(options = {}) {
      return request("POST", "/support-tickets", options);
    },

    /** Ouvrir un ticket de support (employe/manager) */
    postSupportTickets6(options = {}) {
      return request("POST", "/support-tickets", options);
    },

    /** Ouvrir un ticket de support (employe/manager) */
    postSupportTickets7(options = {}) {
      return request("POST", "/support-tickets", options);
    },

    /** Ouvrir un ticket de support (employe/manager) */
    postSupportTickets8(options = {}) {
      return request("POST", "/support-tickets", options);
    },

    /** Detail d'un ticket de support (tenant) */
    getSupportTicketsBySupportTicket(options = {}) {
      return request("GET", "/support-tickets/{supportTicket}", options);
    },

    /** Detail d'un ticket de support (tenant) */
    getSupportTicketsBySupportTicket2(options = {}) {
      return request("GET", "/support-tickets/{supportTicket}", options);
    },

    /** Detail d'un ticket de support (tenant) */
    getSupportTicketsBySupportTicket3(options = {}) {
      return request("GET", "/support-tickets/{supportTicket}", options);
    },

    /** Detail d'un ticket de support (tenant) */
    getSupportTicketsBySupportTicket4(options = {}) {
      return request("GET", "/support-tickets/{supportTicket}", options);
    },

    /** Detail d'un ticket de support (tenant) */
    getSupportTicketsBySupportTicket5(options = {}) {
      return request("GET", "/support-tickets/{supportTicket}", options);
    },

    /** Detail d'un ticket de support (tenant) */
    getSupportTicketsBySupportTicket6(options = {}) {
      return request("GET", "/support-tickets/{supportTicket}", options);
    },

    /** Detail d'un ticket de support (tenant) */
    getSupportTicketsBySupportTicket7(options = {}) {
      return request("GET", "/support-tickets/{supportTicket}", options);
    },

    /** Detail d'un ticket de support (tenant) */
    getSupportTicketsBySupportTicket8(options = {}) {
      return request("GET", "/support-tickets/{supportTicket}", options);
    },

    /** Cloturer un ticket de support (auteur ou manager tenant) */
    postSupportTicketsBySupportTicketClose(options = {}) {
      return request("POST", "/support-tickets/{supportTicket}/close", options);
    },

    /** Cloturer un ticket de support (auteur ou manager tenant) */
    postSupportTicketsBySupportTicketClose2(options = {}) {
      return request("POST", "/support-tickets/{supportTicket}/close", options);
    },

    /** Cloturer un ticket de support (auteur ou manager tenant) */
    postSupportTicketsBySupportTicketClose3(options = {}) {
      return request("POST", "/support-tickets/{supportTicket}/close", options);
    },

    /** Cloturer un ticket de support (auteur ou manager tenant) */
    postSupportTicketsBySupportTicketClose4(options = {}) {
      return request("POST", "/support-tickets/{supportTicket}/close", options);
    },

    /** Cloturer un ticket de support (auteur ou manager tenant) */
    postSupportTicketsBySupportTicketClose5(options = {}) {
      return request("POST", "/support-tickets/{supportTicket}/close", options);
    },

    /** Cloturer un ticket de support (auteur ou manager tenant) */
    postSupportTicketsBySupportTicketClose6(options = {}) {
      return request("POST", "/support-tickets/{supportTicket}/close", options);
    },

    /** Cloturer un ticket de support (auteur ou manager tenant) */
    postSupportTicketsBySupportTicketClose7(options = {}) {
      return request("POST", "/support-tickets/{supportTicket}/close", options);
    },

    /** Cloturer un ticket de support (auteur ou manager tenant) */
    postSupportTicketsBySupportTicketClose8(options = {}) {
      return request("POST", "/support-tickets/{supportTicket}/close", options);
    },

    /** Repondre a un ticket de support (tenant) */
    postSupportTicketsBySupportTicketReply(options = {}) {
      return request("POST", "/support-tickets/{supportTicket}/reply", options);
    },

    /** Repondre a un ticket de support (tenant) */
    postSupportTicketsBySupportTicketReply2(options = {}) {
      return request("POST", "/support-tickets/{supportTicket}/reply", options);
    },

    /** Repondre a un ticket de support (tenant) */
    postSupportTicketsBySupportTicketReply3(options = {}) {
      return request("POST", "/support-tickets/{supportTicket}/reply", options);
    },

    /** Repondre a un ticket de support (tenant) */
    postSupportTicketsBySupportTicketReply4(options = {}) {
      return request("POST", "/support-tickets/{supportTicket}/reply", options);
    },

    /** Repondre a un ticket de support (tenant) */
    postSupportTicketsBySupportTicketReply5(options = {}) {
      return request("POST", "/support-tickets/{supportTicket}/reply", options);
    },

    /** Repondre a un ticket de support (tenant) */
    postSupportTicketsBySupportTicketReply6(options = {}) {
      return request("POST", "/support-tickets/{supportTicket}/reply", options);
    },

    /** Repondre a un ticket de support (tenant) */
    postSupportTicketsBySupportTicketReply7(options = {}) {
      return request("POST", "/support-tickets/{supportTicket}/reply", options);
    },

    /** Repondre a un ticket de support (tenant) */
    postSupportTicketsBySupportTicketReply8(options = {}) {
      return request("POST", "/support-tickets/{supportTicket}/reply", options);
    },

    /** Registre des pays supportes */
    listSupportedCountries(options = {}) {
      return request("GET", "/supported-countries", options);
    },

    /** Registre des pays supportes */
    listSupportedCountries2(options = {}) {
      return request("GET", "/supported-countries", options);
    },

    /** Registre des pays supportes */
    listSupportedCountries3(options = {}) {
      return request("GET", "/supported-countries", options);
    },

    /** Registre des pays supportes */
    listSupportedCountries4(options = {}) {
      return request("GET", "/supported-countries", options);
    },

    /** Registre des pays supportes */
    listSupportedCountries5(options = {}) {
      return request("GET", "/supported-countries", options);
    },

    /** Registre des pays supportes */
    listSupportedCountries6(options = {}) {
      return request("GET", "/supported-countries", options);
    },

    /** Registre des pays supportes */
    listSupportedCountries7(options = {}) {
      return request("GET", "/supported-countries", options);
    },

    /** Registre des pays supportes */
    listSupportedCountries8(options = {}) {
      return request("GET", "/supported-countries", options);
    },

    /** Registre des pays supportes */
    listSupportedCountries9(options = {}) {
      return request("GET", "/supported-countries", options);
    },

    /** Registre des pays supportes */
    listSupportedCountries10(options = {}) {
      return request("GET", "/supported-countries", options);
    },

    /** Lister les taches */
    getTasks(options = {}) {
      return request("GET", "/tasks", options);
    },

    /** Lister les taches */
    getTasks2(options = {}) {
      return request("GET", "/tasks", options);
    },

    /** Lister les taches */
    getTasks3(options = {}) {
      return request("GET", "/tasks", options);
    },

    /** Lister les taches */
    getTasks4(options = {}) {
      return request("GET", "/tasks", options);
    },

    /** Lister les taches */
    getTasks5(options = {}) {
      return request("GET", "/tasks", options);
    },

    /** Lister les taches */
    getTasks6(options = {}) {
      return request("GET", "/tasks", options);
    },

    /** Lister les taches */
    getTasks7(options = {}) {
      return request("GET", "/tasks", options);
    },

    /** Lister les taches */
    getTasks8(options = {}) {
      return request("GET", "/tasks", options);
    },

    /** Creer une tache */
    postTasks(options = {}) {
      return request("POST", "/tasks", options);
    },

    /** Creer une tache */
    postTasks2(options = {}) {
      return request("POST", "/tasks", options);
    },

    /** Creer une tache */
    postTasks3(options = {}) {
      return request("POST", "/tasks", options);
    },

    /** Creer une tache */
    postTasks4(options = {}) {
      return request("POST", "/tasks", options);
    },

    /** Creer une tache */
    postTasks5(options = {}) {
      return request("POST", "/tasks", options);
    },

    /** Creer une tache */
    postTasks6(options = {}) {
      return request("POST", "/tasks", options);
    },

    /** Creer une tache */
    postTasks7(options = {}) {
      return request("POST", "/tasks", options);
    },

    /** Creer une tache */
    postTasks8(options = {}) {
      return request("POST", "/tasks", options);
    },

    /** Supprimer une tache */
    deleteTasksByTask(options = {}) {
      return request("DELETE", "/tasks/{task}", options);
    },

    /** Supprimer une tache */
    deleteTasksByTask2(options = {}) {
      return request("DELETE", "/tasks/{task}", options);
    },

    /** Supprimer une tache */
    deleteTasksByTask3(options = {}) {
      return request("DELETE", "/tasks/{task}", options);
    },

    /** Supprimer une tache */
    deleteTasksByTask4(options = {}) {
      return request("DELETE", "/tasks/{task}", options);
    },

    /** Supprimer une tache */
    deleteTasksByTask5(options = {}) {
      return request("DELETE", "/tasks/{task}", options);
    },

    /** Supprimer une tache */
    deleteTasksByTask6(options = {}) {
      return request("DELETE", "/tasks/{task}", options);
    },

    /** Supprimer une tache */
    deleteTasksByTask7(options = {}) {
      return request("DELETE", "/tasks/{task}", options);
    },

    /** Supprimer une tache */
    deleteTasksByTask8(options = {}) {
      return request("DELETE", "/tasks/{task}", options);
    },

    /** Voir une tache */
    getTasksByTask(options = {}) {
      return request("GET", "/tasks/{task}", options);
    },

    /** Voir une tache */
    getTasksByTask2(options = {}) {
      return request("GET", "/tasks/{task}", options);
    },

    /** Voir une tache */
    getTasksByTask3(options = {}) {
      return request("GET", "/tasks/{task}", options);
    },

    /** Voir une tache */
    getTasksByTask4(options = {}) {
      return request("GET", "/tasks/{task}", options);
    },

    /** Voir une tache */
    getTasksByTask5(options = {}) {
      return request("GET", "/tasks/{task}", options);
    },

    /** Voir une tache */
    getTasksByTask6(options = {}) {
      return request("GET", "/tasks/{task}", options);
    },

    /** Voir une tache */
    getTasksByTask7(options = {}) {
      return request("GET", "/tasks/{task}", options);
    },

    /** Voir une tache */
    getTasksByTask8(options = {}) {
      return request("GET", "/tasks/{task}", options);
    },

    /** Mettre a jour partiellement une tache */
    patchTasksByTask(options = {}) {
      return request("PATCH", "/tasks/{task}", options);
    },

    /** Mettre a jour partiellement une tache */
    patchTasksByTask2(options = {}) {
      return request("PATCH", "/tasks/{task}", options);
    },

    /** Mettre a jour partiellement une tache */
    patchTasksByTask3(options = {}) {
      return request("PATCH", "/tasks/{task}", options);
    },

    /** Mettre a jour partiellement une tache */
    patchTasksByTask4(options = {}) {
      return request("PATCH", "/tasks/{task}", options);
    },

    /** Mettre a jour partiellement une tache */
    patchTasksByTask5(options = {}) {
      return request("PATCH", "/tasks/{task}", options);
    },

    /** Mettre a jour partiellement une tache */
    patchTasksByTask6(options = {}) {
      return request("PATCH", "/tasks/{task}", options);
    },

    /** Mettre a jour partiellement une tache */
    patchTasksByTask7(options = {}) {
      return request("PATCH", "/tasks/{task}", options);
    },

    /** Mettre a jour partiellement une tache */
    patchTasksByTask8(options = {}) {
      return request("PATCH", "/tasks/{task}", options);
    },

    /** Mettre a jour une tache */
    putTasksByTask(options = {}) {
      return request("PUT", "/tasks/{task}", options);
    },

    /** Mettre a jour une tache */
    putTasksByTask2(options = {}) {
      return request("PUT", "/tasks/{task}", options);
    },

    /** Mettre a jour une tache */
    putTasksByTask3(options = {}) {
      return request("PUT", "/tasks/{task}", options);
    },

    /** Mettre a jour une tache */
    putTasksByTask4(options = {}) {
      return request("PUT", "/tasks/{task}", options);
    },

    /** Mettre a jour une tache */
    putTasksByTask5(options = {}) {
      return request("PUT", "/tasks/{task}", options);
    },

    /** Mettre a jour une tache */
    putTasksByTask6(options = {}) {
      return request("PUT", "/tasks/{task}", options);
    },

    /** Mettre a jour une tache */
    putTasksByTask7(options = {}) {
      return request("PUT", "/tasks/{task}", options);
    },

    /** Mettre a jour une tache */
    putTasksByTask8(options = {}) {
      return request("PUT", "/tasks/{task}", options);
    },

    /** Commentaires d'une tâche (auteur + employés liés) */
    getTasksByTaskComments(options = {}) {
      return request("GET", "/tasks/{task}/comments", options);
    },

    /** Commentaires d'une tâche (auteur + employés liés) */
    getTasksByTaskComments2(options = {}) {
      return request("GET", "/tasks/{task}/comments", options);
    },

    /** Commentaires d'une tâche (auteur + employés liés) */
    getTasksByTaskComments3(options = {}) {
      return request("GET", "/tasks/{task}/comments", options);
    },

    /** Commentaires d'une tâche (auteur + employés liés) */
    getTasksByTaskComments4(options = {}) {
      return request("GET", "/tasks/{task}/comments", options);
    },

    /** Commentaires d'une tâche (auteur + employés liés) */
    getTasksByTaskComments5(options = {}) {
      return request("GET", "/tasks/{task}/comments", options);
    },

    /** Commentaires d'une tâche (auteur + employés liés) */
    getTasksByTaskComments6(options = {}) {
      return request("GET", "/tasks/{task}/comments", options);
    },

    /** Commentaires d'une tâche (auteur + employés liés) */
    getTasksByTaskComments7(options = {}) {
      return request("GET", "/tasks/{task}/comments", options);
    },

    /** Commentaires d'une tâche (auteur + employés liés) */
    getTasksByTaskComments8(options = {}) {
      return request("GET", "/tasks/{task}/comments", options);
    },

    /** Ajouter un commentaire sur une tache */
    postTasksByTaskComments(options = {}) {
      return request("POST", "/tasks/{task}/comments", options);
    },

    /** Ajouter un commentaire sur une tache */
    postTasksByTaskComments2(options = {}) {
      return request("POST", "/tasks/{task}/comments", options);
    },

    /** Ajouter un commentaire sur une tache */
    postTasksByTaskComments3(options = {}) {
      return request("POST", "/tasks/{task}/comments", options);
    },

    /** Ajouter un commentaire sur une tache */
    postTasksByTaskComments4(options = {}) {
      return request("POST", "/tasks/{task}/comments", options);
    },

    /** Ajouter un commentaire sur une tache */
    postTasksByTaskComments5(options = {}) {
      return request("POST", "/tasks/{task}/comments", options);
    },

    /** Ajouter un commentaire sur une tache */
    postTasksByTaskComments6(options = {}) {
      return request("POST", "/tasks/{task}/comments", options);
    },

    /** Ajouter un commentaire sur une tache */
    postTasksByTaskComments7(options = {}) {
      return request("POST", "/tasks/{task}/comments", options);
    },

    /** Ajouter un commentaire sur une tache */
    postTasksByTaskComments8(options = {}) {
      return request("POST", "/tasks/{task}/comments", options);
    },

    /** Lister les taches du jour */
    getTasksToday(options = {}) {
      return request("GET", "/tasks/today", options);
    },

    /** Lister les taches du jour */
    getTasksToday2(options = {}) {
      return request("GET", "/tasks/today", options);
    },

    /** Lister les taches du jour */
    getTasksToday3(options = {}) {
      return request("GET", "/tasks/today", options);
    },

    /** Lister les taches du jour */
    getTasksToday4(options = {}) {
      return request("GET", "/tasks/today", options);
    },

    /** Lister les taches du jour */
    getTasksToday5(options = {}) {
      return request("GET", "/tasks/today", options);
    },

    /** Lister les taches du jour */
    getTasksToday6(options = {}) {
      return request("GET", "/tasks/today", options);
    },

    /** Lister les taches du jour */
    getTasksToday7(options = {}) {
      return request("GET", "/tasks/today", options);
    },

    /** Lister les taches du jour */
    getTasksToday8(options = {}) {
      return request("GET", "/tasks/today", options);
    },

    /** Lister le bareme fiscal (tranches d'impot) (manager) */
    listTaxSlabs(options = {}) {
      return request("GET", "/tax-slabs", options);
    },

    /** Lister le bareme fiscal (tranches d'impot) (manager) */
    listTaxSlabs2(options = {}) {
      return request("GET", "/tax-slabs", options);
    },

    /** Lister le bareme fiscal (tranches d'impot) (manager) */
    listTaxSlabs3(options = {}) {
      return request("GET", "/tax-slabs", options);
    },

    /** Lister le bareme fiscal (tranches d'impot) (manager) */
    listTaxSlabs4(options = {}) {
      return request("GET", "/tax-slabs", options);
    },

    /** Lister le bareme fiscal (tranches d'impot) (manager) */
    listTaxSlabs5(options = {}) {
      return request("GET", "/tax-slabs", options);
    },

    /** Lister le bareme fiscal (tranches d'impot) (manager) */
    listTaxSlabs6(options = {}) {
      return request("GET", "/tax-slabs", options);
    },

    /** Lister le bareme fiscal (tranches d'impot) (manager) */
    listTaxSlabs7(options = {}) {
      return request("GET", "/tax-slabs", options);
    },

    /** Lister le bareme fiscal (tranches d'impot) (manager) */
    listTaxSlabs8(options = {}) {
      return request("GET", "/tax-slabs", options);
    },

    /** Lister le bareme fiscal (tranches d'impot) (manager) */
    listTaxSlabs9(options = {}) {
      return request("GET", "/tax-slabs", options);
    },

    /** Lister le bareme fiscal (tranches d'impot) (manager) */
    listTaxSlabs10(options = {}) {
      return request("GET", "/tax-slabs", options);
    },

    /** Creer une tranche d'impot (manager) */
    createTaxSlab(options = {}) {
      return request("POST", "/tax-slabs", options);
    },

    /** Creer une tranche d'impot (manager) */
    createTaxSlab2(options = {}) {
      return request("POST", "/tax-slabs", options);
    },

    /** Creer une tranche d'impot (manager) */
    createTaxSlab3(options = {}) {
      return request("POST", "/tax-slabs", options);
    },

    /** Creer une tranche d'impot (manager) */
    createTaxSlab4(options = {}) {
      return request("POST", "/tax-slabs", options);
    },

    /** Creer une tranche d'impot (manager) */
    createTaxSlab5(options = {}) {
      return request("POST", "/tax-slabs", options);
    },

    /** Creer une tranche d'impot (manager) */
    createTaxSlab6(options = {}) {
      return request("POST", "/tax-slabs", options);
    },

    /** Creer une tranche d'impot (manager) */
    createTaxSlab7(options = {}) {
      return request("POST", "/tax-slabs", options);
    },

    /** Creer une tranche d'impot (manager) */
    createTaxSlab8(options = {}) {
      return request("POST", "/tax-slabs", options);
    },

    /** Creer une tranche d'impot (manager) */
    createTaxSlab9(options = {}) {
      return request("POST", "/tax-slabs", options);
    },

    /** Creer une tranche d'impot (manager) */
    createTaxSlab10(options = {}) {
      return request("POST", "/tax-slabs", options);
    },

    /** Supprimer une tranche d'impot (manager) */
    deleteTaxSlab(options = {}) {
      return request("DELETE", "/tax-slabs/{taxSlab}", options);
    },

    /** Supprimer une tranche d'impot (manager) */
    deleteTaxSlab2(options = {}) {
      return request("DELETE", "/tax-slabs/{taxSlab}", options);
    },

    /** Supprimer une tranche d'impot (manager) */
    deleteTaxSlab3(options = {}) {
      return request("DELETE", "/tax-slabs/{taxSlab}", options);
    },

    /** Supprimer une tranche d'impot (manager) */
    deleteTaxSlab4(options = {}) {
      return request("DELETE", "/tax-slabs/{taxSlab}", options);
    },

    /** Supprimer une tranche d'impot (manager) */
    deleteTaxSlab5(options = {}) {
      return request("DELETE", "/tax-slabs/{taxSlab}", options);
    },

    /** Supprimer une tranche d'impot (manager) */
    deleteTaxSlab6(options = {}) {
      return request("DELETE", "/tax-slabs/{taxSlab}", options);
    },

    /** Supprimer une tranche d'impot (manager) */
    deleteTaxSlab7(options = {}) {
      return request("DELETE", "/tax-slabs/{taxSlab}", options);
    },

    /** Supprimer une tranche d'impot (manager) */
    deleteTaxSlab8(options = {}) {
      return request("DELETE", "/tax-slabs/{taxSlab}", options);
    },

    /** Supprimer une tranche d'impot (manager) */
    deleteTaxSlab9(options = {}) {
      return request("DELETE", "/tax-slabs/{taxSlab}", options);
    },

    /** Supprimer une tranche d'impot (manager) */
    deleteTaxSlab10(options = {}) {
      return request("DELETE", "/tax-slabs/{taxSlab}", options);
    },

    /** Modifier une tranche d'impot (manager) */
    updateTaxSlab(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}", options);
    },

    /** Modifier une tranche d'impot (manager) */
    updateTaxSlab2(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}", options);
    },

    /** Modifier une tranche d'impot (manager) */
    updateTaxSlab3(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}", options);
    },

    /** Modifier une tranche d'impot (manager) */
    updateTaxSlab4(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}", options);
    },

    /** Modifier une tranche d'impot (manager) */
    updateTaxSlab5(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}", options);
    },

    /** Modifier une tranche d'impot (manager) */
    updateTaxSlab6(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}", options);
    },

    /** Modifier une tranche d'impot (manager) */
    updateTaxSlab7(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}", options);
    },

    /** Modifier une tranche d'impot (manager) */
    updateTaxSlab8(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}", options);
    },

    /** Modifier une tranche d'impot (manager) */
    updateTaxSlab9(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}", options);
    },

    /** Modifier une tranche d'impot (manager) */
    updateTaxSlab10(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}", options);
    },

    /** Historique immuable des modifications d'une tranche (manager) — issue #1813 */
    taxSlabHistory(options = {}) {
      return request("GET", "/tax-slabs/{taxSlab}/history", options);
    },

    /** Historique immuable des modifications d'une tranche (manager) — issue #1813 */
    taxSlabHistory2(options = {}) {
      return request("GET", "/tax-slabs/{taxSlab}/history", options);
    },

    /** Historique immuable des modifications d'une tranche (manager) — issue #1813 */
    taxSlabHistory3(options = {}) {
      return request("GET", "/tax-slabs/{taxSlab}/history", options);
    },

    /** Historique immuable des modifications d'une tranche (manager) — issue #1813 */
    taxSlabHistory4(options = {}) {
      return request("GET", "/tax-slabs/{taxSlab}/history", options);
    },

    /** Historique immuable des modifications d'une tranche (manager) — issue #1813 */
    taxSlabHistory5(options = {}) {
      return request("GET", "/tax-slabs/{taxSlab}/history", options);
    },

    /** Historique immuable des modifications d'une tranche (manager) — issue #1813 */
    taxSlabHistory6(options = {}) {
      return request("GET", "/tax-slabs/{taxSlab}/history", options);
    },

    /** Historique immuable des modifications d'une tranche (manager) — issue #1813 */
    taxSlabHistory7(options = {}) {
      return request("GET", "/tax-slabs/{taxSlab}/history", options);
    },

    /** Historique immuable des modifications d'une tranche (manager) — issue #1813 */
    taxSlabHistory8(options = {}) {
      return request("GET", "/tax-slabs/{taxSlab}/history", options);
    },

    /** Historique immuable des modifications d'une tranche (manager) — issue #1813 */
    taxSlabHistory9(options = {}) {
      return request("GET", "/tax-slabs/{taxSlab}/history", options);
    },

    /** Historique immuable des modifications d'une tranche (manager) — issue #1813 */
    taxSlabHistory10(options = {}) {
      return request("GET", "/tax-slabs/{taxSlab}/history", options);
    },

    /** Soumettre une tranche d'impot pour validation (manager) — issue #1813 */
    submitTaxSlab(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}/submit", options);
    },

    /** Soumettre une tranche d'impot pour validation (manager) — issue #1813 */
    submitTaxSlab2(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}/submit", options);
    },

    /** Soumettre une tranche d'impot pour validation (manager) — issue #1813 */
    submitTaxSlab3(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}/submit", options);
    },

    /** Soumettre une tranche d'impot pour validation (manager) — issue #1813 */
    submitTaxSlab4(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}/submit", options);
    },

    /** Soumettre une tranche d'impot pour validation (manager) — issue #1813 */
    submitTaxSlab5(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}/submit", options);
    },

    /** Soumettre une tranche d'impot pour validation (manager) — issue #1813 */
    submitTaxSlab6(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}/submit", options);
    },

    /** Soumettre une tranche d'impot pour validation (manager) — issue #1813 */
    submitTaxSlab7(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}/submit", options);
    },

    /** Soumettre une tranche d'impot pour validation (manager) — issue #1813 */
    submitTaxSlab8(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}/submit", options);
    },

    /** Soumettre une tranche d'impot pour validation (manager) — issue #1813 */
    submitTaxSlab9(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}/submit", options);
    },

    /** Soumettre une tranche d'impot pour validation (manager) — issue #1813 */
    submitTaxSlab10(options = {}) {
      return request("PUT", "/tax-slabs/{taxSlab}/submit", options);
    },

    /** Synchroniser les devices Traccar */
    postTrackingSyncDevices(options = {}) {
      return request("POST", "/tracking/sync-devices", options);
    },

    /** Synchroniser les devices Traccar */
    postTrackingSyncDevices2(options = {}) {
      return request("POST", "/tracking/sync-devices", options);
    },

    /** Synchroniser les devices Traccar */
    postTrackingSyncDevices3(options = {}) {
      return request("POST", "/tracking/sync-devices", options);
    },

    /** Synchroniser les devices Traccar */
    postTrackingSyncDevices4(options = {}) {
      return request("POST", "/tracking/sync-devices", options);
    },

    /** Synchroniser les devices Traccar */
    postTrackingSyncDevices5(options = {}) {
      return request("POST", "/tracking/sync-devices", options);
    },

    /** Synchroniser les devices Traccar */
    postTrackingSyncDevices6(options = {}) {
      return request("POST", "/tracking/sync-devices", options);
    },

    /** Synchroniser les devices Traccar */
    postTrackingSyncDevices7(options = {}) {
      return request("POST", "/tracking/sync-devices", options);
    },

    /** Synchroniser les devices Traccar */
    postTrackingSyncDevices8(options = {}) {
      return request("POST", "/tracking/sync-devices", options);
    },

    /** Synchroniser les positions Traccar */
    postTrackingSyncPositions(options = {}) {
      return request("POST", "/tracking/sync-positions", options);
    },

    /** Synchroniser les positions Traccar */
    postTrackingSyncPositions2(options = {}) {
      return request("POST", "/tracking/sync-positions", options);
    },

    /** Synchroniser les positions Traccar */
    postTrackingSyncPositions3(options = {}) {
      return request("POST", "/tracking/sync-positions", options);
    },

    /** Synchroniser les positions Traccar */
    postTrackingSyncPositions4(options = {}) {
      return request("POST", "/tracking/sync-positions", options);
    },

    /** Synchroniser les positions Traccar */
    postTrackingSyncPositions5(options = {}) {
      return request("POST", "/tracking/sync-positions", options);
    },

    /** Synchroniser les positions Traccar */
    postTrackingSyncPositions6(options = {}) {
      return request("POST", "/tracking/sync-positions", options);
    },

    /** Synchroniser les positions Traccar */
    postTrackingSyncPositions7(options = {}) {
      return request("POST", "/tracking/sync-positions", options);
    },

    /** Synchroniser les positions Traccar */
    postTrackingSyncPositions8(options = {}) {
      return request("POST", "/tracking/sync-positions", options);
    },

    /** Synchroniser les trajets Traccar */
    postTrackingSyncTrips(options = {}) {
      return request("POST", "/tracking/sync-trips", options);
    },

    /** Synchroniser les trajets Traccar */
    postTrackingSyncTrips2(options = {}) {
      return request("POST", "/tracking/sync-trips", options);
    },

    /** Synchroniser les trajets Traccar */
    postTrackingSyncTrips3(options = {}) {
      return request("POST", "/tracking/sync-trips", options);
    },

    /** Synchroniser les trajets Traccar */
    postTrackingSyncTrips4(options = {}) {
      return request("POST", "/tracking/sync-trips", options);
    },

    /** Synchroniser les trajets Traccar */
    postTrackingSyncTrips5(options = {}) {
      return request("POST", "/tracking/sync-trips", options);
    },

    /** Synchroniser les trajets Traccar */
    postTrackingSyncTrips6(options = {}) {
      return request("POST", "/tracking/sync-trips", options);
    },

    /** Synchroniser les trajets Traccar */
    postTrackingSyncTrips7(options = {}) {
      return request("POST", "/tracking/sync-trips", options);
    },

    /** Synchroniser les trajets Traccar */
    postTrackingSyncTrips8(options = {}) {
      return request("POST", "/tracking/sync-trips", options);
    },

    /** Lister les formations */
    getTrainingCourses(options = {}) {
      return request("GET", "/training/courses", options);
    },

    /** Lister les formations */
    getTrainingCourses2(options = {}) {
      return request("GET", "/training/courses", options);
    },

    /** Lister les formations */
    getTrainingCourses3(options = {}) {
      return request("GET", "/training/courses", options);
    },

    /** Lister les formations */
    getTrainingCourses4(options = {}) {
      return request("GET", "/training/courses", options);
    },

    /** Lister les formations */
    getTrainingCourses5(options = {}) {
      return request("GET", "/training/courses", options);
    },

    /** Lister les formations */
    getTrainingCourses6(options = {}) {
      return request("GET", "/training/courses", options);
    },

    /** Lister les formations */
    getTrainingCourses7(options = {}) {
      return request("GET", "/training/courses", options);
    },

    /** Lister les formations */
    getTrainingCourses8(options = {}) {
      return request("GET", "/training/courses", options);
    },

    /** Lister les formations */
    getTrainingCourses9(options = {}) {
      return request("GET", "/training/courses", options);
    },

    /** Lister les formations */
    getTrainingCourses10(options = {}) {
      return request("GET", "/training/courses", options);
    },

    /** Lister les formations */
    getTrainingCourses11(options = {}) {
      return request("GET", "/training/courses", options);
    },

    /** Creer une formation */
    postTrainingCourses(options = {}) {
      return request("POST", "/training/courses", options);
    },

    /** Creer une formation */
    postTrainingCourses2(options = {}) {
      return request("POST", "/training/courses", options);
    },

    /** Creer une formation */
    postTrainingCourses3(options = {}) {
      return request("POST", "/training/courses", options);
    },

    /** Creer une formation */
    postTrainingCourses4(options = {}) {
      return request("POST", "/training/courses", options);
    },

    /** Creer une formation */
    postTrainingCourses5(options = {}) {
      return request("POST", "/training/courses", options);
    },

    /** Creer une formation */
    postTrainingCourses6(options = {}) {
      return request("POST", "/training/courses", options);
    },

    /** Creer une formation */
    postTrainingCourses7(options = {}) {
      return request("POST", "/training/courses", options);
    },

    /** Creer une formation */
    postTrainingCourses8(options = {}) {
      return request("POST", "/training/courses", options);
    },

    /** Creer une formation */
    postTrainingCourses9(options = {}) {
      return request("POST", "/training/courses", options);
    },

    /** Creer une formation */
    postTrainingCourses10(options = {}) {
      return request("POST", "/training/courses", options);
    },

    /** Creer une formation */
    postTrainingCourses11(options = {}) {
      return request("POST", "/training/courses", options);
    },

    /** Supprimer une formation */
    deleteTrainingCoursesByTrainingCourse(options = {}) {
      return request("DELETE", "/training/courses/{trainingCourse}", options);
    },

    /** Supprimer une formation */
    deleteTrainingCoursesByTrainingCourse2(options = {}) {
      return request("DELETE", "/training/courses/{trainingCourse}", options);
    },

    /** Supprimer une formation */
    deleteTrainingCoursesByTrainingCourse3(options = {}) {
      return request("DELETE", "/training/courses/{trainingCourse}", options);
    },

    /** Supprimer une formation */
    deleteTrainingCoursesByTrainingCourse4(options = {}) {
      return request("DELETE", "/training/courses/{trainingCourse}", options);
    },

    /** Supprimer une formation */
    deleteTrainingCoursesByTrainingCourse5(options = {}) {
      return request("DELETE", "/training/courses/{trainingCourse}", options);
    },

    /** Supprimer une formation */
    deleteTrainingCoursesByTrainingCourse6(options = {}) {
      return request("DELETE", "/training/courses/{trainingCourse}", options);
    },

    /** Supprimer une formation */
    deleteTrainingCoursesByTrainingCourse7(options = {}) {
      return request("DELETE", "/training/courses/{trainingCourse}", options);
    },

    /** Supprimer une formation */
    deleteTrainingCoursesByTrainingCourse8(options = {}) {
      return request("DELETE", "/training/courses/{trainingCourse}", options);
    },

    /** Supprimer une formation */
    deleteTrainingCoursesByTrainingCourse9(options = {}) {
      return request("DELETE", "/training/courses/{trainingCourse}", options);
    },

    /** Supprimer une formation */
    deleteTrainingCoursesByTrainingCourse10(options = {}) {
      return request("DELETE", "/training/courses/{trainingCourse}", options);
    },

    /** Supprimer une formation */
    deleteTrainingCoursesByTrainingCourse11(options = {}) {
      return request("DELETE", "/training/courses/{trainingCourse}", options);
    },

    /** Voir une formation */
    getTrainingCoursesByTrainingCourse(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}", options);
    },

    /** Voir une formation */
    getTrainingCoursesByTrainingCourse2(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}", options);
    },

    /** Voir une formation */
    getTrainingCoursesByTrainingCourse3(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}", options);
    },

    /** Voir une formation */
    getTrainingCoursesByTrainingCourse4(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}", options);
    },

    /** Voir une formation */
    getTrainingCoursesByTrainingCourse5(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}", options);
    },

    /** Voir une formation */
    getTrainingCoursesByTrainingCourse6(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}", options);
    },

    /** Voir une formation */
    getTrainingCoursesByTrainingCourse7(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}", options);
    },

    /** Voir une formation */
    getTrainingCoursesByTrainingCourse8(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}", options);
    },

    /** Voir une formation */
    getTrainingCoursesByTrainingCourse9(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}", options);
    },

    /** Voir une formation */
    getTrainingCoursesByTrainingCourse10(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}", options);
    },

    /** Voir une formation */
    getTrainingCoursesByTrainingCourse11(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}", options);
    },

    /** Modifier une formation */
    putTrainingCoursesByTrainingCourse(options = {}) {
      return request("PUT", "/training/courses/{trainingCourse}", options);
    },

    /** Modifier une formation */
    putTrainingCoursesByTrainingCourse2(options = {}) {
      return request("PUT", "/training/courses/{trainingCourse}", options);
    },

    /** Modifier une formation */
    putTrainingCoursesByTrainingCourse3(options = {}) {
      return request("PUT", "/training/courses/{trainingCourse}", options);
    },

    /** Modifier une formation */
    putTrainingCoursesByTrainingCourse4(options = {}) {
      return request("PUT", "/training/courses/{trainingCourse}", options);
    },

    /** Modifier une formation */
    putTrainingCoursesByTrainingCourse5(options = {}) {
      return request("PUT", "/training/courses/{trainingCourse}", options);
    },

    /** Modifier une formation */
    putTrainingCoursesByTrainingCourse6(options = {}) {
      return request("PUT", "/training/courses/{trainingCourse}", options);
    },

    /** Modifier une formation */
    putTrainingCoursesByTrainingCourse7(options = {}) {
      return request("PUT", "/training/courses/{trainingCourse}", options);
    },

    /** Modifier une formation */
    putTrainingCoursesByTrainingCourse8(options = {}) {
      return request("PUT", "/training/courses/{trainingCourse}", options);
    },

    /** Modifier une formation */
    putTrainingCoursesByTrainingCourse9(options = {}) {
      return request("PUT", "/training/courses/{trainingCourse}", options);
    },

    /** Modifier une formation */
    putTrainingCoursesByTrainingCourse10(options = {}) {
      return request("PUT", "/training/courses/{trainingCourse}", options);
    },

    /** Modifier une formation */
    putTrainingCoursesByTrainingCourse11(options = {}) {
      return request("PUT", "/training/courses/{trainingCourse}", options);
    },

    /** Lister les sessions d'une formation */
    getTrainingCoursesByTrainingCourseSessions(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Lister les sessions d'une formation */
    getTrainingCoursesByTrainingCourseSessions2(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Lister les sessions d'une formation */
    getTrainingCoursesByTrainingCourseSessions3(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Lister les sessions d'une formation */
    getTrainingCoursesByTrainingCourseSessions4(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Lister les sessions d'une formation */
    getTrainingCoursesByTrainingCourseSessions5(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Lister les sessions d'une formation */
    getTrainingCoursesByTrainingCourseSessions6(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Lister les sessions d'une formation */
    getTrainingCoursesByTrainingCourseSessions7(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Lister les sessions d'une formation */
    getTrainingCoursesByTrainingCourseSessions8(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Lister les sessions d'une formation */
    getTrainingCoursesByTrainingCourseSessions9(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Lister les sessions d'une formation */
    getTrainingCoursesByTrainingCourseSessions10(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Lister les sessions d'une formation */
    getTrainingCoursesByTrainingCourseSessions11(options = {}) {
      return request("GET", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Creer une session de formation */
    postTrainingCoursesByTrainingCourseSessions(options = {}) {
      return request("POST", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Creer une session de formation */
    postTrainingCoursesByTrainingCourseSessions2(options = {}) {
      return request("POST", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Creer une session de formation */
    postTrainingCoursesByTrainingCourseSessions3(options = {}) {
      return request("POST", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Creer une session de formation */
    postTrainingCoursesByTrainingCourseSessions4(options = {}) {
      return request("POST", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Creer une session de formation */
    postTrainingCoursesByTrainingCourseSessions5(options = {}) {
      return request("POST", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Creer une session de formation */
    postTrainingCoursesByTrainingCourseSessions6(options = {}) {
      return request("POST", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Creer une session de formation */
    postTrainingCoursesByTrainingCourseSessions7(options = {}) {
      return request("POST", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Creer une session de formation */
    postTrainingCoursesByTrainingCourseSessions8(options = {}) {
      return request("POST", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Creer une session de formation */
    postTrainingCoursesByTrainingCourseSessions9(options = {}) {
      return request("POST", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Creer une session de formation */
    postTrainingCoursesByTrainingCourseSessions10(options = {}) {
      return request("POST", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Creer une session de formation */
    postTrainingCoursesByTrainingCourseSessions11(options = {}) {
      return request("POST", "/training/courses/{trainingCourse}/sessions", options);
    },

    /** Toutes les inscriptions formation du tenant */
    getTrainingEnrollments(options = {}) {
      return request("GET", "/training/enrollments", options);
    },

    /** Toutes les inscriptions formation du tenant */
    getTrainingEnrollments2(options = {}) {
      return request("GET", "/training/enrollments", options);
    },

    /** Toutes les inscriptions formation du tenant */
    getTrainingEnrollments3(options = {}) {
      return request("GET", "/training/enrollments", options);
    },

    /** Toutes les inscriptions formation du tenant */
    getTrainingEnrollments4(options = {}) {
      return request("GET", "/training/enrollments", options);
    },

    /** Toutes les inscriptions formation du tenant */
    getTrainingEnrollments5(options = {}) {
      return request("GET", "/training/enrollments", options);
    },

    /** Toutes les inscriptions formation du tenant */
    getTrainingEnrollments6(options = {}) {
      return request("GET", "/training/enrollments", options);
    },

    /** Toutes les inscriptions formation du tenant */
    getTrainingEnrollments7(options = {}) {
      return request("GET", "/training/enrollments", options);
    },

    /** Toutes les inscriptions formation du tenant */
    getTrainingEnrollments8(options = {}) {
      return request("GET", "/training/enrollments", options);
    },

    /** Toutes les inscriptions formation du tenant */
    getTrainingEnrollments9(options = {}) {
      return request("GET", "/training/enrollments", options);
    },

    /** Toutes les inscriptions formation du tenant */
    getTrainingEnrollments10(options = {}) {
      return request("GET", "/training/enrollments", options);
    },

    /** Toutes les inscriptions formation du tenant */
    getTrainingEnrollments11(options = {}) {
      return request("GET", "/training/enrollments", options);
    },

    /** Supprimer une inscription formation */
    deleteTrainingEnrollmentsByTrainingEnrollment(options = {}) {
      return request("DELETE", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Supprimer une inscription formation */
    deleteTrainingEnrollmentsByTrainingEnrollment2(options = {}) {
      return request("DELETE", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Supprimer une inscription formation */
    deleteTrainingEnrollmentsByTrainingEnrollment3(options = {}) {
      return request("DELETE", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Supprimer une inscription formation */
    deleteTrainingEnrollmentsByTrainingEnrollment4(options = {}) {
      return request("DELETE", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Supprimer une inscription formation */
    deleteTrainingEnrollmentsByTrainingEnrollment5(options = {}) {
      return request("DELETE", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Supprimer une inscription formation */
    deleteTrainingEnrollmentsByTrainingEnrollment6(options = {}) {
      return request("DELETE", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Supprimer une inscription formation */
    deleteTrainingEnrollmentsByTrainingEnrollment7(options = {}) {
      return request("DELETE", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Supprimer une inscription formation */
    deleteTrainingEnrollmentsByTrainingEnrollment8(options = {}) {
      return request("DELETE", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Supprimer une inscription formation */
    deleteTrainingEnrollmentsByTrainingEnrollment9(options = {}) {
      return request("DELETE", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Supprimer une inscription formation */
    deleteTrainingEnrollmentsByTrainingEnrollment10(options = {}) {
      return request("DELETE", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Supprimer une inscription formation */
    deleteTrainingEnrollmentsByTrainingEnrollment11(options = {}) {
      return request("DELETE", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Modifier une inscription */
    putTrainingEnrollmentsByTrainingEnrollment(options = {}) {
      return request("PUT", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Modifier une inscription */
    putTrainingEnrollmentsByTrainingEnrollment2(options = {}) {
      return request("PUT", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Modifier une inscription */
    putTrainingEnrollmentsByTrainingEnrollment3(options = {}) {
      return request("PUT", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Modifier une inscription */
    putTrainingEnrollmentsByTrainingEnrollment4(options = {}) {
      return request("PUT", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Modifier une inscription */
    putTrainingEnrollmentsByTrainingEnrollment5(options = {}) {
      return request("PUT", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Modifier une inscription */
    putTrainingEnrollmentsByTrainingEnrollment6(options = {}) {
      return request("PUT", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Modifier une inscription */
    putTrainingEnrollmentsByTrainingEnrollment7(options = {}) {
      return request("PUT", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Modifier une inscription */
    putTrainingEnrollmentsByTrainingEnrollment8(options = {}) {
      return request("PUT", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Modifier une inscription */
    putTrainingEnrollmentsByTrainingEnrollment9(options = {}) {
      return request("PUT", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Modifier une inscription */
    putTrainingEnrollmentsByTrainingEnrollment10(options = {}) {
      return request("PUT", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Modifier une inscription */
    putTrainingEnrollmentsByTrainingEnrollment11(options = {}) {
      return request("PUT", "/training/enrollments/{trainingEnrollment}", options);
    },

    /** Toutes les sessions de formation du tenant */
    getTrainingSessions(options = {}) {
      return request("GET", "/training/sessions", options);
    },

    /** Toutes les sessions de formation du tenant */
    getTrainingSessions2(options = {}) {
      return request("GET", "/training/sessions", options);
    },

    /** Toutes les sessions de formation du tenant */
    getTrainingSessions3(options = {}) {
      return request("GET", "/training/sessions", options);
    },

    /** Toutes les sessions de formation du tenant */
    getTrainingSessions4(options = {}) {
      return request("GET", "/training/sessions", options);
    },

    /** Toutes les sessions de formation du tenant */
    getTrainingSessions5(options = {}) {
      return request("GET", "/training/sessions", options);
    },

    /** Toutes les sessions de formation du tenant */
    getTrainingSessions6(options = {}) {
      return request("GET", "/training/sessions", options);
    },

    /** Toutes les sessions de formation du tenant */
    getTrainingSessions7(options = {}) {
      return request("GET", "/training/sessions", options);
    },

    /** Toutes les sessions de formation du tenant */
    getTrainingSessions8(options = {}) {
      return request("GET", "/training/sessions", options);
    },

    /** Toutes les sessions de formation du tenant */
    getTrainingSessions9(options = {}) {
      return request("GET", "/training/sessions", options);
    },

    /** Toutes les sessions de formation du tenant */
    getTrainingSessions10(options = {}) {
      return request("GET", "/training/sessions", options);
    },

    /** Toutes les sessions de formation du tenant */
    getTrainingSessions11(options = {}) {
      return request("GET", "/training/sessions", options);
    },

    /** Supprimer une session */
    deleteTrainingSessionsByTrainingSession(options = {}) {
      return request("DELETE", "/training/sessions/{trainingSession}", options);
    },

    /** Supprimer une session */
    deleteTrainingSessionsByTrainingSession2(options = {}) {
      return request("DELETE", "/training/sessions/{trainingSession}", options);
    },

    /** Supprimer une session */
    deleteTrainingSessionsByTrainingSession3(options = {}) {
      return request("DELETE", "/training/sessions/{trainingSession}", options);
    },

    /** Supprimer une session */
    deleteTrainingSessionsByTrainingSession4(options = {}) {
      return request("DELETE", "/training/sessions/{trainingSession}", options);
    },

    /** Supprimer une session */
    deleteTrainingSessionsByTrainingSession5(options = {}) {
      return request("DELETE", "/training/sessions/{trainingSession}", options);
    },

    /** Supprimer une session */
    deleteTrainingSessionsByTrainingSession6(options = {}) {
      return request("DELETE", "/training/sessions/{trainingSession}", options);
    },

    /** Supprimer une session */
    deleteTrainingSessionsByTrainingSession7(options = {}) {
      return request("DELETE", "/training/sessions/{trainingSession}", options);
    },

    /** Supprimer une session */
    deleteTrainingSessionsByTrainingSession8(options = {}) {
      return request("DELETE", "/training/sessions/{trainingSession}", options);
    },

    /** Supprimer une session */
    deleteTrainingSessionsByTrainingSession9(options = {}) {
      return request("DELETE", "/training/sessions/{trainingSession}", options);
    },

    /** Supprimer une session */
    deleteTrainingSessionsByTrainingSession10(options = {}) {
      return request("DELETE", "/training/sessions/{trainingSession}", options);
    },

    /** Supprimer une session */
    deleteTrainingSessionsByTrainingSession11(options = {}) {
      return request("DELETE", "/training/sessions/{trainingSession}", options);
    },

    /** Modifier une session */
    putTrainingSessionsByTrainingSession(options = {}) {
      return request("PUT", "/training/sessions/{trainingSession}", options);
    },

    /** Modifier une session */
    putTrainingSessionsByTrainingSession2(options = {}) {
      return request("PUT", "/training/sessions/{trainingSession}", options);
    },

    /** Modifier une session */
    putTrainingSessionsByTrainingSession3(options = {}) {
      return request("PUT", "/training/sessions/{trainingSession}", options);
    },

    /** Modifier une session */
    putTrainingSessionsByTrainingSession4(options = {}) {
      return request("PUT", "/training/sessions/{trainingSession}", options);
    },

    /** Modifier une session */
    putTrainingSessionsByTrainingSession5(options = {}) {
      return request("PUT", "/training/sessions/{trainingSession}", options);
    },

    /** Modifier une session */
    putTrainingSessionsByTrainingSession6(options = {}) {
      return request("PUT", "/training/sessions/{trainingSession}", options);
    },

    /** Modifier une session */
    putTrainingSessionsByTrainingSession7(options = {}) {
      return request("PUT", "/training/sessions/{trainingSession}", options);
    },

    /** Modifier une session */
    putTrainingSessionsByTrainingSession8(options = {}) {
      return request("PUT", "/training/sessions/{trainingSession}", options);
    },

    /** Modifier une session */
    putTrainingSessionsByTrainingSession9(options = {}) {
      return request("PUT", "/training/sessions/{trainingSession}", options);
    },

    /** Modifier une session */
    putTrainingSessionsByTrainingSession10(options = {}) {
      return request("PUT", "/training/sessions/{trainingSession}", options);
    },

    /** Modifier une session */
    putTrainingSessionsByTrainingSession11(options = {}) {
      return request("PUT", "/training/sessions/{trainingSession}", options);
    },

    /** Inscrire un employe */
    postTrainingSessionsByTrainingSessionEnroll(options = {}) {
      return request("POST", "/training/sessions/{trainingSession}/enroll", options);
    },

    /** Inscrire un employe */
    postTrainingSessionsByTrainingSessionEnroll2(options = {}) {
      return request("POST", "/training/sessions/{trainingSession}/enroll", options);
    },

    /** Inscrire un employe */
    postTrainingSessionsByTrainingSessionEnroll3(options = {}) {
      return request("POST", "/training/sessions/{trainingSession}/enroll", options);
    },

    /** Inscrire un employe */
    postTrainingSessionsByTrainingSessionEnroll4(options = {}) {
      return request("POST", "/training/sessions/{trainingSession}/enroll", options);
    },

    /** Inscrire un employe */
    postTrainingSessionsByTrainingSessionEnroll5(options = {}) {
      return request("POST", "/training/sessions/{trainingSession}/enroll", options);
    },

    /** Inscrire un employe */
    postTrainingSessionsByTrainingSessionEnroll6(options = {}) {
      return request("POST", "/training/sessions/{trainingSession}/enroll", options);
    },

    /** Inscrire un employe */
    postTrainingSessionsByTrainingSessionEnroll7(options = {}) {
      return request("POST", "/training/sessions/{trainingSession}/enroll", options);
    },

    /** Inscrire un employe */
    postTrainingSessionsByTrainingSessionEnroll8(options = {}) {
      return request("POST", "/training/sessions/{trainingSession}/enroll", options);
    },

    /** Inscrire un employe */
    postTrainingSessionsByTrainingSessionEnroll9(options = {}) {
      return request("POST", "/training/sessions/{trainingSession}/enroll", options);
    },

    /** Inscrire un employe */
    postTrainingSessionsByTrainingSessionEnroll10(options = {}) {
      return request("POST", "/training/sessions/{trainingSession}/enroll", options);
    },

    /** Inscrire un employe */
    postTrainingSessionsByTrainingSessionEnroll11(options = {}) {
      return request("POST", "/training/sessions/{trainingSession}/enroll", options);
    },

    /** Demande d'essai guidé / auto-service (onboarding) */
    postTrialSignup(options = {}) {
      return request("POST", "/trial/signup", options);
    },

    /** Demande d'essai guidé / auto-service (onboarding) */
    postTrialSignup2(options = {}) {
      return request("POST", "/trial/signup", options);
    },

    /** Demande d'essai guidé / auto-service (onboarding) */
    postTrialSignup3(options = {}) {
      return request("POST", "/trial/signup", options);
    },

    /** Demande d'essai guidé / auto-service (onboarding) */
    postTrialSignup4(options = {}) {
      return request("POST", "/trial/signup", options);
    },

    /** Demande d'essai guidé / auto-service (onboarding) */
    postTrialSignup5(options = {}) {
      return request("POST", "/trial/signup", options);
    },

    /** Statut du provisioning d'un essai guidé (polling) */
    getTrialStatus(options = {}) {
      return request("GET", "/trial/status", options);
    },

    /** Statut du provisioning d'un essai guidé (polling) */
    getTrialStatus2(options = {}) {
      return request("GET", "/trial/status", options);
    },

    /** Statut du provisioning d'un essai guidé (polling) */
    getTrialStatus3(options = {}) {
      return request("GET", "/trial/status", options);
    },

    /** Statut du provisioning d'un essai guidé (polling) */
    getTrialStatus4(options = {}) {
      return request("GET", "/trial/status", options);
    },

    /** Statut du provisioning d'un essai guidé (polling) */
    getTrialStatus5(options = {}) {
      return request("GET", "/trial/status", options);
    },

    /** Vérifie le code OTP et provisionne l'espace d'essai */
    postTrialVerify(options = {}) {
      return request("POST", "/trial/verify", options);
    },

    /** Vérifie le code OTP et provisionne l'espace d'essai */
    postTrialVerify2(options = {}) {
      return request("POST", "/trial/verify", options);
    },

    /** Vérifie le code OTP et provisionne l'espace d'essai */
    postTrialVerify3(options = {}) {
      return request("POST", "/trial/verify", options);
    },

    /** Vérifie le code OTP et provisionne l'espace d'essai */
    postTrialVerify4(options = {}) {
      return request("POST", "/trial/verify", options);
    },

    /** Vérifie le code OTP et provisionne l'espace d'essai */
    postTrialVerify5(options = {}) {
      return request("POST", "/trial/verify", options);
    },

    /** Change le mot de passe de l'utilisateur connecté */
    postUserChangePassword(options = {}) {
      return request("POST", "/user/change-password", options);
    },

    /** Change le mot de passe de l'utilisateur connecté */
    postUserChangePassword2(options = {}) {
      return request("POST", "/user/change-password", options);
    },

    /** Change le mot de passe de l'utilisateur connecté */
    postUserChangePassword3(options = {}) {
      return request("POST", "/user/change-password", options);
    },

    /** Change le mot de passe de l'utilisateur connecté */
    postUserChangePassword4(options = {}) {
      return request("POST", "/user/change-password", options);
    },

    /** Change le mot de passe de l'utilisateur connecté */
    postUserChangePassword5(options = {}) {
      return request("POST", "/user/change-password", options);
    },

    /** Liste les demandes de création d'entreprise de l'utilisateur */
    getUserCompanyRequests(options = {}) {
      return request("GET", "/user/company-requests", options);
    },

    /** Liste les demandes de création d'entreprise de l'utilisateur */
    getUserCompanyRequests2(options = {}) {
      return request("GET", "/user/company-requests", options);
    },

    /** Liste les demandes de création d'entreprise de l'utilisateur */
    getUserCompanyRequests3(options = {}) {
      return request("GET", "/user/company-requests", options);
    },

    /** Liste les demandes de création d'entreprise de l'utilisateur */
    getUserCompanyRequests4(options = {}) {
      return request("GET", "/user/company-requests", options);
    },

    /** Liste les demandes de création d'entreprise de l'utilisateur */
    getUserCompanyRequests5(options = {}) {
      return request("GET", "/user/company-requests", options);
    },

    /** Crée une demande de création d'entreprise */
    postUserCompanyRequests(options = {}) {
      return request("POST", "/user/company-requests", options);
    },

    /** Crée une demande de création d'entreprise */
    postUserCompanyRequests2(options = {}) {
      return request("POST", "/user/company-requests", options);
    },

    /** Crée une demande de création d'entreprise */
    postUserCompanyRequests3(options = {}) {
      return request("POST", "/user/company-requests", options);
    },

    /** Crée une demande de création d'entreprise */
    postUserCompanyRequests4(options = {}) {
      return request("POST", "/user/company-requests", options);
    },

    /** Crée une demande de création d'entreprise */
    postUserCompanyRequests5(options = {}) {
      return request("POST", "/user/company-requests", options);
    },

    /** Détail d'une demande de création d'entreprise */
    getUserCompanyRequestsById(options = {}) {
      return request("GET", "/user/company-requests/{id}", options);
    },

    /** Détail d'une demande de création d'entreprise */
    getUserCompanyRequestsById2(options = {}) {
      return request("GET", "/user/company-requests/{id}", options);
    },

    /** Détail d'une demande de création d'entreprise */
    getUserCompanyRequestsById3(options = {}) {
      return request("GET", "/user/company-requests/{id}", options);
    },

    /** Détail d'une demande de création d'entreprise */
    getUserCompanyRequestsById4(options = {}) {
      return request("GET", "/user/company-requests/{id}", options);
    },

    /** Détail d'une demande de création d'entreprise */
    getUserCompanyRequestsById5(options = {}) {
      return request("GET", "/user/company-requests/{id}", options);
    },

    /** Liste les liens employé de l'utilisateur ordinaire */
    getUserEmployeeLinks(options = {}) {
      return request("GET", "/user/employee-links", options);
    },

    /** Liste les liens employé de l'utilisateur ordinaire */
    getUserEmployeeLinks2(options = {}) {
      return request("GET", "/user/employee-links", options);
    },

    /** Liste les liens employé de l'utilisateur ordinaire */
    getUserEmployeeLinks3(options = {}) {
      return request("GET", "/user/employee-links", options);
    },

    /** Liste les liens employé de l'utilisateur ordinaire */
    getUserEmployeeLinks4(options = {}) {
      return request("GET", "/user/employee-links", options);
    },

    /** Liste les liens employé de l'utilisateur ordinaire */
    getUserEmployeeLinks5(options = {}) {
      return request("GET", "/user/employee-links", options);
    },

    /** Connexion ou création de compte via Google (compte ordinaire) */
    postUserGoogleSignin(options = {}) {
      return request("POST", "/user/google-signin", options);
    },

    /** Connexion ou création de compte via Google (compte ordinaire) */
    postUserGoogleSignin2(options = {}) {
      return request("POST", "/user/google-signin", options);
    },

    /** Connexion ou création de compte via Google (compte ordinaire) */
    postUserGoogleSignin3(options = {}) {
      return request("POST", "/user/google-signin", options);
    },

    /** Connexion ou création de compte via Google (compte ordinaire) */
    postUserGoogleSignin4(options = {}) {
      return request("POST", "/user/google-signin", options);
    },

    /** Connexion ou création de compte via Google (compte ordinaire) */
    postUserGoogleSignin5(options = {}) {
      return request("POST", "/user/google-signin", options);
    },

    /** Connexion utilisateur sans entreprise (parcours legacy mobile) */
    postUserLogin(options = {}) {
      return request("POST", "/user/login", options);
    },

    /** Connexion utilisateur sans entreprise (parcours legacy mobile) */
    postUserLogin2(options = {}) {
      return request("POST", "/user/login", options);
    },

    /** Connexion utilisateur sans entreprise (parcours legacy mobile) */
    postUserLogin3(options = {}) {
      return request("POST", "/user/login", options);
    },

    /** Connexion utilisateur sans entreprise (parcours legacy mobile) */
    postUserLogin4(options = {}) {
      return request("POST", "/user/login", options);
    },

    /** Connexion utilisateur sans entreprise (parcours legacy mobile) */
    postUserLogin5(options = {}) {
      return request("POST", "/user/login", options);
    },

    /** Déconnecte l'utilisateur (révoque le token) */
    postUserLogout(options = {}) {
      return request("POST", "/user/logout", options);
    },

    /** Déconnecte l'utilisateur (révoque le token) */
    postUserLogout2(options = {}) {
      return request("POST", "/user/logout", options);
    },

    /** Déconnecte l'utilisateur (révoque le token) */
    postUserLogout3(options = {}) {
      return request("POST", "/user/logout", options);
    },

    /** Déconnecte l'utilisateur (révoque le token) */
    postUserLogout4(options = {}) {
      return request("POST", "/user/logout", options);
    },

    /** Déconnecte l'utilisateur (révoque le token) */
    postUserLogout5(options = {}) {
      return request("POST", "/user/logout", options);
    },

    /** Profil de l'utilisateur connecté */
    getUserMe(options = {}) {
      return request("GET", "/user/me", options);
    },

    /** Profil de l'utilisateur connecté */
    getUserMe2(options = {}) {
      return request("GET", "/user/me", options);
    },

    /** Profil de l'utilisateur connecté */
    getUserMe3(options = {}) {
      return request("GET", "/user/me", options);
    },

    /** Profil de l'utilisateur connecté */
    getUserMe4(options = {}) {
      return request("GET", "/user/me", options);
    },

    /** Profil de l'utilisateur connecté */
    getUserMe5(options = {}) {
      return request("GET", "/user/me", options);
    },

    /** Met à jour le profil de l'utilisateur connecté */
    patchUserProfile(options = {}) {
      return request("PATCH", "/user/profile", options);
    },

    /** Met à jour le profil de l'utilisateur connecté */
    patchUserProfile2(options = {}) {
      return request("PATCH", "/user/profile", options);
    },

    /** Met à jour le profil de l'utilisateur connecté */
    patchUserProfile3(options = {}) {
      return request("PATCH", "/user/profile", options);
    },

    /** Met à jour le profil de l'utilisateur connecté */
    patchUserProfile4(options = {}) {
      return request("PATCH", "/user/profile", options);
    },

    /** Met à jour le profil de l'utilisateur connecté */
    patchUserProfile5(options = {}) {
      return request("PATCH", "/user/profile", options);
    },

    /** Crée un compte utilisateur sans entreprise (parcours legacy mobile) */
    postUserRegister(options = {}) {
      return request("POST", "/user/register", options);
    },

    /** Crée un compte utilisateur sans entreprise (parcours legacy mobile) */
    postUserRegister2(options = {}) {
      return request("POST", "/user/register", options);
    },

    /** Crée un compte utilisateur sans entreprise (parcours legacy mobile) */
    postUserRegister3(options = {}) {
      return request("POST", "/user/register", options);
    },

    /** Crée un compte utilisateur sans entreprise (parcours legacy mobile) */
    postUserRegister4(options = {}) {
      return request("POST", "/user/register", options);
    },

    /** Crée un compte utilisateur sans entreprise (parcours legacy mobile) */
    postUserRegister5(options = {}) {
      return request("POST", "/user/register", options);
    },

    /** Liste de toutes les alertes vehicules */
    getVehicleAlerts(options = {}) {
      return request("GET", "/vehicle-alerts", options);
    },

    /** Liste de toutes les alertes vehicules */
    getVehicleAlerts2(options = {}) {
      return request("GET", "/vehicle-alerts", options);
    },

    /** Liste de toutes les alertes vehicules */
    getVehicleAlerts3(options = {}) {
      return request("GET", "/vehicle-alerts", options);
    },

    /** Liste de toutes les alertes vehicules */
    getVehicleAlerts4(options = {}) {
      return request("GET", "/vehicle-alerts", options);
    },

    /** Liste de toutes les alertes vehicules */
    getVehicleAlerts5(options = {}) {
      return request("GET", "/vehicle-alerts", options);
    },

    /** Liste de toutes les alertes vehicules */
    getVehicleAlerts6(options = {}) {
      return request("GET", "/vehicle-alerts", options);
    },

    /** Liste de toutes les alertes vehicules */
    getVehicleAlerts7(options = {}) {
      return request("GET", "/vehicle-alerts", options);
    },

    /** Liste de toutes les alertes vehicules */
    getVehicleAlerts8(options = {}) {
      return request("GET", "/vehicle-alerts", options);
    },

    /** Acquitter une alerte vehicule */
    postVehicleAlertsByIdAcknowledge(options = {}) {
      return request("POST", "/vehicle-alerts/{id}/acknowledge", options);
    },

    /** Acquitter une alerte vehicule */
    postVehicleAlertsByIdAcknowledge2(options = {}) {
      return request("POST", "/vehicle-alerts/{id}/acknowledge", options);
    },

    /** Acquitter une alerte vehicule */
    postVehicleAlertsByIdAcknowledge3(options = {}) {
      return request("POST", "/vehicle-alerts/{id}/acknowledge", options);
    },

    /** Acquitter une alerte vehicule */
    postVehicleAlertsByIdAcknowledge4(options = {}) {
      return request("POST", "/vehicle-alerts/{id}/acknowledge", options);
    },

    /** Acquitter une alerte vehicule */
    postVehicleAlertsByIdAcknowledge5(options = {}) {
      return request("POST", "/vehicle-alerts/{id}/acknowledge", options);
    },

    /** Acquitter une alerte vehicule */
    postVehicleAlertsByIdAcknowledge6(options = {}) {
      return request("POST", "/vehicle-alerts/{id}/acknowledge", options);
    },

    /** Acquitter une alerte vehicule */
    postVehicleAlertsByIdAcknowledge7(options = {}) {
      return request("POST", "/vehicle-alerts/{id}/acknowledge", options);
    },

    /** Acquitter une alerte vehicule */
    postVehicleAlertsByIdAcknowledge8(options = {}) {
      return request("POST", "/vehicle-alerts/{id}/acknowledge", options);
    },

    /** Liste des maintenances vehicules */
    getVehicleMaintenance(options = {}) {
      return request("GET", "/vehicle-maintenance", options);
    },

    /** Liste des maintenances vehicules */
    getVehicleMaintenance2(options = {}) {
      return request("GET", "/vehicle-maintenance", options);
    },

    /** Liste des maintenances vehicules */
    getVehicleMaintenance3(options = {}) {
      return request("GET", "/vehicle-maintenance", options);
    },

    /** Liste des maintenances vehicules */
    getVehicleMaintenance4(options = {}) {
      return request("GET", "/vehicle-maintenance", options);
    },

    /** Liste des maintenances vehicules */
    getVehicleMaintenance5(options = {}) {
      return request("GET", "/vehicle-maintenance", options);
    },

    /** Liste des maintenances vehicules */
    getVehicleMaintenance6(options = {}) {
      return request("GET", "/vehicle-maintenance", options);
    },

    /** Liste des maintenances vehicules */
    getVehicleMaintenance7(options = {}) {
      return request("GET", "/vehicle-maintenance", options);
    },

    /** Liste des maintenances vehicules */
    getVehicleMaintenance8(options = {}) {
      return request("GET", "/vehicle-maintenance", options);
    },

    /** Planifier une maintenance */
    postVehicleMaintenance(options = {}) {
      return request("POST", "/vehicle-maintenance", options);
    },

    /** Planifier une maintenance */
    postVehicleMaintenance2(options = {}) {
      return request("POST", "/vehicle-maintenance", options);
    },

    /** Planifier une maintenance */
    postVehicleMaintenance3(options = {}) {
      return request("POST", "/vehicle-maintenance", options);
    },

    /** Planifier une maintenance */
    postVehicleMaintenance4(options = {}) {
      return request("POST", "/vehicle-maintenance", options);
    },

    /** Planifier une maintenance */
    postVehicleMaintenance5(options = {}) {
      return request("POST", "/vehicle-maintenance", options);
    },

    /** Planifier une maintenance */
    postVehicleMaintenance6(options = {}) {
      return request("POST", "/vehicle-maintenance", options);
    },

    /** Planifier une maintenance */
    postVehicleMaintenance7(options = {}) {
      return request("POST", "/vehicle-maintenance", options);
    },

    /** Planifier une maintenance */
    postVehicleMaintenance8(options = {}) {
      return request("POST", "/vehicle-maintenance", options);
    },

    /** Supprimer une maintenance */
    deleteVehicleMaintenanceById(options = {}) {
      return request("DELETE", "/vehicle-maintenance/{id}", options);
    },

    /** Supprimer une maintenance */
    deleteVehicleMaintenanceById2(options = {}) {
      return request("DELETE", "/vehicle-maintenance/{id}", options);
    },

    /** Supprimer une maintenance */
    deleteVehicleMaintenanceById3(options = {}) {
      return request("DELETE", "/vehicle-maintenance/{id}", options);
    },

    /** Supprimer une maintenance */
    deleteVehicleMaintenanceById4(options = {}) {
      return request("DELETE", "/vehicle-maintenance/{id}", options);
    },

    /** Supprimer une maintenance */
    deleteVehicleMaintenanceById5(options = {}) {
      return request("DELETE", "/vehicle-maintenance/{id}", options);
    },

    /** Supprimer une maintenance */
    deleteVehicleMaintenanceById6(options = {}) {
      return request("DELETE", "/vehicle-maintenance/{id}", options);
    },

    /** Supprimer une maintenance */
    deleteVehicleMaintenanceById7(options = {}) {
      return request("DELETE", "/vehicle-maintenance/{id}", options);
    },

    /** Supprimer une maintenance */
    deleteVehicleMaintenanceById8(options = {}) {
      return request("DELETE", "/vehicle-maintenance/{id}", options);
    },

    /** Modifier une maintenance */
    putVehicleMaintenanceById(options = {}) {
      return request("PUT", "/vehicle-maintenance/{id}", options);
    },

    /** Modifier une maintenance */
    putVehicleMaintenanceById2(options = {}) {
      return request("PUT", "/vehicle-maintenance/{id}", options);
    },

    /** Modifier une maintenance */
    putVehicleMaintenanceById3(options = {}) {
      return request("PUT", "/vehicle-maintenance/{id}", options);
    },

    /** Modifier une maintenance */
    putVehicleMaintenanceById4(options = {}) {
      return request("PUT", "/vehicle-maintenance/{id}", options);
    },

    /** Modifier une maintenance */
    putVehicleMaintenanceById5(options = {}) {
      return request("PUT", "/vehicle-maintenance/{id}", options);
    },

    /** Modifier une maintenance */
    putVehicleMaintenanceById6(options = {}) {
      return request("PUT", "/vehicle-maintenance/{id}", options);
    },

    /** Modifier une maintenance */
    putVehicleMaintenanceById7(options = {}) {
      return request("PUT", "/vehicle-maintenance/{id}", options);
    },

    /** Modifier une maintenance */
    putVehicleMaintenanceById8(options = {}) {
      return request("PUT", "/vehicle-maintenance/{id}", options);
    },

    /** Liste de tous les trajets */
    getVehicleTrips(options = {}) {
      return request("GET", "/vehicle-trips", options);
    },

    /** Liste de tous les trajets */
    getVehicleTrips2(options = {}) {
      return request("GET", "/vehicle-trips", options);
    },

    /** Liste de tous les trajets */
    getVehicleTrips3(options = {}) {
      return request("GET", "/vehicle-trips", options);
    },

    /** Liste de tous les trajets */
    getVehicleTrips4(options = {}) {
      return request("GET", "/vehicle-trips", options);
    },

    /** Liste de tous les trajets */
    getVehicleTrips5(options = {}) {
      return request("GET", "/vehicle-trips", options);
    },

    /** Liste de tous les trajets */
    getVehicleTrips6(options = {}) {
      return request("GET", "/vehicle-trips", options);
    },

    /** Liste de tous les trajets */
    getVehicleTrips7(options = {}) {
      return request("GET", "/vehicle-trips", options);
    },

    /** Liste de tous les trajets */
    getVehicleTrips8(options = {}) {
      return request("GET", "/vehicle-trips", options);
    },

    /** Detail d'un trajet */
    getVehicleTripsById(options = {}) {
      return request("GET", "/vehicle-trips/{id}", options);
    },

    /** Detail d'un trajet */
    getVehicleTripsById2(options = {}) {
      return request("GET", "/vehicle-trips/{id}", options);
    },

    /** Detail d'un trajet */
    getVehicleTripsById3(options = {}) {
      return request("GET", "/vehicle-trips/{id}", options);
    },

    /** Detail d'un trajet */
    getVehicleTripsById4(options = {}) {
      return request("GET", "/vehicle-trips/{id}", options);
    },

    /** Detail d'un trajet */
    getVehicleTripsById5(options = {}) {
      return request("GET", "/vehicle-trips/{id}", options);
    },

    /** Detail d'un trajet */
    getVehicleTripsById6(options = {}) {
      return request("GET", "/vehicle-trips/{id}", options);
    },

    /** Detail d'un trajet */
    getVehicleTripsById7(options = {}) {
      return request("GET", "/vehicle-trips/{id}", options);
    },

    /** Detail d'un trajet */
    getVehicleTripsById8(options = {}) {
      return request("GET", "/vehicle-trips/{id}", options);
    },

    /** Liste des vehicules de l'entreprise */
    getVehicles(options = {}) {
      return request("GET", "/vehicles", options);
    },

    /** Liste des vehicules de l'entreprise */
    getVehicles2(options = {}) {
      return request("GET", "/vehicles", options);
    },

    /** Liste des vehicules de l'entreprise */
    getVehicles3(options = {}) {
      return request("GET", "/vehicles", options);
    },

    /** Liste des vehicules de l'entreprise */
    getVehicles4(options = {}) {
      return request("GET", "/vehicles", options);
    },

    /** Liste des vehicules de l'entreprise */
    getVehicles5(options = {}) {
      return request("GET", "/vehicles", options);
    },

    /** Liste des vehicules de l'entreprise */
    getVehicles6(options = {}) {
      return request("GET", "/vehicles", options);
    },

    /** Liste des vehicules de l'entreprise */
    getVehicles7(options = {}) {
      return request("GET", "/vehicles", options);
    },

    /** Liste des vehicules de l'entreprise */
    getVehicles8(options = {}) {
      return request("GET", "/vehicles", options);
    },

    /** Creer un vehicule */
    postVehicles(options = {}) {
      return request("POST", "/vehicles", options);
    },

    /** Creer un vehicule */
    postVehicles2(options = {}) {
      return request("POST", "/vehicles", options);
    },

    /** Creer un vehicule */
    postVehicles3(options = {}) {
      return request("POST", "/vehicles", options);
    },

    /** Creer un vehicule */
    postVehicles4(options = {}) {
      return request("POST", "/vehicles", options);
    },

    /** Creer un vehicule */
    postVehicles5(options = {}) {
      return request("POST", "/vehicles", options);
    },

    /** Creer un vehicule */
    postVehicles6(options = {}) {
      return request("POST", "/vehicles", options);
    },

    /** Creer un vehicule */
    postVehicles7(options = {}) {
      return request("POST", "/vehicles", options);
    },

    /** Creer un vehicule */
    postVehicles8(options = {}) {
      return request("POST", "/vehicles", options);
    },

    /** Supprimer un vehicule */
    deleteVehiclesById(options = {}) {
      return request("DELETE", "/vehicles/{id}", options);
    },

    /** Supprimer un vehicule */
    deleteVehiclesById2(options = {}) {
      return request("DELETE", "/vehicles/{id}", options);
    },

    /** Supprimer un vehicule */
    deleteVehiclesById3(options = {}) {
      return request("DELETE", "/vehicles/{id}", options);
    },

    /** Supprimer un vehicule */
    deleteVehiclesById4(options = {}) {
      return request("DELETE", "/vehicles/{id}", options);
    },

    /** Supprimer un vehicule */
    deleteVehiclesById5(options = {}) {
      return request("DELETE", "/vehicles/{id}", options);
    },

    /** Supprimer un vehicule */
    deleteVehiclesById6(options = {}) {
      return request("DELETE", "/vehicles/{id}", options);
    },

    /** Supprimer un vehicule */
    deleteVehiclesById7(options = {}) {
      return request("DELETE", "/vehicles/{id}", options);
    },

    /** Supprimer un vehicule */
    deleteVehiclesById8(options = {}) {
      return request("DELETE", "/vehicles/{id}", options);
    },

    /** Detail d'un vehicule */
    getVehiclesById(options = {}) {
      return request("GET", "/vehicles/{id}", options);
    },

    /** Detail d'un vehicule */
    getVehiclesById2(options = {}) {
      return request("GET", "/vehicles/{id}", options);
    },

    /** Detail d'un vehicule */
    getVehiclesById3(options = {}) {
      return request("GET", "/vehicles/{id}", options);
    },

    /** Detail d'un vehicule */
    getVehiclesById4(options = {}) {
      return request("GET", "/vehicles/{id}", options);
    },

    /** Detail d'un vehicule */
    getVehiclesById5(options = {}) {
      return request("GET", "/vehicles/{id}", options);
    },

    /** Detail d'un vehicule */
    getVehiclesById6(options = {}) {
      return request("GET", "/vehicles/{id}", options);
    },

    /** Detail d'un vehicule */
    getVehiclesById7(options = {}) {
      return request("GET", "/vehicles/{id}", options);
    },

    /** Detail d'un vehicule */
    getVehiclesById8(options = {}) {
      return request("GET", "/vehicles/{id}", options);
    },

    /** Modifier un vehicule */
    putVehiclesById(options = {}) {
      return request("PUT", "/vehicles/{id}", options);
    },

    /** Modifier un vehicule */
    putVehiclesById2(options = {}) {
      return request("PUT", "/vehicles/{id}", options);
    },

    /** Modifier un vehicule */
    putVehiclesById3(options = {}) {
      return request("PUT", "/vehicles/{id}", options);
    },

    /** Modifier un vehicule */
    putVehiclesById4(options = {}) {
      return request("PUT", "/vehicles/{id}", options);
    },

    /** Modifier un vehicule */
    putVehiclesById5(options = {}) {
      return request("PUT", "/vehicles/{id}", options);
    },

    /** Modifier un vehicule */
    putVehiclesById6(options = {}) {
      return request("PUT", "/vehicles/{id}", options);
    },

    /** Modifier un vehicule */
    putVehiclesById7(options = {}) {
      return request("PUT", "/vehicles/{id}", options);
    },

    /** Modifier un vehicule */
    putVehiclesById8(options = {}) {
      return request("PUT", "/vehicles/{id}", options);
    },

    /** Alertes d'un vehicule */
    getVehiclesByIdAlerts(options = {}) {
      return request("GET", "/vehicles/{id}/alerts", options);
    },

    /** Alertes d'un vehicule */
    getVehiclesByIdAlerts2(options = {}) {
      return request("GET", "/vehicles/{id}/alerts", options);
    },

    /** Alertes d'un vehicule */
    getVehiclesByIdAlerts3(options = {}) {
      return request("GET", "/vehicles/{id}/alerts", options);
    },

    /** Alertes d'un vehicule */
    getVehiclesByIdAlerts4(options = {}) {
      return request("GET", "/vehicles/{id}/alerts", options);
    },

    /** Alertes d'un vehicule */
    getVehiclesByIdAlerts5(options = {}) {
      return request("GET", "/vehicles/{id}/alerts", options);
    },

    /** Alertes d'un vehicule */
    getVehiclesByIdAlerts6(options = {}) {
      return request("GET", "/vehicles/{id}/alerts", options);
    },

    /** Alertes d'un vehicule */
    getVehiclesByIdAlerts7(options = {}) {
      return request("GET", "/vehicles/{id}/alerts", options);
    },

    /** Alertes d'un vehicule */
    getVehiclesByIdAlerts8(options = {}) {
      return request("GET", "/vehicles/{id}/alerts", options);
    },

    /** Assigner un vehicule a un employe */
    postVehiclesByIdAssign(options = {}) {
      return request("POST", "/vehicles/{id}/assign", options);
    },

    /** Assigner un vehicule a un employe */
    postVehiclesByIdAssign2(options = {}) {
      return request("POST", "/vehicles/{id}/assign", options);
    },

    /** Assigner un vehicule a un employe */
    postVehiclesByIdAssign3(options = {}) {
      return request("POST", "/vehicles/{id}/assign", options);
    },

    /** Assigner un vehicule a un employe */
    postVehiclesByIdAssign4(options = {}) {
      return request("POST", "/vehicles/{id}/assign", options);
    },

    /** Assigner un vehicule a un employe */
    postVehiclesByIdAssign5(options = {}) {
      return request("POST", "/vehicles/{id}/assign", options);
    },

    /** Assigner un vehicule a un employe */
    postVehiclesByIdAssign6(options = {}) {
      return request("POST", "/vehicles/{id}/assign", options);
    },

    /** Assigner un vehicule a un employe */
    postVehiclesByIdAssign7(options = {}) {
      return request("POST", "/vehicles/{id}/assign", options);
    },

    /** Assigner un vehicule a un employe */
    postVehiclesByIdAssign8(options = {}) {
      return request("POST", "/vehicles/{id}/assign", options);
    },

    /** Historique des affectations conducteur */
    getVehiclesByIdAssignments(options = {}) {
      return request("GET", "/vehicles/{id}/assignments", options);
    },

    /** Historique des affectations conducteur */
    getVehiclesByIdAssignments2(options = {}) {
      return request("GET", "/vehicles/{id}/assignments", options);
    },

    /** Historique des affectations conducteur */
    getVehiclesByIdAssignments3(options = {}) {
      return request("GET", "/vehicles/{id}/assignments", options);
    },

    /** Historique des affectations conducteur */
    getVehiclesByIdAssignments4(options = {}) {
      return request("GET", "/vehicles/{id}/assignments", options);
    },

    /** Historique des affectations conducteur */
    getVehiclesByIdAssignments5(options = {}) {
      return request("GET", "/vehicles/{id}/assignments", options);
    },

    /** Historique des affectations conducteur */
    getVehiclesByIdAssignments6(options = {}) {
      return request("GET", "/vehicles/{id}/assignments", options);
    },

    /** Historique des affectations conducteur */
    getVehiclesByIdAssignments7(options = {}) {
      return request("GET", "/vehicles/{id}/assignments", options);
    },

    /** Historique des affectations conducteur */
    getVehiclesByIdAssignments8(options = {}) {
      return request("GET", "/vehicles/{id}/assignments", options);
    },

    /** Historique maintenance d'un vehicule */
    getVehiclesByIdMaintenance(options = {}) {
      return request("GET", "/vehicles/{id}/maintenance", options);
    },

    /** Historique maintenance d'un vehicule */
    getVehiclesByIdMaintenance2(options = {}) {
      return request("GET", "/vehicles/{id}/maintenance", options);
    },

    /** Historique maintenance d'un vehicule */
    getVehiclesByIdMaintenance3(options = {}) {
      return request("GET", "/vehicles/{id}/maintenance", options);
    },

    /** Historique maintenance d'un vehicule */
    getVehiclesByIdMaintenance4(options = {}) {
      return request("GET", "/vehicles/{id}/maintenance", options);
    },

    /** Historique maintenance d'un vehicule */
    getVehiclesByIdMaintenance5(options = {}) {
      return request("GET", "/vehicles/{id}/maintenance", options);
    },

    /** Historique maintenance d'un vehicule */
    getVehiclesByIdMaintenance6(options = {}) {
      return request("GET", "/vehicles/{id}/maintenance", options);
    },

    /** Historique maintenance d'un vehicule */
    getVehiclesByIdMaintenance7(options = {}) {
      return request("GET", "/vehicles/{id}/maintenance", options);
    },

    /** Historique maintenance d'un vehicule */
    getVehiclesByIdMaintenance8(options = {}) {
      return request("GET", "/vehicles/{id}/maintenance", options);
    },

    /** Position actuelle du vehicule (via Traccar) */
    getVehiclesByIdPosition(options = {}) {
      return request("GET", "/vehicles/{id}/position", options);
    },

    /** Position actuelle du vehicule (via Traccar) */
    getVehiclesByIdPosition2(options = {}) {
      return request("GET", "/vehicles/{id}/position", options);
    },

    /** Position actuelle du vehicule (via Traccar) */
    getVehiclesByIdPosition3(options = {}) {
      return request("GET", "/vehicles/{id}/position", options);
    },

    /** Position actuelle du vehicule (via Traccar) */
    getVehiclesByIdPosition4(options = {}) {
      return request("GET", "/vehicles/{id}/position", options);
    },

    /** Position actuelle du vehicule (via Traccar) */
    getVehiclesByIdPosition5(options = {}) {
      return request("GET", "/vehicles/{id}/position", options);
    },

    /** Position actuelle du vehicule (via Traccar) */
    getVehiclesByIdPosition6(options = {}) {
      return request("GET", "/vehicles/{id}/position", options);
    },

    /** Position actuelle du vehicule (via Traccar) */
    getVehiclesByIdPosition7(options = {}) {
      return request("GET", "/vehicles/{id}/position", options);
    },

    /** Position actuelle du vehicule (via Traccar) */
    getVehiclesByIdPosition8(options = {}) {
      return request("GET", "/vehicles/{id}/position", options);
    },

    /** Trajets d'un vehicule */
    getVehiclesByIdTrips(options = {}) {
      return request("GET", "/vehicles/{id}/trips", options);
    },

    /** Trajets d'un vehicule */
    getVehiclesByIdTrips2(options = {}) {
      return request("GET", "/vehicles/{id}/trips", options);
    },

    /** Trajets d'un vehicule */
    getVehiclesByIdTrips3(options = {}) {
      return request("GET", "/vehicles/{id}/trips", options);
    },

    /** Trajets d'un vehicule */
    getVehiclesByIdTrips4(options = {}) {
      return request("GET", "/vehicles/{id}/trips", options);
    },

    /** Trajets d'un vehicule */
    getVehiclesByIdTrips5(options = {}) {
      return request("GET", "/vehicles/{id}/trips", options);
    },

    /** Trajets d'un vehicule */
    getVehiclesByIdTrips6(options = {}) {
      return request("GET", "/vehicles/{id}/trips", options);
    },

    /** Trajets d'un vehicule */
    getVehiclesByIdTrips7(options = {}) {
      return request("GET", "/vehicles/{id}/trips", options);
    },

    /** Trajets d'un vehicule */
    getVehiclesByIdTrips8(options = {}) {
      return request("GET", "/vehicles/{id}/trips", options);
    },

    /** Desassigner un vehicule */
    postVehiclesByIdUnassign(options = {}) {
      return request("POST", "/vehicles/{id}/unassign", options);
    },

    /** Desassigner un vehicule */
    postVehiclesByIdUnassign2(options = {}) {
      return request("POST", "/vehicles/{id}/unassign", options);
    },

    /** Desassigner un vehicule */
    postVehiclesByIdUnassign3(options = {}) {
      return request("POST", "/vehicles/{id}/unassign", options);
    },

    /** Desassigner un vehicule */
    postVehiclesByIdUnassign4(options = {}) {
      return request("POST", "/vehicles/{id}/unassign", options);
    },

    /** Desassigner un vehicule */
    postVehiclesByIdUnassign5(options = {}) {
      return request("POST", "/vehicles/{id}/unassign", options);
    },

    /** Desassigner un vehicule */
    postVehiclesByIdUnassign6(options = {}) {
      return request("POST", "/vehicles/{id}/unassign", options);
    },

    /** Desassigner un vehicule */
    postVehiclesByIdUnassign7(options = {}) {
      return request("POST", "/vehicles/{id}/unassign", options);
    },

    /** Desassigner un vehicule */
    postVehiclesByIdUnassign8(options = {}) {
      return request("POST", "/vehicles/{id}/unassign", options);
    },

    /** Viewer public par token */
    getViewCam(options = {}) {
      return request("GET", "/view/cam", options);
    },

    /** Viewer public par token */
    getViewCam2(options = {}) {
      return request("GET", "/view/cam", options);
    },

    /** Viewer public par token */
    getViewCam3(options = {}) {
      return request("GET", "/view/cam", options);
    },

    /** Viewer public par token */
    getViewCam4(options = {}) {
      return request("GET", "/view/cam", options);
    },

    /** Viewer public par token */
    getViewCam5(options = {}) {
      return request("GET", "/view/cam", options);
    },

    /** Viewer public par token */
    getViewCam6(options = {}) {
      return request("GET", "/view/cam", options);
    },

    /** Viewer public par token */
    getViewCam7(options = {}) {
      return request("GET", "/view/cam", options);
    },

    /** Viewer public par token */
    getViewCam8(options = {}) {
      return request("GET", "/view/cam", options);
    },

    /** Lister les webhooks de l'entreprise */
    getWebhooks(options = {}) {
      return request("GET", "/webhooks", options);
    },

    /** Lister les webhooks de l'entreprise */
    getWebhooks2(options = {}) {
      return request("GET", "/webhooks", options);
    },

    /** Lister les webhooks de l'entreprise */
    getWebhooks3(options = {}) {
      return request("GET", "/webhooks", options);
    },

    /** Lister les webhooks de l'entreprise */
    getWebhooks4(options = {}) {
      return request("GET", "/webhooks", options);
    },

    /** Lister les webhooks de l'entreprise */
    getWebhooks5(options = {}) {
      return request("GET", "/webhooks", options);
    },

    /** Lister les webhooks de l'entreprise */
    getWebhooks6(options = {}) {
      return request("GET", "/webhooks", options);
    },

    /** Lister les webhooks de l'entreprise */
    getWebhooks7(options = {}) {
      return request("GET", "/webhooks", options);
    },

    /** Lister les webhooks de l'entreprise */
    getWebhooks8(options = {}) {
      return request("GET", "/webhooks", options);
    },

    /** Lister les webhooks de l'entreprise */
    getWebhooks9(options = {}) {
      return request("GET", "/webhooks", options);
    },

    /** Lister les webhooks de l'entreprise */
    getWebhooks10(options = {}) {
      return request("GET", "/webhooks", options);
    },

    /** Lister les webhooks de l'entreprise */
    getWebhooks11(options = {}) {
      return request("GET", "/webhooks", options);
    },

    /** Creer un webhook */
    postWebhooks(options = {}) {
      return request("POST", "/webhooks", options);
    },

    /** Creer un webhook */
    postWebhooks2(options = {}) {
      return request("POST", "/webhooks", options);
    },

    /** Creer un webhook */
    postWebhooks3(options = {}) {
      return request("POST", "/webhooks", options);
    },

    /** Creer un webhook */
    postWebhooks4(options = {}) {
      return request("POST", "/webhooks", options);
    },

    /** Creer un webhook */
    postWebhooks5(options = {}) {
      return request("POST", "/webhooks", options);
    },

    /** Creer un webhook */
    postWebhooks6(options = {}) {
      return request("POST", "/webhooks", options);
    },

    /** Creer un webhook */
    postWebhooks7(options = {}) {
      return request("POST", "/webhooks", options);
    },

    /** Creer un webhook */
    postWebhooks8(options = {}) {
      return request("POST", "/webhooks", options);
    },

    /** Creer un webhook */
    postWebhooks9(options = {}) {
      return request("POST", "/webhooks", options);
    },

    /** Creer un webhook */
    postWebhooks10(options = {}) {
      return request("POST", "/webhooks", options);
    },

    /** Creer un webhook */
    postWebhooks11(options = {}) {
      return request("POST", "/webhooks", options);
    },

    /** Supprimer un webhook */
    deleteWebhooksByWebhook(options = {}) {
      return request("DELETE", "/webhooks/{webhook}", options);
    },

    /** Supprimer un webhook */
    deleteWebhooksByWebhook2(options = {}) {
      return request("DELETE", "/webhooks/{webhook}", options);
    },

    /** Supprimer un webhook */
    deleteWebhooksByWebhook3(options = {}) {
      return request("DELETE", "/webhooks/{webhook}", options);
    },

    /** Supprimer un webhook */
    deleteWebhooksByWebhook4(options = {}) {
      return request("DELETE", "/webhooks/{webhook}", options);
    },

    /** Supprimer un webhook */
    deleteWebhooksByWebhook5(options = {}) {
      return request("DELETE", "/webhooks/{webhook}", options);
    },

    /** Supprimer un webhook */
    deleteWebhooksByWebhook6(options = {}) {
      return request("DELETE", "/webhooks/{webhook}", options);
    },

    /** Supprimer un webhook */
    deleteWebhooksByWebhook7(options = {}) {
      return request("DELETE", "/webhooks/{webhook}", options);
    },

    /** Supprimer un webhook */
    deleteWebhooksByWebhook8(options = {}) {
      return request("DELETE", "/webhooks/{webhook}", options);
    },

    /** Supprimer un webhook */
    deleteWebhooksByWebhook9(options = {}) {
      return request("DELETE", "/webhooks/{webhook}", options);
    },

    /** Supprimer un webhook */
    deleteWebhooksByWebhook10(options = {}) {
      return request("DELETE", "/webhooks/{webhook}", options);
    },

    /** Supprimer un webhook */
    deleteWebhooksByWebhook11(options = {}) {
      return request("DELETE", "/webhooks/{webhook}", options);
    },

    /** Voir un webhook et ses dernieres livraisons */
    getWebhooksByWebhook(options = {}) {
      return request("GET", "/webhooks/{webhook}", options);
    },

    /** Voir un webhook et ses dernieres livraisons */
    getWebhooksByWebhook2(options = {}) {
      return request("GET", "/webhooks/{webhook}", options);
    },

    /** Voir un webhook et ses dernieres livraisons */
    getWebhooksByWebhook3(options = {}) {
      return request("GET", "/webhooks/{webhook}", options);
    },

    /** Voir un webhook et ses dernieres livraisons */
    getWebhooksByWebhook4(options = {}) {
      return request("GET", "/webhooks/{webhook}", options);
    },

    /** Voir un webhook et ses dernieres livraisons */
    getWebhooksByWebhook5(options = {}) {
      return request("GET", "/webhooks/{webhook}", options);
    },

    /** Voir un webhook et ses dernieres livraisons */
    getWebhooksByWebhook6(options = {}) {
      return request("GET", "/webhooks/{webhook}", options);
    },

    /** Voir un webhook et ses dernieres livraisons */
    getWebhooksByWebhook7(options = {}) {
      return request("GET", "/webhooks/{webhook}", options);
    },

    /** Voir un webhook et ses dernieres livraisons */
    getWebhooksByWebhook8(options = {}) {
      return request("GET", "/webhooks/{webhook}", options);
    },

    /** Voir un webhook et ses dernieres livraisons */
    getWebhooksByWebhook9(options = {}) {
      return request("GET", "/webhooks/{webhook}", options);
    },

    /** Voir un webhook et ses dernieres livraisons */
    getWebhooksByWebhook10(options = {}) {
      return request("GET", "/webhooks/{webhook}", options);
    },

    /** Voir un webhook et ses dernieres livraisons */
    getWebhooksByWebhook11(options = {}) {
      return request("GET", "/webhooks/{webhook}", options);
    },

    /** Modifier un webhook */
    putWebhooksByWebhook(options = {}) {
      return request("PUT", "/webhooks/{webhook}", options);
    },

    /** Modifier un webhook */
    putWebhooksByWebhook2(options = {}) {
      return request("PUT", "/webhooks/{webhook}", options);
    },

    /** Modifier un webhook */
    putWebhooksByWebhook3(options = {}) {
      return request("PUT", "/webhooks/{webhook}", options);
    },

    /** Modifier un webhook */
    putWebhooksByWebhook4(options = {}) {
      return request("PUT", "/webhooks/{webhook}", options);
    },

    /** Modifier un webhook */
    putWebhooksByWebhook5(options = {}) {
      return request("PUT", "/webhooks/{webhook}", options);
    },

    /** Modifier un webhook */
    putWebhooksByWebhook6(options = {}) {
      return request("PUT", "/webhooks/{webhook}", options);
    },

    /** Modifier un webhook */
    putWebhooksByWebhook7(options = {}) {
      return request("PUT", "/webhooks/{webhook}", options);
    },

    /** Modifier un webhook */
    putWebhooksByWebhook8(options = {}) {
      return request("PUT", "/webhooks/{webhook}", options);
    },

    /** Modifier un webhook */
    putWebhooksByWebhook9(options = {}) {
      return request("PUT", "/webhooks/{webhook}", options);
    },

    /** Modifier un webhook */
    putWebhooksByWebhook10(options = {}) {
      return request("PUT", "/webhooks/{webhook}", options);
    },

    /** Modifier un webhook */
    putWebhooksByWebhook11(options = {}) {
      return request("PUT", "/webhooks/{webhook}", options);
    },

    /** Lister les livraisons webhook dead-letter (retries epuises) */
    getWebhooksByWebhookDeadLetters(options = {}) {
      return request("GET", "/webhooks/{webhook}/dead-letters", options);
    },

    /** Lister les livraisons webhook dead-letter (retries epuises) */
    getWebhooksByWebhookDeadLetters2(options = {}) {
      return request("GET", "/webhooks/{webhook}/dead-letters", options);
    },

    /** Lister les livraisons webhook dead-letter (retries epuises) */
    getWebhooksByWebhookDeadLetters3(options = {}) {
      return request("GET", "/webhooks/{webhook}/dead-letters", options);
    },

    /** Lister les livraisons webhook dead-letter (retries epuises) */
    getWebhooksByWebhookDeadLetters4(options = {}) {
      return request("GET", "/webhooks/{webhook}/dead-letters", options);
    },

    /** Lister les livraisons webhook dead-letter (retries epuises) */
    getWebhooksByWebhookDeadLetters5(options = {}) {
      return request("GET", "/webhooks/{webhook}/dead-letters", options);
    },

    /** Lister les livraisons webhook dead-letter (retries epuises) */
    getWebhooksByWebhookDeadLetters6(options = {}) {
      return request("GET", "/webhooks/{webhook}/dead-letters", options);
    },

    /** Lister les livraisons webhook dead-letter (retries epuises) */
    getWebhooksByWebhookDeadLetters7(options = {}) {
      return request("GET", "/webhooks/{webhook}/dead-letters", options);
    },

    /** Lister les livraisons webhook dead-letter (retries epuises) */
    getWebhooksByWebhookDeadLetters8(options = {}) {
      return request("GET", "/webhooks/{webhook}/dead-letters", options);
    },

    /** Lister les livraisons webhook dead-letter (retries epuises) */
    getWebhooksByWebhookDeadLetters9(options = {}) {
      return request("GET", "/webhooks/{webhook}/dead-letters", options);
    },

    /** Lister les livraisons webhook dead-letter (retries epuises) */
    getWebhooksByWebhookDeadLetters10(options = {}) {
      return request("GET", "/webhooks/{webhook}/dead-letters", options);
    },

    /** Lister les livraisons webhook dead-letter (retries epuises) */
    getWebhooksByWebhookDeadLetters11(options = {}) {
      return request("GET", "/webhooks/{webhook}/dead-letters", options);
    },

    /** Rejouer une livraison webhook dead-letter */
    postWebhooksByWebhookDeadLettersByDeliveryReplay(options = {}) {
      return request("POST", "/webhooks/{webhook}/dead-letters/{delivery}/replay", options);
    },

    /** Rejouer une livraison webhook dead-letter */
    postWebhooksByWebhookDeadLettersByDeliveryReplay2(options = {}) {
      return request("POST", "/webhooks/{webhook}/dead-letters/{delivery}/replay", options);
    },

    /** Rejouer une livraison webhook dead-letter */
    postWebhooksByWebhookDeadLettersByDeliveryReplay3(options = {}) {
      return request("POST", "/webhooks/{webhook}/dead-letters/{delivery}/replay", options);
    },

    /** Rejouer une livraison webhook dead-letter */
    postWebhooksByWebhookDeadLettersByDeliveryReplay4(options = {}) {
      return request("POST", "/webhooks/{webhook}/dead-letters/{delivery}/replay", options);
    },

    /** Rejouer une livraison webhook dead-letter */
    postWebhooksByWebhookDeadLettersByDeliveryReplay5(options = {}) {
      return request("POST", "/webhooks/{webhook}/dead-letters/{delivery}/replay", options);
    },

    /** Rejouer une livraison webhook dead-letter */
    postWebhooksByWebhookDeadLettersByDeliveryReplay6(options = {}) {
      return request("POST", "/webhooks/{webhook}/dead-letters/{delivery}/replay", options);
    },

    /** Rejouer une livraison webhook dead-letter */
    postWebhooksByWebhookDeadLettersByDeliveryReplay7(options = {}) {
      return request("POST", "/webhooks/{webhook}/dead-letters/{delivery}/replay", options);
    },

    /** Rejouer une livraison webhook dead-letter */
    postWebhooksByWebhookDeadLettersByDeliveryReplay8(options = {}) {
      return request("POST", "/webhooks/{webhook}/dead-letters/{delivery}/replay", options);
    },

    /** Rejouer une livraison webhook dead-letter */
    postWebhooksByWebhookDeadLettersByDeliveryReplay9(options = {}) {
      return request("POST", "/webhooks/{webhook}/dead-letters/{delivery}/replay", options);
    },

    /** Rejouer une livraison webhook dead-letter */
    postWebhooksByWebhookDeadLettersByDeliveryReplay10(options = {}) {
      return request("POST", "/webhooks/{webhook}/dead-letters/{delivery}/replay", options);
    },

    /** Rejouer une livraison webhook dead-letter */
    postWebhooksByWebhookDeadLettersByDeliveryReplay11(options = {}) {
      return request("POST", "/webhooks/{webhook}/dead-letters/{delivery}/replay", options);
    },

    /** Tester un endpoint webhook */
    postWebhooksByWebhookTest(options = {}) {
      return request("POST", "/webhooks/{webhook}/test", options);
    },

    /** Tester un endpoint webhook */
    postWebhooksByWebhookTest2(options = {}) {
      return request("POST", "/webhooks/{webhook}/test", options);
    },

    /** Tester un endpoint webhook */
    postWebhooksByWebhookTest3(options = {}) {
      return request("POST", "/webhooks/{webhook}/test", options);
    },

    /** Tester un endpoint webhook */
    postWebhooksByWebhookTest4(options = {}) {
      return request("POST", "/webhooks/{webhook}/test", options);
    },

    /** Tester un endpoint webhook */
    postWebhooksByWebhookTest5(options = {}) {
      return request("POST", "/webhooks/{webhook}/test", options);
    },

    /** Tester un endpoint webhook */
    postWebhooksByWebhookTest6(options = {}) {
      return request("POST", "/webhooks/{webhook}/test", options);
    },

    /** Tester un endpoint webhook */
    postWebhooksByWebhookTest7(options = {}) {
      return request("POST", "/webhooks/{webhook}/test", options);
    },

    /** Tester un endpoint webhook */
    postWebhooksByWebhookTest8(options = {}) {
      return request("POST", "/webhooks/{webhook}/test", options);
    },

    /** Tester un endpoint webhook */
    postWebhooksByWebhookTest9(options = {}) {
      return request("POST", "/webhooks/{webhook}/test", options);
    },

    /** Tester un endpoint webhook */
    postWebhooksByWebhookTest10(options = {}) {
      return request("POST", "/webhooks/{webhook}/test", options);
    },

    /** Tester un endpoint webhook */
    postWebhooksByWebhookTest11(options = {}) {
      return request("POST", "/webhooks/{webhook}/test", options);
    },

    /** Webhook Chargily Pay (paiements CEMAC) */
    postWebhooksChargily(options = {}) {
      return request("POST", "/webhooks/chargily", options);
    },

    /** Webhook Chargily Pay (paiements CEMAC) */
    postWebhooksChargily2(options = {}) {
      return request("POST", "/webhooks/chargily", options);
    },

    /** Webhook Chargily Pay (paiements CEMAC) */
    postWebhooksChargily3(options = {}) {
      return request("POST", "/webhooks/chargily", options);
    },

    /** Webhook Chargily Pay (paiements CEMAC) */
    postWebhooksChargily4(options = {}) {
      return request("POST", "/webhooks/chargily", options);
    },

    /** Webhook Chargily Pay (paiements CEMAC) */
    postWebhooksChargily5(options = {}) {
      return request("POST", "/webhooks/chargily", options);
    },

    /** Webhook de bounce email (SES/SendGrid) — fail-closed si secret absent */
    postWebhooksEmailBounce(options = {}) {
      return request("POST", "/webhooks/email-bounce", options);
    },

    /** Webhook de bounce email (SES/SendGrid) — fail-closed si secret absent */
    postWebhooksEmailBounce2(options = {}) {
      return request("POST", "/webhooks/email-bounce", options);
    },

    /** Webhook de bounce email (SES/SendGrid) — fail-closed si secret absent */
    postWebhooksEmailBounce3(options = {}) {
      return request("POST", "/webhooks/email-bounce", options);
    },

    /** Webhook de bounce email (SES/SendGrid) — fail-closed si secret absent */
    postWebhooksEmailBounce4(options = {}) {
      return request("POST", "/webhooks/email-bounce", options);
    },

    /** Webhook de bounce email (SES/SendGrid) — fail-closed si secret absent */
    postWebhooksEmailBounce5(options = {}) {
      return request("POST", "/webhooks/email-bounce", options);
    },

    /** Lister les types d'evenements webhook disponibles */
    getWebhooksEvents(options = {}) {
      return request("GET", "/webhooks/events", options);
    },

    /** Lister les types d'evenements webhook disponibles */
    getWebhooksEvents2(options = {}) {
      return request("GET", "/webhooks/events", options);
    },

    /** Lister les types d'evenements webhook disponibles */
    getWebhooksEvents3(options = {}) {
      return request("GET", "/webhooks/events", options);
    },

    /** Lister les types d'evenements webhook disponibles */
    getWebhooksEvents4(options = {}) {
      return request("GET", "/webhooks/events", options);
    },

    /** Lister les types d'evenements webhook disponibles */
    getWebhooksEvents5(options = {}) {
      return request("GET", "/webhooks/events", options);
    },

    /** Lister les types d'evenements webhook disponibles */
    getWebhooksEvents6(options = {}) {
      return request("GET", "/webhooks/events", options);
    },

    /** Lister les types d'evenements webhook disponibles */
    getWebhooksEvents7(options = {}) {
      return request("GET", "/webhooks/events", options);
    },

    /** Lister les types d'evenements webhook disponibles */
    getWebhooksEvents8(options = {}) {
      return request("GET", "/webhooks/events", options);
    },

    /** Lister les types d'evenements webhook disponibles */
    getWebhooksEvents9(options = {}) {
      return request("GET", "/webhooks/events", options);
    },

    /** Lister les types d'evenements webhook disponibles */
    getWebhooksEvents10(options = {}) {
      return request("GET", "/webhooks/events", options);
    },

    /** Lister les types d'evenements webhook disponibles */
    getWebhooksEvents11(options = {}) {
      return request("GET", "/webhooks/events", options);
    },

    /** Webhook Stripe (événements de paiement/abonnement) */
    postWebhooksStripe(options = {}) {
      return request("POST", "/webhooks/stripe", options);
    },

    /** Webhook Stripe (événements de paiement/abonnement) */
    postWebhooksStripe2(options = {}) {
      return request("POST", "/webhooks/stripe", options);
    },

    /** Webhook Stripe (événements de paiement/abonnement) */
    postWebhooksStripe3(options = {}) {
      return request("POST", "/webhooks/stripe", options);
    },

    /** Webhook Stripe (événements de paiement/abonnement) */
    postWebhooksStripe4(options = {}) {
      return request("POST", "/webhooks/stripe", options);
    },

    /** Webhook Stripe (événements de paiement/abonnement) */
    postWebhooksStripe5(options = {}) {
      return request("POST", "/webhooks/stripe", options);
    },

    /** Lister les pointeuses ZKTeco de l'entreprise (manager) */
    listZktecoDevices(options = {}) {
      return request("GET", "/zkteco/devices", options);
    },

    /** Lister les pointeuses ZKTeco de l'entreprise (manager) */
    listZktecoDevices2(options = {}) {
      return request("GET", "/zkteco/devices", options);
    },

    /** Lister les pointeuses ZKTeco de l'entreprise (manager) */
    listZktecoDevices3(options = {}) {
      return request("GET", "/zkteco/devices", options);
    },

    /** Lister les pointeuses ZKTeco de l'entreprise (manager) */
    listZktecoDevices4(options = {}) {
      return request("GET", "/zkteco/devices", options);
    },

    /** Lister les pointeuses ZKTeco de l'entreprise (manager) */
    listZktecoDevices5(options = {}) {
      return request("GET", "/zkteco/devices", options);
    },

    /** Lister les pointeuses ZKTeco de l'entreprise (manager) */
    listZktecoDevices6(options = {}) {
      return request("GET", "/zkteco/devices", options);
    },

    /** Lister les pointeuses ZKTeco de l'entreprise (manager) */
    listZktecoDevices7(options = {}) {
      return request("GET", "/zkteco/devices", options);
    },

    /** Lister les pointeuses ZKTeco de l'entreprise (manager) */
    listZktecoDevices8(options = {}) {
      return request("GET", "/zkteco/devices", options);
    },

    /** Lister les pointeuses ZKTeco de l'entreprise (manager) */
    listZktecoDevices9(options = {}) {
      return request("GET", "/zkteco/devices", options);
    },

    /** Lister les pointeuses ZKTeco de l'entreprise (manager) */
    listZktecoDevices10(options = {}) {
      return request("GET", "/zkteco/devices", options);
    },

    /** Lister les pointeuses ZKTeco de l'entreprise (manager) */
    listZktecoDevices11(options = {}) {
      return request("GET", "/zkteco/devices", options);
    },

    /** Enregistrer une nouvelle pointeuse ZKTeco (manager) */
    registerZktecoDevice(options = {}) {
      return request("POST", "/zkteco/devices", options);
    },

    /** Enregistrer une nouvelle pointeuse ZKTeco (manager) */
    registerZktecoDevice2(options = {}) {
      return request("POST", "/zkteco/devices", options);
    },

    /** Enregistrer une nouvelle pointeuse ZKTeco (manager) */
    registerZktecoDevice3(options = {}) {
      return request("POST", "/zkteco/devices", options);
    },

    /** Enregistrer une nouvelle pointeuse ZKTeco (manager) */
    registerZktecoDevice4(options = {}) {
      return request("POST", "/zkteco/devices", options);
    },

    /** Enregistrer une nouvelle pointeuse ZKTeco (manager) */
    registerZktecoDevice5(options = {}) {
      return request("POST", "/zkteco/devices", options);
    },

    /** Enregistrer une nouvelle pointeuse ZKTeco (manager) */
    registerZktecoDevice6(options = {}) {
      return request("POST", "/zkteco/devices", options);
    },

    /** Enregistrer une nouvelle pointeuse ZKTeco (manager) */
    registerZktecoDevice7(options = {}) {
      return request("POST", "/zkteco/devices", options);
    },

    /** Enregistrer une nouvelle pointeuse ZKTeco (manager) */
    registerZktecoDevice8(options = {}) {
      return request("POST", "/zkteco/devices", options);
    },

    /** Enregistrer une nouvelle pointeuse ZKTeco (manager) */
    registerZktecoDevice9(options = {}) {
      return request("POST", "/zkteco/devices", options);
    },

    /** Enregistrer une nouvelle pointeuse ZKTeco (manager) */
    registerZktecoDevice10(options = {}) {
      return request("POST", "/zkteco/devices", options);
    },

    /** Enregistrer une nouvelle pointeuse ZKTeco (manager) */
    registerZktecoDevice11(options = {}) {
      return request("POST", "/zkteco/devices", options);
    },

    /** Supprimer une pointeuse ZKTeco (manager) */
    deleteZktecoDevice(options = {}) {
      return request("DELETE", "/zkteco/devices/{id}", options);
    },

    /** Supprimer une pointeuse ZKTeco (manager) */
    deleteZktecoDevice2(options = {}) {
      return request("DELETE", "/zkteco/devices/{id}", options);
    },

    /** Supprimer une pointeuse ZKTeco (manager) */
    deleteZktecoDevice3(options = {}) {
      return request("DELETE", "/zkteco/devices/{id}", options);
    },

    /** Supprimer une pointeuse ZKTeco (manager) */
    deleteZktecoDevice4(options = {}) {
      return request("DELETE", "/zkteco/devices/{id}", options);
    },

    /** Supprimer une pointeuse ZKTeco (manager) */
    deleteZktecoDevice5(options = {}) {
      return request("DELETE", "/zkteco/devices/{id}", options);
    },

    /** Supprimer une pointeuse ZKTeco (manager) */
    deleteZktecoDevice6(options = {}) {
      return request("DELETE", "/zkteco/devices/{id}", options);
    },

    /** Supprimer une pointeuse ZKTeco (manager) */
    deleteZktecoDevice7(options = {}) {
      return request("DELETE", "/zkteco/devices/{id}", options);
    },

    /** Supprimer une pointeuse ZKTeco (manager) */
    deleteZktecoDevice8(options = {}) {
      return request("DELETE", "/zkteco/devices/{id}", options);
    },

    /** Supprimer une pointeuse ZKTeco (manager) */
    deleteZktecoDevice9(options = {}) {
      return request("DELETE", "/zkteco/devices/{id}", options);
    },

    /** Supprimer une pointeuse ZKTeco (manager) */
    deleteZktecoDevice10(options = {}) {
      return request("DELETE", "/zkteco/devices/{id}", options);
    },

    /** Supprimer une pointeuse ZKTeco (manager) */
    deleteZktecoDevice11(options = {}) {
      return request("DELETE", "/zkteco/devices/{id}", options);
    },

    /** Voir une pointeuse ZKTeco avec son historique de synchronisation (manager) */
    showZktecoDevice(options = {}) {
      return request("GET", "/zkteco/devices/{id}", options);
    },

    /** Voir une pointeuse ZKTeco avec son historique de synchronisation (manager) */
    showZktecoDevice2(options = {}) {
      return request("GET", "/zkteco/devices/{id}", options);
    },

    /** Voir une pointeuse ZKTeco avec son historique de synchronisation (manager) */
    showZktecoDevice3(options = {}) {
      return request("GET", "/zkteco/devices/{id}", options);
    },

    /** Voir une pointeuse ZKTeco avec son historique de synchronisation (manager) */
    showZktecoDevice4(options = {}) {
      return request("GET", "/zkteco/devices/{id}", options);
    },

    /** Voir une pointeuse ZKTeco avec son historique de synchronisation (manager) */
    showZktecoDevice5(options = {}) {
      return request("GET", "/zkteco/devices/{id}", options);
    },

    /** Voir une pointeuse ZKTeco avec son historique de synchronisation (manager) */
    showZktecoDevice6(options = {}) {
      return request("GET", "/zkteco/devices/{id}", options);
    },

    /** Voir une pointeuse ZKTeco avec son historique de synchronisation (manager) */
    showZktecoDevice7(options = {}) {
      return request("GET", "/zkteco/devices/{id}", options);
    },

    /** Voir une pointeuse ZKTeco avec son historique de synchronisation (manager) */
    showZktecoDevice8(options = {}) {
      return request("GET", "/zkteco/devices/{id}", options);
    },

    /** Voir une pointeuse ZKTeco avec son historique de synchronisation (manager) */
    showZktecoDevice9(options = {}) {
      return request("GET", "/zkteco/devices/{id}", options);
    },

    /** Voir une pointeuse ZKTeco avec son historique de synchronisation (manager) */
    showZktecoDevice10(options = {}) {
      return request("GET", "/zkteco/devices/{id}", options);
    },

    /** Voir une pointeuse ZKTeco avec son historique de synchronisation (manager) */
    showZktecoDevice11(options = {}) {
      return request("GET", "/zkteco/devices/{id}", options);
    },

    /** Modifier une pointeuse ZKTeco (manager) */
    updateZktecoDevice(options = {}) {
      return request("PUT", "/zkteco/devices/{id}", options);
    },

    /** Modifier une pointeuse ZKTeco (manager) */
    updateZktecoDevice2(options = {}) {
      return request("PUT", "/zkteco/devices/{id}", options);
    },

    /** Modifier une pointeuse ZKTeco (manager) */
    updateZktecoDevice3(options = {}) {
      return request("PUT", "/zkteco/devices/{id}", options);
    },

    /** Modifier une pointeuse ZKTeco (manager) */
    updateZktecoDevice4(options = {}) {
      return request("PUT", "/zkteco/devices/{id}", options);
    },

    /** Modifier une pointeuse ZKTeco (manager) */
    updateZktecoDevice5(options = {}) {
      return request("PUT", "/zkteco/devices/{id}", options);
    },

    /** Modifier une pointeuse ZKTeco (manager) */
    updateZktecoDevice6(options = {}) {
      return request("PUT", "/zkteco/devices/{id}", options);
    },

    /** Modifier une pointeuse ZKTeco (manager) */
    updateZktecoDevice7(options = {}) {
      return request("PUT", "/zkteco/devices/{id}", options);
    },

    /** Modifier une pointeuse ZKTeco (manager) */
    updateZktecoDevice8(options = {}) {
      return request("PUT", "/zkteco/devices/{id}", options);
    },

    /** Modifier une pointeuse ZKTeco (manager) */
    updateZktecoDevice9(options = {}) {
      return request("PUT", "/zkteco/devices/{id}", options);
    },

    /** Modifier une pointeuse ZKTeco (manager) */
    updateZktecoDevice10(options = {}) {
      return request("PUT", "/zkteco/devices/{id}", options);
    },

    /** Modifier une pointeuse ZKTeco (manager) */
    updateZktecoDevice11(options = {}) {
      return request("PUT", "/zkteco/devices/{id}", options);
    },

    /** Regenerer le jeton d'une pointeuse (manager) */
    postZktecoDevicesByIdRegenerateToken(options = {}) {
      return request("POST", "/zkteco/devices/{id}/regenerate-token", options);
    },

    /** Regenerer le jeton d'une pointeuse (manager) */
    postZktecoDevicesByIdRegenerateToken2(options = {}) {
      return request("POST", "/zkteco/devices/{id}/regenerate-token", options);
    },

    /** Regenerer le jeton d'une pointeuse (manager) */
    postZktecoDevicesByIdRegenerateToken3(options = {}) {
      return request("POST", "/zkteco/devices/{id}/regenerate-token", options);
    },

    /** Regenerer le jeton d'une pointeuse (manager) */
    postZktecoDevicesByIdRegenerateToken4(options = {}) {
      return request("POST", "/zkteco/devices/{id}/regenerate-token", options);
    },

    /** Regenerer le jeton d'une pointeuse (manager) */
    postZktecoDevicesByIdRegenerateToken5(options = {}) {
      return request("POST", "/zkteco/devices/{id}/regenerate-token", options);
    },

    /** Regenerer le jeton d'une pointeuse (manager) */
    postZktecoDevicesByIdRegenerateToken6(options = {}) {
      return request("POST", "/zkteco/devices/{id}/regenerate-token", options);
    },

    /** Regenerer le jeton d'une pointeuse (manager) */
    postZktecoDevicesByIdRegenerateToken7(options = {}) {
      return request("POST", "/zkteco/devices/{id}/regenerate-token", options);
    },

    /** Regenerer le jeton d'une pointeuse (manager) */
    postZktecoDevicesByIdRegenerateToken8(options = {}) {
      return request("POST", "/zkteco/devices/{id}/regenerate-token", options);
    },

    /** Regenerer le jeton d'une pointeuse (manager) */
    postZktecoDevicesByIdRegenerateToken9(options = {}) {
      return request("POST", "/zkteco/devices/{id}/regenerate-token", options);
    },

    /** Regenerer le jeton d'une pointeuse (manager) */
    postZktecoDevicesByIdRegenerateToken10(options = {}) {
      return request("POST", "/zkteco/devices/{id}/regenerate-token", options);
    },

    /** Regenerer le jeton d'une pointeuse (manager) */
    postZktecoDevicesByIdRegenerateToken11(options = {}) {
      return request("POST", "/zkteco/devices/{id}/regenerate-token", options);
    },

    /** Lister l'historique de synchronisation d'une pointeuse (manager) */
    listZktecoSyncLogs(options = {}) {
      return request("GET", "/zkteco/devices/{id}/sync-logs", options);
    },

    /** Lister l'historique de synchronisation d'une pointeuse (manager) */
    listZktecoSyncLogs2(options = {}) {
      return request("GET", "/zkteco/devices/{id}/sync-logs", options);
    },

    /** Lister l'historique de synchronisation d'une pointeuse (manager) */
    listZktecoSyncLogs3(options = {}) {
      return request("GET", "/zkteco/devices/{id}/sync-logs", options);
    },

    /** Lister l'historique de synchronisation d'une pointeuse (manager) */
    listZktecoSyncLogs4(options = {}) {
      return request("GET", "/zkteco/devices/{id}/sync-logs", options);
    },

    /** Lister l'historique de synchronisation d'une pointeuse (manager) */
    listZktecoSyncLogs5(options = {}) {
      return request("GET", "/zkteco/devices/{id}/sync-logs", options);
    },

    /** Lister l'historique de synchronisation d'une pointeuse (manager) */
    listZktecoSyncLogs6(options = {}) {
      return request("GET", "/zkteco/devices/{id}/sync-logs", options);
    },

    /** Lister l'historique de synchronisation d'une pointeuse (manager) */
    listZktecoSyncLogs7(options = {}) {
      return request("GET", "/zkteco/devices/{id}/sync-logs", options);
    },

    /** Lister l'historique de synchronisation d'une pointeuse (manager) */
    listZktecoSyncLogs8(options = {}) {
      return request("GET", "/zkteco/devices/{id}/sync-logs", options);
    },

    /** Lister l'historique de synchronisation d'une pointeuse (manager) */
    listZktecoSyncLogs9(options = {}) {
      return request("GET", "/zkteco/devices/{id}/sync-logs", options);
    },

    /** Lister l'historique de synchronisation d'une pointeuse (manager) */
    listZktecoSyncLogs10(options = {}) {
      return request("GET", "/zkteco/devices/{id}/sync-logs", options);
    },

    /** Lister l'historique de synchronisation d'une pointeuse (manager) */
    listZktecoSyncLogs11(options = {}) {
      return request("GET", "/zkteco/devices/{id}/sync-logs", options);
    },

    /** Pousser la liste des employes vers la pointeuse (manager) */
    pushZktecoUsers(options = {}) {
      return request("POST", "/zkteco/devices/{serialNumber}/push-users", options);
    },

    /** Pousser la liste des employes vers la pointeuse (manager) */
    pushZktecoUsers2(options = {}) {
      return request("POST", "/zkteco/devices/{serialNumber}/push-users", options);
    },

    /** Pousser la liste des employes vers la pointeuse (manager) */
    pushZktecoUsers3(options = {}) {
      return request("POST", "/zkteco/devices/{serialNumber}/push-users", options);
    },

    /** Pousser la liste des employes vers la pointeuse (manager) */
    pushZktecoUsers4(options = {}) {
      return request("POST", "/zkteco/devices/{serialNumber}/push-users", options);
    },

    /** Pousser la liste des employes vers la pointeuse (manager) */
    pushZktecoUsers5(options = {}) {
      return request("POST", "/zkteco/devices/{serialNumber}/push-users", options);
    },

    /** Pousser la liste des employes vers la pointeuse (manager) */
    pushZktecoUsers6(options = {}) {
      return request("POST", "/zkteco/devices/{serialNumber}/push-users", options);
    },

    /** Pousser la liste des employes vers la pointeuse (manager) */
    pushZktecoUsers7(options = {}) {
      return request("POST", "/zkteco/devices/{serialNumber}/push-users", options);
    },

    /** Pousser la liste des employes vers la pointeuse (manager) */
    pushZktecoUsers8(options = {}) {
      return request("POST", "/zkteco/devices/{serialNumber}/push-users", options);
    },

    /** Pousser la liste des employes vers la pointeuse (manager) */
    pushZktecoUsers9(options = {}) {
      return request("POST", "/zkteco/devices/{serialNumber}/push-users", options);
    },

    /** Pousser la liste des employes vers la pointeuse (manager) */
    pushZktecoUsers10(options = {}) {
      return request("POST", "/zkteco/devices/{serialNumber}/push-users", options);
    },

    /** Pousser la liste des employes vers la pointeuse (manager) */
    pushZktecoUsers11(options = {}) {
      return request("POST", "/zkteco/devices/{serialNumber}/push-users", options);
    },

    /** Signal de vie envoye par la pointeuse elle-meme (pas d'authentification employe) */
    zktecoHeartbeat(options = {}) {
      return request("POST", "/zkteco/heartbeat/{serialNumber}", options);
    },

    /** Signal de vie envoye par la pointeuse elle-meme (pas d'authentification employe) */
    zktecoHeartbeat2(options = {}) {
      return request("POST", "/zkteco/heartbeat/{serialNumber}", options);
    },

    /** Signal de vie envoye par la pointeuse elle-meme (pas d'authentification employe) */
    zktecoHeartbeat3(options = {}) {
      return request("POST", "/zkteco/heartbeat/{serialNumber}", options);
    },

    /** Signal de vie envoye par la pointeuse elle-meme (pas d'authentification employe) */
    zktecoHeartbeat4(options = {}) {
      return request("POST", "/zkteco/heartbeat/{serialNumber}", options);
    },

    /** Signal de vie envoye par la pointeuse elle-meme (pas d'authentification employe) */
    zktecoHeartbeat5(options = {}) {
      return request("POST", "/zkteco/heartbeat/{serialNumber}", options);
    },

    /** Signal de vie envoye par la pointeuse elle-meme (pas d'authentification employe) */
    zktecoHeartbeat6(options = {}) {
      return request("POST", "/zkteco/heartbeat/{serialNumber}", options);
    },

    /** Signal de vie envoye par la pointeuse elle-meme (pas d'authentification employe) */
    zktecoHeartbeat7(options = {}) {
      return request("POST", "/zkteco/heartbeat/{serialNumber}", options);
    },

    /** Signal de vie envoye par la pointeuse elle-meme (pas d'authentification employe) */
    zktecoHeartbeat8(options = {}) {
      return request("POST", "/zkteco/heartbeat/{serialNumber}", options);
    },

    /** Signal de vie envoye par la pointeuse elle-meme (pas d'authentification employe) */
    zktecoHeartbeat9(options = {}) {
      return request("POST", "/zkteco/heartbeat/{serialNumber}", options);
    },

    /** Signal de vie envoye par la pointeuse elle-meme (pas d'authentification employe) */
    zktecoHeartbeat10(options = {}) {
      return request("POST", "/zkteco/heartbeat/{serialNumber}", options);
    },

    /** Signal de vie envoye par la pointeuse elle-meme (pas d'authentification employe) */
    zktecoHeartbeat11(options = {}) {
      return request("POST", "/zkteco/heartbeat/{serialNumber}", options);
    },

    /** Pousser des pointages bruts depuis l'appareil vers le serveur (appele par l'appareil) */
    zktecoSyncAttendance(options = {}) {
      return request("POST", "/zkteco/sync-attendance/{serialNumber}", options);
    },

    /** Pousser des pointages bruts depuis l'appareil vers le serveur (appele par l'appareil) */
    zktecoSyncAttendance2(options = {}) {
      return request("POST", "/zkteco/sync-attendance/{serialNumber}", options);
    },

    /** Pousser des pointages bruts depuis l'appareil vers le serveur (appele par l'appareil) */
    zktecoSyncAttendance3(options = {}) {
      return request("POST", "/zkteco/sync-attendance/{serialNumber}", options);
    },

    /** Pousser des pointages bruts depuis l'appareil vers le serveur (appele par l'appareil) */
    zktecoSyncAttendance4(options = {}) {
      return request("POST", "/zkteco/sync-attendance/{serialNumber}", options);
    },

    /** Pousser des pointages bruts depuis l'appareil vers le serveur (appele par l'appareil) */
    zktecoSyncAttendance5(options = {}) {
      return request("POST", "/zkteco/sync-attendance/{serialNumber}", options);
    },

    /** Pousser des pointages bruts depuis l'appareil vers le serveur (appele par l'appareil) */
    zktecoSyncAttendance6(options = {}) {
      return request("POST", "/zkteco/sync-attendance/{serialNumber}", options);
    },

    /** Pousser des pointages bruts depuis l'appareil vers le serveur (appele par l'appareil) */
    zktecoSyncAttendance7(options = {}) {
      return request("POST", "/zkteco/sync-attendance/{serialNumber}", options);
    },

    /** Pousser des pointages bruts depuis l'appareil vers le serveur (appele par l'appareil) */
    zktecoSyncAttendance8(options = {}) {
      return request("POST", "/zkteco/sync-attendance/{serialNumber}", options);
    },

    /** Pousser des pointages bruts depuis l'appareil vers le serveur (appele par l'appareil) */
    zktecoSyncAttendance9(options = {}) {
      return request("POST", "/zkteco/sync-attendance/{serialNumber}", options);
    },

    /** Pousser des pointages bruts depuis l'appareil vers le serveur (appele par l'appareil) */
    zktecoSyncAttendance10(options = {}) {
      return request("POST", "/zkteco/sync-attendance/{serialNumber}", options);
    },

    /** Pousser des pointages bruts depuis l'appareil vers le serveur (appele par l'appareil) */
    zktecoSyncAttendance11(options = {}) {
      return request("POST", "/zkteco/sync-attendance/{serialNumber}", options);
    }
  };
}

export default createLeopardoClient;

export const openApiVersion = "4.24.0";

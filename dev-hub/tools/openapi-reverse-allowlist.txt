# Allowlist inverse (issue #2181) : opérations OpenAPI documentées mais
# absentes des routes PHP.
#
# La passe inverse de check-openapi-route-coverage.py échoue quand une
# opération de openapi.yaml ne correspond à AUCUNE route Laravel réelle
# (un client suivant la spec recevrait un 404). Cette liste ne doit rester
# QUE pour des exclusions volontaires et justifiées (ex. endpoint documenté
# pour compat, route supprimée en attente de dépréciation).
#
# Format : une entrée par ligne « METHODE /chemin/normalisé » ({param}).
# Les lignes commençant par # sont des commentaires.
#
# État au 2026-08-14 (audit #2181) : 0 entrée — tous les chemins documentés
# existent dans les routes. Maintenir ce fichier vide ; toute nouvelle
# entrée doit être justifiée ici.
# 2026-09-05 (#6818) : GET /company-integration-requests documenté dans
# openapi.yaml mais route absente du routeur consolidé (lignée supprimée
# pendant la purge des merges — aucun consommateur). À purger lors de la
# régénération openapi.yaml (#6815).
GET /company-integration-requests
# 2026-09-23 (#7950 HOSP-008) : vitrine publique /stay documentée
# CONTRACT-FIRST depuis la spec SOLUTION_HOSPITALITY.md §6 (gel fondateur
# 2026-09-20). Les routes Laravel correspondantes sont livrées par HOSP-006
# (#7948, en cours sur un lot parallèle) — purger ces 5 entrées au merge de
# #7948 (la passe inverse signalera alors des entrées périmées, stale).

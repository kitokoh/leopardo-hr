# openapi-coverage-allowlist.txt — gaps documentés (exclusions volontaires)
#
# Règle (issue #1473) : toute route absente d'openapi.yaml ET de ce fichier
# fait échouer la CI. Ajouter une route ici UNIQUEMENT avec justification.
# ÉTAT 2026-08-18 : 744/744 routes couvertes — allowlist VIDE.
# Les verbes d'action PUT/PATCH dépréciés (#4930) sont désormais documentés
# dans openapi.yaml avec `deprecated: true` (tags [Deprecated]).

# CRM client — consentements (#5722) : contrat OpenAPI canonique publié par #5712
# (Périmètre CRM V0-08 « Publier les contrats API et OpenAPI du CRM client »).
GET /v1/crm/consents
GET /v1/crm/consents/{param}
POST /v1/crm/consents
POST /v1/crm/consents/{param}/revoke
# CRM client — segments (#5723) : contrat OpenAPI canonique publié par #5712
# (Périmètre CRM V0-08 « Publier les contrats API et OpenAPI du CRM client »).
GET /v1/crm/segments
GET /v1/crm/segments/{param}
GET /v1/crm/segments/{param}/members
POST /v1/crm/segments
PUT /v1/crm/segments/{param}
DELETE /v1/crm/segments/{param}
POST /v1/crm/segments/{param}/rebuild
# CRM client — campagnes (#5724) : contrat OpenAPI canonique publié par #5712
# (Périmètre CRM V0-08 « Publier les contrats API et OpenAPI du CRM client »).
GET /v1/crm/campaigns
GET /v1/crm/campaigns/{param}
GET /v1/crm/campaigns/{param}/report
POST /v1/crm/campaigns
PUT /v1/crm/campaigns/{param}
DELETE /v1/crm/campaigns/{param}
POST /v1/crm/campaigns/{param}/start
POST /v1/crm/campaigns/{param}/pause
POST /v1/crm/campaigns/{param}/resume
POST /v1/crm/campaigns/{param}/cancel
POST /v1/crm/campaigns/{param}/finish
# CRM client — canal email (#5726) : contrat OpenAPI canonique publié par #5712
# (Périmètre CRM V0-08 « Publier les contrats API et OpenAPI du CRM client »).
POST /v1/crm/email/transactional
POST /v1/crm/email/marketing
POST /v1/crm/email/webhook
POST /v1/crm/email/unsubscribe

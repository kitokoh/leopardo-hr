## Plan technique
1. `front/web/src/app/(landing)/checkout/success/page.tsx` : 3 href `DEFAULT_BACKEND_API_URL}/auth/login` → `/auth/login` ; `external: false` ; CTA principal → `<Link>`.
2. Lint (`npm run lint`) + build Next.js.
3. CHANGELOG.md + PR.

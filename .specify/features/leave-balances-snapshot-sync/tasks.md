# Tâches — Snapshot leave_balances synchronisé

- [ ] 1. Implémenter `syncLeaveBalanceSnapshot()` dans `AbsenceService` (helper privé + appel dans create/approve/reject/cancel).
- [ ] 2. Écrire `api/tests/Feature/Absences/LeaveBalancesSnapshotTest.php` (création, approbation, rejet pending/approved, annulation, non déductible, isolation tenant).
- [ ] 3. Mettre à jour `CHANGELOG.md` (`## [Unreleased]`).
- [ ] 4. PR `fix/2329-leave-balances-snapshot-sync` `Closes #2329`, CI verte, merge.

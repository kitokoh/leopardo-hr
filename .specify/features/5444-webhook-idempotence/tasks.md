# Tâches — #5444
1. Inventaire des 4 contrôleurs webhook (routes + signatures).
2. Migration `webhook_events` + service/trait idempotence.
3. Câblage Stripe + PaymentWebhook + EmailBounce + MarketingLead.
4. Tests Feature rejeu/fail-closed ×4 webhooks.
5. `docs/security/WEBHOOKS.md`.
6. PR `Closes #5444`.

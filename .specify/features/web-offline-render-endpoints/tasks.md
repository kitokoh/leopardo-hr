# Tasks — Web-offline cibles API (#3772)

## T1 — Documentation
- [x] `.env.example` : deux cibles documentées (nœud Edge local / Render), interdiction domaines réservés.
- [x] `README.md` : table des cibles + contrat `/api/v1/edge/health`.

## T2 — Validation
- [x] Aucun appel non versionné `/api/edge/*` dans `src/`.
- [x] Build Next OK.
- [x] CHANGELOG `### Fixed` (Closes #3772).

## Tâches
- [x] 1. Passer `base` à `/` dans `vite.config.js`.
- [x] 2. Ajouter `public/_redirects` (fallback SPA Pages).
- [x] 3. Build : assets absolus dans `dist/index.html`.
- [x] 4. CHANGELOG + PR `Closes #2334`, CI verte.

- [x] 5. Test Playwright `navigation-smoke.spec.js` : accès direct /companies → SPA servie (exigence #2334, ajouté dans la PR consolidée #2373).

# Tâches — #5437
1. Script `check-migration-prefixes.mjs` (extraction préfixes, comparaison inter-PR via API).
2. Tests unitaires `node --test` + fixtures (2 branches fictives en collision).
3. Workflow `migration-prefixes.yml` branché.
4. Docs : SCENARIOS_TEST_API_GITHUB_ACTIONS.md + AGENTS.md note.
5. PR `Closes #5437`.

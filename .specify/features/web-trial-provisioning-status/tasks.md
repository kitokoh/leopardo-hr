## Tâches
- [ ] 1. Route `/api/forms/signup` : pass-through `provisioning_token`.
- [ ] 2. Nouveau proxy `GET /api/forms/trial-status`.
- [ ] 3. Helper `fetchTrialStatus` dans `lib/forms.ts` + types.
- [ ] 4. `SignupForm` : step `provisioning` (polling 5 s × 12) + lien sur l'écran OTP + sessionStorage.
- [ ] 5. Tests composant (pending → ready, failed, timeout) + lint/tsc/build verts.
- [ ] 6. CHANGELOG + PR `Closes #2469`, CI verte.

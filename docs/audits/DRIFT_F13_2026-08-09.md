# F-13 — Migration des tests du noyau (paie/HR/présence) vers les vraies migrations

> Issue #1543 (F-13) — session 2026-08-09 (passe 4).
> Mesure générée par `dev-hub/tools/check-test-schema-drift.sh --report-f13`.

## Objectif (#1543)

Migrer les tests des modules **Payroll, HR, Attendance** du schéma manuel
`CreatesMvpSchema` (~2150 lignes, en dérive des 99 migrations) vers
`RefreshTenantDatabase` (vraies migrations `public` + `tenant`).

- Cible : ≥ 80 % des tests du noyau sur migrations réelles.
- Critère d'acceptation : drift du noyau = 0 table ; aucun test du noyau
  n'utilise plus le schéma manuel.

## Résultat mesuré (après cette passe + F-13b #1644)

```
F-13 (#1543) — tests du noyau sur vraies migrations :
  total=40 refresh=40 manual=0  =>  100% sur migrations réelles (cible ≥ 80%)
  OK Seuil F-13 atteint (≥ 80%).
```

| Module | Total | Refresh | Manuel (MVP) |
|---|---|---|---|
| Payroll | 16 | 16 | 0 |
| HR | 1 | 1 | 0 |
| Attendance | 13 | 13 | 0 |
| SmartAttendance | 5 | 5 | 0 |
| Contracts | 5 | 5 | 0 |
| **Total** | **40** | **40** | **0** |

## Travail de cette passe (16 fichiers Feature, racine `Tests\Feature`)

Tests paie/paiement encore sur `CreatesMvpSchema`, migrés vers
`RefreshTenantDatabase` :

- `PaySlipControllerTest`, `PaySlipPdfLocaleTest`
- `CotisationSimulationControllerTest`, `CotisationSimulationTest`
- `SalaryAdvanceProofTest`
- Exports/jobs paie : `BankExportControllerTest`, `GenerateBankExportJobTest`,
  `GeneratePaymentDocumentJobTest`, `GeneratePaymentDocumentJobNotificationTest`,
  `PaymentDocumentControllerTest`, `PaymentBatchControllerTest`,
  `BulkPaymentControllerTest`, `PaymentWebhookControllerTest`,
  `ProcessBulkPaymentJobTest`
- `SocialDeclarationControllerTest` (déclaration CNAS), `EmployeeIbanValidationTest`

Rappel : les tests `Payroll/` (Golden, `PayrollAnomalyServiceTest`,
`PayrollClosingTest`, `PayrollRunControllerTest`, `PayrollAccountingExportTest`,
etc.) étaient déjà sur `RefreshTenantDatabase` (passes 1-3, #1635/#1644).

## Drift de tables restant

`CreatesMvpSchema` contient toujours ~40 tables de moins que les migrations
(rapport `check-test-schema-drift.sh`), mais **plus aucune table manquante
n'impacte un test du noyau** : les tests restés sur le schéma manuel sont hors
périmètre F-13 (Security, Platform, Edge, Estimation, …). Conformément à la
règle F-13b, on ne retire les tables du schéma manuel qu'au fil de l'eau, après
migration du dernier utilisateur (voir `docs/audits/DRIFT_F13B_2026-08-09.md`).

## Régénérer la mesure

```bash
dev-hub/tools/check-test-schema-drift.sh --report-f13 api
```

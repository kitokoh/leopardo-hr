# 🐆 QA pass prod — vérification des fixes funnel #4947→#4955 (issue #5144)

**Date** : 2026-08-19 ~15:10-15:20 UTC · **Auteur** : session QA automatisée (plan 60 jours)
**Cible** : prod `gestionemployerbackend.onrender.com` (v4.24.0) · vitrine `gestionemployer-backend.vercel.app` · admin `leo-admin.pages.dev`
**Méthode** : tests HTTP live (curl), sans accès interne. Échantillons de test créés en prod (emails `qa-claw-*@emalupe.com`, tenant jamais provisionné).

---

## Synthèse exécutive

> ⚠️ **Le funnel prospect est de nouveau intégralement cassé en prod** — dans un mode différent du QA du 17/08 :
> le trial **guided** passe en `failed` (le worker tourne désormais, mais le job de provisioning échoue),
> et le trial **self-service (OTP)** répond **503 TRIAL_OTP_SEND_FAILED** (envoi d'email cassé).

| # | Vérification | Résultat | Référence |
|---|---|---|---|
| 1 | Santé API (DB/Redis/storage/queue) | ✅ OK | — |
| 2 | Trial **guided** : signup → `ready` | ❌ **`failed` 2/2** | **#5161 (P0)** |
| 3 | Trial **self_service** : signup → OTP | ❌ **503 TRIAL_OTP_SEND_FAILED** | **#5162 (P1)** |
| 4 | Checkout | ⚠️ 401 UNAUTHENTICATED (non testable sans tenant) | — |
| 5 | DNS `leopardo-rh.com` | ❌ HTTP 000 (NXDOMAIN) | #3452 |
| 6 | 429 localisé | ✅ « Trop de requêtes. Réessayez plus tard. » | #4955 ✅ |
| 7 | Pricing 14 vs 30 jours | ✅ « 14 jours » seul (17 occ., 0 « 30 ») | #4951 ✅ |
| 8 | Vitrine + admin | ✅ 200 / 200 | — |
| 9 | Création employé (POST /api/v1/employees) | ⚠️ **non testable** : nécessite un tenant, or le trial ne provisionne plus | #4947 (fermée) — à re-vérifier après #5161 |

---

## Détail

### 1. Santé API ✅
`GET /api/v1/health` → `status: ok`, `version: 4.24.0`, DB ok (234 ms), Redis ok (109 ms), storage ok, queue ok (driver redis, **`default: 1`** job — probablement le provisioning échoué de ce QA pass ou un retry). Uptime 1 s (instance fraîche — cold start attendu).

### 2. Trial guided ❌ — **#5161 (P0)**
- Signup : `200 {"success":true,"data":{"status":"provisioning_sandbox","provisioning_token":"…"}}` ✅
- Status après 8 s : `{"status":"failed","provisioned_at":null,"message":"La création de votre espace d'essai a échoué.…"}`
- **2/2 tentatives** (`qa-claw-20260819-1787152525@emalupe.com`, `qa-claw-1908-b-…@emalupe.com`)
- Lecture : le symptôme « pending » de #4948 est réglé (le job s'exécute) mais **le job échoue** — famille probable : envoi magic link (Mailgun, cf. #5139) ou provisioning tenant (schéma/search_path). Logs du worker requis.

### 3. Trial self-service / OTP ❌ — **#5162 (P1)**
- `POST /api/v1/trial/signup` (self_service explicite et flux par défaut) → **HTTP 503** `TRIAL_OTP_SEND_FAILED`, message localisé.
- Reproduit 13× en rafale (puis 429 attendu), 1× en essai isolé.
- Lecture : envoi d'email OTP cassé en prod — même famille que #5139 (transport Mailgun HTTP API / egress Render). Vérifier si le fix #5141 est déployé et si les variables `MAILGUN_*` sont présentes sur Render.

### 4. Checkout ⚠️
`POST /api/v1/billing/checkout` sans session → `401 UNAUTHENTICATED` (comportement fail-closed attendu). L'option retenue de #4952 (Stripe live vs CTA masqués) n'est pas vérifiable sans tenant authentifié — **à re-tester après #5161** (page `/checkout` + achat test).

### 5. DNS leopardo-rh.com ❌
`https://leopardo-rh.com` → HTTP 000 (NXDOMAIN, aucun enregistrement). **Toujours down depuis le 15/08** (#3452) — action humaine registrar requise.

### 6. 429 ✅
Après ~13 requêtes rapides : `TOO_MANY_REQUESTS` + `localized_message: "Trop de requêtes. Réessayez plus tard."` — le fix #4955 tient en prod.

### 7. Pricing ✅
Page `/pricing` (vitrine Vercel) : 17 occurrences « 14 jours », **0** « 30 jours » — #4951 tient.

### 8. Surfaces ✅
Vitrine 200 (0,8 s) · admin `leo-admin.pages.dev` 200 (0,16 s).

### 9. Création employé ⚠️ non testable
Nécessite un tenant authentifié ; aucun tenant ne peut être créé tant que #5161 n'est pas corrigé. La suite de tests HR (#4947) reste la preuve CI en attendant — à re-vérifier en live après le fix.

---

## Issues créées par ce pass
- **#5161** [P0] Trial GUIDED → `failed` (provisioning) — funnel principal mort
- **#5162** [P1] Trial SELF_SERVICE → 503 TRIAL_OTP_SEND_FAILED — envoi OTP cassé

## Actions recommandées
1. **#5161 (P0)** : logs `ProvisionDemoTenantJob` + config Mailgun/egress → fix + redéploiement ; critère : signup → `ready` < 2 min avec `login_url` fonctionnel
2. **#5162 (P1)** : vérifier déploiement du fix #5141 et variables `MAILGUN_*` → envoi OTP fonctionnel
3. **#3452** : action humaine registrar (DNS) — toujours bloquée
4. Re-run de ce QA pass (checklist complète) après les fixes, avec ajout des étapes 4 et 9

---
*Rapport généré depuis le QA pass #5144 — 2026-08-19. Méthode reproductible (curl, cf. corps des issues #5161/#5162).*

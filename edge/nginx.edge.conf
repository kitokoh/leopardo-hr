worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /tmp/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    sendfile      on;
    keepalive_timeout 65;

    client_max_body_size 20M;
fastcgi_read_timeout 300s;
fastcgi_send_timeout 300s;

server {
        # #7966 — port non privilégié (> 1024) pour exécution 100% non-root :
        # nginx (master inclus) tourne en www-data, sans cap NET_BIND_SERVICE.
        listen 8080;
        server_name _;
        root /var/www/html/public;
        index index.php;

        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }

        location ~ \.php$ {
            fastcgi_pass 127.0.0.1:9000;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
            include fastcgi_params;
        }

        # Issue #3592 : sonde réelle (PHP + SQLite via la route EdgeSync
        # /api/v1/edge/health) au lieu d'un 200 statique sur un chemin divergent.
        location /api/v1/edge/health {
            access_log off;
            try_files $uri $uri/ /index.php?$query_string;
        }
    }
}

# CRM client — Providers

ServiceProvider du module CRM client (tenant) — structure DDD imposée par le
Module Structure Validator (architecture-check.yml). Enregistré dans
`bootstrap/providers.php` (squelette #5707).

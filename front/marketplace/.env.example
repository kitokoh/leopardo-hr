# Marketplace — variables d'environnement (voir README.md)

# Base de l'API backend (sans /api/v1). Obligatoire en déploiement :
# ne pas compter sur le fallback code (issue #7963 — suppression prévue).
NEXT_PUBLIC_MARKET_API_BASE=http://localhost:8000

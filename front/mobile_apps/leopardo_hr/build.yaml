targets:
  $default:
    builders:
      json_serializable:
        options:
          # Options pour la génération JSON
          explicit_to_json: true
          include_if_null: false
          field_rename: snake